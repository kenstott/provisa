# Provisa Roadmap

> Generated from `docs/arch/requirements.yaml`. Do not hand-edit.

## 2026-Q3

- **REQ-1017** [MUST] [proposed] Self-Service Provisioning — Any authenticated Firebase user with no existing org membership may create an org via a non-superadmin endpoint; the creator is inserted …
- **REQ-1026** [MUST] [proposed] First-Customer Deployment — First-customer deployment target is a single GCP Compute Engine VM (e2-standard-4: 4 vCPU / 16GB, trimmable to e2-standard-2 / 8GB) runni…
- **REQ-1027** [MUST] [proposed] First-Customer Deployment — Object storage externalized to Cloudflare R2 (Trino exchange/spill and any object storage), replacing bundled MinIO for first-customer pr…
- **REQ-1028** [MUST] [proposed] First-Customer Deployment — Trimmed first-customer runtime profile: single-node Trino (coordinator-only, no separate trino-worker; heap reduced to 3-4GB), R2 for exc…
- **REQ-1029** [MUST] [proposed] First-Customer Deployment — Postgres data persists on a separate GCP persistent disk, mounted into the VM, enabling VM resize/reattach without control-plane data loss.
- **REQ-1030** [MUST] [proposed] Email Delivery — Email transport for first customer: AWS SES (or equivalent SMTP) at low volume, matching the SMTP-only EmailProvider (REQ-1020).
- **REQ-1031** [SHOULD] [proposed] First-Customer Deployment — Documented lift-and-shift portability to Vultr (flat pricing + bundled bandwidth) as the exit hatch when GCP steady-state cost is no long…
- **REQ-1032** [MUST] [proposed] Cost Management — No cloud infrastructure is provisioned until a customer is signed; pre-customer sunk cost limited to a domain and a free Firebase project…
- **REQ-1033** [MUST] [proposed] Demo Tiers & Onboarding — Provision two demo tiers on shared first-customer VM: (a) anonymous ephemeral sandbox requiring no signup; (b) persistent quota-limited f…
- **REQ-1034** [MUST] [proposed] Demo Tiers & Onboarding — On ephemeral sandbox session start, clone a pre-seeded golden template schema (org_template, containing REQ-414 demo federated sources an…
- **REQ-1035** [MUST] [proposed] Demo Tiers & Onboarding — Ephemeral sandbox sessions are scoped by Firebase anonymous auth (REQ-121) without email collection. Prospects get a uid-based session id…
- **REQ-1036** [MUST] [proposed] Demo Tiers & Onboarding — Ephemeral sandbox schemas are dropped on explicit logout OR after an idle-TTL of inactivity. Teardown is self-resetting; a reaper process…
- **REQ-1037** [MUST] [proposed] Demo Tiers & Onboarding — Ephemeral sandbox sessions are subject to: (a) session-scoped TTL (e.g., 24h); (b) per-session query row limit and query cost caps; (c) d…
- **REQ-1039** [MUST] [proposed] Demo Tiers & Onboarding — Funnel routing policy: "just looking" traffic (cold-start landing page) is routed to anonymous ephemeral sandbox (self-reaping, zero aban…
- **REQ-1045** [MUST] [proposed] Demo Tiers & Onboarding — Sandbox and free-tier orgs receive the tightest resource-group and query-cost ceilings, enforced via the same tier-cap mechanism (REQ-104…
- **REQ-1051** [SHOULD] [proposed] Documentation & Developer Experience — Hosted documentation surface: product docs and API reference published on Cloudflare Pages (same deployment or docs subsite), sourced fro…
- **REQ-1052** [MUST] [proposed] Onboarding & Entry Points — Website-to-SaaS entry-point wiring: the marketing site's primary CTAs (calls-to-action) map to the specified onboarding paths — "Try it n…
- **REQ-1185** [MUST] [in-progress] Result Handling & Streaming — Engine-agnostic query results implement a ResultStream Protocol (column_names, column_types, batches(), iter_rows(), rows); two implement…
- **REQ-1191** [SHOULD] [proposed] Large Result Redirect & CTAS — HTTP-addressable local materialization. Extend the existing local_dir file:// materialization to an http:// addressable serve endpoint (e…
- **REQ-1192** [SHOULD] [proposed] Large Result Redirect & CTAS — Per-username access scoping on served redirect files. Only the user who owns (created) a materialized redirect file may retrieve it. Auth…
- **REQ-1193** [SHOULD] [proposed] Large Result Redirect & CTAS — Age-based reaping of local redirect files. Locally-materialized files are reaped after they exceed a maximum age (TTL). The S3 path alrea…
- **REQ-1194** [SHOULD] [proposed] Large Result Redirect & CTAS — Extend engine-native ctas_redirect beyond Trino to all object-store-capable engines. Databricks uses INSERT OVERWRITE DIRECTORY / CTAS to…
- **REQ-1195** [SHOULD] [proposed] Large Result Redirect & CTAS — Sink-tier selection rule. ctas_redirect targets the engine's native object-store sink when a reachable object store is configured (PROVIS…
- **REQ-1204** [SHOULD] [proposed] Large Result Redirect & CTAS — Buffered transports (GraphQL, JSON:API, Bolt) surface materialize/CTAS handles via protocol-native side-channels; streaming transports (p…
- **REQ-1211** [MUST] [accepted] Linux AppImage Deployment — Linux AppImage deployments must stage the docker-compose tree out of the AppImage's self-mount into a persistent, writable location (${PR…
- **REQ-1212** [MUST] [accepted] Docker Image Build — The provisa/provisa:local core Docker image must have the built React SPA present in static/ at image-build time; CI core-image jobs must…

## 2026-Q4

- **REQ-1038** [SHOULD] [proposed] Demo Tiers & Onboarding — Free tier orgs follow a reversible archive lifecycle to reclaim disk and Postgres resources without destructive loss: idle orgs are email…
- **REQ-1040** [MUST] [proposed] Trino Resource Management — Per-tenant Trino resource groups enforce isolated CPU/memory ceilings and concurrency limits (maxRunning, maxQueued) per org_id, preventi…
- **REQ-1041** [SHOULD] [proposed] Trino Scaling & HA — Horizontal Trino worker scale-out path: staged from single-node (coordinator executes, worker replicas=0) -> dedicated workers (include-c…
- **REQ-1042** [SHOULD] [proposed] Trino Resource Management — Workload separation: interactive user queries and MV/batch-refresh workloads run under distinct Trino resource groups (and eventually dis…
- **REQ-1043** [SHOULD] [proposed] Multi-Tenancy & Scaling — Tiered cluster routing for physical isolation: sandbox/free tenants route to a shared cluster with tight resource-group caps (best-effort…
- **REQ-1044** [MUST] [proposed] Query Cost Control & Monetization — Hard query-cost caps as a monetization gate: each org tier enforces ceilings on query cost (CPU time, peak memory, bytes scanned, executi…
- **REQ-1046** [MUST] [proposed] Storage Management & Quotas — Per-tier storage quotas: each org tier enforces a hard ceiling on platform-side storage footprint (Postgres schema bytes for landed/mater…
- **REQ-1047** [MUST] [proposed] Storage Management & Quotas — Explicit rejection on storage-cap breach: operations that would push an org past its storage ceiling (MV materialization, landing/ingest,…
- **REQ-1048** [SHOULD] [proposed] Storage Management & Quotas — Bring-your-own storage (BYO): premium/enterprise orgs may point their platform-side object storage (MV persistence, Trino exchange/spill,…
- **REQ-1049** [MUST] [proposed] Storage Management & Quotas — Per-org storage metering/accounting: platform meters storage consumption per org (Postgres schema bytes + attributable R2 object bytes) t…
- **REQ-1053** [MUST] [proposed] Pricing & Tiering — Pricing page reflects tier entitlements: the public pricing table renders the free/standard/premium tier limits directly from the query-c…
- **REQ-1054** [MUST] [proposed] Multi-Tenancy & Routing — Per-org wildcard subdomain routing: each org is addressable at {org}.provisa.io via a single wildcard DNS record (*.provisa.io) and wildc…
- **REQ-1055** [MUST] [proposed] Multi-Tenancy & Routing — Fixed per-protocol ports shared across all orgs: HTTP 443 (GraphQL/REST/UI), pgwire 5439, Bolt 7687, Arrow Flight 8480 are shared across …
- **REQ-1056** [MUST] [proposed] Multi-Tenancy & Routing — Per-surface org resolution: HTTP surfaces resolve org from Host subdomain and/or JWT tenant_id claim (REQ-594); pgwire and Bolt (which la…
- **REQ-1057** [SHOULD] [proposed] Multi-Tenancy & Routing — Cloudflare wildcard routing: HTTP traffic is proxied via Cloudflare (*.provisa.io -> VM:443); raw-TCP wire protocols are routed via Cloud…
- **REQ-1058** [SHOULD] [proposed] Public Data Publishing — Public-publish anonymous access: unauthenticated requests carry no tenant_id claim; the org is resolved from Host subdomain only, and the…
- **REQ-1059** [SHOULD] [proposed] Public Data Publishing — Publishing is a governance grant, not new machinery: each org has a built-in 'public' role starting with ZERO visibility. Publishing an a…
- **REQ-1060** [MUST] [proposed] Public Data Publishing — Hard caps on anonymous public traffic: unauthenticated published endpoints are internet-facing with no login gate, so they inherit the ti…
- **REQ-1061** [SHOULD] [proposed] Multi-Tenancy & Branding — Subdomain-as-dedicated-channel value proposition: every org gets its own {org}.provisa.io endpoint, conveying dedicated tenancy and brand…
- **REQ-1062** [SHOULD] [proposed] Multi-Tenancy & Branding — Custom domain as a premium gate: premium/enterprise tiers may bind a custom domain (e.g. data.acme.com via CNAME) with custom TLS and whi…
- **REQ-1063** [SHOULD] [proposed] Pricing & Tiering — Usage-credit billing meter: metered consumption is billed as universal compute credits whose consumption is uniform across tiers, with th…
- **REQ-1064** [SHOULD] [proposed] Pricing & Tiering — Credit-boxed + time-boxed enterprise trial: a fixed-duration trial (e.g. 30 days) grants a fixed compute-credit balance; on expiry or non…
- **REQ-1065** [MAY] [proposed] Pricing & Tiering — Signup usage credit: a small usage-credit grant on signup (Apollo $50-style) provides a soft on-ramp to exploration without a time-boxed …
- **REQ-1066** [SHOULD] [proposed] Pricing & Tiering — Premium feature gates beyond compute: SSO/SAML + fine-grained access control (ABAC/SCIM, leveraging REQ-122/123/203/204), longer audit/ob…
- **REQ-1067** [SHOULD] [proposed] Pricing & Tiering — Bring-your-own compute / dedicated cluster as premium gate: premium tenants may run on a dedicated Trino cluster (extends tiered routing …
- **REQ-1068** [SHOULD] [proposed] Data Catalog Integration — Pluggable metadata egress provider pattern: an abstract MetadataEgress interface (mirroring AuthProvider/EmailProvider pattern) enables o…
- **REQ-1069** [SHOULD] [proposed] Data Catalog Integration — Standards-first metadata core: the MetadataEgress provider emits OpenLineage for lineage and maps assets to the OpenMetadata ingestion AP…
- **REQ-1070** [SHOULD] [proposed] Data Catalog Integration — Published metadata payload includes datasets/tables/columns, domains, stewards and ownership (REQ-609/020), approved relationships, and d…
- **REQ-1071** [SHOULD] [proposed] Data Catalog Integration — Governance-signal projection: enforcement facts Provisa already computes (which columns/tables are masked, RLS-restricted, or visibility-…
- **REQ-1072** [SHOULD] [proposed] Data Catalog Integration — Sync mechanism: metadata changes push to the external catalog event-driven via the REQ-942 event substrate. A scheduled full reconcile (u…
- **REQ-1073** [MUST] [proposed] Data Catalog Integration — Premium-gated metadata egress: the metadata egress connector is an enterprise/premium tier entitlement, extending REQ-1066. Targeting reg…

## Unscheduled

- **REQ-1196** [SHOULD] Large Result Redirect & CTAS — Design decision — no Provisa-side byte-metering (Arrow buffer counting) and no compile-time cartesian product-budget control. Once materi…
