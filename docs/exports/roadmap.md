# Provisa Roadmap

> Generated from `docs/arch/requirements.yaml`. Do not hand-edit.

## 2026-Q3

- **REQ-673** [SHOULD] [in-progress] Source Connectors — GQL remote sources support an optional per-source config field (e.g. `count_query` in provisa.yaml) that specifies a query or field path …
- **REQ-656** [SHOULD] [in-progress] JSON:API Remote Schema Connector — JSON:API v1.1 remote source registration: GET any resource endpoint with `Content-Type: application/vnd.api+json`, follow `links.describe…
- **REQ-657** [SHOULD] [in-progress] JSON:API Remote Schema Connector — JSON:API relationship expansion via `?include=` parameter integrates with Provisa JOINs — when a JOIN targets a relationship field, the c…
- **REQ-658** [SHOULD] [in-progress] JSON:API Remote Schema Connector — JSON:API sparse fieldsets via `?fields[type]=col1,col2` integrate with Provisa column projection — the compiler injects sparse fieldset p…
- **REQ-659** [SHOULD] [in-progress] JSON:API Remote Schema Connector — JSON:API pagination via `links.next` / `links.prev` integrated with Provisa limit/offset — the compiler tracks pagination links and issue…
- **REQ-660** [SHOULD] [in-progress] JSON:API Remote Schema Connector — JSON:API filtering via `?filter[field]=value` maps to Provisa native filter columns (with `_nf_` prefix, `native_filter_type: "query_para…
- **REQ-661** [MUST] [in-progress] Cypher Mutations — Labels in Cypher write clauses (CREATE, SET, DELETE) must resolve to exactly one NodeMeta via the semantic label map. Ambiguous or unknow…
- **REQ-662** [MUST] [in-progress] Cypher Mutations — Labels in write operations must already exist in the governed semantic label map. New labels cannot be created via Cypher write clauses —…
- **REQ-663** [MUST] [in-progress] Cypher Mutations — All Cypher write operations (CREATE, SET, DELETE) are gated on `writable_by` ACL defined on the target table's NodeMeta. If a role lacks …
- **REQ-664** [MUST] [in-progress] Cypher Mutations — A source connector must explicitly support DML (INSERT, UPDATE, DELETE) to be writable. Read-only connectors (e.g., Trino-federated, iceb…
- **REQ-665** [MUST] [in-progress] Cypher Mutations — Relationships in Cypher cannot be written — they are derived from foreign-key joins in the semantic layer, not stored edges. CREATE, SET,…
- **REQ-666** [SHOULD] [in-progress] Cypher Mutations — `CREATE (n:Label {props})` is translated to `INSERT INTO catalog.schema.table (columns) VALUES (values)`. Property names in the Cypher ob…
- **REQ-667** [SHOULD] [in-progress] Cypher Mutations — `MATCH (n:Label) WHERE ... DELETE n` is translated to `DELETE FROM catalog.schema.table WHERE ...`. The WHERE clause is reused from the e…
- **REQ-668** [SHOULD] [in-progress] Cypher Mutations — `MATCH (n:Label) WHERE ... SET n.prop = val` is translated to `UPDATE catalog.schema.table SET column = value WHERE ...`. Property-to-col…
- **REQ-669** [MUST] [in-progress] Cypher Mutations — Write operations use a separate parser entry point or translation flag to avoid breaking the read-only query path. Existing read-only par…
- **REQ-670** [SHOULD] [in-progress] Cypher Mutations — Cypher write endpoints return the number of rows affected (rows inserted for CREATE, rows updated for SET, rows deleted for DELETE). The …
- **REQ-671** [MUST] [in-progress] Cypher Mutations — DETACH DELETE (cascading delete across derived relationships) is not supported. Attempting DETACH DELETE is a hard error at parse or tran…
- **REQ-672** [SHOULD] [proposed] Graph Model — M:N relationship support via explicit join tables: a RelationshipMapping may declare a join_table (schema + table name, source FK column,…

## 2026-Q4

- **REQ-684** [SHOULD] [proposed] Encryption — An EncryptionService abstraction with pluggable implementations — LocalKeychain (AES-256-GCM, key from OS keychain), AwsKms, AzureKeyVaul…
- **REQ-685** [SHOULD] [proposed] Encryption — Envelope encryption for all encrypted payloads. A unique Data Encryption Key (DEK) is generated per payload, used to encrypt data locally…
- **REQ-686** [SHOULD] [proposed] Encryption — Application-layer column encryption for sensitive PG data. Source credentials, API keys, RLS rules, and other sensitive columns are encry…
- **REQ-687** [SHOULD] [proposed] Encryption — Trino non-SQL cache row payloads encrypted via EncryptionService before create_and_insert() and decrypted after fetch. One DEK per cache …
- **REQ-688** [SHOULD] [proposed] Encryption — Redis hot table payloads (CTE fragment row data) encrypted via EncryptionService at _store_rows() before write and decrypted at get_rows(…
- **REQ-689** [SHOULD] [proposed] Encryption — Audit log query text column encrypted via EncryptionService before write and decrypted on authorised admin reads. Query text may reveal s…
- **REQ-694** [SHOULD] [proposed] Encryption — Client-owned KMS key model. Org master key is a CMK in the client's own cloud account (AWS KMS, Azure Key Vault, or GCP KMS). Client gran…
- **REQ-695** [MUST] [proposed] Multi-Tenancy — All internal PostgreSQL tables (semantic metadata, non-SQL cache, audit log, materialized view definitions) are scoped to a per-org schem…
- **REQ-696** [MUST] [proposed] Multi-Tenancy — Platform tables (tenants, tenant_config) reside in a dedicated platform schema, never touched by org search_path. The platform schema has…
- **REQ-697** [MUST] [proposed] Multi-Tenancy — init_schema() in provisa/core/db.py accepts an org_id parameter. It creates the org_<org_id> schema if not exists, then runs core schema …
- **REQ-698** [MUST] [proposed] Multi-Tenancy — The Trino non-SQL cache schema is org-scoped. cache_location() in provisa/api_source/trino_cache.py accepts org_id and sets cache_schema=…
- **REQ-699** [MUST] [proposed] Multi-Tenancy — Each org has a dedicated PostgreSQL role named role_<org_id> with USAGE and CREATE on org_<org_id> schema only. The asyncpg pool connects…
- **REQ-700** [MUST] [proposed] Multi-Tenancy — Redis cache keys, hot table keys, APQ keys, and query result keys are all prefixed with the org_id (org:<org_id>:...) rather than tenant_…
- **REQ-701** [SHOULD] [proposed] Multi-Tenancy — Org provisioning creates all required infrastructure atomically — org PG schema, PG role, Redis ACL user, and Trino cache schema — in a s…

## 2027-Q1

- **REQ-690** [MAY] [proposed] Encryption — Client-side decryption in the JDBC driver. Provisa backend passes encrypted blobs through without decrypting. Driver decrypts result colu…
- **REQ-691** [MAY] [proposed] Encryption — Client-side decryption in the Python client (DB-API, SQLAlchemy dialect, ADBC). EncryptionService Python implementation shared across dba…
- **REQ-692** [MAY] [proposed] Encryption — Client-side decryption for GraphQL clients via a thin wrapper. Provisa GraphQL schema marks encrypted fields with an @encrypted directive…
- **REQ-693** [MAY] [proposed] Encryption — High-security mode (security.mode=high in provisa.yaml). When enabled — pgwire server is not started; REST and GraphQL data API endpoints…
