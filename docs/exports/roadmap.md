# Provisa Roadmap

> Generated from `docs/arch/requirements.yaml`. Do not hand-edit.

## 2026-09-30

- **REQ-673** [SHOULD] [proposed] Source Connectors — GQL remote sources support an optional per-source config field (e.g. `count_query` in provisa.yaml) that specifies a query or field path …

## 2026-Q3

- **REQ-656** [SHOULD] [in-progress] JSON:API Remote Schema Connector — JSON:API v1.1 remote source registration: GET any resource endpoint with `Content-Type: application/vnd.api+json`, follow `links.describe…
- **REQ-657** [SHOULD] [in-progress] JSON:API Remote Schema Connector — JSON:API relationship expansion via `?include=` parameter integrates with Provisa JOINs — when a JOIN targets a relationship field, the c…
- **REQ-658** [SHOULD] [in-progress] JSON:API Remote Schema Connector — JSON:API sparse fieldsets via `?fields[type]=col1,col2` integrate with Provisa column projection — the compiler injects sparse fieldset p…
- **REQ-659** [SHOULD] [in-progress] JSON:API Remote Schema Connector — JSON:API pagination via `links.next` / `links.prev` integrated with Provisa limit/offset — the compiler tracks pagination links and issue…
- **REQ-660** [SHOULD] [in-progress] JSON:API Remote Schema Connector — JSON:API filtering via `?filter[field]=value` maps to Provisa native filter columns (with `_nf_` prefix, `native_filter_type: "query_para…
- **REQ-661** [MUST] [in-progress] Cypher Mutations — Labels in Cypher write clauses (CREATE, SET, DELETE) must resolve to exactly one NodeMeta via the semantic label map. Ambiguous or unknow…
- **REQ-662** [MUST] [in-progress] Cypher Mutations — Labels in write operations must already exist in the governed semantic label map. New labels cannot be created via Cypher write clauses —…
- **REQ-663** [MUST] [in-progress] Cypher Mutations — All Cypher write operations (CREATE, SET, DELETE) are gated on `writable_by` ACL defined on the target table's NodeMeta. If a role lacks …
- **REQ-664** [MUST] [in-progress] Cypher Mutations — A source connector must explicitly support DML (INSERT, UPDATE, DELETE) to be writable. Read-only connectors (e.g., Trino-federated, iceb…
- **REQ-665** [MUST] [in-progress] Cypher Mutations — Relationships in Cypher cannot be written — they are derived from foreign-key joins in the semantic layer, not stored edges. CREATE, SET,…
- **REQ-666** [SHOULD] [in-progress] Cypher Mutations — `CREATE (n:Label {props})` is translated to `INSERT INTO catalog.schema.table (columns) VALUES (values)`. Property names in the Cypher ob…
- **REQ-667** [SHOULD] [in-progress] Cypher Mutations — `MATCH (n:Label) WHERE ... DELETE n` is translated to `DELETE FROM catalog.schema.table WHERE ...`. The WHERE clause is reused from the e…
- **REQ-668** [SHOULD] [in-progress] Cypher Mutations — `MATCH (n:Label) WHERE ... SET n.prop = val` is translated to `UPDATE catalog.schema.table SET column = value WHERE ...`. Property-to-col…
- **REQ-669** [MUST] [in-progress] Cypher Mutations — Write operations use a separate parser entry point or translation flag to avoid breaking the read-only query path. Existing read-only par…
- **REQ-670** [SHOULD] [in-progress] Cypher Mutations — Cypher write endpoints return the number of rows affected (rows inserted for CREATE, rows updated for SET, rows deleted for DELETE). The …
- **REQ-671** [MUST] [in-progress] Cypher Mutations — DETACH DELETE (cascading delete across derived relationships) is not supported. Attempting DETACH DELETE is a hard error at parse or tran…
- **REQ-672** [SHOULD] [proposed] Graph Model — M:N relationship support via explicit join tables: a RelationshipMapping may declare a join_table (schema + table name, source FK column,…
- **REQ-704** [SHOULD] [proposed] Consumer Discovery — CLI ingestion tool crawls consumer query artifacts (SQL logs, LookML, dbt models, ODBC traces, notebooks) and extracts field references, …
- **REQ-705** [SHOULD] [proposed] Consumer Discovery — RAG-based clustering identifies semantically equivalent columns across sources and proposes canonical names to reduce schema fragmentation.
- **REQ-706** [SHOULD] [proposed] Consumer Discovery — Auto-generation of draft Provisa registration config (tables, columns, relationships, governance_mode=registry-required) scoped to observ…
- **REQ-707** [SHOULD] [proposed] Consumer Discovery — Compilation of draft config surfaces inconsistencies (conflicting join directions, duplicate columns, unresolvable filters) as structured…
- **REQ-708** [SHOULD] [proposed] Consumer Discovery — Gap report identifies consumers referencing fields not resolvable in the generated SDL, flagged with migration diff showing source mappin…
- **REQ-709** [SHOULD] [proposed] Consumer Discovery — The governed semantic layer acts as an interop boundary — consumer queries remain stable across source migrations because they bind to th…
- **REQ-710** [MUST] [proposed] Consumer Discovery — Provisa does not move or copy data. Sources remain authoritative and in place. The semantic layer governs access contracts only; it does …
- **REQ-711** [SHOULD] [proposed] ADBC Client — adbc_connect() must accept an optional port parameter (default 8815) so callers can connect to Arrow Flight servers running on non-defaul…
- **REQ-815** [MUST] [accepted] Docker Compose Organization — Telemetry sink services (otel-collector, prometheus, tempo, grafana, otlp2parquet) are organized into an independently start/stoppable co…
- **REQ-816** [SHOULD] [accepted] Docker Compose Organization — Demo/sample upstream sources (graphql-demo, petstore-mock) are organized into an independently start/stoppable compose project separate f…
- **REQ-818** [MUST] [accepted] Cypher Mutations — Cypher supports WRITES via the /data/cypher endpoint. CREATE, DELETE, and SET statements execute as direct table writes through the same …
- **REQ-840** [MUST] [proposed] Federation Engine Abstraction — The federation engine is a pluggable abstraction with per-engine drivers. Three driver classes exist: broad federators (Trino, reaches ma…
- **REQ-841** [MUST] [proposed] Federation Engine Abstraction — A reachable source (one with a connector for the selected engine) is exposed by that connector's mechanism — attach (reference the source…
- **REQ-842** [MUST] [proposed] Connector Abstraction — A Connector, indexed by (federation_engine, source_type), encapsulates all engine-specific catalog operations — capability(), catalog_add…
- **REQ-843** [MUST] [proposed] Connector Abstraction — The asset registry is the source of truth for asset existence; the engine catalog is derived, rebuildable state. Connectors project regis…
- **REQ-844** [SHOULD] [proposed] Materialization Store — materialization_store — materialized (landed physically by ANY write means: engine-native CTAS/load, SQLAlchemy upsert, or app-side land;…
- **REQ-845** [SHOULD] [proposed] Materialization Store — The set of sources served by reactive replicas is engine-relative — reactive = { source : reach(source) == land } for the configured fede…
- **REQ-846** [MUST] [proposed] Materialization Store — A cache backend is valid iff a connector exists for it in the configured engine AND a write face exists for landing into it. A land-targe…
- **REQ-847** [MUST] [proposed] Materialization Store — Read-through and write-back discipline. On a pull-through read, a pull failure with no fresh cached data is a hard error; serving stale d…
- **REQ-848** [SHOULD] [proposed] Materialization Store — The cache write face is pluggable — {engine-native CTAS/load, SQLAlchemy upsert, app-side land}. It collapses into the federation engine …
- **REQ-855** [SHOULD] [proposed] Materialization Store — materialization_store freshness policy — the ONE centralized freshness gate referenced by REQ-845 (reactive replica pull-through) and app…
- **REQ-856** [MUST] [proposed] Freshness Module — Freshness gate (REQ-855: three modes TTL/probe/TTL+probe, source-agnostic freshness_token dispatch, transports incl. DB-SQL steward query…
- **REQ-857** [MUST] [proposed] Freshness Module — FreshnessSubject protocol exposes: (1) last-refresh timestamp; (2) last-refresh outcome (success/failure); (3) upstream-subject handles f…
- **REQ-858** [MUST] [proposed] Freshness Module — FreshnessPredicate with pluggable strategies that unify REQ-855's three modes plus a boolean content predicate: TTL (time-based, exists t…
- **REQ-859** [MUST] [proposed] Freshness Module — MV, Source (provisa/core/models.py:183), and ad-hoc cache freshness checks (provisa/openapi/pg_cache.py:49) all become FreshnessSubjects …
- **REQ-860** [SHOULD] [proposed] Freshness Gating — Freshness gating is now available at the Source level (provisa/core/models.py), not just on MV. A source can declare a freshness predicat…
- **REQ-861** [MAY] [proposed] Freshness Gating — File-based sources (CSV, Parquet, etc.) may carry an optional producer command (argv) that runs on-stale (gate-triggered) before the file…
- **REQ-862** [MUST] [proposed] Column-Level Trace Instrumentation — MV creation/refresh is instrumented with detailed column-level trace records: for each refresh, the emitted OTel spans capture the resolv…
- **REQ-863** [SHOULD] [proposed] Query Planning Pipeline — Query planning phases are ordered so that source-set-mutating transforms complete before routing. The pipeline is: query language -> sema…
- **REQ-864** [SHOULD] [proposed] Query Result Cache — The query result cache key is computed from a NORMALIZED IR — a canonical, AST-normalized form of the GOVERNANCE-NORMALIZED IR (the post-…
- **REQ-865** [SHOULD] [proposed] Query Routing — The route decision includes CACHED as a first-class route alongside DIRECT (REQ-027) and FEDERATED/Trino (REQ-028): the Route enum gains …
- **REQ-866** [MUST] [proposed] Cache Isolation — HYPERCRITICAL isolation invariant for the query result cache. The cache key MUST fully partition by the RESOLVED governance identity of t…

## 2026-Q4

- **REQ-684** [SHOULD] [proposed] Encryption — An EncryptionService abstraction with pluggable implementations — LocalKeychain (AES-256-GCM, key from OS keychain), AwsKms, AzureKeyVaul…
- **REQ-685** [SHOULD] [proposed] Encryption — Envelope encryption for all encrypted payloads. A unique Data Encryption Key (DEK) is generated per payload, used to encrypt data locally…
- **REQ-686** [SHOULD] [proposed] Encryption — Application-layer column encryption for sensitive PG data. Source credentials, API keys, RLS rules, and other sensitive columns are encry…
- **REQ-687** [SHOULD] [proposed] Encryption — Trino non-SQL cache row payloads encrypted via EncryptionService before create_and_insert() and decrypted after fetch. One DEK per cache …
- **REQ-688** [SHOULD] [proposed] Encryption — Redis hot table payloads (CTE fragment row data) encrypted via EncryptionService at _store_rows() before write and decrypted at get_rows(…
- **REQ-689** [SHOULD] [proposed] Encryption — Audit log query text column encrypted via EncryptionService before write and decrypted on authorised admin reads. Query text may reveal s…
- **REQ-694** [SHOULD] [proposed] Encryption — Client-owned KMS key model. Org master key is a CMK in the client's own cloud account (AWS KMS, Azure Key Vault, or GCP KMS). Client gran…
- **REQ-695** [MUST] [accepted] Multi-Tenancy — All internal PostgreSQL tables (semantic metadata, non-SQL cache, audit log, materialized view definitions) are scoped to a per-org schem…
- **REQ-696** [MUST] [accepted] Multi-Tenancy — Platform tables (tenants, tenant_config) reside in a dedicated platform schema, never touched by org search_path. The platform schema has…
- **REQ-697** [MUST] [accepted] Multi-Tenancy — init_schema() in provisa/core/db.py accepts an org_id parameter. It creates the org_<org_id> schema if not exists, then runs core schema …
- **REQ-698** [MUST] [accepted] Multi-Tenancy — The Trino non-SQL cache schema is org-scoped. cache_location() in provisa/api_source/trino_cache.py accepts org_id and sets cache_schema=…
- **REQ-699** [MUST] [accepted] Multi-Tenancy — Each org has a dedicated PostgreSQL role named role_<org_id> with USAGE and CREATE on org_<org_id> schema only. The asyncpg pool connects…
- **REQ-700** [MUST] [accepted] Multi-Tenancy — Extends REQ-595 for on-prem multi-org deployments. Redis cache keys, hot table keys, APQ keys, and query result keys are all prefixed wit…
- **REQ-701** [SHOULD] [accepted] Multi-Tenancy — Org provisioning creates all required infrastructure atomically — org PG schema, PG role, Redis ACL user, and Trino cache schema — in a s…
- **REQ-702** [MUST] [accepted] Multi-Tenancy — Demo init code and static demo assets are org-scoped. _seed_built_in_sources() passes state.org_id to _seed_meta_domain(); _seed_ops_pg()…
- **REQ-825** [SHOULD] [proposed] Execution & Routing — The Provisa FederationEngine is a pluggable, abstract execution substrate behind a single contract. It is one of four primitives composed…
- **REQ-826** [SHOULD] [proposed] Caching — Federation strategy and freshness management — the datasources → federation engine arrow of REQ-825. The binding between a datasource tab…
- **REQ-827** [MUST] [proposed] Execution & Routing — Governance-parity conformance across federation engines. Any engine admitted to the FederationEngine contract (REQ-825) MUST produce byte…
- **REQ-828** [SHOULD] [proposed] Deployment & Infrastructure — Pluggable admin/metadata store, decoupled from Postgres, selected via a SQLAlchemy-compliant URI. The control-plane store (sources, domai…
- **REQ-829** [SHOULD] [proposed] Deployment & Infrastructure — Pluggable Redis medium with a lightweight, code-identical embedded realization for desktop. Sibling to REQ-828: where REQ-828 makes the S…
- **REQ-830** [SHOULD] [proposed] Deployment & Infrastructure — Stateful-component topology. The four data-flow primitives (datasources -> federation engine -> semantic layer -> execution pipeline; REQ…

## 2027-Q1

- **REQ-690** [MAY] [proposed] Encryption — Client-side decryption in the JDBC driver. Provisa backend passes encrypted blobs through without decrypting. Driver decrypts result colu…
- **REQ-691** [MAY] [proposed] Encryption — Client-side decryption in the Python client (DB-API, SQLAlchemy dialect, ADBC). EncryptionService Python implementation shared across dba…
- **REQ-692** [MAY] [proposed] Encryption — Client-side decryption for GraphQL clients via a thin wrapper. Provisa GraphQL schema marks encrypted fields with an @encrypted directive…
- **REQ-693** [MAY] [proposed] Encryption — High-security mode (security.mode=high in provisa.yaml). When enabled — pgwire server is not started; REST and GraphQL data API endpoints…
