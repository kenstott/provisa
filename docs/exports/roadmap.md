# Provisa Roadmap

> Generated from `docs/arch/requirements.yaml`. Do not hand-edit.

## 2026-Q3

- **REQ-672** [SHOULD] [proposed] Graph Model — M:N relationship support via explicit join tables: a RelationshipMapping may declare a join_table (schema + table name, source FK column,…
- **REQ-860** [SHOULD] [proposed] Freshness Gating — Freshness gating is now available at the Source level (provisa/core/models.py), not just on MV. A source can declare a freshness predicat…
- **REQ-1009** [MUST] [proposed] Component Library Standardization — Standardize provisa-ui on the Mantine component library (MIT/OSS) as the single modern UI component system. Remove unused @mui/material a…
- **REQ-1010** [SHOULD] [proposed] Design System — Establish a consistent visual design theme driven by CSS-variable design tokens, mapped into Mantine's theme. Primary color is indigo (#6…
- **REQ-1011** [SHOULD] [proposed] Theming — Support both Dark and Light color schemes via Mantine's color-scheme system, with a user-facing theme toggle. Current state is dark-only.
- **REQ-1012** [SHOULD] [proposed] Internationalization — Internationalize the entire frontend: no hardcoded user-facing strings (~536 exist today). Use react-i18next with an 'en' base catalog; i…
- **REQ-1013** [MUST] [proposed] Accessibility — Meet enterprise accessibility requirements: WCAG 2.1 Level AA across the app. Includes focus management and focus-trap in dialogs, full k…
- **REQ-1014** [MUST] [proposed] Accessibility — Accessibility is a tested, enforced requirement: add automated axe-core checks (@axe-core/playwright) into the Playwright e2e coverage fi…
- **REQ-1015** [MAY] [proposed] Design System — If/when AG Grid is adopted for data tables, it must be themed coherently with the design tokens (AG Grid v33 Theming API referencing the …
- **REQ-1016** [MUST] [proposed] Testing — Migrate the provisa-ui test suite (27 Playwright e2e specs + 22 unit/component tests) in lockstep with component modernization (REQ-1009/…

## 2026-Q4

- **REQ-1008** [SHOULD] [proposed] MCP Server — Provisa exposes a Model Context Protocol (MCP) server so external AI agents (Claude Desktop, claude.ai connectors, Claude Code) can disco…

## 2027-Q1

- **REQ-690** [MAY] [proposed] Encryption — Client-side decryption in the JDBC driver. Provisa backend passes encrypted blobs through without decrypting. Driver decrypts result colu…
