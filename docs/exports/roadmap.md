# Provisa Roadmap

> Generated from `docs/arch/requirements.yaml`. Do not hand-edit.

## 2026-Q3

- **REQ-1017** [MUST] [proposed] Self-Service Provisioning — Any authenticated Firebase user with no existing org membership may create an org via a non-superadmin endpoint; the creator is inserted …
- **REQ-1026** [MUST] [proposed] First-Customer Deployment — First-customer deployment target is a single GCP Compute Engine VM (e2-standard-4: 4 vCPU / 16GB, trimmable to e2-standard-2 / 8GB) runni…
- **REQ-1027** [MUST] [proposed] First-Customer Deployment — Object storage externalized to Cloudflare R2 (Trino exchange/spill and any object storage), replacing bundled MinIO for first-customer pr…
- **REQ-1028** [MUST] [proposed] First-Customer Deployment — Trimmed first-customer runtime profile: single-node Trino (coordinator-only, no separate trino-worker; heap reduced to 3-4GB), R2 for exc…
- **REQ-1029** [MUST] [proposed] First-Customer Deployment — Postgres data persists on a separate GCP persistent disk, mounted into the VM, enabling VM resize/reattach without control-plane data loss.
- **REQ-1030** [MUST] [proposed] Email Delivery — Email transport for first customer: AWS SES (or equivalent SMTP) at low volume, matching the SMTP-only EmailProvider (REQ-1330).
- **REQ-1031** [SHOULD] [proposed] First-Customer Deployment — Documented lift-and-shift portability to Vultr (flat pricing + bundled bandwidth) as the exit hatch when GCP steady-state cost is no long…
- **REQ-1032** [MUST] [proposed] Cost Management — No cloud infrastructure is provisioned until a customer is signed; pre-customer sunk cost limited to a domain and a free Firebase project…
- **REQ-1033** [MUST] [proposed] Demo Tiers & Onboarding — Provision two demo tiers on shared first-customer VM: (a) anonymous ephemeral sandbox requiring no signup; (b) persistent quota-limited f…
- **REQ-1034** [MUST] [proposed] Demo Tiers & Onboarding — On ephemeral sandbox session start, clone a pre-seeded golden template schema (org_template, containing REQ-414 demo federated sources an…
- **REQ-1035** [MUST] [proposed] Demo Tiers & Onboarding — Ephemeral sandbox sessions are scoped by Firebase anonymous auth (REQ-121) without email collection. Prospects get a uid-based session id…
- **REQ-1036** [MUST] [proposed] Demo Tiers & Onboarding — Ephemeral sandbox schemas are dropped on explicit logout OR after an idle-TTL of inactivity. Teardown is self-resetting; a reaper process…
- **REQ-1037** [MUST] [proposed] Demo Tiers & Onboarding — Ephemeral sandbox sessions are subject to: (a) session-scoped TTL (e.g., 24h); (b) per-session query row limit and query cost caps; (c) d…
- **REQ-1039** [MUST] [proposed] Demo Tiers & Onboarding — Funnel routing policy: "just looking" traffic (cold-start landing page) is routed to anonymous ephemeral sandbox (self-reaping, zero aban…
- **REQ-1045** [MUST] [proposed] Demo Tiers & Onboarding — Sandbox and free-tier orgs receive the tightest resource-group and query-cost ceilings, enforced via the same tier-cap mechanism (REQ-104…
- **REQ-1051** [SHOULD] [proposed] Documentation & Developer Experience — Hosted documentation surface: product docs and API reference published on Cloudflare Pages (same deployment or docs subsite), sourced fro…
- **REQ-1052** [MUST] [proposed] Onboarding & Entry Points — Website-to-SaaS entry-point wiring: the marketing site's primary CTAs (calls-to-action) map to the specified onboarding paths — "Try it n…
- **REQ-1185** [MUST] [in-progress] Result Handling & Streaming — Engine-agnostic query results implement a ResultStream Protocol (column_names, column_types, batches(), iter_rows(), rows); two implement…
- **REQ-1191** [SHOULD] [proposed] Large Result Redirect & CTAS — HTTP-addressable local materialization. Extend the existing local_dir file:// materialization to an http:// addressable serve endpoint (e…
- **REQ-1192** [SHOULD] [proposed] Large Result Redirect & CTAS — Per-username access scoping on served redirect files. Only the user who owns (created) a materialized redirect file may retrieve it. Auth…
- **REQ-1193** [SHOULD] [proposed] Large Result Redirect & CTAS — Age-based reaping of local redirect files. Locally-materialized files are reaped after they exceed a maximum age (TTL). The S3 path alrea…
- **REQ-1194** [SHOULD] [proposed] Large Result Redirect & CTAS — Extend engine-native ctas_redirect beyond Trino to all object-store-capable engines. Databricks uses INSERT OVERWRITE DIRECTORY / CTAS to…
- **REQ-1195** [SHOULD] [proposed] Large Result Redirect & CTAS — Sink-tier selection rule. ctas_redirect targets the engine's native object-store sink when a reachable object store is configured (PROVIS…
- **REQ-1204** [SHOULD] [proposed] Large Result Redirect & CTAS — Buffered transports (GraphQL, JSON:API, Bolt) surface materialize/CTAS handles via protocol-native side-channels; streaming transports (p…
- **REQ-1211** [MUST] [accepted] Linux AppImage Deployment — Linux AppImage deployments must stage the docker-compose tree out of the AppImage's self-mount into a persistent, writable location (${PR…
- **REQ-1212** [MUST] [accepted] Docker Image Build — The provisa/provisa:local core Docker image must have the built React SPA present in static/ at image-build time; CI core-image jobs must…
- **REQ-1225** [MUST] [in-progress] Engine Streaming Terminals — Warehouse federation runtimes (Snowflake, Databricks, BigQuery, MSSQL, ClickHouse) must implement genuinely streaming `run_sync` terminal…
- **REQ-1230** [MUST] [proposed] Clustered Deployment Configuration — Secondary nodes in a cluster deployment run application tier only (Provisa API/UI + in-process protocol listeners on ports 3000, 5439, 76…
- **REQ-1233** [MUST] [accepted] Org Identity & Subdomain Addressing — The subdomain `{org}.provisa.dev` is the org identity. `cloud.provisa.dev` is the org-agnostic entry point (login + org picker). Every da…
- **REQ-1235** [MUST] [accepted] Org Identity & Subdomain Addressing — The subdomain never authorizes — it only names the org. The authenticated credential must authorize that org: an org-scoped credential's …
- **REQ-1236** [MUST] [accepted] Org Identity & Subdomain Addressing — For wire protocols, the credential is org-scoped (per REQ-1232), so SNI is a cross-check: parse org from SNI, reject the connection if it…
- **REQ-1237** [MUST] [accepted] Org Identity & Subdomain Addressing — OIDC/social identity is person-scoped; Provisa mints the org-scoped session. Google/OIDC providers (e.g., provisa/auth/providers/oauth.py…
- **REQ-1238** [MUST] [accepted] Org Identity & Subdomain Addressing — The `x-org-id` header path in provisa/auth/middleware.py:252-253 currently passes the header without checking `user_org_memberships` — a …
- **REQ-1239** [MUST] [accepted] Deployment & Infrastructure — Wildcard TLS for `*.provisa.dev` via ACME DNS-01. Obtain a true wildcard `*.provisa.dev` cert from Let's Encrypt using ACME DNS-01 with a…
- **REQ-1240** [MUST] [accepted] Deployment & Infrastructure — Cloudflare DNS split: Zone `provisa.dev` stays on Cloudflare. `provisa.dev` + `www.provisa.dev` remain Cloudflare-proxied (orange) for th…
- **REQ-1241** [MUST] [accepted] Deployment & Infrastructure — A session is always scoped to exactly one org and therefore one federation engine. `state.federation_engine` is a per-process singleton b…
- **REQ-1242** [SHOULD] [accepted] Deployment & Infrastructure — In-session org switch (e.g., click "switch to acme" in UI) tears down the org-scoped session and mints a new one for the target org (memb…
- **REQ-1243** [MUST] [accepted] Deployment & Infrastructure — Two isolation lanes coexist: (a) Pooled/shared Trino cluster — the free/small tier, "trino-level isolation," all orgs start here. (b) BYO…
- **REQ-1355** [MUST] [accepted] Tenancy Architecture — Org is the canonical tenancy subject in Provisa; "tenant" terminology is retired. The legacy tenants/tenant_config tables are not a separ…

## 2026-Q4

- **REQ-890** [SHOULD] [in-progress] Federation Engine Abstraction — Pgwire auth must be a pluggable provider interface selected at launch (trust | local | oidc), superseding today's fixed MD5/cleartext wir…
- **REQ-1038** [SHOULD] [proposed] Demo Tiers & Onboarding — Free tier orgs follow a reversible archive lifecycle to reclaim disk and Postgres resources without destructive loss: idle orgs are email…
- **REQ-1040** [MUST] [proposed] Trino Resource Management — Per-tenant Trino resource groups enforce isolated CPU/memory ceilings and concurrency limits (maxRunning, maxQueued) per org_id, preventi…
- **REQ-1041** [SHOULD] [proposed] Trino Scaling & HA — Horizontal Trino worker scale-out path: staged from single-node (coordinator executes, worker replicas=0) -> dedicated workers (include-c…
- **REQ-1042** [SHOULD] [proposed] Trino Resource Management — Workload separation: interactive user queries and MV/batch-refresh workloads run under distinct Trino resource groups (and eventually dis…
- **REQ-1043** [SHOULD] [proposed] Multi-Tenancy & Scaling — Tiered cluster routing for physical isolation: sandbox/free tenants route to a shared cluster with tight resource-group caps (best-effort…
- **REQ-1044** [MUST] [proposed] Query Cost Control & Monetization — Hard query-cost caps as a monetization gate: each org tier enforces ceilings on query cost (CPU time, peak memory, bytes scanned, executi…
- **REQ-1053** [MUST] [proposed] Pricing & Tiering — Pricing page reflects tier entitlements: the public pricing table renders the free/standard/premium tier limits directly from the query-c…
- **REQ-1054** [MUST] [proposed] Multi-Tenancy & Routing — Per-org wildcard subdomain routing: each org is addressable at {org}.provisa.io via a single wildcard DNS record (*.provisa.io) and wildc…
- **REQ-1055** [MUST] [proposed] Multi-Tenancy & Routing — Fixed per-protocol ports shared across all orgs: HTTP 443 (GraphQL/REST/UI), pgwire 5439, Bolt 7687, Arrow Flight 8480 are shared across …
- **REQ-1056** [MUST] [proposed] Multi-Tenancy & Routing — Per-surface org resolution: HTTP surfaces resolve org from Host subdomain and/or JWT tenant_id claim (REQ-594); pgwire and Bolt (which la…
- **REQ-1057** [SHOULD] [proposed] Multi-Tenancy & Routing — Cloudflare wildcard routing: HTTP traffic is proxied via Cloudflare (*.provisa.io -> VM:443); raw-TCP wire protocols are routed via Cloud…
- **REQ-1058** [SHOULD] [proposed] Public Data Publishing — Public-publish anonymous access: unauthenticated requests carry no tenant_id claim; the org is resolved from Host subdomain only, and the…
- **REQ-1059** [SHOULD] [proposed] Public Data Publishing — Publishing is a governance grant, not new machinery: each org has a built-in 'public' role starting with ZERO visibility. Publishing an a…
- **REQ-1060** [MUST] [proposed] Public Data Publishing — Hard caps on anonymous public traffic: unauthenticated published endpoints are internet-facing with no login gate, so they inherit the ti…
- **REQ-1061** [SHOULD] [proposed] Multi-Tenancy & Branding — Subdomain-as-dedicated-channel value proposition: every org gets its own {org}.provisa.io endpoint, conveying dedicated tenancy and brand…
- **REQ-1062** [SHOULD] [proposed] Multi-Tenancy & Branding — Custom domain as a premium gate: premium/enterprise tiers may bind a custom domain (e.g. data.acme.com via CNAME) with custom TLS and whi…
- **REQ-1063** [SHOULD] [proposed] Pricing & Tiering — Usage-credit billing meter: metered consumption is billed as universal compute credits whose consumption is uniform across tiers, with th…
- **REQ-1064** [SHOULD] [proposed] Pricing & Tiering — Credit-boxed + time-boxed enterprise trial: a fixed-duration trial (e.g. 30 days) grants a fixed compute-credit balance; on expiry or non…
- **REQ-1065** [MAY] [proposed] Pricing & Tiering — Signup usage credit: a small usage-credit grant on signup (Apollo $50-style) provides a soft on-ramp to exploration without a time-boxed …
- **REQ-1066** [SHOULD] [proposed] Pricing & Tiering — Premium feature gates beyond compute: SSO/SAML + fine-grained access control (ABAC/SCIM, leveraging REQ-122/123/203/204), longer audit/ob…
- **REQ-1067** [SHOULD] [proposed] Pricing & Tiering — Bring-your-own compute / dedicated cluster as premium gate: premium tenants may run on a dedicated Trino cluster (extends tiered routing …
- **REQ-1068** [SHOULD] [accepted] Data Catalog Integration — Pluggable metadata export provider pattern: an abstract MetadataExport interface (mirroring AuthProvider/EmailProvider pattern) enables o…
- **REQ-1069** [SHOULD] [accepted] Data Catalog Integration — Standards-first metadata core: the MetadataExport provider emits OpenLineage for lineage and maps assets to the OpenMetadata ingestion AP…
- **REQ-1070** [SHOULD] [accepted] Data Catalog Integration — Published metadata payload includes datasets/tables/columns, domains, stewards and ownership (REQ-609/020), approved relationships, and d…
- **REQ-1071** [SHOULD] [accepted] Data Catalog Integration — Governance-signal projection: enforcement facts Provisa already computes (which columns/tables are masked, RLS-restricted, or visibility-…
- **REQ-1072** [SHOULD] [accepted] Data Catalog Integration — Sync mechanism: metadata changes push to the external catalog event-driven via the REQ-942 event substrate. A scheduled full reconcile (u…
- **REQ-1073** [MUST] [accepted] Data Catalog Integration — Premium-gated metadata export: the metadata export connector is an enterprise/premium tier entitlement, extending REQ-1066. Targeting reg…
- **REQ-1074** [MUST] [accepted] Admin Configuration — Metadata export admin surface: an Admin tab that configures the per-org export target (provider, endpoint, credentials, reconcile schedul…
- **REQ-1368** [SHOULD] [accepted] Data Catalog Integration — Metadata export user documentation: a published docs page (docs/metadata-export.md, navigated under Security & Governance) covering the s…
- **REQ-1226** [MUST] [proposed] HTTPS/TLS Configuration — GCP/Docker cluster deployment must serve all endpoints over HTTPS with TLS encryption. If the operator does not supply TLS certificates, …
- **REQ-1244** [SHOULD] [proposed] Deployment & Infrastructure — Per-org engine routing is deferred. In-session multi-engine switching requires replacing the per-process singleton (app.py:270 / _query_h…
- **REQ-1245** [MUST] [proposed] Deployment & Infrastructure — Control-plane persistence is required for multi-instance fleet deployments. The control_plane store is currently in-memory (provisa/contr…
- **REQ-1246** [MUST] [proposed] Authentication — First verified Firebase login provisions a user_profile record, decoupled from the local_users/bcrypt-password path. No password is creat…
- **REQ-1247** [MUST] [proposed] Authentication — Self-service signup landing on cloud.provisa.dev presents two options to a not-logged-in user: LOG IN or CREATE ACCOUNT. The choice route…
- **REQ-1248** [MUST] [proposed] Org Membership — During account creation, a Firebase-authenticated user auto-joins an org if they hold a valid org_invites token (via invite URL or code).…
- **REQ-1249** [SHOULD] [proposed] Org Membership — During account creation, a Firebase user without an org invite may CREATE A NEW ORG (self-service): inserts an orgs row with the user as …
- **REQ-1250** [SHOULD] [proposed] Org Membership — Org membership is pull-based via org_join_requests, the inverse of org_invites. New registry table org_join_requests(user_id, org_id, sta…
- **REQ-1251** [SHOULD] [proposed] Org Membership — Admin approval/denial of org_join_requests. When an admin approves a request, the approval path reuses the membership-insert logic (provi…
- **REQ-1252** [SHOULD] [proposed] Org Administration — Admin UI surface (extend OrgsTab.tsx / add admin router endpoint) to list pending org_join_requests for the org, approve requests, and de…
- **REQ-1253** [MUST] [proposed] Cloud Load Balancing — All protocol surfaces (API port 8000, UI port 3000, Arrow Flight 8815, pgwire 5439, Bolt 7687, MCP 8009, gRPC 50051) must be fronted by a…
- **REQ-1255** [SHOULD] [proposed] Org Membership — During self-service org creation (REQ-1249), a Firebase user is offered an opt-in checkbox or toggle to seed the newly created org with t…
- **REQ-1256** [MUST] [proposed] Kubernetes Deployment — Helm chart must ship a served UI surface: a provisa-ui Deployment + Service exposing the SPA on port 3000 internally, reverse-proxying th…
- **REQ-1257** [MUST] [proposed] Kubernetes Deployment — Helm chart must support operator TLS on every endpoint (REQ-1226 parity): PROVISA_TLS_CERT and PROVISA_TLS_KEY env vars, a TLS cert Secre…
- **REQ-1258** [MUST] [proposed] Kubernetes Deployment — Helm provisa Service must expose all protocol endpoints served by the API container: pgwire (5439), Bolt (7687), MCP (8009), gRPC (50051)…
- **REQ-1259** [MUST] [proposed] Kubernetes Deployment — Helm chart cert provisioning must be a values.yaml toggle: supply an existing TLS Secret name, or specify cert-manager issuer configurati…
- **REQ-1261** [MUST] [proposed] Multi-Tenant Onboarding — Self-service multi-tenant onboarding with four coupled workstreams: (1) sign-up → create-org → become-org-admin workflow where any authen…
- **REQ-1262** [MUST] [proposed] Multi-Method Authentication — Login page surfaces multiple simultaneous authentication providers (Google, GitHub, email/password), advertised by the server as a list. …
- **REQ-1264** [MUST] [accepted] Platform Admin Break-Glass Access — Platform super-admin is a seeded local break-glass account (username + bcrypt password via simple auth provider), created at install with…
- **REQ-1265** [MUST] [proposed] Helm Auth Configuration — Helm chart deployment MUST NOT require Firebase. Operator specifies auth provider at install time via values.yaml (OIDC issuer URL, SAML …
- **REQ-1266** [SHOULD] [proposed] Cloud Appliance Bootstrap — Firebase single-admin bootstrap is an optional convenience for cloud-appliance deployments (GCP cloud-init / Terraform). First Google-sig…
- **REQ-1267** [MUST] [proposed] Runtime Auth-Enforcement Gate — A single built image/wheel serves unsecured (demo/none), basic, or firebase/IdP deployments and can switch enforcement at runtime WITHOUT…
- **REQ-1277** [SHOULD] [proposed] Member-less User Onboarding — A newly authenticated user belonging to zero orgs is offered three onboarding paths: (1) join an existing org by entering its org id, (2)…
- **REQ-1285** [SHOULD] [proposed] Organization Access Control — Organizations may enable an "auto-join" flag with a configured default role (e.g., a low-privilege read-only role like "analyst"). When a…
- **REQ-1329** [MUST] [proposed] Source Identification — Replace the sentinel source ID entirely: source_id becomes nullable, and registered relations gain an explicit definition kind (base | de…
- **REQ-1364** [SHOULD] [proposed] Config Export — Config exports carry JSON Pointer refs for internal cross-references plus composed physical resourcePath URIs for data elements, enabling…
- **REQ-1366** [SHOULD] [proposed] Federation Engine Abstraction — Spike: generalize time-travel (as_of) from a hardcoded Iceberg/Delta source-type special case to a first-class connector capability, enab…
- **REQ-1367** [MUST] [accepted] Platform Thesis & Operational Proof — Provisa's thesis: with sufficient rigor, every aspect of a data environment EXCEPT the systems of origin can be described declaratively, …
- **REQ-1376** [MUST] [accepted] Data Governance — Tag assignment is human-only, never automatic. UI may suggest likely-technical columns, but suggestion requires explicit steward acceptan…
- **REQ-1381** [MUST] [accepted] Data Governance — User attributes for ABAC are a SEPARATE concept from the object-tag registry (REQ-1373): object tags are exported governance metadata on …
- **REQ-1382** [MUST] [accepted] Data Governance — User attribute sources: the IdP is the system of record for attributes it provides (OIDC claims — groups, department, region); locally-ad…
- **REQ-1383** [MUST] [accepted] Data Governance — Both IdP-sourced and local attributes resolve into the same `user.*` namespace consumed by RLS predicates; predicates are source-agnostic…
- **REQ-1384** [MUST] [accepted] Data Governance — Tag-scoped RLS rules (follow-on to REQ-1373..1379 and existing table/domain-scoped RLS REQ-040/041/402): an RLS rule may attach to a tag;…
- **REQ-1385** [MUST] [in-progress] Data Catalog Integration — Semantic URIs (provisa:// scheme) on metadata-exported assets, addressed by business identity, self-describing to the point practical: ki…
- **REQ-1389** [MUST] [in-progress] Data Catalog Integration — Catalog upsert preserves human enrichment: an attribute-ownership contract for every exporter. Publishing is an UPSERT on the vendor's st…
- **REQ-1413** [SHOULD] [proposed] Tag Registry — Two semantic-content tags join the code-defined system tag set (REQ-1375, SYSTEM_TAGS in provisa/core/models.py) rather than being seeded…
- **REQ-1414** [SHOULD] [proposed] Tag Registry — Tag assignments are readable as a queryable relation, not only through the admin API: a `column_tags` view yields one row per (object_typ…
- **REQ-1415** [MAY] [proposed] Tag Registry — Tag IDs are emitted into the bracketed comment suffix alongside the modeling role, so a catalog surface shows `[dimension, entity, natura…
- **REQ-1447** [MUST] [proposed] Federation Engine — SaaS runs ONE engine architecture on one GKE cluster. Shared (REQ-1243) and isolated (REQ-1043, REQ-1412) are the same Trino deployment f…
- **REQ-1448** [MUST] [proposed] Federation Engine — An isolated engine idles to zero and wakes on traffic, so the org pays for compute it uses rather than 730 node-hours a month. Today neit…
- **REQ-1449** [SHOULD] [proposed] SaaS Billing — Pro is sold as three fixed sizes -- S (4 vCPU / 32GB), M (8/64), L (16/128) -- expressed as the node pool machine type and the pod reques…
- **REQ-1450** [MUST] [proposed] Federation Engine — The shared engine (REQ-1243) is one multi-tenant Trino deployment on the GKE cluster (REQ-1447), serving every Starter org from a single …
- **REQ-1451** [MUST] [proposed] Deployment Topology — The GKE cluster (REQ-1447) hosts Trino and nothing else. The control plane -- the Provisa API, UI, and background workers -- stays on its…
- **REQ-1452** [MUST] [proposed] Pricing & Tiering — The published price list is set by the comparables, not derived from cost. Starter bills $1.30 per ACTIVE HOUR with a $25/mo minimum and …
- **REQ-1453** [MUST] [proposed] Pricing & Tiering — Every tier must clear roughly 75% MARGINAL gross margin -- revenue minus the infrastructure cost that customer alone causes -- at the pri…
- **REQ-1455** [MUST] [in-progress] Pricing & Tiering — Starter opens with a free evaluation period bounded on BOTH axes: 14 days, 40 active hours, and 25 GB of egress, whichever is reached fir…
- **REQ-1456** [MUST] [proposed] Pricing & Tiering — Launch ships the SHARED lane and Starter alone. Pro and Enterprise are designed, priced and published, but presented as forthcoming: the …
- **REQ-1457** [MUST] [proposed] Deployment Topology — Every engine Provisa runs is the SAME container image. A shared shard's coordinator and workers (REQ-1450), a Pro org's dedicated coordin…
- **REQ-1458** [MUST] [proposed] Pricing & Tiering — Every tier has a CEILING at which the price list stops and a contract begins. Starter's ceiling is 400 active hours a month, one shared s…
- **REQ-1459** [MUST] [proposed] Deployment Topology — HIGH AVAILABILITY on this platform means a PUBLISHED RECOVERY TIME MEASURED IN MINUTES, and it is BASELINE for every tier rather than an …
- **REQ-1460** [SHOULD] [proposed] Deployment Topology — A regional standby of the SHARED LANE is ASYMMETRIC: the control plane and its database are warm in a second region, and the engine is no…
- **REQ-1461** [MUST] [proposed] Federation Engine — The cold-shard wake is reconciled with the executor's retry budget by ORDERING, not by widening the budget. A cold start is roughly 90-12…
- **REQ-1471** [MUST] [proposed] Federation Engine — A shard that has idled to zero begins its cold start at SIGN-IN, not at the first query. The node provision REQ-1448 accepts is roughly 9…
- **REQ-1472** [MUST] [proposed] Authentication — The break-glass superuser of REQ-125 can sign in from a browser under any auth provider. The credential is a username and password, but a…
- **REQ-1487** [SHOULD] [proposed] Environments — An organization runs its governed model in more than one environment -- a dev environment, a test environment, a staging environment, wha…
- **REQ-1488** [SHOULD] [proposed] Environments — An environment is physically a schema of its own. An org's governed model already lives in one Postgres schema reached by SET search_path…
- **REQ-1489** [MUST] [proposed] Environments — Copying an environment carries the governed model and nothing else, and what it carries is stated as an allow-list rather than an exclusi…
- **REQ-1490** [MUST] [proposed] Environments — Copying into an environment that already exists is a merge, not a replacement. The carried classes of REQ-1489 are merged by identity -- …
- **REQ-1491** [MUST] [proposed] Environments — Where an environment points is a per-environment fact that no copy ever supplies. A source row travels between environments because regis…
- **REQ-1492** [MUST] [proposed] Environments — Write authority is granted into an environment and never carried between environments. Provisa executes mutations against sources -- comp…
- **REQ-1493** [SHOULD] [proposed] Environments — Every environment owns a writable store, because a non-prod environment needs data that is neither a read of production nor absent. A reg…
- **REQ-1494** [SHOULD] [proposed] Environments — Masking gains a deterministic form so a masked extract stays usable. Column masking today is redaction -- regex, constant or truncate -- …
- **REQ-1495** [MAY] [proposed] Environments — An environment populates a table from its own model when no source holds one. A structure being designed exists in the environment's mode…
- **REQ-1496** [SHOULD] [proposed] Environments — An environment's governed model is versioned as immutable builds, which is what makes promotion and reversal the same operation with a di…
- **REQ-1498** [MUST] [proposed] Environments — Every environment operation is available from the provisa command line, because an environment change is rarely the only thing happening:…
- **REQ-1499** [MAY] [proposed] Environments — An organization may replicate an environment whole -- bindings, secrets, users and role assignments included -- when what it wants is the…
- **REQ-1500** [MAY] [proposed] Environments — A non-prod environment can be exported as a configuration a desktop Provisa loads, so a developer works against the environment's model o…
- **REQ-1501** [MUST] [proposed] Environments — Environments take their shape from two exemplars, and which one governs is decided per half of the design rather than argued case by case…
- **REQ-1502** [SHOULD] [proposed] Environments — An environment's governed model can be exported to files, held in version control and applied back, which is what makes an environment re…
- **REQ-1503** [MUST] [proposed] Environments — A credential addresses exactly one environment. API keys, service tokens and any other non-interactive credential are issued into an envi…
- **REQ-1504** [SHOULD] [proposed] Environments — An environment can be protected, and a merge into a protected environment waits for an approval by someone other than the person who requ…
- **REQ-1505** [SHOULD] [proposed] Environments — Every environment keeps a change history, and any two environments can be compared for one object. The history records what changed in th…
- **REQ-1506** [SHOULD] [proposed] Environments — A merge can be scoped to part of a model. Promoting one corrected metric should not require promoting every other change the source envir…
- **REQ-1507** [SHOULD] [proposed] Environments — Environments are gated by an entitlement feature, not by a product name. The gate is a Feature in provisa/control_plane/entitlements.py w…
- **REQ-1508** [MUST] [proposed] Environments — One word, one meaning, and the two words stay apart. ENVIRONMENT names what an organization works in -- prod, dev, test, whatever it crea…
- **REQ-1523** [MUST] [proposed] Environments — An environment name is bounded, and so is the number of them. REQ-1488 makes an environment a schema of its own, so a free-form name is a…
- **REQ-1524** [SHOULD] [in-progress] Environments — Every environment has a repository, and the repository is a PROJECTION of the environment, never an authority over it. The governed model…
- **REQ-1525** [MUST] [in-progress] Environments — The exclusion of bindings and secrets from what an environment carries (REQ-1489) is an exclusion of COLUMNS, and it cannot reach inside …
- **REQ-1526** [MUST] [proposed] Environments — The serialized model is a DIRECTORY OF FILES, one file per entity, and never a single document. A single file makes every concurrent chan…
- **REQ-1527** [SHOULD] [in-progress] Environments — Provisa does not run CI; it integrates with the CI the organization already has, and the integration point is the remote of REQ-1524 rath…
- **REQ-1528** [SHOULD] [proposed] Environments — Authority over a model is scoped to an ENVIRONMENT, and the ordinary way to acquire it is to create one. A member who holds no model-edit…
- **REQ-1529** [SHOULD] [proposed] Environments — An organization's environments come in TWO KINDS, and the difference is who made them. A BASE environment -- dev, staging, prod, whatever…
- **REQ-1530** [SHOULD] [proposed] Environments — A DEVELOPER IS ALWAYS LIMITED BY DOMAIN MEMBERSHIP. The role says what kind of act a member may perform -- build a view, define a relatio…
- **REQ-1531** [SHOULD] [proposed] Environments — EVERY MUTATION THAT NAMES A DOMAIN MUST BE GATED ON IT, AND EVERY MUTATION THAT OUGHT TO NAME ONE MUST NAME ONE. REQ-1530 makes a role's …
- **REQ-1532** [MUST] [proposed] Environments — THE BROWSER MUST BE ABLE TO SAY WHICH ENVIRONMENT IT IS READING, AND EVERY REQUEST IT MAKES MUST SAY THE SAME ONE. REQ-1487 makes the env…
- **REQ-1533** [MUST] [proposed] Environments — THE ORG'S ENVIRONMENTS MUST BE ADMINISTRABLE FROM THE ADMIN UI, AND ITS REPOSITORY MUST BE READABLE THERE. Everything REQ-1487..REQ-1531 …
- **REQ-1534** [MUST] [proposed] Environments — AN ENVIRONMENT BEYOND prod MUST BE REFUSED ON A CONTROL PLANE THAT HAS NO SCHEMAS, AND NO SCHEMA DDL MAY BE EMITTED THERE. REQ-1488 makes…
- **REQ-1535** [MUST] [proposed] Control Plane — EMBEDDED POSTGRESQL MUST BE THE DEFAULT CONTROL-PLANE STORE IN EVERY SELF-CONTAINED TIER (demo and native), REPLACING SQLITE. REQ-1488 ma…
- **REQ-1536** [MUST] [proposed] Control Plane — EVERY CONTROL-PLANE STORE THE CAPABILITY CATALOG OFFERS MUST BE OPENABLE, AND THE ENVIRONMENT SUPPORT OF EACH MUST BE ESTABLISHED BY PROB…
- **REQ-1537** [SHOULD] [proposed] Control Plane — CONFIGURING A REPOSITORY REMOTE MUST PROBE IT, AND OFFER TO CREATE IT WHEN IT IS NOT THERE -- NEVER CREATE IT SILENTLY. REQ-1527 pushes e…
- **REQ-1538** [MUST] [proposed] Environments — THE CHOICE REQ-1529 CALLS BASE-OR-BRANCH MUST BE PRESENTED AS "INHERIT CONNECTIONS", AND MUST DEFAULT TO OFF. The governing use case is m…
- **REQ-1539** [SHOULD] [proposed] Environments — What a person may do to DATA inside an environment is what their ROLES say, and an environment never overrides it. An environment is a na…
- **REQ-1540** [SHOULD] [proposed] Admin UI — A single-tenant deployment shows no organization in the navbar. The org switcher is a multi-tenancy control: it names the active org so a…
- **REQ-1541** [MUST] [proposed] Environments — The mirror runs BOTH WAYS. REQ-1527 pushes an environment's projection to the org's own git host; this brings back what that host holds, …
- **REQ-1542** [SHOULD] [proposed] Environments — A merge may END the environment it came from. A feature environment exists to carry one piece of work, and when that work has landed in i…
- **REQ-1543** [SHOULD] [proposed] Environments — Because every model change commits (REQ-1524), an environment can be MOVED ALONG ITS OWN HISTORY rather than edited back by hand. Undo de…
- **REQ-1544** [SHOULD] [proposed] Environments — A deploy refreshes ONLY WHAT IT CHANGED. Each environment already has its own runtime, keyed ``{org}_env_{name}``, so serving a different…
- **REQ-1545** [SHOULD] [proposed] Environments — A MERGE LANDS AS ONE COMMIT on the target's branch -- the source's line squashed FROM ITS CURRENT SHA BACK to where the two branches part…
- **REQ-1546** [SHOULD] [proposed] Environments — A BRANCH SAYS WHETHER IT HOLDS WORK THE REMOTE DOES NOT, and an operator can send it or end it. The mirror after every commit is best eff…
- **REQ-1547** [SHOULD] [proposed] Environments — UPDATING LOCAL FROM THE REMOTE IS AN APPLY, not a ref write. A fetch brings the remote's branches in as tracking refs and moves nothing (…
- **REQ-1548** [SHOULD] [proposed] Environments — THERE IS ONE VERB AND IT IS "APPLY A SHA". A control plane instance holds one model at a time per environment; everything a person can do…
- **REQ-1549** [SHOULD] [proposed] Environments — A MERGE GOES BACK WHERE THE BRANCH CAME FROM, squashed from where the source now IS. The environment a branch was created from is recorde…
- **REQ-1550** [SHOULD] [proposed] Environments — A MERGE IS CONFIRMED IN A DIALOG THAT REQUIRES A COMMENT. The comment is not bookkeeping: the merge lands as one squashed commit (REQ-154…
- **REQ-1551** [SHOULD] [proposed] Environments — WHEN THE TARGET BRANCH IS GOVERNED BY PULL REQUESTS, REVIEW IS ASKED FOR ON THE HOST. An approval row inside Provisa (REQ-1504) decides n…
- **REQ-1552** [SHOULD] [proposed] Environments — THE STATE OF THE BRANCH BEING WORKED IN IS SHOWN WHERE THE WORK HAPPENS, NOT ONLY ON THE ADMIN PAGE. Somebody editing the model is not on…
- **REQ-1553** [SHOULD] [proposed] Environments — NEITHER END OF THE HISTORY IS OFFERED INTO A REFUSAL. Undo and redo are refused at the ends of the line -- at the first commit there is n…
- **REQ-1556** [MUST] [proposed] Environments — A PULL SAYS WHAT IT OVERWROTE, AND A REFUSED PULL SAYS WHAT COLLIDED. Taking what the remote holds is the same act as a merge with the sa…
- **REQ-1555** [MUST] [proposed] Environments — A MERGE THAT OVERWRITES SOMEBODY ELSE'S WORK SAYS SO. Provisa merges by identity: every object the source has, the target gets. That is r…
- **REQ-1554** [MUST] [proposed] Environments — AN ENVIRONMENT IS STATE; A BRANCH IS THAT STATE'S HISTORY. The two are paired one to one and are still not the same object, and every sur…
- **REQ-1557** [MUST] [proposed] Security — A SECRETS SERVICE, WITH PROVISA'S OWN STORE AS THE ONE THAT IS ALWAYS THERE. ${provider:reference} (REQ-125, REQ-557) already names a pro…
- **REQ-1558** [MUST] [proposed] Security — ADMIN / SECRETS: NAMES GO IN, VALUES NEVER COME BACK OUT. An org admin creates a secret by giving it a name and a value, and from then on…
- **REQ-1559** [SHOULD] [proposed] UI — ADMIN NAVIGATES DOWN A LEFT-HAND RAIL, NOT ACROSS A TOP BAR. Every other section of the product carries a handful of surfaces and reads c…
- **REQ-1560** [MUST] [proposed] Security — A SECRET BELONGS TO SOMEONE, AND WHO IT BELONGS TO IS PART OF ITS ADDRESS. A developer needs credentials of their own -- their git token,…
- **REQ-1561** [MUST] [proposed] Environments — A NODE WHOSE PROJECTION DOES NOT HOLD THE COMMIT STILL LISTS ITS ENVIRONMENTS. The repository is a projection and never an authority (REQ…
- **REQ-1562** [MUST] [proposed] Deployment — A HOSTED DEPLOY ASSERTS ITS COMMERCE CONFIGURATION BEFORE IT SHIPS ANYTHING. The plan catalog is the first thing the signup page asks for…
- **REQ-1563** [MUST] [proposed] Deployment — A HOSTED DEPLOY ALSO ASSERTS THAT THE COMMERCIAL SEAM ACTUALLY LOADED. The settings REQ-1562 preflights are only half of what signup need…
- **REQ-1564** [MUST] [proposed] Deployment — THE BILLING KEY IS ASSERTED FOR THE STORE THE DEPLOYMENT ACTUALLY TRANSACTS AGAINST, AND A BLANK KEY IS A MISSING KEY. LEMONSQUEEZY_MODE …
- **REQ-1565** [MUST] [proposed] Pricing & Tiering — THE TRIAL RESET THE REFUSAL PROMISES IS AN ENDPOINT, NOT A HAND-WRITTEN UPDATE. REQ-1474 refuses a second free trial and tells the person…
- **REQ-1566** [MUST] [proposed] Pricing & Tiering — STARTER IS ORDERABLE WITHOUT A TRIAL. Signup routes Starter through the trial checkout because Starter is the plan the trial converts to,…

## Unscheduled

- **REQ-1196** [SHOULD] Large Result Redirect & CTAS — Design decision — no Provisa-side byte-metering (Arrow buffer counting) and no compile-time cartesian product-budget control. Once materi…
- **REQ-1280** [MAY] Commercial Positioning — Provisa SaaS bills on two metered SKUs mapped to the existing gcp-saas topology (REQ-1279): (1) Serving lane — the "active-hour" SKU at $…
- **REQ-1296** [MUST] Authentication — A claimed platform-admin slot lands the administrator in a populated deployment, not an empty one. The bootstrap org id is 'root' and its…
- **REQ-1297** [MUST] Authorization — Every org schema is seeded with exactly five default roles: platform_admin, org_admin, analyst, developer, modeler. These five ids are th…
- **REQ-1298** [MUST] Authorization — A backup platform_admin is made in two steps, never by a second bootstrap claim. An existing platform_admin invites the person into the r…
- **REQ-1299** [MUST] Authorization — The deployment can never be left with zero platform_admins. Revoking the platform_admin role from the last holder, and deleting or disabl…
- **REQ-1300** [MUST] Authorization — An organization can be deleted. Deletion drops the org's tenant schema, its membership and invite rows in the control plane, and its entr…
- **REQ-1301** [SHOULD] Authorization — The root org is a working org, not a placeholder. The platform_admin registers and governs whatever data assets they find useful there, a…
- **REQ-1302** [MUST] Authorization — No organization can be left with zero org_admins by an ordinary administrative act. Revoking org_admin from the last holder in an org is …
- **REQ-1303** [MUST] Authorization — When an org's administrators are unreachable, recovery runs through the platform_admin via an explicit, audited operation — not through a…
- **REQ-1304** [SHOULD] Authorization — Pressing delete on an organization downloads that org's configuration before the deletion is confirmed. The download is the live config Y…
- **REQ-1305** [MUST] Authorization — Removing a person from an org is an org_admin operation and it removes them from both planes. Today DELETE /admin/orgs/{org_id}/members/{…
- **REQ-1306** [MUST] Authorization — A user may leave an org on their own. Leaving performs the same two-plane removal as an org_admin offboarding them (REQ-1305) and additio…
- **REQ-1307** [MUST] Authorization — A user may delete their own Provisa account. Deletion leaves every org they belong to (REQ-1306), revokes every personal access token the…
- **REQ-1308** [MUST] Authorization — A user can never change their own role in an org. Changing a member's role is an org_admin operation, available only to the org_admin of …
- **REQ-1309** [MUST] Authorization — An org id is validated on creation and immutable thereafter. It must match a conservative identifier pattern (lowercase letters and digit…
- **REQ-1310** [MUST] Authorization — An invitation addressed to an email address is delivered to that address. Today create_invite stores the invitee email but nothing sends …
- **REQ-1311** [SHOULD] Authorization — A single user may create at most 100 organizations. The cap is a backstop against runaway or automated creation, not a product tier - eve…
- **REQ-1312** [MUST] Authorization — Deletion scrubs personal data while preserving the record that the deletion happened. Deleting an org (REQ-1300) drops every tenant-scope…
- **REQ-1313** [MUST] Authorization — An invitation may only confer a role the inviter is entitled to confer, and the role must exist in the target org. Today CreateInviteBody…
- **REQ-1314** [MUST] Authorization — An invitation that names no role confers analyst, the least-privileged of the four default roles (REQ-1297). CreateInviteBody.role_id cur…
- **REQ-1315** [SHOULD] Authorization — An org whose provisioning failed is recoverable, not abandoned. Background provisioning records provisioning_state=failed with the error,…
- **REQ-1363** [MUST] Domain Hierarchy — Add hierarchical domain structure via parent_domain_id to Domain model, with flattening applied at execution time to governance, addressi…
- **REQ-1365** [SHOULD] URI Dereferencing API — Governed dereference API for semantic data-location URIs: REST and MCP tool `resolve(uri, as_of?)` that takes a Provisa semantic-address …
- **REQ-1386** [SHOULD] Operational Views — Management/operations reporting is delivered as defined views in the built-in ops domain, extending the REQ-884 registry mechanism (_OPS_…
