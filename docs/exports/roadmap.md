# Provisa Roadmap

> Generated from `docs/arch/requirements.yaml`. Do not hand-edit.

## 2026-Q3

- **REQ-672** [SHOULD] [proposed] Graph Model — M:N relationship support via explicit join tables: a RelationshipMapping may declare a join_table (schema + table name, source FK column,…
- **REQ-860** [SHOULD] [proposed] Freshness Gating — Freshness gating is now available at the Source level (provisa/core/models.py), not just on MV. A source can declare a freshness predicat…
- **REQ-861** [MAY] [proposed] Freshness Gating — File-based sources (CSV, Parquet, etc.) may carry an optional producer command (argv) that runs on-stale (gate-triggered) before the file…
- **REQ-863** [SHOULD] [proposed] Query Planning Pipeline — Query planning phases are ordered so that source-set-mutating transforms complete before routing. The pipeline is: query language -> sema…
- **REQ-887** [SHOULD] [proposed] RDBMS Stored Procedure Auto-Discovery — Extend database source introspection to auto-discover stored procedures and routines (via information_schema.routines, pg_proc, or vendor…
- **REQ-947** [MUST] [proposed] Connector Abstraction — engine.connectors (the unified connector registry per federation engine) must include a connector for EVERY source type the engine can of…
- **REQ-948** [SHOULD] [proposed] Driver Management — Driver availability is per-connector data, not new machinery: Connector.runtime_deps entries are tagged by who provides the driver — "sys…
- **REQ-951** [MUST] [proposed] Connector Abstraction — Connector declares a CAPABILITY SET of reach modes (mechanisms: frozenset[Mechanism]) instead of a single fixed mechanism. Extend Mechani…

## 2026-Q4

- **REQ-694** [SHOULD] [proposed] Encryption — Client-owned KMS key model. Org master key is a CMK in the client's own cloud account (AWS KMS, Azure Key Vault, or GCP KMS). Client gran…
- **REQ-828** [SHOULD] [proposed] Deployment & Infrastructure — Pluggable admin/metadata store, decoupled from Postgres, selected via a SQLAlchemy-compliant URI. The control-plane store (sources, domai…
- **REQ-830** [SHOULD] [proposed] Deployment & Infrastructure — Stateful-component topology. The four data-flow primitives (datasources -> federation engine -> semantic layer -> execution pipeline; REQ…
- **REQ-876** [SHOULD] [proposed] Deployment & Infrastructure — Compose host-port parameterization for stack coexistence. Every published host port in docker-compose.core.yml (and the dev/observability…
- **REQ-877** [MAY] [proposed] Row-Level Delta Capture — Opt-in, per-table row-level delta capture for materialized views — the row-level companion to the column-level lineage trace (REQ-862). E…
- **REQ-878** [MAY] [proposed] Point-In-Time Reconstruction — Point-in-time MV reconstruction from the delta ledger (REQ-877). An append-only delta ledger is a temporal substrate: folding change even…
- **REQ-879** [MUST] [proposed] Materialization Store — Cross-instance MV refresh coordination for a load-balanced fleet sharing one materialization store. Today the live refresh state is per-i…
- **REQ-880** [MUST] [proposed] Materialization Store — The materialized-view store (MV cache target for materialized relationships and views) must be configurable as a SQLAlchemy URI instead o…
- **REQ-884** [MUST] [proposed] Observability — All Provisa internal logs—starting with the query audit log (REQ-074) and extending to any other operational/observability logs—must be m…
- **REQ-885** [SHOULD] [proposed] Extensible Functions — Generalize tracked functions (REQ-205–208: source-resident stored procedures exposed as GraphQL mutations) to support multiple implementa…
- **REQ-886** [MUST] [proposed] Observability — Engine-emitted I/O tracing for UDF/transformer invocations must be mandatory and non-bypassable, recording UDF name, transport type (scri…
- **REQ-889** [MUST] [proposed] Deployment Tier Architecture — The metadata/config/roles store MUST be the embedded single source of truth in every deployment tier. The embedded tier (SQLite or DuckDB…
- **REQ-890** [SHOULD] [proposed] Federation Engine Abstraction — Pgwire auth must be a pluggable provider interface selected at launch (trust | local | oidc), superseding today's fixed MD5/cleartext wir…
- **REQ-891** [SHOULD] [proposed] Federation Engine Abstraction — Pgwire per-role authorization maps an authenticated account to Calcite/source schema+table visibility, enforced in BOTH discovery and exe…
- **REQ-892** [MAY] [proposed] Connector Abstraction — Pgwire may present popular PostgreSQL extension surfaces backed by the federation engine, as a pluggable opt-in-per-deployment mechanism:…
- **REQ-894** [SHOULD] [proposed] Deployment Tier Architecture — DuckDB federation engine is a DESKTOP/EDGE/DEV tier embedded federator, not a production multi-user backend. DuckDB is single-process wit…
- **REQ-895** [MAY] [proposed] Federation Engine Abstraction — RDBMS-as-single-node-federation-engine pattern generalizes across DB vendors via native foreign-data mechanisms (SQL/MED or equivalent). …
- **REQ-896** [MUST] [proposed] Federation Architecture — Provisa's defensible value is the GOVERNED-FEDERATION MODEL and consistent governance philosophy, not any compute engine or infrastructur…
- **REQ-897** [SHOULD] [proposed] Federation Architecture — Federation engines are declared-capability objects; capabilities are planner inputs. Each FederationEngine carries a set of DECLARED capa…
- **REQ-898** [SHOULD] [proposed] PostgreSQL Deployment — Curated, prebuilt catalog of PostgreSQL extensions (sqlite_fdw, parquet_fdw/parquet_s3_fdw,  pg_lake, pg_duckdb, pg_analytics) indexed by…
- **REQ-899** [MAY] [proposed] Federation Engine Abstraction — Catalog of DuckDB extensions eligible to back a federation-source connector, each providing either a relation-producing table function us…
- **REQ-900** [MUST] [proposed] PostgreSQL Deployment — Postgres federation engine's curated v1 connector set: prebuilt FDW/extension bindings for csv, sqlite, sqlserver, oracle, duckdb, and pa…
- **REQ-902** [MUST] [in-progress] Query Compilation — pg_duckdb's transparent execution path cannot emit PostgreSQL 16 nested-JSON syntax (JSON_OBJECT with colon separators); nested-relations…
- **REQ-913** [MUST] [proposed] AST-Based Transforms — Every query transform — governance (RLS, masking, visibility, row-cap), correlated- subquery rewriting, and dialect lowering — MUST opera…
- **REQ-949** [SHOULD] [proposed] Connector Abstraction — Structural debt: provisa/federation/connector.py and provisa/federation/engine.py are monoliths that do not scale as more federation engi…

## 2027-Q1

- **REQ-690** [MAY] [proposed] Encryption — Client-side decryption in the JDBC driver. Provisa backend passes encrypted blobs through without decrypting. Driver decrypts result colu…
- **REQ-691** [MAY] [proposed] Encryption — Client-side decryption in the Python client (DB-API, SQLAlchemy dialect, ADBC). EncryptionService Python implementation shared across dba…
- **REQ-692** [MAY] [proposed] Encryption — Client-side decryption for GraphQL clients via a thin wrapper. Provisa GraphQL schema marks encrypted fields with an @encrypted directive…
- **REQ-693** [MAY] [proposed] Encryption — High-security mode (security.mode=high in provisa.yaml). When enabled — pgwire server is not started; REST and GraphQL data API endpoints…
