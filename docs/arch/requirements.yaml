- id: REQ-001
  status: complete
  group: 1. Access Governance & Security
  category: Query Governance
  priority: MUST
  type: behavioral
  since: 2026-03
  description: Any authenticated identity can query using any supported language (GraphQL, SQL, pgwire,
    Arrow Flight). Data returned is governed solely by table/column visibility, RLS, and masking — there
    is no capability gate on querying itself. Relationship join enforcement applies unless the role holds
    the ignore_relationships capability. There is no query registry and no pre-approval concept.
  use_case: Removing the query-execution capability gate means governance is expressed entirely through
    data-layer controls (visibility, RLS, masking), not through access lists.
  code:
  - provisa/security/
  - provisa/compiler/
  tests:
  - tests/integration/test_governance_integration.py
  - tests/unit/test_governance.py
  - tests/unit/test_registry_removed.py
  - provisa-ui/e2e/governance-core.spec.ts
  scenario: 'Given an authenticated identity with role "analyst"

    When a GraphQL query is submitted against a registered table

    Then data is returned filtered by RLS and masking rules only

    And no capability gate rejects the query

    '
  integration_test: required
  integration_test_reason: Behavior verifiable only end-to-end across all transports (GraphQL, SQL, pgwire,
    Arrow Flight) with real governance applied.
  e2e: true
  e2e_reason: Verifying that data is returned filtered by RLS and masking across all transports (GraphQL,
    SQL, pgwire, Arrow Flight) requires a real running server with actual governance applied end-to-end.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - app-developer
  - compliance
  - data-engineer
  competitive_position:
    status: ahead
    rationale: Provisa expresses governance entirely through data-layer controls (visibility, RLS, masking)
      across all query protocols (GraphQL, SQL, pgwire, Arrow Flight) with no capability gate on querying
      itself. Competitors like Hasura and Denodo still use protocol-specific access control lists or query
      whitelists that must be managed per-endpoint.
- id: REQ-002
  status: complete
  superseded_by: REQ-266
  group: 1. Access Governance & Security
  category: Query Governance
  priority: MUST
  type: constraint
  description: Rights and Stage 2 governance enforcement is platform-level — applied to every query at
    compile time. No client path can bypass it without bypassing the server (REQ-266).
  use_case: Platform-level rights enforcement prevents any client path from bypassing governance controls.
  code:
  - provisa/security/
  - provisa/compiler/
  tests:
  - tests/unit/test_governance.py
  - tests/integration/test_governance_integration.py
  since: 2026-01
  scenario: 'Given a query submitted via any client path

    When the query reaches the server

    Then Stage 2 governance is applied at compile time and cannot be bypassed

    '
  integration_test: required
  integration_test_reason: Platform-level enforcement across compile-time and execution requires real
    query pipeline verification.
  stakeholders:
  - compliance
  - ops
  competitive_position:
    status: ahead
    rationale: Platform-level compile-time enforcement across every transport (GraphQL, SQL, pgwire, Arrow
      Flight, gRPC) means no client path can bypass governance without bypassing the server. Trino and
      Hasura enforce governance at query execution time per connector, leaving compile-time bypass vectors
      open in some edge cases.
- id: REQ-003
  status: complete
  superseded_by: REQ-001
  group: 1. Access Governance & Security
  category: Query Governance
  priority: MUST
  type: behavioral
  description: All queries and mutations are governed by user rights alone — table/view rights plus relationship
    rights. No registry membership or query approval is required for any operation.
  use_case: Uniform rights-based governance lets users run any query their rights permit without approval
    overhead.
  code:
  - provisa/core/
  tests:
  - tests/unit/test_governance.py
  - tests/integration/test_governance_integration.py
  since: 2026-01
  scenario: 'Given a user with table/view rights

    When the user submits a query or mutation

    Then it is executed based solely on their rights without requiring registry membership or approval

    '
  integration_test: required
  integration_test_reason: Uniform rights-based governance verified only end-to-end with real role assignments
    and query execution.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - compliance
  - data-engineer
  competitive_position:
    status: parity
    rationale: Rights-based governance without query approval registries is standard in Denodo and Dremio.
      Trino also uses role-based access without requiring query pre-approval, making this table-stakes
      for serious federation platforms.
- id: REQ-004
  status: complete
  group: 1. Access Governance & Security
  category: Query Governance
  priority: MUST
  type: constraint
  description: Test endpoint accepts arbitrary queries against registered schema with full guards; MUST
    NOT be exposed in production.
  use_case: Isolated test endpoint lets developers validate queries safely without risking production
    exposure.
  code:
  - provisa/core/
  tests:
  - tests/unit/test_governance.py
  - tests/unit/test_test_endpoint_guard.py
  since: 2026-01
  scenario: 'Given a test endpoint accepting arbitrary queries

    When the system is deployed to production

    Then the test endpoint is not exposed

    '
  integration_test: not_required
  integration_test_reason: Test endpoint configuration and documentation can be verified with unit tests.
  stakeholders:
  - app-developer
  - ops
  competitive_position:
    status: neutral
    rationale: ''
- id: REQ-005
  status: complete
  group: 1. Access Governance & Security
  category: Query Governance
  priority: MUST
  type: behavioral
  description: Result-size ceilings are defined per role/table in config (`max_rows`); Stage 2 injects
    LIMIT when a query would exceed the role's ceiling for any referenced table. Clients may always narrow
    further (fewer columns, additional filters).
  use_case: Per-role/table row ceilings bound result size without an approved-query registry.
  code:
  - provisa/core/
  tests:
  - tests/unit/test_governance.py
  - tests/integration/test_governance_integration.py
  since: 2026-01
  scenario: 'Given a role with a configured max_rows ceiling for a table

    When a query references that table and would exceed the ceiling

    Then Stage 2 injects a LIMIT capping results at the role''s ceiling

    '
  integration_test: required
  integration_test_reason: Per-role row ceiling injection requires real compilation and execution with
    varied roles.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - compliance
  - data-engineer
  competitive_position:
    status: ahead
    rationale: Per-role, per-table row ceiling with automatic LIMIT injection at compile time (Stage 2
      AST rewrite) is more granular than Trino's per-query memory limits or Snowflake's resource monitors,
      which operate at session or warehouse level rather than per-role per-table.
- id: REQ-006
  status: complete
  group: 1. Access Governance & Security
  category: Query Governance
  priority: MAY
  type: behavioral
  description: Large-result redirect and Arrow output are available to any query the user's rights permit,
    subject to configured thresholds (REQ-029, REQ-137).
  use_case: Large-result and Arrow output available to any rights-permitted query, governed by configured
    thresholds.
  code:
  - provisa/core/
  tests:
  - tests/e2e/test_large_result.py
  - tests/unit/test_governance.py
  - tests/unit/test_redirect_s3.py
  since: 2026-01
  scenario: 'Given a user with rights to query a table

    When the query result exceeds the configured large-result threshold

    Then large-result redirect and Arrow output are available

    '
  integration_test: required
  integration_test_reason: Large-result redirect and Arrow output behavior verifiable only via HTTP/Arrow
    Flight transport with real thresholds.
  e2e: true
  e2e_reason: Large-result redirect and Arrow output require a real HTTP or Arrow Flight transport with
    a running server to verify the automatic redirect behavior when result size exceeds the configured
    threshold.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - app-developer
  - data-engineer
  competitive_position:
    status: ahead
    rationale: 'The key differentiator is automatic, transparent redirection: when a result exceeds the
      threshold, Provisa silently returns an S3 pointer instead of streaming the payload — protecting
      upstream infrastructure (API gateway, application memory) from being clogged with large results
      without any client-side opt-in. Dremio (Arrow Flight native) and Snowflake (Arrow result format)
      support large-result formats but require client awareness or explicit configuration. Provisa''s
      automatic redirect is a governance-enforced backstop, not a client feature.'
- id: REQ-038
  status: complete
  group: 1. Access Governance & Security
  category: Security
  priority: MUST
  type: structural
  description: 'Two independent enforcement layers: schema visibility and SQL enforcement (Stage 2). Both
    derive from the user''s table/view and relationship rights.'
  use_case: Independent visibility and SQL-enforcement layers ensure governance cannot be bypassed by
    compromising one layer.
  code:
  - provisa/security/
  tests:
  - tests/e2e/test_security.py
  - tests/integration/test_governance_integration.py
  - tests/unit/test_masking.py
  - tests/unit/test_rls.py
  - tests/unit/test_visibility.py
  since: 2026-01
  integration_test: required
  integration_test_reason: Independent layers (schema visibility and SQL enforcement) verified end-to-end
    across different query paths.
  stakeholders:
  - compliance
  - data-engineer
  competitive_position:
    status: ahead
    rationale: Two independent enforcement layers (schema visibility + SQL-level AST rewrite) applied
      on every request provides defense-in-depth. Hasura applies schema visibility but its SQL enforcement
      layer is connector-dependent; Trino relies on a single connector-level authorization plugin rather
      than two independent layers.
- id: REQ-039
  status: complete
  group: 1. Access Governance & Security
  category: Security
  priority: MUST
  type: behavioral
  description: 'Schema visibility layer: unauthorized tables/columns do not appear in SDL or query builder;
    compiler rejects at parse time.'
  use_case: Schema visibility enforcement ensures unauthorized tables and columns are invisible to clients
    at design time.
  code:
  - provisa/security/
  tests:
  - tests/e2e/test_security.py
  - tests/integration/test_governance_integration.py
  - tests/unit/test_masking.py
  - tests/unit/test_rls.py
  - tests/unit/test_visibility.py
  - provisa-ui/e2e/governance-core.spec.ts
  since: 2026-01
  scenario: 'Given a user without rights to a table or column

    When the user accesses the SDL or query builder

    Then unauthorized tables and columns do not appear and are rejected at parse time

    '
  integration_test: required
  integration_test_reason: Schema visibility enforcement requires real SDL generation and query builder
    interaction verification.
  e2e: true
  e2e_reason: Verifying that unauthorized tables and columns are absent from the SDL requires a real running
    server serving the GraphQL schema over the network.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - data-engineer
  - app-developer
  competitive_position:
    status: parity
    rationale: Schema-level visibility enforcement (unauthorized tables/columns absent from SDL) is a
      standard feature in Hasura, Denodo, and Dremio. All serious federation platforms hide unauthorized
      objects at design time.
- id: REQ-040
  status: complete
  group: 1. Access Governance & Security
  category: Security
  priority: MUST
  type: behavioral
  description: 'SQL enforcement layer: executor injects RLS WHERE clauses and strips unauthorized columns
    before execution, every request.'
  use_case: SQL-level RLS and column stripping ensure governance is enforced at execution time, not just
    at schema time.
  code:
  - provisa/security/
  tests:
  - tests/e2e/test_security.py
  - tests/integration/test_governance_integration.py
  - tests/integration/test_rls_execution.py
  - tests/unit/test_inherited_roles.py
  - tests/unit/test_layer_contracts.py
  - tests/unit/test_masking.py
  - tests/unit/test_rls.py
  - tests/unit/test_sampling_rls.py
  - tests/unit/test_visibility.py
  since: 2026-01
  scenario: 'Given a query submitted by a user with restricted rights

    When the executor processes the query

    Then RLS WHERE clauses are injected and unauthorized columns are stripped before execution

    '
  integration_test: required
  integration_test_reason: RLS WHERE injection and column stripping verified only at execution time with
    real data source.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - compliance
  - data-engineer
  competitive_position:
    status: ahead
    rationale: Executor-level RLS WHERE clause injection and column stripping via AST rewrite on every
      request — without relying on the underlying data source to implement RLS — is a differentiator over
      Trino and Dremio, which delegate row filtering to connector-level policies that vary per source.
- id: REQ-041
  status: complete
  group: 1. Access Governance & Security
  category: Security
  priority: MUST
  type: structural
  description: RLS rules defined at table registration as PG-style SQL filter expressions mapped to user
    roles.
  use_case: Declarative RLS rules let stewards define row-level access without writing database triggers
    or views.
  code:
  - provisa/security/
  tests:
  - tests/e2e/test_security.py
  - tests/integration/test_governance_integration.py
  - tests/integration/test_rls_execution.py
  - tests/unit/test_layer_contracts.py
  - tests/unit/test_masking.py
  - tests/unit/test_rls.py
  - tests/unit/test_visibility.py
  since: 2026-01
  integration_test: required
  integration_test_reason: RLS rule evaluation requires real table registration and role-based filtering
    at execution.
  stakeholders:
  - compliance
  - data-engineer
  competitive_position:
    status: ahead
    rationale: Declarative PG-style SQL filter expressions as RLS rules, defined at table registration
      time, work uniformly across all federated sources (PostgreSQL, MySQL, REST, S3, etc.). Competitors
      like Trino require source-specific row filter plugins; Denodo uses source-native policies that don't
      unify across heterogeneous sources.
- id: REQ-042
  status: complete
  superseded_by: REQ-746
  group: 1. Access Governance & Security
  category: Security
  priority: MUST
  type: structural
  description: Source registration, table registration, relationship definition, security configuration,
    query development, query authorization, and ignore-relationships rights are distinct and independently
    configured.
  use_case: Fine-grained, independently assignable rights prevent privilege creep across registration
    and execution roles.
  code:
  - provisa/security/
  tests:
  - tests/e2e/test_security.py
  - tests/integration/test_governance_integration.py
  - tests/unit/test_governance.py
  - tests/unit/test_masking.py
  - tests/unit/test_rls.py
  - tests/unit/test_visibility.py
  since: 2026-01
  integration_test: not_required
  integration_test_reason: Fine-grained rights model is a configuration schema that can be verified via
    unit tests.
  stakeholders:
  - compliance
  - data-engineer
  competitive_position:
    status: ahead
    rationale: Seven independently assignable rights (source registration, table registration, relationship
      definition, security configuration, query development, query authorization, ignore-relationships)
      are more granular than Trino's coarse role model or Hasura's admin/role dichotomy, reducing privilege
      creep in multi-team environments.
- id: REQ-203
  status: complete
  group: 1. Access Governance & Security
  category: ABAC Approval Hook
  priority: MAY
  type: behavioral
  description: 'Pluggable operation approval hook for enterprises with complex ABAC that can''t be expressed
    as static RLS rules. Evaluated at query time. Request: user_id, roles, session_vars, tables, columns,
    operation. Response: approved/denied + optional additional filter. Position: after RLS injection,
    before execution.'
  use_case: ABAC approval hook lets enterprises enforce attribute-based policies too complex for static
    RLS rules.
  code:
  - provisa/security/
  tests:
  - tests/integration/test_abac_hook_endpoint.py
  - tests/integration/test_governance_integration.py
  - tests/unit/test_abac_hook.py
  - tests/unit/test_abac_hook_extended.py
  - tests/unit/test_approval_hook.py
  since: 2026-01
  scenario: 'Given a table with an approval hook configured

    When a query references that table

    Then the approval hook is called after RLS injection and before execution with the query context

    '
  integration_test: required
  integration_test_reason: Approval hook evaluation at query time requires real HTTP/gRPC/socket calls
    with actual approval decision feedback.
  e2e: true
  e2e_reason: The approval hook is invoked over HTTP/gRPC/Unix socket at query time; verifying the full
    call lifecycle (post-RLS injection, pre-execution) requires a real running server and hook endpoint.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - compliance
  - ops
  - app-developer
  competitive_position:
    status: ahead
    rationale: A pluggable ABAC approval hook evaluated at query time — receiving user_id, roles, session_vars,
      tables, columns, and operation — is not offered by Trino, Hasura, or Dremio out of the box. Denodo
      has an external security plugin but it is not as composable with the RLS pipeline (post-injection,
      pre-execution positioning).
- id: REQ-204
  status: complete
  group: 1. Access Governance & Security
  category: ABAC Approval Hook
  priority: MAY
  type: behavioral
  description: 'Approval hook scoping — per-table (`approval_hook: true`), per-source (`approval_hook:
    true` on source config), or global (`auth.approval_hook`). Compiler checks at query time: if no table
    in the query has approval_hook enabled (directly, via source, or global), skip the call entirely.
    Zero overhead for unscoped tables.'
  use_case: Scoped hook evaluation skips the approval call entirely for unscoped tables, keeping overhead
    at zero.
  code:
  - provisa/security/
  tests:
  - tests/integration/test_governance_integration.py
  - tests/unit/test_abac_hook.py
  - tests/unit/test_abac_hook_extended.py
  - tests/unit/test_approval_hook.py
  since: 2026-01
  scenario: 'Given a query referencing only tables without approval_hook enabled

    When the compiler evaluates the query

    Then the approval hook call is skipped entirely with zero overhead

    '
  integration_test: required
  integration_test_reason: Hook scoping logic requires end-to-end verification that hooks are called or
    skipped correctly per configuration.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - compliance
  - ops
  competitive_position:
    status: ahead
    rationale: Compile-time hook scoping that skips the approval call entirely for unscoped tables (zero
      overhead) is a performance design competitors do not replicate. Hasura's event/action hooks fire
      per-operation without selective skip logic based on which tables are referenced.
- id: REQ-246
  status: complete
  group: 1. Access Governance & Security
  category: ABAC Approval Hook
  priority: MAY
  type: structural
  description: 'Approval hook protocols — three transport options, configured via `auth.approval_hook.type`
    (Part of Phase AE): - `webhook` (default): HTTP POST to URL. ~5-50ms. Simplest, works with any external
    ABAC service. - `grpc`: gRPC with persistent connection + multiplexing. ~1-5ms. Binary protocol for
    high-volume same-datacenter deployments. Proto service definition shipped with Provisa. - `unix_socket`:
    Unix domain socket for same-machine sidecars (e.g., OPA). <0.5ms. Path configured via `auth.approval_hook.socket_path`.'
  use_case: Three approval hook transports (HTTP/gRPC/Unix socket) let enterprises match latency requirements
    to ABAC infra.
  code:
  - provisa/security/
  tests:
  - tests/integration/test_governance_integration.py
  - tests/unit/test_abac_hook.py
  - tests/unit/test_abac_hook_extended.py
  - tests/unit/test_approval_hook.py
  since: 2026-01
  integration_test: required
  integration_test_reason: Three transport protocols (HTTP/gRPC/Unix socket) require real network or IPC
    verification with latency validation.
  e2e: true
  e2e_reason: Three approval hook transport protocols (HTTP webhook, gRPC, Unix socket) each cross a real
    network or IPC boundary and can only be verified end-to- end with a live server and hook endpoint.
  stakeholders:
  - ops
  - app-developer
  competitive_position:
    status: ahead
    rationale: Three approval hook transports (HTTP webhook ~50ms, gRPC ~5ms, Unix domain socket <0.5ms)
      let enterprises choose latency tier to match co-location of the ABAC sidecar. No comparable multi-transport
      ABAC hook is offered by Trino, Hasura, Dremio, or Denodo.
- id: REQ-247
  status: complete
  group: 1. Access Governance & Security
  category: ABAC Approval Hook
  priority: MAY
  type: structural
  description: 'Approval hook config (Part of Phase AE): auth.approval_hook.type: webhook | grpc | unix_socket;
    url for webhook/grpc; socket_path for unix_socket; timeout_ms: 500; fallback: deny | allow on timeout;
    scope: all or omit for per-table/per-source scoping. Per-table: tables[].approval_hook: true. Per-source:
    sources[].approval_hook: true. Global: auth.approval_hook.scope: all.'
  use_case: Granular approval hook scoping (global/source/table) avoids adding governance overhead to
    unscoped tables.
  code:
  - provisa/security/
  tests:
  - tests/integration/test_governance_integration.py
  - tests/unit/test_abac_hook.py
  - tests/unit/test_abac_hook_extended.py
  - tests/unit/test_approval_hook.py
  since: 2026-01
  integration_test: required
  integration_test_reason: Approval hook configuration scoping requires end-to-end verification across
    global/source/table levels.
  stakeholders:
  - compliance
  - data-engineer
  - ops
  competitive_position:
    status: ahead
    rationale: Pluggable approval hooks (webhook/gRPC/unix socket) with per-table/per-source scoping is
      more granular than Hasura's action/permission model and not available in Trino or Dremio.
- id: REQ-262
  status: complete
  group: 1. Access Governance & Security
  category: Two-Stage Compiler (Governed SQL)
  priority: MUST
  type: structural
  description: 'Compiler refactored into two explicit stages. Stage 1: GraphQL → plain PG-style SQL (semantic
    compilation only — resolves aliases, domains, relationships, emits physical column names and table
    names with AS aliases). Stage 2: plain SQL → governed SQL (AST rewrite applying RLS, masking, visibility,
    and the row cap). Stage 2 is a standalone transformer that accepts SQL + role and returns governed
    SQL, with no knowledge of GraphQL or the semantic model.'
  use_case: Two-stage compiler separates semantic translation from governance so Stage 2 can be reused
    by any SQL client.
  code:
  - provisa/compiler/stage2.py
  - provisa/compiler/sql_gen.py
  tests:
  - tests/integration/test_governance_integration.py
  - tests/unit/test_graphql_alias.py
  - tests/unit/test_stage2.py
  - tests/unit/test_two_stage_compiler.py
  since: 2026-01
  integration_test: required
  integration_test_reason: Two-stage compiler design verified end-to-end with Stage 2 reused by SQL clients
    to confirm semantic + governance separation.
  stakeholders:
  - app-developer
  - data-engineer
  competitive_position:
    status: ahead
    rationale: A two-stage compiler where Stage 2 is a standalone SQL governance transformer (accepts
      SQL + role, returns governed SQL) is architecturally unique among open-source federation engines.
      Trino's authorizer and Hasura's permission system are tightly coupled to their query planners and
      cannot be reused as standalone transformers by external SQL clients.
- id: REQ-263
  status: complete
  group: 1. Access Governance & Security
  category: Two-Stage Compiler (Governed SQL)
  priority: MUST
  type: behavioral
  description: 'Stage 2 governance transformer applies four governance concerns: (1) RLS — inject WHERE
    predicate per table reference per role (SQLGlot AST rewrite); (2) column masking — wrap masked column
    expressions with masking function (AST rewrite); (3) column visibility — remove or NULL-out columns
    invisible to the role (AST rewrite); (4) row cap — inject/cap LIMIT to the most restrictive per-role/table
    `max_rows` ceiling for any referenced table. A role without the FULL_RESULTS capability receives the
    configured default cap. The row cap is the single mechanism for bounding result size; statistical
    sampling is a user query feature, not a governance concern (REQ-478).'
  use_case: Stage 2 governance transformer enforces RLS, masking, visibility, and the row cap uniformly
    on every SQL path and transport.
  code:
  - provisa/compiler/stage2.py
  - provisa/compiler/sql_gen.py
  tests:
  - tests/integration/test_governance_integration.py
  - tests/unit/test_mask_inject.py
  - tests/unit/test_stage2.py
  - tests/unit/test_stage2_complex.py
  - tests/unit/test_two_stage_compiler.py
  since: 2026-01
  scenario: 'Given a SQL query submitted by a role with governance rules configured

    When Stage 2 processes the query

    Then RLS predicates, column masking, column visibility, and row cap are all applied via AST rewrite

    '
  integration_test: required
  integration_test_reason: Stage 2 governance transformer (RLS, masking, visibility, row cap) requires
    real data execution to verify all four concerns applied.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - compliance
  - data-engineer
  competitive_position:
    status: ahead
    rationale: AST-level rewrite enforcing all four governance concerns (RLS, column masking, column visibility,
      row cap) in a single Stage 2 pass, uniformly across every SQL-speaking transport, is more complete
      than Hasura (no column masking) or Trino (no built-in column masking; RLS is connector-specific).
- id: REQ-264
  status: complete
  group: 1. Access Governance & Security
  category: Two-Stage Compiler (Governed SQL)
  priority: MUST
  type: behavioral
  description: 'Stage 2 must handle all SQL structural patterns: subqueries, CTEs, JOINs, `SELECT *` (expand
    via schema introspection then apply visibility), UNION, and nested expressions. RLS and masking must
    be injected at every table reference in the full AST, not only the outermost SELECT.'
  use_case: Stage 2 handling of CTEs and subqueries ensures governance is applied at every table reference,
    not just outermost.
  code:
  - provisa/compiler/stage2.py
  - provisa/compiler/sql_gen.py
  tests:
  - tests/e2e/test_sql_endpoint.py
  - tests/integration/test_governance_integration.py
  - tests/unit/test_mask_inject.py
  - tests/unit/test_sql_endpoint_governance.py
  - tests/unit/test_stage2.py
  - tests/unit/test_two_stage_compiler.py
  since: 2026-01
  scenario: 'Given a SQL query with subqueries, CTEs, JOINs, SELECT *, UNION, or nested expressions

    When Stage 2 processes the query

    Then RLS and masking are injected at every table reference in the full AST

    '
  integration_test: required
  integration_test_reason: Stage 2 handling of CTEs, subqueries, and UNION requires real SQL execution
    to verify governance at all table references.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - data-engineer
  - app-developer
  competitive_position:
    status: ahead
    rationale: RLS and masking injected at every table reference across subqueries, CTEs, JOINs, UNION,
      and SELECT * (with schema introspection expansion) prevents governance bypass via SQL structural
      tricks. Trino's row filters and Hasura's permissions do not guarantee injection into every AST node
      of complex nested queries.
- id: REQ-265
  status: complete
  group: 1. Access Governance & Security
  category: Two-Stage Compiler (Governed SQL)
  priority: MUST
  type: constraint
  description: Stage 2 operates on physical column names (left side of AS alias pairs). Stage 1 guarantees
    all aliases are explicit in emitted SQL, so Stage 2 never needs to resolve semantic model names.
  use_case: Stage 2 operating on physical column names makes it independent of the semantic model and
    alias resolution.
  code:
  - provisa/compiler/stage2.py
  - provisa/compiler/sql_gen.py
  tests:
  - tests/unit/test_stage2.py
  - tests/unit/test_two_stage_compiler.py
  - tests/integration/test_governance_integration.py
  since: 2026-01
  scenario: 'Given SQL emitted by Stage 1 with explicit AS aliases

    When Stage 2 processes the SQL

    Then Stage 2 operates on physical column names without resolving semantic model names

    '
  integration_test: not_required
  integration_test_reason: Stage 2 operating on physical column names is a compiler invariant verifiable
    via unit tests.
  stakeholders:
  - app-developer
  competitive_position:
    status: ahead
    rationale: Two-stage compiler architecture that enforces governance on physical column names after
      alias resolution is a unique design not found in Hasura, Denodo, or Trino.
- id: REQ-266
  status: complete
  group: 1. Access Governance & Security
  category: Two-Stage Compiler (Governed SQL)
  priority: MUST
  type: constraint
  description: With Stage 2 as a standalone SQL transformer, any SQL may enter the pipeline from any client
    — GraphQL, raw SQL via `/data/sql`, JDBC, DB-API, SQLAlchemy — and governance is uniformly enforced.
    No client can bypass governance without bypassing the server.
  use_case: Stage 2 as standalone SQL transformer ensures no client can bypass governance without bypassing
    the server.
  code:
  - provisa/compiler/stage2.py
  - provisa/compiler/sql_gen.py
  tests:
  - tests/integration/test_governance_integration.py
  - tests/unit/test_sql_endpoint_governance.py
  - tests/unit/test_stage2.py
  - tests/unit/test_two_stage_compiler.py
  - provisa-ui/e2e/governance-core.spec.ts
  since: 2026-01
  scenario: 'Given a SQL query from any client (GraphQL, /data/sql, JDBC, DB-API, SQLAlchemy)

    When the query enters the pipeline

    Then Stage 2 governance is uniformly enforced regardless of client

    '
  integration_test: required
  integration_test_reason: Uniform governance across all SQL client paths requires end-to-end verification
    via GraphQL, REST /data/sql, JDBC, and SQLAlchemy.
  e2e: true
  e2e_reason: Confirming uniform Stage 2 governance across GraphQL, REST /data/sql, JDBC, and SQLAlchemy
    client paths requires real HTTP and JDBC transport against a running server.
  stakeholders:
  - app-developer
  - ops
  competitive_position:
    status: ahead
    rationale: Uniform Stage 2 governance regardless of entry protocol (GraphQL, raw SQL via REST, JDBC,
      DB-API, SQLAlchemy) means there is no privileged back door. Hasura's raw SQL passthrough (run_sql)
      historically bypassed row-level permissions; Trino's JDBC path does not add governance beyond what
      connectors provide.
- id: REQ-267
  status: complete
  group: 1. Access Governance & Security
  category: Two-Stage Compiler (Governed SQL)
  priority: SHOULD
  type: behavioral
  description: 'A `/data/sql` REST endpoint accepts raw PG-compatible SQL + role (via auth), passes it
    through Stage 2 governance, routes and executes identically to the GraphQL path. Subject to the user''s
    rights: any table or view the user''s rights permit may be queried freely; Stage 2 governance is applied
    uniformly.'
  use_case: /data/sql endpoint lets raw SQL tools submit queries that still pass through full governance
    enforcement.
  code:
  - provisa/compiler/stage2.py
  - provisa/compiler/sql_gen.py
  tests:
  - tests/integration/test_governance_integration.py
  - tests/unit/test_sql_endpoint_governance.py
  - tests/unit/test_stage2.py
  - tests/unit/test_two_stage_compiler.py
  - provisa-ui/e2e/governance-core.spec.ts
  since: 2026-01
  scenario: 'Given a raw SQL query submitted to /data/sql with valid auth

    When the endpoint processes the query

    Then it passes through Stage 2 governance and executes identically to the GraphQL path

    '
  integration_test: required
  integration_test_reason: /data/sql endpoint requires real HTTP transport and Stage 2 governance verification
    with role-based table access.
  e2e: true
  e2e_reason: The /data/sql REST endpoint must be exercised with real HTTP POST requests to a running
    server to verify Stage 2 governance is applied on the network-crossing SQL path.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - app-developer
  - data-engineer
  competitive_position:
    status: ahead
    rationale: A governed raw SQL endpoint (/data/sql) that applies full Stage 2 governance (RLS, masking,
      visibility, row cap) to arbitrary PG-compatible SQL is not offered by Hasura or Trino in a governed
      form. Dremio's SQL endpoint is governed only by its own catalog permissions, not by a cross-source
      federation governance layer.
- id: REQ-369
  status: complete
  group: 1. Access Governance & Security
  category: Rate Limiting
  priority: MUST
  type: behavioral
  description: 'Per-role rate limits configurable in `provisa.yaml`: max requests per second, max concurrent
    SSE subscriptions, max concurrent Arrow Flight streams. Limits enforced at the API layer before compilation
    or execution. Requests exceeding limits rejected with HTTP 429 and a `Retry-After` header.'
  use_case: Per-role rate limits prevent any single role from exhausting server resources or upstream
    connection pools.
  code:
  - provisa/api/
  tests:
  - tests/unit/test_rate_limiting.py
  - tests/integration/test_rate_limiting_integration.py
  - provisa-ui/e2e/security-rate-limiting.spec.ts
  since: 2026-01
  scenario: 'Given a role with configured rate limits

    When requests exceed the rate limit

    Then requests are rejected with HTTP 429 and a Retry-After header before compilation or execution

    '
  integration_test: required
  integration_test_reason: Per-role rate limits with HTTP 429 responses require real network transport
    and Redis backing verification.
  e2e: true
  e2e_reason: Verifying HTTP 429 responses and Retry-After headers under per-role rate limits requires
    real HTTP requests to a running server backed by Redis.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - ops
  - compliance
  competitive_position:
    status: parity
    rationale: Per-role API rate limiting with HTTP 429 and Retry-After headers is standard practice.
      Hasura Cloud and Denodo's API layer both support per-role rate limiting; this is expected by enterprise
      buyers and does not differentiate.
- id: REQ-370
  status: complete
  group: 1. Access Governance & Security
  category: Rate Limiting
  priority: MUST
  type: behavioral
  description: The NL query service (`POST /query/nl`) has an independent rate limit configurable via
    `nl.rate_limit` (requests per minute per role). LLM API cost is the primary driver — unbounded NL
    requests are a cost risk regardless of server capacity. Requests exceeding the limit are rejected
    before any LLM call is made.
  use_case: NL rate limit caps LLM API cost exposure without restricting other query paths.
  code:
  - provisa/nl/
  tests:
  - tests/unit/test_rate_limiting.py
  - tests/integration/test_rate_limiting_integration.py
  - provisa-ui/e2e/security-rate-limiting.spec.ts
  since: 2026-01
  scenario: 'Given a role with a configured nl.rate_limit

    When NL query requests exceed the per-minute limit

    Then requests are rejected before any LLM call is made

    '
  integration_test: required
  integration_test_reason: NL rate limit blocks LLM calls at API layer; requires real HTTP POST and LLM
    cost verification.
  e2e: true
  e2e_reason: Confirming that NL query requests are rejected before any LLM call at the API layer requires
    a real HTTP POST to the running /query/nl endpoint.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - ops
  - executive
  competitive_position:
    status: ahead
    rationale: An independent, pre-LLM-call rate limit on the NL query service per role is a governance
      feature tied to Provisa's built-in AI-assisted query capability. Competitors (Trino, Hasura, Denodo,
      Dremio) do not have a governed NL-to-SQL endpoint with cost-protecting rate limits built in.
- id: REQ-371
  status: complete
  group: 1. Access Governance & Security
  category: Rate Limiting
  priority: MUST
  type: infrastructure
  description: Rate limit state stored in Redis (existing `cache.redis_url`) using a sliding window counter.
    No rate limit state is held in-process — stateless Provisa containers (REQ-057) share limit state
    across all instances.
  use_case: Redis-backed rate limit state ensures limits are enforced correctly across all horizontal
    Provisa instances.
  code:
  - provisa/api/
  tests:
  - tests/integration/test_cache_store.py
  - tests/unit/test_cache_store.py
  - tests/unit/test_rate_limiting.py
  since: 2026-01
  integration_test: required
  integration_test_reason: Redis-backed rate limit state sharing across instances requires real multi-instance
    deployment verification.
  stakeholders:
  - ops
  competitive_position:
    status: parity
    rationale: Redis sliding-window rate limit state shared across stateless instances is a well-understood
      pattern used by Hasura Cloud and API gateway layers. This is an implementation correctness requirement,
      not a differentiator.
- id: REQ-402
  status: complete
  group: 1. Access Governance & Security
  category: Security
  priority: SHOULD
  type: structural
  description: RLS rules can be scoped to a domain instead of a single table by setting `domain_id` (nullable
    FK to domains) instead of `table_id` (now also nullable); a single domain-level rule applies to all
    tables in that domain unless overridden by a table-specific rule.
  use_case: Domain-level RLS rules reduce configuration burden when the same row filters apply across
    many tables in a semantic domain.
  code:
  - provisa/core/schema.sql
  - provisa/core/repositories/rls.py
  - provisa/compiler/
  tests:
  - tests/e2e/test_security.py
  - tests/integration/test_governance_integration.py
  - tests/unit/test_rls.py
  since: 2026-01
  integration_test: required
  integration_test_reason: Domain-level RLS scoping requires real table registration and domain hierarchy
    testing at execution.
  stakeholders:
  - data-engineer
  - compliance
  competitive_position:
    status: ahead
    rationale: Domain-scoped RLS rules (a single rule applying to all tables in a domain unless overridden
      by a table-specific rule) reduce governance configuration overhead at scale. Trino, Hasura, and
      Dremio require per-table row filter policies with no concept of domain-level inheritance.
- id: REQ-531
  status: complete
  group: 1. Access Governance & Security
  category: Security
  priority: MUST
  type: behavioral
  description: Masked columns are rejected from WHERE and HAVING clauses at query parse time, before execution,
    preventing inference of the unmasked value through binary search filtering. Enforced via V005 validation
    in the SQL validator (provisa/compiler/sql_validator.py). Applies across all query interfaces (GraphQL,
    SQL, Cypher).
  use_case: Predicate guard closes the filtering side-channel so callers cannot infer unmasked values
    by submitting successive WHERE comparisons against a masked column.
  code:
  - provisa/compiler/sql_validator.py
  tests:
  - tests/e2e/test_security.py
  - tests/unit/test_cypher_graph_fns.py
  - tests/unit/test_stage2.py
  - tests/integration/test_security_integration_extra.py
  since: 2026-01
  scenario: 'Given a query with a masked column in a WHERE or HAVING clause

    When the query is parsed

    Then it is rejected at parse time before execution via V005 validation

    '
  integration_test: required
  integration_test_reason: Masked column predicate rejection requires real query parsing and compilation
    to verify WHERE/HAVING guard.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - compliance
  - data-engineer
  competitive_position:
    status: ahead
    rationale: Rejecting masked columns from WHERE and HAVING at parse time (before execution) prevents
      inference of unmasked values via binary search filtering. Hasura and Dremio do not perform this
      inference-blocking validation; Snowflake's dynamic data masking similarly does not block filtered
      predicate inference.
- id: REQ-554
  status: complete
  group: 1. Access Governance & Security
  category: Security
  priority: MUST
  type: behavioral
  description: 'All roles receive sampled results capped at DEFAULT_SAMPLE_SIZE (default: 100 rows) unless
    the role holds the `full_results` capability. The cap is controlled by the PROVISA_SAMPLE_SIZE environment
    variable and enforced via the Stage 2 row cap mechanism.'
  use_case: Default row cap prevents accidental large result delivery to unprivileged roles without requiring
    per-query LIMIT clauses.
  code:
  - provisa/compiler/sampling.py
  - provisa/compiler/stage2.py
  tests:
  - tests/e2e/test_security.py
  - tests/unit/test_stage2.py
  - tests/integration/test_security_integration_extra.py
  since: 2026-01
  scenario: 'Given a role without the full_results capability

    When a query is executed

    Then results are capped at DEFAULT_SAMPLE_SIZE rows via the Stage 2 row cap mechanism

    '
  integration_test: required
  integration_test_reason: Default row cap behavior requires real role-based query execution to verify
    sampling versus full results.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - compliance
  - data-engineer
  competitive_position:
    status: parity
    rationale: Default result size caps enforced per role are standard in Snowflake (resource monitors),
      BigQuery (slot limits), and Denodo (result limits). This is expected enterprise behavior and does
      not differentiate.
- id: REQ-555
  status: complete
  group: 1. Access Governance & Security
  category: ABAC Approval Hook
  priority: SHOULD
  type: behavioral
  description: The gRPC approval hook maintains a single persistent channel per Provisa instance (one
    grpc.aio channel reused across all calls to that endpoint), reducing connection overhead for high-throughput
    co-located policy services.
  use_case: Persistent gRPC channel eliminates per-call connection overhead for high-volume approval checks
    against a co-located policy service.
  code:
  - provisa/auth/approval_hook.py
  tests:
  - tests/integration/test_audit_auth_integration.py
  - tests/unit/test_abac_hook_extended.py
  - tests/unit/test_approval_hook.py
  - provisa-ui/e2e/security-rate-limiting.spec.ts
  since: 2026-01
  scenario: 'Given a Provisa instance configured with gRPC approval hook

    When multiple approval hook calls are made

    Then a single persistent grpc.aio channel is reused across all calls

    '
  integration_test: required
  integration_test_reason: Persistent gRPC channel reuse requires real high-volume call verification to
    measure connection overhead reduction.
  e2e: true
  e2e_reason: Persistent gRPC channel reuse across multiple approval hook calls can only be verified by
    issuing real concurrent requests to a running server with a gRPC hook endpoint.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - ops
  competitive_position:
    status: ahead
    rationale: A persistent gRPC channel reused per Provisa instance for the approval hook reduces connection
      overhead to near zero for co-located policy services. Hasura's action/event system opens new HTTP
      connections per request; no competitor exposes a reusable gRPC policy channel at this architectural
      level.
- id: REQ-556
  status: complete
  group: 1. Access Governance & Security
  category: ABAC Approval Hook
  priority: MUST
  type: behavioral
  description: The approval hook client implements a circuit breaker that opens after 5 consecutive failures
    (configurable via circuit_breaker_threshold) and enters half-open state after 30 seconds (configurable
    via circuit_breaker_cooldown_s), preventing cascading failures from a slow or unavailable hook endpoint.
  use_case: Circuit breaker prevents a slow or unavailable approval hook from blocking all query execution
    when the fallback policy is 'allow'.
  code:
  - provisa/auth/approval_hook.py
  tests:
  - tests/unit/test_abac_hook_extended.py
  - tests/unit/test_approval_hook.py
  - tests/integration/test_security_integration_extra.py
  since: 2026-01
  scenario: 'Given an approval hook endpoint that fails consecutively 5 times

    When the circuit breaker threshold is reached

    Then the circuit opens and enters half-open state after the configured cooldown period

    '
  integration_test: required
  integration_test_reason: Circuit breaker behavior requires real approval hook failures and timeout recovery
    verification.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - ops
  - compliance
  competitive_position:
    status: ahead
    rationale: Built-in circuit breaker (5-failure threshold, 30s cooldown, half-open state) on the ABAC
      hook client prevents cascading failures from slow or unavailable external policy services. This
      resilience pattern is not included in Hasura's remote schema or action hooks, nor in Trino's authorization
      plugins.
- id: REQ-557
  status: complete
  group: 1. Access Governance & Security
  category: Security
  priority: MUST
  type: constraint
  description: Data source credentials use ${env:VAR_NAME} syntax in the Provisa config YAML; values are
    resolved at runtime from environment variables via the secrets provider in provisa/core/secrets.py.
    Plaintext passwords are never stored in the config database.
  use_case: Environment-variable secret references keep credentials out of config files and the config
    database, satisfying standard secrets hygiene requirements.
  code:
  - provisa/core/secrets.py
  tests:
  - tests/e2e/test_security.py
  - tests/unit/test_secrets.py
  since: 2026-01
  scenario: 'Given a data source credential configured with ${env:VAR_NAME} syntax

    When Provisa resolves the config at runtime

    Then the value is read from the environment variable and plaintext passwords are never stored

    '
  integration_test: not_required
  integration_test_reason: Environment variable secret resolution is a configuration loading concern verifiable
    via unit tests.
  stakeholders:
  - ops
  - compliance
  competitive_position:
    status: parity
    rationale: Environment variable interpolation for secrets in config files is standard across Trino,
      Hasura, Dremio, and Denodo. All serious platforms avoid plaintext credentials in config; this is
      a baseline security hygiene requirement.
- id: REQ-591
  status: complete
  group: 1. Access Governance & Security
  category: Security
  priority: MUST
  type: constraint
  description: SET LOCAL scopes app.tenant_id to the current database transaction. When the transaction
    commits or rolls back, the variable resets, preventing tenant context leakage across transactions.
  use_case: Transaction-scoped tenant context prevents cross-tenant data leakage if middleware is bypassed
    or connection is reused across requests.
  code:
  - provisa/core/db.py
  tests:
  - tests/e2e/test_security.py
  - tests/unit/test_core_registration.py
  - tests/integration/test_security_integration_extra.py
  since: 2026-01
  scenario: 'Given a database transaction with SET LOCAL app.tenant_id applied

    When the transaction commits or rolls back

    Then the tenant_id variable resets preventing context leakage to subsequent transactions

    '
  integration_test: required
  integration_test_reason: SET LOCAL transaction scoping requires real database transaction lifecycle
    verification.
  stakeholders:
  - ops
  - compliance
  competitive_position:
    status: parity
    rationale: Transaction-scoped SET LOCAL for tenant context isolation is standard PostgreSQL practice
      used by Hasura's multi-tenant setups and other PG-backed platforms. This is a correctness requirement,
      not a competitive differentiator.
- id: REQ-594
  status: complete
  group: 1. Access Governance & Security
  category: Security
  priority: MUST
  type: behavioral
  description: AuthMiddleware defines a skip-path set `{/billing/signup, /billing/webhook, /health, /live,
    /ready, /data/openapi/*, /auth/login, /auth/provider-type, /auth/bootstrap-status, /setup/status}`.
    Requests to these paths bypass the bearer-token gate entirely and do not require a JWT. The set is
    exact — every entry is an unauthenticated hole, so an addition is a deliberate edit. REQ-1355 moved
    this set out of TenantMiddleware, which was registered behind a flag that was always False at app-factory
    time and therefore never ran, leaving the billing webhook to 401 on the auth gate.
  use_case: Skip-path exemptions allow Lemon Squeezy billing webhooks (which authenticate by HMAC signature,
    not a bearer token), health/liveness probes, the pre-login provider probes, and OpenAPI docs to function
    without a JWT while all other paths enforce authentication.
  code:
  - provisa/auth/middleware.py
  tests:
  - tests/e2e/test_security.py
  - tests/unit/test_tenancy_requirements.py
  - tests/integration/test_security_integration_extra.py
  - provisa-ui/e2e/security-rate-limiting.spec.ts
  since: 2026-01
  scenario: 'Given a request to /billing/signup, /billing/webhook, /health, /docs, or /openapi.json

    When AuthMiddleware processes the request

    Then the bearer-token gate is bypassed and no JWT is required

    '
  integration_test: required
  integration_test_reason: Tenant skip-path exemptions require real HTTP requests to health/billing endpoints
    without tenant JWTs.
  e2e: true
  e2e_reason: Confirming that skip-path requests (/health, /billing/webhook, etc.) bypass tenant resolution
    requires real HTTP requests to a running server without tenant JWTs.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - ops
  - app-developer
  competitive_position:
    status: neutral
    rationale: ''
- id: REQ-596
  status: complete
  group: 1. Access Governance & Security
  category: Audit Logging
  priority: MUST
  type: behavioral
  description: 'Every query is recorded in `query_audit_log` with tenant_id, user_id, role_id, SHA-256
    query hash, table_ids, source, status_code, duration_ms, and logged_at. The table is append-only:
    PostgreSQL rules block DELETE and UPDATE at the database level. Query text is never stored verbatim
    — only its SHA-256 hash. Two indexes support tenant-scoped and per-user time-range queries. The write
    happens at the ONE governed pipeline, never per transport: the governors (_govern_and_route, _govern_and_route_compiled)
    open a PendingAudit carrying the acting principal, the resolved registered_tables ids (from gov_ctx.table_map),
    the source surface and the start time; a governance refusal writes its row immediately with status
    403; a completed statement writes 200 (or 500) at the terminal. Terminals that drain the engine on
    a worker thread instead of _execute_plan (pgwire simple query and COPY, Flight SQL, gRPC ENGINE streaming,
    airport scans, Cypher REST, CTAS) finalize explicitly through the same idempotent finalize_audit,
    so every surface writes exactly one row. The acting principal reaches the pipeline through a single
    ContextVar channel (provisa/audit/context.py) bound at each surface boundary — HTTP AuthMiddleware,
    pgwire session, Flight, gRPC interceptor, MCP transport, Bolt session, airport role token — and re-bound
    inside the coroutine with with_audit_identity wherever the surface crosses a thread via run_coroutine_threadsafe
    (ContextVars do not propagate there). No identity bound (an unsecured deployment authenticates nobody)
    → no row.'
  use_case: Append-only audit log with hash-only query storage satisfies data minimization requirements
    while providing immutable evidence of query activity for compliance and investigation, and is the
    fact table every ops report view (REQ-1386) reads.
  code:
  - provisa/audit/query_log.py
  - provisa/audit/context.py
  - provisa/audit/pipeline.py
  - provisa/pgwire/_pipeline.py
  tests:
  - tests/unit/test_audit_requirements.py
  - tests/unit/test_audit_pipeline.py
  - tests/integration/test_audit_auth_integration.py
  since: 2026-01
  scenario: 'Given any query executed against the system

    When the query completes

    Then it is recorded in query_audit_log with required fields and only the SHA-256 hash of the query
    text

    '
  integration_test: required
  integration_test_reason: Append-only audit log with SHA-256 hashing and database-level rules requires
    real query execution and deletion attempts.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - compliance
  - ops
  competitive_position:
    status: ahead
    rationale: Append-only audit log (PostgreSQL rules blocking DELETE and UPDATE at DB level) with SHA-256
      query hashing (no verbatim query text stored) and tenant-scoped/per-user indexes satisfies SOC2
      requirements out of the box. Trino and Dremio offer audit logging but do not enforce append-only
      at the database level or hash query text by default.
- id: REQ-603
  status: complete
  group: 1. Access Governance & Security
  category: Query Governance
  priority: MUST
  type: behavioral
  description: 'V002 relationship governance: every JOIN ON condition in SQL and Cypher queries must match
    an approved, registered relationship (source_col = target_col). SQL and Cypher queries that traverse
    a join not backed by a registered relationship are rejected at compile time. GraphQL queries that
    traverse relationships defined in the SDL are always pre-approved and exempt from the V002 check.'
  use_case: V002 enforces approved navigation paths so joins between sensitive tables require explicit
    steward registration and approval, providing an audit surface for cross-table data access.
  code:
  - provisa/compiler/sql_validator.py
  tests:
  - tests/unit/test_cypher_graph_fns.py
  - tests/unit/test_stage2.py
  - tests/integration/test_security_integration_extra.py
  since: 2026-01
  scenario: 'Given a SQL or Cypher query with a JOIN ON condition

    When the compiler validates the query

    Then it is rejected at compile time if the join is not backed by a registered relationship

    '
  integration_test: required
  integration_test_reason: V002 relationship governance requires real JOIN compilation and execution with
    registered relationships.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - compliance
  - data-engineer
  competitive_position:
    status: ahead
    rationale: Rejecting at compile time any JOIN not backed by a registered, approved relationship (V002
      enforcement) enforces referential governance across SQL and Cypher queries. Trino and Hasura do
      not validate JOIN conditions against a registered relationship registry; Denodo's relationship model
      is advisory rather than enforced at compile time.
- id: REQ-613
  status: complete
  superseded_by: REQ-596
  group: 1. Access Governance & Security
  category: Query Governance
  priority: MUST
  type: behavioral
  description: 'Every query that touches a domain asset is logged in an append-only audit log (query_audit_log).
    The log captures: user_id, role_id, query_hash, table_ids, source, status_code, duration_ms, and logged_at.
    The log is protected by PostgreSQL rules that prevent DELETE and UPDATE operations (SOC2 append-only
    requirement). Indexed by (tenant_id, logged_at) and (user_id, logged_at) for efficient compliance
    reporting.'
  use_case: Append-only query audit log provides SOC2-compliant evidence of who queried what data and
    when.
  code:
  - provisa/audit/query_log.py
  - provisa/audit/compliance_reporter.py
  tests:
  - tests/unit/test_audit_requirements.py
  - tests/integration/test_security_integration_extra.py
  since: 2026-01
  scenario: 'Given any query touching a domain asset

    When the query is executed

    Then it is logged in the append-only query_audit_log with all required fields

    '
  integration_test: required
  integration_test_reason: Append-only audit log with PostgreSQL rule protection requires real query logging
    and deletion attempt verification.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - compliance
  - ops
  competitive_position:
    status: ahead
    rationale: Every query touching a domain asset is recorded in a DB-enforced append-only audit log
      (PostgreSQL rules block DELETE/UPDATE), indexed for compliance reporting. This SOC2-ready audit
      trail is built into the federation layer itself, unlike Trino (logs to files/plugins) or Dremio
      (job history, not append-only at DB level).
- id: REQ-120
  status: complete
  group: 2. Authentication & Identity
  category: Authentication
  priority: MUST
  type: structural
  description: Pluggable auth provider interface — abstract AuthProvider producing AuthIdentity (user_id,
    email, roles, claims) mapped to Provisa roles. One provider at a time configured in YAML.
  use_case: Pluggable auth provider interface lets organizations connect any identity system without code
    changes.
  code:
  - provisa/auth/
  tests:
  - tests/unit/test_auth_providers.py
  - tests/unit/test_auth_middleware.py
  - tests/integration/test_auth_integration.py
  since: 2026-01
  scenario: 'Given a YAML config specifying an auth provider

    When the system initializes

    Then a single AuthProvider is loaded and produces AuthIdentity objects with user_id, email, roles,
    and claims

    '
  integration_test: not_required
  integration_test_reason: Abstract interface design is pure logic; provider swapping verified via unit/integration
    mocks.
  stakeholders:
  - app-developer
  - compliance
  - ops
  competitive_position:
    status: parity
    rationale: A pluggable auth provider interface is standard for enterprise platforms — Hasura, Denodo,
      and Dremio all support configurable identity backends. The single-provider-at-a-time constraint
      is a simplification relative to Denodo's multi-provider federation.
- id: REQ-121
  status: complete
  group: 2. Authentication & Identity
  category: Authentication
  priority: MUST
  type: behavioral
  description: Firebase Authentication — validates Firebase ID tokens via firebase-admin SDK. Supports
    all Firebase auth methods (email/password, Google, Apple, GitHub, phone, anonymous, SAML, OIDC).
  use_case: Firebase auth integration supports all Firebase sign-in methods including SSO and anonymous
    users.
  code:
  - provisa/auth/
  tests:
  - tests/unit/test_auth_providers.py
  - tests/unit/test_auth_middleware.py
  - tests/integration/test_auth_integration.py
  - provisa-ui/e2e/auth-providers.spec.ts
  since: 2026-01
  scenario: 'Given Firebase is configured as the auth provider

    When a request arrives with a Firebase ID token

    Then the token is validated via firebase-admin SDK and the identity is resolved

    '
  integration_test: required
  integration_test_reason: Firebase token validation requires real JWKS fetch, signature verification,
    and token expiry enforcement over HTTP.
  e2e: true
  e2e_reason: Firebase ID token validation requires real JWKS retrieval over HTTP from Firebase and signature
    verification against a live token, which only a running server can perform.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - compliance
  - app-developer
  - ops
  competitive_position:
    status: ahead
    rationale: Native Firebase Authentication support is uncommon among data federation platforms — Trino,
      Denodo, and Dremio offer no first-class Firebase integration. This positions Provisa for mobile/web
      app developer audiences that Hasura targets but addresses at the federation SQL/GraphQL/Flight layer
      rather than just GraphQL.
- id: REQ-122
  status: complete
  group: 2. Authentication & Identity
  category: Authentication
  priority: MUST
  type: behavioral
  description: Keycloak OIDC — validates JWT access tokens from Keycloak via OIDC discovery + JWKS. Realm
    roles + client roles → Provisa role mapping.
  use_case: Keycloak OIDC integration maps realm and client roles to Provisa roles for enterprise SSO
    deployments.
  code:
  - provisa/auth/
  tests:
  - tests/unit/test_auth_providers.py
  - tests/unit/test_auth_middleware.py
  - tests/integration/test_auth_integration.py
  - provisa-ui/e2e/auth-providers.spec.ts
  since: 2026-01
  scenario: 'Given Keycloak is configured as the OIDC provider

    When a request arrives with a Keycloak JWT access token

    Then the token is validated via OIDC discovery and JWKS, and realm/client roles are mapped to Provisa
    roles

    '
  integration_test: required
  integration_test_reason: OIDC discovery, JWKS fetch, and JWT validation require real Keycloak instance
    and network transport.
  e2e: true
  e2e_reason: Keycloak OIDC discovery and JWKS-based JWT validation require a real Keycloak instance and
    network transport against a running Provisa server.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - compliance
  - app-developer
  - ops
  competitive_position:
    status: parity
    rationale: Keycloak OIDC with realm/client role mapping is standard enterprise capability — Hasura,
      Dremio, and Denodo all support Keycloak or generic OIDC with role claim mapping.
- id: REQ-123
  status: complete
  group: 2. Authentication & Identity
  category: Authentication
  priority: MUST
  type: behavioral
  description: Generic OAuth 2.0 / OIDC — works with any OIDC-compliant provider (PingFederate, Okta,
    Azure AD, Auth0). OIDC discovery URL → JWKS → JWT validation. Configurable role claim mapping.
  use_case: Generic OIDC support lets any compliant provider (Okta, Azure AD, Auth0) integrate without
    custom code.
  code:
  - provisa/auth/
  tests:
  - tests/integration/test_auth_integration.py
  - tests/unit/test_auth_middleware.py
  - tests/unit/test_auth_providers.py
  - tests/unit/test_config_validation_edge_cases.py
  - provisa-ui/e2e/auth-providers.spec.ts
  since: 2026-01
  scenario: 'Given a generic OIDC provider is configured with a discovery URL

    When a request arrives with a JWT access token

    Then the token is validated via JWKS and roles are mapped using the configured claim mapping

    '
  integration_test: required
  integration_test_reason: OIDC discovery, JWKS fetch, and JWT validation require real OIDC provider and
    network transport.
  e2e: true
  e2e_reason: Generic OIDC discovery and JWKS-based JWT validation require real network calls to an OIDC
    provider endpoint from a running Provisa server.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - compliance
  - app-developer
  - ops
  competitive_position:
    status: parity
    rationale: Generic OIDC/OAuth 2.0 support covering Okta, Azure AD, Auth0, and PingFederate is table-stakes
      for enterprise platforms — Trino (via LDAP/custom plugins), Hasura, Denodo, and Dremio all offer
      equivalent generic OIDC integration.
- id: REQ-124
  status: complete
  group: 2. Authentication & Identity
  category: Authentication
  priority: MAY
  type: behavioral
  description: 'Simple username/password auth for testing — users defined in config YAML with bcrypt hashed
    passwords. Issues short-lived JWT. NOT for production (requires allow_simple_auth: true flag).'
  use_case: Simple username/password auth lets developers test the platform locally without configuring
    an IdP.
  code:
  - provisa/auth/
  tests:
  - tests/unit/test_auth_providers.py
  - tests/unit/test_auth_middleware.py
  - tests/integration/test_auth_integration.py
  since: 2026-01
  scenario: 'Given allow_simple_auth is true and users are defined in config YAML with bcrypt passwords

    When a developer submits valid credentials

    Then a short-lived JWT is issued for local testing

    '
  integration_test: not_required
  integration_test_reason: bcrypt hashing and JWT generation are pure crypto logic; token validation verified
    via unit tests.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - app-developer
  competitive_position:
    status: neutral
    rationale: ''
- id: REQ-125
  status: complete
  superseded_by: REQ-1264
  group: 2. Authentication & Identity
  category: Authentication
  priority: MUST
  type: behavioral
  description: Superuser bootstrap access — superuser credentials in config (username + password from
    env secret). Always platform_admin role + all capabilities regardless of auth provider. For initial
    setup. Role name updated by REQ-1327's platform_admin/org_admin split (the old undifferentiated "admin"
    role no longer exists).
  use_case: Superuser bootstrap ensures admins can always access the platform for initial setup regardless
    of auth provider.
  code:
  - provisa/auth/
  tests:
  - tests/unit/test_auth_providers.py
  - tests/unit/test_auth_middleware.py
  - tests/integration/test_auth_integration.py
  since: 2026-01
  scenario: 'Given superuser credentials are set in config via env secret

    When the superuser authenticates

    Then they receive platform_admin role and all capabilities regardless of the configured auth provider

    '
  integration_test: not_required
  integration_test_reason: Superuser bootstrap logic is config parsing and role elevation; no external
    auth flow involved.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - compliance
  - ops
  competitive_position:
    status: neutral
    rationale: ''
- id: REQ-535
  status: complete
  superseded_by: REQ-1327
  group: 2. Authentication & Identity
  category: Authentication
  priority: SHOULD
  type: behavioral
  description: When no auth provider is configured (dev mode), any request is treated as the anonymous
    dev principal (user_id `anonymous`), with the role taken from the `x-provisa-role` header or defaulting
    to `org_admin` (REQ-1327's data-plane administrator — the undifferentiated `admin` role no longer
    exists, and platform_admin is control-plane only so a demo deployment need not define it). This identity
    maps to all configured roles with wildcard domain access, enabling unrestricted local development
    without configuring an IdP.
  use_case: Dev mode role identity lets developers test without an IdP while still exercising role-based
    code paths.
  code:
  - provisa/auth/middleware.py
  tests:
  - tests/unit/test_auth_middleware.py
  - tests/integration/test_audit_auth_integration.py
  since: 2026-01
  scenario: 'Given no auth provider is configured

    When any request arrives

    Then it is treated as the anonymous dev principal with role org_admin by default, all roles, and wildcard
    domain access

    '
  integration_test: not_required
  integration_test_reason: Role-identity fallback is pure middleware logic; role mapping verified via
    unit/integration mocks.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - app-developer
  competitive_position:
    status: neutral
    rationale: ''
- id: REQ-551
  status: complete
  group: 2. Authentication & Identity
  category: Authentication
  priority: MUST
  type: structural
  description: 'The `assignments_source` config field controls how role assignments are resolved: `claims`
    reads role assignments directly from JWT token claims; `provisa` reads role assignments from Provisa''s
    own internal assignment store. Defaults to `claims`.'
  use_case: Two assignment resolution modes let organizations choose between IdP-managed roles (claims)
    and Provisa-managed roles (provisa store) without changing auth provider.
  code:
  - provisa/core/models.py
  - provisa/auth/
  tests:
  - tests/unit/test_auth_providers.py
  - tests/integration/test_auth_integration.py
  since: 2026-01
  scenario: 'Given assignments_source is set to either claims or provisa

    When a user authenticates

    Then role assignments are resolved from the configured source (JWT claims or internal store)

    '
  integration_test: not_required
  integration_test_reason: Assignment resolution routing is pure logic; both claims and provisa store
    paths verifiable via mocks.
  stakeholders:
  - compliance
  - ops
  competitive_position:
    status: ahead
    rationale: The dual-mode role resolution (JWT claims vs. Provisa internal store) without requiring
      an auth provider change is uncommon — Trino and Dremio bind roles exclusively to the IdP token,
      while Denodo requires separate entitlement configuration. This lets organizations migrate role governance
      from IdP to Provisa-managed RBAC incrementally.
- id: REQ-012
  status: complete
  group: 3. Source Registration & Data Modeling
  category: Registration & Governance
  priority: MUST
  type: behavioral
  description: Source registration is privileged; validates connection, calls Trino dynamic catalog API,
    no restart required, available within seconds.
  use_case: Privileged, live source registration lets stewards connect new data sources in seconds without
    downtime.
  code:
  - provisa/core/
  tests:
  - tests/integration/test_introspect.py
  - tests/integration/test_schema_gen.py
  - tests/unit/test_core_registration.py
  - tests/unit/test_source_registration_validation.py
  - provisa-ui/e2e/registration-admin.spec.ts
  since: 2026-01
  scenario: 'Given a privileged steward with registration rights

    When they submit a new source registration

    Then Provisa validates the connection, calls the Trino dynamic catalog API, and makes the source

    available within seconds without a server restart

    '
  integration_test: required
  integration_test_reason: Validates connection to real Trino dynamic catalog API and checks source registration
    availability within seconds.
  e2e: true
  e2e_reason: Live source registration via the Trino dynamic catalog API and verifying the source is queryable
    within seconds requires a real running Provisa server and Trino instance.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - data-engineer
  - ops
  competitive_position:
    status: ahead
    rationale: Live, no-restart source registration via Trino dynamic catalog API is unique among federation
      platforms. Denodo and Dremio require restart or admin-heavy reconfiguration; Trino itself has no
      native governance layer wrapping dynamic catalog registration.
- id: REQ-013
  status: complete
  group: 3. Source Registration & Data Modeling
  category: Registration & Governance
  priority: MUST
  type: constraint
  description: Source registration does not expose data — no table queryable until explicitly registered
    by a steward.
  use_case: Data is invisible until explicitly registered, ensuring no accidental exposure of raw database
    tables.
  code:
  - provisa/core/
  tests:
  - tests/integration/test_schema_gen.py
  - tests/integration/test_introspect.py
  - tests/unit/test_core_registration.py
  - provisa-ui/e2e/registration-admin.spec.ts
  since: 2026-01
  scenario: 'Given a source that has been registered but no tables have been explicitly registered

    When a consumer queries the schema

    Then no tables from that source are queryable or visible

    '
  integration_test: required
  integration_test_reason: Requires real data source integration to verify unregistered tables are not
    queryable.
  e2e: true
  e2e_reason: Verifying that unregistered tables are not queryable or visible in the schema requires a
    real running server with an actual data source connected.
  stakeholders:
  - compliance
  - data-engineer
  competitive_position:
    status: ahead
    rationale: Explicit table-level publish-to-expose model (data invisible until registered) is a stronger
      default than Trino/Presto, Dremio, or Hasura, where all tables in a connected source are queryable
      by default subject to ACLs. Denodo has catalog publication but not this strict default-invisible
      posture.
- id: REQ-014
  status: complete
  group: 3. Source Registration & Data Modeling
  category: Registration & Governance
  priority: MUST
  type: constraint
  description: Unregistered tables do not exist — cannot be referenced in queries, do not appear in schema
    browser, cannot be mutation targets.
  use_case: Unregistered tables are completely absent from schema and runtime, eliminating enumeration
    attacks.
  code:
  - provisa/core/
  tests:
  - tests/integration/test_schema_gen.py
  - tests/integration/test_introspect.py
  - tests/unit/test_core_registration.py
  since: 2026-01
  scenario: 'Given a table that has not been registered by a steward

    When a consumer attempts to reference it in a query, schema browser, or mutation

    Then the table does not exist in any query layer and the operation is rejected

    '
  integration_test: required
  integration_test_reason: Requires real data environment to verify unregistered tables do not appear
    in schema browser and cannot be mutation targets.
  stakeholders:
  - compliance
  - data-engineer
  competitive_position:
    status: ahead
    rationale: Complete absence of unregistered tables from schema and runtime (not just hidden but non-existent)
      prevents enumeration attacks. Competitors like Hasura and Trino expose full schema by default; Denodo
      supports selective publishing but still reflects unregistered tables in catalog browsing for admins.
- id: REQ-015
  status: complete
  group: 3. Source Registration & Data Modeling
  category: Registration & Governance
  priority: MUST
  type: behavioral
  description: There is no per-table governance mode. Every table and view is queryable directly under
    the user's rights (table/view rights + relationship rights) with Stage 2 governance applied uniformly.
    No registry-required mode exists.
  use_case: Uniform rights-based access removes per-table governance modes; every table follows the same
    model.
  code:
  - provisa/core/
  tests:
  - tests/integration/test_schema_gen.py
  - tests/integration/test_introspect.py
  - tests/unit/test_core_registration.py
  since: 2026-01
  scenario: 'Given any registered table or view

    When a user with the appropriate rights queries it

    Then Stage 2 governance is applied uniformly without any per-table mode distinctions

    '
  integration_test: not_required
  integration_test_reason: Pure governance routing logic; can be validated with mocked sources.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - compliance
  - data-engineer
  competitive_position:
    status: ahead
    rationale: Uniform Stage 2 governance applied to every table without per-table mode switches eliminates
      misconfiguration risk. Hasura requires per-table permission rules that can be omitted; Denodo and
      Snowflake require per-object policy assignment.
- id: REQ-016
  status: complete
  group: 3. Source Registration & Data Modeling
  category: Registration & Governance
  priority: SHOULD
  type: behavioral
  description: Table publication triggers schema generation pass; table immediately available in query
    builder.
  use_case: Immediate schema availability after table publication removes friction for query developers.
  code:
  - provisa/core/
  tests:
  - tests/integration/test_schema_gen.py
  - tests/integration/test_introspect.py
  - tests/unit/test_core_registration.py
  since: 2026-01
  scenario: 'Given a steward who publishes a table

    When the publication completes

    Then a schema generation pass is triggered and the table is immediately available in the query

    builder

    '
  integration_test: required
  integration_test_reason: Requires real Trino integration to verify schema generation triggers on table
    publication.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - data-engineer
  - ops
  competitive_position:
    status: parity
    rationale: Immediate schema availability after table publication is expected behavior in Hasura and
      Denodo; none require restart for schema propagation.
- id: REQ-017
  status: complete
  group: 3. Source Registration & Data Modeling
  category: Registration & Governance
  priority: SHOULD
  type: behavioral
  description: 'NoSQL/non-relational sources are exposed read-only through their native Trino connector
    (e.g. the MongoDB connector), driven by the type-specific mapping DSL (REQ-251); no mutations. (Revised
    2026-06-18: the original "automatic Parquet materialization" wording is superseded by the connector
    approach, which is the implementation.)'
  use_case: Native Trino connectors make NoSQL sources queryable without manual ETL pipelines.
  code:
  - provisa/core/
  tests:
  - tests/integration/test_schema_gen.py
  - tests/integration/test_introspect.py
  - tests/unit/test_core_registration.py
  since: 2026-01
  scenario: 'Given a registered NoSQL source with a native Trino connector

    When a consumer queries a table from that source

    Then the query is executed read-only through the Trino connector with no mutation path available

    '
  integration_test: required
  integration_test_reason: Requires real NoSQL source (MongoDB, etc.) and Trino connector to validate
    native connector routing.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - data-engineer
  - app-developer
  competitive_position:
    status: parity
    rationale: Read-only NoSQL access via native connectors is standard in Trino/Presto (MongoDB, Elasticsearch,
      Redis connectors) and Dremio; no differentiation here.
  related_reqs:
  - REQ-825
- id: REQ-018
  status: complete
  group: 3. Source Registration & Data Modeling
  category: Registration & Governance
  priority: SHOULD
  type: behavioral
  description: Trino FK metadata used to infer candidate intra-source relationships for steward confirmation/rejection.
  use_case: FK-inferred relationship suggestions reduce manual steward work when registering related tables.
  code:
  - provisa/core/
  tests:
  - tests/integration/test_schema_gen.py
  - tests/integration/test_introspect.py
  - tests/unit/test_core_registration.py
  since: 2026-01
  scenario: 'Given tables in a registered source with FK constraints visible via Trino metadata

    When a steward reviews relationship candidates

    Then intra-source FK relationships are presented as candidates for confirmation or rejection

    '
  integration_test: required
  integration_test_reason: Requires real database with FK metadata to validate inference of relationship
    candidates.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - data-engineer
  competitive_position:
    status: parity
    rationale: FK-inferred relationship suggestions exist in Hasura (auto-track FK relationships) and
      Denodo (relationship discovery from catalog); this is table-stakes for a federation platform.
- id: REQ-019
  status: complete
  group: 3. Source Registration & Data Modeling
  category: Registration & Governance
  priority: SHOULD
  type: behavioral
  description: 'Cross-source relationships defined manually by steward with cardinality (many-to-one,
    one-to-many). (Revised 2026-06-18: one-to-one removed — the relationship-field model is a strict binary,
    single object vs list, so a 1:1 collapses to many-to-one; model a true 1:1 as a many-to-one in each
    direction.)'
  use_case: Manual cross-source relationship definitions let stewards federate data across different databases
    with full control.
  code:
  - provisa/core/
  tests:
  - tests/integration/test_schema_gen.py
  - tests/integration/test_introspect.py
  - tests/unit/test_core_registration.py
  - tests/unit/test_metadata_snapshot_builder.py
  since: 2026-01
  scenario: 'Given two tables in different registered sources

    When a steward manually defines a cross-source relationship with cardinality

    Then the relationship is persisted and available for query traversal

    '
  integration_test: not_required
  integration_test_reason: Relationship cardinality definition is pure data modeling logic; validation
    works with test fixtures.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - data-engineer
  competitive_position:
    status: ahead
    rationale: Manual cross-source relationship definition with explicit cardinality (and a principled
      removal of 1:1 as a distinct mode) is more rigorous than Hasura DDN or Denodo, which allow ad-hoc
      cross-source joins without steward-governed cardinality declaration.
- id: REQ-020
  status: complete
  group: 3. Source Registration & Data Modeling
  category: Registration & Governance
  priority: SHOULD
  type: behavioral
  description: Relationships owned by defining steward, versioned, flagged for re-review on schema changes
    affecting join fields.
  use_case: Versioned, owner-tracked relationships flag stale joins when schema changes, preventing silent
    data errors.
  code:
  - provisa/core/
  tests:
  - tests/integration/test_introspect.py
  - tests/integration/test_schema_gen.py
  - tests/unit/test_core_registration.py
  - tests/unit/test_relationship_metadata.py
  - tests/unit/test_metadata_snapshot_builder.py
  since: 2026-01
  scenario: 'Given a registered relationship between two tables

    When a schema change affects one of the join fields

    Then the relationship is flagged for re-review and the owning steward is notified

    '
  integration_test: not_required
  integration_test_reason: Versioning and ownership tracking are state management concerns; can be tested
    without integration.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - compliance
  - data-engineer
  competitive_position:
    status: ahead
    rationale: Versioned, owner-tracked relationships flagged for re-review on schema changes is a differentiator;
      Hasura, Dremio, and Trino offer no built-in mechanism to flag stale cross-source joins when underlying
      schema changes.
- id: REQ-021
  status: complete
  group: 3. Source Registration & Data Modeling
  category: Registration & Governance
  priority: MUST
  type: structural
  description: GraphQL schema reflects registration model (business intent), not raw database structure.
  use_case: Business-intent schema means the GraphQL API reflects concepts consumers understand, not raw
    table structures.
  code:
  - provisa/core/
  tests:
  - tests/integration/test_schema_gen.py
  - tests/integration/test_introspect.py
  - tests/unit/test_core_registration.py
  since: 2026-01
  scenario: 'Given tables registered with aliases and descriptions

    When the GraphQL schema is generated

    Then it reflects the business registration model rather than raw database column and table names

    '
  integration_test: not_required
  integration_test_reason: GraphQL schema generation is schema transformation logic; can be validated
    against test fixtures.
  stakeholders:
  - app-developer
  competitive_position:
    status: ahead
    rationale: GraphQL schema generated from business registration model (not raw DB structure) is Provisa's
      core value proposition. Hasura exposes physical table/column names by default; Denodo requires manual
      alias configuration layer-by-layer.
- id: REQ-119
  status: complete
  group: 3. Source Registration & Data Modeling
  category: JSONB & API Sources
  priority: SHOULD
  type: behavioral
  description: Stewards can promote specific nested fields from JSONB columns into native PostgreSQL generated
    columns (GENERATED ALWAYS AS ... STORED). Promoted columns are filterable, indexable, relationship-eligible,
    and auto-maintained by PostgreSQL. Supports dot-path extraction for nested fields.
  use_case: JSONB field promotion to generated columns makes nested document fields indexable and relationship-eligible.
  code:
  - provisa/api_source/
  tests:
  - tests/unit/test_api_promotions.py
  - tests/integration/test_schema_compiler_integration.py
  since: 2026-01
  scenario: 'Given a JSONB column with nested fields

    When a steward promotes a nested field via dot-path

    Then a PostgreSQL generated column is created that is filterable, indexable, and

    relationship-eligible

    '
  integration_test: required
  integration_test_reason: Requires real PostgreSQL to validate generated column creation and maintenance.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - data-engineer
  competitive_position:
    status: ahead
    rationale: Steward-driven JSONB field promotion to PostgreSQL generated columns (filterable, indexable,
      relationship-eligible) is not available in Hasura, Trino, or Denodo as a first-class governed operation.
      Competitors leave nested JSON flattening to ETL or ad-hoc SQL views.
- id: REQ-133
  status: complete
  group: 3. Source Registration & Data Modeling
  category: Views (Governed Computed Datasets)
  priority: MUST
  type: structural
  description: Views are SQL-defined computed datasets registered in the Provisa config with full column-level
    governance (visibility, masking, descriptions, aliases).
  use_case: SQL-defined governed views let stewards expose computed datasets with full column-level governance
    applied.
  code:
  - provisa/compiler/
  - provisa/mv/
  tests:
  - tests/unit/test_mv_registry.py
  - tests/e2e/test_mv_optimization.py
  since: 2026-01
  scenario: 'Given a SQL-defined view registered in the Provisa config

    When the schema is generated

    Then the view appears with full column-level governance including visibility, masking,

    descriptions, and aliases

    '
  integration_test: not_required
  integration_test_reason: View registration and config model are data structure concerns; can be tested
    with fixtures.
  stakeholders:
  - data-engineer
  competitive_position:
    status: ahead
    rationale: SQL-defined views with full column-level governance (visibility, masking, descriptions,
      aliases) applied uniformly is stronger than Hasura (no native masking on views) and Trino/Dremio
      (no column-level governance on virtual views without external policy engines).
- id: REQ-134
  status: complete
  group: 3. Source Registration & Data Modeling
  category: Views (Governed Computed Datasets)
  priority: MUST
  type: behavioral
  description: Views go through the same governance pipeline as tables — RLS, masking, sampling, role-based
    schema visibility, approval workflow.
  use_case: Views through the same governance pipeline as tables ensures computed datasets receive identical
    security treatment.
  code:
  - provisa/compiler/
  - provisa/mv/
  tests:
  - tests/unit/test_mv_registry.py
  - tests/e2e/test_mv_optimization.py
  - tests/integration/test_schema_compiler_integration.py
  since: 2026-01
  scenario: 'Given a registered view with RLS rules and masking applied

    When a consumer queries the view

    Then RLS, masking, sampling, and role-based visibility are enforced identically to a table

    '
  integration_test: required
  integration_test_reason: Requires real data environment to verify governance pipeline (RLS, masking,
    sampling) applies to views.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - compliance
  - data-engineer
  competitive_position:
    status: ahead
    rationale: Views through the identical governance pipeline as tables (RLS, masking, sampling, approval)
      is not the default in any major competitor; Hasura separates view and table permission models, Denodo
      applies governance via separate policy objects.
- id: REQ-135
  status: complete
  group: 3. Source Registration & Data Modeling
  category: Views (Governed Computed Datasets)
  priority: SHOULD
  type: behavioral
  description: 'Views with `materialize: true` are backed by a periodically refreshed MV (CTAS). Views
    without materialization run as live subqueries via Trino.'
  use_case: Materialized views enable pre-computed result caching, while live views support real-time
    freshness needs.
  code:
  - provisa/compiler/
  - provisa/mv/
  tests:
  - tests/unit/test_mv_registry.py
  - tests/e2e/test_mv_optimization.py
  - tests/integration/test_registration_governance_integration.py
  since: 2026-01
  scenario: 'Given a view registered with materialize: true

    When the view is queried

    Then it is served from the periodically refreshed materialized view; views without that flag

    run as live subqueries

    '
  integration_test: required
  integration_test_reason: Requires real Trino and MV system to validate materialization and refresh behavior.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - ops
  - data-engineer
  competitive_position:
    status: parity
    rationale: Materialized vs live view duality exists in Dremio (reflections), Snowflake (materialized
      views), and Denodo; this is a standard capability for data platforms.
  related_reqs:
  - REQ-826
  - REQ-844
  - REQ-843
  - REQ-848
- id: REQ-136
  status: complete
  group: 3. Source Registration & Data Modeling
  category: Views (Governed Computed Datasets)
  priority: MUST
  type: constraint
  description: Views are the governed mechanism for adding computed semantics (aggregations, transformations)
    to the platform. This preserves the GraphQL constraint that no new semantics can be added outside
    the platform.
  use_case: Views as the governed mechanism for computed semantics ensures all aggregations and transforms
    are auditable.
  code:
  - provisa/compiler/
  - provisa/mv/
  tests:
  - tests/unit/test_mv_registry.py
  - tests/e2e/test_mv_optimization.py
  - tests/integration/test_schema_compiler_integration.py
  since: 2026-01
  scenario: 'Given a consumer who wants to expose aggregated or transformed data

    When they attempt to do so outside of the view mechanism

    Then the platform rejects it; only views may introduce new computed semantics

    '
  integration_test: not_required
  integration_test_reason: View-based computed semantics constraint is architectural; can be validated
    with schema inspection.
  stakeholders:
  - data-engineer
  competitive_position:
    status: ahead
    rationale: Enforcing views as the only governed mechanism for computed semantics (no escape hatch)
      is a stricter governance contract than any competitor. Hasura, Denodo, and Trino all allow out-of-band
      transformations that bypass the platform's audit trail.
- id: REQ-151
  status: complete
  group: 3. Source Registration & Data Modeling
  category: Column Path Extraction
  priority: SHOULD
  type: behavioral
  description: Columns with `path` extract values from JSON source columns using PG `>>` syntax. SQLGlot
    transpiles to `json_extract_scalar` for Trino.
  use_case: Column path extraction from JSON columns makes nested document fields queryable as first-class
    columns.
  code:
  - provisa/compiler/sql_gen.py
  tests:
  - tests/unit/test_sql_gen.py
  - tests/integration/test_schema_compiler_integration.py
  since: 2026-01
  scenario: 'Given a column configured with a path pointing into a JSON source column

    When a query is compiled for a PostgreSQL source

    Then PG >> syntax is used; when compiled for Trino, json_extract_scalar is used

    '
  integration_test: required
  integration_test_reason: Requires real PostgreSQL and Trino to validate JSON path extraction via PG
    and transpilation to Trino.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - data-engineer
  competitive_position:
    status: parity
    rationale: JSON path column extraction with dialect-aware transpilation (PG `>>` vs Trino `json_extract_scalar`)
      is internal query compiler logic; Dremio and Trino handle similar JSON path extraction natively.
- id: REQ-152
  status: complete
  group: 3. Source Registration & Data Modeling
  category: Column Path Extraction
  priority: MUST
  type: behavioral
  description: Path columns on PostgreSQL sources route direct. Non-PG sources force Trino routing.
  use_case: Path column routing rules ensure JSON extraction uses the correct engine for each source type.
  code:
  - provisa/compiler/sql_gen.py
  tests:
  - tests/unit/test_sql_gen.py
  - tests/integration/test_registration_governance_integration.py
  since: 2026-01
  scenario: 'Given a table with path columns

    When the query engine routes a query

    Then PostgreSQL sources use the direct route and all non-PG sources are forced through Trino

    '
  integration_test: required
  integration_test_reason: Requires real PostgreSQL and non-PG sources to validate routing behavior based
    on source type.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - data-engineer
  - ops
  competitive_position:
    status: neutral
    rationale: ''
- id: REQ-153
  status: complete
  group: 3. Source Registration & Data Modeling
  category: Column Path Extraction
  priority: MUST
  type: constraint
  description: Path columns are read-only computed fields — mutations unaffected.
  use_case: Path columns are read-only, preventing mutations from targeting computed derived fields.
  code:
  - provisa/compiler/sql_gen.py
  tests:
  - tests/unit/test_sql_gen.py
  since: 2026-01
  scenario: 'Given a table with one or more path columns

    When a mutation is submitted targeting those columns

    Then the mutation is unaffected by the path column definition; path columns are not writable

    '
  integration_test: not_required
  integration_test_reason: Path column read-only constraint is a mutation routing rule; can be validated
    with fixtures.
  stakeholders:
  - data-engineer
  competitive_position:
    status: neutral
    rationale: ''
- id: REQ-154
  status: complete
  group: 3. Source Registration & Data Modeling
  category: Naming & Schema
  priority: MAY
  type: structural
  description: Optional `domain_prefix` prepends `domain_id__` (double underscore) to all GraphQL names.
  use_case: Domain prefix with double underscore lets stewards namespace all GraphQL names to avoid type
    conflicts.
  code:
  - provisa/compiler/
  tests:
  - tests/integration/test_schema_compiler_integration.py
  - tests/unit/test_naming.py
  - tests/unit/test_schema_service.py
  since: 2026-01
  scenario: 'Given a domain configured with domain_prefix enabled

    When the GraphQL schema is generated

    Then all GraphQL names are prefixed with domain_id__ (double underscore)

    '
  integration_test: not_required
  integration_test_reason: Domain prefix naming is pure schema transformation; can be tested without data
    sources.
  stakeholders:
  - app-developer
  competitive_position:
    status: neutral
    rationale: ''
- id: REQ-155
  status: complete
  group: 3. Source Registration & Data Modeling
  category: Naming & Schema
  priority: SHOULD
  type: structural
  description: Table and column `alias` fields override GraphQL names.
  use_case: Column and table aliases let stewards expose business-friendly names without altering underlying
    schema.
  code:
  - provisa/compiler/
  tests:
  - tests/integration/test_schema_compiler_integration.py
  - tests/unit/test_graphql_alias.py
  - tests/unit/test_naming.py
  - tests/unit/test_schema_service.py
  since: 2026-01
  scenario: 'Given a table or column with an alias field set

    When the GraphQL schema is generated

    Then the alias is used as the GraphQL name instead of the physical name

    '
  integration_test: not_required
  integration_test_reason: Alias override logic is name resolution logic; can be tested with fixtures.
  stakeholders:
  - data-engineer
  - app-developer
  competitive_position:
    status: parity
    rationale: Table and column aliasing for GraphQL names exists in Hasura (custom field names) and Denodo
      (alias layers); this is standard catalog metadata capability.
- id: REQ-156
  status: complete
  group: 3. Source Registration & Data Modeling
  category: Naming & Schema
  priority: SHOULD
  type: structural
  description: Table and column `description` fields included in GraphQL SDL.
  use_case: Description fields in SDL give query developers in-schema documentation without leaving the
    GraphQL tooling.
  code:
  - provisa/compiler/
  tests:
  - tests/unit/test_naming.py
  - tests/unit/test_schema_service.py
  since: 2026-01
  scenario: 'Given a table or column with a description field set

    When the GraphQL SDL is generated

    Then the description appears as a GraphQL docstring on that type or field

    '
  integration_test: not_required
  integration_test_reason: Description inclusion in SDL is schema generation logic; no runtime behavior
    to test.
  stakeholders:
  - app-developer
  competitive_position:
    status: parity
    rationale: Description fields in GraphQL SDL are standard in Hasura and Postgraphile; no competitive
      differentiation.
- id: REQ-157
  status: complete
  group: 3. Source Registration & Data Modeling
  category: Naming & Schema
  priority: SHOULD
  type: behavioral
  description: Order-by enum values preserve original column case (not uppercased).
  use_case: Preserved original column case in order-by enums prevents case mismatch errors in consumer
    queries.
  code:
  - provisa/compiler/
  tests:
  - tests/unit/test_naming.py
  - tests/unit/test_schema_service.py
  since: 2026-01
  scenario: 'Given a column with mixed-case name

    When order-by enum values are generated

    Then the original case is preserved without uppercasing

    '
  integration_test: not_required
  integration_test_reason: Order-by enum casing is string preservation logic; can be tested without data.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - app-developer
  competitive_position:
    status: neutral
    rationale: ''
- id: REQ-158
  status: complete
  group: 3. Source Registration & Data Modeling
  category: Auto-Materialized Relationships
  priority: SHOULD
  type: behavioral
  description: 'Cross-source relationships with `materialize: true` auto-generate MV definitions at startup.'
  use_case: Automatic MV generation for materialized cross-source relationships eliminates manual ETL
    setup for joins.
  code:
  - provisa/mv/
  tests:
  - tests/unit/test_mv_registry.py
  - tests/unit/test_mv_refresh.py
  - tests/integration/test_registration_governance_integration.py
  since: 2026-01
  scenario: 'Given a cross-source relationship configured with materialize: true

    When the platform starts up

    Then MV definitions are auto-generated for that relationship

    '
  integration_test: required
  integration_test_reason: Requires real Trino to validate automatic MV definition generation at startup.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - ops
  - data-engineer
  competitive_position:
    status: ahead
    rationale: Automatic MV generation at startup for cross-source materialized relationships is unique.
      Dremio's reflections are single-source; Hasura DDN has no automatic cross-source materialization;
      Denodo relies on manual cache configuration.
  related_reqs:
  - REQ-826
  - REQ-844
- id: REQ-159
  status: complete
  group: 3. Source Registration & Data Modeling
  category: Auto-Materialized Relationships
  priority: SHOULD
  type: constraint
  description: Only cross-source relationships generate MVs. Same-source relationships are already fast
    via direct routing.
  use_case: MVs only for cross-source relationships avoids unnecessary materialization overhead for same-
    source fast joins.
  code:
  - provisa/mv/
  tests:
  - tests/unit/test_mv_registry.py
  - tests/unit/test_mv_refresh.py
  since: 2026-01
  scenario: 'Given a same-source relationship

    When materialize: true is configured

    Then no MV is generated; MVs are only generated for cross-source relationships

    '
  integration_test: not_required
  integration_test_reason: MV generation scope (cross-source only) is a conditional logic rule; can be
    tested with fixtures.
  stakeholders:
  - data-engineer
  competitive_position:
    status: ahead
    rationale: Selective MV generation only for cross-source joins (not same-source) avoids unnecessary
      materialization overhead — Denodo and Dremio materialize more aggressively.
  related_reqs:
  - REQ-826
- id: REQ-160
  status: complete
  group: 3. Source Registration & Data Modeling
  category: Auto-Materialized Relationships
  priority: MUST
  type: behavioral
  description: Auto-MVs start STALE and are populated by the background refresh loop.
  use_case: Auto-MVs starting STALE ensures the refresh loop populates them before they serve live traffic.
  code:
  - provisa/mv/
  tests:
  - tests/unit/test_mv_registry.py
  - tests/unit/test_mv_refresh.py
  - tests/integration/test_registration_governance_integration.py
  since: 2026-01
  scenario: 'Given an auto-generated MV created at startup

    When it is first created

    Then its state is STALE and the background refresh loop populates it before it serves queries

    '
  integration_test: required
  integration_test_reason: Requires real MV system to validate staleness tracking and refresh loop interaction.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - ops
  - data-engineer
  competitive_position:
    status: neutral
    rationale: ''
- id: REQ-194
  status: complete
  group: 3. Source Registration & Data Modeling
  category: Naming Convention
  priority: MUST
  type: structural
  description: 'The naming authority is the single source of truth for all client-facing names. Physical
    backend column names are never exposed to clients. **Alias** (`column.alias` in config): the canonical
    name. SQL uses it verbatim (no convention applied). GQL applies its configured convention to it. CQL
    derives from GQL. **No alias**: each query language derives its name from the physical column name
    via its configured convention. **SQL convention**: configurable, default `snake_case`. Applied via
    `apply_sql_name()`. **GQL convention**: configurable (`apollo_graphql` default = camelCase, `hasura_graphql`
    = snake_case). Applied via `apply_gql_name()`. **CQL**: derived from the GQL name.'
  use_case: Consistent multi-language naming with a single authority ensures client-facing names are predictable
    and independent of physical schema.
  code:
  - provisa/compiler/
  tests:
  - tests/unit/test_graphql_alias.py
  - tests/unit/test_naming.py
  since: 2026-01
  scenario: 'Given a column with and without an alias configured

    When names are generated for SQL, GQL, and CQL

    Then the naming authority applies the configured convention consistently and physical names are

    never exposed

    '
  integration_test: not_required
  integration_test_reason: Naming authority and convention logic is pure transformation; can be validated
    with test fixtures.
  stakeholders:
  - app-developer
  - data-engineer
  competitive_position:
    status: ahead
    rationale: A single naming authority governing SQL, GQL, and CQL names with configurable per-language
      conventions (hiding physical names entirely) is not matched by any competitor. Trino exposes physical
      names; Hasura applies naming conventions only to GraphQL, not SQL or other protocols.
- id: REQ-195
  status: complete
  group: 3. Source Registration & Data Modeling
  category: Naming Convention
  priority: SHOULD
  type: structural
  description: 'Aligns with Hasura v2 `naming_convention` setting: `hasura-default` maps to `snake_case`,
    `graphql-default` maps to `camelCase`. DDN `namingConvention: graphql` maps to `camelCase`.'
  use_case: Hasura v2 parity — naming convention alignment lets teams familiar with Hasura conventions
    feel at home.
  code:
  - provisa/compiler/
  tests:
  - tests/unit/test_naming.py
  since: 2026-01
  scenario: 'Given a Provisa deployment configured with hasura-default naming

    When schema names are generated

    Then they use snake_case matching Hasura v2 hasura-default behavior

    '
  integration_test: not_required
  integration_test_reason: Hasura naming convention alignment is configuration mapping; no runtime integration
    required.
  stakeholders:
  - app-developer
  competitive_position:
    status: parity
    rationale: This aligns Provisa with Hasura v2 naming conventions to ease migration; it is compatibility
      work, not a differentiating capability.
- id: REQ-250
  status: complete
  group: 3. Source Registration & Data Modeling
  category: Registration & Governance
  priority: MUST
  type: structural
  description: All Trino catalog configuration must flow through Provisa's config YAML — users never manage
    Trino .properties files directly. Provisa generates catalog properties, table definition files, and
    auto-registers tables at runtime following the Kafka source config pattern as exemplar.
  use_case: Config-driven Trino catalog generation hides Trino properties files from operators entirely.
  code:
  - provisa/core/
  tests:
  - tests/e2e/test_kafka_flow.py
  - tests/integration/test_elasticsearch_introspect.py
  - tests/integration/test_introspect.py
  - tests/integration/test_nosql_catalogs.py
  - tests/integration/test_schema_gen.py
  - tests/unit/test_core_registration.py
  - tests/unit/test_prometheus_source.py
  - tests/unit/test_redis_source.py
  - tests/unit/test_trino_catalog_files.py
  since: 2026-01
  scenario: 'Given an operator configuring a new Trino-backed source

    When they edit only the Provisa config YAML

    Then Provisa generates all required Trino catalog properties and registers tables at runtime

    without any manual .properties file edits

    '
  integration_test: required
  integration_test_reason: Requires real Trino to validate config-driven catalog property generation and
    auto-registration.
  stakeholders:
  - ops
  - data-engineer
  competitive_position:
    status: ahead
    rationale: Config-YAML-driven Trino catalog generation with zero manual .properties file management
      is unique. Trino itself requires manual catalog .properties files; Dremio has a GUI but no YAML-as-single-source-of-truth
      model; Denodo has its own admin console.
  related_reqs:
  - REQ-825
- id: REQ-251
  status: complete
  group: 3. Source Registration & Data Modeling
  category: Registration & Governance
  priority: MUST
  type: structural
  description: 'Each NoSQL/non-relational source type requires a type-specific mapping DSL in the Provisa
    config to define how its native data structures map to relational tables. Examples: Redis key patterns
    → rows, hash fields → columns; Elasticsearch index patterns with nested field paths; Prometheus metric
    names with label-to-column mapping.'
  use_case: Type-specific mapping DSL lets stewards define how Redis keys, Elasticsearch fields, and Prometheus
    metrics become SQL columns.
  code:
  - provisa/core/
  tests:
  - tests/integration/test_introspect.py
  - tests/integration/test_nosql_catalogs.py
  - tests/integration/test_schema_gen.py
  - tests/unit/test_core_registration.py
  - tests/unit/test_table_search.py
  since: 2026-01
  scenario: 'Given a steward registering a NoSQL source such as Redis or Elasticsearch

    When they define the mapping DSL in the Provisa config

    Then native data structures are mapped to relational tables with typed columns

    '
  integration_test: required
  integration_test_reason: Requires real NoSQL sources (Redis, Elasticsearch, Prometheus) and Trino to
    validate type-specific mapping.
  stakeholders:
  - data-engineer
  competitive_position:
    status: parity
    rationale: Type-specific mapping DSLs for NoSQL sources (Redis, Elasticsearch, Prometheus) are supported
      natively by Trino connectors and Dremio source plugins; the DSL is Provisa's configuration layer
      on top of standard connector capability.
  related_reqs:
  - REQ-825
- id: REQ-363
  status: complete
  group: 3. Source Registration & Data Modeling
  category: Semantic Layer / Semantic Model
  priority: MUST
  type: behavioral
  description: 'The SQLAlchemy dialect introspects table and column metadata via `POST /data/graphql`
    (GraphQL introspection query) instead of any admin or unfiltered endpoint. This ensures the dialect''s
    `get_table_names()` and `get_columns()` responses are role-governed: the GraphQL server applies the
    Semantic Layer filter before returning the introspection result, so callers see only tables and columns
    their role is permitted to access.'
  use_case: SQLAlchemy dialect introspects via governed GraphQL endpoint; role determines table and column
    visibility.
  code:
  - provisa/compiler/
  - provisa/security/
  tests:
  - tests/unit/test_visibility.py
  - tests/unit/test_enforcement_metadata.py
  - tests/integration/test_registration_governance_integration.py
  - provisa-ui/e2e/registration-admin.spec.ts
  since: 2026-01
  scenario: 'Given a SQLAlchemy client using the Provisa dialect with a specific role

    When get_table_names() or get_columns() is called

    Then the results are filtered through the governed GraphQL introspection endpoint and only

    permitted tables and columns are returned

    '
  integration_test: required
  integration_test_reason: Requires real HTTP GraphQL endpoint to verify role-governed introspection via
    POST endpoint.
  e2e: true
  e2e_reason: Role-governed introspection via POST /data/graphql requires a real running server — only
    a live HTTP round-trip can confirm that the GraphQL endpoint filters tables and columns by role before
    returning results.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - compliance
  - app-developer
  competitive_position:
    status: ahead
    rationale: SQLAlchemy dialect introspection routed through the governed GraphQL endpoint (role-filtered
      results) is unique. Hasura's SQLAlchemy adapter exposes unfiltered catalog; Trino/Dremio JDBC drivers
      return all visible objects without governance-layer filtering.
- id: REQ-366
  status: complete
  group: 3. Source Registration & Data Modeling
  category: Registration & Governance
  priority: MUST
  type: behavioral
  description: 'Views require an approval workflow, OR the originator must already hold the rights to
    the underlying tables and to any joins used within the view. Any join within a view likewise requires
    approval or originator rights. Convenience views (adding no new semantics) are discouraged — instead
    grant the relationship rights and query in any form. Creating a view implies new semantics: derived/calculated
    values, or the view name itself as a new business concept. Approval gates therefore apply to both
    views (for the semantics they introduce, consistent with REQ-134) and relationships (for navigational
    intent).'
  use_case: View approval (or originator-rights) plus join approval ensures new semantics and cross-domain
    imports are governed; convenience views are discouraged in favor of relationship rights.
  code:
  - provisa/core/
  tests:
  - tests/integration/test_schema_gen.py
  - tests/unit/test_core_registration.py
  since: 2026-01
  scenario: 'Given a user attempting to create a view over tables they do not own

    When they submit the view creation

    Then an approval workflow is triggered unless the originator already holds rights to all

    underlying tables and joins

    '
  integration_test: not_required
  integration_test_reason: View approval logic is authorization policy; can be tested with mocked role/rights
    checks.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - compliance
  - data-engineer
  competitive_position:
    status: ahead
    rationale: View creation gated by approval workflow or originator rights (with join-level approval)
      is a governance contract not available in any competitor. Hasura, Dremio, and Denodo allow view
      creation based on ACLs but have no formal approval queue enforcing steward sign-off.
- id: REQ-367
  status: complete
  group: 3. Source Registration & Data Modeling
  category: Domain Model
  priority: MUST
  type: structural
  description: 'There is only one view type — always intradomain (the output belongs to a domain). Two
    valid reasons to create a view: (1) the source is cross-domain (importing external data into the domain
    as a named business concept), (2) the source is local (deriving new or calculated data from existing
    domain assets). Two hard constraints follow: cross-domain data may only enter a domain via a view,
    and new or derived data may only exist as a view. No other reason to create a view is valid; there
    is no separate cross-domain view type — just views, always intradomain, used for exactly these two
    purposes.'
  use_case: Unified view model with two hard constraints ensures cross-domain imports are read-only adapters
    while computed data is always created within a single domain.
  code:
  - provisa/compiler/sql_gen.py
  - provisa/mv/
  tests:
  - tests/unit/test_domain_views.py
  since: 2026-01
  scenario: 'Given a request to create a view in a domain

    When the view source is cross-domain or derives new data locally

    Then the view is valid; any other motivation is rejected as it violates the two-purpose rule

    '
  integration_test: not_required
  integration_test_reason: View type constraints are architectural rules; can be validated via static
    config analysis.
  stakeholders:
  - data-engineer
  competitive_position:
    status: ahead
    rationale: Enforcing a two-purpose-only view model (cross-domain import or local derivation, nothing
      else) as a hard platform constraint is unique; competitors treat views as general-purpose objects
      with no semantic intent enforcement.
- id: REQ-392
  status: complete
  group: 3. Source Registration & Data Modeling
  category: Domain Model
  priority: SHOULD
  type: structural
  description: 'Schema endpoint returns node_labels with a `pk: string | null` field (first PK column)
    and a `pk_columns: string[]` field per label, designating the primary key column name(s) (absorbs
    REQ-398). Allows graph UI to construct reliable `WHERE NOT n.<pk> IN [<value>]` exclusion clauses
    (instead of heuristic-based `id(n)` fallback), enabling "Exclude from query", deep-linking to nodes,
    "show related to this entity", and inspector PK highlighting.'
  use_case: Explicit PK designation in schema enables semantic graph operations and node-centric navigation
    without node ID heuristics.
  code:
  - provisa/api/rest/cypher_router.py
  - provisa/cypher/label_map.py
  tests:
  - tests/unit/test_core_registration.py
  - tests/unit/test_graph_schema.py
  - tests/unit/test_graph_schema_pk.py
  - tests/unit/test_relationship_alias.py
  - tests/unit/test_schema_pk_requirements.py
  since: 2026-01
  scenario: 'Given a schema endpoint response for a graph query

    When node_labels are returned

    Then each label includes a pk field designating the primary key column name or null

    '
  integration_test: not_required
  integration_test_reason: Primary key designation in schema endpoint is data structure representation;
    can be tested with fixtures.
  e2e: true
  e2e_reason: The pk field per node_label in the schema REST endpoint response must be verified against
    a real running server — unit fixtures cannot confirm the network response shape returned by the live
    cypher_router.
  stakeholders:
  - app-developer
  competitive_position:
    status: ahead
    rationale: Exposing pk per node label in the schema endpoint to drive graph UI exclusion queries is
      a unique integration between the semantic layer and graph visualization not present in competitors.
- id: REQ-393
  status: complete
  group: 3. Source Registration & Data Modeling
  category: Domain Model
  priority: SHOULD
  type: structural
  description: 'The semantic layer must support user-designated primary key columns per table column via
    an `is_primary_key: bool` field. Designation is informational only — Provisa does not enforce uniqueness;
    the underlying datastore is authoritative.'
  use_case: User-designated PKs enable semantic graph operations without requiring Provisa to validate
    database constraints.
  code:
  - provisa/core/
  - provisa/compiler/
  tests:
  - tests/unit/test_core_registration.py
  - tests/unit/test_domain_table_uniqueness.py
  since: 2026-01
  scenario: 'Given a steward designating a column as primary key via is_primary_key: true

    When the schema is compiled

    Then the designation is recorded as informational metadata without uniqueness enforcement

    '
  integration_test: not_required
  integration_test_reason: User-designated PK field is configuration metadata; no enforcement required
    from Provisa.
  stakeholders:
  - data-engineer
  competitive_position:
    status: parity
    rationale: User-designated PK columns are a standard semantic layer feature present in Hasura, Denodo,
      and dbt.
- id: REQ-394
  status: complete
  group: 3. Source Registration & Data Modeling
  category: Domain Model
  priority: SHOULD
  type: behavioral
  description: Multiple PK checkboxes on a table infer a composite key; the first designated PK column
    is used as the canonical `id_column` for Cypher node identity resolution, taking priority over all
    heuristics.
  use_case: First PK column as canonical id_column ensures consistent node identity across Cypher graph
    operations.
  code:
  - provisa/cypher/
  - provisa/compiler/
  tests:
  - tests/unit/test_core_registration.py
  since: 2026-01
  scenario: 'Given a table with multiple columns designated as primary keys

    When Cypher node identity resolution runs

    Then the first designated PK column is used as the canonical id_column

    '
  integration_test: not_required
  integration_test_reason: Composite key canonical selection is pure resolution logic; can be tested with
    fixtures.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - app-developer
  competitive_position:
    status: ahead
    rationale: Composite PK inference with deterministic canonical id_column resolution for Cypher node
      identity is more sophisticated than Hasura's single-column PK approach.
- id: REQ-399
  status: complete
  group: 3. Source Registration & Data Modeling
  category: Domain Model
  priority: SHOULD
  type: structural
  description: When a Relationship is saved, the source_column on the source table is marked as `is_foreign_key=true`
    in table_columns. Two new boolean columns `is_foreign_key` and `is_alternate_key` are added to table_columns
    via `ALTER TABLE ... ADD COLUMN IF NOT EXISTS`.
  use_case: Foreign key and alternate key tracking on columns enables semantic relationship metadata without
    external constraints.
  code:
  - provisa/core/schema.sql
  - provisa/core/repositories/
  tests:
  - tests/unit/test_core_registration.py
  since: 2026-01
  scenario: 'Given a steward saving a relationship between two tables

    When the relationship is persisted

    Then the source_column is marked is_foreign_key=true in table_columns

    '
  integration_test: required
  integration_test_reason: Requires real PostgreSQL to validate ALTER TABLE ADD COLUMN and tracking of
    is_foreign_key status.
  stakeholders:
  - data-engineer
  - ops
  competitive_position:
    status: parity
    rationale: Automatic FK/PK marking on relationship save is standard in Hasura and most ORMs.
- id: REQ-400
  status: complete
  group: 3. Source Registration & Data Modeling
  category: Domain Model
  priority: SHOULD
  type: behavioral
  description: When a Relationship is saved, the target_column on the target table is marked as `is_primary_key=true`
    if no other column in that table already has `is_primary_key=true`; otherwise it is marked as `is_alternate_key=true`.
  use_case: Automatic PK/AK designation on relationship target columns reduces steward manual key configuration
    work.
  code:
  - provisa/core/repositories/relationship.py
  - provisa/core/models.py
  tests:
  - tests/unit/test_core_registration.py
  since: 2026-01
  scenario: 'Given a relationship being saved where the target table has no existing primary key

    When the relationship is persisted

    Then the target_column is marked is_primary_key=true; if a PK already exists it is marked

    is_alternate_key=true

    '
  integration_test: not_required
  integration_test_reason: Automatic PK/AK designation on save is state management logic; can be tested
    with fixtures.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - data-engineer
  competitive_position:
    status: parity
    rationale: Automatic PK inference on relationship target is standard in Hasura and most ORMs.
- id: REQ-413
  status: complete
  group: 3. Source Registration & Data Modeling
  category: Registration & Governance
  priority: SHOULD
  type: behavioral
  description: Auto-generate GQL relationships from FK constraints in database schema introspection —
    relationships discoverable from FK metadata in addition to manual steward configuration and AI-assisted
    hints.
  use_case: Auto-generated FK relationships reduce manual relationship registration overhead while preserving
    steward control.
  code:
  - provisa/compiler/introspect.py
  tests:
  - tests/unit/test_discovery_requirements.py
  - tests/integration/test_registration_governance_integration.py
  - provisa-ui/e2e/registration-admin.spec.ts
  since: 2026-01
  scenario: 'Given a database source with FK constraints

    When schema introspection runs

    Then GQL relationships are auto-generated from the FK metadata

    '
  integration_test: required
  integration_test_reason: Requires real database with FK constraints to validate auto-generation of GQL
    relationships.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - data-engineer
  competitive_position:
    status: parity
    rationale: Auto-generating relationships from FK constraints is standard in Hasura (auto-track foreign
      keys) and Denodo; no differentiation.
- id: REQ-414
  status: complete
  group: 3. Source Registration & Data Modeling
  category: Registration & Governance
  priority: SHOULD
  type: behavioral
  description: Demo/install example schema must include at least one FK relationship to exercise auto-generated
    relationship discovery.
  use_case: Demo schema coverage of FK auto-discovery ensures feature is validated during onboarding.
  code:
  - demo/files/create_demo_files.py
  tests:
  - tests/e2e/test_installer.py
  - tests/unit/test_core_registration.py
  - tests/integration/test_registration_governance_integration.py
  - provisa-ui/e2e/registration-admin.spec.ts
  since: 2026-01
  scenario: 'Given the demo installation schema

    When relationship discovery runs

    Then at least one FK relationship is auto-discovered and exercised

    '
  integration_test: required
  integration_test_reason: Requires end-to-end install with demo schema to verify FK relationship discovery
    works.
  e2e: true
  e2e_reason: The demo installer's FK auto-discovery can only be confirmed end-to-end — it requires the
    full live install path (real DB, schema registration, and relationship discovery) that unit and integration
    fixtures cannot replicate.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - app-developer
  - ops
  competitive_position:
    status: neutral
    rationale: ''
- id: REQ-415
  status: complete
  group: 3. Source Registration & Data Modeling
  category: Registration & Governance
  priority: MAY
  type: behavioral
  description: The `hasura_v2_relationship_style` option controls whether FK-derived relationships use
    Hasura V2's naming conventions — singular for many-to-one, plural for one-to-many using inflection.
  use_case: V2-compatible relationship naming enables teams to adopt Hasura conventions without schema
    migration.
  code:
  - provisa/compiler/introspect.py
  tests:
  - tests/unit/test_discovery_requirements.py
  - tests/unit/test_fk_relationship_style.py
  since: 2026-01
  scenario: 'Given hasura_v2_relationship_style enabled

    When FK-derived relationships are named

    Then many-to-one names are singular and one-to-many names are plural via inflection

    '
  integration_test: not_required
  integration_test_reason: Relationship naming convention selection is configuration logic; can be tested
    with fixtures.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - app-developer
  - data-engineer
  competitive_position:
    status: parity
    rationale: Hasura v2 naming convention compatibility (singular/plural inflection for FK-derived relationships)
      matches Hasura's own behavior; this is parity-by-design, not differentiation.
- id: REQ-417
  status: complete
  group: 3. Source Registration & Data Modeling
  category: Registration & Governance
  priority: SHOULD
  type: behavioral
  description: Hasura v2 migration tool (`provisa/hasura_v2/mapper.py`) maps Hasura Remote Schemas to
    Provisa `graphql_remote` source registrations instead of skipping them with "NOT SUPPORTED" warning.
    Provisa already has GraphQL Remote source capability (`graphql_remote_adapter.py` and `graphql_remote_router.py`);
    the mapper was a gap. Migration preserves Remote Schema name, URL, headers, and authentication configuration
    in the registered source.
  use_case: Hasura Remote Schema migration removes GTM blocker — customers see supported sources instead
    of "NOT SUPPORTED" during migration path evaluation.
  code:
  - provisa/hasura_v2/mapper.py
  - provisa/source_adapters/graphql_remote_adapter.py
  - provisa/api/admin/graphql_remote_router.py
  tests:
  - tests/integration/test_graphql_remote_source.py
  - tests/unit/test_core_registration.py
  - tests/unit/test_hasura_remote_schema.py
  - tests/unit/test_hasura_v2_comprehensive.py
  - tests/unit/test_import_shared.py
  since: 2026-01
  scenario: 'Given a Hasura v2 metadata file containing Remote Schema entries

    When the migration tool runs

    Then each Remote Schema is mapped to a graphql_remote source registration preserving name, URL,

    headers, and auth

    '
  integration_test: required
  integration_test_reason: Requires real Hasura v2 Remote Schema endpoint and Provisa graphql_remote adapter
    to validate migration.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - app-developer
  competitive_position:
    status: ahead
    rationale: A dedicated Hasura v2 migration tool that maps Remote Schemas to governed graphql_remote
      source registrations is unique; no other federation platform provides a structured migration path
      from Hasura specifically. This directly removes a GTM adoption blocker.
- id: REQ-418
  status: complete
  group: 3. Source Registration & Data Modeling
  category: Domain Model
  priority: MUST
  type: behavioral
  description: 'Report authoring workflow: analysts pull cross-domain data into their own domain via views
    (data-import adapters), then define calculations and relationships only within that domain. New or
    derived data exists only as views; no direct cross-domain calculations or relationships.'
  use_case: 'Unified view model ensures domain logic stays local and views serve exactly two purposes:
    importing external data and creating derived data within a single domain.'
  code:
  - provisa/compiler/
  - provisa/mv/
  tests:
  - tests/unit/test_domain_views.py
  since: 2026-01
  scenario: 'Given an analyst building a cross-domain report

    When they import cross-domain data

    Then it must be done via views; all calculations and relationships are defined only within the

    analyst''s own domain

    '
  integration_test: not_required
  integration_test_reason: Report authoring workflow is user guidance; enforcement via view/relationship
    approval rules.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - data-engineer
  competitive_position:
    status: ahead
    rationale: Domain-scoped report authoring workflow (cross-domain data enters only via views, all calculations
      local) enforced at the platform level is unique; Hasura DDN and Denodo allow ad-hoc cross-domain
      joins in views without domain boundary enforcement.
- id: REQ-432
  status: complete
  group: 3. Source Registration & Data Modeling
  category: Registration & Governance
  priority: MUST
  type: constraint
  description: A registered table must be unique by (domain_id, table_name) — stronger guarantee than
    the DB constraint UNIQUE(source_id, schema_name, table_name). Table registration (admin registerTable/updateTable
    mutations) rejects any collision of domain+table against a different physical table. Startup/schema-rebuild
    validates the entire registry and fails if any duplicate exists.
  use_case: Domain+table uniqueness prevents ambiguous table references across all query layers (GraphQL,
    Cypher, view SQL).
  code:
  - provisa/core/
  tests:
  - tests/unit/test_core_registration.py
  - tests/unit/test_schema_visibility_filters.py
  since: 2026-01
  scenario: 'Given an attempt to register a table under a (domain_id, table_name) pair already in use

    When the registerTable mutation is submitted

    Then the registration is rejected with a uniqueness collision error

    '
  integration_test: not_required
  integration_test_reason: Domain+table uniqueness constraint is a validation rule; can be tested with
    test fixtures.
  stakeholders:
  - data-engineer
  competitive_position:
    status: parity
    rationale: Unique constraint on (domain_id, table_name) is standard data modeling practice.
- id: REQ-433
  status: complete
  group: 3. Source Registration & Data Modeling
  category: Registration & Governance
  priority: MUST
  type: behavioral
  description: A datasource may be associated with multiple domains. Any domain owner may register any
    unclaimed table from that source. Once a table is claimed by one domain, no other domain may register
    it — first-come ownership model. Unique constraint enforced on (source_id, normalized_table_name).
    The UI greys out claimed tables regardless of which domain claimed them.
  use_case: Multi-domain with first-claim ownership prevents the same physical table from being registered
    multiple times while allowing flexible domain-to-source associations.
  code:
  - provisa/core/
  - provisa-ui/src/pages/TablesPage
  tests:
  - tests/unit/test_dataset_uniqueness.py
  - provisa-ui/e2e/registration-admin.spec.ts
  since: 2026-01
  scenario: 'Given a datasource shared across multiple domains

    When a domain owner claims a table

    Then no other domain may claim that same physical table; the UI greys it out for all other

    domains

    '
  integration_test: not_required
  integration_test_reason: First-claim ownership model is state management; can be tested without real
    data sources.
  e2e: true
  e2e_reason: The UI greying-out of claimed tables across domains requires a browser- level E2E test —
    the visual behaviour and UI state cannot be verified by unit or integration tests alone.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - data-engineer
  competitive_position:
    status: ahead
    rationale: Multi-domain first-claim ownership of physical tables (with UI greying of claimed tables)
      is a unique governance model; Hasura, Trino, and Denodo have no concept of domain ownership with
      exclusive first-claim semantics on physical tables.
- id: REQ-434
  status: complete
  group: 3. Source Registration & Data Modeling
  category: Registration & Governance
  priority: SHOULD
  type: behavioral
  description: 'Creation-request mechanism: any governed create operation (view, relationship, etc.) attempted
    by a user lacking the authority to perform it produces a persisted request rather than an error. The
    request enters a queue (REQ-063) visible to every user holding the rights to execute that create.
    An authorized user may execute or reject the request; rejection carries a specific, actionable reason.
    No create is performed until an authorized user executes the request.'
  use_case: Creation-request queue lets users without create authority propose views/relationships for
    an authorized holder to execute.
  code:
  - provisa/core/
  - provisa-ui/src/
  tests:
  - tests/unit/test_core_registration.py
  - tests/unit/test_creation_requests.py
  since: 2026-01
  scenario: 'Given a user without create authority attempting to create a view or relationship

    When they submit the creation

    Then a persisted request is created in the queue rather than an error; an authorized user may

    execute or reject it

    '
  integration_test: not_required
  integration_test_reason: Creation-request queue is authorization and persistence logic; can be tested
    with mocks.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - compliance
  - data-engineer
  competitive_position:
    status: ahead
    rationale: A persisted creation-request queue (rather than an error) for governed operations is a
      unique workflow primitive; competitors return permission errors and leave the coordination problem
      to external processes.
- id: REQ-542
  status: complete
  group: 3. Source Registration & Data Modeling
  category: Naming & Schema
  priority: SHOULD
  type: behavioral
  description: Regex naming rules in config are applied to table names in order when generating GraphQL
    field names, before uniqueness resolution.
  use_case: Ordered naming rules let stewards strip common prefixes (e.g., prod_pg_) from auto-generated
    GraphQL names without manual aliasing.
  code:
  - provisa/compiler/naming.py
  tests:
  - tests/unit/test_naming.py
  - tests/unit/test_schema_service.py
  since: 2026-01
  scenario: 'Given a config with ordered regex naming rules

    When GraphQL field names are generated for table names

    Then each rule is applied in order before uniqueness resolution

    '
  integration_test: not_required
  integration_test_reason: Regex naming rule application is string transformation logic; can be tested
    with fixtures.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - data-engineer
  competitive_position:
    status: ahead
    rationale: Ordered regex naming rules applied before GraphQL name generation give stewards fine-grained
      control over schema shape not available in Hasura or Trino.
- id: REQ-543
  status: complete
  group: 3. Source Registration & Data Modeling
  category: Auto-Materialized Relationships
  priority: MUST
  type: behavioral
  description: Mutations to source tables of a materialized cross-source relationship mark the corresponding
    MV as stale for re-refresh. The `refresh_interval` defaults to 300 seconds (5 minutes) when not explicitly
    set.
  use_case: Mutation-triggered staleness ensures the MV is refreshed promptly after writes, keeping cached
    join results consistent.
  code:
  - provisa/mv/
  tests:
  - tests/unit/test_mv_refresh.py
  - tests/integration/test_registration_governance_integration.py
  since: 2026-01
  scenario: 'Given a materialized cross-source relationship MV

    When a mutation is applied to one of its source tables

    Then the MV is marked stale and scheduled for re-refresh within the refresh_interval

    '
  integration_test: required
  integration_test_reason: Requires real MV system to validate staleness marking on mutations and default
    refresh interval.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - ops
  - data-engineer
  related_reqs:
  - REQ-856
  competitive_position:
    status: ahead
    rationale: Mutation-triggered MV staleness marking with configurable refresh interval for cross-source
      materialized relationships is not available in any direct competitor. Dremio reflections are time-based
      only; Denodo caches have no mutation-aware invalidation.
- id: REQ-604
  status: complete
  group: 3. Source Registration & Data Modeling
  category: Naming & Schema
  priority: SHOULD
  type: structural
  description: Relationships support an optional `graphql_alias` field that names the SDL field exposed
    on the parent type. When `graphql_alias` is absent, the SDL field name is derived from the target
    table's `field_name` and the relationship cardinality via `rel_field_name(target.field_name, cardinality)`
    (singular for many-to-one, plural for one-to-many).
  use_case: graphql_alias lets stewards expose relationship traversal fields under business-friendly names
    independent of the target table's raw field_name.
  code:
  - provisa/compiler/schema_gen.py
  tests:
  - tests/unit/test_graphql_alias.py
  - tests/unit/test_naming.py
  - tests/unit/test_schema_service.py
  since: 2026-01
  scenario: 'Given a relationship with graphql_alias set

    When the SDL is generated

    Then the relationship field on the parent type uses the graphql_alias value

    '
  integration_test: not_required
  integration_test_reason: Relationship graphql_alias field is schema naming logic; can be tested without
    data.
  stakeholders:
  - app-developer
  competitive_position:
    status: ahead
    rationale: Per-relationship graphql_alias overrides for SDL field naming provide schema control beyond
      what Hasura v2 offers and not present in Trino or Dremio.
- id: REQ-605
  status: complete
  group: 3. Source Registration & Data Modeling
  category: Registration & Governance
  priority: SHOULD
  type: behavioral
  description: 'When `root_table_ids` is set on a `SchemaInput`, tables whose IDs are absent from that
    set are excluded from root query fields in the generated SDL but remain present as GraphQL named types
    reachable via relationship fields. Domain access filtering applies the same mechanism: tables in inaccessible
    domains become type-defs only. Full removal of a type (including its definition) requires deleting
    the table registration entirely.'
  use_case: Type-def-only tables let stewards expose navigable object graphs where some types are reachable
    only via relationship traversal, not by independent client query, without requiring separate registration.
  code:
  - provisa/compiler/schema_gen.py
  tests:
  - tests/unit/test_visibility.py
  since: 2026-01
  scenario: 'Given a SchemaInput with root_table_ids set excluding some tables

    When the SDL is generated

    Then excluded tables are present as named types but absent from root query fields

    '
  integration_test: not_required
  integration_test_reason: Root table filtering and type-def-only visibility is schema generation logic;
    can be tested with fixtures.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - compliance
  - app-developer
  competitive_position:
    status: ahead
    rationale: Type-def-only table exposure (queryable only via relationship traversal, not as root query
      fields) as a first-class registration feature is unique; Hasura and Denodo expose all registered
      tables as top-level queryable objects with no traversal-only mode.
- id: REQ-609
  status: complete
  group: 3. Source Registration & Data Modeling
  category: Domain Model
  priority: MUST
  type: constraint
  description: Every domain must have a designated steward before it can serve governed data. A domain
    may exist in a pending state until a steward is assigned; a domain without a steward cannot expose
    tables, views, or relationships to consumers.
  use_case: Steward requirement ensures accountability is always assigned before governed data is exposed.
  code:
  - provisa/core/domain_policy.py
  tests:
  - tests/unit/test_domain_policy.py
  - tests/unit/test_metadata_snapshot_builder.py
  - tests/unit/test_metadata_export_atlas_native.py
  since: 2026-01
  scenario: 'Given a domain with no steward assigned

    When a consumer attempts to access tables, views, or relationships from that domain

    Then access is denied until a steward is designated

    '
  integration_test: not_required
  integration_test_reason: Steward requirement is a data model constraint; can be validated via table
    inspection.
  stakeholders:
  - compliance
  competitive_position:
    status: ahead
    rationale: Platform-enforced steward assignment as a hard prerequisite before any governed data is
      exposed is unique; Hasura and Dremio have no concept of a mandatory data steward gating table availability.
- id: REQ-610
  status: complete
  group: 3. Source Registration & Data Modeling
  category: Domain Model
  priority: SHOULD
  type: behavioral
  description: A field access grant belongs to the requesting domain, not to the specific view that prompted
    it. Any subsequent view in the requesting domain may use the granted fields without additional cross-domain
    approval. New fields not covered by the existing grant require a new request.
  use_case: Domain-scoped field grants prevent repeated approval ceremonies for the same cross-domain
    data access pattern.
  code:
  - provisa/core/
  tests:
  - tests/unit/test_core_registration.py
  since: 2026-01
  scenario: 'Given a domain that has received a cross-domain field access grant

    When a new view in that domain uses the granted fields

    Then no additional approval is required; only new fields outside the grant trigger a new request

    '
  integration_test: not_required
  integration_test_reason: Domain-scoped field grants are state management; can be tested with persisted
    fixtures.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - compliance
  - data-engineer
  competitive_position:
    status: ahead
    rationale: Domain-scoped field grants (one approval covers all future views in the requesting domain
      using those fields) is a governance model not available in any competitor; Hasura, Denodo, and Snowflake
      require per-object permission grants.
- id: REQ-611
  status: complete
  group: 3. Source Registration & Data Modeling
  category: Registration & Governance
  priority: SHOULD
  type: structural
  description: 'Discovery is structured across five tiers of increasing governance: (1) Registered source
    schema — raw inventory, admin-level visibility; (2) Unclaimed tables — introspected from registered
    sources with no domain owner; (3) Domain assets — claimed tables and steward-defined views, fully
    governed; (4) Relationships — approved traversal paths between Tier 3 assets; (5) Field grants — domain-to-domain
    field access permissions. Each tier is a prerequisite for the next.'
  use_case: Structured discovery tiers give stewards and analysts a clear mental model of what data is
    governed and at what level.
  code:
  - provisa/core/
  - provisa/discovery/
  tests:
  - tests/unit/test_discovery_requirements.py
  since: 2026-01
  scenario: 'Given a newly registered source

    When a steward navigates the discovery workflow

    Then each of the five governance tiers is presented in sequence as prerequisites are met

    '
  integration_test: not_required
  integration_test_reason: Discovery tier structure is API design; tiers can be tested independently with
    fixtures.
  stakeholders:
  - app-developer
  - data-engineer
  competitive_position:
    status: ahead
    rationale: A structured five-tier discovery model (raw inventory -> unclaimed -> domain assets ->
      relationships -> field grants) with explicit prerequisites is unique; Trino, Hasura, and Dremio
      have flat catalog browsing with no governed discovery workflow.
- id: REQ-612
  status: complete
  group: 3. Source Registration & Data Modeling
  category: Registration & Governance
  priority: SHOULD
  type: behavioral
  description: 'Relationship candidates are ranked by a four-level confidence hierarchy: (Highest) Approved
    catalog relationship validated by both stewards; (High) Intra-source FK constraint — explicit modeling
    intent; (Medium) Intra-source semantic inference — column name/type similarity; (Low) Cross-source
    semantic inference — naming conventions diverge, high false-positive risk. Candidates corroborated
    by multiple evidence types accumulate confidence.'
  use_case: Confidence hierarchy lets stewards prioritize which relationship suggestions to review first,
    reducing false-positive noise.
  code:
  - provisa/discovery/analyzer.py
  - provisa/discovery/collector.py
  tests:
  - tests/unit/test_discovery_requirements.py
  since: 2026-01
  scenario: 'Given multiple relationship candidates of varying evidence types

    When candidates are presented to a steward

    Then they are ranked by confidence from approved catalog down to cross-source semantic inference

    '
  integration_test: not_required
  integration_test_reason: Relationship confidence ranking is algorithmic scoring; can be tested with
    synthetic candidates.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - data-engineer
  competitive_position:
    status: ahead
    rationale: Four-level confidence hierarchy for relationship candidates (approved > FK > intra-source
      semantic > cross-source semantic) with multi-evidence accumulation is more sophisticated than any
      competitor; Hasura and Denodo surface FK suggestions only, with no ranked inference system.
- id: REQ-635
  status: complete
  group: 3. Source Registration & Data Modeling
  category: Registration & Governance
  priority: MUST
  type: behavioral
  description: The schema name presented to users must be the name the data source itself uses to group
    datasets. For relational databases this is the native schema (or database for MySQL). For flat/API
    sources with no native grouping concept, a fixed constant naming the source type is used.
  use_case: Schema names derived from native source grouping prevent user confusion and keep the semantic
    layer consistent with the underlying data source.
  code:
  - provisa/api/admin/introspect.py
  tests:
  - tests/integration/test_introspect.py
  - tests/unit/test_core_registration.py
  since: 2026-01
  scenario: 'Given a relational database source

    When available schemas are listed

    Then the native schema names are presented; for flat/API sources a fixed source-type constant

    is used

    '
  integration_test: required
  integration_test_reason: Requires real data sources to validate schema names derived from native source
    grouping.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - data-engineer
  - app-developer
  competitive_position:
    status: neutral
    rationale: ''
- id: REQ-636
  status: complete
  group: 3. Source Registration & Data Modeling
  category: Registration & Governance
  priority: MUST
  type: behavioral
  description: When a Trino connector is configured for a source type (i.e., the type is in SOURCE_TO_CONNECTOR),
    Trino is the preferred path for schema and table introspection. Native driver introspection is only
    used for source types with no Trino connector or those that override via native_schemas/native_tables
    returning a non-None value.
  use_case: Trino-first introspection ensures schema discovery uses the same path as query execution,
    keeping introspection results consistent with what is queryable.
  code:
  - provisa/api/admin/introspect.py
  - provisa/core/models.py
  tests:
  - tests/integration/test_introspect.py
  - tests/unit/test_core_registration.py
  - tests/unit/test_native_introspect.py
  - tests/unit/test_compiler_introspect.py
  since: 2026-01
  scenario: 'Given a source type with a Trino connector configured

    When schema or table introspection is triggered

    Then Trino is used as the introspection path; native driver is used only when no connector

    exists

    '
  integration_test: required
  integration_test_reason: Requires real Trino and native driver introspection to validate source-type-based
    routing.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - data-engineer
  - ops
  competitive_position:
    status: neutral
    rationale: ''
  related_reqs:
  - REQ-825
- id: REQ-637
  status: complete
  group: 3. Source Registration & Data Modeling
  category: Registration & Governance
  priority: MUST
  type: constraint
  description: PG cache schemas, Trino catalog names, and other implementation-layer namespaces must never
    appear as schema or table names presented to the user. Only native source schema/table names and registered
    aliases are exposed.
  use_case: Hiding implementation namespaces prevents users from seeing or depending on internal infrastructure
    details that may change.
  code:
  - provisa/api/admin/introspect.py
  tests:
  - tests/integration/test_introspect.py
  - tests/unit/test_catalog_cache.py
  - tests/unit/test_core_registration.py
  since: 2026-01
  scenario: 'Given a schema or table listing request

    When the response is built

    Then PG cache schemas and Trino catalog names are filtered out; only native and alias names

    appear

    '
  integration_test: not_required
  integration_test_reason: Implementation namespace filtering is schema transformation; can be tested
    with mocked introspection.
  stakeholders:
  - compliance
  competitive_position:
    status: neutral
    rationale: ''
- id: REQ-638
  status: complete
  group: 3. Source Registration & Data Modeling
  category: Registration & Governance
  priority: MUST
  type: behavioral
  description: The UI calls one availableSchemas endpoint and one availableTables endpoint. Backend routing
    selects the correct introspection strategy per source type internally, with no source-type-specific
    endpoints exposed to the UI.
  use_case: Single introspection endpoints for schemas and tables simplify the UI and centralize routing
    logic in one place.
  code:
  - provisa/api/admin/schema.py
  - provisa/api/admin/introspect.py
  tests:
  - tests/integration/test_introspect.py
  - tests/unit/test_available_tables.py
  - tests/unit/test_core_registration.py
  - provisa-ui/e2e/security-rate-limiting.spec.ts
  since: 2026-01
  scenario: 'Given a UI requesting schema and table lists for any source type

    When it calls availableSchemas and availableTables

    Then the backend selects the correct strategy internally without exposing per-type endpoints

    '
  integration_test: not_required
  integration_test_reason: Single introspection endpoint routing is API design; can be tested with mocked
    sources.
  e2e: true
  e2e_reason: The requirement is explicitly about the UI calling availableSchemas and availableTables
    REST endpoints, so correct routing behavior is only verifiable by observing a real browser UI registration
    flow against a live server.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - app-developer
  competitive_position:
    status: neutral
    rationale: ''
- id: REQ-639
  status: complete
  group: 3. Source Registration & Data Modeling
  category: Registration & Governance
  priority: MUST
  type: constraint
  description: A source type not explicitly handled by the introspection router must return [] (empty
    list) for both schemas and tables — never None. Returning None triggers a Trino fallback path that
    is not appropriate for non-Trino sources.
  use_case: Returning empty list instead of None for unknown source types prevents broken Trino fallback
    execution on source types that have no Trino connector.
  code:
  - provisa/api/admin/introspect.py
  tests:
  - tests/integration/test_introspect.py
  - tests/unit/test_core_registration.py
  since: 2026-01
  scenario: 'Given a source type not handled by the introspection router

    When schemas or tables are requested

    Then [] is returned for both; None is never returned

    '
  integration_test: not_required
  integration_test_reason: Empty list fallback for unknown source types is error handling logic; can be
    tested with fixtures.
  stakeholders:
  - ops
  competitive_position:
    status: neutral
    rationale: ''
  related_reqs:
  - REQ-825
- id: REQ-640
  status: complete
  group: 3. Source Registration & Data Modeling
  category: Naming & Schema
  priority: MUST
  type: constraint
  description: Dataset name comparisons must always normalize both sides through the centralized normalization
    function (toSnakeCase in the UI via naming.ts; provisa.compiler.naming in the backend) before comparing.
    Inline ad-hoc normalization such as name.toLowerCase() or hardcoded replace(/-/g, '_') is forbidden.
    This applies to isRegistered checks, alias lookups, duplicate detection, and cross-source name matching.
  use_case: Centralized normalization for all name comparisons prevents silent mismatches caused by different
    casing conventions across source types.
  code:
  - provisa-ui/src/naming.ts
  - provisa-ui/src/pages/TablesPage.tsx
  - provisa/compiler/naming.py
  tests:
  - tests/unit/test_naming.py
  - tests/unit/test_schema_service.py
  since: 2026-01
  scenario: 'Given a name comparison anywhere in the system (isRegistered, alias lookup, duplicate check)

    When the comparison runs

    Then both sides are normalized through the centralized function; ad-hoc normalization is absent

    '
  integration_test: not_required
  integration_test_reason: Centralized name normalization is pure function logic; can be tested with string
    test cases.
  stakeholders:
  - data-engineer
  - app-developer
  competitive_position:
    status: neutral
    rationale: ''
- id: REQ-641
  status: complete
  group: 3. Source Registration & Data Modeling
  category: Registration & Governance
  priority: MUST
  type: structural
  description: 'Every registered dataset has two identity forms: (1) physical identity — the native name
    as returned by the source, used by the query engine; (2) semantic identity — domainId + tableName
    (alias, normalized), used by the API, GraphQL schema, and end users. Resolution between forms uses
    registered_tables: physical-to-semantic via (sourceId, normalizedPhysicalName); semantic-to-physical
    via (domainId, tableName).'
  use_case: Dual identity model with centralized resolution prevents ad-hoc string comparisons that break
    across naming conventions and source types.
  code:
  - provisa/core/repositories/table.py
  tests:
  - tests/unit/test_graphql_alias.py
  - tests/unit/test_naming.py
  since: 2026-01
  scenario: 'Given a registered dataset

    When the system resolves its identity in either direction

    Then physical-to-semantic resolution uses (sourceId, normalizedPhysicalName) and

    semantic-to-physical uses (domainId, tableName) via registered_tables

    '
  integration_test: not_required
  integration_test_reason: Dual identity resolution is lookup logic; can be tested with mock registry
    fixtures.
  stakeholders:
  - data-engineer
  - app-developer
  competitive_position:
    status: ahead
    rationale: Explicit dual-identity model (physical vs. semantic) for every dataset with deterministic
      routing between them is more rigorous than Hasura's single-name approach.
- id: REQ-147
  status: complete
  group: 4. Source Connectors
  category: Kafka Sources
  priority: SHOULD
  type: behavioral
  description: Kafka topics queryable via Trino Kafka connector. Routed through Trino (TRINO_ONLY source).
  use_case: Kafka topic querying via Trino lets analysts query streaming data with the same SQL and governance
    as relational tables.
  code:
  - provisa/kafka/
  tests:
  - tests/unit/test_kafka_schema.py
  - tests/integration/test_kafka_source.py
  since: 2026-01
  scenario: 'Given a Kafka topic registered as a TRINO_ONLY source

    When a consumer queries the topic via Provisa

    Then the query is routed through Trino and returns results from the Kafka topic

    '
  integration_test: required
  integration_test_reason: Kafka topic queries require real Kafka broker and Trino connector with actual
    data ingestion.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - data-engineer
  - app-developer
  competitive_position:
    status: parity
    rationale: Kafka topic querying via Trino is a standard capability Trino/Presto natively support through
      the Kafka connector. Hasura and Denodo also support Kafka as a source; routing through Trino is
      an established pattern.
- id: REQ-148
  status: complete
  group: 4. Source Connectors
  category: Kafka Sources
  priority: MUST
  type: constraint
  description: Default time window (`default_window`) auto-injected as WHERE clause on `_timestamp`. Prevents
    unbounded reads.
  use_case: Auto-injected time-window filter prevents unbounded Kafka topic scans that would exhaust cluster
    resources.
  code:
  - provisa/kafka/
  tests:
  - tests/integration/test_kafka_source.py
  - tests/unit/test_kafka_schema.py
  - tests/unit/test_kafka_window.py
  since: 2026-01
  scenario: 'Given a Kafka source with a default_window configured

    When a query is issued without an explicit time filter

    Then a WHERE clause on _timestamp is auto-injected to bound the read window

    '
  integration_test: required
  integration_test_reason: Time window injection must validate behavior on real Kafka topics to prevent
    unbounded scans.
  stakeholders:
  - data-engineer
  - ops
  competitive_position:
    status: ahead
    rationale: Auto-injected time-window filter on _timestamp to prevent unbounded Kafka reads is a built-in
      safety guard that Trino, Hasura, and Denodo do not provide automatically — operators must configure
      scan limits manually in those platforms.
- id: REQ-149
  status: complete
  group: 4. Source Connectors
  category: Kafka Sources
  priority: SHOULD
  type: behavioral
  description: Discriminator filter for multi-type topics — multiple table configs on the same physical
    topic, each filtered by a discriminator field/value.
  use_case: Discriminator filters let a single physical Kafka topic serve multiple logical table definitions
    safely.
  code:
  - provisa/kafka/
  tests:
  - tests/unit/test_kafka_schema.py
  - tests/integration/test_kafka_source.py
  since: 2026-01
  scenario: 'Given multiple table configs registered against the same Kafka topic with different discriminator
    values

    When each table is queried

    Then only messages matching that table''s discriminator field/value are returned

    '
  integration_test: required
  integration_test_reason: Discriminator filtering on multi-type topics requires live Kafka to verify
    correct row filtering.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - data-engineer
  - compliance
  competitive_position:
    status: ahead
    rationale: Discriminator-based multi-type topic partitioning (multiple logical tables on one physical
      topic filtered by a discriminator field) is not natively supported by Trino Kafka connector or Hasura;
      it requires custom schema configuration in those platforms.
- id: REQ-150
  status: complete
  group: 4. Source Connectors
  category: Kafka Sources
  priority: SHOULD
  type: structural
  description: Manual schema definition for topics without Schema Registry.
  use_case: Manual schema definition for Schema Registry-less topics lets stewards govern message structure
    explicitly.
  code:
  - provisa/kafka/
  tests:
  - tests/unit/test_kafka_schema.py
  - tests/integration/test_kafka_source.py
  since: 2026-01
  integration_test: not_required
  integration_test_reason: Manual schema definition is config validation; mocks can verify DDL generation.
  stakeholders:
  - data-engineer
  competitive_position:
    status: parity
    rationale: Manual schema definition for topics without Schema Registry is standard in Trino Kafka
      connector and Hasura CDC pipelines. Most federation tools expose this capability.
- id: REQ-229
  status: complete
  group: 4. Source Connectors
  category: Direct-Route Dialect Expansion
  priority: MUST
  type: structural
  description: 'For every source type, three things must be true for direct-route capability: (1) Trino
    connector is packaged in the Trino deployment, (2) SQLGlot has the dialect for transpilation, (3)
    `SOURCE_TO_DIALECT` and `SOURCE_TO_CONNECTOR` entries exist in Provisa config. New direct-route sources
    to add: clickhouse, mariadb, singlestore, redshift, databricks, hive, druid, exasol. File and lake
    sources (iceberg, hive_s3, delta) are TRINO_ONLY — no direct-route, no SQLGlot dialect entry required;
    only Trino connector packaging and Provisa config entries needed. Each source must have all applicable
    items verified.'
  use_case: Verified direct-route support for new RDBMS dialects lets data engineers connect more source
    types without Trino. File and lake sources require only connector and config entries since they always
    route through Trino.
  code:
  - provisa/transpiler/
  tests:
  - tests/unit/test_dialect_expansion.py
  since: 2026-01
  integration_test: not_required
  integration_test_reason: Config entry verification is pure logic; dialect mapping and connector packaging
    verified at registration.
  stakeholders:
  - data-engineer
  - ops
  competitive_position:
    status: parity
    rationale: Multi-dialect expansion (Clickhouse, MariaDB, Redshift, Databricks, Hive, etc.) is standard
      for Trino and Presto which already ship these connectors natively. Denodo also supports a wide range
      of RDBMS dialects.
  related_reqs:
  - REQ-825
- id: REQ-295
  status: complete
  group: 4. Source Connectors
  category: Query-API Sources (Neo4j & SPARQL)
  priority: SHOULD
  type: structural
  description: Neo4j registered as a non-Trino source using the existing API source pipeline. Source config
    accepts Neo4j HTTP API endpoint URL, target database name, and auth (none/basic/bearer token). Tables
    defined as steward-authored Cypher SELECT queries. The Neo4j HTTP API v2 response envelope (`data.fields`
    / `data.values`) is unwrapped by the built-in `neo4j_tabular` response normalizer into flat row dicts
    before the existing flattener runs. Column names match the aliases declared in the Cypher `RETURN`
    clause. Does not use Trino.
  use_case: Neo4j registered as an API source lets stewards expose Cypher query results as governed GraphQL
    tables.
  code:
  - provisa/neo4j/
  - provisa/sparql/
  - provisa/api_source/
  tests:
  - tests/e2e/test_query_pipeline.py
  - tests/integration/test_neo4j_exec.py
  - tests/integration/test_sparql_exec.py
  - tests/unit/test_neo4j_normalizers.py
  since: 2026-01
  scenario: 'Given a Neo4j source registered with endpoint URL and auth

    When a steward defines a table using a Cypher SELECT query

    Then the HTTP API v2 response is unwrapped by neo4j_tabular and returned as flat rows

    '
  integration_test: required
  integration_test_reason: Neo4j HTTP API integration requires live endpoint and response envelope unwrapping.
  stakeholders:
  - data-engineer
  - app-developer
  competitive_position:
    status: ahead
    rationale: Neo4j as a governed API source using Cypher queries surfaced as GraphQL tables is not natively
      supported by Trino, Hasura, Denodo, or Dremio without custom development. Provisa wraps Neo4j HTTP
      API v2 with a built-in normalizer and governance enforcement.
- id: REQ-296
  status: complete
  group: 4. Source Connectors
  category: Query-API Sources (Neo4j & SPARQL)
  priority: SHOULD
  type: behavioral
  description: Neo4j table registration includes a query preview step. On submission, Provisa executes
    the steward's Cypher query against the live endpoint with an implicit `LIMIT 5` appended and displays
    the resulting rows. If the response contains node or edge objects rather than flat scalar values,
    registration is blocked with an error directing the steward to rewrite the query using explicit scalar
    projections (`RETURN n.name AS name, n.age AS age` rather than `RETURN n`). Steward owns the response
    shape via query authoring; Provisa does not auto-flatten graph objects.
  use_case: Query preview at Neo4j table registration catches non-scalar projections early before they
    cause runtime errors.
  code:
  - provisa/neo4j/
  - provisa/sparql/
  - provisa/api_source/
  tests:
  - tests/integration/test_direct_exec.py
  - tests/integration/test_neo4j_exec.py
  - tests/unit/test_cypher_graph_fns.py
  - tests/unit/test_neo4j_normalizers.py
  - tests/unit/test_neo4j_preview.py
  - provisa-ui/e2e/registration-admin.spec.ts
  since: 2026-01
  scenario: 'Given a steward submitting a Cypher query for Neo4j table registration

    When the query returns node or edge objects instead of flat scalar projections

    Then registration is blocked with an error directing the steward to use explicit scalar RETURN aliases

    '
  integration_test: required
  integration_test_reason: Query preview execution against live Neo4j endpoint needed to verify shape
    validation.
  e2e: true
  e2e_reason: The query preview step is a UI interaction at Neo4j table registration time — blocking of
    non-scalar projections with an inline error message is only verifiable in a browser admin surface
    against a live Neo4j endpoint.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - data-engineer
  - compliance
  competitive_position:
    status: ahead
    rationale: Registration-time Cypher query preview with shape validation (blocking non-scalar projections)
      is a developer experience safety guard not present in Hasura, Trino, or Denodo Neo4j integrations,
      which expose raw response errors at runtime.
- id: REQ-297
  status: complete
  group: 4. Source Connectors
  category: Query-API Sources (Neo4j & SPARQL)
  priority: SHOULD
  type: structural
  description: SPARQL connector supports any SPARQL 1.1 compliant triplestore endpoint (Apache Jena Fuseki,
    Ontotext GraphDB, Stardog, Blazegraph, Virtuoso). Source config accepts endpoint URL, auth (none/basic/bearer),
    and optional default graph URI. Tables defined as SPARQL SELECT queries. The built-in `sparql_bindings`
    response normalizer unwraps the standard SPARQL 1.1 JSON results envelope (`results.bindings[].variable.value`)
    into flat row dicts before the existing flattener runs. Column names match SPARQL SELECT variable
    names. Does not use Trino.
  use_case: SPARQL connector lets stewards expose any SPARQL 1.1 triplestore as governed GraphQL query
    results.
  code:
  - provisa/neo4j/
  - provisa/sparql/
  - provisa/api_source/
  tests:
  - tests/unit/test_neo4j_normalizers.py
  - tests/integration/test_neo4j_exec.py
  since: 2026-01
  integration_test: required
  integration_test_reason: SPARQL triplestore connectivity and response envelope parsing require live
    endpoint.
  stakeholders:
  - data-engineer
  - app-developer
  competitive_position:
    status: ahead
    rationale: SPARQL 1.1 triplestore support (Fuseki, GraphDB, Stardog, Blazegraph, Virtuoso) as a governed
      data source is absent from Trino, Hasura, Denodo, and Dremio in a first-class, spec-compliant way;
      semantic web data federation is a differentiator.
- id: REQ-298
  status: complete
  group: 4. Source Connectors
  category: Query-API Sources (Neo4j & SPARQL)
  priority: SHOULD
  type: behavioral
  description: 'API source caller extended to support HTTP POST with a request body. Required for Neo4j
    (POST to `/db/{database}/query/v2` with `Content-Type: application/json` body `{"statement": "<cypher>"}`)
    and SPARQL (POST with `Content-Type: application/x-www-form-urlencoded` body `query=<encoded-sparql>`
    per SPARQL 1.1 protocol). `ApiEndpoint.method` already accepts arbitrary HTTP verbs; this closes the
    implementation gap where POST bodies were not transmitted. Existing GET endpoints unaffected.'
  use_case: HTTP POST support for API source caller enables Neo4j and SPARQL endpoints that require POST
    requests.
  code:
  - provisa/neo4j/
  - provisa/sparql/
  - provisa/api_source/
  tests:
  - tests/unit/test_neo4j_normalizers.py
  - tests/integration/test_neo4j_exec.py
  since: 2026-01
  scenario: 'Given a Neo4j or SPARQL source requiring POST requests with a request body

    When the API source caller executes a query

    Then the POST body is transmitted correctly and existing GET endpoints are unaffected

    '
  integration_test: required
  integration_test_reason: HTTP POST body transmission must verify against real Neo4j and SPARQL endpoints.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - data-engineer
  - app-developer
  competitive_position:
    status: ahead
    rationale: Native HTTP POST support for Cypher (Neo4j) and SPARQL sources without requiring a separate
      connector layer is not available in Trino, Hasura, or Dremio out of the box.
- id: REQ-299
  status: complete
  group: 4. Source Connectors
  category: Query-API Sources (Neo4j & SPARQL)
  priority: SHOULD
  type: structural
  description: 'A `response_normalizer: str | None` field added to `ApiEndpoint`. When set, the named
    normalizer is applied to the raw API response before `response_root` navigation and the existing flattener.
    Built-in normalizers: `neo4j_tabular` (zips `data.fields[]` with each `data.values[][]` entry into
    a flat dict) and `sparql_bindings` (extracts `results.bindings[]` and maps each `{variable: {type,
    value}}` binding to `{variable: value}`). Normalizer names are validated at source registration time;
    unknown names are rejected. Extensible for future query-API envelope formats without changes to the
    core flattener.'
  use_case: Named response normalizers (neo4j_tabular, sparql_bindings) cleanly unwrap API envelopes before
    flattening.
  code:
  - provisa/neo4j/
  - provisa/sparql/
  - provisa/api_source/
  tests:
  - tests/integration/test_neo4j_exec.py
  - tests/unit/test_api_flattener.py
  - tests/unit/test_api_normalizer_requirements.py
  - tests/unit/test_misc_requirements.py
  - tests/unit/test_neo4j_normalizers.py
  since: 2026-01
  integration_test: not_required
  integration_test_reason: Response normalizer registration and lookup is pure logic; envelope unwrapping
    testable with mocks.
  stakeholders:
  - data-engineer
  competitive_position:
    status: parity
    rationale: Remote GraphQL schema introspection and registration is Hasura's core capability. Trino
      lacks it natively; Denodo supports it. Provisa is at parity with Hasura for basic remote schema
      connectivity.
- id: REQ-307
  status: complete
  group: 4. Source Connectors
  category: GraphQL Remote Schema Connector (REQ-307–313)
  priority: SHOULD
  type: structural
  description: Provisa supports a "remote GraphQL schema" source type. The steward registers an external
    GraphQL endpoint URL plus optional auth (none/bearer/basic). Provisa introspects the remote schema
    via a standard `__schema` introspection query and stores the result.
  use_case: Remote GraphQL schema source type lets stewards expose external GraphQL APIs as governed Provisa
    tables.
  code:
  - provisa/graphql_remote/
  tests:
  - tests/integration/test_graphql_execution.py
  - tests/integration/test_graphql_remote_integration.py
  - tests/integration/test_graphql_remote_source.py
  - tests/unit/test_graphql_remote_introspect.py
  - tests/unit/test_graphql_remote_mapper.py
  - tests/unit/test_remote_adapter_contract.py
  - tests/unit/test_schema_service.py
  since: 2026-01
  integration_test: required
  integration_test_reason: Remote GraphQL introspection and schema fetch require live endpoint.
  stakeholders:
  - data-engineer
  - app-developer
  competitive_position:
    status: parity
    rationale: Remote GraphQL schema introspection and registration is Hasura's core capability. Trino
      lacks it natively; Denodo supports it. Provisa is at parity with Hasura for basic remote schema
      connectivity.
- id: REQ-308
  status: complete
  group: 4. Source Connectors
  category: GraphQL Remote Schema Connector (REQ-307–313)
  priority: SHOULD
  type: behavioral
  description: Each Query field on the remote schema is exposed as a virtual read-only table in Provisa.
    Column names and types are inferred from the field's return type (object fields → columns). Each Mutation
    field is exposed as a tracked function with `return_schema` derived from the mutation's return type.
    This eliminates manual table and function registration for remote GraphQL APIs.
  use_case: Auto-registering remote Query fields as tables and Mutation fields as functions eliminates
    manual registration.
  code:
  - provisa/graphql_remote/
  tests:
  - tests/integration/test_graphql_execution.py
  - tests/integration/test_graphql_remote_integration.py
  - tests/integration/test_graphql_remote_source.py
  - tests/unit/test_graphql_remote_introspect.py
  - tests/unit/test_graphql_remote_mapper.py
  - tests/unit/test_remote_adapter_contract.py
  - tests/unit/test_schema_service.py
  since: 2026-01
  scenario: 'Given a remote GraphQL schema is registered in Provisa

    When introspection completes

    Then Query fields are auto-registered as virtual read-only tables and Mutation fields as tracked functions

    '
  integration_test: required
  integration_test_reason: Auto-registration of Query/Mutation fields requires live schema verification.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - data-engineer
  - app-developer
  competitive_position:
    status: parity
    rationale: Auto-registering Query fields as tables and Mutation fields as tracked functions is exactly
      what Hasura's Remote Schemas feature does. This is direct parity with Hasura.
- id: REQ-309
  status: complete
  group: 4. Source Connectors
  category: GraphQL Remote Schema Connector (REQ-307–313)
  priority: SHOULD
  type: behavioral
  description: At query execution time, Provisa translates the incoming GraphQL request into a remote
    GraphQL query and forwards it to the remote endpoint. The response rows are materialized as Parquet
    in a Trino Iceberg table on S3 (`results.api_cache`, `s3a://provisa-results/api_cache/`). Repeated
    calls within TTL are served from the Iceberg table via Trino — zero remote hops. The cache table is
    automatically dropped after TTL expires.
  use_case: S3/Iceberg materialization of remote schema results eliminates repeated network hops and enables
    federated SQL (WHERE/ORDER BY/LIMIT) over cached data.
  code:
  - provisa/graphql_remote/
  - provisa/api_source/trino_cache.py
  tests:
  - tests/integration/test_graphql_execution.py
  - tests/integration/test_graphql_remote_integration.py
  - tests/integration/test_graphql_remote_source.py
  - tests/unit/test_api_cache.py
  - tests/unit/test_graphql_remote_introspect.py
  - tests/unit/test_graphql_remote_mapper.py
  - tests/unit/test_remote_adapter_contract.py
  - tests/unit/test_schema_service.py
  since: 2026-01
  scenario: 'Given a remote GraphQL source query executed within TTL

    When the same query is issued again

    Then results are served from the Iceberg cache in Trino with zero remote hops

    '
  integration_test: required
  integration_test_reason: Remote GraphQL query execution, S3/Iceberg caching, and TTL expiry require
    real infra.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - data-engineer
  - ops
  - app-developer
  competitive_position:
    status: ahead
    rationale: Materializing remote GraphQL results as Parquet in S3/Iceberg via Trino with TTL-based
      caching and zero-hop repeat queries is not present in Hasura (which proxies every request to the
      remote) or Denodo (in-memory cache only). This enables federated SQL over cached remote data.
  related_reqs:
  - REQ-826
  - REQ-825
  - REQ-844
  - REQ-845
- id: REQ-310
  status: complete
  group: 4. Source Connectors
  category: GraphQL Remote Schema Connector (REQ-307–313)
  priority: MUST
  type: constraint
  description: RLS, column masking, domain access control, and column visibility are applied to remote
    schema results in Stage 2 of the governance pipeline, identically to local table results. The remote
    endpoint has no knowledge of Provisa's governance rules.
  use_case: Stage 2 governance applied to remote schema results ensures external APIs never bypass RLS
    or masking.
  code:
  - provisa/graphql_remote/
  tests:
  - tests/integration/test_graphql_execution.py
  - tests/integration/test_graphql_remote_integration.py
  - tests/integration/test_graphql_remote_source.py
  - tests/unit/test_graphql_remote_introspect.py
  - tests/unit/test_graphql_remote_mapper.py
  - tests/unit/test_remote_adapter_contract.py
  - tests/unit/test_schema_service.py
  since: 2026-01
  scenario: 'Given a remote GraphQL source with RLS and column masking rules configured

    When results are returned from the remote endpoint

    Then Stage 2 governance applies RLS, masking, domain access control, and visibility identically to
    local tables

    '
  integration_test: required
  integration_test_reason: Governance application on remote results requires Stage 2 pipeline with real
    data.
  stakeholders:
  - data-engineer
  - compliance
  competitive_position:
    status: ahead
    rationale: Applying full Stage 2 governance (RLS, masking, domain access control) to remote GraphQL
      schema results uniformly is a differentiator versus Hasura, which requires separate permission configuration
      per remote schema field and does not apply unified row-level security across local and remote sources.
- id: REQ-311
  status: complete
  group: 4. Source Connectors
  category: GraphQL Remote Schema Connector (REQ-307–313)
  priority: SHOULD
  type: behavioral
  description: Remote schema introspection is re-run on demand via an "Refresh Schema" admin mutation.
    Provisa does not poll for remote schema changes automatically — the steward triggers refresh when
    the remote API evolves. On refresh, existing table and function registrations derived from the remote
    schema are updated; custom RLS/masking rules applied on top are preserved.
  use_case: On-demand schema refresh lets stewards update remote schema registrations when the upstream
    API evolves.
  code:
  - provisa/graphql_remote/
  tests:
  - tests/integration/test_graphql_execution.py
  - tests/integration/test_graphql_remote_integration.py
  - tests/integration/test_graphql_remote_source.py
  - tests/unit/test_graphql_remote_introspect.py
  - tests/unit/test_graphql_remote_mapper.py
  - tests/unit/test_remote_adapter_contract.py
  - tests/unit/test_schema_service.py
  - provisa-ui/e2e/registration-admin.spec.ts
  since: 2026-01
  scenario: 'Given a remote GraphQL schema that has changed upstream

    When a steward triggers the Refresh Schema admin mutation

    Then registrations are updated and existing RLS/masking rules are preserved

    '
  integration_test: required
  integration_test_reason: Schema refresh against live endpoint must verify preservation of governance
    rules.
  e2e: true
  e2e_reason: Schema refresh is triggered via an admin GraphQL mutation endpoint, so verifying that the
    mutation preserves governance rules while updating registrations requires a real running server serving
    the GraphQL API.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - data-engineer
  - ops
  competitive_position:
    status: parity
    rationale: On-demand schema refresh preserving governance rules is standard in Hasura (metadata reload)
      and Denodo (data model refresh). Most federation platforms expose a refresh mechanism.
- id: REQ-312
  status: complete
  group: 4. Source Connectors
  category: GraphQL Remote Schema Connector (REQ-307–313)
  priority: MUST
  type: constraint
  description: Remote schema type name conflicts are resolved by prefixing all generated type names with
    a steward-supplied namespace (e.g. `Shopify__Order` rather than `Order`). Namespace is set at source
    registration time and cannot be changed without re-registering.
  use_case: Namespace prefixing prevents remote schema type name collisions with local registered table
    types.
  code:
  - provisa/graphql_remote/
  tests:
  - tests/integration/test_graphql_execution.py
  - tests/integration/test_graphql_remote_integration.py
  - tests/integration/test_graphql_remote_source.py
  - tests/unit/test_graphql_remote_introspect.py
  - tests/unit/test_graphql_remote_mapper.py
  - tests/unit/test_remote_adapter_contract.py
  - tests/unit/test_schema_service.py
  since: 2026-01
  scenario: 'Given a remote GraphQL source registered with a namespace prefix

    When type names are generated from introspection

    Then all generated types are prefixed with the steward-supplied namespace to prevent collisions

    '
  integration_test: not_required
  integration_test_reason: Namespace prefixing is type name transformation logic; mocks can verify collision
    handling.
  stakeholders:
  - data-engineer
  - app-developer
  competitive_position:
    status: parity
    rationale: Namespace prefixing to prevent type name collisions is also implemented in Hasura Remote
      Schemas (namespace prefix option). This is standard practice for federated GraphQL composition.
- id: REQ-313
  status: complete
  group: 4. Source Connectors
  category: GraphQL Remote Schema Connector (REQ-307–313)
  priority: SHOULD
  type: behavioral
  description: Relationships between remote schema virtual tables and local registered tables are supported
    via the standard relationship model. Provisa resolves the local side from cache/DB and the remote
    side via the cached remote call. Cross-source join behaviour follows the existing federation routing
    rules.
  use_case: Relationships between remote and local tables follow standard federation routing so joins
    work transparently.
  code:
  - provisa/graphql_remote/
  tests:
  - tests/integration/test_graphql_execution.py
  - tests/integration/test_graphql_remote_integration.py
  - tests/integration/test_graphql_remote_source.py
  - tests/unit/test_graphql_remote_introspect.py
  - tests/unit/test_graphql_remote_mapper.py
  - tests/unit/test_remote_adapter_contract.py
  - tests/unit/test_schema_service.py
  since: 2026-01
  scenario: 'Given a relationship defined between a remote schema virtual table and a local table

    When a joined query is executed

    Then the local side is resolved from cache/DB and the remote side via the cached remote call

    '
  integration_test: required
  integration_test_reason: Cross-source relationships between remote and local tables require federation
    routing verification.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - data-engineer
  - app-developer
  competitive_position:
    status: ahead
    rationale: Cross-source relationships between remote schema virtual tables and local tables with federation
      routing are not natively supported in Hasura (remote schema joins require explicit join hints) and
      absent in Trino and Denodo without manual ETL. Provisa provides transparent federation.
  related_reqs:
  - REQ-826
- id: REQ-314
  status: complete
  group: 4. Source Connectors
  category: OpenAPI Auto-Registration Connector
  priority: SHOULD
  type: structural
  description: An OpenAPI source is registered by providing an OpenAPI 3.x or Swagger 2.0 spec. The spec
    path may be a local file path (absolute or relative to the config directory) or a remote URL. Provisa
    fetches remote specs at registration time and caches them; local specs are read directly.
  use_case: OpenAPI spec registration auto-discovers all GET endpoints as governed virtual tables without
    manual config.
  code:
  - provisa/openapi/
  tests:
  - tests/unit/test_openapi_loader.py
  - tests/integration/test_openapi_source.py
  since: 2026-01
  integration_test: required
  integration_test_reason: OpenAPI spec fetching (remote URLs) and caching require real HTTP and file
    I/O.
  stakeholders:
  - data-engineer
  - ops
  competitive_position:
    status: parity
    rationale: OpenAPI 3.x/Swagger 2.0 spec registration with remote URL fetch is supported by Hasura
      Actions and Denodo's REST data service adapter. Most enterprise virtualization platforms support
      OpenAPI discovery.
  related_reqs:
  - REQ-826
- id: REQ-315
  status: complete
  group: 4. Source Connectors
  category: OpenAPI Auto-Registration Connector
  priority: MAY
  type: behavioral
  description: If auto-discovery of a spec is not possible (behind auth, no spec endpoint, hand-written
    API), the steward may manually author an OpenAPI 3.x spec in the admin UI or upload a YAML/JSON file.
    The manually created spec is stored locally and treated identically to a fetched one.
  use_case: Manual OpenAPI spec authoring lets stewards govern APIs that have no public spec endpoint.
  code:
  - provisa/openapi/
  tests:
  - tests/unit/test_openapi_loader.py
  - tests/integration/test_openapi_source.py
  since: 2026-01
  scenario: 'Given an API with no public spec endpoint

    When a steward manually uploads a YAML/JSON OpenAPI 3.x spec

    Then it is stored locally and treated identically to a fetched spec

    '
  integration_test: not_required
  integration_test_reason: Manual OpenAPI authoring UI storage is config handling; mocks verify file persistence.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - data-engineer
  competitive_position:
    status: parity
    rationale: Manual YAML/JSON spec authoring as a fallback is standard in Hasura Actions and custom
      REST connectors in Denodo. This is an expected fallback path for all platforms.
- id: REQ-316
  status: complete
  group: 4. Source Connectors
  category: OpenAPI Auto-Registration Connector
  priority: SHOULD
  type: behavioral
  description: On registration, Provisa parses the spec and auto-registers all GET operations as virtual
    query tables. Path parameters and query parameters become GraphQL arguments. The `responses.200` (or
    `responses.2xx`) schema determines the virtual table's column set.
  use_case: GET operation auto-registration maps path and query params to GraphQL arguments automatically.
  code:
  - provisa/openapi/
  tests:
  - tests/unit/test_openapi_loader.py
  - tests/integration/test_openapi_source.py
  since: 2026-01
  scenario: 'Given an OpenAPI spec is registered

    When Provisa parses the spec

    Then all GET operations are auto-registered as virtual query tables with path/query params as GraphQL
    arguments

    '
  integration_test: not_required
  integration_test_reason: GET operation parsing and GraphQL argument mapping is pure schema transformation
    logic.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - data-engineer
  - app-developer
  competitive_position:
    status: ahead
    rationale: Auto-registering all GET operations from an OpenAPI spec as virtual query tables with path/query
      params as GraphQL arguments is not natively supported in Trino or Dremio; Hasura requires manual
      Action definition per endpoint. Provisa provides full auto-discovery.
- id: REQ-317
  status: complete
  group: 4. Source Connectors
  category: OpenAPI Auto-Registration Connector
  priority: SHOULD
  type: behavioral
  description: All non-GET operations (POST, PUT, PATCH, DELETE) are auto-registered as tracked functions
    (mutations). Request body schema properties become GraphQL mutation input arguments. The `responses.200`/`2xx`
    schema becomes the mutation's `return_schema`.
  use_case: Non-GET operations auto-registered as mutations expose REST write endpoints as governed GraphQL
    mutations.
  code:
  - provisa/openapi/
  tests:
  - tests/unit/test_openapi_loader.py
  - tests/integration/test_openapi_source.py
  since: 2026-01
  scenario: 'Given an OpenAPI spec with POST/PUT/PATCH/DELETE operations

    When the spec is registered

    Then those operations are auto-registered as tracked functions with request body properties as mutation
    input arguments

    '
  integration_test: not_required
  integration_test_reason: POST/PUT/PATCH/DELETE operation parsing is pure schema transformation logic.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - data-engineer
  - app-developer
  competitive_position:
    status: ahead
    rationale: Auto-registering non-GET operations (POST/PUT/PATCH/DELETE) as tracked GraphQL mutations
      from an OpenAPI spec is not natively supported in Trino, Denodo, or Dremio; Hasura requires individual
      Action definitions. Full spec-driven write mutation registration is a differentiator.
- id: REQ-318
  status: complete
  group: 4. Source Connectors
  category: OpenAPI Auto-Registration Connector
  priority: SHOULD
  type: behavioral
  description: GET operation results are materialized as Parquet in a Trino Iceberg table on S3 (`results.api_cache`,
    `s3a://provisa-results/api_cache/`). The cache key is a SHA-256 hash of `source_id + operation path
    + native args`. Repeated calls within TTL hit Trino directly. The cache table is dropped after TTL
    expires. Mutations are never cached — they are side-effecting.
  use_case: S3/Iceberg materialization of GET operation results prevents repeated upstream REST calls
    and enables Trino SQL filtering over cached rows.
  code:
  - provisa/api_source/trino_cache.py
  - provisa/api_source/router_integration.py
  tests:
  - tests/unit/test_api_cache.py
  - tests/integration/test_openapi_source.py
  since: 2026-01
  scenario: 'Given a GET operation result cached in Trino Iceberg on S3

    When the same query with identical args is issued within TTL

    Then results are served from Trino directly with zero upstream REST calls

    '
  integration_test: required
  integration_test_reason: GET result caching to S3/Iceberg and TTL expiry require real S3 and Trino.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - data-engineer
  - ops
  - app-developer
  competitive_position:
    status: ahead
    rationale: S3/Iceberg caching of REST GET results via Trino with SHA-256 keyed TTL and zero-hop repeated
      calls is not present in Hasura Actions (proxies each call) or Denodo (in-memory cache). This enables
      full SQL analytics over materialized REST data.
  related_reqs:
  - REQ-826
  - REQ-825
  - REQ-844
  - REQ-845
- id: REQ-319
  status: complete
  group: 4. Source Connectors
  category: OpenAPI Auto-Registration Connector
  priority: MUST
  type: constraint
  description: 'Full governance is applied to all OpenAPI source results: RLS, column masking, domain
    access control, and column visibility are enforced in Stage 2 identically to local tables.'
  use_case: Stage 2 governance applied to OpenAPI results ensures REST API data never bypasses RLS or
    masking.
  code:
  - provisa/openapi/
  tests:
  - tests/unit/test_openapi_loader.py
  - tests/integration/test_openapi_source.py
  since: 2026-01
  scenario: 'Given an OpenAPI source with governance rules configured

    When results are returned from the REST API

    Then Stage 2 applies RLS, column masking, domain access control, and column visibility identically
    to local tables

    '
  integration_test: required
  integration_test_reason: Governance enforcement on OpenAPI results requires Stage 2 pipeline with real
    data.
  stakeholders:
  - data-engineer
  - compliance
  competitive_position:
    status: ahead
    rationale: Applying Stage 2 governance (RLS, masking, domain access control) uniformly to OpenAPI
      source results is a gap for Hasura Actions (no row-level security on action results) and Denodo
      (governance applied at the virtual view layer, not uniformly at source).
- id: REQ-320
  status: complete
  group: 4. Source Connectors
  category: OpenAPI Auto-Registration Connector
  priority: MUST
  type: constraint
  description: Auth configuration for the upstream REST API (bearer token, API key header, basic auth,
    OAuth2 client credentials) is stored per-source in the secrets store and injected into outgoing requests.
    The caller's Provisa token is not forwarded.
  use_case: Per-source upstream auth config keeps API credentials in the secrets store and out of client
    requests.
  code:
  - provisa/openapi/
  tests:
  - tests/unit/test_openapi_loader.py
  - tests/integration/test_openapi_source.py
  since: 2026-01
  scenario: 'Given an OpenAPI source with bearer token auth configured in the secrets store

    When Provisa makes an outgoing request to the upstream REST API

    Then the stored token is injected and the caller''s Provisa token is not forwarded

    '
  integration_test: required
  integration_test_reason: Upstream auth injection requires live endpoint verification to prevent credential
    leaks.
  stakeholders:
  - compliance
  - ops
  competitive_position:
    status: parity
    rationale: Per-source upstream auth (bearer, API key, basic, OAuth2 client credentials) stored in
      secrets and injected into outgoing requests is standard in Hasura Actions and Denodo REST connectors.
      This is an expected enterprise feature.
- id: REQ-321
  status: complete
  group: 4. Source Connectors
  category: OpenAPI Auto-Registration Connector
  priority: SHOULD
  type: behavioral
  description: Spec refresh is triggered on demand via an admin mutation. On refresh, existing virtual
    table and tracked function registrations derived from the spec are updated; governance rules applied
    on top are preserved.
  use_case: On-demand spec refresh preserves governance rules while updating virtual table registrations
    from updated specs.
  code:
  - provisa/openapi/
  tests:
  - tests/unit/test_openapi_loader.py
  - tests/integration/test_openapi_source.py
  - provisa-ui/e2e/registration-admin.spec.ts
  since: 2026-01
  scenario: 'Given an OpenAPI spec that has been updated upstream

    When a steward triggers the spec refresh admin mutation

    Then registrations are updated and governance rules applied on top are preserved

    '
  integration_test: required
  integration_test_reason: Spec refresh against live endpoint must verify governance rule preservation.
  e2e: true
  e2e_reason: Spec refresh is triggered via an admin GraphQL mutation endpoint, and verifying preservation
    of governance rules while updating registrations from a re-fetched live URL requires the full API
    transport layer.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - data-engineer
  - ops
  competitive_position:
    status: parity
    rationale: On-demand spec refresh preserving governance rules is standard in Hasura (reload metadata)
      and Denodo. This is an expected operational capability for any federation platform.
- id: REQ-322
  status: complete
  group: 4. Source Connectors
  category: gRPC Remote Schema Connector (REQ-322–329)
  priority: SHOULD
  type: structural
  description: Provisa supports a "grpc_remote" source type. The steward registers an external gRPC server
    address plus a `.proto` file path or URL. Provisa parses the proto and auto-registers virtual tables
    and tracked functions. The `provisa/grpc_remote/` module handles all external gRPC connectivity and
    is entirely distinct from `provisa/grpc/`, which serves Provisa's own gRPC server.
  use_case: gRPC remote source type lets stewards expose external gRPC services as governed GraphQL tables
    and mutations.
  code:
  - provisa/grpc_remote/
  tests:
  - tests/integration/test_grpc_execution.py
  - tests/unit/test_grpc_remote_loader.py
  - tests/unit/test_remote_adapter_contract.py
  - tests/unit/test_schema_service.py
  since: 2026-01
  integration_test: required
  integration_test_reason: External gRPC connectivity requires live server; channel reuse and caching
    need network verification.
  stakeholders:
  - data-engineer
  - app-developer
  - ops
  competitive_position:
    status: ahead
    rationale: First-class gRPC as a registered source type with proto-based auto-registration is absent
      from Trino, Hasura, Denodo, and Dremio which have no native gRPC connector. This is a genuine differentiator
      for microservice-heavy architectures.
- id: REQ-323
  status: complete
  group: 4. Source Connectors
  category: gRPC Remote Schema Connector (REQ-322–329)
  priority: SHOULD
  type: behavioral
  description: 'Query vs mutation classification defaults to a name-prefix rule: methods whose names start
    with `Get`, `List`, `Find`, `Fetch`, `Search`, or `Stream` are classified as queries (virtual tables);
    all others are classified as mutations (tracked functions). Per-method override precedence is retained
    in the admin UI. Structural heuristic (server-streaming / repeated-message) is kept as fallback for
    non-prefixed methods.'
  use_case: Default name-prefix classification removes manual tagging while preserving per-method overrides
    and structural fallback.
  code:
  - provisa/grpc_remote/
  tests:
  - tests/e2e/test_grpc_query.py
  - tests/integration/test_grpc_execution.py
  - tests/unit/test_grpc_remote_loader.py
  - tests/unit/test_grpc_remote_mapper.py
  - tests/unit/test_remote_adapter_contract.py
  - tests/unit/test_schema_service.py
  - tests/unit/test_table_search.py
  since: 2026-01
  scenario: 'Given a gRPC service with methods named GetUsers, CreateUser, and StreamEvents

    When Provisa auto-classifies them

    Then GetUsers and StreamEvents are classified as queries and CreateUser as a mutation

    '
  integration_test: not_required
  integration_test_reason: Name-prefix classification and fallback heuristics are pure logic verifiable
    with mocks.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - data-engineer
  competitive_position:
    status: ahead
    rationale: Name-prefix heuristic for auto-classifying gRPC methods as queries vs. mutations (Get/List/Find
      → query, else mutation) is a Provisa-specific design with no equivalent in competitors, none of
      which support gRPC as a first-class source.
- id: REQ-324
  status: complete
  group: 4. Source Connectors
  category: gRPC Remote Schema Connector (REQ-322–329)
  priority: SHOULD
  type: structural
  description: 'Proto scalar types are mapped to SQL/GraphQL types as follows: `string`/`bytes` → `text`;
    `int32`/`uint32`/`sint32`/`fixed32`/`sfixed32` → `integer`; `int64`/`uint64`/`sint64`/`fixed64`/`sfixed64`
    → `bigint`; `float` → `real`; `double` → `numeric`; `bool` → `boolean`; `repeated <T>` → `jsonb`;
    nested message → `jsonb`; enum → `text`.'
  use_case: Proto scalar type mapping to SQL/GraphQL types gives gRPC message fields correct query and
    filter semantics.
  code:
  - provisa/grpc_remote/
  tests:
  - tests/integration/test_graphql_remote_source.py
  - tests/integration/test_grpc_execution.py
  - tests/unit/test_grpc_remote_loader.py
  - tests/unit/test_remote_adapter_contract.py
  - tests/unit/test_schema_service.py
  since: 2026-01
  integration_test: not_required
  integration_test_reason: Proto scalar type mapping is data type transformation logic.
  stakeholders:
  - data-engineer
  - app-developer
  competitive_position:
    status: parity
    rationale: Proto scalar type mapping to SQL/GraphQL is a standard requirement for any gRPC connector.
- id: REQ-325
  status: complete
  group: 4. Source Connectors
  category: gRPC Remote Schema Connector (REQ-322–329)
  priority: SHOULD
  type: behavioral
  description: Each query-classified gRPC method is exposed as a virtual read-only table. Output message
    fields become columns using the type mapping in REQ-324. Input message fields become GraphQL query
    arguments. Server-streaming methods (`Stream*`) collect all streamed response messages into a list
    before returning rows.
  use_case: Query-classified gRPC methods exposed as virtual tables let clients query remote services
    with GraphQL syntax.
  code:
  - provisa/grpc_remote/
  tests:
  - tests/e2e/test_grpc_query.py
  - tests/integration/test_graphql_remote_source.py
  - tests/integration/test_grpc_execution.py
  - tests/unit/test_grpc_remote_loader.py
  - tests/unit/test_remote_adapter_contract.py
  - tests/unit/test_schema_service.py
  since: 2026-01
  scenario: 'Given a query-classified gRPC method with input and output message fields

    When it is exposed as a virtual table

    Then output fields become columns, input fields become GraphQL arguments, and streaming methods collect
    all messages

    '
  integration_test: required
  integration_test_reason: Query-classified gRPC method execution and server-streaming collection require
    live server.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - data-engineer
  - app-developer
  competitive_position:
    status: ahead
    rationale: gRPC query methods exposed as virtual read-only tables with server-streaming collection
      is not available in any major competitor. Trino, Hasura, Denodo, and Dremio have no native gRPC
      source connector.
- id: REQ-326
  status: complete
  group: 4. Source Connectors
  category: gRPC Remote Schema Connector (REQ-322–329)
  priority: SHOULD
  type: behavioral
  description: Each mutation-classified gRPC method is exposed as a tracked function (mutation). Input
    message fields become GraphQL mutation input arguments. The output message schema becomes the mutation's
    `return_schema`.
  use_case: Mutation-classified gRPC methods exposed as tracked functions let clients call remote services
    via GraphQL mutations.
  code:
  - provisa/grpc_remote/
  tests:
  - tests/integration/test_graphql_remote_source.py
  - tests/integration/test_grpc_execution.py
  - tests/unit/test_grpc_remote_loader.py
  - tests/unit/test_remote_adapter_contract.py
  - tests/unit/test_schema_service.py
  since: 2026-01
  scenario: 'Given a mutation-classified gRPC method

    When it is exposed as a tracked function

    Then input message fields become GraphQL mutation input arguments and the output schema becomes return_schema

    '
  integration_test: required
  integration_test_reason: Mutation-classified gRPC execution requires live server connectivity.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - data-engineer
  - app-developer
  competitive_position:
    status: ahead
    rationale: gRPC mutation methods exposed as tracked GraphQL functions is unique to Provisa among the
      listed competitors; no major federation platform supports gRPC write-path methods as governed mutations.
- id: REQ-327
  status: complete
  group: 4. Source Connectors
  category: gRPC Remote Schema Connector (REQ-322–329)
  priority: SHOULD
  type: behavioral
  description: Query method results are materialized as Parquet in a Trino Iceberg table on S3 (`results.api_cache`,
    `s3a://provisa-results/api_cache/`). The cache key is a SHA-256 hash of `source_id + method + native
    args`. Mutations are never cached — they are side-effecting. One `grpc.aio.Channel` is reused per
    registered source across requests, stored in `AppState.grpc_remote_channels`. The cache table is dropped
    after TTL expires.
  use_case: S3/Iceberg materialization of gRPC query results enables Trino SQL filtering over cached rows
    and eliminates repeated remote calls. Channel reuse reduces connection overhead.
  code:
  - provisa/grpc_remote/
  - provisa/api_source/trino_cache.py
  tests:
  - tests/e2e/test_grpc_query.py
  - tests/integration/test_cache_store.py
  - tests/integration/test_grpc_execution.py
  - tests/unit/test_api_cache.py
  - tests/unit/test_cache_store.py
  - tests/unit/test_grpc_remote_cache.py
  - tests/unit/test_grpc_remote_loader.py
  - tests/unit/test_remote_adapter_contract.py
  - tests/unit/test_schema_service.py
  since: 2026-01
  scenario: 'Given a gRPC query method result cached in Trino Iceberg on S3

    When the same call is repeated within TTL

    Then results are served from Trino directly and the gRPC channel is reused without a new connection

    '
  integration_test: required
  integration_test_reason: gRPC query caching to S3/Iceberg, channel reuse, and TTL expiry require real
    infra.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - data-engineer
  - ops
  - app-developer
  competitive_position:
    status: ahead
    rationale: S3/Iceberg materialization of gRPC query results with channel reuse and TTL invalidation
      is a unique architectural capability; no competitor supports gRPC sources at all, let alone with
      intelligent caching to enable federated SQL over gRPC data.
  related_reqs:
  - REQ-826
  - REQ-825
  - REQ-844
  - REQ-845
- id: REQ-328
  status: complete
  group: 4. Source Connectors
  category: gRPC Remote Schema Connector (REQ-322–329)
  priority: MUST
  type: constraint
  description: 'Full governance is applied to gRPC remote source results: RLS, column masking, domain
    access control, and column visibility are enforced in Stage 2 identically to local table results.
    The remote gRPC server has no knowledge of Provisa''s governance rules.'
  use_case: Stage 2 governance applied to gRPC results ensures remote service data never bypasses RLS
    or masking.
  code:
  - provisa/grpc_remote/
  tests:
  - tests/integration/test_grpc_execution.py
  - tests/unit/test_grpc_remote_loader.py
  - tests/unit/test_remote_adapter_contract.py
  - tests/unit/test_schema_service.py
  since: 2026-01
  scenario: 'Given a gRPC remote source with RLS and column masking configured

    When results are returned from the remote gRPC server

    Then Stage 2 governance applies RLS, masking, domain access control, and visibility identically to
    local tables

    '
  integration_test: required
  integration_test_reason: Stage 2 governance enforcement on gRPC results requires real data pipeline.
  stakeholders:
  - data-engineer
  - compliance
  competitive_position:
    status: ahead
    rationale: Applying full Stage 2 governance (RLS, masking, domain control) to gRPC remote source results
      is unique to Provisa; no competitor has a gRPC source connector with governance enforcement.
- id: REQ-329
  status: complete
  group: 4. Source Connectors
  category: gRPC Remote Schema Connector (REQ-322–329)
  priority: SHOULD
  type: behavioral
  description: Proto schema refresh is triggered on demand via an admin mutation. On refresh, Provisa
    re-parses the proto, updates virtual table and tracked function registrations, and preserves any RLS/masking
    rules applied on top. Proto import paths (for well-known types such as `google/protobuf/timestamp.proto`)
    are stored at registration time and re-used on refresh.
  use_case: On-demand proto refresh preserves governance rules while updating registrations when the remote
    proto changes.
  code:
  - provisa/grpc_remote/
  tests:
  - tests/integration/test_grpc_execution.py
  - tests/unit/test_grpc_remote_loader.py
  - tests/unit/test_remote_adapter_contract.py
  - tests/unit/test_schema_service.py
  - provisa-ui/e2e/registration-admin.spec.ts
  since: 2026-01
  scenario: 'Given a gRPC source whose proto has changed

    When a steward triggers the proto refresh admin mutation

    Then registrations are updated, proto import paths are reused, and RLS/masking rules are preserved

    '
  integration_test: required
  integration_test_reason: Proto refresh against live server must verify governance rule preservation.
  e2e: true
  e2e_reason: The proto refresh is triggered via an admin mutation over the API boundary, requiring a
    real running server to verify the mutation is received, proto is re-parsed, and updated registrations
    are returned.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - data-engineer
  - ops
  competitive_position:
    status: ahead
    rationale: On-demand proto refresh with governance rule preservation is specific to Provisa's gRPC
      source type; competitors do not support gRPC sources and therefore have no equivalent capability.
- id: REQ-331
  status: complete
  group: 4. Source Connectors
  category: Ingest Sources — Governed HTTP Push Receiver (REQ-331–337)
  priority: SHOULD
  type: behavioral
  description: A new `ingest` source type allows external services (OTEL Collector, Fluentd, custom webhooks,
    etc.) to POST JSON events to Provisa via `POST /events/ingest/{source_id}/{table}`. Provisa owns the
    write path and persists events to a steward-configured backing relational store.
  use_case: Ingest source type lets external push producers (OTEL Collector, Fluentd) write events to
    governed tables.
  code:
  - provisa/ingest/
  tests:
  - tests/unit/test_ingest_ddl.py
  - tests/unit/test_ingest_router.py
  - tests/integration/test_ingest_push_integration.py
  - provisa-ui/e2e/infrastructure.spec.ts
  since: 2026-01
  scenario: 'Given an external service configured to POST JSON events to Provisa

    When a POST is made to /events/ingest/{source_id}/{table}

    Then Provisa persists the events to the steward-configured backing relational store

    '
  integration_test: required
  integration_test_reason: POST event ingestion and persistence to backing store require real database
    connectivity.
  e2e: true
  e2e_reason: The ingest endpoint is a REST transport surface (POST /events/ingest/{source_id}/{table})
    that requires a live running server to verify HTTP routing, persistence, and response codes.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - data-engineer
  - app-developer
  competitive_position:
    status: ahead
    rationale: A governed HTTP push receiver (ingest source) that writes external events from OTEL Collector,
      Fluentd, and webhooks into Provisa's governed data fabric is not present in Trino, Hasura, Denodo,
      or Dremio. These platforms are read-only federation engines; Provisa uniquely owns the write path.
- id: REQ-332
  status: complete
  group: 4. Source Connectors
  category: Ingest Sources — Governed HTTP Push Receiver (REQ-331–337)
  priority: SHOULD
  type: structural
  description: Each ingest source specifies a SQLAlchemy-compatible connection (dialect, host, port, database,
    username/password). Provisa creates one `AsyncEngine` per source at startup and reuses it across all
    requests. The `dialect` field on the `sources` table carries the SQLAlchemy driver string (e.g. `postgresql+asyncpg`,
    `mysql+aiomysql`).
  use_case: Per-source SQLAlchemy AsyncEngine lets ingest tables target any supported relational store
    asynchronously.
  code:
  - provisa/ingest/
  tests:
  - tests/unit/test_ingest_ddl.py
  - tests/unit/test_ingest_router.py
  since: 2026-01
  integration_test: required
  integration_test_reason: Per-source AsyncEngine creation and reuse requires live database connectivity.
  stakeholders:
  - data-engineer
  - ops
  competitive_position:
    status: ahead
    rationale: Governed HTTP push receiver with per-source AsyncEngine pooling is more operationally complete
      than ad-hoc ingestion in Hasura Actions or Dremio.
  related_reqs:
  - REQ-828
- id: REQ-333
  status: complete
  group: 4. Source Connectors
  category: Ingest Sources — Governed HTTP Push Receiver (REQ-331–337)
  priority: SHOULD
  type: behavioral
  description: Stewards define the schema for each ingest table as a list of columns with `column_name`,
    `data_type` (SQL type, allowlisted), and `path` (dot-notation extraction path into the POST payload).
    Provisa auto-generates and executes `CREATE TABLE IF NOT EXISTS` DDL at startup using these definitions.
    System columns `_received_at` and `_updated_at` (TIMESTAMPTZ) are always injected.
  use_case: Auto-generated DDL from column definitions creates ingest tables at startup with system audit
    columns included.
  code:
  - provisa/ingest/
  tests:
  - tests/unit/test_ingest_ddl.py
  - tests/unit/test_ingest_router.py
  - tests/integration/test_ingest_push_integration.py
  since: 2026-01
  scenario: 'Given an ingest table defined with column_name, data_type, and path for each column

    When Provisa starts up

    Then CREATE TABLE IF NOT EXISTS DDL is executed with system columns _received_at and _updated_at injected

    '
  integration_test: required
  integration_test_reason: DDL execution and system column injection require real database schema creation.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - data-engineer
  - ops
  competitive_position:
    status: ahead
    rationale: Auto-generated DDL with system audit columns (_received_at, _updated_at) at startup from
      steward-declared column definitions is not present in Trino or Hasura; Denodo requires external
      DDL management. Provisa treats governance schema creation as a first-class operation.
- id: REQ-334
  status: complete
  group: 4. Source Connectors
  category: Ingest Sources — Governed HTTP Push Receiver (REQ-331–337)
  priority: SHOULD
  type: behavioral
  description: The `path` field on `table_columns` uses dot-notation to walk nested JSON payloads. Array
    index segments are supported (e.g. `resourceLogs.0.resource.attributes`). If `path` is absent, the
    column name is used as the top-level key. Missing paths yield NULL.
  use_case: Dot-notation path extraction lets stewards map deeply nested JSON payloads to flat relational
    columns.
  code:
  - provisa/ingest/
  tests:
  - tests/unit/test_ingest_ddl.py
  - tests/unit/test_ingest_router.py
  since: 2026-01
  scenario: 'Given an ingest column with path "resourceLogs.0.resource.attributes"

    When a POST payload is received

    Then the value at that nested path is extracted into the column and missing paths yield NULL

    '
  integration_test: not_required
  integration_test_reason: Dot-notation JSON path extraction is pure logic; mocks can verify nested payload
    mapping.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - data-engineer
  competitive_position:
    status: ahead
    rationale: Dot-notation path extraction with array index support for nested JSON ingest columns is
      more flexible than Hasura's flat column model.
- id: REQ-335
  status: complete
  group: 4. Source Connectors
  category: Ingest Sources — Governed HTTP Push Receiver (REQ-331–337)
  priority: SHOULD
  type: behavioral
  description: The ingest endpoint accepts a single JSON object or a JSON array of objects per request.
    Each event in the array is extracted and written as a separate row. Returns HTTP 202 with a count
    of inserted rows. Source or table not found returns 404; engine unavailable returns 503.
  use_case: Batch ingest endpoint accepts arrays of events and returns inserted count so producers confirm
    delivery.
  code:
  - provisa/ingest/
  tests:
  - tests/unit/test_ingest_ddl.py
  - tests/unit/test_ingest_router.py
  - tests/integration/test_ingest_push_integration.py
  - provisa-ui/e2e/infrastructure.spec.ts
  since: 2026-01
  scenario: 'Given a POST to the ingest endpoint with a JSON array of events

    When all events are written

    Then HTTP 202 is returned with the count of inserted rows; 404 for unknown source/table, 503 for unavailable
    engine

    '
  integration_test: required
  integration_test_reason: HTTP 202 response and row insertion count require real database transaction
    verification.
  e2e: true
  e2e_reason: HTTP 202 response with inserted row count, 404/503 error codes are REST transport behaviors
    that require a real running server and cannot be verified by unit or integration tests alone.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - data-engineer
  - app-developer
  competitive_position:
    status: parity
    rationale: Batch JSON ingestion endpoint with HTTP 202 and inserted count is a standard webhook receiver
      pattern available in many platforms including Hasura Event Triggers and generic HTTP sinks for Kafka/Flink
      pipelines.
- id: REQ-336
  status: complete
  group: 4. Source Connectors
  category: Ingest Sources — Governed HTTP Push Receiver (REQ-331–337)
  priority: SHOULD
  type: behavioral
  description: 'Ingest tables are subscribable via the standard SSE endpoint (`GET /data/subscribe/{table}`).
    The subscription provider polls `_updated_at` watermark at a configurable interval (default: 5 s).
    Full governance is applied: RLS row filtering and column masking are enforced identically to local
    table subscriptions.'
  use_case: Ingest tables subscribable via SSE lets consumers receive pushed events as a live stream with
    full governance.
  code:
  - provisa/ingest/
  tests:
  - tests/unit/test_ingest_ddl.py
  - tests/unit/test_ingest_router.py
  - tests/integration/test_ingest_push_integration.py
  - provisa-ui/e2e/infrastructure.spec.ts
  since: 2026-01
  scenario: 'Given an ingest table with SSE subscription active

    When new events are ingested and the _updated_at watermark advances

    Then subscribers receive new rows via SSE with RLS and column masking applied

    '
  integration_test: required
  integration_test_reason: SSE subscription with watermark polling and governance enforcement require
    live subscription stream.
  e2e: true
  e2e_reason: SSE subscription delivery over GET /data/subscribe/{table} is a live network transport that
    requires a real running server to verify event streaming, watermark polling, and governance enforcement
    across the wire.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - data-engineer
  - app-developer
  - compliance
  competitive_position:
    status: ahead
    rationale: SSE subscription on ingest tables with _updated_at watermark polling and full governance
      (RLS + column masking) on the live stream is not available in Trino, Denodo, or Dremio. Hasura supports
      live queries but not on push-ingested tables with unified governance.
- id: REQ-337
  status: complete
  group: 4. Source Connectors
  category: Ingest Sources — Governed HTTP Push Receiver (REQ-331–337)
  priority: SHOULD
  type: structural
  description: '`table_columns.data_type TEXT` stores the steward-declared SQL type for ingest columns.
    This field is also used by future schema introspection tooling for sources where Trino is not available
    (ingest, API sources).'
  use_case: data_type column on table_columns stores the steward-declared SQL type needed for non-Trino
    source introspection.
  code:
  - provisa/ingest/
  tests:
  - tests/unit/test_ingest_ddl.py
  - tests/unit/test_ingest_router.py
  since: 2026-01
  integration_test: not_required
  integration_test_reason: data_type column storage is schema metadata; schema introspection tooling can
    use mocks.
  stakeholders:
  - data-engineer
  competitive_position:
    status: neutral
    rationale: ''
- id: REQ-338
  status: complete
  group: 4. Source Connectors
  category: Phase AT — WebSocket & RSS Sources (REQ-338–344)
  priority: SHOULD
  type: behavioral
  description: '`websocket` is a supported source type. Connects to a WebSocket server, optionally sends
    a JSON subscribe payload on connect, receives JSON messages as a stream of ChangeEvents.'
  use_case: WebSocket sources feed real-time events (market data, IoT, CDC streams) into the governed
    data fabric.
  code:
  - provisa/subscriptions/
  tests:
  - tests/unit/test_websocket_provider.py
  - tests/unit/test_rss_provider.py
  - tests/integration/test_websocket_rss_integration.py
  since: 2026-01
  scenario: 'Given a WebSocket source registered with an optional subscribe payload

    When Provisa connects to the WebSocket server

    Then received JSON messages are emitted as ChangeEvents into the governed data fabric

    '
  integration_test: required
  integration_test_reason: WebSocket server connectivity and message streaming require live endpoint.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - data-engineer
  - app-developer
  competitive_position:
    status: ahead
    rationale: WebSocket as a native governed source type feeding the query fabric is not present in Trino,
      Hasura, Denodo, or Dremio. These platforms handle streaming data through separate pipeline infrastructure
      (Kafka, Flink); Provisa natively subscribes to WebSocket streams.
- id: REQ-339
  status: complete
  group: 4. Source Connectors
  category: Phase AT — WebSocket & RSS Sources (REQ-338–344)
  priority: SHOULD
  type: behavioral
  description: 'WebSocket provider auto-reconnects on disconnect or error with configurable `reconnect_interval`
    (default: 5 s). The reconnect loop runs until `close()` is called.'
  use_case: Auto-reconnect ensures continuous event ingestion without operator intervention on transient
    disconnects.
  code:
  - provisa/subscriptions/
  tests:
  - tests/unit/test_websocket_provider.py
  - tests/unit/test_rss_provider.py
  - tests/integration/test_websocket_rss_integration.py
  since: 2026-01
  scenario: 'Given a WebSocket source that experiences a transient disconnect

    When the connection drops

    Then the provider auto-reconnects after reconnect_interval and continues emitting events until close()
    is called

    '
  integration_test: required
  integration_test_reason: Auto-reconnect behavior verification requires simulated WebSocket disconnects.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - data-engineer
  - ops
  competitive_position:
    status: ahead
    rationale: Built-in configurable auto-reconnect for WebSocket sources is a built-in operational feature
      not present in any competitor's native connector; competitors offload reconnection logic to external
      pipeline tools.
- id: REQ-340
  status: complete
  group: 4. Source Connectors
  category: Phase AT — WebSocket & RSS Sources (REQ-338–344)
  priority: SHOULD
  type: structural
  description: 'WebSocket message fields: `op` (insert/update/delete, default insert), `_ts`/`timestamp`
    (ISO 8601 or Unix epoch float, default now), all other fields become the row. `event_path` dot-notation
    extracts a sub-object from the message before mapping.'
  use_case: Standardized message field mapping lets stewards define schema once regardless of upstream
    message shape.
  code:
  - provisa/subscriptions/
  tests:
  - tests/unit/test_websocket_provider.py
  - tests/unit/test_rss_provider.py
  since: 2026-01
  integration_test: not_required
  integration_test_reason: Message field mapping and operation parsing is pure logic.
  stakeholders:
  - data-engineer
  competitive_position:
    status: ahead
    rationale: WebSocket source with op/timestamp field semantics and dot-notation event_path extraction
      is not available in Trino, Hasura, or Dremio as a first-class connector.
- id: REQ-341
  status: complete
  group: 4. Source Connectors
  category: Phase AT — WebSocket & RSS Sources (REQ-338–344)
  priority: SHOULD
  type: structural
  description: WebSocket source connection URL is constructed from `host`, `port`, `path`, and `use_ssl`
    (federation_hints). `subscribe_payload` (JSON string in federation_hints) is sent on connect. `event_path`
    (federation_hints) selects a sub-field of each message.
  use_case: URL construction from config allows WebSocket sources to be registered via standard Provisa
    YAML config.
  code:
  - provisa/subscriptions/
  tests:
  - tests/unit/test_websocket_provider.py
  - tests/unit/test_rss_provider.py
  since: 2026-01
  integration_test: required
  integration_test_reason: URL construction and subscribe payload delivery require live WebSocket endpoint.
  stakeholders:
  - data-engineer
  - app-developer
  competitive_position:
    status: neutral
    rationale: ''
- id: REQ-342
  status: complete
  group: 4. Source Connectors
  category: Phase AT — WebSocket & RSS Sources (REQ-338–344)
  priority: SHOULD
  type: behavioral
  description: '`rss` is a supported source type. Polls an RSS 2.0 or Atom feed URL at a configurable
    interval (default: 300 s = 5 min). Only items with a publication date newer than the last-seen watermark
    are emitted as ChangeEvents with `operation="insert"`.'
  use_case: RSS/Atom feeds become governed polling sources for news, alerts, and changelog data.
  code:
  - provisa/subscriptions/
  tests:
  - tests/unit/test_websocket_provider.py
  - tests/unit/test_rss_provider.py
  - tests/integration/test_websocket_rss_integration.py
  since: 2026-01
  scenario: 'Given an RSS source polling an Atom feed every 300 seconds

    When new items are published after the last-seen watermark

    Then only those items are emitted as ChangeEvents with operation="insert"

    '
  integration_test: required
  integration_test_reason: RSS/Atom feed polling and watermark tracking require live feed endpoints.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - data-engineer
  - app-developer
  competitive_position:
    status: ahead
    rationale: RSS/Atom as a native governed polling source is not present in Trino, Hasura, Denodo, or
      Dremio. This is a unique low-barrier connector for content, alerting, and changelog data that requires
      no external pipeline tooling.
- id: REQ-343
  status: complete
  group: 4. Source Connectors
  category: Phase AT — WebSocket & RSS Sources (REQ-338–344)
  priority: SHOULD
  type: behavioral
  description: 'RSS parser handles both RSS 2.0 (`<item>`) and Atom (`<entry>`) formats. Fields extracted:
    `title`, `link`, `description`/`summary`, `published` (pubDate/updated), `id` (guid/id). Dates accept
    RFC 2822 and ISO 8601 formats; unparseable dates fall back to a sentinel minimum date (`datetime.min`,
    UTC) to explicitly mark them unparseable rather than silently substituting the current time.'
  use_case: RSS 2.0 and Atom format parity ensures any feed can be registered without format-specific
    handling.
  code:
  - provisa/subscriptions/
  tests:
  - tests/unit/test_websocket_provider.py
  - tests/unit/test_rss_provider.py
  - tests/integration/test_websocket_rss_integration.py
  since: 2026-01
  scenario: 'Given an RSS 2.0 feed and an Atom feed registered as sources

    When items are parsed

    Then both formats extract title, link, description/summary, published, and id; unparseable dates use
    datetime.min

    '
  integration_test: required
  integration_test_reason: RSS 2.0 and Atom format parsing with real feed data requires network fetch.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - data-engineer
  - app-developer
  competitive_position:
    status: ahead
    rationale: Native RSS/Atom connector with normalized field extraction is not available in Trino, Hasura,
      Dremio, or Snowflake without custom code.
- id: REQ-344
  status: complete
  group: 4. Source Connectors
  category: Phase AT — WebSocket & RSS Sources (REQ-338–344)
  priority: SHOULD
  type: structural
  description: RSS source URL is resolved from `federation_hints.feed_url` if present, otherwise built
    from `host`, `port`, `path`, and `use_ssl`. `poll_interval` (federation_hints) overrides the default.
  use_case: Feed URL resolution from config enables RSS sources to be registered the same way as other
    API sources.
  code:
  - provisa/subscriptions/
  tests:
  - tests/unit/test_websocket_provider.py
  - tests/unit/test_rss_provider.py
  since: 2026-01
  integration_test: not_required
  integration_test_reason: URL resolution from config is pure config logic.
  stakeholders:
  - data-engineer
  competitive_position:
    status: neutral
    rationale: ''
- id: REQ-372
  status: complete
  group: 4. Source Connectors
  category: File & Lake Sources
  priority: SHOULD
  type: behavioral
  description: Iceberg and Delta Lake sources support time-travel queries via an optional `as_of` argument
    on root query fields (snapshot ID, version number, or ISO 8601 timestamp). The compiler translates
    `as_of` to Trino `FOR TIMESTAMP AS OF` / `FOR VERSION AS OF` syntax. Time-travel is read-only and
    only available on source types whose Trino connector supports it. Queries against non-time-travel-capable
    sources that supply `as_of` are rejected at compile time.
  use_case: Time-travel queries let consumers audit historical snapshots of Iceberg and Delta tables without
    separate table copies.
  code:
  - provisa/compiler/sql_gen.py
  tests:
  - tests/unit/test_time_travel.py
  - tests/integration/test_registration_governance_integration.py
  since: 2026-01
  scenario: 'Given an Iceberg source with an as_of argument supplied at query time

    When the compiler processes the query

    Then FOR TIMESTAMP AS OF / FOR VERSION AS OF syntax is emitted; non-capable sources with as_of are
    rejected

    '
  integration_test: required
  integration_test_reason: Time-travel query translation requires live Iceberg/Delta tables with snapshot
    history.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - data-engineer
  - app-developer
  competitive_position:
    status: parity
    rationale: Time-travel queries (FOR TIMESTAMP AS OF / FOR VERSION AS OF) on Iceberg and Delta Lake
      are natively supported by Trino and Dremio. Snowflake also has time-travel; this is expected on
      any lakehouse-capable platform.
  related_reqs:
  - REQ-826
  - REQ-825
- id: REQ-419
  status: complete
  group: 4. Source Connectors
  category: Vector Search
  priority: MUST
  type: structural
  description: A model registry must be declared in Provisa config listing available embedding models
    with id, provider, dimensions, API key env var or base URL, and enabled flag. Models not in the registry
    cannot be used for embedding generation or query vectorization.
  use_case: Explicit model registry enables air-gapped deployments and audit trails for embedding model
    usage.
  code:
  - provisa/vector/registry.py
  tests:
  - tests/unit/test_vector.py
  since: 2026-01
  integration_test: not_required
  integration_test_reason: Model registry declaration and validation is config schema logic.
  stakeholders:
  - data-engineer
  - ops
  competitive_position:
    status: ahead
    rationale: An explicit model registry for embedding models with air-gapped deployment support and
      audit-trail integration is not present in Trino, Hasura, Denodo, or Dremio. Snowflake Cortex has
      a similar concept but is cloud-only; Provisa's registry enables on-premise regulated deployments.
- id: REQ-420
  status: complete
  group: 4. Source Connectors
  category: Vector Search
  priority: SHOULD
  type: structural
  description: 'Model registry must support multiple provider types: OpenAI-compatible APIs, Ollama (local),
    and local HuggingFace model paths. This enables air-gapped and regulated deployments to use on-premise
    models throughout.'
  use_case: Multi-provider support removes vendor lock-in and enables regulated deployments without external
    API calls.
  code:
  - provisa/vector/providers.py
  tests:
  - tests/unit/test_vector.py
  since: 2026-01
  integration_test: deferred
  integration_test_reason: 'Multi-provider model support (OpenAI, Ollama, HuggingFace) requires external
    API/model verification. [deferred: requires external infrastructure not in CI stack]'
  stakeholders:
  - data-engineer
  - ops
  competitive_position:
    status: ahead
    rationale: Multi-provider embedding model support (OpenAI, Ollama local, HuggingFace local paths)
      within a federation platform is unique. Denodo, Trino, and Dremio have no embedding model integration;
      Snowflake Cortex is locked to Snowflake-hosted models only.
- id: REQ-421
  status: complete
  group: 4. Source Connectors
  category: Vector Search
  priority: SHOULD
  type: structural
  description: 'A column may be declared as an embedding vector by setting `embedding: true` with attributes:
    model id (must reference model registry), dimensions, and optional source_column mapping to the physical
    column name in the source.'
  use_case: Schema-level embedding declarations enable Provisa to manage vector columns as first-class
    citizens.
  code:
  - provisa/compiler/introspect.py
  tests:
  - tests/unit/test_vector.py
  since: 2026-01
  integration_test: not_required
  integration_test_reason: Embedding column schema declaration is metadata storage.
  stakeholders:
  - data-engineer
  competitive_position:
    status: ahead
    rationale: Schema-level embedding column declarations as first-class citizens in a federation layer
      are not present in Trino, Hasura, Denodo, or Dremio. Snowflake supports embedding columns only within
      Snowflake tables, not across federated sources.
- id: REQ-422
  status: complete
  group: 4. Source Connectors
  category: Vector Search
  priority: SHOULD
  type: behavioral
  description: 'Source capability auto-detection at registration time must identify native vector support:
    pgvector extension for PostgreSQL, Atlas Vector Search for MongoDB, Cortex for Snowflake. Sources
    without detected capability are flagged as requiring fallback.'
  use_case: Early capability detection allows fallback materialization strategy to be planned at source
    registration.
  code:
  - provisa/source_adapters/introspect.py
  tests:
  - tests/unit/test_vector.py
  since: 2026-01
  scenario: 'Given a PostgreSQL source being registered

    When Provisa checks for native vector support

    Then it detects the pgvector extension and marks the source as native-capable, or flags it for fallback

    '
  integration_test: deferred
  integration_test_reason: 'Capability auto-detection at registration requires querying live sources (pgvector,
    MongoDB Atlas, Snowflake). [deferred: requires external infrastructure not in CI stack]'
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - data-engineer
  - ops
  competitive_position:
    status: ahead
    rationale: Automatic vector capability detection at source registration time (pgvector, MongoDB Atlas
      Vector Search, Snowflake Cortex) with planned fallback materialization strategy is not present in
      any competitor federation platform; vector search is only available within individual source silos.
- id: REQ-423
  status: complete
  group: 4. Source Connectors
  category: Vector Search
  priority: SHOULD
  type: behavioral
  description: A `cosine_similarity(column, query_vector)` UDF must be available in Provisa SQL. It translates
    to the native vector operator for sources with native capability (pgvector `<=>`, Snowflake `VECTOR_COSINE_SIMILARITY()`,
    etc.) and to the pgvector fallback cache for sources without.
  use_case: Unified UDF hides native vs. fallback complexity from analysts.
  code:
  - provisa/compiler/sql_gen.py
  tests:
  - tests/unit/test_vector.py
  since: 2026-01
  scenario: 'Given a query using cosine_similarity(column, query_vector)

    When compiled for a native-capable source

    Then the UDF translates to the native vector operator; for non-native sources it routes to the pgvector
    fallback cache

    '
  integration_test: deferred
  integration_test_reason: 'cosine_similarity UDF translation requires real vector operations on native
    and fallback paths. [deferred: requires external infrastructure not in CI stack]'
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - data-engineer
  - app-developer
  competitive_position:
    status: ahead
    rationale: A unified cosine_similarity UDF that transparently routes to native vector operators (pgvector
      <=>), Snowflake VECTOR_COSINE_SIMILARITY, or fallback cache is a unique federation-layer abstraction;
      no competitor provides a cross-source unified vector search UDF.
- id: REQ-424
  status: complete
  group: 4. Source Connectors
  category: Vector Search
  priority: SHOULD
  type: behavioral
  description: For sources without native vector capability, Provisa must transparently materialize the
    embedding column to an internal pgvector-enabled PostgreSQL cache table, build an HNSW index, rewrite
    the query against the cache joining PKs back to the source, and return unified results. The caller
    must not be aware of the fallback path.
  use_case: Transparent fallback enables vector search on any source without source-side changes.
  code:
  - provisa/vector/fallback_cache.py
  tests:
  - tests/unit/test_table_search.py
  - tests/unit/test_vector.py
  since: 2026-01
  scenario: 'Given a source without native vector capability

    When a cosine_similarity query is executed

    Then the embedding column is materialized to the pgvector cache, an HNSW index is built, and results
    are returned transparently

    '
  integration_test: deferred
  integration_test_reason: 'Transparent fallback to pgvector cache requires real PostgreSQL, S3, and HNSW
    indexing. [deferred: requires external infrastructure not in CI stack]'
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - data-engineer
  - ops
  competitive_position:
    status: ahead
    rationale: Transparent pgvector fallback materialization with HNSW index for sources lacking native
      vector capability is unique to Provisa; competitors require vector search to be handled within the
      native source and provide no cross-source fallback path.
- id: REQ-425
  status: complete
  group: 4. Source Connectors
  category: Vector Search
  priority: MUST
  type: constraint
  description: The pgvector fallback cache must support TTL-based invalidation, mutation-triggered invalidation,
    manual refresh via admin API, and row count drift detection. Stale cache must not silently serve results
    beyond configured TTL.
  use_case: Multiple invalidation strategies with drift detection prevent silent data staleness.
  code:
  - provisa/vector/cache_invalidation.py
  tests:
  - tests/unit/test_config_validation_edge_cases.py
  - tests/unit/test_vector.py
  since: 2026-01
  scenario: 'Given a pgvector fallback cache that has exceeded its configured TTL

    When a cosine_similarity query is issued

    Then the stale cache is not served and invalidation or refresh is triggered

    '
  integration_test: deferred
  integration_test_reason: 'TTL-based invalidation and drift detection require monitoring live cache state.
    [deferred: requires external infrastructure not in CI stack]'
  stakeholders:
  - ops
  - compliance
  competitive_position:
    status: ahead
    rationale: Multi-strategy cache invalidation (TTL, mutation-triggered, manual refresh, row count drift
      detection) for the vector fallback cache is a sophisticated operational feature not present in any
      competitor federation platform, which lack vector fallback infrastructure entirely.
  related_reqs:
  - REQ-826
  - REQ-844
  - REQ-856
- id: REQ-426
  status: complete
  group: 4. Source Connectors
  category: Vector Search
  priority: MUST
  type: constraint
  description: 'Embedding columns are subject to the full Provisa governance stack: RLS, column masking,
    domain boundary rules, and sensitivity tiers. A `restricted` sensitivity tier on an embedding column
    must prevent cosine similarity search for unauthorized roles.'
  use_case: Embedding vectors inherit standard governance, preventing privacy bypass through vector search.
  code:
  - provisa/compiler/sql_gen.py
  tests:
  - tests/unit/test_vector.py
  since: 2026-01
  scenario: 'Given an embedding column with a restricted sensitivity tier

    When an unauthorized role attempts a cosine_similarity search on that column

    Then the query is rejected and the column is absent from the schema for that role

    '
  integration_test: deferred
  integration_test_reason: 'Governance enforcement on embeddings requires Stage 2 pipeline with real data
    and roles. [deferred: requires external infrastructure not in CI stack]'
  stakeholders:
  - data-engineer
  - compliance
  competitive_position:
    status: ahead
    rationale: Embedding columns subject to Provisa's full governance stack (RLS, masking, sensitivity
      tiers, domain boundaries) with all-or-nothing access enforcement prevents privacy bypass through
      vector search — a governance gap in all competitor platforms that expose vector search.
- id: REQ-427
  status: complete
  group: 4. Source Connectors
  category: Vector Search
  priority: SHOULD
  type: behavioral
  description: A virtual embedding column may be declared on a table with a `generated_from` subquery
    that must return exactly one text value per row. The subquery may reference multiple columns, apply
    transformations, and include joins to related tables. Provisa validates the subquery against a sample
    row at declaration time.
  use_case: Generated embedding columns enable text-to-vector transformations without storing raw text.
  code:
  - provisa/vector/generation.py
  tests:
  - tests/unit/test_table_search.py
  - tests/unit/test_vector.py
  since: 2026-01
  scenario: 'Given a virtual embedding column declared with a generated_from subquery

    When the column is declared

    Then Provisa validates the subquery returns exactly one text value per row against a sample row

    '
  integration_test: deferred
  integration_test_reason: 'Virtual embedding generation from subquery requires live source data and sample
    validation. [deferred: requires external infrastructure not in CI stack]'
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - data-engineer
  - app-developer
  competitive_position:
    status: ahead
    rationale: Virtual embedding columns with generated_from subquery (computed from multi-column transformations
      and cross-table joins, validated at declaration time) are not present in Trino, Hasura, Denodo,
      Dremio, or Snowflake Cortex, which require pre-computed vectors stored in source tables.
- id: REQ-428
  status: complete
  group: 4. Source Connectors
  category: Vector Search
  priority: SHOULD
  type: behavioral
  description: Generated embedding columns must support scheduled incremental refresh — only rows where
    the source data has changed since last embedding are re-embedded. Full rebuild is triggered on model
    change or schema change affecting the generating subquery.
  use_case: Incremental refresh avoids unnecessary embedding API calls and reduces operational cost.
  code:
  - provisa/vector/scheduled_refresh.py
  tests:
  - tests/unit/test_vector.py
  since: 2026-01
  scenario: 'Given a generated embedding column with changed source rows

    When the scheduled incremental refresh runs

    Then only changed rows are re-embedded; a model or schema change triggers a full rebuild

    '
  integration_test: deferred
  integration_test_reason: 'Incremental embedding refresh requires real model API calls and watermark
    tracking. [deferred: requires external infrastructure not in CI stack]'
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - data-engineer
  - ops
  competitive_position:
    status: ahead
    rationale: Scheduled incremental embedding refresh (re-embed only changed rows, full rebuild on model/schema
      change) is a unique operational capability; no competitor federation platform manages embedding
      lifecycle — they rely on external ETL pipelines.
- id: REQ-429
  status: complete
  group: 4. Source Connectors
  category: Vector Search
  priority: MUST
  type: constraint
  description: Once an embedding column is generated with a declared model, that model is locked for that
    column. Provisa must reject queries that attempt to use a query vector from a different model or incompatible
    dimensions, with a clear error identifying the conflict.
  use_case: Model locking prevents silent semantic errors from mismatched embedding spaces.
  code:
  - provisa/compiler/sql_gen.py
  tests:
  - tests/unit/test_vector.py
  since: 2026-01
  scenario: 'Given an embedding column locked to a specific model

    When a query attempts cosine_similarity with a vector from a different model or incompatible dimensions

    Then the query is rejected with a clear error identifying the model/dimension conflict

    '
  integration_test: not_required
  integration_test_reason: Model locking validation and dimension mismatch detection is pure logic.
  stakeholders:
  - data-engineer
  - app-developer
  competitive_position:
    status: ahead
    rationale: Compile-time model locking and dimension mismatch rejection for vector searches prevents
      silent semantic errors from mismatched embedding spaces — a correctness guarantee not present in
      any competitor federation platform.
- id: REQ-430
  status: complete
  group: 4. Source Connectors
  category: Vector Search
  priority: SHOULD
  type: behavioral
  description: 'Query-time vectorization must be supported: when a similarity search is expressed with
    a text string rather than a raw vector, Provisa calls the declared embedding model to generate the
    query vector before executing the search. Both text input and raw vector input must be supported interfaces.'
  use_case: Query-time vectorization enables natural text-based search without pre-vectorization ceremony.
  code:
  - provisa/vector/query_vectorization.py
  tests:
  - tests/unit/test_schema_visibility_filters.py
  - tests/unit/test_vector.py
  since: 2026-01
  scenario: 'Given a similarity search expressed with a text string

    When the query is executed

    Then Provisa calls the declared embedding model to generate the query vector before running the search

    '
  integration_test: deferred
  integration_test_reason: 'Query-time vectorization requires live embedding model API calls. [deferred:
    requires external infrastructure not in CI stack]'
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - data-engineer
  - app-developer
  competitive_position:
    status: ahead
    rationale: Query-time vectorization (text string → embedding → similarity search inline) as a first-class
      operation in a federation platform is unique; Snowflake Cortex supports it only within Snowflake;
      no cross-source federation platform offers this capability.
- id: REQ-431
  status: complete
  group: 4. Source Connectors
  category: Vector Search
  priority: SHOULD
  type: structural
  description: 'Vector search features are organized as three independent phases: (1) native vector search
    with model registry, embedding column declaration, and cosine_similarity UDF translation; (2) non-native
    fallback via pgvector cache materialization; (3) declarative embedding generation from source text
    columns. Each phase is independently deployable and delivers standalone value.'
  use_case: Phased decomposition enables incremental feature adoption and reduces delivery risk.
  code:
  - provisa/vector/
  tests:
  - tests/unit/test_schema_visibility_filters.py
  - tests/unit/test_vector.py
  since: 2026-01
  integration_test: deferred
  integration_test_reason: 'Three-phase vector feature architecture requires end-to-end testing of native,
    fallback, and generation phases. [deferred: requires external infrastructure not in CI stack]'
  stakeholders:
  - data-engineer
  - ops
  competitive_position:
    status: ahead
    rationale: Three-phase vector search with native cosine_similarity UDF translation and non-native
      fallback is more comprehensive than Hasura's nascent vector support and not in Trino/Dremio.
- id: REQ-673
  status: complete
  since: 2026-07
  group: 4. Source Connectors
  category: Cost Statistics
  priority: SHOULD
  type: structural
  description: 'Connector cardinality capability — cardinality(source, table) -> Estimate{value, exact,
    method}. A source may expose a CHEAP volume metric so a caller can size a table without a full scan.
    Resolution order: (1) a cheap native statistic when the source has one (GraphQL totalCount, OpenAPI
    X-Total-Count, PG pg_class.reltuples, Trino SHOW STATS, Parquet/Iceberg row-group metadata); (2) an
    exact COUNT(*) only when the connector reports counting is cheap; (3) UNKNOWN when sizing is expensive
    and no native estimate exists — fail-open, never a hidden full scan. The Estimate carries `exact`
    (true for a native count(*)/totalCount, false for reltuples-style estimates) and `method`, so a consumer
    knows what the number is and what it may be used for. The producer is SPARSE and OPT-IN: a native
    count is authored only for expensive-count sources — the API sources (openapi/graphql_remote/grpc),
    where a count today means materializing the full pull, and huge un-indexed row-stores. Cheap-count
    sources contribute nothing and carry no maintenance. Generalizes and supersedes the GQL-specific count_query
    helper (provisa/sources/counts.py) under the connector abstraction (REQ-840-843). DONE (2026-07):
    the estimate type + resolution order are implemented — provisa/federation/ cardinality.py::Estimate(value,
    exact, method: CardinalityMethod) and resolve_cardinality(native_stat, exact_count_cheap, exact_count)
    returning, in order, a source''s native statistic (exact totalCount or approximate reltuples), else
    an exact COUNT(*) only when the connector says counting is cheap, else UNKNOWN (fail-open, never a
    hidden full scan). REMAINING (kept in-progress): the per-source native-stat PRODUCERS wired onto the
    connectors (graphql totalCount, openapi X-Total-Count, PG reltuples, Trino SHOW STATS, Parquet/Iceberg
    row-group metadata) — sparse/opt-in for expensive-count sources.'
  code:
  - provisa/federation/cardinality.py
  tests:
  - tests/unit/test_cardinality.py
  use_case: One estimate serves two consumer classes. The AUTOMATED PLANNER reads it to choose a federation
    strategy (materialize vs virtual, REQ-826), gate hot-table promotion (REQ-236) without a blind COUNT(*),
    and set redirect/probe thresholds. HUMAN PLANNERS read the same estimate — in the catalog, in plan/EXPLAIN
    output, and on the cold graph-counts path — to size a source before committing to an expensive federated
    query, with exact/estimate/unknown surfaced honestly. An "unknown, sizing requires a full pull" answer
    is itself actionable, unlike today's bare 0.
  related_reqs:
  - REQ-840
  - REQ-841
  - REQ-842
  - REQ-843
  - REQ-826
  - REQ-236
  - REQ-875
  stakeholders:
  - data-engineer
  - app-developer
  competitive_position:
    status: ahead
    rationale: A source-agnostic cardinality capability that prefers cheap native statistics and reports
      estimate/exact/unknown honestly — feeding both the query planner and human capacity planning — is
      absent in Hasura and Dremio.
- id: REQ-540
  status: complete
  group: 4. Source Connectors
  category: GovData Sources
  priority: SHOULD
  type: behavioral
  description: Sources of type `govdata` expose U.S. government open data partitioned by subject grouping.
    Configuring a govdata source with a subject exposes all schemas for that subject automatically.
  use_case: Subject-grouped GovData sources let stewards selectively grant access to government data by
    domain area.
  code:
  - provisa/core/models.py
  tests:
  - tests/integration/test_govdata_source.py
  - tests/unit/test_core_registration.py
  since: 2026-01
  scenario: 'Given a govdata source configured with a subject grouping

    When the source is registered

    Then all schemas for that subject are automatically exposed as governed tables

    '
  integration_test: required
  integration_test_reason: GovData subject-grouped source exposure requires live government data endpoint
    access.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - data-engineer
  - app-developer
  competitive_position:
    status: ahead
    rationale: Subject-grouped U.S. government open data (GovData) as a native source type is a domain-specific
      connector absent from Trino, Hasura, Denodo, and Dremio. This targets regulated government data
      consumers with automated schema exposure.
- id: REQ-541
  status: complete
  group: 4. Source Connectors
  category: GovData Sources
  priority: MUST
  type: constraint
  description: The `ref` and `geo` schemas are always included as linker schemas in every GovData source
    — they are not configurable and not tied to any subject grouping.
  use_case: Always-included linker schemas ensure GovData joins on reference and geography data are always
    available without explicit configuration.
  code:
  - provisa/core/models.py
  tests:
  - tests/integration/test_govdata_source.py
  - tests/unit/test_core_registration.py
  since: 2026-01
  scenario: 'Given any GovData source regardless of subject configuration

    When the source is registered

    Then the ref and geo schemas are always included as linker schemas

    '
  integration_test: required
  integration_test_reason: Linker schema inclusion verification requires live GovData endpoint with ref/geo
    schemas.
  stakeholders:
  - data-engineer
  - app-developer
  competitive_position:
    status: neutral
    rationale: ''
- id: REQ-550
  status: complete
  group: 4. Source Connectors
  category: Registration & Governance
  priority: SHOULD
  type: structural
  description: 'Sources are classified into three execution categories: Direct-capable (has both native
    driver and federated connector; executes single-source queries via native driver), Federation-only
    (no direct driver; always routed through Trino), and Materialize (no federated connector; data fetched
    and cached as Parquet in S3 or PostgreSQL before being queryable by the federation engine).'
  use_case: Tripartite source categorization gives stewards a clear mental model of how each source type
    executes queries and when the federation engine is involved.
  code:
  - provisa/core/models.py
  - provisa/transpiler/router.py
  tests:
  - tests/integration/test_direct_exec.py
  - tests/unit/test_routing.py
  since: 2026-01
  integration_test: not_required
  integration_test_reason: Source categorization is conceptual classification logic; routing decisions
    testable with mocks.
  stakeholders:
  - data-engineer
  - ops
  competitive_position:
    status: ahead
    rationale: Three-way source execution classification (direct-capable, federation-only, write-only)
      with automatic routing is more nuanced than Hasura's single-connector model or Trino's uniform federation.
- id: REQ-553
  status: complete
  group: 4. Source Connectors
  category: Registration & Governance
  priority: SHOULD
  type: structural
  description: File-based sources (`csv`, `parquet`, `sqlite`) use the `path` config field instead of
    `host`/`port` connection parameters. The `path` may be a local filesystem path or an fsspec-supported
    remote object-storage URL (`s3://`, `ftp://`, `sftp://`); the crawler detects fsspec URIs via `fsspec.core.url_to_fs`
    and walks them the same way as local directories.
  use_case: File path config for file-based sources lets stewards register local files and remote object-storage
    files as governed tables using the same config structure as network-connected sources.
  code:
  - provisa/core/models.py
  - provisa/file_source/source.py
  - provisa/file_source/crawler.py
  tests:
  - tests/integration/test_schema_gen.py
  - tests/unit/test_core_registration.py
  since: 2026-01
  integration_test: required
  integration_test_reason: File-based source registration requires real local file I/O and schema introspection.
  stakeholders:
  - data-engineer
  - ops
  competitive_position:
    status: parity
    rationale: File-based source registration (CSV, Parquet, SQLite) via path config is standard in Trino
      (file connector, Parquet on S3) and Dremio (local file sources). All serious federation platforms
      support file-based sources.
- id: REQ-597
  status: complete
  group: 4. Source Connectors
  category: GraphQL Remote Schema Connector (REQ-307–313)
  priority: SHOULD
  type: behavioral
  description: 'GraphQL remote source registration accepts a `field_overrides` map (`{fieldName: "query"
    | "mutation"}`) that is applied after introspection and takes priority over structural classification.
    Only query-type fields can be reclassified as mutations via overrides; mutation-type fields have no
    override path in GraphQL.'
  use_case: field_overrides lets stewards correct mis-classified remote fields (e.g. a mutation named
    like a query) at registration time without modifying the upstream schema.
  code:
  - provisa/graphql_remote/mapper.py
  tests:
  - tests/integration/test_graphql_execution.py
  - tests/integration/test_graphql_remote_source.py
  - tests/unit/test_graphql_remote_mapper.py
  - tests/unit/test_graphql_remote_overrides_requirements.py
  - tests/unit/test_misc_requirements.py
  - tests/unit/test_remote_adapter_contract.py
  - tests/unit/test_schema_service.py
  since: 2026-01
  scenario: 'Given a GQL remote source with a query-type field that behaves as a mutation

    When field_overrides maps that field to "mutation"

    Then the field is registered as a tracked function and the override takes priority over structural
    classification

    '
  integration_test: not_required
  integration_test_reason: field_overrides map application is pure schema transformation logic.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - data-engineer
  competitive_position:
    status: parity
    rationale: Field-level override to correct misclassified remote schema fields is a standard admin
      escape hatch; Hasura provides a similar mechanism for Remote Schema field type overrides. This is
      expected operational flexibility.
- id: REQ-598
  status: complete
  group: 4. Source Connectors
  category: GraphQL Remote Schema Connector (REQ-307–313)
  priority: SHOULD
  type: behavioral
  description: 'Remote schema source registrations (GraphQL, gRPC, OpenAPI) accept a `relationships` list
    that stores FK/PK join paths as manually declared relationships (no `remote_managed` flag). Relationships
    auto-detected during introspection are stored with `remote_managed: True`. On schema refresh, auto-detected
    (`remote_managed: True`) relationships are re-run and may change; manually declared relationships
    (no flag) are preserved unchanged.'
  use_case: Distinguishing manual from auto-detected relationships ensures steward-declared join paths
    survive schema refreshes without being overwritten by introspection results.
  code:
  - provisa/graphql_remote/mapper.py
  - provisa/api/admin/graphql_remote_router.py
  - provisa/api/admin/grpc_remote_router.py
  - provisa/api/admin/openapi_router.py
  tests:
  - tests/integration/test_graphql_execution.py
  - tests/integration/test_graphql_remote_source.py
  - tests/unit/test_graphql_remote_mapper.py
  - tests/unit/test_remote_adapter_contract.py
  - tests/unit/test_schema_service.py
  - provisa-ui/e2e/registration-admin.spec.ts
  since: 2026-01
  scenario: 'Given a remote schema source with both manually declared and auto-detected relationships

    When a schema refresh is triggered

    Then auto-detected relationships are re-run and may change; manually declared relationships are preserved
    unchanged

    '
  integration_test: not_required
  integration_test_reason: Relationship metadata storage and refresh preservation is config logic.
  e2e: true
  e2e_reason: Relationship preservation on schema refresh requires triggering a refresh via an admin API
    endpoint and verifying which relationships survive, crossing the REST API transport boundary with
    a real running server.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - data-engineer
  competitive_position:
    status: ahead
    rationale: Distinguishing manually declared relationships from auto-detected (remote_managed) ones
      so steward joins survive schema refresh without overwrite is a governance-preserving refresh model
      not present in Hasura (full refresh overwrites all joins) or Denodo.
- id: REQ-599
  status: complete
  group: 4. Source Connectors
  category: GraphQL Remote Schema Connector (REQ-307–313)
  priority: SHOULD
  type: behavioral
  description: For all remote schema source types (GraphQL, gRPC, OpenAPI), required input parameters
    that are not already response fields are registered as `_nf_`-prefixed native filter columns. The
    `native_filter_type` is `query_param` for GQL and OpenAPI query parameters, `path_param` for OpenAPI
    path parameters, and `grpc_input` for gRPC. When a parameter name matches a response field name, parameter
    metadata is merged into the existing column rather than creating a duplicate.
  use_case: Native filter columns let consumers filter remote-source queries using standard `where` arguments
    that are passed through to the upstream API rather than applied as post-fetch SQL predicates.
  code:
  - provisa/graphql_remote/mapper.py
  - provisa/api/admin/grpc_remote_router.py
  - provisa/openapi/register.py
  - provisa/api/app.py
  tests:
  - tests/e2e/test_grpc_query.py
  - tests/integration/test_graphql_execution.py
  - tests/integration/test_graphql_remote_source.py
  - tests/integration/test_grpc_execution.py
  - tests/integration/test_openapi_source.py
  - tests/unit/test_graphql_remote_mapper.py
  - tests/unit/test_remote_adapter_contract.py
  - tests/unit/test_schema_service.py
  since: 2026-01
  scenario: 'Given a remote schema source with required input parameters not in the response fields

    When the source is registered

    Then those parameters become _nf_-prefixed native filter columns with the appropriate native_filter_type

    '
  integration_test: not_required
  integration_test_reason: Native filter column registration is schema metadata; parameter type mapping
    is pure logic.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - data-engineer
  - app-developer
  competitive_position:
    status: ahead
    rationale: Native filter columns (_nf_-prefixed) that pass parameters through to upstream APIs as
      query/path/gRPC inputs rather than applying post-fetch predicates is a filter pushdown model for
      non-SQL sources not present in Hasura Actions, Denodo REST connectors, or Trino.
- id: REQ-600
  status: complete
  group: 4. Source Connectors
  category: GraphQL Remote Schema Connector (REQ-307–313)
  priority: MUST
  type: constraint
  description: 'Remote schema sources enforce depth limits on nested object expansion: `graphql_remote.max_object_depth`
    (default: 5) caps GQL OBJECT nesting; `graphql_remote.max_list_depth` (default: 2) caps LIST-typed
    nested OBJECT inclusion; `graphql_remote.max_list_items` (default: 100) injects `first: N` to cap
    array size. For gRPC and OpenAPI, nested message/object sub-field resolution is one level deep only.
    Fields beyond these limits are excluded from the fetch.'
  use_case: Depth limits prevent unbounded data expansion and runaway remote API calls when remote schemas
    contain deeply recursive or list-heavy structures.
  code:
  - provisa/graphql_remote/mapper.py
  - provisa/grpc_remote/mapper.py
  - provisa/openapi/register.py
  tests:
  - tests/integration/test_graphql_execution.py
  - tests/integration/test_graphql_remote_source.py
  - tests/unit/test_graphql_remote_mapper.py
  - tests/unit/test_grpc_remote_loader.py
  - tests/unit/test_openapi_loader.py
  - tests/unit/test_remote_adapter_contract.py
  - tests/unit/test_schema_service.py
  since: 2026-01
  scenario: 'Given a remote GraphQL schema with deeply nested OBJECT fields beyond max_object_depth

    When the schema is introspected

    Then fields beyond the depth limit are excluded from the fetch and array sizes are capped at max_list_items

    '
  integration_test: not_required
  integration_test_reason: Depth limit enforcement is pure logic; field exclusion testable with mocks.
  stakeholders:
  - ops
  competitive_position:
    status: ahead
    rationale: Configurable depth limits (max_object_depth, max_list_depth, max_list_items) to prevent
      unbounded remote schema expansion at introspection time are safety guards absent from Hasura Remote
      Schemas and Denodo GraphQL connectors, which have no built-in nesting limits.
- id: REQ-601
  status: complete
  group: 4. Source Connectors
  category: OpenAPI Auto-Registration Connector
  priority: SHOULD
  type: behavioral
  description: OpenAPI virtual table names are derived from the operation's `operationId`. If no `operationId`
    is defined, Provisa slugifies `{method}_{path}`. An alias is derived by stripping the leading verb
    segment and singularizing the noun (e.g. `findPetsByStatus` → `pet_by_status`). The alias is used
    as the consumer-facing name in GraphQL and other query interfaces.
  use_case: operationId-based naming and verb-stripped aliases give OpenAPI tables predictable, consumer-friendly
    names without manual configuration.
  code:
  - provisa/openapi/register.py
  tests:
  - tests/integration/test_openapi_pet_by_status.py
  - tests/unit/test_graphql_alias.py
  - tests/unit/test_openapi_loader.py
  since: 2026-01
  scenario: 'Given an OpenAPI spec with operationId "findPetsByStatus"

    When the spec is registered

    Then the virtual table alias is "pet_by_status" used as the consumer-facing GraphQL name

    '
  integration_test: not_required
  integration_test_reason: operationId-based naming and verb-stripping is pure string transformation.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - data-engineer
  - app-developer
  competitive_position:
    status: ahead
    rationale: Consumer-friendly alias derivation (verb-stripped, singularized noun) from OpenAPI operationId
      is automatic in Provisa; Hasura requires manual Action naming and Denodo exposes raw path-based
      identifiers.
- id: REQ-602
  status: complete
  group: 4. Source Connectors
  category: GraphQL Remote Schema Connector (REQ-307–313)
  priority: SHOULD
  type: behavioral
  description: Schema generation synthesizes `ColumnMetadata` for remote schema tables (GraphQL remote,
    gRPC remote, OpenAPI) at schema build time since these sources have no Trino catalog to introspect.
    Type mapping (GQL scalar → Provisa type, proto scalar → SQL type, JSON Schema → Provisa type) is applied
    during synthesis to produce typed column metadata equivalent to what catalog introspection produces
    for local relational tables.
  use_case: Synthesized ColumnMetadata lets remote schema tables participate in schema generation and
    query compilation identically to locally connected relational tables.
  code:
  - provisa/compiler/schema_gen.py
  - provisa/api/app.py
  tests:
  - tests/integration/test_graphql_execution.py
  - tests/integration/test_graphql_remote_source.py
  - tests/integration/test_schema_gen.py
  - tests/unit/test_graphql_remote_mapper.py
  - tests/unit/test_remote_adapter_contract.py
  - tests/unit/test_schema_service.py
  since: 2026-01
  scenario: 'Given remote schema tables (GraphQL remote, gRPC remote, OpenAPI) registered in Provisa

    When schema generation runs

    Then ColumnMetadata is synthesized with correct type mappings equivalent to catalog introspection
    for local tables

    '
  integration_test: required
  integration_test_reason: Schema generation with synthesized ColumnMetadata requires remote source type
    mappings verified against live endpoints.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - data-engineer
  - app-developer
  competitive_position:
    status: ahead
    rationale: Synthesized ColumnMetadata at schema build time for remote schema sources (GQL, gRPC, OpenAPI)
      that participates identically in query compilation is a unified schema model not present in Hasura
      (separate remote schema type system) or Trino (no remote schema concept).
- id: REQ-652
  status: complete
  group: 4. Source Connectors
  category: Vector Search
  priority: MUST
  type: constraint
  description: Column masking does not apply to embedding columns — a vector cannot be partially masked
    and remain semantically meaningful. Access to an embedding column is enforced via visibility (hide
    entirely), RLS (filter rows), sensitivity tier (block search), and domain boundary rules; the column
    is either visible and searchable for a given role, or absent from the schema entirely.
  use_case: Preventing partial-masking of vectors ensures access control is all-or-nothing for embedding
    columns, which cannot be meaningfully redacted at the field level.
  code:
  - provisa/vector/governance.py
  tests:
  - tests/unit/test_vector.py
  since: 2026-01
  scenario: 'Given an embedding column with access restrictions

    When a role without sufficient permissions queries the column

    Then the column is absent from the schema entirely; column masking is never applied to embedding columns

    '
  integration_test: deferred
  integration_test_reason: 'All-or-nothing embedding access control requires governance Stage 2 with real
    data and roles. [deferred: requires external infrastructure not in CI stack]'
  stakeholders:
  - compliance
  - data-engineer
  competitive_position:
    status: ahead
    rationale: All-or-nothing governance for embedding columns (no partial masking; must be fully visible
      or entirely absent) is a principled vector access control model not present in any competitor; Snowflake
      Cortex has no cross-source governance enforcement on embeddings.
- id: REQ-007
  status: complete
  group: 5. Query Languages, Compilation & Operations
  category: Compiler & Schema
  priority: SHOULD
  type: structural
  description: Compiler is purpose-built with no dependency on PostGraphile, DuckDB, or any third-party
    GraphQL server framework.
  use_case: Purpose-built compiler eliminates third-party framework constraints and reduces attack surface.
  code:
  - provisa/compiler/sql_gen.py
  tests:
  - tests/integration/test_schema_gen.py
  - tests/unit/test_schema_service.py
  - tests/unit/test_sql_gen.py
  since: 2026-01
  scenario: 'Given Provisa is running

    When the compiler processes a GraphQL query

    Then it executes without invoking PostGraphile, DuckDB, or any third-party GraphQL server framework

    '
  integration_test: not_required
  integration_test_reason: Purpose-built compiler architecture is a design property verifiable through
    unit/module tests.
  stakeholders:
  - app-developer
  - ops
  competitive_position:
    status: ahead
    rationale: Purpose-built compiler with no dependency on PostGraphile, DuckDB, or third-party GraphQL
      frameworks gives Provisa full control over governance injection at compile time — a key differentiator
      from Hasura.
- id: REQ-008
  status: complete
  group: 5. Query Languages, Compilation & Operations
  category: Compiler & Schema
  priority: SHOULD
  type: behavioral
  description: Schema generation pass runs at registration time; queries Trino INFORMATION_SCHEMA, applies
    per-role column visibility, incorporates relationships, produces GraphQL SDL.
  use_case: Schema generation at registration time means the GraphQL schema always reflects current data
    and role visibility.
  code:
  - provisa/compiler/sql_gen.py
  tests:
  - tests/integration/test_schema_gen.py
  - tests/unit/test_schema_service.py
  - tests/unit/test_sql_gen.py
  since: 2026-01
  scenario: 'Given a table is registered

    When the schema generation pass runs

    Then it queries Trino INFORMATION_SCHEMA, applies per-role column visibility, incorporates

    relationships, and produces GraphQL SDL

    '
  integration_test: required
  integration_test_reason: Schema generation at registration time requires real Trino INFORMATION_SCHEMA
    queries and role-based column filtering.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - data-engineer
  - app-developer
  competitive_position:
    status: parity
    rationale: Schema generation at registration time with role-based column visibility is standard in
      Hasura v2 and Denodo. Integrating RLS into schema generation is expected of any serious data virtualization
      platform.
- id: REQ-009
  status: complete
  group: 5. Query Languages, Compilation & Operations
  category: Compiler & Schema
  priority: MUST
  type: behavioral
  description: 'Query compilation produces PG-style SQL from validated GraphQL AST — single SQL statement,
    no resolver chain, no N+1. Exception: action query fields (REQ-361) and remote-schema relationships
    (REQ-313) resolve nested relationships via batched secondary fetches, which is outside this single-statement
    guarantee.'
  use_case: Single-statement SQL compilation eliminates N+1 query patterns and improves query execution
    performance.
  code:
  - provisa/compiler/sql_gen.py
  tests:
  - tests/integration/test_graphql_remote_source.py
  - tests/integration/test_schema_gen.py
  - tests/unit/test_layer_contracts.py
  - tests/unit/test_schema_service.py
  - tests/unit/test_sql_gen.py
  since: 2026-01
  scenario: 'Given a valid GraphQL query AST

    When the compiler processes it

    Then it emits a single PG-style SQL statement with no resolver chain and no N+1 pattern

    '
  integration_test: required
  integration_test_reason: Single-statement SQL compilation requires end-to-end testing through GraphQL
    parser, compiler, and Trino execution.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - data-engineer
  - app-developer
  - ops
  competitive_position:
    status: ahead
    rationale: Single-statement SQL compilation with no N+1 is a material differentiator vs Hasura's resolver
      chain model (Hasura v2 still uses DataLoader-style batching, not single-SQL joins). Trino-based
      federation engines (Trino/Presto) compile to single SQL but don't expose a GraphQL layer with this
      guarantee.
- id: REQ-010
  status: complete
  group: 5. Query Languages, Compilation & Operations
  category: Compiler & Schema
  priority: MUST
  type: structural
  description: 'Trino type mapping: VARCHAR→String, INTEGER→Int, BOOLEAN→Boolean, TIMESTAMP→DateTime,
    JSONB→JSON. Nullability preserved.'
  use_case: Consistent Trino type mapping ensures GraphQL consumers receive predictable, well-typed field
    values.
  code:
  - provisa/compiler/sql_gen.py
  tests:
  - tests/integration/test_schema_gen.py
  - tests/unit/test_schema_service.py
  - tests/unit/test_sql_gen.py
  since: 2026-01
  scenario: 'Given a Trino table with VARCHAR, INTEGER, BOOLEAN, TIMESTAMP, and JSONB columns

    When the schema compiler maps the column types

    Then GraphQL fields are typed String, Int, Boolean, DateTime, and JSON respectively with

    nullability preserved

    '
  integration_test: not_required
  integration_test_reason: Type mapping rules are pure logic verifiable through unit tests with mocked
    Trino types.
  stakeholders:
  - app-developer
  competitive_position:
    status: neutral
    rationale: ''
- id: REQ-011
  status: complete
  group: 5. Query Languages, Compilation & Operations
  category: Compiler & Schema
  priority: MUST
  type: constraint
  description: References to unregistered tables, excluded columns, undefined relationships, or type mismatches
    rejected at compile time with precise errors.
  use_case: Compile-time rejection of invalid references catches developer errors early before they reach
    the data layer.
  code:
  - provisa/compiler/sql_gen.py
  tests:
  - tests/integration/test_schema_gen.py
  - tests/unit/test_schema_service.py
  - tests/unit/test_sql_gen.py
  since: 2026-01
  scenario: 'Given a GraphQL query referencing an unregistered table or excluded column

    When the compiler processes it

    Then it rejects the query at compile time with a precise error

    '
  integration_test: not_required
  integration_test_reason: Compile-time validation is pure logic over AST structures, no database or runtime
    required.
  stakeholders:
  - app-developer
  competitive_position:
    status: parity
    rationale: Compile-time validation of schema references is standard in Hasura and any typed GraphQL
      layer. Denodo and Trino also reject references to unregistered/unknown objects at parse or plan
      time.
- id: REQ-032
  status: complete
  group: 5. Query Languages, Compilation & Operations
  category: Mutation Execution
  priority: MUST
  type: behavioral
  description: DB mutations are single-source by definition, bypass Trino, no routing decision, no registry
    approval — but always require write authority on the target table (REQ-033). Webhook mutations are
    governed separately and require specific steward approval for use (REQ-209).
  use_case: DB mutations skip the registry approval path but still require table write authority; webhook
    mutations require explicit approval.
  code:
  - provisa/compiler/sql_gen.py
  - provisa/executor/
  tests:
  - tests/unit/test_mutation_sql.py
  - tests/integration/test_mutations.py
  since: 2026-01
  scenario: 'Given a DB mutation targeting a registered table

    When the mutation is executed

    Then it bypasses Trino and registry approval but enforces write authority on the target table

    '
  integration_test: required
  integration_test_reason: Mutation routing, write authority checking, and webhook approval require real
    database state and auth context.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - compliance
  - app-developer
  - data-engineer
  competitive_position:
    status: parity
    rationale: Single-source mutation routing that bypasses the federation engine is the standard architecture
      used by Hasura (direct Postgres connection) and Denodo (source-specific DML passthrough).
- id: REQ-033
  status: complete
  group: 5. Query Languages, Compilation & Operations
  category: Mutation Execution
  priority: MUST
  type: constraint
  description: User must have write rights to target table for any mutation.
  use_case: Write-rights enforcement ensures users can only mutate tables they are explicitly authorized
    to change.
  code:
  - provisa/compiler/sql_gen.py
  - provisa/executor/
  tests:
  - tests/unit/test_mutation_sql.py
  - tests/integration/test_mutations.py
  since: 2026-01
  scenario: 'Given a user without write rights to a table

    When they attempt a mutation on that table

    Then the mutation is rejected

    '
  integration_test: required
  integration_test_reason: Write-rights enforcement requires real database connections and role-based
    permission checks.
  stakeholders:
  - compliance
  - app-developer
  competitive_position:
    status: parity
    rationale: Write-rights enforcement before mutation execution is a baseline security requirement present
      in Hasura (role permissions), Denodo (VDP ACLs), and all enterprise data virtualization platforms.
- id: REQ-034
  status: complete
  group: 5. Query Languages, Compilation & Operations
  category: Mutation Execution
  priority: MUST
  type: behavioral
  description: Mutation input types reflect only columns user's role is permitted to write; excluded column
    references rejected at parse time.
  use_case: Role-filtered mutation input prevents users from writing to columns their role cannot access.
  code:
  - provisa/compiler/sql_gen.py
  - provisa/executor/
  tests:
  - tests/unit/test_mutation_sql.py
  - tests/integration/test_mutations.py
  since: 2026-01
  scenario: 'Given a user whose role excludes certain columns

    When a mutation input type is generated for that role

    Then excluded columns are absent from the input type and references to them are rejected at

    parse time

    '
  integration_test: not_required
  integration_test_reason: Role-filtered mutation input types are schema generation logic verifiable through
    unit tests.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - app-developer
  competitive_position:
    status: ahead
    rationale: Role-filtered mutation input types — where excluded columns are absent from the GraphQL
      input schema itself, not just rejected at runtime — is more rigorous than Hasura v2 which surface-filters
      but does not always strip input types at parse time. Trino has no GraphQL mutation layer at all.
- id: REQ-035
  status: complete
  group: 5. Query Languages, Compilation & Operations
  category: Mutation Execution
  priority: MUST
  type: behavioral
  description: RLS WHERE clauses injected into UPDATE and DELETE before execution.
  use_case: RLS injection into UPDATE and DELETE ensures row-level security is enforced on all write operations.
  code:
  - provisa/compiler/sql_gen.py
  - provisa/executor/
  tests:
  - tests/unit/test_mutation_sql.py
  - tests/integration/test_mutations.py
  since: 2026-01
  scenario: 'Given a table with RLS rules configured

    When an UPDATE or DELETE mutation is compiled

    Then RLS WHERE clauses are injected into the SQL before execution

    '
  integration_test: required
  integration_test_reason: RLS WHERE injection into UPDATE/DELETE requires real database execution to
    verify correctness.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - compliance
  - data-engineer
  competitive_position:
    status: ahead
    rationale: Automatic RLS WHERE injection into UPDATE and DELETE ensures row-level security is enforced
      on all write paths; Hasura v2 supports RLS-equivalent check constraints but requires explicit per-permission
      row filters, making Provisa's automatic injection harder to misconfigure. Trino has no mutation
      layer.
- id: REQ-036
  status: complete
  group: 5. Query Languages, Compilation & Operations
  category: Mutation Execution
  priority: MUST
  type: constraint
  description: Mutations can only target registered tables — compiler does not generate mutation types
    for unregistered tables.
  use_case: Mutations only target registered tables, preventing writes to unmanaged or unknown database
    objects.
  code:
  - provisa/compiler/sql_gen.py
  - provisa/executor/
  tests:
  - tests/unit/test_mutation_sql.py
  - tests/integration/test_mutations.py
  since: 2026-01
  scenario: 'Given an unregistered table

    When the schema compiler runs

    Then no mutation types are generated for that table

    '
  integration_test: not_required
  integration_test_reason: Mutation type generation for registered tables only is schema logic verifiable
    through unit tests.
  stakeholders:
  - app-developer
  competitive_position:
    status: parity
    rationale: Restricting mutations to registered tables is standard governance available in Hasura (tracked
      tables only) and Denodo (governed virtual tables only).
- id: REQ-037
  status: complete
  group: 5. Query Languages, Compilation & Operations
  category: Mutation Execution
  priority: MUST
  type: constraint
  description: Cross-source transactions not supported. NoSQL sources do not support mutations.
  use_case: Restricting mutations to single-source and relational targets prevents partial cross-source
    transaction failures.
  code:
  - provisa/compiler/sql_gen.py
  - provisa/executor/
  tests:
  - tests/unit/test_mutation_sql.py
  - tests/integration/test_mutations.py
  since: 2026-01
  scenario: 'Given a mutation targeting a NoSQL source or spanning multiple sources

    When the compiler processes it

    Then it is rejected

    '
  integration_test: not_required
  integration_test_reason: Cross-source transaction restriction is a validation rule at compile time,
    not runtime behavior.
  stakeholders:
  - data-engineer
  - ops
  competitive_position:
    status: parity
    rationale: No cross-source transactions is a well-known limitation of all data virtualization and
      federation platforms including Trino, Denodo, and Hasura — none provide distributed transactions
      across heterogeneous sources.
- id: REQ-066
  status: complete
  group: 5. Query Languages, Compilation & Operations
  category: SQLGlot Transpilation
  priority: MUST
  type: behavioral
  description: Compiler emits PG-style SQL as canonical output; SQLGlot translates to Trino SQL or target
    RDBMS dialect.
  use_case: PG-style canonical SQL output gives a single, well-understood intermediate representation
    for all dialects.
  code:
  - provisa/transpiler/
  tests:
  - tests/integration/test_compiler_integration.py
  - tests/unit/test_layer_contracts.py
  - tests/unit/test_transpile.py
  - tests/unit/test_transpiler.py
  since: 2026-01
  scenario: 'Given a compiled GraphQL query

    When the transpiler processes it

    Then PG-style SQL is emitted as canonical output and SQLGlot translates it to the target dialect

    '
  integration_test: not_required
  integration_test_reason: PG-style SQL canonical output and SQLGlot dialect translation are pure logic
    verifiable through unit tests.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - app-developer
  - data-engineer
  competitive_position:
    status: parity
    rationale: PG-canonical SQL with SQLGlot transpilation is the standard approach for multi-dialect
      support, used by dbt and others.
- id: REQ-067
  status: complete
  group: 5. Query Languages, Compilation & Operations
  category: SQLGlot Transpilation
  priority: MUST
  type: behavioral
  description: Target dialect determined by source type captured at table registration time.
  use_case: Dialect detection at table registration time means transpilation is automatic and requires
    no per-query config.
  code:
  - provisa/transpiler/
  tests:
  - tests/unit/test_transpile.py
  - tests/unit/test_transpiler.py
  - tests/integration/test_compiler_integration.py
  since: 2026-01
  scenario: 'Given a table registered with a specific source type

    When a query is transpiled

    Then the target SQL dialect matches the source type recorded at registration time

    '
  integration_test: not_required
  integration_test_reason: Dialect detection from table registration metadata is pure logic, no execution
    required.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - app-developer
  - data-engineer
  competitive_position:
    status: neutral
    rationale: ''
- id: REQ-068
  status: complete
  group: 5. Query Languages, Compilation & Operations
  category: SQLGlot Transpilation
  priority: SHOULD
  type: structural
  description: 'Supported dialects: PostgreSQL, MySQL, SQL Server, Trino, DuckDB, Snowflake, BigQuery.'
  use_case: Multi-dialect SQLGlot support lets Provisa federate across PostgreSQL, MySQL, SQL Server,
    Trino, and more.
  code:
  - provisa/transpiler/
  tests:
  - tests/unit/test_transpile.py
  - tests/unit/test_transpiler.py
  - tests/integration/test_compiler_integration.py
  since: 2026-01
  scenario: 'Given a compiled SQL statement

    When transpiled to PostgreSQL, MySQL, SQL Server, Trino, DuckDB, Snowflake, or BigQuery

    Then the output is syntactically correct for that dialect

    '
  integration_test: deferred
  integration_test_reason: Multi-dialect support requires test environments for each RDBMS (MySQL, SQL
    Server, BigQuery, Snowflake, Cassandra, etc.) not available in CI.
  stakeholders:
  - data-engineer
  - app-developer
  - ops
  competitive_position:
    status: parity
    rationale: Multi-dialect SQL support (PostgreSQL, MySQL, SQL Server, Trino, DuckDB, Snowflake, BigQuery)
      via SQLGlot is roughly equivalent to what Trino achieves via connectors and what Denodo achieves
      via its data source adapters, though the SQLGlot approach is more code-portable.
- id: REQ-196
  status: complete
  group: 5. Query Languages, Compilation & Operations
  category: Aggregates
  priority: SHOULD
  type: behavioral
  description: Auto-generated aggregate queries following Hasura v2 pattern. Every table gets a `<table>_aggregate`
    root field. Numeric columns get sum/avg/stddev/variance. All comparable columns get min/max. All columns
    get count. No configuration required for default behavior.
  use_case: Hasura v2 parity — auto-generated aggregate fields expose sum/avg/min/max without any steward
    configuration.
  code:
  - provisa/compiler/sql_gen.py
  - provisa/mv/
  tests:
  - tests/unit/test_sql_gen_aggregate.py
  - tests/integration/test_compiler_integration.py
  since: 2026-01
  scenario: 'Given a registered table with numeric and comparable columns

    When the schema compiler runs

    Then a <table>_aggregate root field is generated with sum/avg/stddev/variance on numeric columns,

    min/max on comparable columns, and count on all columns

    '
  integration_test: required
  integration_test_reason: Auto-generated aggregate queries require real table schemas and database execution
    to verify correctness.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - data-engineer
  - app-developer
  competitive_position:
    status: parity
    rationale: Auto-generated aggregate root fields following Hasura v2 pattern is explicitly Hasura parity,
      not differentiation. Trino supports aggregation natively in SQL but has no auto-generated GraphQL
      aggregate layer.
- id: REQ-197
  status: complete
  group: 5. Query Languages, Compilation & Operations
  category: Aggregates
  priority: SHOULD
  type: behavioral
  description: Per-role aggregate gating via `allow_aggregations` (matching v2) or per-table `aggregates`
    config section for explicit override of auto-detected functions and role visibility.
  use_case: Hasura v2 parity — per-role aggregate gating prevents unauthorised users from deriving statistical
    insights.
  code:
  - provisa/compiler/sql_gen.py
  - provisa/mv/
  tests:
  - tests/unit/test_sql_gen_aggregate.py
  - tests/integration/test_compiler_integration.py
  since: 2026-01
  scenario: 'Given a role without allow_aggregations permission

    When the schema is generated

    Then aggregate root fields are not exposed to that role

    '
  integration_test: required
  integration_test_reason: Per-role aggregate gating requires real database queries filtered by role-based
    visibility.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - compliance
  - data-engineer
  - app-developer
  competitive_position:
    status: parity
    rationale: Per-role aggregate gating matches Hasura v2's allow_aggregations permission flag exactly.
      Denodo also supports role-based query capability restrictions.
- id: REQ-198
  status: complete
  superseded_by: REQ-882
  group: 5. Query Languages, Compilation & Operations
  category: Aggregates
  priority: SHOULD
  type: behavioral
  description: Aggregate MV routing — when a query requests aggregates over a pattern already materialized
    in an MV, the compiler rewrites the query to use the MV. Requires aggregate catalog + query rewriter.
  use_case: Aggregate MV routing transparently accelerates aggregate queries without requiring query rewrites.
  code:
  - provisa/compiler/sql_gen.py
  - provisa/mv/
  tests:
  - tests/integration/test_compiler_integration.py
  - tests/unit/test_aggregate_mv_catalog.py
  - tests/unit/test_aggregate_mv_routing.py
  - tests/unit/test_sql_gen_aggregate.py
  since: 2026-01
  scenario: 'Given an aggregate query whose pattern matches a materialized view

    When the compiler processes the query

    Then it rewrites the query to use the MV instead of the base table

    '
  integration_test: required
  integration_test_reason: Aggregate MV routing requires query rewriting and execution against real materialized
    views.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - data-engineer
  - ops
  competitive_position:
    status: ahead
    rationale: Automatic query rewriting to materialized views when the aggregate pattern matches is not
      available in Hasura (no query rewriter) or Denodo's standard tier. Dremio has reflections (similar
      concept) but requires manual configuration; Provisa's catalog-driven automatic rewrite is more transparent.
  related_reqs:
  - REQ-826
- id: REQ-199
  status: complete
  group: 5. Query Languages, Compilation & Operations
  category: Aggregates
  priority: MAY
  type: behavioral
  description: 'View auto-materialization for aggregate optimization — expensive views auto-materialized
    and registered in aggregate catalog instead of requiring bespoke `materialized_views` entries. Default
    TTL configurable globally via `materialized_views.default_ttl` (seconds, default: 3600). Individual
    views can override with `refresh_interval`. Stale MVs refreshed by background loop; queries against
    stale MVs fall back to live execution. (Explicit, defined exception to REQ-064 fail-fast — stale-MV→live
    is designed behavior, not silent error handling.)'
  use_case: View auto-materialization avoids repeated expensive subquery execution for high-traffic computed
    datasets.
  code:
  - provisa/compiler/sql_gen.py
  - provisa/mv/
  tests:
  - tests/integration/test_compiler_integration.py
  - tests/unit/test_aggregate_mv_catalog.py
  - tests/unit/test_aggregate_mv_routing.py
  - tests/unit/test_sql_gen_aggregate.py
  since: 2026-01
  scenario: 'Given an expensive view eligible for auto-materialization

    When the background loop refreshes it and a query targets that view

    Then the query uses the MV; if the MV is stale, it falls back to live execution

    '
  integration_test: required
  integration_test_reason: View auto-materialization with background refresh requires real database, MV
    creation, TTL scheduling, and stale-MV fallback testing.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - ops
  - data-engineer
  competitive_position:
    status: ahead
    rationale: Automatic view materialization with stale-MV live fallback is not offered by Hasura or
      Trino; Dremio Reflections require explicit definition and do not fall back automatically to live
      execution on staleness. Provisa's TTL-based auto-MV with transparent fallback is a genuine differentiator
      for query performance.
  related_reqs:
  - REQ-826
  - REQ-856
- id: REQ-200
  status: complete
  group: 5. Query Languages, Compilation & Operations
  category: OrderBy Alignment
  priority: MUST
  type: structural
  description: 'GraphQL order_by schema must follow Hasura v2 convention: column-keyed input type `{column_name:
    direction}` instead of current `{field: ENUM, direction: ENUM}` struct.'
  use_case: Hasura v2 parity — column-keyed order_by schema lets Hasura-trained developers sort results
    intuitively.
  code:
  - provisa/compiler/
  tests:
  - tests/unit/test_orderby_alignment.py
  - tests/integration/test_compiler_integration.py
  since: 2026-01
  scenario: 'Given a table with sortable columns

    When the GraphQL schema is generated

    Then order_by input types use column-keyed format {column_name: direction} per Hasura v2

    convention

    '
  integration_test: not_required
  integration_test_reason: OrderBy schema generation is pure schema logic verifiable through unit tests.
  stakeholders:
  - app-developer
  competitive_position:
    status: parity
    rationale: Column-keyed order_by schema following Hasura v2 convention is explicit Hasura parity.
      No competitive differentiation — this is a catch-up requirement.
- id: REQ-201
  status: complete
  group: 5. Query Languages, Compilation & Operations
  category: OrderBy Alignment
  priority: MUST
  type: structural
  description: 'OrderBy direction enum must include 6 values: `asc`, `asc_nulls_first`, `asc_nulls_last`,
    `desc`, `desc_nulls_first`, `desc_nulls_last`. SQL compiler maps to `ORDER BY col ASC NULLS FIRST`,
    etc.'
  use_case: Hasura v2 parity — six-value direction enum gives precise NULL-ordering control matching standard
    SQL.
  code:
  - provisa/compiler/
  tests:
  - tests/unit/test_orderby_alignment.py
  - tests/integration/test_compiler_integration.py
  since: 2026-01
  scenario: 'Given a GraphQL query with order_by using asc_nulls_first

    When the compiler generates SQL

    Then it emits ORDER BY col ASC NULLS FIRST

    '
  integration_test: required
  integration_test_reason: NULL-ordering direction enum must produce correct SQL at compile time and execute
    correctly in real database.
  stakeholders:
  - data-engineer
  - app-developer
  competitive_position:
    status: parity
    rationale: Six-value direction enum with NULL ordering matches Hasura v2 and standard SQL semantics
      available across all serious SQL-over-GraphQL platforms.
- id: REQ-202
  status: complete
  group: 5. Query Languages, Compilation & Operations
  category: OrderBy Alignment
  priority: SHOULD
  type: behavioral
  description: 'Relationship ordering — order by related object fields (e.g., `order_by: {author: {name:
    asc}}`). Matches Hasura v2 default behavior.'
  use_case: Hasura v2 parity — relationship ordering lets clients sort parent rows by child attributes
    in one request.
  code:
  - provisa/compiler/
  tests:
  - tests/unit/test_orderby_alignment.py
  - tests/integration/test_compiler_integration.py
  since: 2026-01
  scenario: 'Given a query with order_by referencing a related object field

    When the compiler processes it

    Then the SQL ORDER BY clause references the related table''s column

    '
  integration_test: required
  integration_test_reason: Relationship ordering requires real table joins and execution to verify parent
    row sorting by child attributes.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - data-engineer
  - app-developer
  competitive_position:
    status: parity
    rationale: Relationship ordering matching Hasura v2 default behavior is explicit parity. Hasura has
      offered this since v2; it is a catch-up requirement for Provisa.
- id: REQ-205
  status: complete
  group: 5. Query Languages, Compilation & Operations
  category: Tracked Functions & Custom Mutations
  priority: SHOULD
  type: behavioral
  description: Database functions (stored procedures, UDFs) registered in Provisa config and exposed as
    GraphQL mutations or queries. VOLATILE functions exposed as mutations; STABLE/IMMUTABLE as queries.
    Follows Hasura v2's `pg_track_function` pattern.
  use_case: Tracked DB functions expose stored procedures as GraphQL mutations so clients use one API
    for all writes.
  code:
  - provisa/compiler/
  - provisa/executor/
  - provisa/webhooks/
  tests:
  - tests/e2e/test_mutations.py
  - tests/integration/test_compiler_integration.py
  - tests/unit/test_actions.py
  - tests/unit/test_function_gen.py
  since: 2026-01
  scenario: 'Given a VOLATILE database function registered in Provisa config

    When the schema is generated

    Then it is exposed as a GraphQL mutation field

    '
  integration_test: required
  integration_test_reason: Tracked DB functions exposed as mutations requires real database function execution
    and governance enforcement.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - data-engineer
  - app-developer
  - compliance
  competitive_position:
    status: parity
    rationale: Tracked DB functions exposed as GraphQL mutations following Hasura v2's pg_track_function
      pattern is explicit Hasura parity; Hasura has had this since v2 GA.
- id: REQ-206
  status: complete
  group: 5. Query Languages, Compilation & Operations
  category: Tracked Functions & Custom Mutations
  priority: SHOULD
  type: structural
  description: Function config section in `provisa.yaml` (|) provides structured registration of stored
    procedures with governance metadata.
  use_case: Function config section provides a structured way to register stored procedures with governance
    metadata.
  code:
  - provisa/compiler/
  - provisa/executor/
  - provisa/webhooks/
  tests:
  - tests/e2e/test_mutations.py
  - tests/integration/test_compiler_integration.py
  - tests/unit/test_actions.py
  - tests/unit/test_function_gen.py
  since: 2026-01
  scenario: 'Given a function defined in the provisa.yaml functions config section

    When the schema compiler runs

    Then the function is registered with its governance metadata and exposed in the GraphQL schema

    '
  integration_test: not_required
  integration_test_reason: Function config structure in provisa.yaml is schema validation logic verifiable
    through unit tests.
  stakeholders:
  - data-engineer
  competitive_position:
    status: parity
    rationale: Structured function/procedure registration with governance metadata is available in Hasura
      (Actions) and Denodo.
- id: REQ-207
  status: complete
  group: 5. Query Languages, Compilation & Operations
  category: Tracked Functions & Custom Mutations
  priority: MUST
  type: constraint
  description: Function return type MUST reference a registered table. The result set maps back to GraphQL
    using that table's type definition, column governance (visibility, masking), and RLS rules. This ensures
    function results go through the same security pipeline as direct queries.
  use_case: Requiring functions to return a registered table ensures their results go through the same
    security pipeline.
  code:
  - provisa/compiler/
  - provisa/executor/
  - provisa/webhooks/
  tests:
  - tests/e2e/test_mutations.py
  - tests/e2e/test_query_pipeline.py
  - tests/integration/test_compiler_integration.py
  - tests/unit/test_actions.py
  - tests/unit/test_function_gen.py
  since: 2026-01
  scenario: 'Given a tracked function with a return type referencing a registered table

    When results are returned

    Then column governance, masking, and RLS rules from that table are applied

    '
  integration_test: not_required
  integration_test_reason: Function return type referencing registered tables is schema validation logic
    at compile time.
  stakeholders:
  - data-engineer
  - compliance
  competitive_position:
    status: ahead
    rationale: Requiring function return types to reference registered tables so that results pass through
      the same RLS, masking, and column-visibility pipeline is not enforced by Hasura (functions can return
      arbitrary types without governance re-application). This is a meaningful security-by-design differentiator.
- id: REQ-208
  status: complete
  group: 5. Query Languages, Compilation & Operations
  category: Tracked Functions & Custom Mutations
  priority: MUST
  type: behavioral
  description: Functions execute via direct DB connection (same as mutations). Never routed through Trino.
  use_case: Direct DB execution for functions avoids Trino round-trips on mutation-class operations.
  code:
  - provisa/compiler/
  - provisa/executor/
  - provisa/webhooks/
  tests:
  - tests/e2e/test_mutations.py
  - tests/integration/test_compiler_integration.py
  - tests/integration/test_direct_exec.py
  - tests/unit/test_actions.py
  - tests/unit/test_function_gen.py
  since: 2026-01
  scenario: 'Given a tracked database function

    When it is executed via GraphQL

    Then it runs via a direct DB connection and is never routed through Trino

    '
  integration_test: required
  integration_test_reason: Direct DB execution for functions requires real database connections and cannot
    be verified through mocks.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - data-engineer
  - ops
  competitive_position:
    status: neutral
    rationale: ''
- id: REQ-209
  status: complete
  group: 5. Query Languages, Compilation & Operations
  category: Tracked Functions & Custom Mutations
  priority: MUST
  type: behavioral
  description: 'Webhook-backed mutations — external HTTP endpoint called as a GraphQL mutation (Part of
    Phase AC). Config includes name, url, method, domain_id, governance (requires_approval), arguments
    with name and type, returns (registered table or inline type), visible_to, and timeout_ms. Webhook
    mutations are NOT DB mutations (REQ-031/REQ-032) and require specific steward approval for use; `governance:
    registry-required` gates them.'
  use_case: Webhook mutations let GraphQL clients trigger external services without learning a second
    API.
  code:
  - provisa/compiler/
  - provisa/executor/
  - provisa/webhooks/
  tests:
  - tests/e2e/test_mutations.py
  - tests/integration/test_compiler_integration.py
  - tests/unit/test_actions.py
  - tests/unit/test_function_gen.py
  - provisa-ui/e2e/registration-admin.spec.ts
  since: 2026-01
  scenario: 'Given a webhook mutation configured with governance: requires_approval

    When a client invokes it

    Then steward approval is required and the external HTTP endpoint is called

    '
  integration_test: required
  integration_test_reason: Webhook mutations require HTTP transport, approval gate checking, and governance
    enforcement.
  e2e: true
  e2e_reason: Webhook mutations require a live HTTP transport to verify that the external endpoint is
    actually called and that the steward approval gate is enforced across the network boundary.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - compliance
  - app-developer
  competitive_position:
    status: ahead
    rationale: Webhook-backed mutations with steward approval governance (registry-required gate) combine
      API orchestration with data governance in a single layer; Hasura Actions support webhook mutations
      but have no built-in registry approval workflow. Denodo has no equivalent webhook mutation concept.
- id: REQ-210
  status: complete
  group: 5. Query Languages, Compilation & Operations
  category: Tracked Functions & Custom Mutations
  priority: MAY
  type: structural
  description: Webhook mutations support inline return type definitions (not backed by a registered table)
    for cases where the webhook returns a custom shape. Inline types define fields with names and GraphQL
    types.
  use_case: Inline return types for webhooks let custom response shapes be exposed without creating a
    registered table.
  code:
  - provisa/compiler/
  - provisa/executor/
  - provisa/webhooks/
  tests:
  - tests/e2e/test_mutations.py
  - tests/integration/test_compiler_integration.py
  - tests/unit/test_actions.py
  - tests/unit/test_function_gen.py
  since: 2026-01
  scenario: 'Given a webhook mutation with an inline return type definition

    When the schema is generated

    Then a GraphQL type is created from the inline field definitions without requiring a registered

    table

    '
  integration_test: not_required
  integration_test_reason: Inline return type definitions are schema generation logic verifiable through
    unit tests.
  stakeholders:
  - app-developer
  competitive_position:
    status: parity
    rationale: Inline return type definitions for webhooks (not backed by a registered table) match Hasura
      Actions' custom type capability. No meaningful competitive differentiation.
- id: REQ-211
  status: complete
  group: 5. Query Languages, Compilation & Operations
  category: Tracked Functions & Custom Mutations
  priority: MUST
  type: constraint
  description: Function and webhook argument types map to GraphQL input types. Arguments are validated
    at parse time. SQL injection prevented via parameterized calls for DB functions and JSON serialization
    for webhooks.
  use_case: Argument validation at parse time prevents SQL injection and type errors before any DB or
    HTTP call is made.
  code:
  - provisa/compiler/
  - provisa/executor/
  - provisa/webhooks/
  tests:
  - tests/e2e/test_mutations.py
  - tests/integration/test_compiler_integration.py
  - tests/unit/test_actions.py
  - tests/unit/test_function_gen.py
  since: 2026-01
  scenario: 'Given a function or webhook with typed arguments

    When a GraphQL mutation is parsed

    Then arguments are validated at parse time and SQL injection is prevented via parameterized calls

    '
  integration_test: not_required
  integration_test_reason: Argument type validation and SQL injection prevention are pure logic verifiable
    through unit tests.
  stakeholders:
  - compliance
  - app-developer
  competitive_position:
    status: parity
    rationale: Argument validation at parse time with parameterized calls for SQL injection prevention
      is baseline security practice present in Hasura, Denodo, and all production data API layers.
- id: REQ-252
  status: complete
  group: 5. Query Languages, Compilation & Operations
  category: Compiler & Schema
  priority: SHOULD
  type: behavioral
  description: 'Schema inference must be supported where the Trino connector provides auto-discovery (MongoDB,
    Cassandra, Elasticsearch). Sources with inference support should offer a `discover: true` flag that
    introspects and generates a starting column list. Explicit column definitions always take precedence
    over inferred schemas. Sources without inference capability (Redis) require explicit column definitions.'
  use_case: Schema inference for supported connectors (MongoDB, Cassandra) reduces manual column definition
    work.
  code:
  - provisa/compiler/sql_gen.py
  tests:
  - tests/integration/test_schema_gen.py
  - tests/unit/test_discover_inference.py
  - tests/unit/test_mongodb_discovery.py
  - tests/unit/test_schema_service.py
  - tests/unit/test_sql_gen.py
  since: 2026-01
  scenario: 'Given a MongoDB source with discover: true

    When the schema compiler runs

    Then it introspects the connector and generates a starting column list, with explicit definitions

    taking precedence

    '
  integration_test: required
  integration_test_reason: Schema inference for Trino connectors requires real connector discovery and
    table introspection.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - data-engineer
  - ops
  competitive_position:
    status: parity
    rationale: Schema inference via connector introspection (MongoDB, Cassandra) is a standard capability
      offered by Trino connectors and Denodo's schema discovery tooling. The discover:true flag simplifies
      it but does not uniquely differentiate Provisa.
  related_reqs:
  - REQ-825
- id: REQ-253
  status: complete
  group: 5. Query Languages, Compilation & Operations
  category: Compiler & Schema
  priority: MUST
  type: behavioral
  description: Naming convention changes (global, source, or table level) reflected immediately in GraphQL
    schema. Admin mutations trigger _rebuild_schemas() which regenerates in-memory graphql-core schema
    objects. GraphiQL and Voyager fetch fresh introspection on each page load. No client-side schema caching.
  use_case: Immediate schema rebuild on naming convention changes ensures GraphQL clients always see consistent
    names.
  code:
  - provisa/compiler/sql_gen.py
  tests:
  - tests/e2e/test_naming_reload.py
  - tests/integration/test_schema_gen.py
  - tests/unit/test_schema_service.py
  - tests/unit/test_sql_gen.py
  - provisa-ui/e2e/registration-admin.spec.ts
  since: 2026-01
  scenario: 'Given a naming convention change is applied via admin mutation

    When _rebuild_schemas() completes

    Then the in-memory GraphQL schema is regenerated and fresh introspection is returned on the next

    request

    '
  integration_test: required
  integration_test_reason: Naming convention changes and schema rebuild require real HTTP clients fetching
    GraphQL introspection.
  e2e: true
  e2e_reason: Naming convention changes and immediate schema rebuild must be verified by real HTTP clients
    fetching fresh GraphQL introspection from a live server, which cannot be replicated without a full
    running stack.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - app-developer
  - ops
  competitive_position:
    status: parity
    rationale: Immediate in-memory schema rebuild on config changes is offered by Hasura (hasura metadata
      reload) and Denodo (hot reload). The no-client-caching guarantee is a sensible default but not unique.
- id: REQ-259
  status: complete
  group: 5. Query Languages, Compilation & Operations
  category: Compiler & Schema
  priority: MAY
  type: behavioral
  description: Apollo Federation v2 subgraph support — when enabled, Provisa generates a Federation v2
    compliant schema with @key directives on entity types (derived from primary keys), _service and _entities
    root fields, and batch entity resolution. Provisa can be composed into an Apollo Gateway/Router supergraph.
    Entity resolution respects RLS, masking, and role-based visibility. Disabled by default.
  use_case: Apollo Federation v2 subgraph support lets Provisa participate in a federated supergraph alongside
    other APIs.
  code:
  - provisa/compiler/sql_gen.py
  tests:
  - tests/integration/test_schema_gen.py
  - tests/unit/test_schema_service.py
  - tests/unit/test_sql_gen.py
  since: 2026-01
  scenario: 'Given Apollo Federation v2 support is enabled

    When the schema is generated

    Then @key directives, _service, and _entities fields are present and entity resolution respects

    RLS and masking

    '
  integration_test: required
  integration_test_reason: Apollo Federation v2 subgraph support requires real GraphQL introspection,
    entity resolution, and composition with a federated gateway.
  e2e: true
  e2e_reason: Apollo Federation v2 subgraph support requires real GraphQL introspection over HTTP and
    composition with a live federated gateway to verify @key directives, _entities resolution, and governance
    enforcement.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - app-developer
  - ops
  competitive_position:
    status: ahead
    rationale: Apollo Federation v2 subgraph support with RLS, masking, and role-based visibility applied
      during entity resolution is not available in Hasura (which is a supergraph consumer, not a subgraph),
      Trino, or Denodo. Provisa is the only governed data federation layer that can participate as an
      Apollo subgraph.
- id: REQ-300
  status: complete
  group: 5. Query Languages, Compilation & Operations
  category: GraphQL Variable Defaults
  priority: MUST
  type: behavioral
  description: 'GraphQL operations may declare variable default values (e.g. `query Q($limit: Int = 10)`).
    The compiler MUST apply those defaults for any variable not present in the request `variables` dict,
    per GraphQL spec §6.4.1. Missing a declared default must not produce a 500 — the default value is
    used as if the caller had supplied it.'
  use_case: GraphQL variable defaults applied by compiler prevent 500 errors when callers omit optional
    parameters.
  code:
  - provisa/compiler/
  tests:
  - tests/integration/test_compiler_integration.py
  - tests/integration/test_graphql_execution.py
  - tests/unit/test_params.py
  since: 2026-01
  scenario: 'Given a GraphQL operation declaring $limit: Int = 10 and a request omitting the limit variable

    When the compiler processes the request

    Then it applies the default value 10 as if the caller had supplied it

    '
  integration_test: not_required
  integration_test_reason: GraphQL variable default application is pure compiler logic verifiable through
    unit tests.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - app-developer
  competitive_position:
    status: parity
    rationale: GraphQL variable default values per spec §6.4.1 is a correctness requirement that any spec-compliant
      GraphQL server must satisfy. No competitive differentiation.
- id: REQ-301
  status: complete
  group: 5. Query Languages, Compilation & Operations
  category: GraphQL Variable Defaults
  priority: MUST
  type: constraint
  description: LIMIT and OFFSET values MUST be emitted as positional parameters (`$N`) in compiled SQL,
    never interpolated as literals. This ensures consistent parameterization regardless of whether the
    value originated from a GraphQL variable or an inline literal argument.
  use_case: Parameterized LIMIT/OFFSET prevents SQL injection and ensures consistent query plans regardless
    of value source.
  code:
  - provisa/compiler/
  tests:
  - tests/integration/test_compiler_integration.py
  - tests/integration/test_graphql_execution.py
  - tests/unit/test_params.py
  since: 2026-01
  scenario: 'Given a GraphQL query with limit and offset arguments

    When the compiler generates SQL

    Then LIMIT and OFFSET are emitted as positional parameters $N, never as interpolated literals

    '
  integration_test: not_required
  integration_test_reason: Parameterized LIMIT/OFFSET generation is pure SQL compilation logic verifiable
    through unit tests.
  stakeholders:
  - app-developer
  competitive_position:
    status: neutral
    rationale: ''
- id: REQ-304
  status: complete
  group: 5. Query Languages, Compilation & Operations
  category: Tracked DB Functions — Custom Return Schema (REQ-304–306)
  priority: MAY
  type: structural
  description: 'Tracked DB functions MAY declare a `return_schema` (JSON Schema) in place of a `returns`
    (registered table reference). When `return_schema` is present and `returns` is empty, the schema compiler
    converts the JSON Schema definition to a `GraphQLObjectType` at schema-build time. `type: array` yields
    `GraphQLList(GraphQLNonNull(obj))`; `type: object` yields a nullable single object. This enables functions
    that return custom shapes not backed by any registered table.'
  use_case: Custom return_schema for tracked functions lets stored procedures returning non-table shapes
    be exposed safely.
  code:
  - provisa/compiler/
  - provisa/api/admin/
  tests:
  - tests/integration/test_compiler_integration.py
  - tests/unit/test_function_gen.py
  - tests/unit/test_schema_service.py
  since: 2026-01
  scenario: 'Given a tracked function with return_schema defined as a JSON Schema object

    When the schema compiler runs

    Then a GraphQLObjectType is generated from the JSON Schema and the function is exposed with that

    return type

    '
  integration_test: not_required
  integration_test_reason: JSON Schema to GraphQL type conversion is pure schema generation logic verifiable
    through unit tests.
  stakeholders:
  - app-developer
  competitive_position:
    status: parity
    rationale: Custom return_schema for tracked functions (JSON Schema to GraphQL type) is similar to
      Hasura Actions' custom type support; it is a parity requirement, not a differentiator.
- id: REQ-305
  status: complete
  group: 5. Query Languages, Compilation & Operations
  category: Tracked DB Functions — Custom Return Schema (REQ-304–306)
  priority: MAY
  type: ui
  description: 'The admin UI for tracked DB functions provides a "Return Type" mode toggle: "Registered
    Table" (existing `schema.table` select) or "Custom Schema". In custom schema mode, the steward pastes
    a sample JSON response (single object or array); Provisa infers a JSON Schema from it client-side
    (field names + primitive types). The steward may edit the inferred schema before saving. The inferred+edited
    JSON Schema is stored as `return_schema JSONB` on `tracked_functions`.'
  use_case: Admin UI return type toggle lets stewards define custom function return schemas by pasting
    a sample response.
  code:
  - provisa/compiler/
  - provisa/api/admin/
  tests:
  - tests/integration/test_compiler_integration.py
  - tests/unit/test_function_gen.py
  - tests/unit/test_schema_service.py
  since: 2026-01
  scenario: 'Given a steward registers a tracked function in the admin UI

    When they select Custom Schema mode and paste a sample JSON response

    Then Provisa infers a JSON Schema client-side and stores it as return_schema JSONB

    '
  integration_test: not_required
  integration_test_reason: Admin UI for return type mode is a UI feature with client-side JSON Schema
    inference logic.
  e2e: true
  e2e_reason: The admin UI return type mode toggle and client-side JSON Schema inference require browser
    interaction against a live admin surface to verify the steward workflow end-to-end.
  stakeholders:
  - app-developer
  competitive_position:
    status: ahead
    rationale: Inline custom return schema editor with JSON sample inference for tracked DB functions
      is more ergonomic than Hasura's manual type definition workflow.
- id: REQ-306
  status: complete
  group: 5. Query Languages, Compilation & Operations
  category: Tracked DB Functions — Custom Return Schema (REQ-304–306)
  priority: MUST
  type: structural
  description: 'JSON Schema → GraphQL type mapping for `return_schema`: `"string"` → `GraphQLString`,
    `"integer"` → `GraphQLInt`, `"number"` → `GraphQLFloat`, `"boolean"` → `GraphQLBoolean`. Unknown or
    missing `type` defaults to `GraphQLString`. Properties with nested objects are not recursively expanded
    in V1 — only top-level scalar fields are mapped.'
  use_case: JSON Schema to GraphQL type mapping for return_schema ensures functions with custom shapes
    have typed fields.
  code:
  - provisa/compiler/
  - provisa/api/admin/
  tests:
  - tests/integration/test_compiler_integration.py
  - tests/unit/test_function_gen.py
  - tests/unit/test_schema_service.py
  since: 2026-01
  scenario: 'Given a return_schema with string, integer, number, and boolean properties

    When the schema compiler maps it to GraphQL types

    Then fields are typed GraphQLString, GraphQLInt, GraphQLFloat, and GraphQLBoolean respectively

    '
  integration_test: not_required
  integration_test_reason: JSON Schema to GraphQL type mapping is pure logic verifiable through unit tests.
  stakeholders:
  - app-developer
  competitive_position:
    status: neutral
    rationale: ''
- id: REQ-345
  status: complete
  group: 5. Query Languages, Compilation & Operations
  category: Cypher Query Frontend (Phase AU)
  priority: SHOULD
  type: behavioral
  description: Provisa exposes a `POST /query/cypher` endpoint that accepts a Cypher SELECT query and
    optional named parameters (`$param`). The query is compiled to SQL and executed via Trino. All existing
    governance (RLS, column masking, domain visibility, row ceiling) applies via Stage 2 identically to
    GraphQL-compiled queries.
  use_case: Graph-native users write Cypher against Provisa without learning GraphQL or SQL.
  code:
  - provisa/cypher/
  tests:
  - tests/integration/test_cypher_endpoint.py
  - tests/unit/test_cypher_graph_fns.py
  - tests/unit/test_cypher_parser.py
  - tests/unit/test_cypher_translator.py
  - tests/unit/test_sql_to_cypher.py
  - provisa-ui/e2e/cypher-api.spec.ts
  since: 2026-01
  scenario: 'Given a graph user submitting a Cypher SELECT query to POST /query/cypher

    When the compiler processes it

    Then it compiles to SQL, executes via Trino, and applies Stage 2 governance identically to

    GraphQL queries

    '
  integration_test: required
  integration_test_reason: Cypher endpoint requires HTTP transport, Trino execution, and governance enforcement
    (RLS, masking).
  e2e: true
  e2e_reason: The POST /query/cypher REST endpoint requires a live running server with real HTTP transport
    and Trino execution to verify that governance (RLS, masking) is enforced on the actual network response.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - app-developer
  - data-engineer
  - compliance
  competitive_position:
    status: ahead
    rationale: A Cypher-to-SQL compiler with full Stage 2 governance (RLS, masking, domain visibility)
      on a data federation platform is unique — no competitor (Trino, Hasura, Denodo, Dremio) supports
      Cypher as a query frontend over federated relational data with automatic governance enforcement.
- id: REQ-346
  status: rejected
  superseded_by: REQ-818
  rejection_reason: 'Superseded by REQ-818. Cypher is no longer read-only: CREATE/DELETE/SET now execute
    as governed direct table writes through the mutation pipeline. The still-true parts — MERGE/ DETACH/REMOVE
    and APOC are rejected — are carried by REQ-818 and REQ-671. This entry''s blanket "strictly read-only,
    all write clauses rejected" claim is now inaccurate.'
  group: 5. Query Languages, Compilation & Operations
  category: Cypher Query Frontend (Phase AU)
  priority: MUST
  type: constraint
  description: 'SUPERSEDED BY REQ-818. Originally: the Cypher compiler is strictly read-only, rejecting
    any write clause (CREATE/MERGE/SET/DELETE/DETACH/REMOVE) or APOC reference at parse time. Cypher now
    supports governed CREATE/DELETE/SET writes (REQ-818); only MERGE/DETACH/REMOVE and APOC remain rejected.
    Use REQ-818 and REQ-671 instead.'
  use_case: Read-only enforcement prevents graph users from accidentally mutating data via Cypher.
  code:
  - provisa/cypher/
  tests:
  - tests/integration/test_cypher_endpoint.py
  - tests/unit/test_cypher_parser.py
  - tests/unit/test_cypher_translator.py
  - tests/unit/test_sql_to_cypher.py
  since: 2026-01
  scenario: 'Given a Cypher query containing a CREATE or DELETE clause

    When the parser processes it

    Then it is rejected at parse time with a precise error and no fallback occurs

    '
  integration_test: not_required
  integration_test_reason: Read-only clause rejection is pure parser logic verifiable through unit tests
    without execution.
  stakeholders:
  - compliance
  competitive_position:
    status: ahead
    rationale: Read-only Cypher enforcement at parse time with write-clause rejection is a stronger safety
      guarantee than Hasura's role-based GraphQL mutation gating.
- id: REQ-347
  status: complete
  group: 5. Query Languages, Compilation & Operations
  category: Cypher Query Frontend (Phase AU)
  priority: MUST
  type: behavioral
  description: 'Cypher clauses map to SQL as follows: `MATCH` → `JOIN`; `OPTIONAL MATCH` → `LEFT JOIN`;
    `WHERE` → `WHERE`; `RETURN` → `SELECT`; `ORDER BY` → `ORDER BY`; `SKIP`/`LIMIT` → `OFFSET`/`LIMIT`;
    `WITH` (pipeline) → CTE or subquery. Node label `:Label` and relationship type `:TYPE` are resolved
    via the steward-declared label mapping (REQ-351) to physical table and join references.'
  use_case: Cypher clause mapping enables graph queries to execute as governed SQL via Trino.
  code:
  - provisa/cypher/
  tests:
  - tests/e2e/test_query_pipeline.py
  - tests/integration/test_cypher_endpoint.py
  - tests/unit/test_cypher_label_map.py
  - tests/unit/test_cypher_parser.py
  - tests/unit/test_cypher_translator.py
  - tests/unit/test_sql_to_cypher.py
  since: 2026-01
  scenario: 'Given a Cypher query with MATCH, WHERE, RETURN, ORDER BY, and LIMIT clauses

    When the translator processes it

    Then it emits SQL with JOIN, WHERE, SELECT, ORDER BY, and LIMIT clauses respectively

    '
  integration_test: required
  integration_test_reason: Cypher clause mapping to SQL requires compilation and real Trino execution
    to verify correctness.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - data-engineer
  - app-developer
  competitive_position:
    status: ahead
    rationale: Cypher clause-to-SQL translation (MATCH→JOIN, OPTIONAL MATCH→LEFT JOIN, etc.) executed
      over Trino-federated sources is not available in any mainstream competitor; Denodo, Hasura, and
      Dremio have no Cypher compiler.
- id: REQ-348
  status: complete
  group: 5. Query Languages, Compilation & Operations
  category: Cypher Query Frontend (Phase AU)
  priority: SHOULD
  type: behavioral
  description: Path queries — `shortestPath(...)`, `allShortestPaths(...)`, and variable-length relationship
    patterns `[*1..n]` — translate to Trino recursive CTEs (`WITH RECURSIVE`) against the adjacency relation
    defined in the label mapping. Maximum hop depth is enforced at compile time; unbounded `[*]` is rejected.
  use_case: Path queries and variable-length traversals translate to recursive CTEs for graph algorithms
    over relational data.
  code:
  - provisa/cypher/
  tests:
  - tests/integration/test_cypher_endpoint.py
  - tests/unit/test_cypher_label_map.py
  - tests/unit/test_cypher_parser.py
  - tests/unit/test_cypher_translator.py
  - tests/unit/test_sql_to_cypher.py
  since: 2026-01
  scenario: 'Given a Cypher query with shortestPath or [*1..n] variable-length pattern

    When the translator processes it

    Then it emits a WITH RECURSIVE CTE and rejects unbounded [*] patterns at compile time

    '
  integration_test: required
  integration_test_reason: Path queries and recursive CTEs require real database execution to verify correct
    hop depth and traversal.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - data-engineer
  - app-developer
  competitive_position:
    status: ahead
    rationale: Path queries and variable-length traversals compiled to Trino recursive CTEs over federated
      relational data is entirely novel — no competitor combines graph path syntax, recursive SQL, and
      federated execution with governance enforcement.
- id: REQ-349
  status: complete
  group: 5. Query Languages, Compilation & Operations
  category: Cypher Query Frontend (Phase AU)
  priority: SHOULD
  type: behavioral
  description: When a `RETURN` clause references a whole node variable, relationship variable, or path
    variable (not a scalar property), a Stage 3 SQLGlot rewrite pass wraps the projected columns for that
    variable into a single JSON object column using `CAST(ROW(...) AS JSON)`. This rewrite runs after
    Stage 2 governance and before execution. It does not modify scalar property projections.
  use_case: Node/edge/path return types give graph clients structured output matching their mental model.
  code:
  - provisa/cypher/
  tests:
  - tests/integration/test_cypher_endpoint.py
  - tests/unit/test_cypher_parser.py
  - tests/unit/test_cypher_translator.py
  - tests/unit/test_sql_to_cypher.py
  since: 2026-01
  scenario: 'Given a Cypher RETURN clause referencing a whole node variable

    When Stage 3 rewrite runs

    Then the node columns are wrapped into a single JSON object via CAST(ROW(...) AS JSON)

    '
  integration_test: not_required
  integration_test_reason: Node/edge/path JSON wrapping is pure SQL rewrite logic verifiable through unit
    tests.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - app-developer
  competitive_position:
    status: ahead
    rationale: Node/edge/path variable return types wrapped as JSON objects (CAST(ROW(...) AS JSON)) after
      governance passes is a unique output model for graph results from relational data; competitors that
      support any graph query (none over federated relational data) do not apply governed SQL-layer wrapping.
- id: REQ-350
  status: complete
  superseded_by: REQ-750
  group: 5. Query Languages, Compilation & Operations
  category: Cypher Query Frontend (Phase AU)
  priority: SHOULD
  type: structural
  description: 'Three graph output types are defined in the GraphQL schema and returned when Stage 3 wrapping
    is applied: `Node { id: ID!, label: String!, properties: JSON }`, `Edge { id: ID!, type: String!,
    startNode: Node!, endNode: Node!, properties: JSON }`, `Path { nodes: [Node!]!, edges: [Edge!]! }`.
    Scalar property projections return plain column types as normal.'
  use_case: Typed graph output (Node, Edge, Path) lets graph clients parse results without custom deserialization.
  code:
  - provisa/cypher/
  tests:
  - tests/integration/test_cypher_endpoint.py
  - tests/unit/test_cypher_graph_fns.py
  - tests/unit/test_cypher_label_map.py
  - tests/unit/test_cypher_parser.py
  - tests/unit/test_cypher_translator.py
  - tests/unit/test_sql_to_cypher.py
  since: 2026-01
  scenario: 'Given a Cypher query returning node, edge, and path variables

    When results are returned

    Then they conform to the Node, Edge, and Path GraphQL types respectively

    '
  integration_test: not_required
  integration_test_reason: Typed graph output (Node, Edge, Path) is schema generation logic verifiable
    through unit tests.
  stakeholders:
  - app-developer
  competitive_position:
    status: ahead
    rationale: Typed graph output (Node, Edge, Path) in the GraphQL schema generated from relational metadata
      is a unique combination; Hasura and Denodo have no graph type system.
- id: REQ-351
  status: complete
  group: 5. Query Languages, Compilation & Operations
  category: Cypher Query Frontend (Phase AU)
  priority: MUST
  type: structural
  description: The Cypher label map is derived entirely from the existing Provisa `CompilationContext`
    — no separate config. `TableMeta.type_name` (PascalCase GraphQL type name) becomes the Cypher node
    label; registered `JoinMeta` entries become relationship types. Consumers write Cypher using the same
    type names already visible in the Provisa GraphQL schema. No additional registration step is required.
  use_case: Cypher label map derived from existing compilation context — no extra registration step for
    Cypher users.
  code:
  - provisa/cypher/
  tests:
  - tests/integration/test_cypher_endpoint.py
  - tests/unit/test_cypher_graph_fns.py
  - tests/unit/test_cypher_label_map.py
  - tests/unit/test_cypher_parser.py
  - tests/unit/test_cypher_translator.py
  - tests/unit/test_sql_to_cypher.py
  since: 2026-01
  scenario: 'Given a registered table with type_name Order and a JoinMeta entry

    When a Cypher query references :Order and the relationship type

    Then they resolve to the correct physical table and join without any additional registration

    '
  integration_test: not_required
  integration_test_reason: Cypher label map derivation from CompilationContext is pure logic, no database
    required.
  stakeholders:
  - app-developer
  - data-engineer
  competitive_position:
    status: ahead
    rationale: Cypher label map derived automatically from the existing compilation context (no extra
      registration) means Cypher is zero-configuration for existing GraphQL schema users; competitors
      with any Cypher support (none over federated data) require separate graph schema definition.
- id: REQ-352
  status: complete
  group: 5. Query Languages, Compilation & Operations
  category: Cypher Query Frontend (Phase AU)
  priority: MUST
  type: behavioral
  description: Cypher named parameters (`$param`) are translated to Trino positional parameters at compile
    time. Parameter types are inferred from the label mapping schema. Missing parameters with no default
    are rejected at compile time.
  use_case: Named parameters in Cypher translate to parameterized SQL; prevents injection and enables
    query reuse.
  code:
  - provisa/cypher/
  tests:
  - tests/integration/test_cypher_endpoint.py
  - tests/unit/test_cypher_label_map.py
  - tests/unit/test_cypher_parser.py
  - tests/unit/test_cypher_translator.py
  - tests/unit/test_sql_to_cypher.py
  since: 2026-01
  scenario: 'Given a Cypher query with $param and no default

    When the parameter is missing from the request

    Then it is rejected at compile time

    '
  integration_test: not_required
  integration_test_reason: Cypher parameter translation and type inference are pure compiler logic verifiable
    through unit tests.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - app-developer
  competitive_position:
    status: ahead
    rationale: Cypher named parameter translation to Trino positional parameters with type inference from
      label mapping schema is unique — no competitor supports Cypher-to-SQL compilation.
- id: REQ-353
  status: complete
  group: 5. Query Languages, Compilation & Operations
  category: Cypher Query Frontend (Phase AU)
  priority: SHOULD
  type: behavioral
  description: WITHDRAWN (2026-06-19). Cross-source Cypher queries are allowed — Trino joins across catalogs
    natively, so a query whose labels resolve to tables on different sources translates and executes normally.
    No cross-source restriction is enforced. (Supersedes REQ-481.)
  use_case: Cross-source graph traversal is a core capability, not an error.
  code:
  - provisa/cypher/
  tests:
  - tests/integration/test_compiler_integration.py
  - tests/unit/test_cypher_label_map.py
  - tests/unit/test_cypher_translator.py
  - tests/unit/test_sql_to_cypher.py
  since: 2026-01
  scenario: 'Given a Cypher query whose node labels resolve to tables on different Trino catalogs

    When the translator processes it

    Then it generates a cross-catalog JOIN and executes normally without error

    '
  integration_test: required
  integration_test_reason: Cross-source Cypher queries require real Trino federation and execution across
    multiple catalogs.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - data-engineer
  - app-developer
  - ops
  competitive_position:
    status: ahead
    rationale: Cross-source Cypher queries that span multiple Trino catalogs via native federated JOINs
      is unique; no competitor supports cross-source graph traversal over heterogeneous data stores with
      governance applied.
- id: REQ-354
  status: complete
  group: 5. Query Languages, Compilation & Operations
  category: Natural Language Query Service (Phase AV)
  priority: SHOULD
  type: behavioral
  description: Provisa exposes a `POST /query/nl` endpoint accepting a natural language question. The
    service submits an async job and returns a `job_id` immediately. The consumer polls `GET /query/nl/{job_id}`
    or receives the result via SSE when complete.
  use_case: Natural language entry point for non-technical users who cannot write SQL, GraphQL, or Cypher.
  code:
  - provisa/nl/
  tests:
  - tests/unit/test_nl_loop.py
  - tests/unit/test_nl_runner.py
  - tests/integration/test_nl_endpoint.py
  - provisa-ui/e2e/cypher-api.spec.ts
  since: 2026-01
  scenario: 'Given a non-technical user submitting a natural language question to POST /query/nl

    When the service receives it

    Then it returns a job_id immediately and the result is available via polling or SSE

    '
  integration_test: required
  integration_test_reason: NL endpoint requires HTTP transport, async job management, and real query execution
    through the pipeline.
  e2e: true
  e2e_reason: The POST /query/nl REST endpoint with async job polling and SSE delivery requires a real
    running server and HTTP client to verify the round-trip transport behavior.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - app-developer
  - executive
  competitive_position:
    status: ahead
    rationale: A governed NL query endpoint (async job, SSE result) over a federated multi-source data
      platform is not offered by Trino, Hasura, or Denodo in their core product; Dremio has a limited
      NL-to-SQL preview but without governance integration.
- id: REQ-355
  status: complete
  group: 5. Query Languages, Compilation & Operations
  category: Natural Language Query Service (Phase AV)
  priority: SHOULD
  type: behavioral
  description: For each submitted NL query the service runs three independent parallel generation loops
    — one targeting Cypher, one GraphQL, and one SQL. Each loop uses an LLM to generate a candidate query,
    validates it through the existing Provisa compiler pipeline (parse + compile, no execution), feeds
    any compiler error back to the LLM as a correction signal, and retries until the query is valid or
    a max iteration limit is reached. The three loops are independent — failure in one does not block
    the others.
  use_case: Parallel three-target generation maximizes answer quality; one valid result is enough to respond.
  code:
  - provisa/nl/
  tests:
  - tests/e2e/test_query_pipeline.py
  - tests/integration/test_nl_endpoint.py
  - tests/unit/test_cypher_graph_fns.py
  - tests/unit/test_nl_loop.py
  - tests/unit/test_nl_runner.py
  - tests/steps/steps_natural_language_query_service_phase_av.py
  since: 2026-01
  scenario: 'Given an NL query submitted to the service

    When the three generation loops run

    Then each independently generates and validates a Cypher, GraphQL, and SQL candidate with

    compiler-driven refinement

    '
  integration_test: required
  integration_test_reason: Parallel three-target generation requires LLM API calls, compiler validation,
    and real database execution.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - data-engineer
  - app-developer
  - executive
  competitive_position:
    status: ahead
    rationale: Parallel three-target LLM generation (SQL, GraphQL, Cypher) with compiler-driven refinement
      loops is unique — no competitor generates all three query forms from a natural language input, and
      none use the compiler as a deterministic validation oracle to refine LLM output.
- id: REQ-356
  status: complete
  group: 5. Query Languages, Compilation & Operations
  category: Natural Language Query Service (Phase AV)
  priority: MUST
  type: constraint
  description: 'The LLM prompt for each generation loop is scoped to the consumer''s role. The schema
    context supplied to the LLM is the GraphQL SDL visible to that role — tables, fields, and relationships
    the consumer is not permitted to see are absent from the prompt. Generated queries referencing invisible
    schema elements are rejected by the compiler, triggering a refinement loop iteration. Governance is
    enforced at two layers: prompt scoping and compiler validation.'
  use_case: Role-scoped LLM prompt prevents the AI from generating queries referencing data the user cannot
    see.
  code:
  - provisa/nl/
  tests:
  - tests/unit/test_nl_loop.py
  - tests/unit/test_nl_runner.py
  - tests/integration/test_nl_endpoint.py
  since: 2026-01
  scenario: 'Given a user with restricted role submitting an NL query

    When the LLM prompt is constructed

    Then only schema elements visible to that role are included in the prompt

    '
  integration_test: required
  integration_test_reason: Role-scoped LLM prompt requires real role-based schema visibility filtering
    and compiler validation.
  stakeholders:
  - compliance
  - app-developer
  competitive_position:
    status: ahead
    rationale: Role-scoped LLM prompt construction (supplying only the schema visible to the requesting
      role) enforces data governance at the AI layer; no competitor's NL query feature applies role-based
      schema scoping to the LLM context, creating a double-enforcement model (prompt + compiler).
- id: REQ-357
  status: complete
  group: 5. Query Languages, Compilation & Operations
  category: Natural Language Query Service (Phase AV)
  priority: SHOULD
  type: behavioral
  description: 'Once all three generation loops produce valid queries, all three are executed in parallel
    via the standard Provisa pipeline (Stage 2 governance applies to each). The response includes all
    three query texts and their results: `{ cypher: { query, result }, graphql: { query, result }, sql:
    { query, result } }`. A branch that exhausts its iteration limit returns `{ query: null, result: null,
    error: "<last compiler error>" }` for that branch without blocking the response.'
  use_case: All three query forms returned lets power users choose the most legible or efficient representation.
  code:
  - provisa/nl/
  tests:
  - tests/e2e/test_query_pipeline.py
  - tests/integration/test_nl_endpoint.py
  - tests/unit/test_cypher_graph_fns.py
  - tests/unit/test_nl_loop.py
  - tests/unit/test_nl_runner.py
  - tests/steps/steps_natural_language_query_service_phase_av.py
  since: 2026-01
  scenario: 'Given all three generation loops complete

    When results are returned

    Then the response includes cypher, graphql, and sql branches each with query text and result

    '
  integration_test: required
  integration_test_reason: Parallel execution of all three query forms requires real database and governance
    enforcement.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - data-engineer
  - app-developer
  competitive_position:
    status: ahead
    rationale: Returning all three query forms (Cypher, GraphQL, SQL) with results in a single NL response
      is unique; no competitor NL product returns multiple query language outputs with governed results.
- id: REQ-358
  status: complete
  group: 5. Query Languages, Compilation & Operations
  category: Natural Language Query Service (Phase AV)
  priority: SHOULD
  type: behavioral
  description: 'The NL query service is differentiated from commodity text-to-SQL tools by: (1) three-target
    generation (SQL, GraphQL, Cypher) — Cypher output for graph-shaped questions is not available in any
    comparable tool; (2) role-scoped schema context — the LLM cannot generate queries referencing data
    the consumer is not permitted to see; (3) compiler-driven refinement loop — deterministic validation
    replaces heuristic result quality checks.'
  use_case: 'Differentiator: only platform offering role-scoped, compiler-validated, multi-target NL query
    generation.'
  code:
  - provisa/nl/
  tests:
  - tests/integration/test_nl_endpoint.py
  - tests/unit/test_cypher_graph_fns.py
  - tests/unit/test_nl_loop.py
  - tests/unit/test_nl_runner.py
  - tests/steps/steps_natural_language_query_service_phase_av.py
  since: 2026-01
  scenario: 'Given the NL query service

    When compared to commodity text-to-SQL tools

    Then it provides three-target output, role-scoped prompts, and compiler-driven refinement

    '
  integration_test: not_required
  integration_test_reason: NL service differentiation is a positioning claim, not a functional requirement
    with testable behavior.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - executive
  - app-developer
  competitive_position:
    status: ahead
    rationale: 'This requirement codifies the NL service differentiation explicitly: three-target output,
      role-scoped prompts, and compiler-driven refinement together constitute a capability set not offered
      by any comparable platform (Dremio NL preview, BigQuery Duet AI, Snowflake Cortex all lack at least
      two of the three properties).'
- id: REQ-359
  status: complete
  group: 5. Query Languages, Compilation & Operations
  category: Natural Language Query Service (Phase AV)
  priority: MUST
  type: constraint
  description: Any valid query produced by the NL service is executed directly under the consumer's rights,
    and may be saved by the consumer for reuse. Generated queries are governed by the same rights + Stage
    2 enforcement as hand-written queries — the NL service never bypasses governance.
  use_case: NL-generated queries run under the consumer's rights with full Stage 2 governance — never
    bypassed.
  code:
  - provisa/nl/
  tests:
  - tests/integration/test_direct_exec.py
  - tests/integration/test_nl_endpoint.py
  - tests/unit/test_nl_loop.py
  - tests/unit/test_nl_runner.py
  since: 2026-01
  scenario: 'Given a valid query produced by the NL service

    When it is executed

    Then it runs under the consumer''s rights with Stage 2 governance applied, never bypassed

    '
  integration_test: required
  integration_test_reason: NL-generated queries require real execution under consumer rights with full
    Stage 2 governance.
  stakeholders:
  - compliance
  - data-engineer
  competitive_position:
    status: ahead
    rationale: NL-generated queries executing under the consumer's rights with full Stage 2 governance
      — never bypassed — is stronger than Dremio and Snowflake Cortex, which do not apply row-level security
      to AI-generated queries at the same enforcement layer as hand-written queries.
- id: REQ-360
  status: complete
  group: 5. Query Languages, Compilation & Operations
  category: Tracked Functions & Custom Mutations
  priority: SHOULD
  type: behavioral
  description: 'Action query fields (tracked functions and webhooks with `exposed_as: query`) must support
    standard filter/sort/pagination GraphQL arguments: `where`, `order_by`, `limit`, `offset` — applied
    as Python post-processing after the function executes and results are materialized.'
  use_case: Filter/sort/pagination on action query fields gives function-backed results the same query
    ergonomics as tables.
  code:
  - provisa/compiler/
  - provisa/executor/
  - provisa/webhooks/
  tests:
  - tests/e2e/test_mutations.py
  - tests/integration/test_compiler_integration.py
  - tests/unit/test_actions.py
  - tests/unit/test_function_gen.py
  since: 2026-01
  scenario: 'Given an action query field with where, order_by, limit, and offset arguments

    When the function executes and results are materialized

    Then filter, sort, and pagination are applied as Python post-processing

    '
  integration_test: required
  integration_test_reason: Filter/sort/pagination on action query fields requires real function execution
    and Python post-processing.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - app-developer
  - data-engineer
  competitive_position:
    status: parity
    rationale: Filter/sort/pagination arguments on function-backed query fields is offered by Hasura Actions
      (with where/order_by/limit/offset post-processing). The Python post-processing approach is equivalent
      in result, if less efficient than SQL pushdown.
- id: REQ-361
  status: complete
  group: 5. Query Languages, Compilation & Operations
  category: Tracked Functions & Custom Mutations
  priority: MUST
  type: behavioral
  description: Action query fields returning a known table type must resolve nested relationship fields
    by batching lookups against the related source table and merging results back onto each row. Relationship
    resolution applies the same governance rules (column visibility, RLS, column masking) as direct table
    queries.
  use_case: Governed relationship resolution on action results ensures nested fields respect RLS, masking,
    and visibility like direct queries.
  code:
  - provisa/compiler/
  - provisa/executor/
  - provisa/webhooks/
  tests:
  - tests/e2e/test_mutations.py
  - tests/integration/test_compiler_integration.py
  - tests/unit/test_actions.py
  - tests/unit/test_function_gen.py
  since: 2026-01
  scenario: 'Given an action query field returning a registered table type with nested relationship fields

    When the results are resolved

    Then related rows are fetched via batched lookups with RLS and masking applied

    '
  integration_test: required
  integration_test_reason: Nested relationship resolution on action results requires real database batching
    and governance (RLS, masking).
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - data-engineer
  - app-developer
  - compliance
  competitive_position:
    status: ahead
    rationale: Governed relationship resolution (RLS, masking, column visibility) applied during batched
      secondary fetches on action results is not enforced by Hasura Actions, which does not re-apply row-level
      policies when resolving nested relationships on function results.
- id: REQ-362
  status: complete
  group: 5. Query Languages, Compilation & Operations
  category: Tracked Functions & Custom Mutations
  priority: MUST
  type: behavioral
  description: One-to-many relationships on action result rows must return an array field; many-to-one
    relationships must return an object field or null. Relationship cardinality is sourced from the JoinMeta
    declarations that define the table relationships.
  use_case: Cardinality-correct relationship fields (array vs object) on action results match the JoinMeta
    declarations.
  code:
  - provisa/compiler/
  - provisa/executor/
  - provisa/webhooks/
  tests:
  - tests/e2e/test_mutations.py
  - tests/integration/test_compiler_integration.py
  - tests/unit/test_actions.py
  - tests/unit/test_function_gen.py
  since: 2026-01
  scenario: 'Given an action result with a one-to-many relationship

    When the relationship field is resolved

    Then it returns an array; many-to-one returns an object or null per JoinMeta cardinality

    '
  integration_test: not_required
  integration_test_reason: Cardinality-correct relationship fields on action results is schema generation
    from JoinMeta declarations.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - app-developer
  competitive_position:
    status: parity
    rationale: Cardinality-correct relationship fields (array vs object) on action results sourced from
      JoinMeta declarations is equivalent to Hasura's object/array relationship distinction. Standard
      feature for any GraphQL data layer.
- id: REQ-403
  status: complete
  group: 5. Query Languages, Compilation & Operations
  category: Compiler & Schema
  priority: MUST
  type: behavioral
  description: RLS injection via `inject_rls()` checks for table-specific rules first, then falls back
    to domain-level rules; table-level rules take precedence.
  use_case: Table-level RLS precedence lets stewards override domain rules selectively for exceptions
    without duplicating configuration.
  code:
  - provisa/compiler/sql_gen.py
  tests:
  - tests/integration/test_compiler_integration.py
  - tests/unit/test_rls_compiler_fallback.py
  - tests/unit/test_schema_service.py
  since: 2026-01
  scenario: 'Given a table with both table-specific and domain-level RLS rules

    When inject_rls() runs

    Then table-specific rules take precedence over domain-level rules

    '
  integration_test: required
  integration_test_reason: RLS injection with table-level precedence requires real database queries and
    rule composition testing.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - compliance
  - data-engineer
  competitive_position:
    status: ahead
    rationale: Table-level RLS rules taking precedence over domain-level rules with a two-tier fallback
      is a governance hierarchy not offered by Hasura (single-level per-table RLS filter) or Trino (no
      domain-level policy layer); it gives stewards more precise override control without duplicating
      config.
- id: REQ-409
  status: complete
  group: 5. Query Languages, Compilation & Operations
  category: Compiler & Schema
  priority: MUST
  type: behavioral
  description: Cypher translator detects ISO 8601 datetime string literals in WHERE clauses and wraps
    them as `TIMESTAMP '...'` before SQLGlot parsing, fixing Trino type mismatch errors when filtering
    on timestamp primary key columns.
  use_case: Automatic timestamp coercion eliminates manual casting for timestamp PK comparisons in Cypher
    queries.
  code:
  - provisa/cypher/translator.py
  tests:
  - tests/integration/test_compiler_integration.py
  - tests/unit/test_cypher_translator.py
  - tests/unit/test_schema_service.py
  since: 2026-01
  scenario: 'Given a Cypher WHERE clause with an ISO 8601 datetime string literal

    When the translator processes it

    Then it wraps the literal as TIMESTAMP ''...'' before SQLGlot parsing

    '
  integration_test: required
  integration_test_reason: Timestamp literal detection and coercion in Cypher queries requires real Trino
    execution to verify type mismatch fixes.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - data-engineer
  - app-developer
  competitive_position:
    status: neutral
    rationale: ''
- id: REQ-411
  status: complete
  group: 5. Query Languages, Compilation & Operations
  category: Compiler & Schema
  priority: SHOULD
  type: behavioral
  description: Naming convention option `hasura-default` provides snake_case mutation names (insert_orders,
    update_orders, delete_orders) and snake_case field names, matching Hasura V2's default naming style.
  use_case: Alternative naming convention supports teams migrating from Hasura by providing compatible
    mutation and field name generation.
  code:
  - provisa/compiler/sql_gen.py
  tests:
  - tests/integration/test_compiler_integration.py
  - tests/unit/test_schema_service.py
  - tests/unit/test_sql_gen.py
  since: 2026-01
  scenario: 'Given naming convention is set to hasura-default

    When the schema is generated

    Then mutation names and field names use snake_case matching Hasura V2 defaults

    '
  integration_test: not_required
  integration_test_reason: Naming convention hasura-default is pure schema generation logic verifiable
    through unit tests.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - app-developer
  competitive_position:
    status: parity
    rationale: Hasura-compatible snake_case naming convention is explicit Hasura migration parity. Not
      a competitive differentiator — it lowers switching costs for Hasura customers.
- id: REQ-412
  status: complete
  group: 5. Query Languages, Compilation & Operations
  category: Compiler & Schema
  priority: SHOULD
  type: behavioral
  description: Naming convention option `graphql-default` provides camelCase field names, PascalCase types,
    and camelCase mutations (insertOrders, updateOrders), matching Hasura V2's graphql-default and GraphQL
    best practices. This is now the Provisa default.
  use_case: GraphQL best-practice naming convention improves developer experience and aligns with ecosystem
    defaults.
  code:
  - provisa/compiler/sql_gen.py
  tests:
  - tests/integration/test_compiler_integration.py
  - tests/unit/test_schema_service.py
  - tests/unit/test_sql_gen.py
  since: 2026-01
  scenario: 'Given the default Provisa naming convention

    When the schema is generated

    Then field names are camelCase, types are PascalCase, and mutations are camelCase

    '
  integration_test: not_required
  integration_test_reason: Naming convention graphql-default is pure schema generation logic verifiable
    through unit tests.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - app-developer
  competitive_position:
    status: parity
    rationale: GraphQL-default camelCase naming matching Hasura v2 graphql-default is explicit parity.
      Standard naming in the GraphQL ecosystem.
- id: REQ-416
  status: complete
  group: 5. Query Languages, Compilation & Operations
  category: Compiler & Schema
  priority: MUST
  type: structural
  description: 'Naming convention now uses three preset enums (`snake`, `hasura_graphql`, `apollo_graphql`
    default) with `domain_prefix: bool` as orthogonal option. `snake` (PascalCase types, snake_case fields/mutations),
    `hasura_graphql` (PascalCase types, camelCase fields, snake_case mutations matching Hasura V2), `apollo_graphql`
    (PascalCase types, camelCase fields/mutations per Apollo/GraphQL idiom). Old free-form strings deprecated.'
  use_case: Enum-based preset naming eliminates ambiguous string values and standardizes on clear, documented
    conventions compatible with Hasura and Apollo ecosystems.
  code:
  - provisa/compiler/sql_gen.py
  tests:
  - tests/integration/test_compiler_integration.py
  - tests/unit/test_naming_convention_validation.py
  - tests/unit/test_schema_service.py
  - tests/unit/test_sql_gen.py
  since: 2026-01
  scenario: 'Given a naming_convention set to snake, hasura_graphql, or apollo_graphql

    When the schema is generated

    Then field names, type names, and mutation names follow the rules for the selected preset

    '
  integration_test: not_required
  integration_test_reason: Enum-based naming convention presets are schema generation logic verifiable
    through unit tests.
  stakeholders:
  - app-developer
  - data-engineer
  competitive_position:
    status: ahead
    rationale: Three preset naming conventions (snake, hasura_graphql, apollo_graphql) with orthogonal
      domain_prefix option is more configurable than Hasura's fixed naming and Trino's no-naming-control
      model.
- id: REQ-478
  status: complete
  group: 5. Query Languages, Compilation & Operations
  category: Compiler & Schema
  priority: MAY
  type: behavioral
  description: 'Statistical row sampling is a user query feature, not governance. Root query fields accept
    an optional `sample: Float` argument (percentage in (0, 100]); the compiler emits `TABLESAMPLE BERNOULLI
    (<pct>)` on the base table, which SQLGlot transpiles per dialect (direct PG and Trino both supported).
    Out-of-range values are rejected at compile time. Not combinable with `as_of` (time-travel) or op-relationship
    lateral joins. Raw-SQL clients use native `TABLESAMPLE` directly. Distinct from the REQ-263 row cap,
    which bounds result size.'
  use_case: Statistical sampling lets analysts request a random subset for exploration, separate from
    the governance row cap.
  code:
  - provisa/compiler/sql_gen.py
  - provisa/compiler/schema_gen.py
  tests:
  - tests/integration/test_compiler_integration.py
  - tests/unit/test_schema_service.py
  - tests/unit/test_sql_gen.py
  since: 2026-01
  scenario: 'Given a query with sample: 10.0

    When the compiler processes it

    Then it emits TABLESAMPLE BERNOULLI (10) on the base table and rejects values outside (0, 100]

    '
  integration_test: required
  integration_test_reason: Statistical row sampling via TABLESAMPLE requires real database execution and
    SQLGlot dialect transpilation.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - data-engineer
  - app-developer
  competitive_position:
    status: ahead
    rationale: A governed GraphQL `sample` argument that compiles to TABLESAMPLE BERNOULLI, distinct from
      the row cap, and transparently dialect-transpiled is not offered by Hasura or Denodo; Trino supports
      TABLESAMPLE natively in SQL but has no GraphQL or governance layer. Provisa makes statistical sampling
      a first-class governed data feature.
- id: REQ-525
  status: complete
  group: 5. Query Languages, Compilation & Operations
  category: Compiler & Schema
  priority: MUST
  type: behavioral
  description: Auto-generated `.proto` from the data schema is generated per role. Each role receives
    a proto definition reflecting only the tables and columns visible to that role.
  use_case: Per-role proto generation ensures gRPC clients only see schema elements they are authorized
    to access.
  code:
  - provisa/grpc/proto_gen.py
  tests:
  - tests/integration/test_compiler_integration.py
  - tests/unit/test_grpc_requirements.py
  - tests/unit/test_schema_service.py
  since: 2026-01
  scenario: 'Given two roles with different table and column visibility

    When proto definitions are generated

    Then each role receives a proto reflecting only its visible tables and columns

    '
  integration_test: not_required
  integration_test_reason: Per-role proto generation is pure schema logic based on role-based visibility
    filtering.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - app-developer
  competitive_position:
    status: ahead
    rationale: Per-role auto-generated .proto definitions for gRPC — where each role receives only its
      visible tables and columns — is not offered by any mainstream competitor; Trino, Hasura, and Denodo
      have no governed gRPC/Protobuf schema generation.
- id: REQ-526
  status: complete
  group: 5. Query Languages, Compilation & Operations
  category: Compiler & Schema
  priority: SHOULD
  type: infrastructure
  description: gRPC server reflection is enabled on the Provisa Protobuf gRPC server (port 50051), allowing
    clients to discover available RPCs and message schemas at runtime without a pre-shared proto file.
  use_case: Server reflection enables tooling (grpcurl, grpcui, Postman) to discover gRPC services without
    distributing proto files.
  code:
  - provisa/grpc/reflection.py
  - provisa/grpc/server.py
  tests:
  - tests/e2e/test_grpc_query.py
  - tests/integration/test_compiler_integration.py
  - tests/unit/test_grpc_reflection.py
  - tests/unit/test_grpc_server.py
  - tests/unit/test_schema_service.py
  since: 2026-01
  scenario: 'Given the gRPC server is running on port 50051

    When a client issues a server reflection request

    Then it receives the available RPC and message schema definitions at runtime

    '
  integration_test: required
  integration_test_reason: gRPC server reflection requires real gRPC transport and client discovery at
    runtime.
  e2e: true
  e2e_reason: gRPC server reflection is a transport-layer protocol behavior that requires a real running
    gRPC server on port 50051 and a client issuing a reflection request to verify.
  stakeholders:
  - app-developer
  - ops
  competitive_position:
    status: parity
    rationale: gRPC server reflection is a standard gRPC feature (part of the grpc-reflection spec) enabled
      by most gRPC server implementations. Competitive parity with any platform offering gRPC.
- id: REQ-534
  status: complete
  group: 5. Query Languages, Compilation & Operations
  category: Compiler & Schema
  priority: SHOULD
  type: behavioral
  description: 'GraphQL queries with multiple root fields are compiled into separate SQL queries and executed
    independently. Results are merged into a single response: fields below the redirect threshold are
    returned inline in `data`; fields above the threshold are redirected with per-field entries in `redirects`.
    Binary formats (Parquet, Arrow) are only supported for single-root queries.'
  use_case: Multi-root query execution lets GraphQL clients fetch multiple independent datasets in a single
    request while applying per-field redirect thresholds.
  code:
  - provisa/api/data/endpoint.py
  tests:
  - tests/integration/test_compiler_integration.py
  - tests/integration/test_direct_exec.py
  - tests/unit/test_compiler_requirements.py
  - tests/unit/test_multi_root_queries.py
  - tests/unit/test_schema_service.py
  since: 2026-01
  scenario: 'Given a GraphQL query with multiple root fields

    When it is executed

    Then each root field is compiled and executed independently and results are merged into one

    response

    '
  integration_test: required
  integration_test_reason: Multi-root query execution requires real database queries and HTTP redirect
    thresholds applied per-field.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - app-developer
  - data-engineer
  competitive_position:
    status: parity
    rationale: Multi-root GraphQL query execution with per-field redirect thresholds is a practical scalability
      feature; Hasura supports batched queries and DataLoader patterns. The per-field redirect threshold
      is Provisa-specific but addresses the same concern as Hasura's stream and defer directives.
- id: REQ-537
  status: complete
  group: 5. Query Languages, Compilation & Operations
  category: Compiler & Schema
  priority: SHOULD
  type: behavioral
  description: '`GET /data/schema-version` returns a string combining a per-boot UUID nonce with a monotonically
    incrementing rebuild counter in the format `<boot-id>-<counter>`. Clients use this value to detect
    schema changes and invalidate local schema caches.'
  use_case: Schema version endpoint lets clients detect server-side schema changes and re-fetch SDL or
    introspection without polling.
  code:
  - provisa/api/data/sdl.py
  tests:
  - tests/integration/test_schema_gen.py
  - tests/unit/test_compiler_requirements.py
  - tests/unit/test_schema_service.py
  - provisa-ui/e2e/cypher-api.spec.ts
  since: 2026-01
  scenario: 'Given the schema is rebuilt after a naming convention change

    When GET /data/schema-version is called

    Then it returns a new <boot-id>-<counter> string reflecting the rebuild

    '
  integration_test: required
  integration_test_reason: Schema version endpoint requires HTTP transport and client-side cache invalidation
    behavior.
  e2e: true
  e2e_reason: GET /data/schema-version is an HTTP REST endpoint whose cache-invalidation semantics (boot-id
    + counter format, change on rebuild) can only be verified against a real running server.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - app-developer
  - ops
  competitive_position:
    status: parity
    rationale: A schema version endpoint for client cache invalidation is a developer-convenience feature;
      Hasura exposes a catalog metadata version hash for the same purpose. Not a differentiator.
- id: REQ-571
  status: complete
  group: 5. Query Languages, Compilation & Operations
  category: Cypher Query Frontend (Phase AU)
  priority: MUST
  type: structural
  description: The Cypher parser is a custom recursive-descent implementation with no dependency on any
    external Cypher library. It produces a CypherAST from read-only Cypher queries.
  use_case: Custom parser with no external library dependency gives full control over the parse grammar
    and error messages for Provisa-specific Cypher extensions.
  code:
  - provisa/cypher/parser.py
  tests:
  - tests/integration/test_compiler_integration.py
  - tests/unit/test_cypher_parser.py
  - tests/unit/test_sql_to_cypher.py
  since: 2026-01
  scenario: 'Given a read-only Cypher query

    When the custom recursive-descent parser processes it

    Then it produces a CypherAST without invoking any external Cypher library

    '
  integration_test: not_required
  integration_test_reason: Custom Cypher parser is pure compiler logic verifiable through unit tests.
  stakeholders:
  - app-developer
  - data-engineer
  competitive_position:
    status: ahead
    rationale: Custom recursive-descent Cypher parser with no external library dependency enables full
      governance injection during Cypher-to-SQL compilation — no competitor provides governed Cypher federation.
- id: REQ-572
  status: complete
  group: 5. Query Languages, Compilation & Operations
  category: Cypher Query Frontend (Phase AU)
  priority: SHOULD
  type: behavioral
  description: Provisa handles CALL db.labels(), CALL db.relationshipTypes(), and CALL db.propertyKeys()
    as introspection procedures that return data from the in-memory semantic layer (CypherLabelMap) without
    generating or executing any SQL.
  use_case: Returning schema introspection data from the semantic layer (not the database) lets Neo4j-compatible
    clients discover the graph schema via standard procedure calls.
  code:
  - provisa/api/rest/cypher_router.py
  tests:
  - tests/integration/test_cypher_endpoint.py
  - tests/unit/test_cypher_label_map.py
  - tests/unit/test_cypher_translator.py
  - tests/unit/test_sql_to_cypher.py
  since: 2026-01
  scenario: 'Given a client issuing CALL db.labels()

    When the cypher router handles it

    Then it returns label data from CypherLabelMap without generating or executing SQL

    '
  integration_test: not_required
  integration_test_reason: Introspection procedures return in-memory semantic layer data without SQL execution.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - app-developer
  competitive_position:
    status: ahead
    rationale: Handling CALL db.labels(), db.relationshipTypes(), and db.propertyKeys() as semantic-layer
      introspection procedures (returning data from CypherLabelMap, not from the database) gives Neo4j-compatible
      clients a familiar discovery API over federated relational data; no competitor offers this.
- id: REQ-573
  status: complete
  group: 5. Query Languages, Compilation & Operations
  category: Cypher Query Frontend (Phase AU)
  priority: SHOULD
  type: behavioral
  description: Correlated CALL subqueries of the form CALL { WITH x MATCH (x)-[:R]->(n) RETURN n.prop
    AS alias } are translated to CROSS JOIN LATERAL expressions. The outer-scope variable must appear
    in WITH; multiple imported variables are supported. Non-correlated top-level CALL blocks (without
    WITH) are handled by cypher_calls_to_sql_list.
  use_case: CROSS JOIN LATERAL translation of correlated CALL subqueries lets graph clients use subquery-pattern
    idioms that are common in openCypher workloads.
  code:
  - provisa/cypher/correlated_call.py
  - provisa/cypher/translator.py
  tests:
  - tests/integration/test_compiler_integration.py
  - tests/unit/test_cypher_translator.py
  - tests/unit/test_sql_to_cypher.py
  since: 2026-01
  scenario: 'Given a Cypher CALL subquery with WITH importing an outer variable

    When the translator processes it

    Then it emits a CROSS JOIN LATERAL expression

    '
  integration_test: required
  integration_test_reason: Correlated CALL subqueries to CROSS JOIN LATERAL translation requires real
    database execution.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - data-engineer
  - app-developer
  competitive_position:
    status: ahead
    rationale: Correlated CALL subqueries translated to CROSS JOIN LATERAL over Trino is a sophisticated
      graph-to-SQL translation feature not available in any competitor's relational data federation layer.
- id: REQ-574
  status: complete
  group: 5. Query Languages, Compilation & Operations
  category: Cypher Query Frontend (Phase AU)
  priority: MUST
  type: constraint
  description: Relationships in Provisa exist solely as join metadata in the semantic layer (JoinMeta).
    They carry no stored attributes. Relationship property access (e.g. WHERE r.since > 2020 or RETURN
    r.weight) has no meaning and is not supported.
  use_case: Restricting relationships to join metadata avoids the complexity of relationship property
    storage while covering the vast majority of graph traversal use cases.
  code:
  - provisa/cypher/label_map.py
  - provisa/cypher/translator.py
  tests:
  - tests/integration/test_compiler_integration.py
  - tests/unit/test_cypher_translator.py
  - tests/unit/test_sql_to_cypher.py
  since: 2026-01
  scenario: 'Given a Cypher query accessing r.since on a relationship variable

    When the translator processes it

    Then it is rejected as unsupported

    '
  integration_test: not_required
  integration_test_reason: Relationship attribute restriction is a compile-time constraint verifiable
    through unit tests.
  stakeholders:
  - data-engineer
  competitive_position:
    status: neutral
    rationale: ''
- id: REQ-575
  status: complete
  group: 5. Query Languages, Compilation & Operations
  category: Cypher Query Frontend (Phase AU)
  priority: SHOULD
  type: behavioral
  description: Bidirectional traversal syntax (a)-[]-(b) is rewritten at compile time to a UNION ALL of
    all matching directed forward and backward relationships from the semantic layer. Every relationship
    in the semantic layer is directional; bidirectional syntax is sugar that expands to both directions.
    When only a single relationship direction exists in the schema, the UNION is skipped and a single
    directed branch is emitted (absorbs REQ-758).
  use_case: Bidirectional sugar covers common graph traversal patterns without requiring stewards to register
    both directions of every relationship.
  code:
  - provisa/cypher/translator.py
  tests:
  - tests/integration/test_compiler_integration.py
  - tests/unit/test_cypher_translator.py
  - tests/unit/test_sql_to_cypher.py
  since: 2026-01
  scenario: 'Given a Cypher query with bidirectional traversal (a)-[]-(b)

    When the translator processes it

    Then it emits a UNION ALL of forward and backward directed relationship joins

    '
  integration_test: not_required
  integration_test_reason: Bidirectional traversal syntax expansion is pure SQL generation logic verifiable
    through unit tests.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - app-developer
  - data-engineer
  competitive_position:
    status: ahead
    rationale: Bidirectional Cypher traversal syntax expanded at compile time to UNION ALL of directed
      join pairs is a graph-query convenience that no competitor offers over federated relational data.
- id: REQ-576
  status: complete
  group: 5. Query Languages, Compilation & Operations
  category: Cypher Query Frontend (Phase AU)
  priority: SHOULD
  type: behavioral
  description: When shortestPath endpoints have different node types and no self-referential relationship
    exists in the schema, the translator emits a flat JOIN chain (structurally shortest schema path) rather
    than a recursive CTE with ORDER BY hops. Hops are not tracked in this code path, so the result is
    the structurally shortest schema path, not the data-shortest path across multiple rows.
  use_case: Flat JOIN for heterogeneous shortestPath avoids unnecessary recursive CTE overhead when the
    schema topology determines the path uniquely.
  code:
  - provisa/cypher/path_translator.py
  tests:
  - tests/integration/test_compiler_integration.py
  - tests/unit/test_cypher_path_translator.py
  - tests/unit/test_cypher_translator.py
  - tests/unit/test_sql_to_cypher.py
  since: 2026-01
  scenario: 'Given a shortestPath query between two different node types with a unique schema path

    When the translator processes it

    Then it emits a flat JOIN chain instead of a recursive CTE

    '
  integration_test: required
  integration_test_reason: Flat JOIN optimization for heterogeneous shortestPath requires real database
    execution to verify path correctness.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - data-engineer
  - ops
  competitive_position:
    status: ahead
    rationale: Flat JOIN chain optimization for heterogeneous shortestPath (avoiding unnecessary recursive
      CTE overhead) over federated relational data is unique; competitors with Cypher support (none over
      federated data) do not perform this structural optimization.
- id: REQ-577
  status: complete
  group: 5. Query Languages, Compilation & Operations
  category: Cypher Query Frontend (Phase AU)
  priority: SHOULD
  type: behavioral
  description: When multiple schema paths of equal hop count connect the same start and end node types,
    all matching paths are emitted as UNION ALL branches. Row-level deduplication across branches is not
    performed.
  use_case: Emitting all schema paths as UNION ALL branches ensures no valid traversal is silently omitted
    when multiple relationship types connect the same pair of node types.
  code:
  - provisa/cypher/translator.py
  tests:
  - tests/integration/test_compiler_integration.py
  - tests/unit/test_cypher_translator.py
  - tests/unit/test_sql_to_cypher.py
  since: 2026-01
  scenario: 'Given multiple schema paths of equal hop count between the same node types

    When the translator processes a shortestPath query

    Then all matching paths are emitted as UNION ALL branches without deduplication

    '
  integration_test: required
  integration_test_reason: Multiple schema path emission requires real database execution to verify all
    paths are traversed.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - data-engineer
  - app-developer
  competitive_position:
    status: ahead
    rationale: Emitting all equal-hop schema paths as UNION ALL branches for shortestPath over multiple
      relationship types is a correctness and completeness property not matched by any competitor's graph-over-relational
      solution.
- id: REQ-578
  status: complete
  group: 5. Query Languages, Compilation & Operations
  category: Cypher Query Frontend (Phase AU)
  priority: MAY
  type: constraint
  description: WITH clause CTEs are named _w0, _w1, ... using a positional index assigned within a single
    translation call. Composing multiple independently translated queries (e.g. in a batch) can produce
    colliding CTE names if the results are concatenated without renaming.
  use_case: Positional CTE naming is deterministic within one translation call; the collision risk on
    batch composition is a known limitation that callers must handle.
  code:
  - provisa/cypher/translator.py
  tests:
  - tests/integration/test_compiler_integration.py
  - tests/unit/test_cypher_translator.py
  - tests/unit/test_sql_to_cypher.py
  since: 2026-01
  scenario: 'Given a single Cypher query with multiple WITH clauses

    When the translator processes it

    Then CTEs are named _w0, _w1, ... sequentially within that translation call

    '
  integration_test: not_required
  integration_test_reason: Positional CTE naming is a naming convention verifiable through unit tests.
  stakeholders:
  - app-developer
  - data-engineer
  competitive_position:
    status: neutral
    rationale: ''
- id: REQ-642
  status: complete
  group: 5. Query Languages, Compilation & Operations
  category: Graph Analytics Pipeline
  priority: SHOULD
  type: behavioral
  description: A POST /data/graph-analytics endpoint accepts a Cypher query and algorithm name, executes
    the query via the existing cypher_router pipeline, builds an in-memory NetworkX DiGraph from the resulting
    nodes and edges, runs the named algorithm, merges a `_analytics` dict into each node/edge, and returns
    the augmented nodes and edges as JSON with an `elapsed_ms` field.
  use_case: Server-side graph analytics endpoint lets the browser request algorithm output on any Cypher-defined
    subgraph without building the graph client-side.
  code:
  - provisa/api/rest/graph_analytics_router.py
  tests:
  - tests/e2e/test_query_pipeline.py
  - tests/integration/test_compiler_integration.py
  - tests/unit/test_cypher_graph_fns.py
  - tests/unit/test_graph_analytics_requirements.py
  - provisa-ui/e2e/cypher-api.spec.ts
  since: 2026-01
  scenario: 'Given a POST /data/graph-analytics request with a Cypher query and algorithm name

    When the endpoint processes it

    Then it builds a NetworkX DiGraph, runs the algorithm, and returns augmented nodes and edges

    with elapsed_ms

    '
  integration_test: required
  integration_test_reason: Graph analytics endpoint requires Cypher execution, NetworkX algorithm execution,
    and result merging.
  e2e: true
  e2e_reason: POST /data/graph-analytics is a REST endpoint that combines Cypher execution, NetworkX algorithm
    execution, and JSON response merging — only verifiable through a full live-stack HTTP round-trip.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - app-developer
  - data-engineer
  competitive_position:
    status: ahead
    rationale: A server-side graph analytics endpoint (NetworkX algorithms over Cypher-defined subgraphs
      from federated relational data) is not available in Trino, Hasura, Denodo, or Dremio; competitors
      that offer graph analytics (Neo4j, TigerGraph) operate on native graph stores, not federated relational
      data.
- id: REQ-643
  status: complete
  group: 5. Query Languages, Compilation & Operations
  category: Graph Analytics Pipeline
  priority: SHOULD
  type: structural
  description: 'The graph analytics response merges a `_analytics` dict into every node and edge in the
    result. The keys present in `_analytics` vary by algorithm: centrality algorithms produce `score`;
    community detection produces `cluster`; k-core produces `core_number`; degree centrality also produces
    `in_degree` and `out_degree`.'
  use_case: Uniform _analytics key convention lets the UI apply visual encodings without algorithm-specific
    branching.
  code:
  - provisa/api/rest/graph_analytics_router.py
  tests:
  - tests/e2e/test_query_pipeline.py
  - tests/integration/test_compiler_integration.py
  - tests/unit/test_graph_analytics_requirements.py
  since: 2026-01
  scenario: 'Given a graph analytics response from a centrality algorithm

    When the UI reads _analytics

    Then it contains a score key; community detection contains cluster; k-core contains core_number

    '
  integration_test: not_required
  integration_test_reason: _analytics dict merging by algorithm is pure JSON response formatting logic.
  stakeholders:
  - app-developer
  competitive_position:
    status: ahead
    rationale: In-query graph analytics (centrality, community detection) merged into node/edge results
      as _analytics fields is not available in Hasura, Trino, Dremio, or Snowflake without external tooling.
- id: REQ-650
  status: complete
  group: 5. Query Languages, Compilation & Operations
  category: Graph Analytics Pipeline
  priority: MUST
  type: constraint
  description: 'The graph analytics endpoint enforces a configurable maximum graph size. When the input
    graph exceeds the configured limit (default: 10,000 nodes or 50,000 edges), the endpoint returns HTTP
    413 before running any algorithm.'
  use_case: Graph size cap prevents unbounded memory allocation from oversized graphs passed to in-process
    algorithm libraries.
  code:
  - provisa/api/rest/graph_analytics_router.py
  tests:
  - tests/e2e/test_query_pipeline.py
  - tests/integration/test_compiler_integration.py
  - tests/unit/test_graph_analytics_requirements.py
  since: 2026-01
  scenario: 'Given a graph exceeding 10,000 nodes or 50,000 edges

    When submitted to POST /data/graph-analytics

    Then the endpoint returns HTTP 413 before running any algorithm

    '
  integration_test: not_required
  integration_test_reason: Graph size limit enforcement is a validation constraint verifiable through
    unit tests.
  stakeholders:
  - ops
  competitive_position:
    status: neutral
    rationale: ''
- id: REQ-651
  status: complete
  group: 5. Query Languages, Compilation & Operations
  category: Graph Analytics Pipeline
  priority: MUST
  type: constraint
  description: The Girvan-Newman community detection algorithm is restricted to graphs with fewer than
    500 nodes. Requests for Girvan-Newman on larger graphs are rejected unless the caller supplies `force=true`
    in the params, making the computational risk explicit.
  use_case: Girvan-Newman restriction prevents O(n³) runtime from making large-graph analytics requests
    impractical.
  code:
  - provisa/api/rest/graph_analytics_router.py
  tests:
  - tests/e2e/test_query_pipeline.py
  - tests/integration/test_compiler_integration.py
  - tests/unit/test_graph_analytics_requirements.py
  since: 2026-01
  scenario: 'Given a Girvan-Newman request on a graph with 500 or more nodes and force=false

    When submitted to the endpoint

    Then the request is rejected

    '
  integration_test: not_required
  integration_test_reason: Girvan-Newman size restriction is a validation constraint verifiable through
    unit tests.
  stakeholders:
  - ops
  - data-engineer
  competitive_position:
    status: neutral
    rationale: ''
- id: REQ-653
  status: complete
  group: 5. Query Languages, Compilation & Operations
  category: Compiler & Schema
  priority: SHOULD
  type: structural
  description: Table-level `enable_aggregates` and `enable_group_by` flags in provisa.yaml (both default
    `false`). `enable_aggregates` replaces the role-level `no_aggregations` capability as the primary
    gate for `{table}_aggregate` root fields. `enable_group_by` gates `{table}_group_by` root fields.
    Stewards opt in per table, controlling SDL noise at the table level rather than globally per role.
  use_case: Steward can expose aggregate and group-by fields only on tables where they are meaningful,
    keeping the SDL small and navigable for API consumers.
  code:
  - provisa/compiler/schema_gen.py
  - provisa/core/models.py
  tests:
  - tests/integration/test_compiler_integration.py
  - tests/unit/test_core_registration.py
  - tests/unit/test_schema_service.py
  - provisa-ui/e2e/tables-enable-aggregates.spec.ts
  since: 2026-06
  integration_test: not_required
  integration_test_reason: Table-level enable_aggregates and enable_group_by flags are schema generation
    configuration logic.
  stakeholders:
  - app-developer
  - data-engineer
  competitive_position:
    status: ahead
    rationale: Table-level enable_aggregates/enable_group_by flags as the governance gate for aggregations
      (replacing role-level no_aggregations) gives stewards finer-grained control than Hasura's role-level
      approach.
- id: REQ-654
  status: complete
  group: 5. Query Languages, Compilation & Operations
  category: Compiler & Schema
  priority: SHOULD
  type: behavioral
  description: '`{table}_group_by` root query field. Requires `by: [SelectColumn!]!`; accepts same args
    as the base table query (`where`, `order_by`, `limit`, `offset`, `distinct_on`). Returns `[{TypeName}GroupByRow!]!`
    where `{TypeName}GroupByRow` has `groupKey: JSON!` (object mapping group-by column names to their
    values) and `aggregates: {TypeName}AggregateFields!`. Reuses existing `{TypeName}AggregateFields`
    type unchanged. Maps to `SELECT <by_cols>, <agg_funcs> FROM <table> WHERE <where> GROUP BY <by_cols>
    ORDER BY <order_by> LIMIT <limit> OFFSET <offset>`.'
  use_case: Exposes grouped analytics (e.g. count per user, sum per category) as a first-class query without
    requiring a database view.
  code:
  - provisa/compiler/schema_gen.py
  - provisa/compiler/aggregate_gen.py
  - provisa/compiler/sql_gen.py
  tests:
  - tests/integration/test_compiler_integration.py
  - tests/unit/test_group_by.py
  - tests/unit/test_schema_service.py
  scenario: 'Given a registered table with numeric columns

    When a _group_by query is submitted with by: [category] and aggregate count

    Then the response contains one GroupByRow per distinct category value

    And each row includes groupKey and aggregates fields

    '
  since: 2026-06
  integration_test: required
  integration_test_reason: Group-by root query field requires real database GROUP BY execution and aggregation.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - data-engineer
  - app-developer
  competitive_position:
    status: ahead
    rationale: A first-class `_group_by` root query field with `groupKey` + `aggregates` structure, exposed
      as a governed GraphQL type without requiring a database view, is not offered by Hasura (which requires
      custom views or computed fields for grouped analytics) or Trino (no GraphQL layer). Denodo can expose
      grouped views but requires ETL-style view creation.
- id: REQ-655
  status: complete
  group: 5. Query Languages, Compilation & Operations
  category: Compiler & Schema
  priority: SHOULD
  type: behavioral
  description: '`{table}_group_by` supports two additional filter clauses beyond the base `where`: (1)
    `aggregates(where: {TypeName}BoolExp)` — a `where` arg on the `aggregates` field of `{TypeName}GroupByRow`
    that applies conditional aggregation within each group (SQL `FILTER (WHERE ...)`); (2) `having: {TypeName}HavingExp`
    arg on the root field that filters groups post-aggregation (SQL `HAVING`). `{TypeName}HavingExp` is
    a new input type with the same numeric/comparable fields as `{TypeName}AggregateFields` but with comparison
    operators. Three filters compose orthogonally: `_group_by(where:)` → SQL WHERE, `aggregates(where:)`
    → SQL FILTER, `having:` → SQL HAVING.'
  use_case: Enables queries like "users with more than 3 active inquiries" without client-side filtering
    or custom views.
  code:
  - provisa/compiler/aggregate_gen.py
  - provisa/compiler/sql_gen.py
  tests:
  - tests/integration/test_compiler_integration.py
  - tests/unit/test_group_by.py
  - tests/unit/test_schema_service.py
  scenario: 'Given a _group_by query with a having: clause on an aggregate field

    When the query is compiled

    Then the generated SQL includes a HAVING clause after GROUP BY

    And aggregates(where:) generates a SQL FILTER (WHERE ...) expression

    '
  since: 2026-06
  integration_test: required
  integration_test_reason: Conditional aggregation and HAVING clauses require real database execution
    with FILTER and HAVING SQL.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - data-engineer
  - app-developer
  competitive_position:
    status: ahead
    rationale: 'Three-layer filter composition (WHERE → SQL WHERE, aggregates(where:) → SQL FILTER, having:
      → SQL HAVING) as first-class GraphQL arguments with generated HavingExp input types is not available
      in Hasura v2 (no HAVING support in aggregate queries) or any other GraphQL-over-SQL platform.'
- id: REQ-661
  status: complete
  since: 2026-07
  group: 5. Query Languages, Compilation & Operations
  category: Cypher Mutations
  priority: MUST
  type: constraint
  description: Labels in Cypher write clauses (CREATE, SET, DELETE) must resolve to exactly one NodeMeta
    via the semantic label map. Ambiguous or unknown labels are hard errors at translation time; no fuzzy
    matching or implicit selection is permitted. New labels cannot be created via write clauses — only
    pre-registered tables are writable (absorbs REQ-662).
  use_case: Single-label resolution ensures write operations are unambiguous and cannot accidentally target
    the wrong table.
  code:
  - provisa/cypher/write_translator.py
  tests:
  - tests/e2e/test_mutations.py
  - tests/unit/test_cypher_label_map.py
  - tests/unit/test_cypher_translator.py
  - tests/unit/test_sql_to_cypher.py
  integration_test: not_required
  integration_test_reason: Label resolution in write clauses is compile-time validation verifiable through
    unit tests.
  stakeholders:
  - data-engineer
  - compliance
  competitive_position:
    status: gap
    rationale: Cypher mutations (CREATE/SET/DELETE via Cypher syntax) are in-progress. No competitor offers
      governed Cypher writes over federated relational data either, but this is a planned capability gap
      until implementation is complete.
- id: REQ-662
  status: complete
  superseded_by: REQ-661
  since: 2026-07
  group: 5. Query Languages, Compilation & Operations
  category: Cypher Mutations
  priority: MUST
  type: constraint
  description: Labels in write operations must already exist in the governed semantic label map. New labels
    cannot be created via Cypher write clauses — only pre-registered tables are writable.
  use_case: Restricting writes to pre-registered tables ensures schema integrity and prevents untracked
    table creation.
  code:
  - provisa/cypher/write_translator.py
  tests:
  - tests/e2e/test_mutations.py
  - tests/unit/test_cypher_label_map.py
  - tests/unit/test_cypher_translator.py
  - tests/unit/test_sql_to_cypher.py
  integration_test: not_required
  integration_test_reason: Pre-registered table requirement is compile-time validation logic verifiable
    through unit tests.
  stakeholders:
  - data-engineer
  - compliance
  competitive_position:
    status: gap
    rationale: 'In-progress: restricting Cypher writes to pre-registered tables is not yet implemented.
      Until complete this is a gap in a planned feature, though competitors also lack this (Cypher writes
      over federated relational data).'
- id: REQ-663
  status: complete
  since: 2026-07
  group: 5. Query Languages, Compilation & Operations
  category: Cypher Mutations
  priority: MUST
  type: constraint
  description: All Cypher write operations (CREATE, SET, DELETE) are gated on `writable_by` ACL defined
    on the target table's NodeMeta. If a role lacks write permission, the query is rejected at compile
    time. The check applies uniformly to CREATE, SET, and DELETE.
  use_case: Consistent write access control across all mutation types prevents unauthorized writes to
    sensitive tables.
  code:
  - provisa/cypher/write_translator.py
  - provisa/api/rest/cypher_router.py
  - provisa/api/data/endpoint.py
  - provisa/compiler/stage2.py
  tests:
  - tests/e2e/test_mutations.py
  - tests/unit/test_cypher_mutations.py
  - tests/unit/test_cypher_translator.py
  - tests/unit/test_sql_to_cypher.py
  integration_test: not_required
  integration_test_reason: Write ACL enforcement is compile-time validation based on NodeMeta declarations.
  stakeholders:
  - compliance
  - data-engineer
  competitive_position:
    status: ahead
    rationale: Cypher writes are gated on the same writable_by column ACL as GraphQL/SQL mutations, enforced
      before execution — no competitor governs Cypher writes over federated relational data.
- id: REQ-664
  status: complete
  since: 2026-07
  group: 5. Query Languages, Compilation & Operations
  category: Cypher Mutations
  priority: MUST
  type: constraint
  description: A source connector must explicitly support DML (INSERT, UPDATE, DELETE) to be writable.
    Read-only connectors (e.g., Trino-federated, iceberg catalog without Delta connector) reject write
    operations at translation time with a clear error message.
  use_case: Restricting writes to DML-capable sources prevents silent failures on read-only systems.
  code:
  - provisa/cypher/write_translator.py
  - provisa/core/models.py
  tests:
  - tests/e2e/test_mutations.py
  - tests/unit/test_cypher_translator.py
  - tests/unit/test_sql_to_cypher.py
  integration_test: not_required
  integration_test_reason: DML capability checking is compile-time validation based on connector metadata.
  stakeholders:
  - compliance
  - ops
  competitive_position:
    status: gap
    rationale: 'In-progress: DML-capability checking to gate Cypher writes on connector support is not
      yet implemented.'
- id: REQ-665
  status: complete
  since: 2026-07
  group: 5. Query Languages, Compilation & Operations
  category: Cypher Mutations
  priority: MUST
  type: constraint
  description: Relationships in Cypher cannot be written — they are derived from foreign-key joins in
    the semantic layer, not stored edges. CREATE, SET, and DELETE cannot target relationship entities.
    Attempting to write a relationship is a hard error at translation time.
  use_case: Relationship immutability prevents accidental FK manipulation and keeps relationship metadata
    consistent with the declared join paths.
  code:
  - provisa/cypher/write_translator.py
  - provisa/cypher/label_map.py
  tests:
  - tests/e2e/test_mutations.py
  - tests/unit/test_cypher_mutations.py
  - tests/unit/test_cypher_translator.py
  - tests/unit/test_sql_to_cypher.py
  integration_test: not_required
  integration_test_reason: Relationship immutability is a compile-time constraint verifiable through unit
    tests.
  stakeholders:
  - data-engineer
  - compliance
  competitive_position:
    status: gap
    rationale: 'In-progress: relationship immutability enforcement in Cypher write translation is not
      yet implemented.'
- id: REQ-666
  status: complete
  since: 2026-07
  group: 5. Query Languages, Compilation & Operations
  category: Cypher Mutations
  priority: SHOULD
  type: behavioral
  description: '`CREATE (n:Label {props})` is translated to `INSERT INTO catalog.schema.table (columns)
    VALUES (values)`. Property names in the Cypher object are mapped to table columns via domain-prefix
    stripping and alias resolution. Type coercion aligns Cypher scalar values with target column types.'
  use_case: Property-to-column mapping with type coercion enables straightforward graph-idiomatic CREATE
    statements without explicit SQL casting.
  code:
  - provisa/cypher/write_translator.py
  - provisa/cypher/select_builder.py
  tests:
  - tests/e2e/test_mutations.py
  - tests/unit/test_cypher_label_map.py
  - tests/unit/test_cypher_translator.py
  - tests/unit/test_sql_to_cypher.py
  - tests/integration/test_cypher_endpoint.py
  scenario: 'Given a Cypher CREATE statement with a registered label and scalar properties

    When the WriteTranslator processes the statement

    Then the output is an INSERT INTO SQL statement with correct column-value pairs

    And type coercion is applied to align Cypher scalar types with column types

    '
  integration_test: required
  integration_test_reason: CREATE translation to INSERT with property-to-column mapping requires real
    database execution.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - data-engineer
  - app-developer
  competitive_position:
    status: gap
    rationale: 'In-progress: CREATE→INSERT translation with property-to-column mapping is not yet implemented.'
- id: REQ-667
  status: complete
  since: 2026-07
  group: 5. Query Languages, Compilation & Operations
  category: Cypher Mutations
  priority: SHOULD
  type: behavioral
  description: '`MATCH (n:Label) WHERE ... DELETE n` is translated to `DELETE FROM catalog.schema.table
    WHERE ...`. The WHERE clause is reused from the existing MATCH translator; pattern conditions must
    compile to standard SQL WHERE predicates before deletion.'
  use_case: WHERE clause reuse avoids duplicating filter translation logic between read and write paths.
  code:
  - provisa/cypher/write_translator.py
  - provisa/cypher/select_builder.py
  tests:
  - tests/e2e/test_mutations.py
  - tests/unit/test_cypher_label_map.py
  - tests/unit/test_cypher_translator.py
  - tests/unit/test_sql_to_cypher.py
  - tests/integration/test_cypher_endpoint.py
  scenario: 'Given a Cypher MATCH-DELETE statement targeting a registered label

    When the WriteTranslator processes the statement

    Then the output is a DELETE FROM SQL statement with the WHERE clause from the MATCH pattern

    '
  integration_test: required
  integration_test_reason: DELETE translation to SQL requires real database execution to verify WHERE
    clause application.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - data-engineer
  - app-developer
  competitive_position:
    status: gap
    rationale: 'In-progress: MATCH-DELETE→SQL DELETE translation is not yet implemented.'
- id: REQ-668
  status: complete
  since: 2026-07
  group: 5. Query Languages, Compilation & Operations
  category: Cypher Mutations
  priority: SHOULD
  type: behavioral
  description: '`MATCH (n:Label) WHERE ... SET n.prop = val` is translated to `UPDATE catalog.schema.table
    SET column = value WHERE ...`. Property-to-column mapping applies domain-prefix stripping; assignment
    expressions must be valid SQL expressions. Multiple SET clauses compose as comma-separated column
    updates.'
  use_case: Property-to-column mapping with multi-assignment composition lets graph clients issue idiomatic
    UPDATE patterns.
  code:
  - provisa/cypher/write_translator.py
  - provisa/cypher/select_builder.py
  tests:
  - tests/e2e/test_mutations.py
  - tests/unit/test_cypher_label_map.py
  - tests/unit/test_cypher_translator.py
  - tests/unit/test_sql_to_cypher.py
  - tests/integration/test_cypher_endpoint.py
  scenario: 'Given a Cypher MATCH-SET statement with multiple property assignments

    When the WriteTranslator processes the statement

    Then the output is an UPDATE SQL statement with comma-separated SET clauses

    And domain-prefix stripping maps Cypher property names to physical column names

    '
  integration_test: required
  integration_test_reason: UPDATE translation with property-to-column mapping requires real database execution.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - data-engineer
  - app-developer
  competitive_position:
    status: gap
    rationale: 'In-progress: MATCH-SET→SQL UPDATE translation with multi-assignment composition is not
      yet implemented.'
- id: REQ-669
  status: complete
  since: 2026-07
  group: 5. Query Languages, Compilation & Operations
  category: Cypher Mutations
  priority: MUST
  type: structural
  description: Write operations use a separate parser entry point or translation flag to avoid breaking
    the read-only query path. Existing read-only parser must remain unchanged. The WriteTranslator class
    encapsulates CREATE, SET, and DELETE translation logic separately from QueryTranslator.
  use_case: Separate write translation path prevents mutation syntax from breaking existing read-only
    query parsing and execution.
  code:
  - provisa/cypher/write_translator.py
  tests:
  - tests/e2e/test_mutations.py
  - tests/unit/test_cypher_path_translator.py
  - tests/unit/test_cypher_translator.py
  - tests/unit/test_sql_to_cypher.py
  integration_test: not_required
  integration_test_reason: Separate write parser entry point is implementation architecture verifiable
    through unit tests.
  stakeholders:
  - app-developer
  competitive_position:
    status: ahead
    rationale: Cypher write mutation support (CREATE/MERGE/SET/DELETE) compiled to SQL via a dedicated
      WriteTranslator is unique — no competitor compiles Cypher mutations to federated SQL.
- id: REQ-670
  status: complete
  since: 2026-07
  group: 5. Query Languages, Compilation & Operations
  category: Cypher Mutations
  priority: SHOULD
  type: behavioral
  description: Cypher write endpoints return the number of rows affected (rows inserted for CREATE, rows
    updated for SET, rows deleted for DELETE). The response includes an `affected_rows` field in the JSON
    response body.
  use_case: Affected-row counts let clients validate mutation success and detect silent failures.
  code:
  - provisa/api/rest/cypher_router.py
  tests:
  - tests/e2e/test_mutations.py
  - tests/unit/test_cypher_translator.py
  - tests/unit/test_sql_to_cypher.py
  - tests/integration/test_cypher_endpoint.py
  - provisa-ui/e2e/cypher-write-affected-rows.spec.ts
  scenario: 'Given a successful Cypher CREATE statement executed via the write endpoint

    When the response is returned to the client

    Then the JSON body includes an affected_rows field with the count of inserted rows

    '
  integration_test: required
  integration_test_reason: Affected-row counts require real database execution to verify mutation impact.
  e2e: true
  e2e_reason: The affected_rows field in the Cypher write endpoint JSON response requires a real running
    server executing actual DML against a database to verify the row count is correct.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - app-developer
  - data-engineer
  competitive_position:
    status: gap
    rationale: 'In-progress: affected_rows response field for Cypher write operations is not yet implemented.'
- id: REQ-671
  status: complete
  since: 2026-07
  group: 5. Query Languages, Compilation & Operations
  category: Cypher Mutations
  priority: MUST
  type: constraint
  description: DETACH DELETE (cascading delete across derived relationships) is not supported. Attempting
    DETACH DELETE is a hard error at parse or translation time. Clients must explicitly DELETE each table
    in the correct dependency order.
  use_case: Explicit cascade prevention avoids accidental data deletion across unintended table relationships.
  code:
  - provisa/cypher/write_translator.py
  - provisa/cypher/parser.py
  tests:
  - tests/e2e/test_mutations.py
  - tests/unit/test_cypher_translator.py
  - tests/unit/test_sql_to_cypher.py
  integration_test: not_required
  integration_test_reason: DETACH DELETE rejection is compile-time validation verifiable through unit
    tests.
  stakeholders:
  - compliance
  - data-engineer
  competitive_position:
    status: neutral
    rationale: ''
- id: REQ-027
  status: complete
  group: 6. Execution, Routing, Caching & Performance
  category: Execution & Routing
  priority: MUST
  type: behavioral
  description: 'Single-source queries route to direct RDBMS connection; SQLGlot transpiles to target dialect.
    Target: sub-100ms to low hundreds of ms.'
  use_case: Single-source direct routing delivers sub-100ms query latency for the majority of requests.
  code:
  - provisa/executor/
  - provisa/transpiler/
  tests:
  - tests/e2e/test_routing.py
  - tests/integration/test_direct_exec.py
  - tests/integration/test_execution_routing.py
  - tests/unit/test_routing.py
  - provisa-ui/e2e/execution-routing.spec.ts
  since: 2026-01
  scenario: 'Given a query targeting a single registered RDBMS source

    When the query is executed

    Then it routes directly to the RDBMS connection and SQLGlot transpiles to the target dialect

    '
  integration_test: required
  integration_test_reason: Latency targets and query routing to actual RDBMS backends require end-to-end
    verification with real network transport.
  e2e: true
  e2e_reason: Query routing to a real RDBMS over the network and sub-100ms latency targets cannot be validated
    without a live server accepting HTTP/GraphQL requests and a real backend connection.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - app-developer
  - ops
  - data-engineer
  competitive_position:
    status: parity
    rationale: Direct single-source routing with dialect transpilation is table stakes for any federation
      engine. Trino, Denodo, and Dremio all route single-source queries to native connectors with sub-100ms
      targets.
- id: REQ-028
  status: complete
  group: 6. Execution, Routing, Caching & Performance
  category: Execution & Routing
  priority: MUST
  type: behavioral
  description: 'Cross-source queries route to Trino; SQLGlot transpiles to Trino SQL. Target: 300-500ms.'
  use_case: Cross-source Trino routing enables federated queries across databases within a 300–500ms target.
  code:
  - provisa/executor/
  - provisa/transpiler/
  tests:
  - tests/unit/test_routing.py
  - tests/e2e/test_routing.py
  - tests/integration/test_execution_routing.py
  - provisa-ui/e2e/execution-routing.spec.ts
  since: 2026-01
  scenario: 'Given a query joining tables from multiple registered sources

    When the query is executed

    Then it routes to Trino and SQLGlot transpiles to Trino SQL within 300–500ms

    '
  integration_test: required
  integration_test_reason: Cross-source queries require real Trino federation engine and network round-trips
    to verify 300-500ms target.
  e2e: true
  e2e_reason: Cross-source Trino routing with a 300-500ms latency target requires a running server accepting
    requests and a live Trino federation engine — not mockable in integration tests alone.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - app-developer
  - ops
  - data-engineer
  competitive_position:
    status: parity
    rationale: Cross-source routing through a distributed query engine (Trino) is the standard architecture
      for open-source federation; Trino/Presto pioneered this and Dremio uses a similar Arrow-based planner.
  related_reqs:
  - REQ-825
- id: REQ-029
  status: complete
  group: 6. Execution, Routing, Caching & Performance
  category: Execution & Routing
  priority: MUST
  type: behavioral
  description: Large results above threshold redirect to blob storage with presigned URL and TTL.
  use_case: Large-result redirect to blob storage prevents memory exhaustion on the Provisa server for
    big datasets.
  code:
  - provisa/executor/
  - provisa/transpiler/
  tests:
  - tests/e2e/test_large_result.py
  - tests/e2e/test_routing.py
  - tests/integration/test_direct_exec.py
  - tests/integration/test_execution_routing.py
  - tests/unit/test_redirect_s3.py
  - tests/unit/test_routing.py
  - provisa-ui/e2e/execution-routing.spec.ts
  since: 2026-01
  scenario: 'Given a query whose result set exceeds the configured size threshold

    When the query completes

    Then the result is stored in blob storage and the client receives a presigned URL with TTL

    '
  integration_test: required
  integration_test_reason: Large-result redirect with presigned URLs and blob storage requires real HTTP
    responses and cloud storage interaction.
  e2e: true
  e2e_reason: Large-result blob storage redirect with presigned URL delivery requires the full HTTP response
    path from a real running server to a real cloud storage backend.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - app-developer
  - ops
  competitive_position:
    status: parity
    rationale: Large-result offload to blob storage with presigned URLs is a standard pattern; Dremio
      and Snowflake both support result spilling and staged offload for oversized result sets.
- id: REQ-030
  status: complete
  group: 6. Execution, Routing, Caching & Performance
  category: Execution & Routing
  priority: MAY
  type: structural
  description: Steward routing override configurable per table/view for edge cases where default routing
    is inappropriate.
  use_case: Per-table steward routing overrides handle edge cases where the default routing heuristic
    is suboptimal.
  code:
  - provisa/executor/
  - provisa/transpiler/
  tests:
  - tests/unit/test_routing.py
  - tests/e2e/test_routing.py
  - tests/integration/test_execution_routing.py
  since: 2026-01
  integration_test: required
  integration_test_reason: Per-table steward routing overrides require end-to-end verification with actual
    routing decisions.
  e2e: true
  e2e_reason: Per-table steward routing overrides must be verified against actual routing decisions made
    by a live server — the override effect is only observable end-to-end.
  stakeholders:
  - data-engineer
  - app-developer
  competitive_position:
    status: parity
    rationale: Per-table routing overrides are available in Denodo as custom wrappers and in Dremio via
      reflection policies; this is a standard operator escape hatch.
- id: REQ-031
  status: complete
  group: 6. Execution, Routing, Caching & Performance
  category: Execution & Routing
  priority: MUST
  type: constraint
  description: 'DB mutations ALWAYS route to direct RDBMS connection — Trino never involved in mutation
    execution. Webhook mutations are NOT DB mutations: they reference an external HTTP endpoint that presumably
    changes underlying data. Provisa cannot verify whether anything changed, so this is the steward''s
    responsibility to define (see REQ-209).'
  use_case: DB mutations always bypass Trino, ensuring writes are atomic and target the correct source
    directly.
  code:
  - provisa/executor/
  - provisa/transpiler/
  tests:
  - tests/e2e/test_routing.py
  - tests/integration/test_direct_exec.py
  - tests/integration/test_execution_routing.py
  - tests/unit/test_routing.py
  - provisa-ui/e2e/execution-routing.spec.ts
  since: 2026-01
  integration_test: required
  integration_test_reason: DB mutation routing isolation and transaction atomicity require real RDBMS
    connections and write verification.
  e2e: true
  e2e_reason: Confirming DB mutations always bypass Trino and reach the RDBMS directly requires issuing
    a mutation through the live API and verifying it is received by the RDBMS, not Trino.
  stakeholders:
  - data-engineer
  - compliance
  competitive_position:
    status: parity
    rationale: Routing writes exclusively to direct RDBMS connections (bypassing the federation engine)
      is the standard safe approach; Denodo and Trino both prohibit mutation through the federated query
      layer.
- id: REQ-052
  status: complete
  group: 6. Execution, Routing, Caching & Performance
  category: Data & Storage
  priority: MUST
  type: structural
  description: Each registered RDBMS source maintains warm connection pool; min pool size configurable
    per source.
  use_case: Warm connection pools eliminate per-request TCP handshake latency for frequently queried RDBMS
    sources.
  code:
  - provisa/executor/
  - provisa/core/
  tests:
  - tests/unit/test_connection_pool.py
  - tests/integration/test_pool.py
  since: 2026-01
  scenario: 'Given a registered RDBMS source with a configured min pool size

    When the server starts

    Then a warm connection pool is maintained with at least the configured minimum connections

    '
  integration_test: required
  integration_test_reason: Warm connection pool sizing and per-source configuration require real database
    connections to measure pool behavior.
  stakeholders:
  - ops
  - data-engineer
  competitive_position:
    status: parity
    rationale: Warm connection pools with configurable minimums are standard in any production data gateway;
      Hasura, Denodo, and Dremio all maintain persistent connector pools.
- id: REQ-053
  status: complete
  group: 6. Execution, Routing, Caching & Performance
  category: Data & Storage
  priority: MAY
  type: structural
  description: PgBouncer is the documented per-PostgreSQL-source opt-in via `use_pgbouncer` (default off);
    it is NOT forced on, because a default-on would require a running PgBouncer for every PG source. Default
    is direct asyncpg pooling (REQ-052).
  use_case: Optional PgBouncer per-source avoids mandatory sidecar infrastructure while offering connection
    pooling where needed.
  code:
  - provisa/executor/
  - provisa/core/
  tests:
  - tests/integration/test_direct_exec.py
  - tests/integration/test_pool.py
  - tests/unit/test_connection_pool.py
  since: 2026-01
  scenario: 'Given a PostgreSQL source with use_pgbouncer: true in its config

    When the source is registered

    Then Provisa routes connections through PgBouncer rather than direct asyncpg pooling

    '
  integration_test: deferred
  integration_test_reason: PgBouncer integration requires optional external service not guaranteed in
    CI environment.
  stakeholders:
  - ops
  - data-engineer
  competitive_position:
    status: parity
    rationale: PgBouncer opt-in per source is standard practice for PostgreSQL connection pooling.
  related_reqs:
  - REQ-828
- id: REQ-054
  status: complete
  group: 6. Execution, Routing, Caching & Performance
  category: Data & Storage
  priority: MUST
  type: structural
  description: Trino read path maintains single persistent connection to coordinator.
  use_case: Single persistent Trino coordinator connection avoids repeated authentication and session
    setup overhead.
  code:
  - provisa/executor/
  - provisa/core/
  tests:
  - tests/unit/test_connection_pool.py
  - tests/integration/test_pool.py
  since: 2026-01
  scenario: 'Given a running Provisa server with Trino configured

    When multiple read queries are routed to Trino

    Then all queries share a single persistent connection to the Trino coordinator

    '
  integration_test: required
  integration_test_reason: Persistent Trino coordinator connection requires real federation engine and
    network state verification.
  stakeholders:
  - ops
  - data-engineer
  competitive_position:
    status: neutral
    rationale: ''
- id: REQ-230
  status: complete
  group: 6. Execution, Routing, Caching & Performance
  category: Hot Tables (Redis-Cached Lookups)
  priority: MUST
  type: behavioral
  description: 'Hot tables stored as a single JSON blob in Redis and injected as a VALUES CTE; column
    governance (RLS/masking/visibility) is NOT applied at storage time but by Stage-2 apply_governance
    over the governed SQL that wraps the CTE (pipeline order: governance -> cache/CTE -> route). The per-row
    PK-hash/sorted-set storage in the spec was judged unnecessary because hot tables are tiny inline tables.'
  use_case: Centralised JSON blob storage with governance applied at query time simplifies cache maintenance
    and ensures all governance rules are enforced on hot data.
  code:
  - provisa/cache/
  tests:
  - tests/integration/test_cache_store.py
  - tests/integration/test_hot_tables_real.py
  - tests/unit/test_apq_cache.py
  - tests/unit/test_cache_key.py
  - tests/unit/test_cache_store.py
  - tests/unit/test_hot_tables.py
  - tests/unit/test_mask_inject.py
  since: 2026-01
  scenario: 'Given a table designated as hot

    When a query references that table

    Then the cached JSON blob is injected as a VALUES CTE and governance is applied by Stage-2 at

    query time

    '
  integration_test: required
  integration_test_reason: Hot table caching with governance injection requires real Redis storage and
    governance rule application in query compilation pipeline.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - app-developer
  - data-engineer
  competitive_position:
    status: ahead
    rationale: Injecting small lookup tables as VALUES CTEs at compile time — with governance applied
      at query time over the CTE — is not a native feature of Trino, Hasura, or Denodo. Competitors either
      scan the lookup table at execution time or require manual denormalization, incurring cross-source
      round-trips that Provisa eliminates.
  related_reqs:
  - REQ-826
- id: REQ-231
  status: complete
  group: 6. Execution, Routing, Caching & Performance
  category: Hot Tables (Redis-Cached Lookups)
  priority: MUST
  type: behavioral
  description: 'Hot table refresh follows MV pattern: TTL-based via `refresh_interval` (default: `materialized_views.default_ttl`),
    background refresh loop, stale fallback to live query. Mutations to the source table trigger immediate
    invalidation + async reload.'
  use_case: TTL-based hot table refresh keeps cached lookup data current without manual cache invalidation.
  code:
  - provisa/cache/
  tests:
  - tests/integration/test_hot_tables_real.py
  - tests/unit/test_apq_cache.py
  - tests/unit/test_cache_key.py
  - tests/unit/test_hot_tables.py
  - tests/unit/test_hot_tables_integration.py
  since: 2026-01
  scenario: 'Given a hot table with a configured refresh_interval

    When the TTL expires or a mutation occurs on the source table

    Then the cache is invalidated and asynchronously reloaded, falling back to live query if stale

    '
  integration_test: required
  integration_test_reason: TTL-based refresh, mutation-triggered invalidation, and stale fallback require
    real Redis and source database interaction.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - ops
  - data-engineer
  competitive_position:
    status: parity
    rationale: TTL-based refresh with stale fallback and mutation-triggered invalidation is available
      in Hasura's caching layer and Dremio's reflection refresh; the pattern is industry-standard for
      materialized caches.
  related_reqs:
  - REQ-826
- id: REQ-232
  status: complete
  group: 6. Execution, Routing, Caching & Performance
  category: Hot Tables (Redis-Cached Lookups)
  priority: SHOULD
  type: behavioral
  description: Hot table JOIN optimization -- when a query joins a hot table (e.g., `orders JOIN countries
    ON country_code`), the compiler injects the hot table data as constants into the SQL via a `VALUES`-based
    CTE. The DB engine sees literal rows, not a table reference. This eliminates the second table scan
    and works across sources (constants travel with the query to Trino/Snowflake/ClickHouse). SQLGlot
    transpiles the VALUES clause per dialect.
  use_case: Hot table JOIN optimisation eliminates a second table scan by injecting lookup data as SQL
    constants.
  code:
  - provisa/cache/
  tests:
  - tests/integration/test_hot_tables_real.py
  - tests/unit/test_apq_cache.py
  - tests/unit/test_cache_key.py
  - tests/unit/test_hot_tables.py
  - tests/unit/test_hot_tables_integration.py
  since: 2026-01
  scenario: 'Given a query that JOINs a hot table

    When the compiler processes the query

    Then the hot table data is injected as a VALUES-based CTE and the DB engine sees no table

    reference

    '
  integration_test: required
  integration_test_reason: Hot table JOIN optimization with VALUES CTE injection requires end-to-end query
    compilation and execution across sources.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - app-developer
  - data-engineer
  competitive_position:
    status: ahead
    rationale: Compiler-injected VALUES-CTE JOIN optimization that eliminates a second table scan and
      works transparently across Trino, Snowflake, and ClickHouse via SQLGlot is not offered by Trino,
      Denodo, or Hasura natively; those platforms always issue a second connector scan for the lookup
      side of a join.
  related_reqs:
  - REQ-826
- id: REQ-233
  status: complete
  group: 6. Execution, Routing, Caching & Performance
  category: Hot Tables (Redis-Cached Lookups)
  priority: MUST
  type: constraint
  description: Hot caching is bounded by both `max_rows` (own config default, falls back to `auto_threshold`)
    and `max_bytes` (default 10MB, measured after JSON serialization) — a wide table within `max_rows`
    but over the byte ceiling is not cached.
  use_case: Dual byte and row limits prevent hot caching of unexpectedly wide tables that would exceed
    Redis memory budgets.
  code:
  - provisa/cache/
  tests:
  - tests/integration/test_hot_tables_real.py
  - tests/unit/test_apq_cache.py
  - tests/unit/test_cache_key.py
  - tests/unit/test_hot_tables.py
  since: 2026-01
  integration_test: required
  integration_test_reason: Dual byte and row limit enforcement requires real Redis storage measurement
    and actual caching behavior verification.
  stakeholders:
  - ops
  - data-engineer
  competitive_position:
    status: ahead
    rationale: Dual-bound hot cache (max_rows AND max_bytes) prevents cache bloat from wide tables — more
      robust than Hasura's unbounded query caching or Redis TTL-only strategies.
  related_reqs:
  - REQ-826
- id: REQ-234
  status: complete
  group: 6. Execution, Routing, Caching & Performance
  category: Materialized View Lifecycle
  priority: SHOULD
  type: behavioral
  description: 'Auto-materialized view storage reclamation -- when a view is removed from config, disabled,
    or its source table is unregistered, the backing MV table is dropped (Trino `DROP TABLE`). Background
    cleanup task runs on config reload and periodically (default: daily). Orphaned MV tables (present
    in target schema but not in MV registry) are flagged and optionally auto-dropped after a grace period.'
  use_case: MV storage reclamation prevents orphaned Iceberg tables accumulating when views are removed
    from config.
  code:
  - provisa/mv/
  tests:
  - tests/integration/test_execution_routing.py
  - tests/unit/test_config_reload.py
  - tests/unit/test_mv_lifecycle.py
  since: 2026-01
  scenario: 'Given a materialized view that is removed from config or whose source is unregistered

    When config is reloaded or the daily cleanup runs

    Then the backing MV table is dropped and any orphaned MV tables are flagged for auto-drop

    '
  integration_test: required
  integration_test_reason: MV storage reclamation via Trino DROP TABLE and orphaned table detection require
    real Iceberg backend and Trino coordinator.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - ops
  - data-engineer
  competitive_position:
    status: parity
    rationale: Automated MV lifecycle (drop on removal, orphan detection, periodic cleanup) is available
      in Dremio via reflection garbage collection and in Snowflake's dynamic table lifecycle; no competitive
      differentiation.
  related_reqs:
  - REQ-826
- id: REQ-235
  status: complete
  group: 6. Execution, Routing, Caching & Performance
  category: Materialized View Lifecycle
  priority: MUST
  type: constraint
  description: 'Auto-materialized aggregate views must have a size guard -- `materialized_views.max_rows`
    (default: 1,000,000). Views whose source query would produce more rows than the limit skip materialization
    and fall back to live execution. Size estimated via `SELECT COUNT(*)` probe before CTAS. Configurable
    per view to override the global default.'
  use_case: MV size guard prevents materialising oversized views that would exhaust storage or exceed
    refresh budgets.
  code:
  - provisa/mv/
  tests:
  - tests/unit/test_mv_lifecycle.py
  - tests/integration/test_execution_routing.py
  since: 2026-01
  integration_test: required
  integration_test_reason: MV size guard enforcement via SELECT COUNT(*) probe and storage decisions require
    real database queries.
  stakeholders:
  - ops
  - data-engineer
  competitive_position:
    status: parity
    rationale: MV size guards (row count probe before CTAS) are standard in Dremio's reflection planner
      and Snowflake dynamic tables; falling back to live execution on oversized results is a common safety
      pattern.
  related_reqs:
  - REQ-826
- id: REQ-236
  status: complete
  group: 6. Execution, Routing, Caching & Performance
  category: Hot Table Auto-Detection
  priority: SHOULD
  type: behavioral
  description: A table is auto-hot if it is the target of a many-to-one relationship OR its row count
    is <= `auto_threshold` (SELECT COUNT(*) at schema build via `detect_hot_tables_by_count`).
  use_case: Auto-detection of small lookups and relationship targets caches them in Redis without explicit
    config.
  code:
  - provisa/cache/
  tests:
  - tests/unit/test_hot_tables.py
  - tests/integration/test_hot_tables_real.py
  since: 2026-01
  scenario: 'Given a table that is the target of a many-to-one relationship or has row count <= auto_threshold

    When schema is built

    Then the table is automatically designated as hot and cached in Redis

    '
  integration_test: required
  integration_test_reason: Auto-hot table detection via row count and relationship analysis requires real
    schema introspection and caching behavior.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - data-engineer
  - ops
  competitive_position:
    status: ahead
    rationale: Automatic hot-table detection via schema relationship analysis (many-to-one targets) combined
      with row-count probing at schema build time is not a built-in feature of Trino, Denodo, or Dremio;
      those require explicit manual designation of cached lookup tables.
  related_reqs:
  - REQ-826
- id: REQ-237
  status: complete
  group: 6. Execution, Routing, Caching & Performance
  category: Hot Table Auto-Detection
  priority: MAY
  type: behavioral
  description: 'Auto-hot tables can be opted out via `hot: false` on the table config. Explicit `hot:
    true` overrides the auto-detection criteria (forces caching regardless of row count or relationship
    status). Auto-detection runs on every schema rebuild; tables that grow beyond the threshold are automatically
    evicted from Redis on next rebuild.'
  use_case: Explicit hot:false opt-out lets stewards prevent specific tables from being auto-cached in
    Redis.
  code:
  - provisa/cache/
  tests:
  - tests/unit/test_hot_tables.py
  - tests/integration/test_hot_tables_real.py
  since: 2026-01
  scenario: 'Given a table with hot: false in its config

    When schema is rebuilt

    Then the table is not cached in Redis even if it meets auto-detection criteria

    '
  integration_test: required
  integration_test_reason: Hot opt-out and auto-detection with per-rebuild eviction require real Redis
    state management and schema rebuild cycles.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - data-engineer
  - ops
  competitive_position:
    status: ahead
    rationale: 'Auto-hot detection with explicit opt-out (hot: false) and override (hot: true) gives stewards
      declarative cache control beyond what Hasura, Dremio, or Trino offer natively.'
  related_reqs:
  - REQ-826
- id: REQ-238
  status: complete
  group: 6. Execution, Routing, Caching & Performance
  category: Warm Tables (Replica Profile — Low-Latency Placement)
  priority: SHOULD
  type: behavioral
  description: Warm tables -- frequently queried RDBMS tables materialized into a low-latency, engine-reachable
    location. In materialization_store terms (REQ-844) this is the `warm` profile of the REPLICA role
    — an engine-relative low-latency PLACEMENT of a reachable source (distinct from `reactive`, which
    lands an unreachable source). The mechanism below is the current Trino realization of that placement
    rather than its definition — it materializes into the Iceberg results catalog so Trino's built-in
    file system cache (`fs.cache.enabled=true`) caches the Parquet files on local SSD; other engines realize
    the low-latency tier differently. Provides ~10-50ms reads vs 100ms+ network round-trip to remote source.
    Same TTL/refresh pattern as MVs.
  use_case: Warm tables on local SSD reduce RDBMS round-trip latency from 100ms+ to ~10-50ms for frequent
    queries.
  code:
  - provisa/mv/
  tests:
  - tests/unit/test_apq_cache.py
  - tests/unit/test_cache_key.py
  - tests/unit/test_catalog_cache.py
  - tests/unit/test_warm_tables.py
  since: 2026-01
  scenario: 'Given a table materialized into the Iceberg results catalog with Trino file cache enabled

    When a query targets that table

    Then Trino serves the result from local SSD Parquet cache at ~10-50ms latency

    '
  integration_test: deferred
  integration_test_reason: Warm tables on SSD cache require Trino file system cache with actual SSD hardware
    and network latency measurement.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - ops
  - data-engineer
  competitive_position:
    status: parity
    rationale: Materializing frequently queried tables into a local-SSD-backed columnar store (Iceberg/Parquet)
      for low-latency reads is the core value proposition of Dremio's reflections and Snowflake's result
      cache; Provisa is at parity here.
  related_reqs:
  - REQ-826
  - REQ-825
  - REQ-844
- id: REQ-239
  status: complete
  group: 6. Execution, Routing, Caching & Performance
  category: Warm Tables (Replica Profile — Low-Latency Placement)
  priority: SHOULD
  type: behavioral
  description: 'Warm table auto-promotion -- track query frequency per table (increment counter on each
    compiled query). Tables exceeding `warm_tables.query_threshold` (default: 100 queries per refresh
    interval) are auto-materialized into Iceberg. Tables falling below the threshold are demoted (backing
    table dropped) on next refresh cycle.'
  use_case: Auto-promotion of frequently queried tables to warm tier improves performance without manual
    configuration.
  code:
  - provisa/mv/
  tests:
  - tests/unit/test_apq_cache.py
  - tests/unit/test_cache_key.py
  - tests/unit/test_warm_tables.py
  - tests/integration/test_execution_routing.py
  since: 2026-01
  scenario: 'Given a table whose query count exceeds warm_tables.query_threshold within a refresh interval

    When the promotion check runs

    Then the table is auto-materialized into Iceberg; tables falling below threshold are demoted

    '
  integration_test: required
  integration_test_reason: Query frequency tracking and auto-promotion require real query compilation
    tracking and Iceberg materialization.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - ops
  - data-engineer
  competitive_position:
    status: ahead
    rationale: Automatic frequency-based promotion and demotion between live and warm-materialized tiers
      — without manual operator action — is not offered by Trino or Denodo; Dremio's acceleration recommendations
      are advisory, not automatic promotion/demotion.
  related_reqs:
  - REQ-826
  - REQ-825
- id: REQ-240
  status: complete
  group: 6. Execution, Routing, Caching & Performance
  category: Warm Tables (Replica Profile — Low-Latency Placement)
  priority: MAY
  type: structural
  description: 'Warm table config: `warm: true` to force, `warm: false` to opt out. Global `warm_tables.query_threshold`,
    `warm_tables.max_rows` (default: 10,000,000), `warm_tables.refresh_interval` (default: same as `materialized_views.default_ttl`).
    Trino file cache config (`fs.cache.enabled`, `fs.cache.directories`, `fs.cache.max-sizes`) managed
    in Trino catalog properties.'
  use_case: Warm table config guards (max_rows, threshold) prevent promotion of tables too large for local
    SSD cache.
  code:
  - provisa/mv/
  tests:
  - tests/integration/test_direct_exec.py
  - tests/unit/test_apq_cache.py
  - tests/unit/test_cache_key.py
  - tests/unit/test_catalog_cache.py
  - tests/unit/test_warm_tables.py
  since: 2026-01
  integration_test: required
  integration_test_reason: Warm table config limits and Trino file cache properties require real cache
    configuration and materialization verification.
  stakeholders:
  - ops
  - data-engineer
  competitive_position:
    status: ahead
    rationale: Warm tables via Trino file cache with row/refresh limits and opt-in/opt-out per table is
      a governed caching tier not present in Hasura or standard Trino setups.
  related_reqs:
  - REQ-826
  - REQ-825
- id: REQ-241
  status: complete
  group: 6. Execution, Routing, Caching & Performance
  category: Warm Tables (Replica Profile — Low-Latency Placement)
  priority: MUST
  type: constraint
  description: A table lives in at most one cache tier — hot wins over warm. `WarmTableManager.check_promotions`
    skips any table the hot tier manages (`HotTableManager.managed_tables()`).
  use_case: Strict tier exclusivity prevents hot tables from being accidentally promoted to warm, ensuring
    optimal latency.
  code:
  - provisa/mv/
  tests:
  - tests/unit/test_apq_cache.py
  - tests/unit/test_cache_key.py
  - tests/unit/test_warm_tables.py
  - tests/integration/test_execution_routing.py
  since: 2026-01
  integration_test: required
  integration_test_reason: Tier exclusivity (hot vs warm) requires real cache manager state and tier skipping
    logic verification.
  stakeholders:
  - ops
  - data-engineer
  competitive_position:
    status: ahead
    rationale: Strict tier exclusivity enforced at the code level (warm manager skips hot-managed tables)
      prevents subtle double-caching bugs; competitors with multi-tier caches (Dremio reflections + result
      cache) do not enforce this constraint programmatically.
  related_reqs:
  - REQ-826
  - REQ-825
- id: REQ-275
  status: complete
  group: 6. Execution, Routing, Caching & Performance
  category: Federation Performance
  priority: SHOULD
  type: behavioral
  description: On source registration, Provisa runs `ANALYZE` against the registered source's tables (where
    the connector supports it) to prime the federation engine's cost-based optimizer with baseline row
    counts and column statistics.
  use_case: Running ANALYZE on registration primes the federation cost-based optimizer so join plans are
    efficient immediately.
  code:
  - provisa/executor/
  - provisa/compiler/
  tests:
  - tests/integration/test_execution_routing.py
  - tests/integration/test_federation_integration.py
  - tests/unit/test_federation.py
  - tests/unit/test_federation_hints.py
  - tests/unit/test_federation_v2_extended.py
  since: 2026-01
  scenario: 'Given a source being registered with a connector that supports ANALYZE

    When registration completes

    Then Provisa runs ANALYZE on the source''s tables to prime the cost-based optimizer

    '
  integration_test: required
  integration_test_reason: Running ANALYZE on source registration requires real database connectors and
    federation cost-based optimizer interaction.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - ops
  - data-engineer
  competitive_position:
    status: parity
    rationale: Running ANALYZE on source registration to prime the cost-based optimizer is standard Trino/Presto
      practice; Dremio performs similar statistics collection on connector registration.
- id: REQ-276
  status: complete
  group: 6. Execution, Routing, Caching & Performance
  category: Federation Performance
  priority: MAY
  type: behavioral
  description: Admin API exposes a "Refresh Statistics" mutation per source that re-runs `ANALYZE` on
    demand. Useful for volatile sources where baseline stats have aged.
  use_case: On-demand Refresh Statistics mutation lets stewards refresh cost stats for volatile sources
    without a restart.
  code:
  - provisa/executor/
  - provisa/compiler/
  tests:
  - tests/integration/test_execution_routing.py
  - tests/integration/test_federation_integration.py
  - tests/unit/test_federation.py
  - tests/unit/test_federation_hints.py
  - tests/unit/test_federation_v2_extended.py
  since: 2026-01
  scenario: 'Given a registered source with stale statistics

    When a steward calls the Refresh Statistics mutation

    Then ANALYZE is re-run on demand for that source

    '
  integration_test: required
  integration_test_reason: Admin API mutation for on-demand ANALYZE requires HTTP API and real database
    statistics refresh verification.
  e2e: true
  e2e_reason: The Admin API Refresh Statistics mutation must be called over the network against a running
    server, and the ANALYZE effect on a real database must be verified end-to-end.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - ops
  - data-engineer
  competitive_position:
    status: parity
    rationale: On-demand statistics refresh via an admin API is available in Dremio (Refresh Reflection)
      and Snowflake (SYSTEM$REFRESH_TABLE_STATS); this is expected enterprise functionality.
- id: REQ-277
  status: complete
  group: 6. Execution, Routing, Caching & Performance
  category: Federation Performance
  priority: MAY
  type: behavioral
  description: 'Per-query and per-table/view session property overrides: steward or developer can attach
    named session hints (`join_distribution_type`, `join_max_broadcast_table_size`, `join_reordering_strategy`).
    Provisa injects matching `SET SESSION` statements before execution.'
  use_case: Per-query session property hints let stewards tune federation join strategies for known expensive
    queries.
  code:
  - provisa/executor/
  - provisa/compiler/
  tests:
  - tests/integration/test_execution_routing.py
  - tests/integration/test_federation_integration.py
  - tests/unit/test_federation.py
  - tests/unit/test_federation_hints.py
  - tests/unit/test_federation_v2_extended.py
  since: 2026-01
  scenario: 'Given a query or table/view with session hints configured

    When the query is executed via the federation engine

    Then Provisa injects the corresponding SET SESSION statements before execution

    '
  integration_test: required
  integration_test_reason: Per-query session property overrides require real Trino federation engine execution
    with SET SESSION statement injection.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - data-engineer
  - app-developer
  competitive_position:
    status: parity
    rationale: Per-query session property hints for join strategy are native Trino functionality; exposing
      them through Provisa config does not add competitive differentiation beyond what Trino already provides.
- id: REQ-278
  status: complete
  group: 6. Execution, Routing, Caching & Performance
  category: Federation Performance
  priority: MAY
  type: structural
  description: 'Source-level default session properties: each registered source config accepts a `federation_hints:`
    block. Properties in that block are injected for any query that touches that source. Per-query hints
    override source-level defaults.'
  use_case: Source-level federation hints apply performance tuning to all queries touching a source without
    per-query config.
  code:
  - provisa/executor/
  - provisa/compiler/
  tests:
  - tests/integration/test_execution_routing.py
  - tests/integration/test_federation_integration.py
  - tests/unit/test_federation.py
  - tests/unit/test_federation_hints.py
  - tests/unit/test_federation_v2_extended.py
  since: 2026-01
  integration_test: required
  integration_test_reason: Source-level default session property injection requires real query execution
    with Trino federation engine.
  stakeholders:
  - data-engineer
  - ops
  competitive_position:
    status: parity
    rationale: Per-source Trino session properties via federation_hints are available in Trino's native
      session property mechanism.
- id: REQ-279
  status: complete
  group: 6. Execution, Routing, Caching & Performance
  category: Federation Performance
  priority: MAY
  type: behavioral
  description: 'Provisa-branded comment hint syntax `/*+ hint */` in query text. Supported hints: `BROADCAST(<table>)`,
    `NO_REORDER`, `BROADCAST_SIZE(<size>)`. Provisa parser strips the comment before forwarding SQL to
    the federation engine and translates to the equivalent session properties. The federation engine never
    sees the comment.'
  use_case: Provisa-branded hint syntax decouples query authors from Trino session property names and
    future engine changes.
  code:
  - provisa/executor/
  - provisa/compiler/
  tests:
  - tests/integration/test_execution_routing.py
  - tests/integration/test_federation_integration.py
  - tests/unit/test_federation.py
  - tests/unit/test_federation_hints.py
  - tests/unit/test_federation_v2_extended.py
  since: 2026-01
  scenario: 'Given a query containing a /*+ BROADCAST(table) */ hint comment

    When the query is compiled

    Then the comment is stripped and translated to the equivalent Trino session property before

    forwarding

    '
  integration_test: not_required
  integration_test_reason: Provisa-branded hint comment parsing and translation to session properties
    is pure compiler logic with mock-able federation engine.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - data-engineer
  - app-developer
  competitive_position:
    status: ahead
    rationale: A portable, engine-agnostic hint syntax (/*+ BROADCAST(...) */) that Provisa strips and
      translates to engine-specific session properties decouples query authors from Trino internals; Denodo
      and Hasura have no equivalent abstraction layer for federation engine hints.
- id: REQ-280
  status: complete
  group: 6. Execution, Routing, Caching & Performance
  category: Federation Performance
  priority: MUST
  type: behavioral
  description: ANALYZE runs on the API cache table after each materialization CTAS; ANALYZE failure is
    logged, not raised (connector tolerance, matching `analyze_source_tables` / REQ-275).
  use_case: ANALYZE after API cache CTAS keeps cost estimates fresh without failing the entire materialization
    on ANALYZE errors.
  code:
  - provisa/executor/
  - provisa/compiler/
  tests:
  - tests/integration/test_execution_routing.py
  - tests/integration/test_federation_integration.py
  - tests/unit/test_federation.py
  - tests/unit/test_federation_hints.py
  - tests/unit/test_federation_v2_extended.py
  since: 2026-01
  scenario: 'Given a materialization CTAS that has completed

    When ANALYZE runs on the resulting API cache table

    Then ANALYZE failures are logged but do not raise or fail the materialization

    '
  integration_test: required
  integration_test_reason: ANALYZE after API cache CTAS requires real Trino materialization and database
    statistics collection.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - ops
  - data-engineer
  competitive_position:
    status: neutral
    rationale: ''
- id: REQ-281
  status: complete
  group: 6. Execution, Routing, Caching & Performance
  category: Federation Performance
  priority: SHOULD
  type: behavioral
  description: Source-level `federation_hints` use the Provisa-branded @provisa vocabulary (`join=broadcast|partitioned`,
    `reorder=none|auto`, `broadcast_size=<size>`), translated to Trino session props by `provisa/compiler/directives.py:translate_federation_hints`
    at query time. Raw Trino session-prop keys still pass through (deprecated) for backward compatibility.
  use_case: Source-level federation hints vocabulary is translated to Trino session props, keeping query
    authors decoupled from engine-specific property names.
  code:
  - provisa/executor/
  - provisa/compiler/
  tests:
  - tests/integration/test_direct_exec.py
  - tests/integration/test_execution_routing.py
  - tests/integration/test_federation_integration.py
  - tests/unit/test_compiler_requirements.py
  - tests/unit/test_federation.py
  - tests/unit/test_federation_hints.py
  - tests/unit/test_federation_v2_extended.py
  since: 2026-01
  scenario: 'Given a source config with federation_hints using the @provisa vocabulary

    When a query touches that source

    Then translate_federation_hints converts the hints to Trino session props before execution

    '
  integration_test: not_required
  integration_test_reason: Source-level federation hint vocabulary translation is pure compiler logic
    in directives.py.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  competitive_position:
    status: ahead
    rationale: A @provisa branded vocabulary for federation hints that translates to engine-specific properties
      (with backward-compatible passthrough) creates a stable, portable API surface; Trino and Dremio
      expose raw engine properties directly, creating coupling between query configs and engine versions.
  stakeholders:
  - data-engineer
  - app-developer
- id: REQ-397
  status: complete
  group: 6. Execution, Routing, Caching & Performance
  category: Execution & Routing
  priority: MUST
  type: behavioral
  description: When a PK is available, the exclusion WHERE clause must use `n.<pk_col> IN [<pk_value>]`
    instead of `id(n) IN [<nodeId>]`.
  use_case: PK-based exclusion is semantic and stable across node re-materialization.
  code:
  - provisa-ui/src/components/graph/graph-model.ts
  - provisa/cypher/
  tests:
  - tests/unit/test_graph_exclusion.py
  - tests/integration/test_execution_routing.py
  since: 2026-01
  scenario: 'Given a query with a node exclusion clause and an available primary key

    When the exclusion WHERE clause is generated

    Then it uses n.<pk_col> IN [<pk_value>] rather than id(n) IN [<nodeId>]

    '
  integration_test: not_required
  integration_test_reason: PK-based exclusion WHERE clause generation is pure SQL compilation logic with
    mockable routing.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - app-developer
  - data-engineer
  competitive_position:
    status: neutral
    rationale: ''
- id: REQ-536
  status: complete
  group: 6. Execution, Routing, Caching & Performance
  category: Execution & Routing
  priority: MUST
  type: behavioral
  description: 'All data responses include cache status headers: `X-Provisa-Cache: HIT|MISS` on every
    response, and `X-Provisa-Cache-Age: <seconds>` on cache HITs, indicating how many seconds old the
    cached result is.'
  use_case: Cache headers let clients and proxies distinguish fresh vs cached results and detect stale
    responses.
  code:
  - provisa/cache/middleware.py
  tests:
  - tests/unit/test_cache_headers_requirements.py
  - tests/unit/test_misc_requirements.py
  - tests/unit/test_cache_headers_requirements.py
  - tests/integration/test_execution_routing.py
  - provisa-ui/e2e/security-rate-limiting.spec.ts
  since: 2026-01
  scenario: 'Given any data response from Provisa

    When the response is returned to the client

    Then it includes X-Provisa-Cache: HIT|MISS and X-Provisa-Cache-Age on cache HITs

    '
  integration_test: required
  integration_test_reason: Cache status headers require real HTTP response transport and actual cache
    hit/miss state verification.
  e2e: true
  e2e_reason: X-Provisa-Cache HIT/MISS response headers are only observable on actual HTTP responses from
    a live server — integration tests with mocked transport cannot verify real header injection.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  competitive_position:
    status: parity
    rationale: X-Cache HIT/MISS headers are standard HTTP caching convention (used by Varnish, CDNs, Hasura's
      caching extension); no competitive advantage.
  stakeholders:
  - app-developer
  - ops
- id: REQ-544
  status: complete
  group: 6. Execution, Routing, Caching & Performance
  category: Cache
  priority: MUST
  type: behavioral
  description: 'Cache TTL resolves in order: table-level TTL > source-level TTL > global default TTL.
    Setting `cache_enabled: false` on a source disables caching for all tables in that source regardless
    of table-level TTL. Cache keys always include `role_id` and RLS context values for security partitioning.'
  use_case: Hierarchical TTL lets operators tune cache freshness per source or per table while security-partitioned
    cache keys prevent cross-role result leakage.
  code:
  - provisa/cache/policy.py
  - provisa/cache/key.py
  tests:
  - tests/integration/test_execution_routing.py
  - tests/unit/test_apq_cache.py
  - tests/unit/test_cache_key.py
  - tests/unit/test_cache_policy.py
  since: 2026-01
  scenario: 'Given cache TTL configured at table, source, and global levels

    When a cache key is resolved

    Then table-level TTL takes precedence, and cache keys include role_id and RLS context for

    security partitioning

    '
  integration_test: required
  integration_test_reason: Hierarchical TTL resolution and security-partitioned cache keys require real
    Redis storage with role and RLS context.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  competitive_position:
    status: ahead
    rationale: Hierarchical TTL resolution (table > source > global) combined with security-partitioned
      cache keys that include role_id and RLS context values prevents cross-role result leakage — an isolation
      guarantee Dremio and Trino's result caches do not enforce at the cache key level.
  stakeholders:
  - ops
  - data-engineer
  - compliance
  related_reqs:
  - REQ-826
  - REQ-864
  - REQ-865
  - REQ-866
- id: REQ-552
  status: complete
  group: 6. Execution, Routing, Caching & Performance
  category: Execution & Routing
  priority: MUST
  type: behavioral
  description: Cross-source JOINs routed through the federation engine apply automatic type coercion when
    joining columns across sources with differing native types.
  use_case: Automatic type coercion during cross-source joins prevents type mismatch errors when federating
    tables from heterogeneous source systems.
  code:
  - provisa/transpiler/
  - provisa/executor/
  tests:
  - tests/unit/test_routing.py
  - tests/e2e/test_routing.py
  - tests/integration/test_execution_routing.py
  since: 2026-01
  scenario: 'Given a cross-source JOIN where join columns have differing native types across sources

    When the query is routed through the federation engine

    Then automatic type coercion is applied to prevent type mismatch errors

    '
  integration_test: required
  integration_test_reason: Automatic type coercion during cross-source JOINs requires real Trino federation
    engine and heterogeneous source execution.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  competitive_position:
    status: parity
    rationale: Automatic type coercion during cross-source joins is handled natively by Trino's type system
      and is similarly handled by Denodo's data integration layer; this is expected behavior for any serious
      federation engine.
  stakeholders:
  - app-developer
  - data-engineer
- id: REQ-595
  status: complete
  group: 6. Execution, Routing, Caching & Performance
  category: Caching
  priority: MUST
  type: constraint
  description: In multi-tenant mode, RedisCacheStore prefixes every cache key with the tenant_id (`provisa:cache:<tenant_id>:<sha256_key>`).
    Table invalidation keys follow `provisa:table:<tenant_id>:<table_id>`. The APQ cache uses `provisa:apq:<tenant_id>:<sha256_hex>`.
    Default APQ TTL is 86400 seconds, configurable via `PROVISA_APQ_TTL`. One tenant's cache flush never
    touches another tenant's cached results. check_cache and store_result in cache/middleware.py pass
    tenant_id (org_id) through to RedisCacheStore.get/set, so prefix isolation is active for query result
    caching.
  use_case: Tenant-prefixed cache keys guarantee that cache invalidation, eviction, and key collisions
    cannot cross tenant boundaries on shared Redis infrastructure.
  code:
  - provisa/cache/store.py
  - provisa/cache/middleware.py
  - provisa/apq/cache.py
  tests:
  - tests/e2e/test_caching.py
  - tests/integration/test_cache_store.py
  - tests/integration/test_execution_routing.py
  - tests/unit/test_cache_requirements.py
  - tests/unit/test_cache_store.py
  - tests/unit/test_config_validation_edge_cases.py
  - tests/unit/test_mv_tenant_isolation.py
  since: 2026-01
  integration_test: required
  integration_test_reason: Tenant-prefixed cache key isolation and multi-tenant cache boundaries require
    real Redis storage and full query execution path.
  competitive_position:
    status: ahead
    rationale: Tenant-prefixed cache key isolation (tenant_id in every cache key and invalidation key)
      with APQ cache partitioning is not offered by Trino, Dremio, or open-source Hasura in their multi-tenant
      caching; this is a gap in the current implementation (middleware omits tenant_id) but the design
      is a differentiator once closed.
  stakeholders:
  - ops
  - compliance
- id: REQ-047
  status: complete
  group: 7. Result Delivery
  category: Output & Delivery
  priority: SHOULD
  type: behavioral
  description: JSON output preserves native GraphQL nested structure.
  use_case: Native GraphQL JSON output preserves nested relationships, making response structure self-explanatory.
  code:
  - provisa/executor/
  tests:
  - tests/unit/test_formats.py
  - tests/integration/test_output_formats.py
  since: 2026-01
  scenario: 'Given a GraphQL query returning nested relationships

    When the result is delivered as JSON

    Then the nested structure mirrors the GraphQL response shape with relationships intact

    '
  integration_test: not_required
  integration_test_reason: Pure format serialization logic; mock query results verify nested JSON structure.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  competitive_position:
    status: parity
    rationale: Nested JSON output preserving GraphQL relationships is standard in Hasura, PostGraphile,
      and any GraphQL gateway. No competitive differentiation here.
  stakeholders:
  - app-developer
- id: REQ-048
  status: complete
  group: 7. Result Delivery
  category: Output & Delivery
  priority: SHOULD
  type: behavioral
  description: 'NDJSON streaming variant: one JSON object per line.'
  use_case: NDJSON streaming enables line-by-line processing of large results without loading the full
    payload into memory.
  code:
  - provisa/executor/
  tests:
  - tests/unit/test_formats.py
  - tests/integration/test_output_formats.py
  since: 2026-01
  scenario: 'Given a query returning multiple rows

    When the output format is NDJSON

    Then each result row is emitted as a single JSON object on its own line

    '
  integration_test: not_required
  integration_test_reason: Line-by-line streaming format transformation; mocks suffice to verify output
    structure.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  competitive_position:
    status: parity
    rationale: NDJSON streaming is a common output format supported by Trino (via HTTP streaming), Dremio,
      and most REST-based query engines. Standard capability.
  stakeholders:
  - app-developer
- id: REQ-049
  status: complete
  group: 7. Result Delivery
  category: Output & Delivery
  priority: SHOULD
  type: behavioral
  description: 'Normalized tabular: flattened to relational tables with FK relationships preserved, Parquet
    or CSV.'
  use_case: Normalized tabular output with FK preservation lets BI tools load data into relational models
    without ETL.
  code:
  - provisa/executor/
  tests:
  - tests/integration/test_output_formats.py
  - tests/unit/test_formats.py
  - tests/unit/test_normalize.py
  - tests/unit/test_normalized_endpoint.py
  since: 2026-01
  scenario: 'Given a query with nested relationships

    When the output format is normalized tabular (Parquet or CSV)

    Then results are flattened to relational tables with FK relationships preserved

    '
  integration_test: not_required
  integration_test_reason: Schema normalization and flattening logic; mock schema metadata enables verification.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  competitive_position:
    status: ahead
    rationale: Normalized tabular output with FK preservation emitted directly from a federation query
      is not offered by Trino, Dremio, or Snowflake — those engines return flat result sets and leave
      normalization to ETL tools. Provisa eliminates that post-processing step for BI consumers.
  stakeholders:
  - app-developer
  - data-engineer
- id: REQ-050
  status: complete
  group: 7. Result Delivery
  category: Output & Delivery
  priority: SHOULD
  type: behavioral
  description: 'Denormalized tabular: fully flattened single table, Parquet or CSV, single file or partitioned.'
  use_case: Denormalized flat-file output lets data science consumers load results directly into dataframes
    or ML pipelines.
  code:
  - provisa/executor/
  tests:
  - tests/unit/test_formats.py
  - tests/integration/test_output_formats.py
  since: 2026-01
  scenario: 'Given a query with nested relationships

    When the output format is denormalized tabular (Parquet or CSV)

    Then results are fully flattened into a single table, optionally partitioned

    '
  integration_test: not_required
  integration_test_reason: Denormalization and CSV/Parquet encoding logic; mocks give full fidelity for
    structure testing.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  competitive_position:
    status: parity
    rationale: Denormalized flat Parquet/CSV output is the default delivery mode for Dremio, Snowflake
      UNLOAD, and Trino with Hive/Iceberg connectors. Provisa meets the baseline but does not differentiate
      here.
  stakeholders:
  - app-developer
  - data-engineer
- id: REQ-051
  status: complete
  superseded_by: REQ-045
  group: 7. Result Delivery
  category: Output & Delivery
  priority: SHOULD
  type: behavioral
  description: Arrow buffer via gRPC Arrow Flight endpoint; Trino produces Arrow natively.
  use_case: Native Arrow Flight delivery eliminates serialization overhead for high-throughput analytics
    consumers.
  code:
  - provisa/executor/
  tests:
  - tests/unit/test_formats.py
  - tests/integration/test_output_formats.py
  - provisa-ui/e2e/execution-routing.spec.ts
  since: 2026-01
  scenario: 'Given a query submitted via the Arrow Flight endpoint

    When Trino executes the query

    Then results are delivered as Arrow record batches via gRPC with no intermediate serialization

    '
  integration_test: required
  integration_test_reason: Arrow Flight wire protocol and gRPC marshaling; real binary format + Trino-native
    Arrow production must verify end-to-end.
  e2e: true
  e2e_reason: Arrow Flight gRPC wire protocol with Trino-native Arrow production is only verifiable by
    connecting a real Arrow Flight client to a running Flight server — unit and integration mocks cannot
    validate the binary gRPC transport.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  competitive_position:
    status: ahead
    rationale: Native Arrow Flight delivery is not exposed by Hasura, Denodo, or Snowflake; Dremio supports
      Flight but only for its own lakehouse. Provisa surfaces Arrow Flight across heterogeneous federated
      sources, removing serialization overhead that every competitor still imposes.
  stakeholders:
  - app-developer
- id: REQ-137
  status: complete
  group: 7. Result Delivery
  category: Large Result Redirect & CTAS
  priority: SHOULD
  type: behavioral
  description: Client-controlled redirect via `X-Provisa-Redirect-Format` and `X-Provisa-Redirect-Threshold`
    headers. Format without threshold implies force redirect.
  use_case: Client-controlled redirect format and threshold lets high-volume consumers choose their preferred
    output path.
  code:
  - provisa/executor/
  tests:
  - tests/e2e/test_large_result.py
  - tests/integration/test_blob_upload.py
  - tests/unit/test_redirect.py
  - provisa-ui/e2e/execution-routing.spec.ts
  since: 2026-01
  scenario: 'Given a client sets X-Provisa-Redirect-Format and optionally X-Provisa-Redirect-Threshold

    When the query executes

    Then results are redirected to the specified format; format alone forces redirect regardless of size

    '
  integration_test: required
  integration_test_reason: HTTP header parsing and redirect decision routing; header behavior + threshold
    logic requires HTTP transport verification.
  e2e: true
  e2e_reason: X-Provisa-Redirect-Format and X-Provisa-Redirect-Threshold header parsing and the resulting
    redirect decision are only verifiable by sending real HTTP requests with those headers to a live server.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  competitive_position:
    status: ahead
    rationale: Per-request client-controlled redirect format and threshold via HTTP headers is not a feature
      of Trino, Dremio, Snowflake, or Denodo — those platforms redirect large results via server-side
      config or require separate export APIs. Provisa gives each consumer fine-grained control inline
      with the query call.
  stakeholders:
  - app-developer
  - ops
- id: REQ-138
  status: complete
  group: 7. Result Delivery
  category: Large Result Redirect & CTAS
  priority: SHOULD
  type: behavioral
  description: Trino-native formats (Parquet, ORC) use CTAS — Trino writes directly to S3 via Iceberg,
    data never passes through Provisa.
  use_case: Trino-native CTAS redirect lets Trino write Parquet and ORC directly to S3 without data passing
    through Provisa.
  code:
  - provisa/executor/
  tests:
  - tests/e2e/test_large_result.py
  - tests/integration/test_blob_upload.py
  - tests/unit/test_redirect.py
  since: 2026-01
  scenario: 'Given a redirect format of Parquet or ORC

    When the query executes

    Then Trino writes results directly to S3 via Iceberg CTAS and no data passes through Provisa

    '
  integration_test: required
  integration_test_reason: Trino CTAS to S3 via Iceberg; real Trino execution + S3 write behavior requires
    live cluster.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  competitive_position:
    status: parity
    rationale: Trino itself supports CTAS to S3 via Iceberg, and Dremio and Snowflake both have CTAS-style
      export paths. Provisa's CTAS redirect is a sound integration of Trino's native capability but is
      not differentiating on its own.
  stakeholders:
  - app-developer
  - ops
- id: REQ-139
  status: complete
  group: 7. Result Delivery
  category: Large Result Redirect & CTAS
  priority: SHOULD
  type: behavioral
  description: Non-native formats (JSON, NDJSON, CSV, Arrow IPC) serialized by Provisa and uploaded to
    S3 via boto3.
  use_case: Provisa-side serialization handles non-native formats for consumers that need JSON, CSV, or
    Arrow IPC on S3.
  code:
  - provisa/executor/
  tests:
  - tests/e2e/test_large_result.py
  - tests/integration/test_blob_upload.py
  - tests/unit/test_redirect.py
  since: 2026-01
  scenario: 'Given a redirect format of JSON, NDJSON, CSV, or Arrow IPC

    When the query executes above the threshold

    Then Provisa serializes the result and uploads it to S3 via boto3

    '
  integration_test: required
  integration_test_reason: Provisa-side serialization and boto3 S3 upload; real S3 bucket write + format
    encoding verification.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  competitive_position:
    status: parity
    rationale: Server-side serialization and S3 upload of non-native formats is standard in Dremio and
      Snowflake UNLOAD, which both write JSON and CSV to cloud storage. Provisa matches this baseline.
  stakeholders:
  - app-developer
  - ops
- id: REQ-140
  status: complete
  group: 7. Result Delivery
  category: Large Result Redirect & CTAS
  priority: MUST
  type: behavioral
  description: Threshold-based redirect uses LIMIT threshold+1 probe — no COUNT(*), no double execution
    for inline results.
  use_case: Threshold probe via LIMIT avoids double execution overhead when deciding whether to redirect
    large results.
  code:
  - provisa/executor/
  tests:
  - tests/e2e/test_large_result.py
  - tests/integration/test_blob_upload.py
  - tests/integration/test_direct_exec.py
  - tests/unit/test_redirect.py
  since: 2026-01
  scenario: 'Given a redirect threshold is set

    When the query executes

    Then a LIMIT threshold+1 probe determines redirect without COUNT(*) or re-executing the query

    '
  integration_test: required
  integration_test_reason: LIMIT-based probe execution and redirect threshold decision; real Trino query
    execution required.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  competitive_position:
    status: ahead
    rationale: The LIMIT threshold+1 probe avoiding COUNT(*) or double execution is an efficiency design
      not documented in Trino, Dremio, or Snowflake — those platforms either count or re-execute. Provisa's
      approach halves execution cost at the redirect decision boundary.
  stakeholders:
  - ops
- id: REQ-141
  status: complete
  group: 7. Result Delivery
  category: Large Result Redirect & CTAS
  priority: SHOULD
  type: behavioral
  description: S3 data cleanup scheduled after presigned URL TTL expires.
  use_case: Scheduled S3 data cleanup prevents indefinite storage cost accumulation from expired redirect
    results.
  code:
  - provisa/executor/
  tests:
  - tests/e2e/test_large_result.py
  - tests/integration/test_blob_upload.py
  - tests/unit/test_redirect.py
  since: 2026-01
  scenario: 'Given redirect results have been written to S3 with a presigned URL

    When the presigned URL TTL expires

    Then a scheduled job removes the corresponding S3 data

    '
  integration_test: required
  integration_test_reason: S3 data cleanup scheduling and TTL expiration; real S3 lifecycle + async cleanup
    behavior requires live infra.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  competitive_position:
    status: parity
    rationale: TTL-based S3 cleanup after presigned URL expiry is standard practice and available in Snowflake
      stage purge and Dremio reflections expiry. No material differentiation.
  stakeholders:
  - ops
  - compliance
- id: REQ-142
  status: complete
  group: 7. Result Delivery
  category: Large Result Redirect & CTAS
  priority: MAY
  type: structural
  description: 'Default redirect format configurable via `PROVISA_REDIRECT_FORMAT` (default: parquet).'
  use_case: Configurable default redirect format lets operators choose the storage format best suited
    to their consumer mix.
  code:
  - provisa/executor/
  tests:
  - tests/e2e/test_large_result.py
  - tests/integration/test_blob_upload.py
  - tests/unit/test_redirect.py
  since: 2026-01
  integration_test: not_required
  integration_test_reason: Environment variable parsing and default config injection; mocks verify config
    flow.
  stakeholders:
  - ops
  competitive_position:
    status: parity
    rationale: Configurable redirect format (Parquet default) is standard in data platforms like Dremio
      and Snowflake.
- id: REQ-143
  status: complete
  group: 7. Result Delivery
  category: Arrow Flight
  priority: SHOULD
  type: behavioral
  description: Arrow Flight server (port 8815) streams record batches via gRPC. Full security pipeline
    applied.
  use_case: Arrow Flight server streams record batches with full security applied, enabling zero-copy
    governed data delivery.
  code:
  - provisa/api/flight/
  tests:
  - tests/integration/test_arrow_flight_integration.py
  - tests/unit/test_flight_server_helpers.py
  - tests/unit/test_zaychik_flight_unit.py
  - provisa-ui/e2e/execution-routing.spec.ts
  since: 2026-01
  scenario: 'Given a client connects to the Arrow Flight server on port 8815

    When a query is submitted

    Then record batches are streamed via gRPC with the full Provisa security pipeline applied

    '
  integration_test: required
  integration_test_reason: Arrow Flight gRPC server on port 8815 with record batch streaming; real gRPC
    transport + security pipeline requires end-to-end.
  e2e: true
  e2e_reason: The Arrow Flight server on port 8815 streaming record batches via gRPC with the full security
    pipeline is only verifiable by connecting a real Flight client over the network to a live running
    server.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  competitive_position:
    status: ahead
    rationale: A governed Arrow Flight server with the full security pipeline applied at the Flight layer
      is not offered by Hasura, Denodo, or Snowflake. Dremio provides Flight but without federation governance;
      Provisa applies RLS and tenant isolation to Arrow Flight delivery, which no competitor does across
      heterogeneous sources.
  stakeholders:
  - app-developer
- id: REQ-144
  status: complete
  group: 7. Result Delivery
  category: Arrow Flight
  priority: SHOULD
  type: behavioral
  description: Zaychik Arrow Flight SQL proxy translates between Flight SQL clients and Trino JDBC.
  use_case: Zaychik Flight SQL proxy lets Flight SQL clients query Trino without requiring a native Trino
    Flight endpoint.
  code:
  - provisa/api/flight/
  tests:
  - tests/integration/test_arrow_flight_integration.py
  - tests/unit/test_flight_server_helpers.py
  - tests/unit/test_zaychik_flight_unit.py
  since: 2026-01
  scenario: 'Given a Flight SQL client submits a query

    When Zaychik receives the request

    Then it translates the Flight SQL protocol to Trino JDBC and returns results as Arrow batches

    '
  integration_test: required
  integration_test_reason: Zaychik Flight SQL proxy translation and JDBC bridge; real JDBC + Flight SQL
    protocol translation requires live Trino.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  competitive_position:
    status: ahead
    rationale: A Flight SQL proxy (Zaychik) bridging Flight SQL clients to Trino JDBC is not available
      in Hasura, Denodo, or Snowflake. Dremio supports Flight SQL natively but only for its own engine;
      Provisa exposes the same protocol across all federated sources.
  stakeholders:
  - app-developer
- id: REQ-145
  status: complete
  group: 7. Result Delivery
  category: Arrow Flight
  priority: MUST
  type: constraint
  description: Flight server streams batch-by-batch via GeneratorStream — full result never materialized
    in Provisa memory. Unbounded result support.
  use_case: GeneratorStream batch delivery means arbitrarily large result sets never fully materialize
    in Provisa memory.
  code:
  - provisa/api/flight/
  tests:
  - tests/integration/test_arrow_flight_integration.py
  - tests/unit/test_flight_server_helpers.py
  - tests/unit/test_zaychik_flight_unit.py
  since: 2026-01
  scenario: 'Given a query returning an arbitrarily large result set via Arrow Flight

    When the Flight server processes the query

    Then batches are streamed via GeneratorStream and the full result is never held in Provisa memory

    '
  integration_test: required
  integration_test_reason: GeneratorStream batch materialization and unbounded result streaming; real
    large result iteration + memory behavior requires live query.
  competitive_position:
    status: ahead
    rationale: GeneratorStream batch-by-batch delivery with no full materialization is a stronger memory
      guarantee than Trino's own HTTP chunked response (which Trino clients reassemble in memory) and
      is not offered by Dremio or Snowflake Flight paths. Provisa supports unbounded result sets without
      coordinator-side buffering.
  stakeholders:
  - ops
- id: REQ-146
  status: complete
  group: 7. Result Delivery
  category: Arrow Flight
  priority: SHOULD
  type: behavioral
  description: Falls back to materializing via Trino REST if Zaychik unavailable.
  use_case: Fallback to Trino REST ensures Flight delivery works even when the Zaychik proxy is unavailable.
  code:
  - provisa/api/flight/
  tests:
  - tests/integration/test_arrow_flight_integration.py
  - tests/unit/test_flight_server_helpers.py
  - tests/unit/test_zaychik_flight_unit.py
  since: 2026-01
  scenario: 'Given the Zaychik proxy is unavailable

    When a Flight query is submitted

    Then the Flight server falls back to materializing results via Trino REST API

    '
  integration_test: required
  integration_test_reason: Fallback from Zaychik to Trino REST on unavailability; failure path requires
    live infrastructure state.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  competitive_position:
    status: parity
    rationale: Fallback from a preferred delivery path to a REST/HTTP materialization path is a standard
      resilience pattern present in Dremio and Trino client libraries. Not a differentiator.
  stakeholders:
  - ops
- id: REQ-043
  status: complete
  group: 8. Client Access & Protocols
  category: API & Integration
  priority: SHOULD
  type: behavioral
  description: GraphQL endpoint is primary entry point for queries and mutations.
  use_case: GraphQL as the primary entry point gives consumers a strongly typed, self-documenting API
    surface.
  code:
  - provisa/api/rest/
  - provisa/api/jsonapi/
  - provisa/api/flight/
  tests:
  - tests/unit/test_rest_generator.py
  - tests/unit/test_jsonapi.py
  - tests/integration/test_client_access_integration.py
  since: 2026-01
  scenario: 'Given a consumer with valid credentials

    When they submit a query or mutation to the GraphQL endpoint

    Then the request is processed and a typed response is returned

    '
  integration_test: required
  integration_test_reason: GraphQL endpoint behavior requires end-to-end validation through HTTP transport
    and real schema compilation.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  competitive_position:
    status: parity
    rationale: GraphQL as a primary query entry point is standard for Hasura (its core value prop) and
      supported by Dgraph and others. Provisa offering this is expected, not differentiating.
  stakeholders:
  - app-developer
- id: REQ-044
  status: complete
  superseded_by: REQ-029
  group: 8. Client Access & Protocols
  category: API & Integration
  priority: SHOULD
  type: behavioral
  description: Presigned URL redirect for large result consumers with TTL-bounded access.
  use_case: Presigned URL redirect gives large-result consumers time-bounded, secure access without server-side
    buffering.
  code:
  - provisa/api/rest/
  - provisa/api/jsonapi/
  - provisa/api/flight/
  tests:
  - tests/e2e/test_large_result.py
  - tests/integration/test_client_access_integration.py
  - tests/unit/test_jsonapi.py
  - tests/unit/test_redirect_s3.py
  - tests/unit/test_rest_generator.py
  since: 2026-01
  scenario: 'Given a consumer requesting a large result set

    When the server generates a presigned URL with a TTL

    Then the consumer can access the result via the URL within the TTL without server-side buffering

    '
  integration_test: required
  integration_test_reason: Presigned URL TTL and redirect behavior requires testing against real storage
    backend and HTTP response headers.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  competitive_position:
    status: parity
    rationale: Presigned URL redirect for large results is standard in cloud platforms like Snowflake
      (GET_PRESIGNED_URL) and Dremio. This is a hygiene feature, not a differentiator.
  stakeholders:
  - app-developer
  - ops
- id: REQ-045
  status: complete
  group: 8. Client Access & Protocols
  category: API & Integration
  priority: SHOULD
  type: behavioral
  description: gRPC Arrow Flight endpoint for high-throughput consumers; Trino produces Arrow natively
    for zero-copy delivery.
  use_case: Arrow Flight endpoint enables high-throughput consumers to stream data with zero-copy native
    Arrow delivery.
  code:
  - provisa/api/rest/
  - provisa/api/jsonapi/
  - provisa/api/flight/
  tests:
  - tests/unit/test_rest_generator.py
  - tests/unit/test_jsonapi.py
  - tests/integration/test_arrow_flight_integration.py
  since: 2026-01
  scenario: 'Given a high-throughput consumer connecting via gRPC Arrow Flight

    When Trino produces Arrow natively

    Then data streams with zero-copy delivery to the consumer

    '
  integration_test: required
  integration_test_reason: Arrow Flight endpoint zero-copy delivery requires real gRPC wire protocol and
    Trino native Arrow serialization.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  competitive_position:
    status: ahead
    rationale: Exposing Arrow Flight natively from a federation layer is uncommon — Trino/Presto have
      no Flight endpoint, and Hasura/Denodo do not expose Arrow Flight. Dremio offers Flight but only
      for its own lakehouse, not as a cross-source federation gateway.
  stakeholders:
  - app-developer
  - ops
- id: REQ-126
  status: complete
  group: 8. Client Access & Protocols
  category: JDBC/ODBC Integration
  priority: SHOULD
  type: behavioral
  description: JDBC driver that exposes registered tables and views as virtual tables. Connection authenticates
    against Provisa, maps user to role.
  use_case: JDBC driver exposes registered tables/views as virtual tables, enabling any JDBC-compatible
    tool to query governed data.
  code:
  - provisa-client/
  tests:
  - tests/unit/test_drivers.py
  - tests/unit/test_flight_modes.py
  - tests/integration/test_arrow_flight_integration.py
  - tests/integration/test_client_access_integration.py
  since: 2026-01
  scenario: 'Given a JDBC client connecting to Provisa

    When the connection authenticates and maps the user to a role

    Then registered tables and views are accessible as virtual tables

    '
  integration_test: required
  integration_test_reason: JDBC driver connection establishment and role assignment requires real wire
    protocol handshake and auth provider integration.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  competitive_position:
    status: parity
    rationale: JDBC drivers are standard for Trino, Dremio, Denodo, and Snowflake. All serious data platforms
      ship one; this is a table-stakes requirement.
  stakeholders:
  - app-developer
  - ops
- id: REQ-127
  status: complete
  group: 8. Client Access & Protocols
  category: JDBC/ODBC Integration
  priority: SHOULD
  type: behavioral
  description: '`getTables()` returns registered tables and views visible to the authenticated role, by
    their registered names.'
  use_case: getTables() returning registered tables/views lets BI tools discover governed datasets through
    standard JDBC metadata.
  code:
  - provisa-client/
  tests:
  - tests/unit/test_drivers.py
  since: 2026-01
  scenario: 'Given a JDBC client calling getTables()

    When the authenticated role has visibility to certain tables and views

    Then only those registered tables and views are returned by their registered names

    '
  integration_test: deferred
  integration_test_reason: 'getTables() metadata discovery requires real compilation context and role-based
    table visibility filtering. [deferred: requires external infrastructure not in CI stack]'
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  competitive_position:
    status: parity
    rationale: getTables() metadata discovery over JDBC is expected from any JDBC-compliant driver; Trino,
      Dremio, and Snowflake all implement it correctly.
  stakeholders:
  - app-developer
- id: REQ-128
  status: complete
  group: 8. Client Access & Protocols
  category: JDBC/ODBC Integration
  priority: SHOULD
  type: behavioral
  description: '`getColumns(tableName)` introspects the registered table/view output schema — column names
    and types from compiled metadata, filtered by role visibility.'
  use_case: getColumns() introspection lets JDBC tools render correct column types without manual schema
    configuration.
  code:
  - provisa-client/
  tests:
  - tests/integration/test_pgwire_column_visibility_integration.py
  - tests/unit/test_drivers.py
  - tests/unit/test_view_column_types.py
  since: 2026-01
  scenario: 'Given a JDBC client calling getColumns(tableName)

    When compiled metadata is available and filtered by role visibility

    Then column names and types are returned from the registered schema

    '
  integration_test: required
  integration_test_reason: getColumns() introspection with role visibility filtering requires real compiled
    schema and role context.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  competitive_position:
    status: ahead
    rationale: getColumns() filtered by role visibility — returning only columns the authenticated role
      may see — is not standard in Trino or Dremio JDBC drivers, which return all columns and rely on
      query-time enforcement. Provisa surfaces governance at the metadata layer.
  stakeholders:
  - app-developer
- id: REQ-129
  status: complete
  group: 8. Client Access & Protocols
  category: JDBC/ODBC Integration
  priority: SHOULD
  type: behavioral
  description: '`executeQuery(sql)` runs arbitrary SQL against registered tables/views, passes it through
    Stage 2 governance, executes via Provisa''s HTTP API, and deserializes the result into a JDBC ResultSet.
    (Amended 2026-06-19: transport is Arrow IPC / JSON, not Parquet — Arrow IPC streams a ResultSet far
    better than a columnar file format, which would buffer the whole result.)'
  use_case: executeQuery lets JDBC tools run governed SQL against registered tables/views using standard
    SQL syntax.
  code:
  - provisa-client/
  tests:
  - tests/unit/test_drivers.py
  since: 2026-01
  scenario: 'Given a JDBC client calling executeQuery(sql)

    When the SQL is passed through Stage 2 governance and executed via the HTTP API

    Then the result is deserialized into a JDBC ResultSet via Arrow IPC or JSON transport

    '
  integration_test: deferred
  integration_test_reason: 'executeQuery() governance pipeline and Arrow/JSON deserialization requires
    end-to-end HTTP API execution with Stage 2 governance. [deferred: requires external infrastructure
    not in CI stack]'
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - app-developer
  - ops
  competitive_position:
    status: ahead
    rationale: JDBC executeQuery routing through Stage 2 governance (RLS, masking) with Arrow IPC streaming
      is not offered by Trino or Dremio JDBC — they stream JDBC ResultSets without inline masking applied
      at driver transport time.
- id: REQ-130
  status: complete
  superseded_by: REQ-272
  group: 8. Client Access & Protocols
  category: JDBC/ODBC Integration
  priority: MUST
  type: constraint
  description: Full security pipeline (RLS, masking, sampling) applied at query time — not baked into
    views.
  use_case: Full security pipeline at JDBC query time ensures BI tool users receive only data their role
    permits.
  code:
  - provisa-client/
  tests:
  - tests/e2e/test_query_pipeline.py
  - tests/unit/test_drivers.py
  since: 2026-01
  scenario: 'Given a JDBC client executing a query

    When the query runs through the security pipeline

    Then RLS, masking, and sampling are applied at query time rather than being baked into views

    '
  integration_test: deferred
  integration_test_reason: 'Full RLS/masking/sampling security pipeline requires real data source and
    query execution. [deferred: requires external infrastructure not in CI stack]'
  stakeholders:
  - compliance
  - app-developer
  competitive_position:
    status: ahead
    rationale: Applying RLS, masking, and sampling at every JDBC query time with no ungoverned path is
      stronger than Denodo or Dremio, where governance is typically enforced at view or policy definition
      time and can be bypassed via direct source connections. Snowflake enforces masking at column policy
      level but does not support cross-source RLS across federated tables.
- id: REQ-131
  status: complete
  group: 8. Client Access & Protocols
  category: JDBC/ODBC Integration
  priority: SHOULD
  type: structural
  description: 'Connection string format: `jdbc:provisa://host:port`. Authentication via standard JDBC
    username/password properties.'
  use_case: Standard JDBC connection string format means no custom driver configuration is needed in most
    BI tools.
  code:
  - provisa-client/
  tests:
  - tests/unit/test_drivers.py
  since: 2026-01
  integration_test: not_required
  integration_test_reason: Connection string parsing and JDBC properties handling is pure string parsing
    logic.
  stakeholders:
  - app-developer
  competitive_position:
    status: parity
    rationale: Standard JDBC connection string format is table stakes for enterprise data platforms.
- id: REQ-132
  status: complete
  group: 8. Client Access & Protocols
  category: JDBC/ODBC Integration
  priority: SHOULD
  type: structural
  description: 'The driver ships as a single self-contained shaded JAR. (Amended 2026-06-19: the Flight
    transport (REQ-293) requires gRPC/Netty, so "JDK + Arrow only" is not achievable; instead all bundled
    third-party packages — Gson, gRPC, Netty, protobuf, guava — are shade-relocated under `io.provisa.shaded.*`
    so they never collide with a host application''s own copies. The JDBC SPI and Apache Arrow remain
    at their original packages.)'
  use_case: Single self-contained JAR eliminates dependency conflicts when deploying the driver in enterprise
    environments.
  code:
  - provisa-client/
  tests:
  - tests/unit/test_drivers.py
  since: 2026-01
  integration_test: not_required
  integration_test_reason: JAR shading and packaging is build-time configuration and artifact structure,
    not runtime behavior.
  stakeholders:
  - app-developer
  - ops
  competitive_position:
    status: parity
    rationale: Single shaded JAR packaging is standard for JDBC drivers.
- id: REQ-161
  status: complete
  group: 8. Client Access & Protocols
  category: Query Development Tools
  priority: SHOULD
  type: behavioral
  description: '`POST /data/compile` returns compiled SQL with RLS/masking applied, route decision, and
    params without executing.'
  use_case: compile endpoint lets developers inspect governed SQL and routing before submitting a query
    for approval.
  code:
  - provisa/api/rest/
  - provisa/api/data/
  tests:
  - tests/integration/test_compile_endpoint.py
  - tests/unit/test_client_access.py
  - tests/unit/test_compile_route.py
  since: 2026-01
  scenario: 'Given a developer posting a query to /data/compile

    When the server applies RLS and masking

    Then compiled SQL, route decision, and params are returned without executing the query

    '
  integration_test: required
  integration_test_reason: POST /data/compile endpoint behavior requires real schema compilation and governance
    rule application via HTTP.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - app-developer
  competitive_position:
    status: ahead
    rationale: A compile endpoint that returns governed SQL with RLS/masking applied, routing decision,
      and params without executing is not offered by Trino, Hasura, or Denodo. It lets developers inspect
      and audit governance output before execution — a unique developer-transparency feature.
- id: REQ-163
  status: complete
  group: 8. Client Access & Protocols
  category: Query Development Tools
  priority: SHOULD
  type: ui
  description: GraphiQL Provisa plugin with View SQL (compiled governed SQL preview).
  use_case: GraphiQL plugin with View SQL integrates governed-SQL inspection into the IDE.
  code:
  - provisa/api/rest/
  - provisa/api/data/
  tests:
  - tests/integration/test_compile_endpoint.py
  - tests/unit/test_client_access.py
  since: 2026-01
  integration_test: not_required
  integration_test_reason: GraphiQL plugin View SQL feature is UI rendering logic calling the compile
    endpoint; endpoint behavior is tested separately.
  e2e: true
  e2e_reason: The GraphiQL plugin View SQL panel is browser-rendered UI; only a browser- level test can
    verify the plugin mounts, calls /data/compile, and displays the governed SQL inline in the IDE.
  stakeholders:
  - app-developer
  competitive_position:
    status: ahead
    rationale: A GraphiQL plugin that shows the compiled, governed SQL before execution is a differentiator
      — Hasura's GraphiQL shows raw SQL but not RLS/masking rewrites. Provisa surfaces governance artifacts
      inline in the developer IDE.
- id: REQ-256
  status: complete
  group: 8. Client Access & Protocols
  category: API & Integration
  priority: SHOULD
  type: behavioral
  description: Auto-generated plain REST endpoints for every registered table via `GET /data/rest/{table}`
    with query string mapping to GraphQL args (`?limit=10&where.id.eq=1`). Compiles and executes via existing
    pipeline (RLS, masking, routing) with same security as GraphQL.
  use_case: Auto-generated plain REST endpoints let REST-only tools query governed data without learning
    GraphQL.
  code:
  - provisa/api/rest/
  - provisa/api/jsonapi/
  - provisa/api/flight/
  tests:
  - tests/e2e/test_query_pipeline.py
  - tests/integration/test_client_access_integration.py
  - tests/unit/test_jsonapi.py
  - tests/unit/test_rest_generator.py
  since: 2026-01
  scenario: 'Given a REST-only client querying GET /data/rest/{table}

    When the query string maps to GraphQL args

    Then the request compiles and executes with the same RLS, masking, and routing as GraphQL

    '
  integration_test: required
  integration_test_reason: Auto-generated REST endpoints with query string mapping and governance execution
    requires end-to-end HTTP API testing.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - app-developer
  competitive_position:
    status: parity
    rationale: Auto-generated REST endpoints for every registered table are available in Hasura (REST
      endpoints on GraphQL queries) and PostgREST-style systems. This is increasingly standard for developer-facing
      API layers.
- id: REQ-257
  status: complete
  group: 8. Client Access & Protocols
  category: API & Integration
  priority: SHOULD
  type: behavioral
  description: 'Auto-generated JSON:API compliant endpoints for every registered table via `GET /data/jsonapi/{table}`
    following spec (jsonapi.org): resource objects with type/id/attributes/relationships, sparse fieldsets
    (`?fields[orders]=amount`), inclusion of related resources (`?include=customer`), filtering (`?filter[region]=US`),
    sorting (`?sort=-created_at`), pagination (`?page[number]=2&page[size]=25`), compound documents. Compiles
    and executes via existing pipeline.'
  use_case: JSON:API compliant endpoints let clients use standard JSON:API tooling (sparse fields, includes,
    pagination).
  code:
  - provisa/api/rest/
  - provisa/api/jsonapi/
  - provisa/api/flight/
  tests:
  - tests/integration/test_jsonapi_integration.py
  - tests/unit/test_jsonapi.py
  - tests/unit/test_jsonapi_include.py
  - tests/unit/test_rest_generator.py
  since: 2026-01
  scenario: 'Given a client querying GET /data/jsonapi/{table}

    When the request includes sparse fieldsets, includes, filters, sorting, or pagination

    Then a JSON:API compliant response with compound documents is returned

    '
  integration_test: required
  integration_test_reason: JSON:API compliance with sparse fields, includes, filtering, sorting, pagination
    requires real HTTP endpoint behavior and spec conformance.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - app-developer
  competitive_position:
    status: ahead
    rationale: Auto-generated JSON:API compliant endpoints (with sparse fieldsets, includes, compound
      documents) run through the full governance pipeline are not offered by Trino, Dremio, or Hasura.
      Hasura can expose REST but does not implement the JSON:API spec.
- id: REQ-258
  status: complete
  group: 8. Client Access & Protocols
  category: API & Integration
  priority: SHOULD
  type: behavioral
  description: SSE subscriptions via `GET /data/subscribe/{table}` with pluggable notification providers
    per source type. PostgreSQL uses LISTEN/NOTIFY via asyncpg, MongoDB uses Change Streams via motor
    collection.watch(), Kafka uses consumer groups. Each provider implements a common async watch() interface
    returning change events. RLS filtering and schema validation apply regardless of provider.
  use_case: Pluggable SSE subscription providers let each source type (PG, Mongo, Kafka) use its native
    change mechanism.
  code:
  - provisa/api/rest/
  - provisa/api/jsonapi/
  - provisa/api/flight/
  tests:
  - tests/integration/test_client_access_integration.py
  - tests/unit/test_jsonapi.py
  - tests/unit/test_rest_generator.py
  - tests/unit/test_subscription_providers.py
  since: 2026-01
  scenario: 'Given a client subscribing to GET /data/subscribe/{table}

    When the source type is PostgreSQL, MongoDB, or Kafka

    Then change events stream via SSE using the native provider with RLS filtering applied

    '
  integration_test: required
  integration_test_reason: SSE subscriptions with pluggable providers (LISTEN/NOTIFY, Change Streams,
    Kafka) require real async transport and source-specific change detection.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - app-developer
  - ops
  competitive_position:
    status: ahead
    rationale: Pluggable SSE subscriptions with RLS filtering that use each source's native change mechanism
      (LISTEN/NOTIFY, MongoDB Change Streams, Kafka) is significantly more capable than Hasura's subscription
      model (PostgreSQL-only) and not available at all in Trino, Dremio, or Denodo.
  related_reqs:
  - REQ-828
- id: REQ-268
  status: complete
  group: 8. Client Access & Protocols
  category: SQL & Multi-Protocol Client Access
  priority: SHOULD
  type: behavioral
  description: Python DB-API 2.0 (PEP 249) interface in `provisa-client`. `provisa_client.connect(url,
    user, password)` returns a PEP 249 connection. `cursor.execute(query)` accepts GraphQL (detected by
    leading `{` or `query`/`mutation` keyword) or SQL. Authenticates via username/password; server assigns
    role.
  use_case: DB-API 2.0 interface lets Python code query Provisa with standard cursor.execute() without
    a custom client.
  code:
  - provisa-client/
  - provisa/api/
  tests:
  - tests/integration/test_adbc.py
  - tests/integration/test_sqlalchemy_dialect.py
  - tests/unit/test_client_access.py
  - tests/unit/test_client_role.py
  - tests/unit/test_proto_gen.py
  - tests/unit/test_protocol_clients.py
  since: 2026-01
  scenario: 'Given a Python caller using provisa_client.connect()

    When cursor.execute() is called with GraphQL or SQL

    Then the query executes with the server-assigned role via DB-API 2.0 semantics

    '
  integration_test: required
  integration_test_reason: DB-API 2.0 cursor.execute() with mixed GraphQL/SQL detection and role assignment
    requires HTTP transport and auth provider.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - app-developer
  competitive_position:
    status: parity
    rationale: Python DB-API 2.0 (PEP 249) is a standard interface; Trino, Snowflake, and BigQuery all
      ship PEP 249-compliant connectors. The dual GraphQL/SQL detection on cursor.execute() is modest
      differentiation but not materially ahead.
- id: REQ-269
  status: complete
  group: 8. Client Access & Protocols
  category: SQL & Multi-Protocol Client Access
  priority: SHOULD
  type: behavioral
  description: DB-API 2.0 connection exposes all registered tables and views the user's rights permit
    for arbitrary SQL. There is no connection `mode` parameter — governance is uniform via rights + Stage
    2.
  use_case: Uniform rights-governed SQL access in DB-API removes mode selection; every query is governed
    identically.
  code:
  - provisa-client/
  - provisa/api/
  tests:
  - tests/integration/test_adbc.py
  - tests/integration/test_sqlalchemy_dialect.py
  - tests/unit/test_client_access.py
  - tests/unit/test_proto_gen.py
  since: 2026-01
  scenario: "Given a DB-API 2.0 connection\nWhen arbitrary SQL is executed\nThen only tables and views\
    \ permitted by the user's rights are accessible with uniform Stage 2\n  governance\n"
  integration_test: required
  integration_test_reason: Uniform governance via rights+Stage2 requires real query execution with role
    visibility and security filters.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - compliance
  - app-developer
  competitive_position:
    status: ahead
    rationale: Uniform governance (rights + Stage 2) with no mode parameter — preventing clients from
      selecting an ungoverned path — is stronger than Snowflake or Trino, which allow bypasses via catalog-level
      connections or role escalation in some configurations.
- id: REQ-270
  status: complete
  group: 8. Client Access & Protocols
  category: SQL & Multi-Protocol Client Access
  priority: SHOULD
  type: behavioral
  description: SQLAlchemy dialect for Provisa. `create_engine("provisa+http://user:password@host:8001")`.
    Dialect maps SQLAlchemy Core expressions to Provisa SQL. `engine.connect()` returns a DB-API 2.0 connection.
    `pandas.read_sql(query, engine)` works out of the box. `inspector.get_table_names()` returns registered
    table and view names the role may access.
  use_case: SQLAlchemy dialect lets pandas and ORM users read governed data with standard SQL tooling
    and no custom code.
  code:
  - provisa-client/
  - provisa/api/
  tests:
  - tests/integration/test_adbc.py
  - tests/integration/test_sqlalchemy_dialect.py
  - tests/unit/test_client_access.py
  - tests/unit/test_proto_gen.py
  since: 2026-01
  scenario: 'Given a pandas or ORM user creating a SQLAlchemy engine with the Provisa dialect

    When they call read_sql() or inspector.get_table_names()

    Then governed data is returned using standard SQLAlchemy patterns

    '
  integration_test: required
  integration_test_reason: SQLAlchemy dialect engine creation and inspector metadata requires real HTTP
    connection and table discovery.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - app-developer
  competitive_position:
    status: parity
    rationale: SQLAlchemy dialects exist for Trino (trino-sqlalchemy), Snowflake (snowflake-sqlalchemy),
      and BigQuery (sqlalchemy-bigquery). Provisa having one is expected parity, not a differentiator.
- id: REQ-271
  status: complete
  group: 8. Client Access & Protocols
  category: SQL & Multi-Protocol Client Access
  priority: SHOULD
  type: behavioral
  description: ADBC (Arrow Database Connectivity) interface in `provisa-client`. `provisa_client.adbc_connect(url,
    user, password)` returns an ADBC connection using Provisa's Arrow Flight endpoint as transport. Results
    stream as Arrow RecordBatches natively. Compatible with `adbc_driver_manager` and `pandas.read_sql`
    via ADBC. Authenticates via username/password.
  use_case: ADBC interface streams Arrow RecordBatches natively so analytics tools get zero-copy columnar
    data.
  code:
  - provisa-client/
  - provisa/api/
  tests:
  - tests/integration/test_adbc.py
  - tests/integration/test_sqlalchemy_dialect.py
  - tests/unit/test_client_access.py
  - tests/unit/test_proto_gen.py
  since: 2026-01
  scenario: 'Given an analytics tool connecting via adbc_connect()

    When a query executes over Arrow Flight

    Then results stream as Arrow RecordBatches with zero-copy columnar delivery

    '
  integration_test: required
  integration_test_reason: ADBC Arrow Flight streaming with RecordBatches requires gRPC transport and
    real Arrow serialization.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - app-developer
  - ops
  competitive_position:
    status: ahead
    rationale: ADBC (Arrow Database Connectivity) over Arrow Flight as a native client interface is not
      yet offered by Trino, Hasura, or Denodo. Dremio has Arrow Flight but no ADBC client library. Provisa
      is ahead on this emerging standard.
- id: REQ-272
  status: complete
  group: 8. Client Access & Protocols
  category: SQL & Multi-Protocol Client Access
  priority: MUST
  type: constraint
  description: JDBC driver runs arbitrary SQL against registered tables and views with full Stage 2 governance
    — RLS, masking, sampling, and ceilings applied on every query. There is no ungoverned access path.
  use_case: Uniform Stage 2 governance in JDBC ensures arbitrary SQL from BI tools always has RLS and
    masking applied.
  code:
  - provisa-client/
  - provisa/api/
  tests:
  - tests/integration/test_adbc.py
  - tests/integration/test_sqlalchemy_dialect.py
  - tests/unit/test_client_access.py
  - tests/unit/test_proto_gen.py
  since: 2026-01
  scenario: 'Given a BI tool executing arbitrary SQL via JDBC

    When the query runs

    Then RLS, masking, sampling, and ceilings are always applied with no ungoverned access path

    '
  integration_test: required
  integration_test_reason: JDBC Stage 2 governance (RLS/masking/sampling/ceilings) requires real data
    execution path.
  stakeholders:
  - compliance
  - app-developer
  competitive_position:
    status: ahead
    rationale: No ungoverned JDBC access path — Stage 2 RLS/masking/sampling/ceilings applied on every
      arbitrary SQL query — is materially stronger than Denodo (where BI tool users can sometimes access
      base views directly) and Dremio (which applies policies at reflection/dataset level, not uniformly
      at the JDBC wire).
- id: REQ-273
  status: complete
  group: 8. Client Access & Protocols
  category: SQL & Multi-Protocol Client Access
  priority: MUST
  type: constraint
  description: All clients (DB-API, SQLAlchemy, ADBC, JDBC) authenticate via username/password. Server
    assigns role from configured auth provider. No client-supplied role parameter accepted or required.
    This binds the wire protocols as well as the HTTP surface — on Arrow Flight the handshake and the
    ticket both carry a bearer credential, the executing role is derived from the validated identity,
    and a role named by the client is a REQUEST honored only when that identity's own assignments carry
    it; on gRPC the same holds for `x-provisa-role`. A surface that echoes back the role it was handed,
    or that treats the presence of a role name as proof of identity, does not satisfy this requirement.
  use_case: Server-assigned roles prevent clients from escalating privileges by supplying their own role
    parameter.
  code:
  - provisa-client/
  - provisa/api/
  - provisa/api/flight/server.py
  - provisa/grpc/auth.py
  tests:
  - tests/integration/test_adbc.py
  - tests/unit/test_flight_auth.py
  - tests/unit/test_grpc_auth.py
  - tests/integration/test_sqlalchemy_dialect.py
  - tests/unit/test_client_access.py
  - tests/unit/test_client_role.py
  - tests/unit/test_proto_gen.py
  - tests/unit/test_protocol_clients.py
  since: 2026-01
  scenario: 'Given any client authenticating via username/password

    When the server processes the credentials

    Then the role is assigned server-side and no client-supplied role parameter is accepted

    '
  integration_test: required
  integration_test_reason: Server-assigned roles via auth provider requires integration with real identity
    system and role lookup.
  stakeholders:
  - compliance
  - app-developer
  - ops
  competitive_position:
    status: ahead
    rationale: Server-assigned roles with no client-supplied role parameter accepted is a governance-by-design
      constraint that Trino and Dremio do not enforce — both allow clients to pass role or session overrides.
      This prevents privilege escalation at the protocol level.
- id: REQ-274
  status: complete
  group: 8. Client Access & Protocols
  category: SQL & Multi-Protocol Client Access
  priority: SHOULD
  type: behavioral
  description: 'Query language selection is per-call for DB-API and GraphQL clients: pass a GraphQL string
    to execute via Stage 1+2; pass a SQL string to execute via Stage 2 only. ADBC and SQLAlchemy always
    use SQL (Stage 2 only). JDBC uses SQL always.'
  use_case: Per-call query language selection lets the same client send GraphQL or SQL based on what it
    has available.
  code:
  - provisa-client/
  - provisa/api/
  tests:
  - tests/integration/test_adbc.py
  - tests/integration/test_sqlalchemy_dialect.py
  - tests/unit/test_client_access.py
  - tests/unit/test_proto_gen.py
  since: 2026-01
  scenario: 'Given a DB-API or GraphQL client making a call

    When a GraphQL string is passed it executes via Stage 1+2; when SQL is passed it uses Stage 2 only

    Then ADBC, SQLAlchemy, and JDBC always use SQL via Stage 2 only

    '
  integration_test: not_required
  integration_test_reason: Per-call query language selection logic is routing decision; individual paths
    (Stage 1+2, Stage 2) tested separately.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - app-developer
  competitive_position:
    status: ahead
    rationale: Per-call query language selection (GraphQL routes through Stage 1+2; SQL routes through
      Stage 2 only) with the same client is not offered by Hasura, Trino, or Dremio — each provides a
      single query language per connection type.
- id: REQ-288
  status: complete
  group: 8. Client Access & Protocols
  category: Automatic Persisted Queries (APQ)
  priority: SHOULD
  type: behavioral
  description: Provisa implements the Apollo APQ wire protocol (GraphQL over HTTP with `extensions.persistedQuery.sha256Hash`).
    Client sends hash only; if server has it, executes without the query text. If server does not have
    it, returns `PersistedQueryNotFound`; client resends with full query text and hash; server stores
    and executes. Standard Apollo client behaviour works without modification.
  use_case: APQ wire protocol support lets Apollo clients reduce bandwidth by sending only a hash after
    the first request.
  code:
  - provisa/apq/
  tests:
  - tests/integration/test_abac_hook_endpoint.py
  - tests/integration/test_apq_integration.py
  - tests/unit/pgwire/test_wire_protocol.py
  - tests/unit/test_apq.py
  - tests/unit/test_registry_removed.py
  since: 2026-01
  scenario: "Given an Apollo client sending only a hash\nWhen the server has the query cached it executes\
    \ immediately; when not it returns\n  PersistedQueryNotFound\nThen the client resends with full text,\
    \ server stores and executes without modification\n"
  integration_test: required
  integration_test_reason: Apollo APQ wire protocol with hash/query text exchange requires HTTP request/response
    behavior validation.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - app-developer
  competitive_position:
    status: parity
    rationale: Apollo APQ wire protocol is supported by Hasura and any Apollo Server-based GraphQL endpoint.
      For GraphQL platforms this is standard; for a data federation layer it is mildly differentiating
      but Hasura already has it.
- id: REQ-289
  status: complete
  group: 8. Client Access & Protocols
  category: Automatic Persisted Queries (APQ)
  priority: SHOULD
  type: structural
  description: 'APQ cache stored in Redis (existing `cache.redis_url`). TTL configurable via `apq.ttl`
    (default: 24h). Cache miss on cold start is expected and handled by the standard APQ retry flow —
    no preloading required.'
  use_case: Redis APQ cache with configurable TTL reuses query text across process restarts without preloading.
  code:
  - provisa/apq/
  tests:
  - tests/integration/test_apq_integration.py
  - tests/integration/test_cache_store.py
  - tests/unit/test_apq.py
  - tests/unit/test_cache_store.py
  - tests/unit/test_config_reload.py
  since: 2026-01
  integration_test: required
  integration_test_reason: APQ Redis cache TTL and cold-start behavior requires real Redis backend and
    cache hit/miss verification.
  stakeholders:
  - ops
  competitive_position:
    status: parity
    rationale: Redis-backed APQ cache with configurable TTL is the standard Apollo APQ implementation
      pattern; Hasura uses the same approach. No meaningful competitive distinction.
- id: REQ-290
  status: complete
  group: 8. Client Access & Protocols
  category: Automatic Persisted Queries (APQ)
  priority: SHOULD
  type: behavioral
  description: APQ applies to any query the authenticated caller's rights permit. APQ is fully automatic
    — any successfully executed query is registered in the APQ cache and reusable by hash on subsequent
    calls. No steward involvement.
  use_case: Automatic APQ registration lets clients benefit from hash-based execution with no config.
  code:
  - provisa/apq/
  tests:
  - tests/unit/test_apq.py
  - tests/integration/test_apq_integration.py
  since: 2026-01
  scenario: 'Given an authenticated caller executing any permitted query

    When the query succeeds

    Then it is automatically registered in the APQ cache and reusable by hash with no steward action

    '
  integration_test: required
  integration_test_reason: Automatic APQ registration for queries requires end-to-end query execution
    and cache storage.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - app-developer
  competitive_position:
    status: parity
    rationale: Automatic APQ registration for all executed queries (no steward involvement) is consistent
      with the Apollo APQ spec behavior; Hasura behaves identically. This is expected for any Apollo APQ
      implementation.
- id: REQ-291
  status: complete
  group: 8. Client Access & Protocols
  category: Automatic Persisted Queries (APQ)
  priority: MUST
  type: constraint
  description: APQ registration is gated only by the standard rights + Stage 2 governance check. A query
    referencing tables or columns the caller's rights do not permit is rejected at the governance check
    — before it can be registered in the APQ cache. Only queries that pass governance can be cached.
  use_case: Rights-gated APQ registration prevents hash-only execution of queries the caller is not permitted
    to run.
  code:
  - provisa/apq/
  tests:
  - tests/unit/test_apq.py
  - tests/integration/test_apq_integration.py
  since: 2026-01
  scenario: 'Given a caller submitting a query referencing tables or columns beyond their rights

    When the governance check runs

    Then the query is rejected before APQ cache registration

    '
  integration_test: required
  integration_test_reason: Rights-gated APQ cache requires real governance check preventing unauthed query
    hashing.
  stakeholders:
  - compliance
  competitive_position:
    status: ahead
    rationale: Gating APQ cache registration on the governance check — so only rights-permitted queries
      can be hash-registered — is a governance-first constraint not present in Hasura's APQ implementation,
      which caches queries regardless of role at registration time.
- id: REQ-293
  status: complete
  group: 8. Client Access & Protocols
  category: JDBC/ODBC Integration
  priority: SHOULD
  type: behavioral
  description: JDBC driver transport via Arrow Flight — connect to Provisa's existing Flight server (`grpc://host:8815`)
    for streaming query results. Arrow record batches stream from the first row with backpressure, zero
    serialization overhead, and no full-result buffering. Flight is used automatically when the server
    is reachable; falls back to HTTP silently if not. No user configuration required. The Flight ticket
    carries the SQL or GraphQL query + role + variables as JSON; Stage 2 governance applies uniformly.
  use_case: JDBC transport via Arrow Flight streams results with backpressure so BI tools handle large
    result sets safely.
  code:
  - provisa-client/
  tests:
  - tests/e2e/test_grpc_query.py
  - tests/unit/test_drivers.py
  - tests/integration/test_client_access_integration.py
  since: 2026-01
  scenario: 'Given a JDBC client connected to Provisa

    When the Flight server is reachable

    Then results stream as Arrow record batches with backpressure; falls back to HTTP silently if not

    '
  integration_test: required
  integration_test_reason: JDBC Arrow Flight transport with backpressure and fallback requires real gRPC
    endpoint and HTTP fallback path.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - app-developer
  - ops
  competitive_position:
    status: ahead
    rationale: Automatic JDBC transport via Arrow Flight with silent HTTP fallback is not offered by any
      competitor's JDBC driver — Trino, Snowflake, and Dremio JDBC drivers use their own binary or HTTP/2
      protocols, not Arrow Flight. This gives Provisa's JDBC driver zero-copy streaming capability unique
      in the space.
- id: REQ-398
  status: complete
  superseded_by: REQ-392
  group: 8. Client Access & Protocols
  category: API & Integration
  priority: SHOULD
  type: behavioral
  description: The `/data/graph-schema` REST endpoint must expose `pk_columns` (list of column names per
    node label) so the UI can determine exclusion eligibility.
  use_case: Schema endpoint pk_columns expose lets UI conditionally enable/disable "Exclude from query"
    per node.
  code:
  - provisa/api/rest/cypher_router.py
  tests:
  - tests/unit/test_graph_schema.py
  - tests/integration/test_client_access_integration.py
  since: 2026-01
  scenario: 'Given the UI requesting /data/graph-schema

    When the endpoint responds

    Then pk_columns are included per node label so the UI can determine exclusion eligibility

    '
  integration_test: required
  integration_test_reason: /data/graph-schema pk_columns exposure requires real schema compilation and
    endpoint response validation.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - app-developer
  competitive_position:
    status: neutral
    rationale: ''
- id: REQ-405
  status: complete
  group: 8. Client Access & Protocols
  category: API & Integration
  priority: SHOULD
  type: ui
  description: '`graphql_api` and `graphql_remote` source sub-types are collapsed into a single `graphql`
    type in the SourcesPage UI; `grpc_api` and `grpc_remote` are collapsed into a single `grpc` type.'
  use_case: Collapsing API/remote variants eliminates redundant form paths and simplifies steward source
    registration.
  code:
  - provisa-ui/src/pages/SourcesPage.tsx
  tests:
  - tests/integration/test_client_access_integration.py
  - tests/integration/test_graphql_remote_source.py
  - tests/unit/test_admin_requirements.py
  since: 2026-01
  integration_test: not_required
  integration_test_reason: UI form collapsing of source type variants is presentation logic; source registration
    behavior tested separately.
  e2e: true
  e2e_reason: The SourcesPage source-type collapse (graphql_api+graphql_remote -> graphql, grpc_api+grpc_remote
    -> grpc) is browser-rendered form logic; only a browser test can verify the collapsed radio/select
    renders correctly and the correct variant is submitted.
  stakeholders:
  - app-developer
  competitive_position:
    status: neutral
    rationale: ''
- id: REQ-406
  status: complete
  group: 8. Client Access & Protocols
  category: API & Integration
  priority: SHOULD
  type: ui
  description: OpenAPI source registration supports an inline spec editor (monospace textarea) as an alternative
    to providing a file path or URL; the mode is selected via a radio toggle "Spec path / URL" vs "Write
    spec inline". Inline content is sent as `spec_content`; file/URL path is sent as `spec_path`.
  use_case: Inline spec editing lets stewards paste a spec directly into the UI without first writing
    it to a file.
  code:
  - provisa-ui/src/pages/SourcesPage.tsx
  - provisa/api/admin/openapi_router.py
  tests:
  - tests/unit/test_admin_requirements.py
  - tests/integration/test_client_access_integration.py
  since: 2026-01
  integration_test: not_required
  integration_test_reason: Inline spec editor UI toggle is form control logic; backend spec parsing tested
    separately.
  e2e: true
  e2e_reason: The inline spec editor radio toggle and monospace textarea are browser- rendered UI controls;
    only a browser test can verify the toggle switches modes, the textarea appears, and inline content
    is sent as spec_content.
  stakeholders:
  - app-developer
  competitive_position:
    status: ahead
    rationale: Inline OpenAPI spec editor in the source registration UI lowers the barrier to API source
      registration compared to Hasura's file-path-only approach.
- id: REQ-407
  status: complete
  group: 8. Client Access & Protocols
  category: API & Integration
  priority: SHOULD
  type: behavioral
  description: 'OpenAPI source backend accepts optional `spec_content: str` on `OpenAPIRegisterRequest`
    and `OpenAPIPreviewRequest`; when provided it is parsed (YAML then JSON fallback) and used in place
    of loading from disk; `path` is stored as `":inline:"` sentinel.'
  use_case: Backend inline-spec support decouples OpenAPI registration from file-system access on the
    server.
  code:
  - provisa/api/admin/openapi_router.py
  tests:
  - tests/unit/test_admin_requirements.py
  - tests/integration/test_client_access_integration.py
  since: 2026-01
  scenario: 'Given a registration request with spec_content provided

    When the backend processes the request

    Then the inline spec is parsed (YAML then JSON fallback) and path is stored as ":inline:"

    '
  integration_test: required
  integration_test_reason: 'OpenAPI backend inline spec_content parsing and :inline: sentinel storage
    requires real source registration flow.'
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - app-developer
  competitive_position:
    status: neutral
    rationale: ''
- id: REQ-408
  status: complete
  group: 8. Client Access & Protocols
  category: API & Integration
  priority: SHOULD
  type: behavioral
  description: 'OpenAPI operations can carry `x-provisa-kind: query` or `x-provisa-kind: mutation` extension
    to override the GET-heuristic that classifies POST operations as mutations by default. Allows POST-as-read
    endpoints to be exposed as GraphQL queries.'
  use_case: '`x-provisa-kind` override lets stewards correctly classify non-standard POST read operations
    without changing the upstream API.'
  code:
  - provisa/openapi/mapper.py
  tests:
  - tests/unit/test_admin_requirements.py
  - tests/integration/test_client_access_integration.py
  since: 2026-01
  scenario: 'Given an OpenAPI operation with x-provisa-kind: query on a POST endpoint

    When the mapper processes the spec

    Then the POST operation is exposed as a GraphQL query instead of a mutation

    '
  integration_test: required
  integration_test_reason: x-provisa-kind extension override for operation classification requires real
    OpenAPI parsing and GraphQL generation.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - app-developer
  competitive_position:
    status: ahead
    rationale: An `x-provisa-kind` OpenAPI extension that overrides the GET/POST heuristic for GraphQL
      operation classification is a Provisa-specific extension not present in Hasura's OpenAPI import
      or Trino's REST catalog. It enables stewards to correctly federate non-standard REST APIs without
      modifying the upstream spec.
- id: REQ-527
  status: complete
  group: 8. Client Access & Protocols
  category: pgwire Server
  priority: SHOULD
  type: behavioral
  description: pgwire server is disabled by default and starts only when `PROVISA_PGWIRE_PORT` environment
    variable is set to a non-zero integer. Binds to `0.0.0.0` on the configured port.
  use_case: Disabled-by-default pgwire prevents accidental exposure of the PostgreSQL wire protocol endpoint
    when it is not needed.
  code:
  - provisa/api/app.py
  tests:
  - tests/integration/test_pgwire_integration.py
  - tests/unit/pgwire/test_wire_protocol.py
  - tests/unit/test_pgwire_requirements.py
  since: 2026-01
  scenario: "Given PROVISA_PGWIRE_PORT is not set or is zero\nWhen the server starts\nThen the pgwire\
    \ listener does not bind; when the variable is set to a non-zero integer it binds\n  to 0.0.0.0\n"
  integration_test: required
  integration_test_reason: pgwire server startup gated by PROVISA_PGWIRE_PORT requires testing actual
    port binding and startup behavior.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - ops
  competitive_position:
    status: ahead
    rationale: A native pgwire (PostgreSQL wire protocol) listener built into a data federation platform
      is not available in Trino/Presto, Hasura, or Dremio. Denodo has a JDBC layer but no pgwire endpoint.
      This lets any PostgreSQL-compatible tool (psql, DBeaver, Tableau) connect to Provisa without a special
      driver.
- id: REQ-529
  status: complete
  group: 8. Client Access & Protocols
  category: pgwire Server
  priority: MUST
  type: behavioral
  description: 'pgwire authentication routes through the same pluggable AuthProvider contract as every
    other surface — no provider-name allowlist. PG auth type 3 (cleartext password) carries the credential;
    what the password field holds depends on the configured provider. Trust mode (`provider: none`): username
    becomes role_id, password ignored. `simple` and `basic`: password verified against the provider''s
    credential store. OIDC-family providers (`oidc`, `oauth`, `keycloak`, `firebase`): the password is
    a bearer token verified through `AuthProvider.validate_token`. A personal access token is accepted
    as the password under any provider. An unconfigured or failing provider returns a FATAL 28P01; it
    never falls through to trust.'
  use_case: A DBA connecting over psql or DBeaver authenticates against the same identity source as the
    REST and GraphQL surfaces, so credentials, revocation, and audit have one home rather than a pgwire-specific
    exception. The wire protocol carries a credential, not a redirect flow, so no provider is structurally
    incompatible with it.
  code:
  - provisa/pgwire/server.py
  tests:
  - tests/integration/test_pgwire_integration.py
  - tests/unit/pgwire/test_wire_protocol.py
  - tests/unit/test_pgwire_requirements.py
  since: 2026-01
  scenario: 'Given pgwire is launched with provider `none`

    When a client connects with any password

    Then the username becomes the role_id and the session is established


    Given pgwire is launched with a credential provider (`simple` or `basic`)

    When a client presents a valid password for a known user

    Then the credential is verified through the provider and a role is resolved


    Given pgwire is launched with an OIDC-family provider

    When a client presents a bearer token as the password

    Then the token is validated through AuthProvider.validate_token and a role is resolved


    Given any configured provider and an invalid credential

    When authentication is attempted

    Then a FATAL 28P01 is returned and no session is established

    '
  integration_test: required
  integration_test_reason: pgwire PG auth type 3 with trust/simple provider validation requires real wire
    protocol handshake.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - compliance
  - ops
  competitive_position:
    status: parity
    rationale: Standard PostgreSQL cleartext/bcrypt auth over pgwire is expected for any Postgres-compatible
      wire protocol implementation.
- id: REQ-530
  status: complete
  group: 8. Client Access & Protocols
  category: pgwire Server
  priority: SHOULD
  type: behavioral
  description: pgwire TLS is enabled by setting `PROVISA_PGWIRE_CERT` and `PROVISA_PGWIRE_KEY` to PEM
    certificate and key paths. When both are set the server wraps connections in TLS. When absent the
    server replies `N` to SSL negotiation requests.
  use_case: Optional TLS for pgwire allows encrypted connections in production without requiring TLS in
    local development.
  code:
  - provisa/pgwire/server.py
  tests:
  - tests/integration/test_pgwire_integration.py
  - tests/unit/pgwire/test_wire_protocol.py
  - tests/unit/test_pgwire_requirements.py
  since: 2026-01
  scenario: 'Given PROVISA_PGWIRE_CERT and PROVISA_PGWIRE_KEY are set

    When a client connects

    Then the connection is wrapped in TLS; when absent the server replies N to SSL negotiation

    '
  integration_test: required
  integration_test_reason: pgwire TLS negotiation via cert/key PEM files requires real SSL/TLS socket
    behavior.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - compliance
  - ops
  competitive_position:
    status: parity
    rationale: TLS support via cert/key env vars is standard operational practice.
- id: REQ-532
  status: complete
  group: 8. Client Access & Protocols
  category: pgwire Server
  priority: SHOULD
  type: behavioral
  description: Queries against `information_schema` and `pg_catalog` are intercepted and answered from
    an in-memory DuckDB database built per request from the role's compilation context — no Trino round-trip
    occurs. Intercepted tables include `pg_namespace`, `pg_class`, `pg_attribute`, `pg_type`, `pg_constraint`,
    `pg_roles`, `pg_settings`, `pg_stat_activity`, `schemata`, `tables`, `columns`, `views`, `table_constraints`,
    `key_column_usage`, `referential_constraints`, and others. Stubs return zero rows.
  use_case: Local catalog intercept allows BI tools (DBeaver, Tableau, JDBC schema browsers) to populate
    their schema tree from `pg_catalog` without generating Trino queries.
  code:
  - provisa/pgwire/catalog.py
  tests:
  - tests/integration/test_pgwire_integration.py
  - tests/unit/pgwire/test_catalog.py
  - tests/unit/pgwire/test_phase3.py
  - tests/unit/pgwire/test_phase4.py
  - tests/unit/pgwire/test_wire_protocol.py
  - tests/unit/test_pgwire_requirements.py
  since: 2026-01
  scenario: 'Given a BI tool querying information_schema or pg_catalog via pgwire

    When the query is intercepted

    Then it is answered from an in-memory DuckDB built from the role''s compilation context

    '
  integration_test: required
  integration_test_reason: pg_catalog/information_schema interception with in-memory DuckDB requires BI
    tool schema discovery via pgwire protocol.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - app-developer
  - ops
  competitive_position:
    status: ahead
    rationale: Intercepting pg_catalog and information_schema queries and answering them from an in-memory
      DuckDB built from the role's compilation context — with role-filtered visibility — is not done by
      any competitor. Trino has a pseudo-catalog but it is not role-scoped; Denodo does not expose pgwire
      at all.
- id: REQ-538
  status: complete
  group: 8. Client Access & Protocols
  category: JDBC/ODBC Integration
  priority: SHOULD
  type: structural
  description: 'The auto-generated `.proto` file maps Provisa/Trino column types to protobuf scalar types:
    `integer → int32`, `bigint → int64`, `varchar → string`, `decimal → double`, `boolean → bool`, `timestamp
    → google.protobuf.Timestamp`. Each registered table produces one proto `message`. Relationships between
    tables produce nested message fields.'
  use_case: Documented proto type mapping lets gRPC clients generate correct typed stubs without manual
    type translation.
  code:
  - provisa/grpc/proto_gen.py
  tests:
  - tests/unit/test_grpc_requirements.py
  since: 2026-01
  integration_test: not_required
  integration_test_reason: Proto type mapping documentation is schema generation logic; type conversion
    tested separately.
  stakeholders:
  - app-developer
  competitive_position:
    status: neutral
    rationale: ''
- id: REQ-579
  status: complete
  group: 8. Client Access & Protocols
  category: pgwire Server
  priority: SHOULD
  type: behavioral
  description: The pgwire server reports server version `14.0.provisa` to connecting clients. Tools that
    gate features on the PostgreSQL version number behave as though connected to PostgreSQL 14.
  use_case: Reporting a stable PostgreSQL version ensures JDBC drivers, DBeaver, and other tools that
    gate features on version behave predictably.
  code:
  - provisa/pgwire/server.py
  - provisa/pgwire/catalog.py
  tests:
  - tests/integration/test_pgwire_integration.py
  - tests/unit/pgwire/test_wire_protocol.py
  - tests/unit/test_pgwire_requirements.py
  since: 2026-01
  scenario: 'Given a client connecting to pgwire

    When the server reports its version

    Then `14.0.provisa` is returned so tools behave as though connected to PostgreSQL 14

    '
  integration_test: required
  integration_test_reason: pgwire version reporting (14.0.provisa) requires real wire protocol startup
    response to clients.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - app-developer
  - ops
  competitive_position:
    status: parity
    rationale: Reporting server version 14.0 for PostgreSQL wire compatibility is standard practice for
      pg-compatible systems (e.g., CockroachDB, AlloyDB).
- id: REQ-580
  status: complete
  group: 8. Client Access & Protocols
  category: pgwire Server
  priority: SHOULD
  type: behavioral
  description: pgwire simple-query protocol supports multi-statement queries. Semicolon-separated statements
    are split and executed sequentially within a single simple-query message.
  use_case: Multi-statement simple queries allow JDBC tools and psql scripts that send batched DDL or
    DML to work without modification.
  code:
  - provisa/pgwire/server.py
  tests:
  - tests/integration/test_pgwire_integration.py
  - tests/unit/pgwire/test_wire_protocol.py
  - tests/unit/test_pgwire_requirements.py
  since: 2026-01
  scenario: 'Given a JDBC tool or psql script sending semicolon-separated statements in a single message

    When the pgwire server receives the message

    Then statements are split and executed sequentially

    '
  integration_test: required
  integration_test_reason: pgwire multi-statement simple-query execution requires real wire protocol parsing
    and sequential execution.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - app-developer
  competitive_position:
    status: parity
    rationale: Multi-statement simple-query support is standard in PostgreSQL wire protocol implementations;
      Buenavista and similar pgwire shims all handle this. No meaningful competitive dimension.
- id: REQ-581
  status: complete
  group: 8. Client Access & Protocols
  category: pgwire Server
  priority: SHOULD
  type: behavioral
  description: Parameterized queries using `$1`, `$2`, ... positional parameters are supported in both
    simple-query and extended-query (Bind/Execute) protocol modes. Parameters are substituted as SQL literals
    before execution — the upstream engine never sees a prepared statement.
  use_case: Parameterized query support enables standard JDBC/psycopg2 prepared statement patterns. Literal
    substitution avoids prepared-statement protocol complexity with Trino.
  code:
  - provisa/pgwire/server.py
  tests:
  - tests/integration/test_pgwire_integration.py
  - tests/unit/pgwire/test_phase2.py
  - tests/unit/pgwire/test_wire_protocol.py
  - tests/unit/test_pgwire_requirements.py
  since: 2026-01
  scenario: 'Given a JDBC or psycopg2 client using $1, $2 positional parameters

    When the query executes

    Then parameters are substituted as SQL literals before reaching the upstream engine

    '
  integration_test: required
  integration_test_reason: Parameterized $1/$2 support in simple/extended-query protocols requires real
    wire protocol binding and substitution.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - app-developer
  competitive_position:
    status: parity
    rationale: Parameterized query support ($1, $2) via literal substitution is required for JDBC/psycopg2
      compatibility and implemented by all pgwire-compatible endpoints. The literal substitution strategy
      (vs. real prepared statements) is a design choice, not a differentiator.
- id: REQ-582
  status: complete
  group: 8. Client Access & Protocols
  category: pgwire Server
  priority: SHOULD
  type: behavioral
  description: 'pgwire DDL dispatches to one of two paths based on `ddl_catalog`. Trino path (non-registered
    catalog such as `iceberg`, `hive`, `otel`, `results`): supports only `CREATE TABLE` and `CREATE VIEW`;
    ALTER, DROP, and CREATE INDEX are rejected. Direct path (registered source ID): supports full DDL
    including CREATE, ALTER, DROP, indexes, and sequences.'
  use_case: Two DDL paths let stewards write tables to Iceberg for lake storage or directly to registered
    RDBMS sources for full DDL control.
  code:
  - provisa/pgwire/ddl_handler.py
  tests:
  - tests/integration/test_pgwire_integration.py
  - tests/unit/pgwire/test_wire_protocol.py
  - tests/unit/test_pgwire_requirements.py
  since: 2026-01
  scenario: "Given a DDL statement submitted over pgwire\nWhen ddl_catalog is a Trino catalog only CREATE\
    \ TABLE and CREATE VIEW are allowed; when it is a\n  registered source ID full DDL is supported\n\
    Then the statement is dispatched to the correct path\n"
  integration_test: required
  integration_test_reason: pgwire DDL routing (Trino vs. direct) with path-based restrictions requires
    real DDL execution against distinct backends.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - app-developer
  - ops
  competitive_position:
    status: ahead
    rationale: Two DDL dispatch paths — Trino-managed catalogs (Iceberg/Hive) with restricted DDL, and
      direct registered-source DDL with full statement support — is a federation-layer capability not
      present in Trino, Dremio, or Hasura. Competitors do not route DDL based on target catalog type from
      a pgwire endpoint.
- id: REQ-583
  status: complete
  group: 8. Client Access & Protocols
  category: pgwire Server
  priority: SHOULD
  type: behavioral
  description: After DDL execution on either the Trino or direct path, the new table is registered into
    the role's compilation context so it is immediately queryable without a server restart.
  use_case: Immediate post-DDL registration lets analytic workflows create tables and query them in the
    same session.
  code:
  - provisa/pgwire/ddl_handler.py
  tests:
  - tests/integration/test_direct_exec.py
  - tests/integration/test_pgwire_integration.py
  - tests/unit/pgwire/test_wire_protocol.py
  - tests/unit/test_pgwire_requirements.py
  since: 2026-01
  scenario: 'Given a DDL statement that creates a table

    When execution completes

    Then the table is registered into the role''s compilation context and immediately queryable

    '
  integration_test: required
  integration_test_reason: Post-DDL table registration to compilation context requires real DDL execution
    and role context update.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - app-developer
  competitive_position:
    status: ahead
    rationale: Immediate post-DDL registration into the role's compilation context — no server restart
      needed — is a live schema evolution feature not offered by Trino (requires catalog refresh) or Dremio
      (requires dataset promotion). Provisa makes new tables instantly queryable in the same session.
- id: REQ-584
  status: complete
  group: 8. Client Access & Protocols
  category: pgwire Server
  priority: SHOULD
  type: behavioral
  description: DDL write target is resolved from the domain's `ddl_catalog` and `ddl_schema` config fields.
    If `ddl_catalog` is unset, the system defaults to the Iceberg catalog. If `ddl_schema` is unset, it
    defaults to the domain ID. The domain is resolved through the role's `domain_access` list.
  use_case: Domain-driven DDL target resolution ensures DDL writes land in the correct catalog and schema
    without per-query configuration.
  code:
  - provisa/pgwire/ddl_handler.py
  - provisa/api/app.py
  tests:
  - tests/integration/test_pgwire_integration.py
  - tests/unit/pgwire/test_wire_protocol.py
  - tests/unit/test_pgwire_requirements.py
  since: 2026-01
  scenario: 'Given a role with domain_access configured

    When DDL executes without specifying catalog or schema

    Then ddl_catalog defaults to Iceberg and ddl_schema defaults to the domain ID

    '
  integration_test: required
  integration_test_reason: DDL target resolution from domain config with fallback defaults requires real
    domain/role lookup and schema assignment.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - app-developer
  - ops
  competitive_position:
    status: ahead
    rationale: Domain-driven DDL target resolution (ddl_catalog, ddl_schema defaulting from the role's
      domain_access) is a governance-driven DDL routing feature absent from Trino, Dremio, and Hasura
      — all of which require explicit catalog/schema qualification in DDL statements.
- id: REQ-585
  status: complete
  group: 8. Client Access & Protocols
  category: pgwire Server
  priority: SHOULD
  type: behavioral
  description: 'pgwire supports `COPY ... TO STDOUT` (export) and `COPY ... FROM STDIN` (import). COPY
    TO STDOUT supports both table reference and arbitrary subquery forms. Supported formats for COPY output:
    `text` (tab-delimited, default) and `csv`. Binary COPY format is not supported.'
  use_case: COPY protocol support enables bulk data export and import via psql, pgdump-compatible tools,
    and JDBC copy managers without requiring the HTTP API.
  code:
  - provisa/pgwire/copy_handler.py
  tests:
  - tests/e2e/test_output_formats.py
  - tests/integration/test_pgwire_integration.py
  - tests/unit/pgwire/test_wire_protocol.py
  - tests/unit/test_pgwire_requirements.py
  since: 2026-01
  scenario: 'Given a psql or JDBC copy manager issuing COPY TO STDOUT or COPY FROM STDIN

    When the command executes

    Then text and csv formats are supported; binary format is rejected

    '
  integration_test: required
  integration_test_reason: pgwire COPY TO STDOUT/FROM STDIN with text/csv formats requires real wire protocol
    bulk export/import.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - app-developer
  competitive_position:
    status: ahead
    rationale: COPY TO STDOUT and COPY FROM STDIN via pgwire with RLS enforcement at copy time is not
      available in Trino, Dremio, Hasura, or Denodo's client access layers. This enables psql/pg_dump
      workflows against federated data.
- id: REQ-586
  status: complete
  group: 8. Client Access & Protocols
  category: pgwire Server
  priority: MUST
  type: constraint
  description: '`COPY ... FROM STDIN` is restricted to sources with types `postgresql`, `mysql`, `sqlite`,
    or `mariadb`. Attempting COPY FROM against a Trino-only source raises SQLSTATE 42501. If no column
    list is provided, columns are inferred from the registered schema.'
  use_case: Restricting COPY FROM to writable source types prevents accidental writes to read-only Iceberg
    or Trino-federated tables.
  code:
  - provisa/pgwire/copy_handler.py
  tests:
  - tests/integration/test_pgwire_integration.py
  - tests/unit/pgwire/test_wire_protocol.py
  - tests/unit/test_pgwire_requirements.py
  since: 2026-01
  scenario: 'Given a COPY FROM STDIN against a Trino-only source

    When the command is received

    Then SQLSTATE 42501 is raised

    '
  integration_test: required
  integration_test_reason: COPY FROM source-type restriction requires real write validation against writable
    source types.
  stakeholders:
  - compliance
  competitive_position:
    status: ahead
    rationale: Type-safe COPY FROM restriction to writable source types (PostgreSQL, MySQL, SQLite, MariaDB)
      with explicit SQLSTATE on violation is a governance guardrail not present in any competitor's pgwire
      or JDBC interface — competitors either allow writes to all targets or block all writes universally.
- id: REQ-587
  status: complete
  group: 8. Client Access & Protocols
  category: pgwire Server
  priority: SHOULD
  type: behavioral
  description: Transaction control commands (SET, BEGIN, START TRANSACTION, COMMIT, ROLLBACK, SAVEPOINT,
    RELEASE, DISCARD, RESET, DEALLOCATE) are intercepted and return an empty success response. The server
    is stateless with respect to transactions — `in_transaction()` always returns False; there is no transaction
    isolation or rollback support.
  use_case: Accepting but ignoring transaction commands allows JDBC drivers and ORMs that issue implicit
    BEGIN/COMMIT to function without errors, while making the stateless nature explicit.
  code:
  - provisa/pgwire/server.py
  - provisa/pgwire/catalog.py
  tests:
  - tests/integration/test_pgwire_integration.py
  - tests/unit/pgwire/test_phase2.py
  - tests/unit/pgwire/test_server.py
  - tests/unit/pgwire/test_wire_protocol.py
  - tests/unit/test_pgwire_requirements.py
  since: 2026-01
  scenario: 'Given a JDBC driver or ORM issuing BEGIN, COMMIT, or ROLLBACK

    When the command is received by pgwire

    Then an empty success response is returned with no actual transaction state maintained

    '
  integration_test: required
  integration_test_reason: Transaction control interception (BEGIN/COMMIT/ROLLBACK) requires pgwire protocol
    message handling verification.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - app-developer
  - ops
  competitive_position:
    status: parity
    rationale: Accepting and ignoring transaction control commands (BEGIN/COMMIT/ROLLBACK) to satisfy
      JDBC/ORM compatibility is a well-known workaround used by PrestoDB, Trino's JDBC driver, and Buenavista-based
      systems. Not a differentiator.
- id: REQ-588
  status: complete
  group: 8. Client Access & Protocols
  category: pgwire Server
  priority: SHOULD
  type: behavioral
  description: 'The catalog intercept layer handles scalar expression queries: `current_user` / `session_user`
    → authenticated `role_id`; `current_database()` → `"provisa"`; `current_schema()` → `"public"`; `version()`
    → `"PostgreSQL 14.0 on Provisa"`; `pg_backend_pid()` → `0`; `current_setting(...)` → value from a
    fixed settings table; `SHOW <setting>` → value from the same settings table.'
  use_case: Scalar expression intercepts prevent JDBC drivers and ORMs from failing on startup queries
    that probe server identity and settings.
  code:
  - provisa/pgwire/catalog.py
  tests:
  - tests/integration/test_pgwire_integration.py
  - tests/unit/pgwire/test_phase2.py
  - tests/unit/pgwire/test_phase4.py
  - tests/unit/pgwire/test_server.py
  - tests/unit/pgwire/test_wire_protocol.py
  - tests/unit/test_pgwire_requirements.py
  since: 2026-01
  scenario: 'Given a JDBC driver or ORM issuing scalar probes like current_user or version()

    When the catalog intercept layer processes the query

    Then hardcoded values are returned without a Trino round-trip

    '
  integration_test: required
  integration_test_reason: Scalar expression intercepts (current_user, version, etc.) require pgwire protocol
    function interception testing.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - app-developer
  - ops
  competitive_position:
    status: ahead
    rationale: Intercepting scalar session expressions (current_user, version(), pg_backend_pid()) and
      returning role-aware values (current_user → role_id) from the catalog layer is more governance-aware
      than generic pgwire shims like Buenavista, which return static or passthrough values not tied to
      the authenticated role.
- id: REQ-589
  status: complete
  group: 8. Client Access & Protocols
  category: pgwire Server
  priority: SHOULD
  type: behavioral
  description: 'The extended-query protocol (Bind/Execute) supports binary-encoded parameters. Supported
    OIDs for binary decode: 16 (bool), 17 (bytea), 20 (int8), 21 (int2), 23 (int4), 25 (text), 700 (float4),
    701 (float8), 1043 (varchar), 1082 (date), 1114 (timestamp), 1184 (timestamptz), 1700 (numeric), 2950
    (uuid). Unsupported OIDs raise `"Unsupported binary parameter type: <oid>"`. Result columns are also
    sent in binary when the client requests it.'
  use_case: Binary parameter encoding support enables psycopg2 and asyncpg to send typed parameters without
    text serialization, preserving full type fidelity for numeric and temporal values.
  code:
  - vendor/buenavista/buenavista/postgres.py
  tests:
  - tests/integration/test_pgwire_integration.py
  - tests/unit/pgwire/test_wire_protocol.py
  - tests/unit/test_pgwire_requirements.py
  since: 2026-01
  scenario: 'Given psycopg2 or asyncpg sending binary-encoded parameters via Bind/Execute

    When the server decodes them

    Then supported OIDs are decoded correctly; unsupported OIDs raise an error

    '
  integration_test: required
  integration_test_reason: Binary parameter encoding (Bind/Execute OID support) requires pgwire extended-query
    protocol with real binary marshaling.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - app-developer
  competitive_position:
    status: parity
    rationale: Binary parameter encoding for the extended-query protocol (Bind/Execute) with a defined
      set of supported OIDs is standard for any serious pgwire implementation. Buenavista and other shims
      handle this.
  related_reqs:
  - REQ-828
- id: REQ-590
  status: complete
  group: 8. Client Access & Protocols
  category: pgwire Server
  priority: MUST
  type: constraint
  description: 'pgwire enforces hard-coded timeouts: DDL operations time out after 60 seconds; query operations
    time out after 120 seconds. These are enforced in the handler threads via `future.result(timeout=N)`.'
  use_case: Hard-coded timeouts prevent long-running DDL or query operations from blocking pgwire handler
    threads indefinitely.
  code:
  - provisa/pgwire/ddl_handler.py
  - provisa/pgwire/server.py
  tests:
  - tests/integration/test_pgwire_integration.py
  - tests/unit/pgwire/test_wire_protocol.py
  - tests/unit/test_pgwire_requirements.py
  since: 2026-01
  scenario: 'Given a DDL or query operation running in a pgwire handler thread

    When the operation exceeds 60s (DDL) or 120s (query)

    Then a timeout error is raised via future.result(timeout=N)

    '
  integration_test: required
  integration_test_reason: Hard-coded timeout enforcement on DDL/query operations requires real thread
    execution and timeout verification.
  stakeholders:
  - ops
  competitive_position:
    status: parity
    rationale: Timeout enforcement on DDL and query operations is basic operational hygiene present in
      Trino (query.max-run-time), Snowflake (STATEMENT_TIMEOUT_IN_SECONDS), and all production federation
      engines.
- id: REQ-606
  status: complete
  group: 8. Client Access & Protocols
  category: SQL & Multi-Protocol Client Access
  priority: SHOULD
  type: behavioral
  description: 'ProvisaClient accepts a bearer token (`token` parameter) as an alternative to username/password
    for authentication. When a token is present, it is sent as `Authorization: Bearer <token>` on every
    request. When omitted, the client relies on password-based login or no auth (unsecured deployment).'
  use_case: Bearer token support lets ProvisaClient integrate with identity systems that issue tokens
    directly, without requiring a Provisa-local username/password pair.
  code:
  - provisa-client/provisa_client/client.py
  tests:
  - provisa-client/tests/test_client.py
  - tests/unit/test_client_access.py
  - tests/unit/test_proto_gen.py
  - tests/integration/test_client_access_integration.py
  since: 2026-01
  scenario: 'Given a ProvisaClient instantiated with a token parameter

    When a request is made

    Then Authorization: Bearer <token> is sent on every request

    '
  integration_test: required
  integration_test_reason: Bearer token authentication in ProvisaClient requires HTTP Authorization header
    transport and server validation.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - app-developer
  - compliance
  competitive_position:
    status: parity
    rationale: Bearer token authentication in a client library is standard across Snowflake, Dremio, and
      Hasura clients. No meaningful competitive dimension.
- id: REQ-607
  status: complete
  group: 8. Client Access & Protocols
  category: SQL & Multi-Protocol Client Access
  priority: SHOULD
  type: behavioral
  description: 'ProvisaClient error contract: `query()` raises `httpx.HTTPStatusError` on HTTP-level errors
    (4xx/5xx). `query_df()` raises `RuntimeError` when the GraphQL response body contains an `errors`
    field, regardless of HTTP status.'
  use_case: Distinct error types for transport errors vs. GraphQL errors let callers handle network failures
    and schema/data errors with separate recovery paths.
  code:
  - provisa-client/provisa_client/client.py
  tests:
  - provisa-client/tests/test_client.py
  - tests/unit/test_client_access.py
  - tests/unit/test_proto_gen.py
  - tests/integration/test_client_access_integration.py
  since: 2026-01
  scenario: "Given a ProvisaClient caller\nWhen query() receives a 4xx/5xx response it raises httpx.HTTPStatusError;\
    \ when query_df()\n  receives a GraphQL errors field it raises RuntimeError\nThen callers can handle\
    \ transport and schema errors separately\n"
  integration_test: required
  integration_test_reason: Error contract distinction (HTTPStatusError vs. GraphQL errors) requires real
    HTTP response and error body validation.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - app-developer
  competitive_position:
    status: parity
    rationale: Distinct error types for transport (HTTP 4xx/5xx) vs. application-level (GraphQL errors)
      errors are standard client library design; Hasura, Apollo Client, and similar clients all distinguish
      these layers.
- id: REQ-608
  status: complete
  superseded_by: REQ-711
  group: 8. Client Access & Protocols
  category: SQL & Multi-Protocol Client Access
  priority: SHOULD
  type: structural
  description: The ADBC interface (`adbc_connect`) connects to the Arrow Flight server on port 8815. The
    Flight port is hardcoded in `adbc_connect` and is not configurable via any parameter — callers must
    run the Flight server on port 8815 or reconfigure the server.
  use_case: Hardcoded Flight port in adbc_connect simplifies the ADBC connection signature; the server
    config is the single place to change the port.
  code:
  - provisa-client/provisa_client/adbc.py
  tests:
  - provisa-client/tests/test_adbc.py
  - tests/unit/test_client_access.py
  - tests/unit/test_proto_gen.py
  - tests/integration/test_client_access_integration.py
  since: 2026-01
  integration_test: required
  integration_test_reason: Hardcoded Flight port 8815 in adbc_connect requires real gRPC connection to
    verify port behavior.
  stakeholders:
  - app-developer
  - ops
  competitive_position:
    status: gap
    rationale: Hardcoding the Flight port (8815) in adbc_connect with no configurable parameter is a deployment
      inflexibility. Dremio's Flight client and DuckDB's ADBC drivers allow the port to be specified at
      connection time, which Provisa does not currently support.
- id: REQ-614
  status: complete
  group: 8. Client Access & Protocols
  category: pgwire
  priority: MUST
  type: constraint
  description: The pgwire listener accepts only SQL statements. GraphQL and Cypher query strings are not
    parsed or executed over the pgwire protocol.
  use_case: SQL-only restriction on pgwire keeps the protocol surface minimal and avoids ambiguity between
    query languages on a single connection.
  code:
  - provisa/pgwire/_pipeline.py
  tests:
  - tests/integration/test_pgwire_integration.py
  - tests/unit/pgwire/test_wire_protocol.py
  - tests/unit/test_cypher_graph_fns.py
  - tests/unit/test_pgwire_requirements.py
  since: 2026-01
  scenario: 'Given a client submitting a GraphQL or Cypher string over pgwire

    When the listener receives the statement

    Then it is rejected; only SQL statements are accepted

    '
  integration_test: required
  integration_test_reason: pgwire SQL-only restriction requires wire protocol statement parsing and rejection
    verification.
  stakeholders:
  - app-developer
  competitive_position:
    status: neutral
    rationale: ''
- id: REQ-615
  status: complete
  group: 8. Client Access & Protocols
  category: pgwire
  priority: MUST
  type: constraint
  description: The pgwire listener does not support DML mutations (INSERT, UPDATE, DELETE). SQL statements
    of these types are not routed to a write path. This restriction covers DML statements only — COPY
    ... FROM STDIN bulk import and DDL are a separate path gated by the `ddl` capability (REQ-585, REQ-616).
  use_case: Read-only restriction on pgwire keeps the connection appropriate for analytics tools and prevents
    accidental data modification via SQL clients.
  code:
  - provisa/pgwire/_pipeline.py
  tests:
  - tests/integration/test_pgwire_integration.py
  - tests/unit/pgwire/test_wire_protocol.py
  - tests/unit/test_pgwire_requirements.py
  since: 2026-01
  scenario: 'Given a SQL client submitting INSERT, UPDATE, or DELETE over pgwire

    When the listener receives the statement

    Then the statement is not routed to any write path

    '
  integration_test: required
  integration_test_reason: pgwire read-only restriction on DML requires wire protocol statement type validation
    and rejection.
  stakeholders:
  - compliance
  competitive_position:
    status: neutral
    rationale: ''
- id: REQ-616
  status: complete
  group: 8. Client Access & Protocols
  category: pgwire
  priority: MUST
  type: constraint
  description: COPY and DDL statements submitted over pgwire require the `ddl` role capability. Roles
    without `ddl` receive a permission error (PG error code 42501). Roles with `ddl` have COPY and DDL
    executed via the DDL handler and COPY handler respectively.
  use_case: Capability-gated DDL and COPY lets power users perform schema operations via psql or DBeaver
    while protecting other roles from accidental destructive statements.
  code:
  - provisa/pgwire/server.py
  - provisa/pgwire/ddl_handler.py
  - provisa/pgwire/copy_handler.py
  tests:
  - tests/integration/test_pgwire_integration.py
  - tests/unit/pgwire/test_wire_protocol.py
  - tests/unit/test_pgwire_requirements.py
  since: 2026-01
  scenario: 'Given a role without the ddl capability submitting COPY or DDL over pgwire

    When the statement is received

    Then PG error code 42501 is returned

    '
  integration_test: required
  integration_test_reason: DDL/COPY capability-gating requires real role permission check before handler
    dispatch.
  stakeholders:
  - compliance
  - ops
  competitive_position:
    status: ahead
    rationale: DDL capability gating over pgwire (COPY/DDL require ddl role capability) applies Provisa's
      governance model to the SQL wire protocol — not available in Hasura or Dremio.
- id: REQ-617
  status: complete
  group: 8. Client Access & Protocols
  category: gRPC
  priority: MUST
  type: behavioral
  description: Role selection on every Provisa gRPC RPC is via the `x-provisa-role` metadata key. Missing
    or unrecognised role metadata causes the call to be rejected with UNAUTHENTICATED. Streaming query
    RPCs emit one response message per result row; mutation RPCs are unary. On a secured deployment the
    key selects among the roles the caller's validated identity already holds, and nothing more — a server
    interceptor validates the bearer credential in `authorization` (a provider token or a personal access
    token) ahead of every RPC on the server, reflection included, and derives the executing role from
    that identity per REQ-273. A key naming a role the identity does not hold is rejected with PERMISSION_DENIED
    rather than granted.
  use_case: Metadata-based role selection lets a single gRPC channel serve multiple roles by setting the
    key per-call without reconnecting.
  code:
  - provisa/grpc/server.py
  - provisa/grpc/auth.py
  tests:
  - tests/e2e/test_grpc_query.py
  - tests/unit/test_grpc_requirements.py
  - tests/unit/test_grpc_auth.py
  - tests/integration/test_client_access_integration.py
  since: 2026-01
  scenario: 'Given a gRPC caller omitting or providing an unrecognised x-provisa-role metadata key

    When the RPC is received

    Then the call is rejected with UNAUTHENTICATED

    '
  integration_test: required
  integration_test_reason: gRPC x-provisa-role metadata routing requires real metadata parsing and role
    validation on wire protocol.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - app-developer
  competitive_position:
    status: ahead
    rationale: x-provisa-role metadata key for per-RPC role selection on gRPC with streaming query role
      enforcement is more granular than standard gRPC auth headers.
- id: REQ-172
  status: complete
  group: 9. Live Data & Events
  category: Dataset Change Events
  priority: SHOULD
  type: behavioral
  description: Mutations emit a dataset change event to a Kafka topic — no row-level detail, just `{table,
    source, timestamp}`.
  use_case: Mutation change events let downstream consumers react to data changes without polling the
    source.
  code:
  - provisa/events/
  tests:
  - tests/unit/test_kafka_change_events.py
  - tests/integration/test_live_data_integration.py
  since: 2026-01
  scenario: 'Given a mutation is executed against a registered table

    When the mutation completes

    Then a change event containing table, source, and timestamp is emitted to the configured Kafka topic

    '
  integration_test: required
  integration_test_reason: Kafka topic emission requires real message broker and verification of actual
    payloads sent.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - ops
  - data-engineer
  competitive_position:
    status: parity
    rationale: Dataset change events on mutation are available in Hasura (event triggers) and Debezium-based
      stacks.
- id: REQ-173
  status: complete
  group: 9. Live Data & Events
  category: Dataset Change Events
  priority: MUST
  type: behavioral
  description: Change events fire on the same mutation hook that invalidates cache and marks MVs stale.
  use_case: Change events and cache invalidation fire together, keeping all derived state consistent after
    mutations.
  code:
  - provisa/events/
  tests:
  - tests/unit/test_kafka_change_events.py
  - tests/integration/test_live_data_integration.py
  since: 2026-01
  scenario: 'Given a mutation hook fires after a data change

    When the hook executes

    Then a change event is emitted and cache is invalidated and MVs are marked stale in the same hook

    '
  integration_test: required
  integration_test_reason: Cache invalidation and change event timing require end-to-end verification
    against real cache and mutation hooks.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - ops
  - app-developer
  competitive_position:
    status: parity
    rationale: Cache invalidation and MV staleness marking on mutation is standard in Hasura and materialized
      view systems.
  related_reqs:
  - REQ-826
- id: REQ-174
  status: complete
  group: 9. Live Data & Events
  category: Dataset Change Events
  priority: SHOULD
  type: behavioral
  description: Producers running complex ETL outside Provisa can signal changes via a trivial mutation
    (touch operation).
  use_case: Touch mutations let external ETL pipelines signal Provisa that source data changed without
    a direct DB write.
  code:
  - provisa/events/
  tests:
  - tests/unit/test_kafka_change_events.py
  - tests/integration/test_live_data_integration.py
  since: 2026-01
  scenario: 'Given an external ETL process has modified source data outside Provisa

    When the ETL calls a touch mutation on the relevant table

    Then Provisa fires the mutation hook and emits a change event as if data had changed directly

    '
  integration_test: required
  integration_test_reason: Touch mutation behavior requires real database write and Kafka event verification.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - data-engineer
  - app-developer
  competitive_position:
    status: ahead
    rationale: Touch mutation for external ETL signal is a pragmatic integration pattern not explicitly
      supported in Hasura or Dremio.
- id: REQ-175
  status: complete
  group: 9. Live Data & Events
  category: Dataset Change Events
  priority: MAY
  type: structural
  description: 'Change event topic configurable via `PROVISA_CHANGE_EVENT_TOPIC` (default: `provisa.change-events`).'
  use_case: Configurable change event topic lets operators isolate event streams by environment or team.
  code:
  - provisa/events/
  tests:
  - tests/unit/test_kafka_change_events.py
  - tests/integration/test_live_data_integration.py
  since: 2026-01
  integration_test: not_required
  integration_test_reason: Configuration parsing and default value assignment are pure logic.
  stakeholders:
  - ops
  competitive_position:
    status: neutral
    rationale: ''
- id: REQ-176
  status: complete
  group: 9. Live Data & Events
  category: Kafka Sinks (Table/View Publishing)
  priority: SHOULD
  type: behavioral
  description: Registered tables and views can optionally have a Kafka sink — results published to a topic
    on trigger. Sinks may use change_event, schedule, manual, or poll depending on source capability.
  use_case: Table/view Kafka sinks push governed results to topics so consumers never bypass security.
  code:
  - provisa/kafka/
  tests:
  - tests/unit/test_kafka_sink.py
  - tests/integration/test_kafka_sink.py
  since: 2026-01
  scenario: 'Given a registered table or view has a Kafka sink configured

    When the configured trigger fires

    Then the query results are published to the configured Kafka topic

    '
  integration_test: required
  integration_test_reason: Kafka sink delivery requires real message broker, trigger execution, and verification
    of published results.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - ops
  - data-engineer
  - compliance
  competitive_position:
    status: ahead
    rationale: Kafka sinks on registered tables/views with multiple trigger modes (change_event, schedule,
      manual, poll) is more flexible than Hasura's event trigger model and not native to Dremio or Trino.
- id: REQ-177
  status: complete
  group: 9. Live Data & Events
  category: Kafka Sinks (Table/View Publishing)
  priority: SHOULD
  type: structural
  description: 'Sink triggers: `change_event` (re-run when source table changes), `schedule` (cron/interval),
    `manual` (on-demand), `poll` (watermark-based continuous polling — required when CDC is unavailable).'
  use_case: Flexible sink triggers (CDC, schedule, manual, poll) match delivery to what each source physically
    supports.
  code:
  - provisa/kafka/
  tests:
  - tests/integration/test_kafka_sink.py
  - tests/unit/test_event_triggers.py
  - tests/unit/test_kafka_sink.py
  since: 2026-01
  integration_test: required
  integration_test_reason: Sink trigger behavior (CDC, schedule, manual, poll) requires real event sources
    and timing verification.
  stakeholders:
  - ops
  - data-engineer
  competitive_position:
    status: ahead
    rationale: Four sink trigger modes including watermark-based poll (for CDC-unavailable sources) is
      more operationally complete than Hasura's insert/update/delete-only event triggers.
- id: REQ-178
  status: complete
  group: 9. Live Data & Events
  category: Kafka Sinks (Table/View Publishing)
  priority: MUST
  type: behavioral
  description: Sinks are opt-in per registered table or view, configured by the steward.
  use_case: Opt-in sink configuration prevents accidental data publishing without steward intent.
  code:
  - provisa/kafka/
  tests:
  - tests/unit/test_kafka_sink.py
  - tests/integration/test_kafka_sink.py
  since: 2026-01
  scenario: 'Given a registered table has no Kafka sink configured

    When the table data changes

    Then no Kafka messages are published for that table

    '
  integration_test: not_required
  integration_test_reason: Opt-in sink configuration is a pure config validation and routing rule.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - ops
  competitive_position:
    status: parity
    rationale: Opt-in per-table sink configuration is standard in event-driven data platforms.
- id: REQ-180
  status: complete
  group: 9. Live Data & Events
  category: Kafka Sinks (Table/View Publishing)
  priority: SHOULD
  type: behavioral
  description: Sinks can be added to or removed from a table/view at any time, independently of other
    config.
  use_case: Independent sink attachment lets operators add or remove delivery channels without touching
    other config.
  code:
  - provisa/kafka/
  tests:
  - tests/unit/test_kafka_sink.py
  - tests/integration/test_kafka_sink.py
  since: 2026-01
  scenario: 'Given a table with an existing Kafka sink

    When the steward removes the sink configuration

    Then subsequent triggers produce no Kafka messages and other table config is unchanged

    '
  integration_test: not_required
  integration_test_reason: Adding/removing sink config is pure config mutation with no side effects to
    verify.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - ops
  competitive_position:
    status: neutral
    rationale: ''
- id: REQ-181
  status: complete
  group: 9. Live Data & Events
  category: Kafka Sinks (Table/View Publishing)
  priority: SHOULD
  type: structural
  description: Sink output format is JSON (one message per row, keyed by optional column).
  use_case: JSON-per-row Kafka messages let any downstream consumer deserialize results without custom
    decoders.
  code:
  - provisa/kafka/
  tests:
  - tests/unit/test_kafka_sink.py
  - tests/integration/test_kafka_sink.py
  since: 2026-01
  integration_test: required
  integration_test_reason: JSON message format and keying strategy require real Kafka message inspection.
  stakeholders:
  - data-engineer
  - app-developer
  competitive_position:
    status: parity
    rationale: JSON output format keyed by optional column is standard for Kafka sinks.
- id: REQ-260
  status: complete
  group: 9. Live Data & Events
  category: Subscriptions
  priority: SHOULD
  type: behavioral
  description: 'Polling-based subscription provider for sources without native CDC. A `watermark_column`
    (monotonic timestamp or sequence, e.g. `updated_at`) must be declared on the table config. Without
    a `watermark_column`, poll subscriptions are unavailable for that source and config validation fails
    at startup when poll delivery is configured anyway (absorbs REQ-283). Part of Phase AM (Live Query
    Engine). Note: Poll-based subscriptions cannot detect hard deletes — a deleted row leaves no updated
    watermark value and will not appear in any poll. Applications requiring delete visibility must use
    soft deletes (e.g. a `deleted_at` flag) that update the watermark column; the delete is then visible
    as an update event containing the soft-delete marker.'
  use_case: Poll-based subscriptions enable live delivery for sources that do not support CDC or LISTEN/NOTIFY.
  code:
  - provisa/subscriptions/
  tests:
  - tests/e2e/test_subscriptions.py
  - tests/integration/test_sse_subscriptions.py
  - tests/unit/test_live_engine_full.py
  - tests/unit/test_subscribe.py
  since: 2026-01
  scenario: 'Given a table config declares a watermark_column and a source without native CDC

    When a poll subscription is created for that table

    Then new or updated rows since the last watermark are delivered to the subscriber on each poll interval

    '
  integration_test: required
  integration_test_reason: Watermark-based polling requires real database access, watermark tracking,
    and timing verification.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - ops
  - data-engineer
  competitive_position:
    status: parity
    rationale: Watermark-based polling subscription for sources without CDC is available in Hasura's polling
      subscriptions and Debezium.
- id: REQ-261
  status: complete
  group: 9. Live Data & Events
  category: Subscriptions
  priority: SHOULD
  type: behavioral
  description: Debezium CDC subscription provider for non-PG RDBMS sources (MySQL, MariaDB, SQL Server,
    Oracle). Debezium captures changes from the source's transaction log and publishes to Kafka topics.
    Provisa's Kafka notification provider consumes these CDC events and streams them as SSE subscriptions.
    Requires Kafka + Debezium connector infrastructure.
  use_case: Debezium CDC subscriptions enable real-time events for MySQL, MariaDB, SQL Server, and Oracle
    sources.
  code:
  - provisa/subscriptions/
  tests:
  - tests/e2e/test_subscriptions.py
  - tests/integration/test_debezium_cdc.py
  - tests/integration/test_sse_subscriptions.py
  - tests/unit/test_debezium_provider.py
  - tests/unit/test_subscribe.py
  since: 2026-01
  scenario: "Given a MySQL source is connected via Debezium and Kafka\nWhen a row is inserted, updated,\
    \ or deleted in MySQL\nThen the change is captured by Debezium, published to Kafka, consumed by Provisa,\
    \ and streamed\n  as an SSE event to subscribers\n"
  integration_test: deferred
  integration_test_reason: Debezium CDC requires external Kafka + Debezium connector infrastructure not
    in CI.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - ops
  - data-engineer
  competitive_position:
    status: parity
    rationale: Debezium CDC for non-PG RDBMS is a well-established pattern available in Hasura and standalone
      Kafka Connect setups.
- id: REQ-282
  status: complete
  group: 9. Live Data & Events
  category: Live Query Engine (Unified Subscription & Sink Delivery)
  priority: SHOULD
  type: behavioral
  description: 'A single Live Query Engine powers all poll-based live delivery. It is the common implementation
    for: (a) SSE subscriptions on tables/views without CDC, (b) Kafka sinks on tables/views without CDC.
    Delivery mode (CDC vs poll) is orthogonal to the output mechanism (SSE vs Kafka sink).'
  use_case: Single Live Query Engine powers all poll-based delivery so CDC vs poll and SSE vs sink are
    independent choices.
  code:
  - provisa/live/
  - provisa/subscriptions/
  tests:
  - tests/integration/test_live_sse_integration.py
  - tests/unit/test_live_engine.py
  - tests/unit/test_live_engine_full.py
  since: 2026-01
  scenario: 'Given a table configured for poll-based delivery with both SSE subscription and Kafka sink
    outputs

    When the poll interval fires

    Then the Live Query Engine executes the query once and delivers results to both SSE and Kafka outputs

    '
  integration_test: required
  integration_test_reason: Poll engine powering multiple output types requires end-to-end verification
    of delivery independence.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - ops
  - data-engineer
  - app-developer
  competitive_position:
    status: ahead
    rationale: Unified Live Query Engine powering both SSE subscriptions and Kafka sinks from one poll
      loop avoids duplicated delivery infrastructure — more efficient than Hasura's separate subscription
      and event trigger systems.
- id: REQ-283
  status: complete
  superseded_by: REQ-260
  group: 9. Live Data & Events
  category: Live Query Engine (Unified Subscription & Sink Delivery)
  priority: MUST
  type: constraint
  description: '`watermark_column` is a required config field for any poll-based live delivery. The column
    must be monotonically increasing (e.g. `updated_at`, `created_at`) and is declared on the table/view
    config (REQ-260). Without `watermark_column`, poll delivery is unavailable and config validation fails
    at startup.'
  use_case: Required watermark_column config makes poll delivery explicit and prevents unbounded or duplicate
    event delivery.
  code:
  - provisa/live/
  - provisa/subscriptions/
  tests:
  - tests/integration/test_live_sse_integration.py
  - tests/unit/test_config_validation_edge_cases.py
  - tests/unit/test_live_engine.py
  - tests/unit/test_live_engine_full.py
  since: 2026-01
  integration_test: not_required
  integration_test_reason: Watermark config validation and startup error handling are pure schema validation
    logic.
  stakeholders:
  - ops
  competitive_position:
    status: neutral
    rationale: ''
- id: REQ-285
  status: complete
  superseded_by: REQ-813
  group: 9. Live Data & Events
  category: Live Query Engine (Unified Subscription & Sink Delivery)
  priority: MUST
  type: behavioral
  description: 'Tables declare delivery mode in the subscription/sink config: `delivery: cdc` or `delivery:
    poll`. `cdc` is available for PostgreSQL (LISTEN/NOTIFY), Debezium-connected sources (REQ-261), and
    MongoDB (Change Streams per REQ-258). All other sources — Trino-federated, JDBC with restricted access,
    Kafka topics, API sources — must use `delivery: poll`. Config validation rejects `delivery: cdc` for
    sources that do not support it.'
  use_case: Delivery mode validation at config load time prevents misconfiguring CDC on sources that do
    not support it.
  code:
  - provisa/live/
  - provisa/subscriptions/
  tests:
  - tests/integration/test_live_sse_integration.py
  - tests/unit/test_available_tables.py
  - tests/unit/test_config_validation_edge_cases.py
  - tests/unit/test_live_engine.py
  - tests/unit/test_live_engine_full.py
  since: 2026-01
  scenario: 'Given a Trino-federated source with delivery: cdc in its config

    When Provisa starts up

    Then config validation fails with an error indicating CDC is not supported for that source

    '
  integration_test: not_required
  integration_test_reason: Delivery mode config validation at startup is pure logic and constraint checking.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - ops
  competitive_position:
    status: parity
    rationale: Delivery mode declaration (cdc vs. poll) per table is standard in event-driven platforms.
- id: REQ-286
  status: complete
  group: 9. Live Data & Events
  category: Live Query Engine (Unified Subscription & Sink Delivery)
  priority: SHOULD
  type: behavioral
  description: SSE subscription and Kafka sink are equivalent output mechanisms for a table or view. A
    single live definition may list both `sse_subscription` and `kafka_sink` in its `outputs`. The poll
    engine executes once per interval; each output tracks its own watermark independently in a `live_query_state`
    table (keyed by source name + output_type). A slow Kafka consumer does not block SSE delivery and
    vice versa.
  use_case: Independent watermark tracking per output type ensures a slow Kafka consumer never delays
    SSE subscribers.
  code:
  - provisa/live/
  - provisa/subscriptions/
  tests:
  - tests/integration/test_live_sse_integration.py
  - tests/unit/test_live_engine.py
  - tests/unit/test_live_engine_full.py
  since: 2026-01
  scenario: 'Given a table with both sse_subscription and kafka_sink outputs configured

    When the Kafka consumer falls behind

    Then SSE delivery continues at normal intervals unaffected by the Kafka consumer lag

    '
  integration_test: required
  integration_test_reason: Independent watermark tracking per output type requires real Kafka + SSE clients
    to verify isolation.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - ops
  - data-engineer
  - app-developer
  competitive_position:
    status: ahead
    rationale: A single live definition producing both SSE and Kafka output simultaneously from one poll
      is not available in Hasura or Dremio.
- id: REQ-287
  status: complete
  group: 9. Live Data & Events
  category: Live Query Engine (Unified Subscription & Sink Delivery)
  priority: MUST
  type: structural
  description: 'Live query state persisted in a `live_query_state` PG table: `(source, output_type, last_watermark,
    last_polled_at, status)`. On restart, polling resumes from the last committed watermark — no events
    are replayed or skipped due to process restart. `status` tracks `active`, `paused`, `error`.'
  use_case: Persisted live query state lets poll delivery resume from exactly the last committed watermark
    after a restart.
  code:
  - provisa/live/
  - provisa/subscriptions/
  tests:
  - tests/integration/test_live_sse_integration.py
  - tests/unit/test_live_engine.py
  - tests/unit/test_live_engine_full.py
  since: 2026-01
  integration_test: required
  integration_test_reason: Persisted live query state requires real database table, process restart, and
    watermark resumption verification.
  stakeholders:
  - ops
  - data-engineer
  competitive_position:
    status: parity
    rationale: Watermark persistence in a state table for resume-on-restart is standard in streaming systems
      like Kafka Connect and Flink.
- id: REQ-565
  status: complete
  group: 9. Live Data & Events
  category: Subscriptions
  priority: SHOULD
  type: behavioral
  description: At startup, Provisa idempotently installs `AFTER INSERT OR UPDATE OR DELETE` triggers on
    all registered PostgreSQL subscription tables. Each trigger calls `pg_notify('provisa_{table}', json_build_object('op',
    lower(TG_OP), 'row', ...))` so that raw DML from any writer is picked up by SSE subscriptions.
  use_case: Auto-installed triggers ensure external writes to PostgreSQL tables are captured by SSE subscriptions
    without requiring the writer to call pg_notify manually.
  code:
  - provisa/subscriptions/pg_triggers.py
  tests:
  - tests/e2e/test_subscriptions.py
  - tests/integration/test_sse_subscriptions.py
  - tests/unit/test_event_triggers.py
  - tests/unit/test_subscribe.py
  since: 2026-01
  scenario: 'Given Provisa has started and registered a PostgreSQL subscription table

    When an external process inserts a row directly into the table

    Then the trigger fires pg_notify and the SSE subscriber receives the change event

    '
  integration_test: required
  integration_test_reason: PostgreSQL trigger installation and NOTIFY behavior require real database with
    DDL execution.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - ops
  - data-engineer
  competitive_position:
    status: parity
    rationale: Idempotent AFTER INSERT/UPDATE/DELETE trigger installation for PostgreSQL LISTEN/NOTIFY
      is the standard pattern used by Hasura and others.
  related_reqs:
  - REQ-828
- id: REQ-566
  status: complete
  group: 9. Live Data & Events
  category: Subscriptions
  priority: SHOULD
  type: behavioral
  description: When PostgreSQL trigger installation fails (e.g. insufficient privilege), Provisa logs
    a warning and falls back to watermark-based polling for that table, provided `watermark_column` is
    configured on the table. Tables where installation succeeds are tracked in-memory to select the LISTEN/NOTIFY
    provider path.
  use_case: Graceful fallback to polling prevents a trigger permission failure from making a table entirely
    unsubscribable.
  code:
  - provisa/subscriptions/pg_triggers.py
  tests:
  - tests/e2e/test_subscriptions.py
  - tests/integration/test_live_data_integration.py
  - tests/unit/test_subscribe.py
  since: 2026-01
  scenario: 'Given a PostgreSQL table where Provisa lacks trigger creation privileges

    When Provisa starts up

    Then it logs a warning and uses watermark-based polling for that table instead of LISTEN/NOTIFY

    '
  integration_test: required
  integration_test_reason: Privilege failure handling and fallback to polling require real database permission
    scenarios.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - ops
  - compliance
  competitive_position:
    status: parity
    rationale: Graceful fallback to watermark polling when trigger installation fails is defensive practice
      available in Hasura's subscription model.
- id: REQ-567
  status: complete
  group: 9. Live Data & Events
  category: Subscriptions
  priority: SHOULD
  type: behavioral
  description: When a subscription field selects columns from joined tables via registered relationships,
    the subscription engine collects all physical table names referenced by the join walk and calls `watch_many(all_watch_tables)`
    on the PG notification provider. A change to any joined physical table re-fires the subscription query.
  use_case: Watching all involved physical tables ensures that relationship-traversing subscriptions surface
    changes to nested data without requiring a separate subscription per table.
  code:
  - provisa/api/data/subscription_sse.py
  tests:
  - tests/e2e/test_subscriptions.py
  - tests/integration/test_sse_subscriptions.py
  - tests/unit/test_audit_requirements.py
  - tests/unit/test_live_engine_full.py
  since: 2026-01
  scenario: 'Given a subscription that joins two tables via a registered relationship

    When a row in the joined table changes

    Then the subscription query re-fires and the updated result is streamed to the subscriber

    '
  integration_test: required
  integration_test_reason: Multi-table watching via joins requires real database execution and relationship
    traversal.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - data-engineer
  - app-developer
  competitive_position:
    status: ahead
    rationale: Multi-table watch via joined relationship traversal for subscription change detection is
      more complete than Hasura's single-table subscription model.
- id: REQ-568
  status: complete
  group: 9. Live Data & Events
  category: Subscriptions
  priority: SHOULD
  type: structural
  description: 'Each SSE change event is emitted as a JSON object with the shape `{"event": "<op>", "table":
    "<table>", "row": {...}}` where `op` is one of `insert`, `update`, or `delete`.'
  use_case: Standardized event shape lets clients deserialize change events without custom parsing per
    table.
  code:
  - provisa/api/data/subscription_sse.py
  - provisa/subscriptions/pg_provider.py
  tests:
  - tests/e2e/test_subscriptions.py
  - tests/integration/test_sse_subscriptions.py
  - tests/unit/test_subscribe.py
  since: 2026-01
  integration_test: not_required
  integration_test_reason: Event shape serialization is pure logic; mocked subscriptions can verify JSON
    structure.
  stakeholders:
  - app-developer
  competitive_position:
    status: parity
    rationale: JSON event shape {event, table, row} with op field is standard for CDC event formats.
- id: REQ-058
  status: complete
  group: 10. UI & Admin Surfaces
  category: UI & Frontend
  priority: SHOULD
  type: ui
  description: Branded, custom React-based UI — rendered surface determined entirely by user's assembled
    role set.
  use_case: Role-driven rendered UI ensures each user sees only the capabilities relevant to their assigned
    permissions.
  code:
  - provisa-ui/src/
  tests:
  - provisa-ui/e2e/no-domain-mode.spec.ts
  - provisa-ui/e2e/relationships-header.spec.ts
  - provisa-ui/src/__tests__/ui_requirements.test.ts
  - tests/unit/test_ui_role_requirements.py
  since: 2026-01
  scenario: 'Given a user with an assembled role set

    When they access the UI

    Then the rendered surface shows only capabilities matching their role set

    '
  integration_test: deferred
  integration_test_reason: 'Role-driven rendered UI behavior requires real authenticated user session
    and permission model to verify. [deferred: requires external infrastructure not in CI stack]'
  e2e: true
  e2e_reason: The rendered surface is determined by the assembled role set and is only verifiable by loading
    the UI in a browser with a real authenticated session.
  stakeholders:
  - app-developer
  - compliance
  competitive_position:
    status: parity
    rationale: Custom branded React UI is standard in enterprise data platforms (Hasura, Dremio, Denodo
      all have custom UIs).
- id: REQ-059
  status: complete
  group: 10. UI & Admin Surfaces
  category: UI & Frontend
  priority: SHOULD
  type: ui
  description: 'Role composition system: admin assembles capabilities from independently assignable building
    blocks.'
  use_case: Composable role building blocks let admins precisely grant capabilities without overprivileging
    any user.
  code:
  - provisa-ui/src/
  tests:
  - provisa-ui/e2e/no-domain-mode.spec.ts
  - provisa-ui/e2e/relationships-header.spec.ts
  - provisa-ui/src/__tests__/ui_requirements.test.ts
  - tests/unit/test_ui_role_requirements.py
  since: 2026-01
  scenario: 'Given an admin user

    When they configure a role

    Then they can independently assign each capability building block

    '
  integration_test: deferred
  integration_test_reason: 'Role composition persistence and multi-user permission scenarios need real
    auth and database verification. [deferred: requires external infrastructure not in CI stack]'
  e2e: true
  e2e_reason: Admin role composition via building blocks requires browser interaction with the admin UI
    backed by a real auth and persistence layer.
  stakeholders:
  - compliance
  - ops
  competitive_position:
    status: ahead
    rationale: Composable role system where capabilities are independently assignable building blocks
      is more flexible than Hasura's preset permission model.
- id: REQ-060
  status: complete
  group: 10. UI & Admin Surfaces
  category: UI & Frontend
  priority: SHOULD
  type: ui
  description: 'Capabilities: Source Registration, Table Registration, Relationship Registration, Security
    Configuration, Query Development, Admin. Holders of a create capability (e.g. Relationship/View Registration)
    also execute creation requests queued by users lacking that capability (REQ-063).'
  use_case: Distinct assignable capabilities let organizations enforce separation of duties across data
    governance roles.
  code:
  - provisa-ui/src/
  tests:
  - provisa-ui/e2e/no-domain-mode.spec.ts
  - provisa-ui/e2e/relationships-header.spec.ts
  - provisa-ui/src/__tests__/ui_requirements.test.ts
  - tests/unit/test_ui_role_requirements.py
  since: 2026-01
  scenario: 'Given a user with the Relationship Registration capability

    When another user submits a creation request they lack authority to execute

    Then the capability holder can see and execute the queued request

    '
  integration_test: deferred
  integration_test_reason: 'Capability execution and separation-of-duty enforcement requires real data
    operations and auth verification. [deferred: requires external infrastructure not in CI stack]'
  e2e: true
  e2e_reason: Capability execution of queued requests across two users requires browser sessions and a
    real auth/database stack to verify the separation-of-duty workflow end to end.
  stakeholders:
  - compliance
  - data-engineer
  competitive_position:
    status: ahead
    rationale: Fine-grained capability set (Source/Table/Relationship/Security/Query/Admin) with create-implies-approve
      semantics is more granular than Hasura's coarser admin/non-admin split.
- id: REQ-061
  status: complete
  group: 10. UI & Admin Surfaces
  category: UI & Frontend
  priority: MUST
  type: ui
  description: Every destructive or consequential action requires explicit confirmation with consequence
    summary.
  use_case: Explicit confirmation for destructive actions prevents accidental data loss or misconfiguration
    in the UI.
  code:
  - provisa-ui/src/
  tests:
  - provisa-ui/e2e/no-domain-mode.spec.ts
  - provisa-ui/e2e/relationships-header.spec.ts
  - provisa-ui/src/__tests__/ui_requirements.test.ts
  - tests/unit/test_ui_role_requirements.py
  since: 2026-01
  scenario: 'Given a user about to perform a destructive or consequential action

    When they initiate the action

    Then a confirmation dialog with consequence summary is shown before proceeding

    '
  integration_test: not_required
  integration_test_reason: Confirmation dialog UI logic verifiable with mocks; no data or network impact.
  stakeholders:
  - app-developer
  competitive_position:
    status: neutral
    rationale: ''
- id: REQ-062
  status: complete
  group: 10. UI & Admin Surfaces
  category: UI & Frontend
  priority: SHOULD
  type: ui
  description: Test endpoint execution shows RLS filters applied, columns excluded, schema scope enforced
    in result metadata.
  use_case: Test endpoint metadata display lets developers see exactly which security rules applied to
    their query result.
  code:
  - provisa-ui/src/
  tests:
  - provisa-ui/e2e/no-domain-mode.spec.ts
  - provisa-ui/e2e/relationships-header.spec.ts
  - provisa-ui/src/__tests__/ui_requirements.test.ts
  - tests/unit/test_ui_role_requirements.py
  since: 2026-01
  scenario: 'Given a developer using the test endpoint

    When they execute a query

    Then the result metadata shows RLS filters applied, columns excluded, and schema scope enforced

    '
  integration_test: deferred
  integration_test_reason: 'Test endpoint metadata display requires real RLS/masking pipeline execution
    and query results. [deferred: requires external infrastructure not in CI stack]'
  e2e: true
  e2e_reason: The test endpoint result metadata (RLS filters applied, columns excluded, schema scope)
    is displayed in the browser UI and requires a live RLS pipeline running behind a real server to verify
    the display is correct.
  stakeholders:
  - app-developer
  - compliance
  competitive_position:
    status: ahead
    rationale: Test endpoint showing RLS filters applied, columns excluded, and schema scope in result
      metadata gives stewards real-time governance visibility not available in Hasura's console or Dremio.
- id: REQ-063
  status: complete
  group: 10. UI & Admin Surfaces
  category: UI & Frontend
  priority: SHOULD
  type: ui
  description: 'Creation-request queue: when a user lacks authority for a create operation (view, relationship),
    they submit a request that enters a queue visible to anyone holding the rights to execute it. Designed
    for steward efficiency; rejection reasons must be specific and actionable.'
  use_case: Actionable rejection reasons in the creation-request queue help requesters quickly fix and
    resubmit.
  code:
  - provisa-ui/src/
  tests:
  - provisa-ui/e2e/no-domain-mode.spec.ts
  - provisa-ui/e2e/relationships-header.spec.ts
  - provisa-ui/src/__tests__/ui_requirements.test.ts
  - tests/unit/test_ui_role_requirements.py
  since: 2026-01
  scenario: 'Given a user without create authority

    When they submit a creation request

    Then it appears in the queue visible to authorized stewards, with specific rejection reasons if denied

    '
  integration_test: deferred
  integration_test_reason: 'Creation-request queue persistence and multi-user workflow requires real database
    and auth model. [deferred: requires external infrastructure not in CI stack]'
  e2e: true
  e2e_reason: The creation-request queue involves multi-user browser interaction (submitter and steward)
    backed by real database persistence that cannot be verified without a full live stack.
  stakeholders:
  - compliance
  - data-engineer
  - ops
  competitive_position:
    status: ahead
    rationale: Creation-request queue for users lacking create authority is a governed self-service pattern
      not present in Hasura, Dremio, or Denodo.
- id: REQ-164
  status: complete
  group: 10. UI & Admin Surfaces
  category: Admin & Configuration
  priority: SHOULD
  type: ui
  description: '`GET/PUT /admin/config` for config YAML download/upload with backup and reload.'
  use_case: Config YAML download/upload with backup lets admins version-control platform configuration
    outside the UI.
  code:
  - provisa/api/admin/
  tests:
  - tests/e2e/test_admin_flow.py
  - tests/integration/test_admin_api.py
  - tests/unit/test_admin_mv.py
  - tests/unit/test_admin_requirements.py
  - tests/unit/test_config_reload.py
  - tests/unit/test_config_secrets.py
  since: 2026-01
  scenario: 'Given an admin user

    When they GET /admin/config or PUT /admin/config with a YAML payload

    Then the config is downloaded or uploaded with backup and platform reload

    '
  integration_test: required
  integration_test_reason: Config YAML download/upload with backup requires real filesystem/database interactions
    and server reload.
  stakeholders:
  - ops
  - app-developer
  competitive_position:
    status: parity
    rationale: Config download/upload with backup is standard admin functionality in enterprise platforms.
- id: REQ-165
  status: complete
  group: 10. UI & Admin Surfaces
  category: Admin & Configuration
  priority: SHOULD
  type: ui
  description: '`GET/PUT /admin/settings` for runtime platform settings (redirect, sampling, cache).'
  use_case: Runtime settings API lets operators adjust redirect, sampling, and cache behaviour without
    a restart.
  code:
  - provisa/api/admin/
  tests:
  - tests/e2e/test_admin_flow.py
  - tests/integration/test_admin_api.py
  - tests/unit/test_admin_mv.py
  - tests/unit/test_admin_requirements.py
  since: 2026-01
  scenario: 'Given an operator

    When they PUT /admin/settings with updated values for redirect, sampling, or cache

    Then the platform applies the new settings at runtime without requiring a restart

    '
  integration_test: required
  integration_test_reason: Runtime settings API behavior (redirect, sampling, cache) requires real backend
    verification.
  stakeholders:
  - ops
  competitive_position:
    status: parity
    rationale: Runtime settings API is standard in enterprise data platforms.
- id: REQ-166
  status: complete
  group: 10. UI & Admin Surfaces
  category: Admin & Configuration
  priority: SHOULD
  type: ui
  description: Editable relationships page with materialize toggle, delete, and add form.
  use_case: Editable relationships page with materialize toggle lets stewards manage cross-source joins
    interactively.
  code:
  - provisa/api/admin/
  tests:
  - tests/e2e/test_admin_flow.py
  - tests/integration/test_admin_api.py
  - tests/unit/test_admin_mv.py
  - tests/unit/test_admin_requirements.py
  since: 2026-01
  scenario: 'Given a steward on the relationships admin page

    When they toggle materialize, delete a relationship, or submit the add form

    Then the change is persisted and reflected in the UI immediately

    '
  integration_test: required
  integration_test_reason: Relationship persistence and toggle behavior requires real database and schema
    operations.
  e2e: true
  e2e_reason: The editable relationships page with materialize toggle, delete, and add form requires browser
    interaction to verify that UI actions persist and are immediately reflected in the rendered page.
  stakeholders:
  - data-engineer
  - compliance
  competitive_position:
    status: parity
    rationale: Editable relationships UI is standard in Hasura console and Denodo admin.
- id: REQ-167
  status: complete
  group: 10. UI & Admin Surfaces
  category: Admin & Configuration
  priority: MAY
  type: ui
  description: AI-suggested relationships via LLM discovery integration on relationships page.
  use_case: AI-suggested relationships reduce steward effort when registering foreign-key links across
    complex schemas.
  code:
  - provisa/api/admin/
  tests:
  - tests/e2e/test_admin_flow.py
  - tests/integration/test_admin_api.py
  - tests/unit/test_admin_mv.py
  - tests/unit/test_admin_requirements.py
  since: 2026-01
  scenario: 'Given a steward on the relationships admin page

    When they trigger AI discovery

    Then LLM-suggested relationships are displayed for review and one-click registration

    '
  integration_test: deferred
  integration_test_reason: LLM API integration not available in CI; core relationship registration logic
    testable with mocks.
  stakeholders:
  - data-engineer
  - ops
  competitive_position:
    status: ahead
    rationale: LLM-suggested relationships via AI discovery on the relationships page is not available
      in Hasura console, Denodo, Dremio, or Trino.
- id: REQ-242
  status: complete
  group: 10. UI & Admin Surfaces
  category: Commands UI
  priority: SHOULD
  type: ui
  description: Admin UI "Commands" page listing all registered functions and webhooks. Grouped by type
    (DB Function / Webhook). Shows source, domain, exposed_as (mutation/query), governance level, return
    table, argument count.
  use_case: Commands admin page gives stewards visibility into all registered functions and webhooks in
    one place.
  code:
  - provisa-ui/src/
  tests:
  - provisa-ui/e2e/no-domain-mode.spec.ts
  - provisa-ui/e2e/relationships-header.spec.ts
  - provisa-ui/src/__tests__/ui_requirements.test.ts
  - tests/unit/test_commands_requirements.py
  since: 2026-01
  scenario: 'Given a steward viewing the Commands admin page

    When the page loads

    Then all registered functions and webhooks are listed, grouped by type, with source, domain,

    exposed_as, governance level, return table, and argument count shown

    '
  integration_test: deferred
  integration_test_reason: 'Admin UI listing requires real database queries to verify function/webhook
    registry visibility. [deferred: requires external infrastructure not in CI stack]'
  e2e: true
  e2e_reason: The Commands admin page listing and grouping of registered functions and webhooks is a browser-rendered
    surface that requires a live database registry to verify correct display.
  stakeholders:
  - ops
  - app-developer
  competitive_position:
    status: parity
    rationale: Commands/functions page listing registered procedures and webhooks is available in Hasura
      console (Actions, Functions).
- id: REQ-243
  status: complete
  group: 10. UI & Admin Surfaces
  category: Commands UI
  priority: SHOULD
  type: ui
  description: 'Add command form with type selector: DB Function (source, schema, function name, exposed_as,
    returns registered table, arguments, visible_to/writable_by) or Webhook (name, URL, method, timeout_ms,
    returns registered table or inline type, arguments, visible_to).'
  use_case: Add command form lets stewards register DB functions and webhooks without editing YAML files
    directly.
  code:
  - provisa-ui/src/
  tests:
  - provisa-ui/e2e/no-domain-mode.spec.ts
  - provisa-ui/e2e/relationships-header.spec.ts
  - provisa-ui/src/__tests__/ui_requirements.test.ts
  - tests/unit/test_commands_requirements.py
  since: 2026-01
  scenario: 'Given a steward on the Commands page

    When they open the add command form and select DB Function or Webhook

    Then the appropriate fields are shown and the command is registered on submit

    '
  integration_test: deferred
  integration_test_reason: 'Add command form persistence requires real database storage and schema validation.
    [deferred: requires external infrastructure not in CI stack]'
  e2e: true
  e2e_reason: Add command form for DB Function/Webhook requires a running server with real database storage
    to verify command registration end-to-end through the UI.
  stakeholders:
  - data-engineer
  - ops
  competitive_position:
    status: parity
    rationale: Add command form with DB Function/Webhook type selector mirrors Hasura Actions UI.
- id: REQ-244
  status: complete
  group: 10. UI & Admin Surfaces
  category: Commands UI
  priority: SHOULD
  type: ui
  description: Inline type builder for webhook return types — dynamic rows of field name + GraphQL type.
    Used when webhook returns a custom shape not backed by a registered table.
  use_case: Inline type builder lets stewards define custom webhook return shapes visually without writing
    JSON Schema.
  code:
  - provisa-ui/src/
  tests:
  - provisa-ui/e2e/no-domain-mode.spec.ts
  - provisa-ui/e2e/relationships-header.spec.ts
  - provisa-ui/src/__tests__/ui_requirements.test.ts
  - tests/unit/test_commands_requirements.py
  since: 2026-01
  scenario: 'Given a steward registering a webhook with a custom return shape

    When they use the inline type builder

    Then they can add dynamic rows of field name and GraphQL type to define the return type

    '
  integration_test: not_required
  integration_test_reason: Inline type builder UI logic is pure form state and JSON schema generation;
    mocks sufficient.
  stakeholders:
  - app-developer
  competitive_position:
    status: ahead
    rationale: Inline type builder for webhook return types without requiring a registered table is more
      flexible than Hasura's action type definition workflow.
- id: REQ-245
  status: complete
  group: 10. UI & Admin Surfaces
  category: Commands UI
  priority: SHOULD
  type: ui
  description: Test command button — execute function/webhook with sample arguments, display result +
    governance pipeline applied (which columns masked, RLS filters, role). Same pattern as query test
    endpoint.
  use_case: Test command button lets developers verify function execution and governance pipeline before
    approving.
  code:
  - provisa-ui/src/
  tests:
  - provisa-ui/e2e/no-domain-mode.spec.ts
  - provisa-ui/e2e/relationships-header.spec.ts
  - provisa-ui/src/__tests__/ui_requirements.test.ts
  - tests/e2e/test_query_pipeline.py
  - tests/unit/test_commands_requirements.py
  since: 2026-01
  scenario: 'Given a developer on the Commands page

    When they click the test button and provide sample arguments

    Then the function or webhook executes and the result plus governance pipeline details are displayed

    '
  integration_test: deferred
  integration_test_reason: 'Test command execution requires real function/webhook invocation and governance
    pipeline verification. [deferred: requires external infrastructure not in CI stack]'
  e2e: true
  e2e_reason: Test command button requires live function/webhook invocation through the running server
    plus governance pipeline execution — not reproducible without a real running stack.
  stakeholders:
  - app-developer
  - data-engineer
  - compliance
  competitive_position:
    status: ahead
    rationale: Test command execution showing governance pipeline applied (masked columns, RLS, role)
      is more transparent than Hasura's action test tool.
- id: REQ-248
  status: complete
  group: 10. UI & Admin Surfaces
  category: UI & Design Patterns
  priority: SHOULD
  type: structural
  description: GraphQL Voyager integration uses iframe with React 18 CDN standalone bundle (not a native
    React component fork). The iframe approach avoids MUI v5/React 19 incompatibility and provides reliable
    isolation. No component fork is planned.
  use_case: GraphQL Voyager iframe integration provides schema visualisation without React version compatibility
    issues.
  code:
  - provisa-ui/src/
  tests:
  - provisa-ui/e2e/no-domain-mode.spec.ts
  - provisa-ui/e2e/relationships-header.spec.ts
  - provisa-ui/src/__tests__/ui_requirements.test.ts
  - tests/unit/test_commands_requirements.py
  since: 2026-01
  integration_test: not_required
  integration_test_reason: Voyager iframe integration is structural React/DOM concern; mocks provide full
    fidelity.
  stakeholders:
  - app-developer
  competitive_position:
    status: neutral
    rationale: ''
- id: REQ-249
  status: complete
  group: 10. UI & Admin Surfaces
  category: UI & Design Patterns
  priority: SHOULD
  type: structural
  description: Column-level masking configuration stored as inline fields on ColumnConfig in models.py
    (mask_type, mask_pattern, etc.) — not as a separate MaskingRule Pydantic model or separate database
    table. Masking rules are loaded from table_columns DB rows at startup in app.py. This co-locates masking
    config with the column definition it applies to.
  use_case: Inline masking config on ColumnConfig keeps column governance co-located, avoiding split config
    lookups.
  code:
  - provisa-ui/src/
  tests:
  - provisa-ui/e2e/no-domain-mode.spec.ts
  - provisa-ui/e2e/relationships-header.spec.ts
  - provisa-ui/src/__tests__/ui_requirements.test.ts
  - tests/unit/test_column_governance_requirements.py
  since: 2026-01
  integration_test: deferred
  integration_test_reason: 'Masking config loading and application in startup requires real database and
    schema model verification. [deferred: requires external infrastructure not in CI stack]'
  stakeholders:
  - compliance
  - data-engineer
  competitive_position:
    status: neutral
    rationale: ''
- id: REQ-395
  status: complete
  group: 10. UI & Admin Surfaces
  category: UI & Frontend
  priority: MUST
  type: ui
  description: The PK designation must be configurable in the TablesPage UI via checkbox per column, visible
    in both add-form and edit-form views, and in the read-only view.
  use_case: UI-based PK designation lets stewards configure keys without editing YAML files.
  code:
  - provisa-ui/src/pages/TablesPage
  - provisa-ui/src/components/ColumnForm
  tests:
  - provisa-ui/e2e/tables-register.spec.ts
  - provisa-ui/src/__tests__/ui_requirements.test.ts
  - tests/unit/test_column_governance_requirements.py
  since: 2026-01
  scenario: 'Given a steward on the TablesPage

    When they add or edit a table column

    Then a PK checkbox is visible and its value is persisted correctly in all form views

    '
  integration_test: deferred
  integration_test_reason: 'PK designation persistence and UI visibility requires real database schema
    updates. [deferred: requires external infrastructure not in CI stack]'
  e2e: true
  e2e_reason: PK checkbox visibility and persistence in add/edit/read-only form views requires a running
    backend with real database schema updates to verify end-to-end round-trip.
  stakeholders:
  - data-engineer
  - app-developer
  competitive_position:
    status: neutral
    rationale: ''
- id: REQ-396
  status: complete
  group: 10. UI & Admin Surfaces
  category: UI & Frontend
  priority: MUST
  type: ui
  description: The graph node context menu "Exclude from query" option must be disabled (greyed out) when
    no primary key is configured for that node label.
  use_case: Disabling exclusion without a PK prevents incomplete or ambiguous row-level filtering.
  code:
  - provisa-ui/src/components/GraphContextMenu
  tests:
  - provisa-ui/e2e/graph-query-panel-height.spec.ts
  - provisa-ui/e2e/graph-show-children.spec.ts
  - provisa-ui/src/__tests__/ui_requirements.test.ts
  - tests/unit/test_column_governance_requirements.py
  since: 2026-01
  scenario: 'Given a graph node with no primary key configured

    When the user opens the context menu

    Then the "Exclude from query" option is disabled and greyed out

    '
  integration_test: deferred
  integration_test_reason: 'Context menu state depends on real database PK configuration; behavior must
    be verified end-to-end. [deferred: requires external infrastructure not in CI stack]'
  e2e: true
  e2e_reason: Graph node context menu disabled state depends on real PK configuration from a live database
    — requires full browser-to-server round-trip to verify the greyed-out condition.
  stakeholders:
  - app-developer
  - data-engineer
  competitive_position:
    status: ahead
    rationale: Disabling graph UI exclusion options when no PK is configured prevents invalid queries
      at the UX level — a safety feature not present in graph tools like Neo4j Bloom.
- id: REQ-401
  status: complete
  group: 10. UI & Admin Surfaces
  category: UI & Frontend
  priority: SHOULD
  type: ui
  description: Foreign key (FK) and alternate key (AK) badges surface as read-only indicators in the column
    editor UI, showing which columns are linked by relationships.
  use_case: FK/AK badges provide visual feedback on relationship column usage without allowing direct
    editing.
  code:
  - provisa-ui/src/components/ColumnForm
  - provisa-ui/src/pages/TablesPage
  tests:
  - provisa-ui/e2e/tables-register.spec.ts
  - provisa-ui/src/__tests__/ui_requirements.test.ts
  - tests/unit/test_column_governance_requirements.py
  since: 2026-01
  scenario: 'Given a column linked by a relationship

    When the steward views the column editor

    Then FK or AK badges appear as read-only indicators showing the relationship linkage

    '
  integration_test: deferred
  integration_test_reason: 'FK/AK badge display requires real relationship registry to determine column
    linkage. [deferred: requires external infrastructure not in CI stack]'
  e2e: true
  e2e_reason: FK/AK badge display in the column editor requires a real relationship registry and running
    server to determine column linkage and render the correct badges.
  stakeholders:
  - data-engineer
  - app-developer
  competitive_position:
    status: neutral
    rationale: ''
- id: REQ-404
  status: complete
  group: 10. UI & Admin Surfaces
  category: UI & Frontend
  priority: SHOULD
  type: ui
  description: 'Security page RLS form includes "Apply To" toggle: "Specific Table" or "Entire Domain".
    Selection determines whether `table_id` or `domain_id` is populated; the other is NULL.'
  use_case: UI toggle lets stewards choose rule scope without understanding the underlying schema design.
  code:
  - provisa-ui/src/pages/SecurityPage
  tests:
  - provisa-ui/src/__tests__/ui_requirements.test.ts
  - tests/unit/test_column_governance_requirements.py
  since: 2026-01
  scenario: 'Given a steward configuring an RLS rule

    When they select "Specific Table" or "Entire Domain" from the Apply To toggle

    Then table_id or domain_id is populated accordingly and the other is set to NULL

    '
  integration_test: deferred
  integration_test_reason: 'RLS scope toggle persistence requires real database to verify table_id vs
    domain_id nullability. [deferred: requires external infrastructure not in CI stack]'
  e2e: true
  e2e_reason: RLS Apply To toggle persistence requires a running server and real database to verify table_id
    vs domain_id nullability after form submission.
  stakeholders:
  - compliance
  - data-engineer
  competitive_position:
    status: ahead
    rationale: RLS Apply-To toggle (Specific Table vs. Entire Domain) in the security UI enables domain-wide
      row security configuration not available in Hasura's per-table RLS or Dremio's policy model.
- id: REQ-410
  status: complete
  group: 10. UI & Admin Surfaces
  category: UI & Frontend
  priority: MUST
  type: ui
  description: GraphFrame Cypher WHERE clause generation must use single-quoted string literals (not double-quoted
    SQL identifiers) for non-numeric PK values to produce valid Cypher syntax.
  use_case: Correct single-quote quoting prevents SQL identifier errors when filtering by string PK values
    in the graph view.
  code:
  - provisa-ui/src/pages/GraphFrame.tsx
  tests:
  - provisa-ui/src/__tests__/ui_requirements.test.ts
  - tests/unit/test_column_governance_requirements.py
  - tests/unit/test_cypher_graph_fns.py
  since: 2026-01
  scenario: 'Given a graph query filtering by a non-numeric PK value

    When the Cypher WHERE clause is generated

    Then string literals use single quotes, not double quotes

    '
  integration_test: not_required
  integration_test_reason: Cypher WHERE clause string quoting is pure logic; mocks verify syntax correctness.
  stakeholders:
  - app-developer
  competitive_position:
    status: neutral
    rationale: ''
- id: REQ-533
  status: complete
  group: 10. UI & Admin Surfaces
  category: Admin & Configuration
  priority: MUST
  type: structural
  description: The admin GraphQL API is a Strawberry-based endpoint mounted at POST /admin/graphql, served
    by FastAPI via strawberry.fastapi.GraphQLRouter. It is wholly separate from the data GraphQL endpoint
    at /data/graphql with distinct schemas, independent routers, and independent access control.
  use_case: Distinct admin API endpoint prevents governance operations from being confused with data query
    operations and allows independent auth enforcement.
  code:
  - provisa/api/app.py
  - provisa/api/admin/schema.py
  tests:
  - tests/integration/test_admin_api.py
  - tests/unit/test_admin_mv.py
  - tests/unit/test_admin_requirements.py
  since: 2026-01
  integration_test: not_required
  integration_test_reason: Strawberry router setup and endpoint mounting are structural; mocks verify
    schema separation.
  stakeholders:
  - ops
  - app-developer
  competitive_position:
    status: neutral
    rationale: ''
- id: REQ-620
  status: complete
  superseded_by: REQ-533
  group: 10. UI & Admin Surfaces
  category: Admin & Configuration
  priority: SHOULD
  type: structural
  description: The admin GraphQL API is mounted at `/admin/graphql` on the backend server (default port
    8001). It is distinct from the data GraphQL API at `/data/graphql` and is used for platform administration
    operations.
  use_case: Separate admin GraphQL endpoint allows tooling and scripts to target administrative mutations
    and queries without mixing them with data queries.
  code:
  - provisa/api/app.py line 3389
  tests:
  - tests/integration/test_admin_api.py
  - tests/unit/test_admin_mv.py
  - tests/unit/test_admin_requirements.py
  since: 2026-01
  integration_test: required
  integration_test_reason: Admin GraphQL endpoint behavior requires HTTP transport verification at correct
    port/path.
  e2e: true
  e2e_reason: Admin GraphQL endpoint reachability at the correct port and path requires a real running
    server to verify HTTP transport behavior.
  stakeholders:
  - ops
  - app-developer
  competitive_position:
    status: neutral
    rationale: ''
- id: REQ-622
  status: complete
  group: 10. UI & Admin Surfaces
  category: Admin & Configuration
  priority: SHOULD
  type: ui
  description: The admin GraphQL API ships with Strawberry's built-in GraphiQL IDE accessible via GET
    /admin/graphql in a browser, allowing interactive exploration of the full admin schema.
  use_case: Integrated GraphiQL removes the need for external GraphQL tooling when exploring or debugging
    admin operations.
  code:
  - provisa/api/app.py
  - provisa/api/admin/schema.py
  tests:
  - tests/integration/test_admin_api.py
  - tests/unit/test_admin_mv.py
  - tests/unit/test_admin_requirements.py
  since: 2026-01
  scenario: 'Given a developer with browser access to the admin server

    When they navigate to GET /admin/graphql

    Then the Strawberry GraphiQL IDE loads and allows interactive schema exploration

    '
  integration_test: not_required
  integration_test_reason: GraphiQL IDE is Strawberry built-in feature; mocks verify route mounting.
  e2e: true
  e2e_reason: GraphiQL IDE loading via browser GET /admin/graphql requires a real running server and browser
    navigation to confirm the UI loads and allows schema exploration.
  stakeholders:
  - app-developer
  - ops
  competitive_position:
    status: neutral
    rationale: ''
- id: REQ-644
  status: complete
  group: 10. UI & Admin Surfaces
  category: Graph Grouping — View Transform
  priority: SHOULD
  type: structural
  description: 'Node grouping in the graph UI is a view transform: the underlying node and edge data is
    never modified; the rendering layer derives a `groupKey` from any node attribute and applies visual
    encodings (color, hull ellipse) without altering model state. GroupingState is held in GraphFrame
    and passed to GraphCanvas and HullOverlay.'
  use_case: View-transform grouping keeps the data model clean and allows switching between grouping attributes
    without re-fetching data.
  code:
  - provisa-ui/src/components/graph/GraphFrame.tsx
  - provisa-ui/src/components/graph/graph-clusters.ts
  tests:
  - provisa-ui/src/__tests__/GraphFrame.test.tsx
  - provisa-ui/src/__tests__/ui_requirements.test.ts
  - tests/unit/test_graph_grouping_requirements.py
  since: 2026-01
  integration_test: not_required
  integration_test_reason: View-transform grouping is pure render logic; mocks verify groupKey derivation
    and state flow.
  stakeholders:
  - app-developer
  competitive_position:
    status: ahead
    rationale: Client-side view transform for graph grouping (no server-side data mutation) with hull
      overlays and supernode collapse is beyond what Hasura, Dremio, or Denodo offer in their UIs.
- id: REQ-645
  status: complete
  group: 10. UI & Admin Surfaces
  category: Graph Grouping — Attribute Discovery
  priority: SHOULD
  type: ui
  description: 'After any query result, the UI scans all node properties to build a list of groupable
    attributes: attributes with more than one distinct value across nodes are included. `domain` (derived
    from the node label prefix) is always the first entry. Schema cluster virtuals (`schema_L1`, `schema_L2`,
    `schema_L3`) are included when their underlying `scl1`/`scl2`/`scl3` properties have multiple distinct
    values. The grouping dropdown is populated dynamically from this list — no attribute names are hardcoded.'
  use_case: Dynamic attribute discovery lets users group by any categorical property in the current result
    set without prior configuration.
  code:
  - provisa-ui/src/components/graph/GraphFrame.tsx
  tests:
  - provisa-ui/src/__tests__/GraphFrame.test.tsx
  - provisa-ui/src/__tests__/ui_requirements.test.ts
  - tests/unit/test_graph_grouping_requirements.py
  - tests/unit/test_llm_discovery.py
  since: 2026-01
  scenario: 'Given a graph query result with nodes having multiple distinct attribute values

    When the grouping dropdown is rendered

    Then it lists all attributes with more than one distinct value, with domain first, and no

    hardcoded names

    '
  integration_test: required
  integration_test_reason: Dynamic attribute discovery from query results requires real schema properties
    and value cardinality.
  stakeholders:
  - app-developer
  - data-engineer
  competitive_position:
    status: neutral
    rationale: ''
- id: REQ-646
  status: complete
  group: 10. UI & Admin Surfaces
  category: Graph Grouping — Color Encoding
  priority: SHOULD
  type: ui
  description: When a grouping attribute is active, each node's `background-color` in the Cytoscape style
    function is overridden to the color assigned to that node's group value. Color assignment uses `clusterColor(groupValue)`
    which derives a stable color from the group key string.
  use_case: Color-by-group gives immediate visual differentiation of node clusters without requiring a
    separate legend interaction.
  code:
  - provisa-ui/src/components/graph/GraphCanvas.tsx
  - provisa-ui/src/components/graph/graph-clusters.ts
  tests:
  - provisa-ui/src/__tests__/GraphFrame.test.tsx
  - provisa-ui/src/__tests__/ui_requirements.test.ts
  - tests/unit/test_graph_grouping_requirements.py
  since: 2026-01
  scenario: 'Given an active grouping attribute in the graph view

    When nodes are rendered

    Then each node''s background color reflects its group value via clusterColor

    '
  integration_test: not_required
  integration_test_reason: Color assignment via clusterColor is pure deterministic logic; mocks verify
    CSS override.
  stakeholders:
  - app-developer
  competitive_position:
    status: neutral
    rationale: ''
- id: REQ-647
  status: complete
  group: 10. UI & Admin Surfaces
  category: Graph Grouping — Hull SVG Overlay
  priority: SHOULD
  type: ui
  description: When a grouping attribute is active, an SVG ellipse is drawn over the Cytoscape canvas
    around each group's node positions. Ellipses are computed by fitting bounding-box centroids and radii
    from node positions in graph coordinates, then converted to screen coordinates using Cytoscape zoom
    and pan. The SVG layer re-renders on every Cytoscape `viewport` event so hulls track pan and zoom.
  use_case: SVG hull ellipses provide spatial grouping cues that remain accurate during pan and zoom without
    requiring re-layout.
  code:
  - provisa-ui/src/components/graph/GraphCanvas.tsx
  tests:
  - provisa-ui/src/__tests__/GraphFrame.test.tsx
  - provisa-ui/src/__tests__/ui_requirements.test.ts
  - tests/unit/test_graph_grouping_requirements.py
  since: 2026-01
  scenario: 'Given an active grouping attribute in the graph view

    When the user pans or zooms the Cytoscape canvas

    Then SVG hull ellipses re-render to accurately enclose each group''s nodes

    '
  integration_test: not_required
  integration_test_reason: SVG hull rendering and Cytoscape coordinate transformation is pure geometry;
    mocks verify viewport event handling.
  stakeholders:
  - app-developer
  competitive_position:
    status: ahead
    rationale: SVG ellipse hull overlay computed from Cytoscape node positions for group visualization
      is a unique graph UI capability not available in any competitor's built-in console.
- id: REQ-648
  status: complete
  group: 10. UI & Admin Surfaces
  category: Graph Grouping — Collapse to Supernode
  priority: SHOULD
  type: ui
  description: 'Each group cluster supports a collapse toggle. When collapsed: member nodes are removed
    from the Cytoscape graph; a synthetic supernode with id `__collapsed_<level>_<cidToId(cid)>` is added
    showing the group label and member count; edges crossing the group boundary are rewritten to connect
    to/from the supernode. Double-clicking the supernode expands the group and restores original nodes
    and edges. Collapsed state is held in a `Set<string>` per frame and reset when the grouping attribute
    changes.'
  use_case: Collapse-to-supernode reduces visual complexity for large clusters while preserving cross-cluster
    edge visibility.
  code:
  - provisa-ui/src/components/graph/graph-clusters.ts
  - provisa-ui/src/components/graph/GraphCanvas.tsx
  tests:
  - provisa-ui/src/__tests__/GraphFrame.test.tsx
  - provisa-ui/src/__tests__/ui_requirements.test.ts
  - tests/unit/test_graph_grouping_requirements.py
  since: 2026-01
  scenario: 'Given a grouped graph with a cluster toggled to collapsed

    When the cluster is collapsed

    Then member nodes are replaced by a synthetic supernode and cross-boundary edges are rewritten;

    double-clicking the supernode restores the original nodes and edges

    '
  integration_test: not_required
  integration_test_reason: Supernode collapse/expand is pure graph state and rendering logic; mocks verify
    edge rewriting.
  stakeholders:
  - app-developer
  competitive_position:
    status: ahead
    rationale: Supernode collapse with aggregate display (member count, icon sampling) in the graph UI
      is a unique visualization feature not present in Hasura console, Dremio, or Denodo.
- id: REQ-649
  status: complete
  group: 10. UI & Admin Surfaces
  category: Graph Node Size Encoding
  priority: MAY
  type: ui
  description: Node size can be driven by any numeric node property via `sizeByProperty` (a per-label
    map of property name). The property value is linearly scaled between the observed min and max across
    all nodes of that label to produce a node diameter between `base` and `base * multiplier`. The `sizeByProperty`
    setting is persisted in localStorage under key `provisa.graph.sizeByProperty`.
  use_case: Size-by-property encoding lets analysts visually compare centrality scores or any other numeric
    metric across nodes without reading raw values.
  code:
  - provisa-ui/src/components/graph/GraphCanvas.tsx
  - provisa-ui/src/pages/GraphPage.tsx
  tests:
  - provisa-ui/src/__tests__/GraphFrame.test.tsx
  - provisa-ui/src/__tests__/ui_requirements.test.ts
  - tests/unit/test_graph_grouping_requirements.py
  since: 2026-01
  scenario: 'Given a sizeByProperty mapping configured for a node label

    When the graph renders nodes of that label

    Then node diameters are linearly scaled between base and base*multiplier based on the property value

    '
  integration_test: not_required
  integration_test_reason: Size-by-property scaling and localStorage persistence are pure UI logic; mocks
    verify linear scaling.
  stakeholders:
  - app-developer
  competitive_position:
    status: ahead
    rationale: Dynamic node size encoding driven by any numeric property with per-label min/max scaling
      is a unique graph analytics UX feature not available in competitor platforms.
- id: REQ-055
  status: complete
  group: 11. Platform, Infrastructure & Delivery
  category: Infrastructure
  priority: SHOULD
  type: infrastructure
  description: 'Docker Compose for development/small-team: single command, Provisa + Trino coordinator
    + configurable workers, all connectors pre-loaded.'
  use_case: Single-command Docker Compose setup lets developers run the full Provisa stack without manual
    configuration.
  code:
  - helm/
  - docker-compose.yml
  tests:
  - tests/e2e/test_health_checks.py
  - tests/integration/test_infra.py
  - tests/unit/test_infra_requirements.py
  since: 2026-01
  scenario: 'Given a developer wants to run the full Provisa stack locally

    When they run the single Docker Compose command

    Then Provisa, Trino coordinator, configurable workers, and all connectors start successfully

    '
  integration_test: required
  integration_test_reason: Docker Compose stack startup behavior requires real containers and orchestration.
  e2e: true
  e2e_reason: Docker Compose single-command startup of Provisa, Trino, and connectors requires a real
    container runtime and live service health checks to verify the full stack launches successfully.
  stakeholders:
  - ops
  - app-developer
  competitive_position:
    status: parity
    rationale: Docker Compose for development is standard practice across all modern data platforms.
- id: REQ-056
  status: complete
  group: 11. Platform, Infrastructure & Delivery
  category: Infrastructure
  priority: SHOULD
  type: infrastructure
  description: 'Helm chart for production Kubernetes: horizontal Trino worker scaling, resource groups,
    HPA autoscaling.'
  use_case: Helm chart with HPA autoscaling lets production deployments scale Trino workers automatically
    under load.
  code:
  - helm/
  - docker-compose.yml
  tests:
  - tests/e2e/test_health_checks.py
  - tests/e2e/test_helm_minikube.py
  - tests/integration/test_infra.py
  - tests/unit/test_infra_requirements.py
  since: 2026-01
  scenario: 'Given a production Kubernetes deployment

    When load increases on Trino workers

    Then HPA autoscaling adds workers automatically via the Helm chart

    '
  integration_test: required
  integration_test_reason: HPA autoscaling and Kubernetes behavior require real cluster infrastructure.
  stakeholders:
  - ops
  competitive_position:
    status: parity
    rationale: Helm chart for Kubernetes with HPA is standard for enterprise data platform deployment.
- id: REQ-057
  status: complete
  group: 11. Platform, Infrastructure & Delivery
  category: Infrastructure
  priority: MUST
  type: constraint
  description: Provisa container is stateless; deployment topology behind Trino endpoint is configuration
    concern.
  use_case: Stateless Provisa container enables horizontal scaling and rolling deployments without session
    affinity concerns.
  code:
  - helm/
  - docker-compose.yml
  tests:
  - tests/e2e/test_health_checks.py
  - tests/integration/test_infra.py
  - tests/unit/test_infra_requirements.py
  since: 2026-01
  scenario: 'Given multiple Provisa container instances running behind a load balancer

    When a request is routed to any instance

    Then the response is identical regardless of which instance handles it

    '
  integration_test: required
  integration_test_reason: Stateless behavior across multiple instances requires real load balancer and
    routing.
  stakeholders:
  - ops
  - app-developer
  competitive_position:
    status: parity
    rationale: Stateless application containers are standard deployment practice.
- id: REQ-064
  status: complete
  group: 11. Platform, Infrastructure & Delivery
  category: Error Handling & Reliability
  priority: MUST
  type: constraint
  description: Never add fallback values or silent error handling — all errors must be explicit and fail-fast.
  use_case: Fail-fast error handling ensures production failures surface immediately instead of silently
    returning bad data.
  tests:
  - tests/unit/test_error_handling.py
  - tests/unit/test_query_timeout.py
  since: 2026-01
  scenario: 'Given a missing or invalid value in the system

    When the code encounters the error condition

    Then an explicit error is raised immediately rather than a fallback value returned

    '
  integration_test: not_required
  integration_test_reason: Error handling policy is pure logic in unit tests.
  stakeholders:
  - app-developer
  - ops
  competitive_position:
    status: neutral
    rationale: ''
- id: REQ-065
  status: complete
  group: 11. Platform, Infrastructure & Delivery
  category: Error Handling & Reliability
  priority: MUST
  type: constraint
  description: No migrations in version 1 development.
  use_case: No-migration policy eliminates schema migration risk and keeps V1 development iteration fast.
  tests:
  - tests/unit/test_error_handling.py
  since: 2026-01
  scenario: 'Given V1 development is active

    When a schema change is needed

    Then no migration scripts are added; the schema is rebuilt from scratch

    '
  integration_test: not_required
  integration_test_reason: Schema policy is a development constraint, not a runtime behavior.
  stakeholders:
  - app-developer
  competitive_position:
    status: neutral
    rationale: ''
- id: REQ-069
  status: complete
  group: 11. Platform, Infrastructure & Delivery
  category: Architecture & Design Patterns
  priority: MUST
  type: constraint
  description: Architecture docs in `docs/arch/` ARE the planning documents — update when requirements
    change, don't implement without planning.
  use_case: Architecture docs as planning documents ensure implementation always reflects a reviewed design.
  tests:
  - tests/unit/test_process_requirements.py
  since: 2026-01
  scenario: 'Given a new requirement or design change

    When implementation begins

    Then the architecture docs in docs/arch/ are updated before or alongside the implementation

    '
  integration_test: not_required
  integration_test_reason: Documentation planning constraint does not require integration testing.
  stakeholders:
  - app-developer
  competitive_position:
    status: neutral
    rationale: ''
- id: REQ-070
  status: complete
  group: 11. Platform, Infrastructure & Delivery
  category: Architecture & Design Patterns
  priority: MUST
  type: constraint
  description: Maximum brevity in communications — code and facts only, no pleasantries or explanations
    unless asked.
  use_case: Maximum brevity discipline keeps communications focused on actionable information rather than
    ceremony.
  tests:
  - tests/unit/test_process_requirements.py
  since: 2026-01
  scenario: 'Given any development communication or tool output

    When responding to a request

    Then only code and facts are returned with no pleasantries or unsolicited explanations

    '
  integration_test: not_required
  integration_test_reason: Communication discipline is a process constraint, not a technical behavior.
  stakeholders:
  - app-developer
  competitive_position:
    status: neutral
    rationale: ''
- id: REQ-071
  status: complete
  group: 11. Platform, Infrastructure & Delivery
  category: Architecture & Design Patterns
  priority: SHOULD
  type: infrastructure
  description: New requirements tracked via requirements-tracker agent appending to `docs/arch/requirements.md`.
  use_case: Automated requirements tracking ensures every design decision is recorded without interrupting
    development flow.
  tests:
  - tests/unit/test_infra_requirements.py
  since: 2026-01
  scenario: 'Given a new requirement or design decision is identified

    When the requirements-tracker agent runs

    Then a new row is appended to docs/arch/requirements.md

    '
  integration_test: not_required
  integration_test_reason: Requirements tracking is a documentation workflow, not runtime behavior.
  stakeholders:
  - app-developer
  competitive_position:
    status: neutral
    rationale: ''
- id: REQ-072
  status: complete
  group: 11. Platform, Infrastructure & Delivery
  category: Commercial Positioning
  priority: SHOULD
  type: infrastructure
  description: 'Core product is open source: Docker Compose, Helm chart, UI, compiler, SQLGlot layer,
    Trino backend.'
  use_case: Open-source core makes Provisa accessible to teams that cannot budget for commercial data
    access platforms.
  tests:
  - tests/unit/test_infra_requirements.py
  since: 2026-01
  scenario: 'Given a team evaluating open-source data platforms

    When they access the Provisa repository

    Then Docker Compose, Helm chart, UI, compiler, SQLGlot layer, and Trino backend are all available

    '
  integration_test: not_required
  integration_test_reason: Open-source positioning is a commercial/deployment fact, not verifiable behavior.
  stakeholders:
  - executive
  - app-developer
  competitive_position:
    status: parity
    rationale: Open-core model (OSS core + SaaS/Enterprise tiers) is the standard commercial model for
      Hasura, Trino (Starburst), and Dremio.
- id: REQ-073
  status: complete
  superseded_by: REQ-1243
  group: 11. Platform, Infrastructure & Delivery
  category: Commercial Positioning
  priority: MAY
  type: infrastructure
  description: 'SaaS tier: two isolation lanes. (a) Pooled shared-Trino cluster for free/small orgs ("trino-level
    isolation"). (b) BYO (bring-your-own) federation engine for enterprise — customer points Provisa at
    their own Databricks/Snowflake cluster. Both lanes route through the same _govern_and_route/_execute_plan
    pipeline (REQ-1244). Org-scoped sessions are pinned to one engine per deployment (subdomain → org
    → engine → credentials).'
  use_case: Two-lane model lets Provisa serve both self-service free/small orgs on shared Trino and enterprise
    customers on their own federation engines without code branches or dual pipelines.
  tests:
  - tests/unit/test_control_plane.py
  - tests/unit/test_process_requirements.py
  - tests/integration/test_multi_org_routing.py
  since: 2026-01
  related_reqs:
  - REQ-1243
  - REQ-1244
  scenario: 'Given an organization in the free/small tier

    When they query via a Provisa SaaS subdomain

    Then the query routes to the shared pooled Trino cluster at trino-level isolation

    Given an enterprise customer who subscribed to BYO

    When they point Provisa to their own Databricks instance

    Then queries route through that customer''s federation engine instance

    And the two lanes share the same _govern_and_route/_execute_plan pipeline

    '
  integration_test: deferred
  integration_test_reason: SaaS tier with customer BYO engine requires real cloud infrastructure and multi-customer
    deployments.
  stakeholders:
  - ops
  - executive
  competitive_position:
    status: parity
    rationale: Hosted SaaS with BYO/BYOC engine is standard in Hasura Cloud, Dremio Cloud, and Starburst
      Galaxy.
- id: REQ-074
  status: complete
  group: 11. Platform, Infrastructure & Delivery
  category: Commercial Positioning
  priority: MAY
  type: infrastructure
  description: 'Enterprise tier: SLA guarantees, dedicated support, advanced audit logging, compliance
    reporting.'
  use_case: Enterprise tier SLA and audit logging meets regulated-industry compliance and procurement
    requirements.
  tests:
  - tests/unit/test_process_requirements.py
  - tests/unit/test_sla_monitor.py
  since: 2026-01
  scenario: 'Given a regulated-industry customer with compliance requirements

    When they subscribe to the enterprise tier

    Then they receive SLA guarantees, dedicated support, advanced audit logging, and compliance reporting

    '
  integration_test: deferred
  integration_test_reason: Enterprise SLA and audit logging require production infrastructure and time.
  stakeholders:
  - compliance
  - ops
  - executive
  competitive_position:
    status: parity
    rationale: Enterprise tier with SLA, dedicated support, and compliance reporting is standard across
      Hasura, Dremio, Denodo, and Starburst.
- id: REQ-169
  status: complete
  group: 11. Platform, Infrastructure & Delivery
  category: Infrastructure
  priority: SHOULD
  type: infrastructure
  description: Trino 480 with Iceberg results catalog (JDBC on PG, native S3 filesystem).
  use_case: Trino 480 with Iceberg results catalog provides modern open-table-format storage for all redirected
    results.
  code:
  - helm/
  - docker-compose.yml
  tests:
  - tests/e2e/test_health_checks.py
  - tests/integration/test_infra.py
  - tests/unit/test_infra_requirements.py
  since: 2026-01
  scenario: 'Given the Provisa stack is running

    When results are redirected to the catalog

    Then they are stored via Iceberg using JDBC on PostgreSQL with native S3 filesystem

    '
  integration_test: required
  integration_test_reason: Trino 480 and Iceberg catalog behavior require real query engine and storage.
  e2e: true
  e2e_reason: Trino 480 with Iceberg results catalog using JDBC on PG and S3 filesystem requires a real
    running Trino engine and storage backend to verify end-to- end result redirection.
  stakeholders:
  - ops
  - data-engineer
  competitive_position:
    status: neutral
    rationale: ''
- id: REQ-170
  status: complete
  group: 11. Platform, Infrastructure & Delivery
  category: Infrastructure
  priority: SHOULD
  type: infrastructure
  description: '`start-ui.sh --reset-volumes` for Docker crash recovery.'
  use_case: reset-volumes flag gives developers a one-command recovery path after Docker volume corruption.
  code:
  - helm/
  - docker-compose.yml
  tests:
  - tests/e2e/test_health_checks.py
  - tests/integration/test_infra.py
  - tests/unit/test_infra_requirements.py
  since: 2026-01
  scenario: 'Given Docker volumes have been corrupted after a crash

    When a developer runs start-ui.sh --reset-volumes

    Then all volumes are reset and the stack starts cleanly

    '
  integration_test: required
  integration_test_reason: Docker crash recovery requires real container lifecycle and volume management.
  e2e: true
  e2e_reason: The --reset-volumes crash recovery flag requires real Docker containers and volume lifecycle
    to verify that volumes are reset and the stack starts cleanly.
  stakeholders:
  - ops
  competitive_position:
    status: neutral
    rationale: ''
- id: REQ-171
  status: complete
  group: 11. Platform, Infrastructure & Delivery
  category: Infrastructure
  priority: MUST
  type: behavioral
  description: MinIO results bucket auto-created at startup.
  use_case: MinIO bucket auto-creation at startup prevents first-run failures due to missing redirect
    storage.
  code:
  - helm/
  - docker-compose.yml
  tests:
  - tests/e2e/test_health_checks.py
  - tests/integration/test_infra.py
  - tests/unit/test_infra_requirements.py
  - provisa-ui/e2e/infrastructure.spec.ts
  since: 2026-01
  scenario: 'Given the Provisa stack starts for the first time

    When the startup sequence runs

    Then the MinIO results bucket is created automatically without manual intervention

    '
  integration_test: required
  integration_test_reason: MinIO bucket auto-creation at startup requires real S3-compatible storage.
  e2e: true
  e2e_reason: MinIO bucket auto-creation at startup requires a real S3-compatible storage service running
    to verify the bucket is created without manual intervention.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - ops
  competitive_position:
    status: neutral
    rationale: ''
- id: REQ-223
  status: complete
  group: 11. Platform, Infrastructure & Delivery
  category: Installer & Packaging
  priority: SHOULD
  type: infrastructure
  description: Single-executable installer that bundles the Provisa platform into one download. Underlying
    technology (Python server, PostgreSQL admin DB, Trino query engine, React UI) is hidden from the user.
    Source datasets are NOT bundled -- they connect over the wire.
  use_case: Single-executable installer hides platform complexity so analysts can run Provisa without
    DevOps knowledge.
  code:
  - packaging/
  tests:
  - tests/e2e/test_installer.py
  - tests/unit/test_infra_requirements.py
  - tests/unit/test_live_engine_full.py
  since: 2026-01
  scenario: 'Given a non-technical analyst downloading Provisa

    When they run the single-executable installer

    Then the full platform starts without requiring knowledge of Python, PostgreSQL, Trino, or Docker

    '
  integration_test: deferred
  integration_test_reason: 'Single-executable installer behavior requires real system binaries and platform-specific
    packaging. [deferred: requires external infrastructure not in CI stack]'
  e2e: false
  e2e_reason: 'Single-executable installer behavior can only be verified by running the actual installer
    binary on a real OS and confirming the full platform starts without prerequisites visible to the user.
    [deferred: requires full UI/installer stack not available in CI]'
  stakeholders:
  - ops
  - app-developer
  competitive_position:
    status: ahead
    rationale: Single-executable installer bundling all platform components (Python server, PG, Trino,
      React UI) into one download is more accessible than Hasura's Docker-required deployment or Dremio's
      multi-component install.
- id: REQ-224
  status: complete
  group: 11. Platform, Infrastructure & Delivery
  category: Installer & Packaging
  priority: SHOULD
  type: infrastructure
  description: Installer expands into a hidden directory (`~/.provisa/`) containing all services. User
    interacts via `provisa start`, `provisa stop`, `provisa status`, `provisa open` (opens browser).
  use_case: CLI commands (start/stop/status/open) give non-technical users a simple interface to manage
    the platform.
  code:
  - packaging/
  tests:
  - tests/e2e/test_installer.py
  - tests/unit/test_infra_requirements.py
  since: 2026-01
  scenario: 'Given Provisa is installed

    When a user runs provisa start, provisa stop, provisa status, or provisa open

    Then the corresponding lifecycle action is performed against services in ~/.provisa/

    '
  integration_test: deferred
  integration_test_reason: 'CLI lifecycle commands require real service orchestration and process management.
    [deferred: requires external infrastructure not in CI stack]'
  e2e: false
  e2e_reason: 'CLI commands (provisa start/stop/status/open) managing services in ~/.provisa/ require
    a real installed environment and process management to verify lifecycle actions complete correctly.
    [deferred: requires full UI/installer stack not available in CI]'
  stakeholders:
  - ops
  - app-developer
  competitive_position:
    status: ahead
    rationale: Hidden ~/.provisa/ layout with provisa start/stop/status/open CLI is a consumer-grade DX
      not present in Hasura, Trino, Dremio, or Denodo.
- id: REQ-225
  status: complete
  group: 11. Platform, Infrastructure & Delivery
  category: Installer & Packaging
  priority: SHOULD
  type: infrastructure
  description: Default deployment uses embedded PostgreSQL (postgres:16) for admin DB and bundled Trino
    for query federation. Vertical scaling by default -- single machine, increase resources as needed.
  use_case: Embedded PostgreSQL and Trino let a single machine run the full platform out of the box.
  code:
  - packaging/
  tests:
  - tests/e2e/test_installer.py
  - tests/unit/test_infra_requirements.py
  since: 2026-01
  scenario: 'Given a fresh Provisa installation with no external services configured

    When the platform starts

    Then it uses embedded PostgreSQL 16 and bundled Trino for all operations on the single machine

    '
  integration_test: deferred
  integration_test_reason: 'Embedded PostgreSQL and Trino startup requires real containers and service
    initialization. [deferred: requires external infrastructure not in CI stack]'
  e2e: false
  e2e_reason: 'Embedded PostgreSQL and bundled Trino startup on a single machine requires real container
    initialization and service health checks to verify the platform runs out of the box. [deferred: requires
    full UI/installer stack not available in CI]'
  stakeholders:
  - ops
  - app-developer
  competitive_position:
    status: ahead
    rationale: Embedded PostgreSQL + bundled Trino as default with vertical scaling first is a simpler
      getting-started path than any competitor requiring external infrastructure.
- id: REQ-226
  status: complete
  group: 11. Platform, Infrastructure & Delivery
  category: Installer & Packaging
  priority: MAY
  type: infrastructure
  description: Users can later connect their own Trino cluster, Spark, external auth provider, or external
    PostgreSQL for the admin DB via config. Pointing to an external Trino instance is the primary scale-out
    mechanism.
  use_case: External Trino and auth provider support lets organisations scale Provisa onto existing enterprise
    infrastructure.
  code:
  - packaging/
  tests:
  - tests/e2e/test_installer.py
  - tests/unit/test_infra_requirements.py
  since: 2026-01
  scenario: 'Given an organisation with an existing Trino cluster or auth provider

    When they update the Provisa config to point to external services

    Then Provisa uses those external services instead of the bundled defaults

    '
  integration_test: deferred
  integration_test_reason: 'External service connectivity requires real network calls and authentication.
    [deferred: requires external infrastructure not in CI stack]'
  stakeholders:
  - ops
  - data-engineer
  competitive_position:
    status: parity
    rationale: External Trino/Spark/auth/PG swap-in via config is standard enterprise extensibility.
- id: REQ-227
  status: complete
  group: 11. Platform, Infrastructure & Delivery
  category: Installer & Packaging
  priority: SHOULD
  type: infrastructure
  description: 'AF2 is delivered in OS phases. AF2a (immediate): macOS arm64/x86 signed + notarized `.dmg`.
    AF2b (follow-on): Linux `.AppImage`. AF2c (follow-on): Windows signed `.exe`. Each phase bundles the
    container runtime for its platform — no Docker, OrbStack, or Colima prerequisite visible to the user.'
  use_case: OS-native signed bundles (DMG/AppImage/EXE) let users install Provisa like any desktop app
    without Docker.
  code:
  - packaging/
  tests:
  - tests/e2e/test_installer.py
  - tests/unit/test_infra_requirements.py
  since: 2026-01
  scenario: 'Given a user on macOS, Linux, or Windows

    When they install Provisa via the OS-native signed bundle

    Then no Docker, OrbStack, or Colima is required as a prerequisite

    '
  integration_test: deferred
  integration_test_reason: 'OS-native signed bundles require platform-specific build artifacts and code
    signing. [deferred: requires external infrastructure not in CI stack]'
  stakeholders:
  - ops
  - app-developer
  competitive_position:
    status: ahead
    rationale: Phased native app delivery (macOS DMG → Linux AppImage → Windows EXE) with signing and
      notarization is more polished than Hasura's Docker-only distribution or Dremio's installer.
- id: REQ-228
  status: complete
  group: 11. Platform, Infrastructure & Delivery
  category: Installer & Packaging
  priority: SHOULD
  type: infrastructure
  description: 'Phase 1 (AF1, complete): Shell script installer with Docker Compose, `provisa` CLI wrapper,
    state in `~/.provisa/`. Requires Docker/OrbStack/Colima. Phase 2 (AF2): Airgapped native app bundle
    — Lima + containerd embedded, all service images bundled as `.tar` archives, zero outbound network
    at install or runtime. AF3 (native OS packages via Homebrew/.deb/.rpm) is superseded by AF2.'
  use_case: AF2 airgapped bundle removes the Docker prerequisite so enterprise users with no internet
    access can install.
  code:
  - packaging/
  tests:
  - tests/e2e/test_installer.py
  - tests/unit/test_infra_requirements.py
  since: 2026-01
  scenario: 'Given an enterprise environment with no internet access

    When a user installs the AF2 native app bundle

    Then all services start using bundled Lima + containerd and .tar image archives without network calls

    '
  integration_test: deferred
  integration_test_reason: 'Airgapped bundle with Lima/containerd requires real container runtime and
    image loading. [deferred: requires external infrastructure not in CI stack]'
  stakeholders:
  - ops
  competitive_position:
    status: ahead
    rationale: Airgapped native app bundle (Lima + containerd, no Docker required) removes the Docker
      prerequisite that blocks enterprise adoption of Hasura and other container-based platforms.
- id: REQ-254
  status: complete
  group: 11. Platform, Infrastructure & Delivery
  category: Testing & Quality
  priority: MUST
  type: constraint
  description: Integration tests must use Docker — spin up required containers (PG, Trino, Redis, etc.),
    install dependencies, run tests, and tear down containers. Tests must not assume a pre-existing Docker
    Compose stack.
  use_case: Integration tests that spin up Docker containers ensure tests validate against real service
    behaviour.
  tests:
  - tests/unit/test_infra_requirements.py
  since: 2026-01
  scenario: 'Given an integration test suite

    When the tests run

    Then all required containers are spun up fresh, tests execute against them, and containers are torn
    down

    '
  integration_test: deferred
  integration_test_reason: 'Integration tests spinning up Docker containers is a test infrastructure requirement.
    [deferred: requires external infrastructure not in CI stack]'
  stakeholders:
  - app-developer
  - ops
  competitive_position:
    status: neutral
    rationale: ''
- id: REQ-255
  status: complete
  group: 11. Platform, Infrastructure & Delivery
  category: Testing & Quality
  priority: MUST
  type: constraint
  description: Unit tests must mock all external components (databases, Trino, Redis, HTTP endpoints,
    file system where applicable). No unit test should require a running external service.
  use_case: Unit tests that mock all external components run fast and independently of any running infrastructure.
  tests:
  - tests/unit/test_infra_requirements.py
  since: 2026-01
  scenario: 'Given a unit test

    When it runs in isolation

    Then no running database, Trino instance, Redis, or HTTP service is required

    '
  integration_test: not_required
  integration_test_reason: Unit test mocking strategy is a development practice, not runtime behavior.
  stakeholders:
  - app-developer
  competitive_position:
    status: neutral
    rationale: ''
- id: REQ-294
  status: complete
  group: 11. Platform, Infrastructure & Delivery
  category: Installer & Packaging
  priority: MUST
  type: constraint
  description: The distribution must be fully airgap-capable. No outbound network calls at install time
    or first launch. All container images are bundled inside the app and loaded via `ctr images import`.
    Image references in docker-compose use digests, not tags. Suitable for enterprise environments with
    no internet access.
  use_case: Fully airgapped bundle lets enterprises in no-internet environments install and run Provisa
    from local media.
  code:
  - packaging/
  tests:
  - tests/e2e/test_installer.py
  - tests/unit/test_infra_requirements.py
  since: 2026-01
  scenario: 'Given an enterprise environment with no internet access

    When Provisa is installed and first launched

    Then no outbound network calls are made and all images load from bundled .tar archives

    '
  integration_test: deferred
  integration_test_reason: 'Airgap-capable distribution requires real network isolation and image bundle
    loading. [deferred: requires external infrastructure not in CI stack]'
  e2e: false
  e2e_reason: 'Full airgap capability with no outbound network calls and image loading from bundled .tar
    archives can only be verified in a real network-isolated environment running the actual distribution.
    [deferred: requires full UI/installer stack not available in CI]'
  stakeholders:
  - ops
  competitive_position:
    status: ahead
    rationale: Full airgap capability with all images bundled and loaded via ctr images import at install
      time is not available in Hasura Cloud, Dremio Cloud, or standard Starburst/Trino distributions.
- id: REQ-302
  status: complete
  group: 11. Platform, Infrastructure & Delivery
  category: OpenTelemetry Instrumentation
  priority: SHOULD
  type: infrastructure
  description: 'OpenTelemetry (OTel) instrumentation across all Provisa components — traces, metrics,
    and logs exported via OTLP. Covers: FastAPI app, GraphQL compiler, SQL executor, Trino driver, admin
    GraphQL API, Arrow Flight server, cache layer, MV subsystem, discovery pipeline, and all source adapters.
    Structured log emission with trace_id/span_id correlation.'
  use_case: OTel instrumentation across all components gives operators unified traces, metrics, and logs
    in one backend.
  code:
  - provisa/otel_compat.py
  - provisa/api/app.py
  tests:
  - tests/unit/test_executor_trino.py
  - tests/unit/test_flight_modes.py
  - tests/unit/test_otel_requirements.py
  - tests/unit/test_otel_trace_pipeline.py
  since: 2026-01
  scenario: 'Given OTel is configured

    When any Provisa component processes a request

    Then traces, metrics, and logs with trace_id/span_id correlation are exported via OTLP

    '
  integration_test: required
  integration_test_reason: OTel instrumentation export requires real trace/metric collectors and network
    transport.
  e2e: true
  e2e_reason: OTel traces, metrics, and logs exported via OTLP require a real running collector endpoint
    and live request processing to verify end-to-end telemetry export with trace/span correlation.
  stakeholders:
  - ops
  - compliance
  competitive_position:
    status: parity
    rationale: OpenTelemetry instrumentation across all components is now standard in enterprise platforms
      (Hasura, Dremio, Starburst all offer OTEL support).
- id: REQ-303
  status: complete
  group: 11. Platform, Infrastructure & Delivery
  category: OpenTelemetry Instrumentation
  priority: SHOULD
  type: infrastructure
  description: 'OTel integration via opentelemetry-sdk, opentelemetry-instrumentation-fastapi, opentelemetry-instrumentation-httpx,
    and OTLP exporter. Auto-instrumentation applies to FastAPI and httpx. Manual span/metric instrumentation
    at: compile_query, execute_trino, execute_direct, handle_api_query, cache check/store, MV rewrite,
    RLS inject, masking inject. Export endpoint configured via environment variable (default: `http://localhost:4317`).'
  use_case: Auto-instrumentation of FastAPI and httpx combined with manual spans gives full request visibility
    end-to-end.
  code:
  - provisa/otel_compat.py
  - provisa/api/app.py
  tests:
  - tests/integration/test_cache_store.py
  - tests/integration/test_direct_exec.py
  - tests/unit/test_cache_store.py
  - tests/unit/test_mask_inject.py
  - tests/unit/test_otel_requirements.py
  since: 2026-01
  scenario: 'Given OTel is enabled with OTEL_EXPORTER_OTLP_ENDPOINT set

    When a query is processed through compile, execute, cache, RLS, and masking stages

    Then manual spans are emitted for each stage and exported to the configured endpoint

    '
  integration_test: required
  integration_test_reason: Manual span instrumentation at multiple stages requires real query execution
    path.
  stakeholders:
  - ops
  - compliance
  competitive_position:
    status: neutral
    rationale: ''
- id: REQ-330
  status: complete
  superseded_by: REQ-815
  group: 11. Platform, Infrastructure & Delivery
  category: Infrastructure & Observability
  priority: MAY
  type: infrastructure
  description: 'Development observability stack available in docker-compose under `observability` profile
    (opt-in). OTel Collector receives OTLP on 4317/4318 and exports metrics to Prometheus and traces to
    Tempo. Grafana on port 3100 with Prometheus and Tempo datasources pre-provisioned and a Provisa dashboard
    included. Provisa app configured via `OTEL_EXPORTER_OTLP_ENDPOINT` env var (default: `http://localhost:4317`).'
  use_case: Opt-in observability stack with OTel, Prometheus, Tempo, and Grafana lets developers debug
    performance locally.
  code:
  - helm/
  - docker-compose.yml
  tests:
  - tests/e2e/test_health_checks.py
  - tests/integration/test_infra.py
  - tests/unit/test_infra_requirements.py
  since: 2026-01
  scenario: 'Given docker-compose is started with the observability profile

    When the Provisa app emits OTLP telemetry

    Then metrics appear in Prometheus, traces in Tempo, and both are visible in Grafana on port 3100

    '
  integration_test: required
  integration_test_reason: Observability stack with Prometheus, Tempo, and Grafana requires real metric/trace
    backends.
  e2e: true
  e2e_reason: The observability stack (OTel Collector, Prometheus, Tempo, Grafana) requires real running
    backends and live telemetry emission to verify metrics appear in Prometheus and traces in Tempo via
    the Grafana UI.
  stakeholders:
  - ops
  competitive_position:
    status: parity
    rationale: Dev observability stack (OTel Collector + Prometheus + Tempo + Grafana) in Docker Compose
      is standard developer tooling.
- id: REQ-528
  status: complete
  group: 11. Platform, Infrastructure & Delivery
  category: Infrastructure
  priority: SHOULD
  type: structural
  description: 'The path to the Provisa config file is controlled by the `PROVISA_CONFIG` environment
    variable, which every launcher sets and which has no default — an unset variable is a startup error
    naming the variable, not a read of `config/provisa.yaml`. Nothing ships that path: the Dockerfile
    bakes an explicit config set that excludes it and no deploy pushes it, so the former default surfaced
    as a FileNotFoundError several frames inside Trino config generation.'
  use_case: Configurable config path lets operators run multiple Provisa instances with different configs
    on the same host.
  code:
  - provisa/core/config_location.py
  - provisa/api/app.py
  - provisa/api/admin/_config_io.py
  tests:
  - tests/integration/test_infra.py
  - tests/unit/test_admin_requirements.py
  since: 2026-01
  scenario: 'Given PROVISA_CONFIG is set to a custom path

    When the Provisa API server starts

    Then it reads configuration from that path

    And given PROVISA_CONFIG is unset

    Then startup fails naming PROVISA_CONFIG rather than reading any default path

    '
  integration_test: not_required
  integration_test_reason: Config file path resolution is pure logic in environment variable handling.
  stakeholders:
  - ops
  - app-developer
  competitive_position:
    status: neutral
    rationale: ''
- id: REQ-539
  status: complete
  group: 11. Platform, Infrastructure & Delivery
  category: Infrastructure
  priority: MUST
  type: behavioral
  description: 'The `GET /health` (or `HEAD /health`) and `GET /setup/status` endpoints are always unauthenticated
    — they bypass the `Authorization: Bearer` requirement even when an auth provider is configured. All
    other endpoints require authentication when `auth.provider` is set.'
  use_case: Unauthenticated health and setup-status endpoints let load balancers, orchestrators, and first-run
    UIs check server availability without credentials.
  code:
  - provisa/api/app.py
  tests:
  - tests/e2e/test_health_checks.py
  - tests/integration/test_infra.py
  - tests/unit/test_infra_requirements.py
  - provisa-ui/e2e/infrastructure.spec.ts
  since: 2026-01
  scenario: 'Given an auth provider is configured

    When GET /health or GET /setup/status is called without an Authorization header

    Then the request succeeds; all other endpoints return 401 without a valid bearer token

    '
  integration_test: required
  integration_test_reason: Unauthenticated health/setup endpoints require real HTTP transport and auth
    middleware.
  e2e: true
  e2e_reason: Verifying that unauthenticated health/setup endpoints bypass auth middleware while all other
    endpoints return 401 requires a real running server with auth configured and HTTP requests across
    the network boundary.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - ops
  - app-developer
  competitive_position:
    status: neutral
    rationale: ''
- id: REQ-545
  status: complete
  group: 11. Platform, Infrastructure & Delivery
  category: OpenTelemetry Instrumentation
  priority: SHOULD
  type: infrastructure
  description: 'Provisa runs two independent OTLP export paths: the operator''s internal collector and
    an optional Provisa support endpoint. Each path has its own telemetry filter configuration and applies
    independently.'
  use_case: Independent export paths let operators share diagnostic telemetry with Provisa support while
    maintaining separate redaction policies for their internal collector.
  code:
  - provisa/api/otel_setup.py
  tests:
  - tests/unit/test_otel_requirements.py
  since: 2026-01
  scenario: 'Given both internal collector and support endpoint are configured

    When telemetry is emitted

    Then each path applies its own filter configuration independently

    '
  integration_test: required
  integration_test_reason: Dual OTLP export paths require real telemetry collectors and independent filter
    chains.
  stakeholders:
  - ops
  - compliance
  competitive_position:
    status: ahead
    rationale: Dual OTLP export paths (operator's collector + optional Provisa support endpoint) with
      independent filter configs is more flexible than single-path telemetry in Hasura or Dremio.
- id: REQ-546
  status: complete
  group: 11. Platform, Infrastructure & Delivery
  category: OpenTelemetry Instrumentation
  priority: MUST
  type: constraint
  description: Telemetry filters run inside a wrapping `_FilteringExporter` before spans leave the process.
    Original span objects are never mutated — filtering operates on copies.
  use_case: Non-mutating span filtering ensures telemetry redaction cannot corrupt spans in the internal
    pipeline.
  code:
  - provisa/api/otel_setup.py
  tests:
  - tests/unit/test_otel_requirements.py
  since: 2026-01
  scenario: 'Given telemetry filters are configured

    When a span is processed by _FilteringExporter

    Then a copy of the span is filtered and the original span object remains unchanged

    '
  integration_test: not_required
  integration_test_reason: Non-mutating span filtering is pure logic in filter implementation.
  stakeholders:
  - compliance
  - ops
  competitive_position:
    status: neutral
    rationale: ''
- id: REQ-547
  status: complete
  group: 11. Platform, Infrastructure & Delivery
  category: OpenTelemetry Instrumentation
  priority: MUST
  type: constraint
  description: SQL literal redaction defaults to `true` on the support telemetry path (`support_telemetry_filter.redact_sql_literals`),
    ensuring query data is not shared with Provisa support by default.
  use_case: Default-on SQL redaction on the support path protects sensitive query data without requiring
    operator configuration.
  code:
  - provisa/api/otel_setup.py
  tests:
  - tests/unit/test_otel_requirements.py
  - tests/integration/test_registration_governance_integration.py
  since: 2026-01
  scenario: 'Given no explicit support_telemetry_filter.redact_sql_literals config is set

    When telemetry is exported to the Provisa support endpoint

    Then SQL literals in spans are redacted by default

    '
  integration_test: required
  integration_test_reason: SQL redaction default on support telemetry requires real span processing and
    export.
  stakeholders:
  - compliance
  competitive_position:
    status: ahead
    rationale: SQL literal redaction defaulting to true on the support telemetry path protects query data
      from leaving the operator's infrastructure by default — a privacy-forward default not explicitly
      documented by Hasura or Dremio.
- id: REQ-548
  status: complete
  group: 11. Platform, Infrastructure & Delivery
  category: OpenTelemetry Instrumentation
  priority: MUST
  type: constraint
  description: The Provisa support OTLP endpoint (`support_endpoint` config or `PROVISA_SUPPORT_OTLP_ENDPOINT`
    env var) is disabled by default. When unset, no telemetry data leaves the operator's infrastructure
    via this path.
  use_case: Opt-in support endpoint ensures no data reaches Provisa infrastructure without explicit operator
    configuration.
  code:
  - provisa/api/otel_setup.py
  tests:
  - tests/unit/test_otel_requirements.py
  since: 2026-01
  scenario: 'Given PROVISA_SUPPORT_OTLP_ENDPOINT is not set

    When the platform runs and emits telemetry

    Then no telemetry is sent to Provisa infrastructure

    '
  integration_test: not_required
  integration_test_reason: Opt-in support endpoint configuration is environment variable handling logic.
  stakeholders:
  - compliance
  - ops
  competitive_position:
    status: neutral
    rationale: ''
- id: REQ-549
  status: complete
  group: 11. Platform, Infrastructure & Delivery
  category: OpenTelemetry Instrumentation
  priority: SHOULD
  type: behavioral
  description: Provisa takes the OTLP transport from an explicit declaration — `OTEL_EXPORTER_OTLP_PROTOCOL`,
    else `observability.protocol`, else the spec default `grpc`. `http/protobuf` uses OTLP/HTTP with `/v1/traces`,
    `/v1/metrics` and `/v1/logs` appended; `grpc` uses OTLP/gRPC in insecure mode. An unrecognised value
    raises. The URL scheme is never read as the transport.
  use_case: The OTLP spec writes a gRPC endpoint as `http://collector:4317` exactly as it writes an HTTP
    one as `http://collector:4318`, so the scheme cannot distinguish them. Inferring it from the scheme
    built an HTTP exporter that POSTed every batch to the gRPC port, where the collector reset the connection
    and the batch processor dropped the spans — the SaaS node exported nothing while its endpoint, collector
    and parquet writer were all healthy, and the ops traces/queries/metrics/logs reports read zero rows.
  code:
  - provisa/api/otel_setup.py
  tests:
  - tests/unit/test_otel_requirements.py
  - tests/integration/test_registration_governance_integration.py
  since: 2026-01
  scenario: 'Given an OTLP endpoint whose declared protocol is http/protobuf

    When Provisa configures the exporter

    Then OTLP/HTTP is used with path suffixes /v1/traces, /v1/metrics, /v1/logs appended automatically

    '
  integration_test: required
  integration_test_reason: OTLP transport detection and endpoint connection requires real network communication.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - ops
  competitive_position:
    status: neutral
    rationale: ''
- id: REQ-558
  status: complete
  superseded_by: REQ-618
  group: 11. Platform, Infrastructure & Delivery
  category: Infrastructure
  priority: SHOULD
  type: infrastructure
  description: The development backend API (`uvicorn main:app`) listens on port 8001 when launched via
    `start-ui.sh` or manually. The AppImage and installer paths use port 8000 as the default.
  use_case: Consistent port convention between dev and production paths prevents connection errors when
    switching environments.
  code:
  - start-ui.sh
  - provisa/api/app.py
  tests:
  - tests/e2e/test_health_checks.py
  - tests/integration/test_infra.py
  - tests/unit/test_infra_requirements.py
  since: 2026-01
  scenario: 'Given the dev backend is started via start-ui.sh

    When the server is ready

    Then it listens on port 8001; the AppImage and installer deployments listen on port 8000

    '
  integration_test: required
  integration_test_reason: Port assignment and listening behavior requires real server socket and network
    binding.
  e2e: true
  e2e_reason: Confirming the dev backend listens on port 8001 and AppImage/installer deployments on port
    8000 requires a real running server with a live socket to verify network binding.
  stakeholders:
  - ops
  - app-developer
  competitive_position:
    status: neutral
    rationale: ''
- id: REQ-559
  status: complete
  group: 11. Platform, Infrastructure & Delivery
  category: Infrastructure
  priority: SHOULD
  type: infrastructure
  description: The `start-ui.sh` script starts the Vite UI dev server on port 3000. The production installer
    serves the UI via `provisa/ui_server.py` on port 3000.
  use_case: Fixed port 3000 for the UI dev server ensures developers can rely on a consistent URL during
    local development.
  code:
  - start-ui.sh
  - provisa/ui_server.py
  tests:
  - tests/e2e/test_health_checks.py
  - tests/integration/test_infra.py
  - tests/unit/test_infra_requirements.py
  since: 2026-01
  scenario: 'Given start-ui.sh is running or the production installer is active

    When the UI server starts

    Then it listens on port 3000 in both dev and production modes

    '
  integration_test: required
  integration_test_reason: Fixed port UI dev server requires real Vite dev server startup and binding.
  e2e: true
  e2e_reason: Confirming the UI dev server and production UI server both bind to port 3000 requires real
    Vite/ui_server startup and a live network socket check.
  stakeholders:
  - app-developer
  - ops
  competitive_position:
    status: neutral
    rationale: ''
- id: REQ-560
  status: complete
  group: 11. Platform, Infrastructure & Delivery
  category: Installer & Packaging
  priority: SHOULD
  type: structural
  description: The Provisa API default port is 8000 in AppImage/installer/Kubernetes deployments (configurable
    via `--api-port` wizard prompt or `api_port` in config). The Arrow Flight server listens on port 8815
    by default.
  use_case: Documented default ports let operators configure firewalls and load balancers without reading
    source code.
  code:
  - provisa/core/models.py
  - provisa/api/app.py
  tests:
  - tests/e2e/test_health_checks.py
  - tests/unit/test_core_registration.py
  since: 2026-01
  scenario: 'Given an AppImage or Kubernetes deployment with no port override

    When the API and Arrow Flight servers start

    Then the API listens on port 8000 and Arrow Flight listens on port 8815

    '
  integration_test: deferred
  integration_test_reason: 'Default port assignment across deployment modes requires real server startup.
    [deferred: requires external infrastructure not in CI stack]'
  e2e: false
  e2e_reason: 'Verifying API listens on port 8000 and Arrow Flight on port 8815 in AppImage/Kubernetes
    deployments requires real server startup and live socket binding verification. [deferred: requires
    full UI/installer stack not available in CI]'
  stakeholders:
  - ops
  competitive_position:
    status: neutral
    rationale: ''
- id: REQ-561
  status: complete
  group: 11. Platform, Infrastructure & Delivery
  category: Installer & Packaging
  priority: MUST
  type: constraint
  description: In a multi-node AppImage deployment, secondary nodes run only the Provisa API and a federation
    engine worker. Singleton stateful services (PostgreSQL, PgBouncer, MinIO, Redis) run exclusively on
    the primary node.
  use_case: Clear primary/secondary service split prevents operators from accidentally running duplicate
    stateful services that would cause data inconsistency.
  code:
  - packaging/
  tests:
  - tests/e2e/test_installer.py
  - tests/unit/test_infra_requirements.py
  - tests/unit/test_live_engine_full.py
  since: 2026-01
  scenario: 'Given a multi-node AppImage deployment

    When secondary nodes start

    Then only the Provisa API and federation engine worker run; PostgreSQL, PgBouncer, MinIO, and

    Redis run only on the primary node

    '
  integration_test: deferred
  integration_test_reason: 'Multi-node primary/secondary service split requires real distributed deployment.
    [deferred: requires external infrastructure not in CI stack]'
  stakeholders:
  - ops
  competitive_position:
    status: ahead
    rationale: Multi-node AppImage deployment with stateless secondary API nodes and singleton services
      on primary node is a production HA pattern not available in Hasura's self-hosted model without Kubernetes.
- id: REQ-562
  status: complete
  superseded_by: REQ-1229
  group: 11. Platform, Infrastructure & Delivery
  category: Installer & Packaging
  priority: MUST
  type: behavioral
  description: In a multi-node deployment, secondary Provisa API instances are stateless and read all
    configuration (sources, tables, relationships, roles, RLS rules) from the primary node's PostgreSQL
    database at startup. No manual config sync between nodes is required.
  use_case: Stateless secondary nodes reading from a single PostgreSQL source ensures all API nodes serve
    identical, authoritative configuration without synchronization overhead.
  code:
  - provisa/api/app.py
  - provisa/core/catalog.py
  tests:
  - tests/e2e/test_installer.py
  - tests/unit/test_core_registration.py
  since: 2026-01
  scenario: 'Given a multi-node deployment with a primary PostgreSQL database

    When a secondary API node starts

    Then it reads all configuration from the primary PostgreSQL without manual sync

    '
  integration_test: deferred
  integration_test_reason: 'Stateless secondary nodes reading configuration requires real multi-node database
    setup. [deferred: requires external infrastructure not in CI stack]'
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - ops
  competitive_position:
    status: ahead
    rationale: Config-at-startup propagation from primary PG to secondary API nodes eliminates a separate
      config service or etcd dependency seen in competing distributed deployments.
- id: REQ-563
  status: complete
  group: 11. Platform, Infrastructure & Delivery
  category: Installer & Packaging
  priority: SHOULD
  type: behavioral
  description: The AppImage first-launch in `--non-interactive` mode installs a systemd unit (`/etc/systemd/system/provisa.service`)
    so Provisa starts automatically on boot. The AppImage generates credentials during first-launch and
    writes them to `~/.provisa/config.yaml`.
  use_case: Systemd integration and auto-generated credentials enable unattended cloud-init and Terraform
    provisioning without manual post-install steps.
  code:
  - packaging/
  tests:
  - tests/e2e/test_installer.py
  - tests/unit/test_infra_requirements.py
  since: 2026-01
  scenario: 'Given AppImage first-launch runs with --non-interactive

    When the first-launch sequence completes

    Then a systemd unit is installed for boot autostart and credentials are written to ~/.provisa/config.yaml

    '
  integration_test: deferred
  integration_test_reason: 'Systemd integration and auto-generated credentials require real OS-level service
    management. [deferred: requires external infrastructure not in CI stack]'
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - ops
  competitive_position:
    status: ahead
    rationale: Systemd unit auto-install in non-interactive mode with credential generation is more production-ready
      than Hasura's shell script or Dremio's manual systemd setup.
- id: REQ-564
  status: complete
  group: 11. Platform, Infrastructure & Delivery
  category: Infrastructure
  priority: SHOULD
  type: infrastructure
  description: The Terraform AWS deployment path provisions a full multi-node Provisa cluster including
    VPC with two public subnets across two AZs, EC2 instances, an ALB on port 8000 for the HTTP API, and
    an NLB on port 8815 for Arrow Flight/gRPC. All nodes are attached to both load balancers.
  use_case: Declarative Terraform provisioning lets operators stand up a production-grade multi-node cluster
    reproducibly without manual AWS console steps.
  code:
  - terraform/aws/
  tests:
  - tests/e2e/test_installer.py
  - tests/integration/test_infra.py
  - tests/unit/test_infra_requirements.py
  since: 2026-01
  scenario: 'Given Terraform is applied to the AWS deployment path

    When provisioning completes

    Then a VPC with two AZ subnets, EC2 instances, ALB on 8000, and NLB on 8815 are created with all

    nodes attached to both load balancers

    '
  integration_test: required
  integration_test_reason: Terraform AWS deployment with ALB/NLB requires real AWS infrastructure provisioning.
  stakeholders:
  - ops
  competitive_position:
    status: parity
    rationale: Terraform AWS deployment with VPC, EC2, ALB, and NLB is standard IaC for enterprise SaaS
      platforms.
- id: REQ-592
  status: complete
  superseded_by: REQ-695
  group: 11. Platform, Infrastructure & Delivery
  category: Multi-Tenancy
  priority: SHOULD
  type: structural
  description: Each tenant maps to an org. The `orgs` table stores org namespaces. The `root` org is seeded
    automatically for single-tenant deployments. In multi-tenant mode, one org is created per customer
    via the admin API. `user_org_memberships` tracks which users belong to which org. The current interim
    model adds org_id FK columns to domains and roles for row-level scoping; these columns are superseded
    by REQ-695 schema-per-org, after which org membership is implicit in schema placement and the org_id
    columns on domains/roles can be dropped.
  use_case: Org registry and user membership model — the provisioning anchor consumed by REQ-695 (schema
    creation) and REQ-699 (PG role creation). The orgs table and user_org_memberships remain valid under
    the schema-per-org model; the org_id FK columns on domains/roles do not.
  code:
  - provisa/core/schema.sql
  - provisa/api/admin/orgs_router.py
  tests:
  - tests/unit/test_core_registration.py
  since: 2026-01
  scenario: 'Given a multi-tenant deployment

    When a new customer is onboarded via the admin API

    Then a new org is created and user_org_memberships tracks all users in that org

    '
  integration_test: required
  integration_test_reason: Org-to-tenant mapping and user_org_memberships require real database schema
    and queries.
  stakeholders:
  - compliance
  - ops
  - app-developer
  competitive_position:
    status: parity
    rationale: Org-per-tenant model with root org for single-tenant is standard multi-tenancy architecture.
- id: REQ-593
  status: complete
  group: 11. Platform, Infrastructure & Delivery
  category: Multi-Tenancy
  priority: SHOULD
  type: structural
  description: The KMS client reads `AWS_KMS_REGION` from the environment and defaults to `us-east-1`.
    Standard AWS credential chain (environment variables, instance profile, ECS task role) applies.
  use_case: Region configuration via environment variable lets operators deploy the KMS client to any
    AWS region without code changes.
  code:
  - provisa/api/billing/kms.py
  tests:
  - tests/unit/test_tenancy_requirements.py
  since: 2026-01
  scenario: 'Given AWS_KMS_REGION is set to a non-default region

    When the KMS client initializes

    Then it connects to the specified region using the standard AWS credential chain

    '
  integration_test: deferred
  integration_test_reason: AWS KMS client requires real AWS KMS service not available in CI.
  stakeholders:
  - ops
  - compliance
  competitive_position:
    status: neutral
    rationale: ''
- id: REQ-618
  status: complete
  group: 11. Platform, Infrastructure & Delivery
  category: Infrastructure
  priority: SHOULD
  type: infrastructure
  description: Backend API server runs on port 8001 with uvicorn hot-reload enabled for changes to `provisa/`
    and `config/` directories. Log output is written to `.logs/server.log`. The server starts as part
    of `start-ui.sh`.
  use_case: Hot-reload on source and config changes lets developers iterate without restarting the server
    manually.
  code:
  - start-ui.sh line 243
  tests:
  - tests/e2e/test_health_checks.py
  - tests/integration/test_infra.py
  - tests/unit/test_config_reload.py
  - tests/unit/test_infra_requirements.py
  since: 2026-01
  scenario: 'Given the backend is running via start-ui.sh

    When a file in provisa/ or config/ is modified

    Then uvicorn hot-reloads the server automatically without a manual restart

    '
  integration_test: required
  integration_test_reason: Hot-reload on source/config changes requires real uvicorn server and file watching.
  e2e: true
  e2e_reason: Verifying uvicorn hot-reload triggers on file changes in provisa/ or config/ directories
    requires a live running server process with real file watching and observable restart behavior.
  stakeholders:
  - app-developer
  competitive_position:
    status: neutral
    rationale: ''
- id: REQ-619
  status: complete
  group: 11. Platform, Infrastructure & Delivery
  category: Infrastructure
  priority: SHOULD
  type: behavioral
  description: '`start-ui.sh` manages the full dev lifecycle: Ctrl+C stops the backend, UI dev server,
    and all Docker Compose services and reverts any Trino jvm.config patches. `--keep-docker` flag leaves
    Docker services running on Ctrl+C. Ctrl+R (SIGUSR1) restarts only the backend without affecting Docker
    services or the UI.'
  use_case: Lifecycle controls (stop-all, keep-docker, backend-only restart) give developers precise control
    over which components restart during development.
  code:
  - start-ui.sh lines 248–258
  - start-ui.sh lines 301–306
  - start-ui.sh lines 340–347
  tests:
  - tests/e2e/test_health_checks.py
  - tests/integration/test_infra.py
  - tests/unit/test_infra_requirements.py
  since: 2026-01
  scenario: 'Given start-ui.sh is running the full dev stack

    When Ctrl+C is pressed without --keep-docker

    Then the backend, UI dev server, and all Docker Compose services stop and Trino patches are reverted

    '
  integration_test: required
  integration_test_reason: Lifecycle controls (stop-all, keep-docker, restart) require real process and
    Docker management.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - app-developer
  - ops
  competitive_position:
    status: neutral
    rationale: ''
- id: REQ-630
  status: complete
  group: 11. Platform, Infrastructure & Delivery
  category: Installer & Packaging
  priority: SHOULD
  type: infrastructure
  description: Provisa ships as three distinct artifact packages per platform (Core, Obs, Demo) to stay
    within the GitHub Actions 2 GB artifact limit. CI builds all three in parallel, each uploaded as a
    separate artifact. The package split maps directly to the three docker-compose layers.
  use_case: Three-package split keeps each CI artifact under the 2 GB GitHub Actions limit while preserving
    logical separation between core, observability, and demo service sets.
  code:
  - packaging/macos/build-dmg.sh
  - packaging/macos/build-dmg-obs.sh
  - packaging/macos/build-dmg-demo.sh
  - packaging/windows/build-sfx.ps1
  - packaging/windows/build-installer-obs.ps1
  - packaging/windows/build-installer-demo.ps1
  - packaging/linux/build-appimage.sh
  tests:
  - tests/e2e/test_installer.py
  - tests/unit/test_infra_requirements.py
  since: 2026-01
  scenario: 'Given a CI build for any supported platform

    When the build runs

    Then three separate artifacts (Core, Obs, Demo) are produced and uploaded independently

    '
  integration_test: deferred
  integration_test_reason: 'Three-package artifact split requires real CI build and artifact upload workflow.
    [deferred: requires external infrastructure not in CI stack]'
  stakeholders:
  - ops
  competitive_position:
    status: neutral
    rationale: ''
- id: REQ-631
  status: complete
  group: 11. Platform, Infrastructure & Delivery
  category: Installer & Packaging
  priority: MUST
  type: constraint
  description: 'Package dependency chain: Core must be installed before Obs; Obs must be installed before
    Demo. The Demo installer checks for the presence of `~/.provisa/extensions/observability/` before
    proceeding. Dependency is enforced at install time by the installer scripts, not enforced by CI job
    ordering.'
  use_case: Explicit install-time dependency checks prevent Demo from being installed without Obs, which
    would leave demo services unable to emit observability data.
  code:
  - packaging/macos/build-dmg-demo.sh
  - packaging/windows/build-installer-demo.ps1
  tests:
  - tests/e2e/test_installer.py
  - tests/unit/test_infra_requirements.py
  since: 2026-01
  scenario: 'Given the Demo installer is run without Obs installed

    When the installer checks for ~/.provisa/extensions/observability/

    Then the installer refuses to proceed with an appropriate error message

    '
  integration_test: deferred
  integration_test_reason: 'Package dependency chain enforcement requires real installer execution and
    file checks. [deferred: requires external infrastructure not in CI stack]'
  stakeholders:
  - ops
  competitive_position:
    status: neutral
    rationale: ''
- id: REQ-632
  status: complete
  group: 11. Platform, Infrastructure & Delivery
  category: Installer & Packaging
  priority: SHOULD
  type: infrastructure
  description: Linux AppImage bundles both core and observability (obs) images directly — there is no
    separate Obs download for Linux. The Demo package is not included in Linux distribution. The Linux
    `first-launch.sh` always starts core + obs together; no extension mechanism is used.
  use_case: Bundling obs directly on Linux serves the server/technical user base where observability is
    production-useful, and eliminates the extension download step on a platform where a single monolithic
    AppImage is simpler to distribute.
  code:
  - packaging/linux/build-appimage.sh
  - packaging/linux/first-launch.sh
  tests:
  - tests/e2e/test_installer.py
  - tests/unit/test_infra_requirements.py
  since: 2026-01
  scenario: 'Given the Linux AppImage is installed

    When first-launch.sh runs

    Then core and obs services start together without a separate extension download step

    '
  integration_test: deferred
  integration_test_reason: 'Linux AppImage with bundled obs requires real AppImage build and first-launch
    execution. [deferred: requires external infrastructure not in CI stack]'
  stakeholders:
  - ops
  competitive_position:
    status: neutral
    rationale: ''
- id: REQ-633
  status: complete
  group: 11. Platform, Infrastructure & Delivery
  category: Installer & Packaging
  priority: SHOULD
  type: infrastructure
  description: 'macOS and Windows use an extension model: the container/compute tier is the only tier
    that requires an external container runtime. On macOS, it uses the user''s Docker (Docker Desktop
    or colima); on Windows, it provisions WSL2 + containerd — never VirtualBox. App images (provisa, provisa-ui,
    zaychik) are built at DMG-build time, shipped in core-images.tar, docker load-ed at first launch,
    and resolved via docker-compose.airgap.yml. Obs and Demo are extension packages that load images into
    the existing runtime and write a compose file into `~/.provisa/extensions/<name>/`. The launcher enumerates
    `extensions/*/docker-compose.*.yml` at startup and appends each found file to the compose file list,
    composing the service set dynamically. `PROVISA_REDIRECT_ENABLED`, MinIO, and OTel env vars are set
    only when the obs extension is present. The native base tier (REQ-979) has no runtime VM at all.'
  use_case: Extension model lets users install observability and demo services incrementally without reinstalling
    Core, and lets the launcher automatically discover installed extensions without hard-coded feature
    flags.
  code:
  - packaging/macos/build-dmg-obs.sh
  - packaging/macos/build-dmg-demo.sh
  - packaging/macos/ProvisaLauncher/Sources/ProvisaLauncher/Services/ScriptRunner.swift
  - packaging/windows/build-container.ps1
  - packaging/windows/install-container.ps1
  - packaging/windows/provisa-container.ps1
  - packaging/windows/wsl/provision-containerd.sh
  - packaging/windows/wsl/start-containerd.sh
  - packaging/windows/build-installer-obs.ps1
  notes: Windows container tier (build-container.ps1) is a separate on-demand download that provisions
    a WSL2 distro named `provisa` with containerd + nerdctl-full, loads the bundled core image tarballs,
    copies the compose tree to /opt/provisa/compose inside the distro, and runs `nerdctl compose` there
    (provisa-container.ps1). macOS container tier bundles core-images.tar built at DMG-build time, docker
    load-s images at first launch, and uses docker-compose.airgap.yml. No VirtualBox or embedded VM. install-container.ps1
    stops the native tier before starting the stack; the tier is reversible by switching back to provisa-native.ps1.
    uninstall.ps1 unregisters the WSL distro.
  tests:
  - tests/e2e/test_installer.py
  - tests/unit/test_infra_requirements.py
  since: 2026-01
  scenario: 'Given Core is installed and the Obs extension is added

    When the Core launcher starts

    Then it discovers extensions/*/docker-compose.*.yml files and composes the service set dynamically

    '
  integration_test: deferred
  integration_test_reason: 'Extension model with dynamic compose discovery requires real installer and
    launcher execution. [deferred: requires external infrastructure not in CI stack]'
  stakeholders:
  - ops
  competitive_position:
    status: neutral
    rationale: ''
- id: REQ-634
  status: complete
  group: 11. Platform, Infrastructure & Delivery
  category: Installer & Packaging
  priority: MUST
  type: constraint
  description: The dev environment runs the Python backend (uvicorn, port 8000) and UI (vite dev server,
    port 3000) on the host, never in containers. Port 8000 applies to the start-ui-install.sh flow; the
    start-ui.sh flow listens on 8001 (REQ-618) — the two dev scripts use different ports by design. `docker-compose.app.yml`
    and `docker-compose.airgap.yml` are packaged-product only and must never be included in dev compose
    stacks. `start-ui-install.sh --demo` is the only dev mode flag; `--demo` always implies obs. Ports
    8000 and 3000 must never appear in any dev compose file.
  use_case: Host-based uvicorn + vite in dev avoids port conflicts with containerised services and enables
    hot-reload without rebuilding images.
  code:
  - start-ui-install.sh
  - docker-compose.dev-install.yml
  - docker-compose.observability.yml
  - docker-compose.demo.yml
  tests:
  - tests/e2e/test_health_checks.py
  - tests/unit/test_infra_requirements.py
  since: 2026-01
  scenario: 'Given the dev environment is started via start-ui-install.sh

    When any dev compose file is inspected

    Then ports 8000 and 3000 do not appear in any compose file and the backend runs on the host

    '
  integration_test: deferred
  integration_test_reason: 'Dev environment constraint on uvicorn/vite host placement requires real compose
    validation. [deferred: requires external infrastructure not in CI stack]'
  stakeholders:
  - app-developer
  competitive_position:
    status: neutral
    rationale: ''
- id: REQ-182
  status: complete
  group: 12. Migration & Compatibility (Hasura)
  category: Hasura Migration Converters
  priority: SHOULD
  type: behavioral
  description: Hasura v2 metadata converter -- CLI tool that reads a Hasura v2 metadata export directory
    and emits valid Provisa YAML config. Converts tracked tables, relationships, permissions, roles, and
    auth.
  use_case: Hasura v2 parity — automates migration from Hasura v2 so teams avoid manual YAML rewriting.
  code:
  - provisa/hasura_v2/
  - provisa/ddn/
  tests:
  - tests/unit/test_ddn.py
  - tests/unit/test_ddn_converter.py
  - tests/unit/test_hasura_converter.py
  - tests/unit/test_hasura_v2.py
  - tests/steps/steps_hasura_migration_converters.py
  since: 2026-01
  scenario: 'Given a Hasura v2 metadata export directory

    When the CLI converter is run against it

    Then valid Provisa YAML config is emitted covering tables, relationships, permissions, roles, and
    auth

    '
  integration_test: not_required
  integration_test_reason: CLI metadata conversion is pure logic over YAML/JSON structures, mockable with
    test fixtures.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - app-developer
  - data-engineer
  competitive_position:
    status: ahead
    rationale: Automated Hasura v2 metadata converter is a unique migration accelerator — no competitor
      provides a tool to convert from Hasura to their platform.
- id: REQ-183
  status: complete
  group: 12. Migration & Compatibility (Hasura)
  category: Hasura Migration Converters
  priority: SHOULD
  type: behavioral
  description: Hasura DDN (v3) HML converter -- CLI tool that reads a DDN supergraph project and emits
    valid Provisa YAML config. Converts ObjectTypes, Models, Relationships, TypePermissions, ModelPermissions,
    and DataConnectorLinks.
  use_case: Hasura v2 parity — automates migration from Hasura DDN so teams avoid rewriting supergraph
    configs.
  code:
  - provisa/hasura_v2/
  - provisa/ddn/
  tests:
  - tests/unit/test_ddn.py
  - tests/unit/test_ddn_converter.py
  - tests/unit/test_hasura_converter.py
  - tests/unit/test_hasura_v2.py
  - tests/steps/steps_hasura_migration_converters.py
  since: 2026-01
  scenario: 'Given a Hasura DDN supergraph project

    When the HML converter CLI tool is run

    Then valid Provisa YAML config is emitted covering ObjectTypes, Models, Relationships, TypePermissions,
    ModelPermissions, and DataConnectorLinks

    '
  integration_test: not_required
  integration_test_reason: CLI metadata conversion is pure logic over YAML/JSON structures, mockable with
    test fixtures.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - app-developer
  - data-engineer
  competitive_position:
    status: ahead
    rationale: Hasura DDN (v3) HML converter covering ObjectTypes, Models, Relationships, and Permissions
      is unique — no other platform has invested in DDN migration tooling.
- id: REQ-184
  status: complete
  group: 12. Migration & Compatibility (Hasura)
  category: Hasura Migration Converters
  priority: SHOULD
  type: behavioral
  description: 'Shared boolean expression-to-SQL converter for Hasura filter expressions. Supports `_eq`,
    `_neq`, `_gt`, `_gte`, `_lt`, `_lte`, `_in`, `_nin`, `_like`, `_ilike`, `_regex`, `_is_null`, `_and`,
    `_or`, `_not`. Session variable mapping: `X-Hasura-<Name>` -> `current_setting(''provisa.<name>'')`.'
  use_case: Hasura v2 parity — shared filter converter ensures RLS rules from Hasura boolean expressions
    work correctly.
  code:
  - provisa/hasura_v2/
  - provisa/ddn/
  tests:
  - tests/unit/test_ddn.py
  - tests/unit/test_ddn_converter.py
  - tests/unit/test_hasura_converter.py
  - tests/unit/test_hasura_v2.py
  - tests/steps/steps_hasura_migration_converters.py
  since: 2026-01
  scenario: 'Given a Hasura boolean filter expression using operators like _eq, _in, _and, _or, _not

    When the shared converter processes it

    Then valid SQL is produced with session variable references mapped to current_setting(''provisa.<name>'')

    '
  integration_test: not_required
  integration_test_reason: Boolean expression compilation to SQL is pure logic, verifiable with unit tests
    and SQL AST comparison.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - data-engineer
  - app-developer
  competitive_position:
    status: ahead
    rationale: Shared boolean expression-to-SQL converter for Hasura filter syntax covers the full operator
      set needed for safe migration — a competitive differentiator for Hasura displacement.
- id: REQ-185
  status: complete
  group: 12. Migration & Compatibility (Hasura)
  category: Hasura Migration Converters
  priority: SHOULD
  type: behavioral
  description: 'v2 converter maps `select_permissions[].columns` per role -> Provisa column `visible_to`.
    `columns: "*"` means all columns visible to that role.'
  use_case: Hasura v2 parity — column visibility from v2 select permissions is preserved automatically
    on import.
  code:
  - provisa/hasura_v2/
  - provisa/ddn/
  tests:
  - tests/unit/test_ddn.py
  - tests/unit/test_ddn_converter.py
  - tests/unit/test_hasura_converter.py
  - tests/unit/test_hasura_v2.py
  - tests/steps/steps_hasura_migration_converters.py
  since: 2026-01
  scenario: 'Given a Hasura v2 metadata export with select_permissions[].columns per role

    When the v2 converter runs

    Then each column''s visible_to is populated from the role''s column list, with "*" meaning all columns

    '
  integration_test: not_required
  integration_test_reason: Permission mapping from v2 config to YAML is pure data transformation, mockable.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - data-engineer
  - compliance
  competitive_position:
    status: ahead
    rationale: Automatic column-level visibility mapping from Hasura select_permissions is more complete
      than manual re-configuration required by Dremio or Denodo for Hasura migration.
- id: REQ-186
  status: complete
  group: 12. Migration & Compatibility (Hasura)
  category: Hasura Migration Converters
  priority: SHOULD
  type: behavioral
  description: v2 converter maps `insert/update_permissions[].columns` per role -> Provisa column `writable_by`.
  use_case: Hasura v2 parity — column write permissions from v2 are preserved automatically on import.
  code:
  - provisa/hasura_v2/
  - provisa/ddn/
  tests:
  - tests/unit/test_ddn.py
  - tests/unit/test_ddn_converter.py
  - tests/unit/test_hasura_converter.py
  - tests/unit/test_hasura_v2.py
  - tests/steps/steps_hasura_migration_converters.py
  since: 2026-01
  scenario: 'Given a Hasura v2 metadata export with insert/update_permissions[].columns per role

    When the v2 converter runs

    Then each column''s writable_by is populated from the role''s insert/update column list

    '
  integration_test: not_required
  integration_test_reason: Permission mapping from v2 config to YAML is pure data transformation, mockable.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - data-engineer
  - compliance
  competitive_position:
    status: ahead
    rationale: Automatic writable_by mapping from Hasura insert/update permissions preserves write governance
      during migration.
- id: REQ-187
  status: complete
  group: 12. Migration & Compatibility (Hasura)
  category: Hasura Migration Converters
  priority: SHOULD
  type: behavioral
  description: 'v2 converter maps `select_permissions[].filter` -> Provisa `rls_rules[]` via boolean expression-to-SQL
    conversion. `filter: {}` (empty) means no RLS filter.'
  use_case: Hasura v2 parity — RLS filters from v2 select permissions are preserved automatically on import.
  code:
  - provisa/hasura_v2/
  - provisa/ddn/
  tests:
  - tests/unit/test_ddn.py
  - tests/unit/test_ddn_converter.py
  - tests/unit/test_hasura_converter.py
  - tests/unit/test_hasura_v2.py
  - tests/steps/steps_hasura_migration_converters.py
  since: 2026-01
  scenario: 'Given a Hasura v2 select_permissions[].filter boolean expression

    When the v2 converter runs

    Then rls_rules[] are generated via boolean expression-to-SQL conversion, with empty filter producing
    no RLS rule

    '
  integration_test: not_required
  integration_test_reason: RLS filter conversion from boolean expressions to SQL is pure logic, unit testable.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - compliance
  - data-engineer
  competitive_position:
    status: ahead
    rationale: RLS rule generation from Hasura filter expressions via boolean-expression-to-SQL is unique
      migration automation.
- id: REQ-188
  status: complete
  group: 12. Migration & Compatibility (Hasura)
  category: Hasura Migration Converters
  priority: SHOULD
  type: behavioral
  description: v2 converter maps `object_relationships` -> cardinality=many-to-one and `array_relationships`
    -> cardinality=one-to-many. Physical column names used directly (no GraphQL resolution needed).
  use_case: Hasura v2 parity — relationship cardinality from v2 metadata is preserved automatically on
    import.
  code:
  - provisa/hasura_v2/
  - provisa/ddn/
  tests:
  - tests/unit/test_ddn.py
  - tests/unit/test_ddn_converter.py
  - tests/unit/test_hasura_converter.py
  - tests/unit/test_hasura_v2.py
  - tests/steps/steps_hasura_migration_converters.py
  since: 2026-01
  scenario: 'Given a Hasura v2 metadata export with object_relationships and array_relationships

    When the v2 converter runs

    Then object_relationships become cardinality=many-to-one and array_relationships become cardinality=one-to-many

    '
  integration_test: not_required
  integration_test_reason: Relationship cardinality mapping is pure configuration translation, mockable.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - data-engineer
  - app-developer
  competitive_position:
    status: ahead
    rationale: Relationship cardinality mapping (object/array) from Hasura metadata to Provisa is automated
      migration not available in any competing platform.
- id: REQ-189
  status: complete
  group: 12. Migration & Compatibility (Hasura)
  category: Hasura Migration Converters
  priority: SHOULD
  type: behavioral
  description: DDN converter resolves GraphQL field names to physical column names through `ObjectType.dataConnectorTypeMapping[].fieldMapping`
    for all field references in relationships, permissions, and column definitions.
  use_case: Hasura v2 parity — DDN field mappings are resolved to physical columns so no manual remapping
    is needed.
  code:
  - provisa/hasura_v2/
  - provisa/ddn/
  tests:
  - tests/unit/test_ddn.py
  - tests/unit/test_ddn_converter.py
  - tests/unit/test_hasura_converter.py
  - tests/unit/test_hasura_v2.py
  - tests/steps/steps_hasura_migration_converters.py
  since: 2026-01
  scenario: 'Given a DDN supergraph with ObjectType.dataConnectorTypeMapping[].fieldMapping entries

    When the DDN converter runs

    Then all GraphQL field names in relationships, permissions, and column definitions are resolved to
    physical column names

    '
  integration_test: not_required
  integration_test_reason: Field name resolution via mapping tables is pure logic, mockable with test
    structures.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - data-engineer
  - app-developer
  competitive_position:
    status: ahead
    rationale: GraphQL field-to-physical-column resolution through DDN ObjectType fieldMapping for all
      relationship/permission references is precise migration automation.
- id: REQ-190
  status: complete
  group: 12. Migration & Compatibility (Hasura)
  category: Hasura Migration Converters
  priority: SHOULD
  type: behavioral
  description: 'v2 auth conversion via optional `--auth-env-file` flag. JWT with `jwk_url` -> Provisa
    `provider: oauth`. JWT `claims_map` -> Provisa `role_mapping[]`. Admin secret -> Provisa `superuser`.
    Webhook auth emits warning (no Provisa equivalent).'
  use_case: Hasura v2 parity — auth config is migrated automatically so JWT and role mappings carry over.
  code:
  - provisa/hasura_v2/
  - provisa/ddn/
  tests:
  - tests/unit/test_ddn.py
  - tests/unit/test_ddn_converter.py
  - tests/unit/test_hasura_converter.py
  - tests/unit/test_hasura_v2.py
  - tests/steps/steps_hasura_migration_converters.py
  since: 2026-01
  scenario: 'Given a Hasura v2 auth config with JWT jwk_url, claims_map, or admin secret

    When the v2 converter runs with --auth-env-file

    Then JWT becomes provider: oauth with role_mapping[], admin secret becomes superuser, and webhook
    auth emits a warning

    '
  integration_test: not_required
  integration_test_reason: Auth config mapping is pure translation logic, mockable with fixture auth config.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - compliance
  - ops
  competitive_position:
    status: ahead
    rationale: JWT and webhook auth conversion from Hasura metadata to Provisa auth config is automated
      identity migration not offered by any competitor.
- id: REQ-191
  status: complete
  group: 12. Migration & Compatibility (Hasura)
  category: Hasura Migration Converters
  priority: SHOULD
  type: behavioral
  description: DDN AggregateExpression metadata preserved in sidecar `provisa-aggregates.yaml` and converted
    to Provisa aggregate config.
  use_case: Hasura v2 parity — aggregate expressions from DDN are preserved so analytics queries keep
    working.
  code:
  - provisa/hasura_v2/
  - provisa/ddn/
  tests:
  - tests/unit/test_ddn.py
  - tests/unit/test_ddn_converter.py
  - tests/unit/test_hasura_converter.py
  - tests/unit/test_hasura_v2.py
  - tests/steps/steps_hasura_migration_converters.py
  since: 2026-01
  scenario: 'Given a DDN project with AggregateExpression metadata

    When the DDN converter runs

    Then aggregate config is emitted in provisa-aggregates.yaml as valid Provisa aggregate config

    '
  integration_test: not_required
  integration_test_reason: Aggregate metadata preservation is pure config mapping, mockable with test
    fixtures.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - data-engineer
  - app-developer
  competitive_position:
    status: ahead
    rationale: DDN AggregateExpression preservation in sidecar provisa-aggregates.yaml maintains analytics
      semantics across migration.
- id: REQ-192
  status: complete
  group: 12. Migration & Compatibility (Hasura)
  category: Hasura Migration Converters
  priority: SHOULD
  type: behavioral
  description: Converters emit warnings for unmappable features (event_triggers, remote_schemas, cron_triggers,
    BooleanExpressionType) without failing conversion. v2 Actions and DDN Commands convert to Provisa
    `functions` config where backed by stored procedures; webhook-backed actions emit warning with handler
    URL. Part of Phases AG/AH (Hasura Converters).
  use_case: Hasura v2 parity — unmappable features emit warnings instead of failing so partial migrations
    complete.
  code:
  - provisa/hasura_v2/
  - provisa/ddn/
  tests:
  - tests/unit/test_ddn.py
  - tests/unit/test_ddn_converter.py
  - tests/unit/test_event_triggers.py
  - tests/unit/test_hasura_converter.py
  - tests/unit/test_hasura_v2.py
  - tests/steps/steps_hasura_migration_converters.py
  since: 2026-01
  scenario: 'Given a Hasura project with event_triggers, remote_schemas, cron_triggers, or webhook-backed
    actions

    When the converter runs

    Then warnings are emitted for unmappable features and conversion completes rather than aborting

    '
  integration_test: not_required
  integration_test_reason: Warning emission for unmappable features is pure control flow, unit testable.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - data-engineer
  - app-developer
  competitive_position:
    status: ahead
    rationale: Warning-not-failure for unmappable features (event_triggers, remote_schemas) with Actions-to-functions
      conversion keeps migration velocity high without data loss.
- id: REQ-193
  status: complete
  group: 12. Migration & Compatibility (Hasura)
  category: Hasura Migration Converters
  priority: MUST
  type: constraint
  description: Both converters produce output that passes `ProvisaConfig.model_validate()` -- Pydantic-valid
    config or nothing. Part of Phases AG/AH (Hasura Converters).
  use_case: Hasura v2 parity — converter output is always Pydantic-valid so imported configs never cause
    silent runtime errors.
  code:
  - provisa/hasura_v2/
  - provisa/ddn/
  tests:
  - tests/unit/test_ddn.py
  - tests/unit/test_ddn_converter.py
  - tests/unit/test_hasura_converter.py
  - tests/unit/test_hasura_v2.py
  since: 2026-01
  scenario: 'Given a completed Hasura v2 or DDN conversion

    When ProvisaConfig.model_validate() is called on the output

    Then it passes validation without errors

    '
  integration_test: not_required
  integration_test_reason: Pydantic validation is pure logic with in-memory schema and test fixtures.
  stakeholders:
  - data-engineer
  - app-developer
  competitive_position:
    status: ahead
    rationale: Pydantic-validated output guarantee from both converters ensures migration output is immediately
      runnable — no manual fixup required for valid Hasura metadata.
- id: REQ-212
  status: complete
  group: 12. Migration & Compatibility (Hasura)
  category: 'Hasura v2 Parity: Low-Complexity Features'
  priority: SHOULD
  type: behavioral
  description: Upsert mutations -- `INSERT ... ON CONFLICT ... DO UPDATE`. New `upsert_<table>` mutation
    field. Conflict columns inferred from primary key metadata. SQLGlot transpiles to dialect-specific
    syntax (MySQL `ON DUPLICATE KEY UPDATE`, etc.).
  use_case: Hasura v2 parity — upsert mutations remove the need for client-side read-then-write logic.
  code:
  - provisa/compiler/
  - provisa/scheduler/
  tests:
  - tests/unit/test_upsert_distinct_on.py
  - tests/unit/test_column_presets.py
  - tests/integration/test_mutation_parity_integration.py
  since: 2026-01
  scenario: 'Given a GraphQL upsert_<table> mutation request

    When the compiler processes it

    Then INSERT ... ON CONFLICT ... DO UPDATE SQL is generated with conflict columns from primary key
    metadata

    '
  integration_test: required
  integration_test_reason: Upsert mutations require real DB with conflict resolution semantics, dialect-specific
    SQL generation, and transaction behavior.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - app-developer
  - data-engineer
  competitive_position:
    status: parity
    rationale: Upsert mutations (INSERT ON CONFLICT DO UPDATE) are supported by Hasura and most GraphQL
      data layers.
- id: REQ-213
  status: complete
  group: 12. Migration & Compatibility (Hasura)
  category: 'Hasura v2 Parity: Low-Complexity Features'
  priority: SHOULD
  type: behavioral
  description: '`DISTINCT ON` query argument -- deduplicate results by specified columns. Added as `distinct_on`
    arg on root query fields. SQLGlot handles dialect differences (window function fallback for non-PG).'
  use_case: Hasura v2 parity — DISTINCT ON deduplication lets clients request unique result sets in a
    single query.
  code:
  - provisa/compiler/
  - provisa/scheduler/
  tests:
  - tests/unit/test_upsert_distinct_on.py
  - tests/unit/test_column_presets.py
  - tests/integration/test_mutation_parity_integration.py
  since: 2026-01
  scenario: 'Given a GraphQL query with a distinct_on argument specifying columns

    When the compiler processes it

    Then deduplicated results are returned using DISTINCT ON or a window function fallback for non-PostgreSQL
    dialects

    '
  integration_test: required
  integration_test_reason: DISTINCT ON deduplication requires real DB query execution and verification
    of result cardinality.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - app-developer
  - data-engineer
  competitive_position:
    status: parity
    rationale: DISTINCT ON as a query argument is available in Hasura and standard PostgreSQL GraphQL
      layers.
- id: REQ-214
  status: complete
  group: 12. Migration & Compatibility (Hasura)
  category: 'Hasura v2 Parity: Low-Complexity Features'
  priority: SHOULD
  type: behavioral
  description: 'Column presets -- auto-set column values on insert/update from session variables or built-in
    functions. Config per table: `column_presets: [{column: created_by, source: header, name: x_user_id},
    {column: updated_at, source: now}]`. Preset columns removed from user input, injected before SQL generation.'
  use_case: Hasura v2 parity — column presets auto-stamp audit fields like created_by without trusting
    client input.
  code:
  - provisa/compiler/
  - provisa/scheduler/
  tests:
  - tests/unit/test_upsert_distinct_on.py
  - tests/unit/test_column_presets.py
  - tests/integration/test_mutation_parity_integration.py
  since: 2026-01
  scenario: 'Given a table config with column_presets for created_by and updated_at

    When an insert or update mutation is executed

    Then preset columns are removed from user input and injected with session variable or built-in function
    values before SQL generation

    '
  integration_test: required
  integration_test_reason: Column presets require real DB to verify session variable substitution and
    value injection into actual rows.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - data-engineer
  - app-developer
  competitive_position:
    status: parity
    rationale: Column presets from session variables are available in Hasura (column presets feature).
- id: REQ-215
  status: complete
  group: 12. Migration & Compatibility (Hasura)
  category: 'Hasura v2 Parity: Low-Complexity Features'
  priority: SHOULD
  type: behavioral
  description: 'Inherited roles -- role hierarchy where child role inherits capabilities and domain_access
    from parent. Config: `parent_role_id` on role definition. Flattened at startup (merge capabilities/domain_access
    up the chain). Lookups remain O(1).'
  use_case: Hasura v2 parity — inherited roles eliminate redundant permission duplication across similar
    role definitions.
  code:
  - provisa/compiler/
  - provisa/scheduler/
  tests:
  - tests/integration/test_mutation_parity_integration.py
  - tests/unit/test_column_presets.py
  - tests/unit/test_inherited_roles_extended.py
  - tests/unit/test_upsert_distinct_on.py
  since: 2026-01
  scenario: 'Given roles configured with parent_role_id forming a hierarchy

    When the system starts up

    Then capabilities and domain_access are flattened up the chain so lookups remain O(1)

    '
  integration_test: not_required
  integration_test_reason: Role hierarchy flattening is pure in-memory graph traversal, mockable with
    test role definitions.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - compliance
  - ops
  competitive_position:
    status: parity
    rationale: Inherited roles with capability/domain merging are available in Hasura v2 and enterprise
      Denodo.
- id: REQ-216
  status: complete
  group: 12. Migration & Compatibility (Hasura)
  category: 'Hasura v2 Parity: Low-Complexity Features'
  priority: SHOULD
  type: behavioral
  description: Scheduled triggers -- time-based execution of registered webhooks or internal functions.
    APScheduler in-process, cron expression syntax. Config per trigger in `provisa.yaml`. Reuses existing
    async background task pattern.
  use_case: Hasura v2 parity — scheduled triggers run webhooks or functions on a cron schedule without
    external cron infra.
  code:
  - provisa/compiler/
  - provisa/scheduler/
  tests:
  - tests/integration/test_mutation_parity_integration.py
  - tests/unit/test_column_presets.py
  - tests/unit/test_scheduled_triggers.py
  - tests/unit/test_scheduler.py
  - tests/unit/test_upsert_distinct_on.py
  since: 2026-01
  scenario: 'Given scheduled trigger config in provisa.yaml with a cron expression

    When the cron fires

    Then the configured webhook or internal function is executed via APScheduler

    '
  integration_test: deferred
  integration_test_reason: Scheduled triggers require APScheduler integration and webhook execution; CI
    lacks external webhook infrastructure for full E2E.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - ops
  - data-engineer
  competitive_position:
    status: parity
    rationale: Scheduled triggers via APScheduler are available in Hasura (cron triggers) and Dremio (scheduled
      reflections).
- id: REQ-217
  status: complete
  group: 12. Migration & Compatibility (Hasura)
  category: 'Hasura v2 Parity: Low-Complexity Features'
  priority: SHOULD
  type: behavioral
  description: Batch mutations already supported by GraphQL spec -- multiple mutations in one request
    execute sequentially. Document existing behavior.
  use_case: Hasura v2 parity — documenting existing batch mutation support clarifies the capability for
    developers.
  code:
  - provisa/compiler/
  - provisa/scheduler/
  tests:
  - tests/integration/test_mutation_parity_integration.py
  - tests/unit/test_batch_mutations.py
  - tests/unit/test_column_presets.py
  - tests/unit/test_upsert_distinct_on.py
  since: 2026-01
  scenario: 'Given a GraphQL request containing multiple mutations

    When the request is executed

    Then mutations execute sequentially per the GraphQL spec

    '
  integration_test: not_required
  integration_test_reason: Batch mutations are GraphQL spec compliance documentation; sequential execution
    is unit testable.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - app-developer
  competitive_position:
    status: parity
    rationale: Sequential batch mutations per GraphQL spec are standard behavior in Hasura and Apollo.
- id: REQ-218
  status: complete
  group: 12. Migration & Compatibility (Hasura)
  category: 'Hasura v2 Parity: Medium-Complexity Features'
  priority: SHOULD
  type: behavioral
  description: Cursor-based pagination -- `first`, `after`, `last`, `before` args on root query fields.
    Returns `edges[{cursor, node}]` + `pageInfo{hasNextPage, hasPreviousPage, startCursor, endCursor}`.
    Cursor encoded as base64 of sort key values. Coexists with existing offset/limit.
  use_case: Hasura v2 parity — cursor-based pagination lets clients page large result sets stably without
    offset drift.
  code:
  - provisa/compiler/
  - provisa/scheduler/
  tests:
  - tests/integration/test_mutation_parity_integration.py
  - tests/unit/test_column_presets.py
  - tests/unit/test_cursor_pagination.py
  - tests/unit/test_upsert_distinct_on.py
  since: 2026-01
  scenario: 'Given a root query field with first/after/last/before cursor pagination args

    When the query executes

    Then edges[{cursor, node}] and pageInfo are returned with base64-encoded cursors

    '
  integration_test: required
  integration_test_reason: Cursor-based pagination requires real DB query execution, sort key encoding/decoding,
    and page boundary verification.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - app-developer
  - data-engineer
  competitive_position:
    status: parity
    rationale: Cursor-based pagination (edges/pageInfo/Relay spec) is standard in Hasura and Apollo Federation.
- id: REQ-219
  status: complete
  superseded_by: REQ-258
  group: 12. Migration & Compatibility (Hasura)
  category: 'Hasura v2 Parity: Medium-Complexity Features'
  priority: SHOULD
  type: behavioral
  description: Subscriptions via Server-Sent Events (SSE) -- `GET /data/subscribe/<table>` endpoint using
    FastAPI StreamingResponse. PostgreSQL LISTEN/NOTIFY via asyncpg `.add_listener()` for change detection.
    No WebSocket complexity. Streams INSERT/UPDATE/DELETE events.
  use_case: Hasura v2 parity — SSE subscriptions push live DB changes to clients without WebSocket complexity.
  code:
  - provisa/compiler/
  - provisa/scheduler/
  tests:
  - tests/unit/test_upsert_distinct_on.py
  - tests/unit/test_column_presets.py
  - tests/integration/test_mutation_parity_integration.py
  - provisa-ui/e2e/infrastructure.spec.ts
  since: 2026-01
  scenario: 'Given a client connected to GET /data/subscribe/<table>

    When INSERT, UPDATE, or DELETE events occur on that table

    Then the events are streamed to the client via SSE using PostgreSQL LISTEN/NOTIFY

    '
  integration_test: required
  integration_test_reason: SSE subscriptions require live DB LISTEN/NOTIFY, asyncpg listener lifecycle,
    and streaming HTTP transport.
  e2e: true
  e2e_reason: SSE subscriptions via GET /data/subscribe/<table> require a real running server with live
    PostgreSQL LISTEN/NOTIFY and streaming HTTP transport to verify event delivery across the network
    boundary.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - app-developer
  - ops
  competitive_position:
    status: parity
    rationale: SSE subscriptions with PostgreSQL LISTEN/NOTIFY are available in Hasura's live queries.
  related_reqs:
  - REQ-828
- id: REQ-220
  status: complete
  group: 12. Migration & Compatibility (Hasura)
  category: 'Hasura v2 Parity: Medium-Complexity Features'
  priority: SHOULD
  type: behavioral
  description: Database event triggers -- table changes (insert/update/delete) fire webhooks. PostgreSQL
    trigger + `pg_notify()` -> asyncpg listener -> HTTP POST to configured URL. Config per table with
    operation filter and retry policy.
  use_case: Hasura v2 parity — DB event triggers automate webhook calls on row changes without application-layer
    polling.
  code:
  - provisa/compiler/
  - provisa/scheduler/
  tests:
  - tests/integration/test_event_triggers_integration.py
  - tests/integration/test_mutation_parity_integration.py
  - tests/unit/test_column_presets.py
  - tests/unit/test_event_triggers.py
  - tests/unit/test_upsert_distinct_on.py
  since: 2026-01
  scenario: 'Given a table with event trigger config specifying a webhook URL and operation filter

    When an insert, update, or delete occurs on that table

    Then an HTTP POST is fired to the configured URL via the asyncpg listener with retry policy applied

    '
  integration_test: required
  integration_test_reason: DB event triggers require PostgreSQL triggers, pg_notify(), asyncpg listeners,
    and HTTP webhook dispatch.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - ops
  - data-engineer
  competitive_position:
    status: parity
    rationale: Database event triggers firing webhooks on table changes are a core Hasura feature.
  related_reqs:
  - REQ-828
- id: REQ-221
  status: complete
  group: 12. Migration & Compatibility (Hasura)
  category: 'Hasura v2 Parity: Medium-Complexity Features'
  priority: SHOULD
  type: behavioral
  description: Enum table auto-detection -- introspect `pg_enum` at schema build time, generate GraphQL
    enum types for columns using PostgreSQL user-defined enums. Map enum columns to GraphQL enum type
    instead of String.
  use_case: Hasura v2 parity — enum type auto-detection exposes PostgreSQL enums as GraphQL enums with
    no manual config.
  code:
  - provisa/compiler/
  - provisa/scheduler/
  tests:
  - tests/unit/test_column_presets.py
  - tests/unit/test_enum_detection.py
  - tests/unit/test_upsert_distinct_on.py
  - tests/integration/test_registration_governance_integration.py
  since: 2026-01
  scenario: 'Given a PostgreSQL schema with user-defined enum types used as column types

    When the schema is built

    Then GraphQL enum types are generated and enum columns are mapped to GraphQL enum types instead of
    String

    '
  integration_test: required
  integration_test_reason: Enum type detection requires real DB introspection of pg_enum and verification
    that GraphQL schema reflects enum values.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - data-engineer
  - app-developer
  competitive_position:
    status: parity
    rationale: PostgreSQL enum auto-detection and GraphQL enum type generation is available in Hasura.
- id: REQ-222
  status: complete
  superseded_by: REQ-256
  group: 12. Migration & Compatibility (Hasura)
  category: 'Hasura v2 Parity: Medium-Complexity Features'
  priority: SHOULD
  type: behavioral
  description: REST endpoint auto-generation -- for each root query field, generate `GET /data/rest/<table>`
    FastAPI endpoint. Map query args to URL query params (`?limit=10&where.id.eq=1`). Reuses GraphQL compilation
    pipeline internally.
  use_case: Hasura v2 parity — auto-generated REST endpoints let REST-only clients consume governed data
    without GraphQL.
  code:
  - provisa/compiler/
  - provisa/scheduler/
  tests:
  - tests/e2e/test_query_pipeline.py
  - tests/integration/test_rest_endpoints.py
  - tests/unit/test_column_presets.py
  - tests/unit/test_upsert_distinct_on.py
  - provisa-ui/e2e/infrastructure.spec.ts
  since: 2026-01
  scenario: 'Given a REST client calling GET /data/rest/<table>?limit=10&where.id.eq=1

    When the endpoint is hit

    Then the request is processed through the GraphQL compilation pipeline and results are returned

    '
  integration_test: required
  integration_test_reason: REST endpoint generation requires HTTP transport, query parameter parsing,
    and end-to-end query compilation.
  e2e: true
  e2e_reason: REST endpoint auto-generation is only verifiable by making an actual HTTP GET request to
    a running server and confirming the response data matches the query parameters.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - app-developer
  competitive_position:
    status: parity
    rationale: REST endpoint auto-generation from GraphQL fields is available in Hasura REST endpoints
      feature.
- id: REQ-621
  status: complete
  group: 12. Migration & Compatibility (Hasura)
  category: Hasura Migration Converters
  priority: MUST
  type: behavioral
  description: 'Both Hasura v2 and DDN converters emit placeholder connection credentials in the output
    config (host: localhost, password: ${env:DB_PASSWORD}). Operators must update connection details before
    starting Provisa.'
  use_case: Placeholder credentials ensure the generated config is syntactically valid while signaling
    that real credentials must be supplied before production use.
  code:
  - provisa/hasura_v2/mapper.py
  - provisa/ddn/mapper.py
  tests:
  - tests/unit/test_ddn.py
  - tests/unit/test_ddn_converter.py
  - tests/unit/test_hasura_converter.py
  - tests/unit/test_hasura_v2.py
  - tests/steps/steps_hasura_migration_converters.py
  since: 2026-01
  scenario: 'Given a completed Hasura v2 or DDN conversion

    When the output config is inspected

    Then placeholder credentials (host: localhost, password: ${env:DB_PASSWORD}) are present and Provisa
    refuses to start without real values

    '
  integration_test: not_required
  integration_test_reason: Placeholder credential insertion is pure config generation, verifiable by parsing
    output YAML.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - ops
  competitive_position:
    status: neutral
    rationale: ''
- id: REQ-623
  status: complete
  group: 12. Migration & Compatibility (Hasura)
  category: Hasura Migration Converters
  priority: SHOULD
  type: behavioral
  description: 'v2 converter maps Hasura source `kind` to Provisa `SourceType`: `pg`/`postgres` -> `postgresql`,
    `mssql` -> `sqlserver`, `bigquery` -> `bigquery`, `citus` -> `postgresql`, `mysql` -> `mysql`. Connection
    URL (`database_url`) is parsed into host/port/database/username/ password components. Pool settings
    (`pool_settings.min_connections`, `pool_settings.max_connections`) are preserved as `pool_min`/`pool_max`.'
  use_case: Hasura v2 parity — source kind and connection URL normalization ensures source configs carry
    over without manual rewriting.
  code:
  - provisa/hasura_v2/mapper.py
  tests:
  - tests/unit/test_ddn_converter.py
  - tests/unit/test_hasura_converter.py
  - tests/unit/test_hasura_v2.py
  - tests/steps/steps_hasura_migration_converters.py
  since: 2026-01
  scenario: 'Given a Hasura v2 source config with kind, database_url, and pool_settings

    When the v2 converter runs

    Then SourceType is mapped correctly and connection URL is parsed into components with pool settings
    preserved

    '
  integration_test: not_required
  integration_test_reason: Source kind mapping and connection URL parsing are pure string transformation,
    mockable.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - ops
  - data-engineer
  competitive_position:
    status: ahead
    rationale: Provisa's source kind mapping covers a broader type set than any single Hasura connector
      and handles Citus-as-PostgreSQL aliasing explicitly.
- id: REQ-624
  status: complete
  group: 12. Migration & Compatibility (Hasura)
  category: Hasura Migration Converters
  priority: SHOULD
  type: behavioral
  description: v2 converter upgrades a role to `write` capability when that role has any `delete_permissions`
    entry on any table. No per-table delete mapping is produced — the capability upgrade is the only artefact.
  use_case: Hasura v2 parity — delete permission grants are reflected as write capability in Provisa role
    definitions.
  code:
  - provisa/hasura_v2/mapper.py
  tests:
  - tests/unit/test_ddn_converter.py
  - tests/unit/test_hasura_converter.py
  - tests/unit/test_hasura_v2.py
  - tests/steps/steps_hasura_migration_converters.py
  since: 2026-01
  scenario: 'Given a Hasura v2 role with delete_permissions on any table

    When the v2 converter runs

    Then the role is upgraded to write capability with no per-table delete mapping produced

    '
  integration_test: not_required
  integration_test_reason: Role capability upgrade is pure config logic based on permission scanning,
    mockable.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - compliance
  - data-engineer
  competitive_position:
    status: neutral
    rationale: ''
- id: REQ-625
  status: complete
  group: 12. Migration & Compatibility (Hasura)
  category: Hasura Migration Converters
  priority: SHOULD
  type: behavioral
  description: 'When a Hasura v2 source `database_url` is an environment variable reference (`{"from_env":
    "VAR"}`) or cannot be parsed, the converter substitutes placeholder values: `host: localhost`, `port:
    5432`, `database: default`, `username: postgres`, `password: ${env:DB_PASSWORD}`. Operators must fix
    these via `--source-overrides` before the config is used.'
  use_case: Explicit placeholder values on parse failure make unparseable connection URLs visible rather
    than producing silent misconfiguration.
  code:
  - provisa/hasura_v2/mapper.py
  tests:
  - tests/unit/test_ddn_converter.py
  - tests/unit/test_hasura_converter.py
  - tests/unit/test_hasura_v2.py
  - tests/steps/steps_hasura_migration_converters.py
  since: 2026-01
  scenario: 'Given a Hasura v2 source with database_url as an env var reference or unparseable URL

    When the v2 converter runs

    Then placeholder connection values are substituted and operators are directed to use --source-overrides

    '
  integration_test: not_required
  integration_test_reason: Placeholder substitution on parse failure is pure conditional config generation,
    mockable.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - ops
  competitive_position:
    status: neutral
    rationale: ''
- id: REQ-626
  status: complete
  group: 12. Migration & Compatibility (Hasura)
  category: Hasura Migration Converters
  priority: SHOULD
  type: behavioral
  description: Roles are collected exclusively from permission entries (select, insert, update, delete
    permissions on tables, action permissions, and inherited role definitions). A Hasura role that exists
    without any permission entry on any table or action does not appear in the converter output.
  use_case: Permission-driven role collection ensures only roles that carry actual access rules are emitted,
    keeping the output minimal.
  code:
  - provisa/hasura_v2/mapper.py
  tests:
  - tests/unit/test_ddn_converter.py
  - tests/unit/test_hasura_converter.py
  - tests/unit/test_hasura_v2.py
  - tests/unit/test_inherited_roles_extended.py
  - tests/steps/steps_hasura_migration_converters.py
  since: 2026-01
  scenario: 'Given a Hasura project with roles that have no permission entries on any table or action

    When the v2 converter runs

    Then those roles are excluded from the output config

    '
  integration_test: not_required
  integration_test_reason: Permission-driven role collection is pure logic over config structures, mockable.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - compliance
  - data-engineer
  competitive_position:
    status: neutral
    rationale: ''
- id: REQ-627
  status: complete
  group: 12. Migration & Compatibility (Hasura)
  category: Hasura Migration Converters
  priority: SHOULD
  type: behavioral
  description: v2 converter derives the Provisa table `alias` from `custom_root_fields.select` (first
    priority), then `custom_root_fields.select_by_pk` (second priority), then `custom_name` (third priority).
    All other custom root fields (`select_aggregate`, `insert`, `update`, `delete`) are not mapped to
    any Provisa equivalent.
  use_case: Hasura v2 parity — table alias is derived from the select root field, the most common customization
    point, without requiring operators to manually specify aliases.
  code:
  - provisa/hasura_v2/mapper.py
  tests:
  - tests/unit/test_ddn_converter.py
  - tests/unit/test_hasura_converter.py
  - tests/unit/test_hasura_v2.py
  - tests/steps/steps_hasura_migration_converters.py
  since: 2026-01
  scenario: 'Given a Hasura v2 table with custom_root_fields or custom_name defined

    When the v2 converter runs

    Then the Provisa table alias is derived with select > select_by_pk > custom_name priority order

    '
  integration_test: not_required
  integration_test_reason: Table alias derivation is pure config mapping with priority rules, mockable.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - data-engineer
  - app-developer
  competitive_position:
    status: neutral
    rationale: ''
- id: REQ-628
  status: complete
  group: 12. Migration & Compatibility (Hasura)
  category: Hasura Migration Converters
  priority: SHOULD
  type: behavioral
  description: When converting DDN HML, if a Model references an ObjectType name that is not found in
    any scanned .hml file, that table is skipped and a warning is emitted to the WarningCollector; the
    conversion continues rather than aborting.
  use_case: Partial DDN projects (where some HML files are missing) produce a best-effort output with
    actionable warnings rather than a hard failure.
  code:
  - provisa/ddn/mapper.py
  tests:
  - tests/unit/test_ddn.py
  - tests/unit/test_ddn_converter.py
  - tests/unit/test_hasura_converter.py
  - tests/steps/steps_hasura_migration_converters.py
  since: 2026-01
  scenario: 'Given a DDN HML project where some ObjectType HML files are missing

    When the DDN converter runs

    Then missing ObjectType tables are skipped with a warning and conversion continues

    '
  integration_test: not_required
  integration_test_reason: Partial DDN project handling with warnings is pure control flow, mockable with
    missing references.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - data-engineer
  - app-developer
  competitive_position:
    status: neutral
    rationale: ''
- id: REQ-629
  status: complete
  group: 12. Migration & Compatibility (Hasura)
  category: Hasura Migration Converters
  priority: SHOULD
  type: structural
  description: Subgraph assignment for DDN HML documents is derived from the directory structure, not
    from a `subgraph` field in the document. The first directory component under the project root is taken
    as the subgraph name. Files under a `globals/` directory are assigned the `globals` subgraph and excluded
    from domain discovery.
  use_case: Correct subgraph-to-domain mapping without requiring stewards to add metadata to individual
    HML files.
  code:
  - provisa/ddn/parser.py
  tests:
  - tests/unit/test_ddn.py
  - tests/unit/test_ddn_converter.py
  - tests/unit/test_hasura_converter.py
  since: 2026-01
  integration_test: not_required
  integration_test_reason: Subgraph assignment from directory structure is pure path parsing, mockable
    with test file trees.
  stakeholders:
  - data-engineer
  competitive_position:
    status: neutral
    rationale: ''
- id: REQ-684
  status: complete
  since: 2026-07
  group: 1. Access Governance & Security
  category: Encryption
  priority: SHOULD
  type: structural
  description: An EncryptionService abstraction with pluggable implementations — LocalKeychain (AES-256-GCM,
    key from OS keychain), AwsKms, AzureKeyVault, and NullEncryption (dev/test passthrough). All encrypt/decrypt
    operations route through this interface. Configured via provisa.yaml encryption.provider and encryption.key_id.
  use_case: Single interface decouples encryption policy from storage and transport layers, enabling the
    same application code to run encrypted on desktop, SMB SaaS, and enterprise BYOK deployments.
  code:
  - provisa/encryption/service.py
  - provisa/encryption/providers.py
  - provisa/encryption/factory.py
  tests:
  - tests/unit/test_encryption.py
  integration_test: not_required
  integration_test_reason: Abstraction and interface contracts are logic-only; pluggable implementations
    tested via mock KMS.
  stakeholders:
  - compliance
  - ops
  competitive_position:
    status: ahead
    rationale: EncryptionService abstraction with pluggable LocalKeychain/AwsKms/AzureKeyVault/Null implementations
      is more portable than Snowflake's single-provider column encryption or Dremio's at-rest-only encryption.
- id: REQ-685
  status: complete
  since: 2026-07
  group: 1. Access Governance & Security
  category: Encryption
  priority: SHOULD
  type: structural
  description: Envelope encryption for all encrypted payloads. A unique Data Encryption Key (DEK) is generated
    per payload, used to encrypt data locally with AES-256-GCM in streaming chunks. The DEK is then encrypted
    by the org master key via KMS. Storage format is encrypted_DEK | IV | ciphertext. DEKs are cached
    in-process with a short TTL to minimize KMS round-trips. Chunk size is configurable (default 64KB)
    to bound memory usage on large payloads.
  use_case: Prevents large payload memory issues and excessive KMS costs. One KMS call per payload, not
    per row. Streaming chunks mean memory overhead is constant regardless of payload size.
  code:
  - provisa/encryption/envelope.py
  tests:
  - tests/unit/test_encryption.py
  integration_test: not_required
  integration_test_reason: Envelope encryption algorithm and chunk handling are pure logic; mock KMS provides
    full fidelity.
  stakeholders:
  - compliance
  - ops
  competitive_position:
    status: ahead
    rationale: Envelope encryption (DEK per payload, KEK from cloud KMS) for all payloads is a stronger
      security model than Hasura's no-column-encryption approach or Dremio's storage-layer-only encryption.
- id: REQ-686
  status: complete
  since: 2026-07
  group: 1. Access Governance & Security
  category: Encryption
  priority: SHOULD
  type: structural
  description: 'Application-layer column encryption for sensitive PG data. API keys and RLS rules are
    encrypted via EncryptionService before write and decrypted after read; encrypted columns stored as
    BYTEA. Structural metadata (table names, column names) remains plaintext to support query planning.
    DONE (2026-07) — the two sensitive DB-resident columns are both encrypted at rest. (1) api_sources.auth
    (keys/tokens) is BYTEA, encrypted on write (openapi register) via the process-configured EncryptionService
    accessor and decrypted before use (api_source loader). (2) rls_rules.filter_expr — the RLS predicate
    is injected as SQL by inject_rls at every governance read, so it is sensitive metadata. Encryption
    is confined to the repository boundary (provisa/core/repositories/rls.py) where upsert encrypts and
    get_for_table_role/list_for_role/ list_all decrypt. The two raw reads that bypassed the repo (the
    app startup RLS loader and the admin GraphQL rls_rules field) were funnelled through list_all so decryption
    is applied uniformly; upsert is the single write choke point (config_loader + admin both use it).
    The meta-RLS projection exposes the row over the introspection surfaces as ciphertext, consistent
    with the threat model. NullEncryption passthrough stores plaintext-bytes in the same BYTEA column,
    so the encrypted path is exercised identically whether or not a provider is configured. Both proven
    end-to-end against the live Postgres store (ciphertext at rest, decrypted round-trip through the repo,
    wrong master key rejected). COMPLETE — these are the only sensitive DB-resident columns: the sources
    and kafka_sources tables persist no credentials at all (the schema stores host/username/auth_type
    only; passwords/keys are never written, resolved at runtime via the secrets provider), and there is
    no tenant_config table. Nothing else in the config DB requires column encryption.'
  code:
  - provisa/encryption/runtime.py
  - provisa/api_source/loader.py
  - provisa/openapi/register.py
  - provisa/core/repositories/rls.py
  - provisa/core/schema_org.py
  - provisa/core/schema.sql
  - provisa/api/app.py
  - provisa/api/admin/schema.py
  tests:
  - tests/integration/test_api_auth_encryption.py
  - tests/integration/test_rls_filter_encryption.py
  - tests/unit/test_encryption_runtime.py
  - tests/unit/test_rls.py
  use_case: Compromised PG instance exposes only opaque blobs for sensitive fields. Defense in depth beyond
    schema-per-org isolation.
  integration_test: required
  integration_test_reason: Column encryption end-to-end requires real PG instance to verify encryption
    at write and decryption at read for multiple table types.
  stakeholders:
  - compliance
  - ops
  competitive_position:
    status: ahead
    rationale: Application-layer column encryption for source credentials, API keys, and RLS rules (not
      just data) protects the semantic layer metadata itself — not available in Hasura, Dremio, or Denodo.
- id: REQ-687
  status: complete
  since: 2026-07
  group: 1. Access Governance & Security
  category: Encryption
  priority: SHOULD
  type: structural
  description: 'DECIDED (bulk-only, client-held key): encrypt ONLY the S3/Iceberg BULK-RESULT payloads
    emitted by the large-result redirect path (provisa/executor/redirect.py), NOT the Trino non-SQL query
    cache. Mechanism = client-side envelope encryption: Provisa generates a per-result DEK, encrypts the
    serialized body (AES-256-GCM) via EncryptionService BEFORE upload, and stores CIPHERTEXT in S3. The
    presigned GET URL therefore delivers ciphertext; the client must possess the DEK to decrypt. The DEK
    travels to the client over the authenticated redirect response channel (not via S3), so a leaked presigned
    URL or a directly-accessed bucket object is useless without it. This is stronger than object-store
    SSE (which trusts the store to hold plaintext + the key). Presigned-URL compatibility is preserved
    precisely because Provisa hands back ciphertext — no SSE-C headers on the GET are required. WHY NOT
    the Trino non-SQL cache: that cache exists so Trino can QUERY cached rows as SQL (Phase-2 pushdown
    applies compiled WHERE/ORDER BY/LIMIT against the cache table; provisa/api_source/ trino_cache.py).
    Encrypting those payload columns makes them opaque to Trino — it can no longer filter/sort/join them,
    defeating the cache. The query cache is instead protected by short TTL + Trino/PG access control,
    and is explicitly OUT of scope for payload encryption. KEY-DELIVERY (IMPLEMENTED — role-bound grant
    + authenticated unwrap): the redirect response carries a GRANT = the raw per-result DEK sealed together
    with the creating role under the master key (a second envelope blob). To decrypt, the client POSTs
    the grant to the authenticated POST /data/redirect/unwrap; the server opens it under the MasterKeyProvider,
    verifies the caller is the creating role (or holds ADMIN/SUPERADMIN), and returns the DEK. The client
    then AES-256-GCM decrypts the iv/ciphertext parsed from the self-describing S3 blob (provisa.encryption.
    split_envelope). Possession of the presigned URL + grant alone is useless — decrypt-time authz is
    enforced AND the grant is integrity-protected so it cannot be re-scoped to another role. Rejected
    alternatives: inline raw DEK (no decrypt-time authz); SSE-C pre-shared key (breaks plain-HTTP clients).
    SECOND-CHOICE fallback, not selected: sensitive-data column-level encryption only. IMPLEMENTATION:
    opt-in via PROVISA_REDIRECT_ENCRYPT (default off, fail-closed — requires a real envelope provider);
    provisa/executor/redirect.py::_encrypt_payload seals the grant; endpoint at provisa/api/data/endpoint.py::redirect_unwrap.
    Proven against live MinIO end-to-end (tests/integration/test_redirect_encryption_minio.py) + unit
    round-trip (tests/unit/test_redirect_encryption.py).'
  use_case: Bulk result objects in S3/Iceberg are unreadable from the object store or a leaked presigned
    URL without the per-result DEK, which is released only over the authenticated redirect channel.
  target: 2026-Q4
  integration_test: required
  integration_test_reason: Cache encryption requires real Trino, real PG catalog, and real S3/Iceberg
    to verify payload encryption lifecycle and DEK management.
  stakeholders:
  - compliance
  - ops
  competitive_position:
    status: ahead
    rationale: Trino non-SQL cache row payload encryption via EncryptionService protects cached data at
      the application layer — beyond what Trino's native caching offers.
  tests:
  - tests/unit/test_redirect_encryption.py
  - tests/integration/test_redirect_encryption_minio.py
- id: REQ-688
  status: complete
  since: 2026-07
  group: 1. Access Governance & Security
  category: Encryption
  priority: SHOULD
  type: structural
  description: Redis hot table payloads (CTE fragment row data) encrypted via EncryptionService at _store_rows()
    before write and decrypted at get_rows() after read. Redis ACL prefix isolation and payload encryption
    operate as two independent controls. Encrypted payload stored as bytes blob under the existing key
    scheme.
  use_case: Redis ACL isolation prevents accidental cross-tenant key access. Payload encryption ensures
    that even a Redis ACL misconfiguration does not expose plaintext row data.
  code:
  - provisa/cache/hot_tables.py
  tests:
  - tests/unit/test_hot_tables_encryption.py
  integration_test: required
  integration_test_reason: Redis encryption requires real Redis instance and actual TTL lifecycle to verify
    payload encryption and ACL isolation interaction.
  stakeholders:
  - compliance
  - ops
  competitive_position:
    status: ahead
    rationale: Redis hot table payload encryption with ACL prefix isolation is defense-in-depth not available
      in Hasura's query cache or standard Redis deployments.
- id: REQ-689
  status: complete
  since: 2026-07
  group: 1. Access Governance & Security
  category: Encryption
  priority: SHOULD
  type: structural
  description: Audit log query text column encrypted via EncryptionService before write and decrypted
    on authorised admin reads. Query text may reveal schema shape and data intent. Stored as BYTEA.
  use_case: Audit log compliance records must not expose query semantics to a party who has PG access
    but not the org encryption key.
  code:
  - provisa/audit/query_log.py
  - provisa/core/schema_org.py
  tests:
  - tests/unit/test_audit_query_encryption.py
  - tests/integration/test_audit_encryption.py
  integration_test: required
  integration_test_reason: Audit log encryption requires real PG instance and authorized admin read path
    to verify encryption at write and authorized decryption.
  stakeholders:
  - compliance
  - ops
  competitive_position:
    status: ahead
    rationale: Audit log query text column encryption protects schema shape and data intent from unauthorized
      audit log readers — not available in Hasura, Dremio, or Denodo audit logs.
- id: REQ-690
  status: complete
  since: 2026-07
  group: 1. Access Governance & Security
  category: Encryption
  priority: MAY
  type: structural
  unit_test: false
  unit_test_reason: The feature lives entirely in the Java JDBC driver (a separate Maven module); its
    unit tests are JUnit tests under jdbc-driver/src/test/java/ (AwsKmsProviderTest, AzureKmsProviderTest,
    GcpKmsProviderTest, KmsProvidersTest, EnvelopeDecryptorTest), not the Python tests/unit/ tree. No
    Python-mockable interface exists.
  description: Client-side decryption in the JDBC driver. Provisa backend passes encrypted blobs through
    without decrypting. Driver decrypts result columns flagged as encrypted via column metadata using
    EncryptionService (Java) configured via connection URL params (kms_provider, kms_key_arn). DEK cached
    in driver process with TTL. Backend never sees plaintext query results.
  use_case: Strongest threat posture — a fully compromised Provisa backend cannot read query result data.
    Modelled on MongoDB CSFLE pattern.
  target: 2027-Q1
  integration_test: required
  integration_test_reason: JDBC driver client-side decryption requires end-to-end JDBC connection, backend
    encrypted response, and driver decryption to verify data flow.
  e2e: true
  e2e_reason: Client-side decryption in the JDBC driver requires a real pgwire network connection, an
    actual encrypted backend response, and driver-side decryption to confirm the full round-trip; no unit
    or integration test can substitute for this cross-process transport.
  stakeholders:
  - app-developer
  - compliance
  competitive_position:
    status: ahead
    rationale: Client-side decryption in the JDBC driver with server-side passthrough is a zero-trust
      data access pattern not offered by any competitor's JDBC driver.
  note: The jdbc-driver module implements the full client-side decryption path — the envelope engine (EnvelopeDecryptor
    + KmsProvider + LocalKmsProvider), the encrypted-column metadata contract (Arrow field metadata provisa_encrypted=true,
    decrypted in ArrowStreamResultSet), the DEK TTL cache, and connection-param parsing (kms_provider,
    kms_key_arn). The AWS/Azure/GCP concrete KmsProvider implementations unwrap the DEK via the real cloud
    KMS Decrypt API (KmsClient.decrypt / CryptographyClient.decrypt / KeyManagementServiceClient.decrypt),
    holding only the CMK identifier plus an injected SDK client (never raw key material) and mapping any
    SDK failure — incl AccessDenied / PERMISSION_DENIED / forbidden on a revoked grant — to DecryptionException
    (fail-loud instant lockout, REQ-694). KmsProviders selects the provider by kms_provider and is wired
    into ProvisaConnection; a named provider whose cloud SDK is absent from the classpath fails loud (SQLException).
    The cloud SDKs are provided-scope (compile+test classpath, not shaded into the fat JAR — the deployer
    supplies the SDK at runtime, keeping the driver lean). mvn clean test green (45 tests). The e2e JDBC
    round-trip (REQ-690 e2e flag) against a live pgwire/Flight encrypted backend response remains a separate
    e2e concern. See docs/arch/jdbc-client-side-encryption.md.
  code:
  - jdbc-driver/src/main/java/io/provisa/jdbc/EnvelopeDecryptor.java
  - jdbc-driver/src/main/java/io/provisa/jdbc/KmsProvider.java
  - jdbc-driver/src/main/java/io/provisa/jdbc/LocalKmsProvider.java
  - jdbc-driver/src/main/java/io/provisa/jdbc/AwsKmsProvider.java
  - jdbc-driver/src/main/java/io/provisa/jdbc/AzureKmsProvider.java
  - jdbc-driver/src/main/java/io/provisa/jdbc/GcpKmsProvider.java
  - jdbc-driver/src/main/java/io/provisa/jdbc/KmsProviders.java
  - jdbc-driver/src/main/java/io/provisa/jdbc/ArrowStreamResultSet.java
  - jdbc-driver/src/main/java/io/provisa/jdbc/ProvisaConnection.java
  - jdbc-driver/src/main/java/io/provisa/jdbc/ProvisaDriver.java
  - jdbc-driver/pom.xml
  tests:
  - jdbc-driver/src/test/java/io/provisa/jdbc/AwsKmsProviderTest.java
  - jdbc-driver/src/test/java/io/provisa/jdbc/AzureKmsProviderTest.java
  - jdbc-driver/src/test/java/io/provisa/jdbc/GcpKmsProviderTest.java
  - jdbc-driver/src/test/java/io/provisa/jdbc/KmsProvidersTest.java
  - jdbc-driver/src/test/java/io/provisa/jdbc/EnvelopeDecryptorTest.java
- id: REQ-691
  status: complete
  since: 2026-07
  group: 1. Access Governance & Security
  category: Encryption
  priority: MAY
  type: structural
  description: Client-side decryption in the Python client (DB-API, SQLAlchemy dialect, ADBC). EncryptionService
    Python implementation shared across dbapi.py, sqlalchemy_dialect.py, and adbc.py. KMS provider and
    key configured via connection params. DEK cached in process with TTL.
  use_case: Python users (pandas, notebooks, data pipelines) get same client-side decrypt guarantees as
    JDBC users without a separate decrypt step.
  target: 2027-Q1
  integration_test: required
  integration_test_reason: Python client decryption requires end-to-end HTTP/pgwire connection and real
    client process to verify DEK caching and decryption across DB-API/SQLAlchemy/ADBC.
  e2e: true
  e2e_reason: Python client-side decryption requires a live HTTP or pgwire connection from a real client
    process receiving encrypted responses to verify DEK caching and decryption across DB-API, SQLAlchemy,
    and ADBC transports.
  stakeholders:
  - app-developer
  - compliance
  competitive_position:
    status: ahead
    rationale: Client-side decryption shared across DB-API, SQLAlchemy dialect, and ADBC in the Python
      client is a comprehensive zero-trust client library not offered by Hasura or Dremio.
  code:
  - provisa-client/provisa_client/encryption.py
  - provisa-client/provisa_client/dbapi.py
  - provisa-client/provisa_client/adbc.py
  - provisa-client/provisa_client/sqlalchemy_dialect.py
  tests:
  - tests/unit/test_client_encryption.py
- id: REQ-692
  status: complete
  since: 2026-07
  group: 1. Access Governance & Security
  category: Encryption
  priority: MAY
  type: structural
  description: Client-side decryption for GraphQL clients via a thin wrapper. Provisa GraphQL schema marks
    encrypted fields with an @encrypted directive. The Provisa GQL client wrapper intercepts responses
    and decrypts flagged fields using EncryptionService before returning to caller. Scoped to non-browser
    clients only — browser-side KMS access weakens the threat model.
  use_case: GraphQL API consumers (non-browser) participate in client-side decrypt without backend plaintext
    exposure.
  target: 2027-Q1
  integration_test: required
  integration_test_reason: GraphQL client decryption requires end-to-end GraphQL transport and real client
    process to verify response interception and field-level decryption.
  e2e: true
  e2e_reason: GraphQL client decryption requires a live GraphQL transport, a real server response with
    encrypted fields, and client wrapper interception to confirm field-level decryption works across the
    network boundary.
  stakeholders:
  - app-developer
  - compliance
  competitive_position:
    status: ahead
    rationale: '@encrypted GraphQL directive with client wrapper intercept for automatic decryption is
      a novel zero-trust GraphQL pattern not available in Hasura, Dremio, or Denodo.'
  code:
  - provisa-client/provisa_client/graphql_decrypt.py
  - provisa/api/data/encrypted_directive.py
  - provisa/api/data/sdl.py
  tests:
  - tests/unit/test_client_encryption.py
  - tests/unit/test_high_security_mode.py
- id: REQ-693
  status: complete
  since: 2026-07
  group: 1. Access Governance & Security
  category: Encryption
  priority: MAY
  type: constraint
  description: High-security mode (security.mode=high in provisa.yaml). When enabled — pgwire server is
    not started; the HTTP data plane returns 403; JDBC and Python client connections without kms_key_arn
    are rejected at auth. Only JDBC and Python clients with client-side decrypt configured may connect.
    Provisa backend handles only encrypted blobs and query planning metadata. Every non-HTTP surface is
    decided the same way, by whether the protocol can carry proof that the client decrypts for itself.
    Bolt and MCP cannot, so neither port opens — Bolt''s HELLO/LOGON exchange negotiates a credential
    rather than a decryption context, and an MCP tool call hands result rows to a model as text. gRPC
    and Arrow Flight are the transports encrypting clients actually use, so they keep serving and instead
    demand the same client-side decryption key the HTTP data endpoints demand, per call. Closing them
    too would leave a high-security deployment with no wire protocol at all. On gRPC the key arrives as
    the x-provisa-kms-key call header and the check runs ahead of the credential check on every RPC, reflection
    included. On Flight it arrives as the ticket''s kms_key field and gates only tickets carrying a query
    — the catalog branch returns table and column names only, so it stays reachable exactly as /data/sdl
    does. The HTTP gate is deny-by-default over the whole /data tree, with the schema-metadata endpoints
    (sdl, introspection, schema-version, domains, proto and compile) enumerated as the exemptions. An
    allow-list of row-returning prefixes had silently left /data/ingest, /data/subscribe and the /data/grpc
    proxies open, so a route added later must now argue its way out of the gate rather than default into
    it. High-security mode additionally pins the REQ-603 relationship guard ON — ignore_relationships
    and a cleared relationship_guard flag are both IGNORED there, so no query may join outside the approved
    relationship catalog regardless of grant. This is belts and suspenders — a production deployment that
    improperly granted the discovery capability to a role does not thereby get a break-out from the model.
  use_case: Regulated or airgapped environments where the Provisa backend must not handle plaintext data
    under any circumstances. Pinning the relationship guard means an operator misconfiguration (granting
    ignore_relationships in prod) cannot widen what a query may read.
  scenario: 'Given security.mode=high

    When the server starts

    Then the pgwire, Bolt and MCP listeners are not opened, and each logs the refusal against REQ-693


    Given security.mode=high

    When a gRPC caller invokes any RPC without the x-provisa-kms-key call header

    Then the call is refused PERMISSION_DENIED before the credential is even read, reflection included


    Given security.mode=high

    When an Arrow Flight client submits a ticket carrying a query but no kms_key

    Then the ticket is refused, while a catalog ticket returning only table and column names is served


    Given security.mode=high

    When a gRPC or Arrow Flight client presents a client-side decryption key on the call

    Then the call proceeds to the ordinary credential and role checks


    Given security.mode=high

    When a request reaches any /data path other than the enumerated schema-metadata endpoints

    Then it is refused 403 without a client-side decryption key, ingest and subscribe included

    '
  target: 2027-Q1
  integration_test: required
  integration_test_reason: High-security mode requires end-to-end verification of server startup behavior,
    endpoint 403s, auth rejection, and plaintext-rejection across real HTTP/pgwire transports.
  e2e: true
  e2e_reason: High-security mode behavior (pgwire disabled, endpoint 403s, auth rejection) can only be
    verified by actually starting the server with security.mode=high and confirming each transport is
    blocked at the network boundary.
  stakeholders:
  - ops
  - compliance
  competitive_position:
    status: ahead
    rationale: High-security mode that disables pgwire and REST APIs, enforcing KMS-keyed JDBC/Python-only
      access is a unique compliance posture not available in any competitor platform.
  code:
  - provisa/security/high_security.py
  - provisa/api/app.py
  - provisa/api/app_startup.py
  - provisa/api/app_loaders.py
  - provisa/core/models.py
  - provisa/grpc/auth.py
  - provisa/api/flight/server.py
  - provisa/api/mcp/server.py
  tests:
  - tests/unit/test_high_security_mode.py
  - tests/unit/test_grpc_auth.py
  - tests/unit/test_flight_auth.py
  - tests/unit/test_mcp_server.py
  - tests/integration/test_high_security_surfaces.py
- id: REQ-694
  status: complete
  since: 2026-07
  group: 1. Access Governance & Security
  category: Encryption
  priority: SHOULD
  type: structural
  description: Client-owned KMS key model. Org master key is a CMK in the client's own cloud account (AWS
    KMS, Azure Key Vault, or GCP KMS). Client grants Provisa a scoped role permission (kms:Decrypt and
    kms:GenerateDataKey) via cross-account IAM or equivalent. Provisa never stores raw key material. Client
    revokes grant to immediately render all their data unreadable. Supported for both SMB managed tier
    and enterprise BYOB tier.
  use_case: Client holds the kill switch — revoking KMS access immediately cuts Provisa's ability to read
    their data without requiring data deletion or migration.
  target: 2026-Q4
  integration_test: deferred
  integration_test_reason: Cross-account KMS and cloud key vault access require live AWS/Azure/GCP infrastructure
    not available in CI.
  stakeholders:
  - compliance
  - ops
  - executive
  competitive_position:
    status: ahead
    rationale: Client-owned CMK model (AWS KMS, Azure Key Vault, GCP KMS) where Provisa never holds the
      master key is stronger than Snowflake's customer-managed key model and not available in Hasura or
      Dremio.
  code:
  - provisa-client/provisa_client/encryption.py
  tests:
  - tests/unit/test_client_encryption.py
- id: REQ-695
  status: complete
  since: 2026-07
  group: 11. Platform, Infrastructure & Delivery
  category: Multi-Tenancy
  priority: MUST
  type: structural
  description: All internal PostgreSQL tables (semantic metadata, non-SQL cache, audit log, materialized
    view definitions) are scoped to a per-org schema named org_<org_id>. The asyncpg connection pool sets
    search_path=org_<org_id> on every connection via the connection init hook. A default org_id of "default"
    is used when ORG_ID env var is not set, producing schema org_default. Existing single-org deployments
    are unaffected — they transparently use org_default.
  use_case: Schema-per-org provides structural isolation between tenants on a shared PostgreSQL instance.
    Accidental cross-tenant data access requires bypassing both the search_path and the role-level grants.
  integration_test: not_required
  integration_test_reason: not active
  stakeholders:
  - ops
  - data-engineer
  competitive_position:
    status: ahead
    rationale: Per-org PostgreSQL schema (org_<org_id>) for all internal metadata provides schema-level
      isolation stronger than row-level tenant filtering used by Hasura Cloud or Dremio.
  code:
  - provisa/core/db.py
  - provisa/api/app.py
  tests:
  - tests/unit/test_org_isolation.py
  - tests/unit/test_table_search.py
  related_reqs:
  - REQ-828
- id: REQ-696
  status: complete
  since: 2026-07
  group: 11. Platform, Infrastructure & Delivery
  category: Multi-Tenancy
  priority: MUST
  type: structural
  description: Platform tables (tenants, tenant_config) reside in a dedicated platform schema, never touched
    by org search_path. The platform schema has a separate asyncpg pool or uses fully-qualified queries
    (platform.tenants). Org schemas cannot reference or join to the platform schema.
  use_case: Tenant registry and billing data must not co-mingle with per-org application data. A compromised
    org connection cannot enumerate other tenants.
  integration_test: not_required
  integration_test_reason: not active
  stakeholders:
  - ops
  competitive_position:
    status: neutral
    rationale: ''
  code:
  - provisa/core/db.py
  - provisa/api/app.py
  tests:
  - tests/unit/test_org_isolation.py
  - tests/unit/test_table_search.py
  related_reqs:
  - REQ-828
- id: REQ-697
  status: complete
  since: 2026-07
  group: 11. Platform, Infrastructure & Delivery
  category: Multi-Tenancy
  priority: MUST
  type: structural
  description: init_schema() in provisa/core/db.py accepts an org_id parameter. It creates the org_<org_id>
    schema if not exists, then runs core schema SQL, audit schema SQL, and MV schema SQL within that schema.
    Called at startup from _init_pg_pool_and_schema() in provisa/api/app.py using ORG_ID env var (default
    "default").
  use_case: Schema creation is idempotent and org-scoped from first boot. No manual schema setup required
    for new tenant provisioning.
  integration_test: not_required
  integration_test_reason: not active
  stakeholders:
  - ops
  - data-engineer
  competitive_position:
    status: neutral
    rationale: ''
  code:
  - provisa/core/db.py
  - provisa/api/app.py
  tests:
  - tests/unit/test_org_isolation.py
- id: REQ-698
  status: complete
  since: 2026-07
  group: 11. Platform, Infrastructure & Delivery
  category: Multi-Tenancy
  priority: MUST
  type: structural
  description: The Trino non-SQL cache schema is org-scoped. cache_location() in provisa/api_source/trino_cache.py
    accepts org_id and sets cache_schema=org_<org_id>_api_cache. All callers (endpoint.py, router_integration.py,
    cypher_router.py) pass org_id from request context. The provisa_admin Trino catalog requires no change
    as Trino uses fully-qualified catalog.schema.table references.
  use_case: Non-SQL cached rows for different orgs land in separate PG schemas within the provisa_admin
    catalog. A Trino query for org A cannot reference org B cache tables without an explicit schema qualifier.
  integration_test: not_required
  integration_test_reason: not active
  stakeholders:
  - ops
  - data-engineer
  competitive_position:
    status: ahead
    rationale: Org-scoped Trino cache schemas (org_<org_id>_api_cache) ensure no cross-tenant cache bleed
      in the query federation layer — not present in standard Trino or Hasura multi-tenant setups.
  code:
  - provisa/api_source/trino_cache.py
  - provisa/api/data/endpoint.py
  tests:
  - tests/unit/test_catalog_cache.py
  - tests/unit/test_org_isolation.py
- id: REQ-699
  status: complete
  since: 2026-07
  group: 11. Platform, Infrastructure & Delivery
  category: Multi-Tenancy
  priority: MUST
  type: structural
  description: Each org has a dedicated PostgreSQL role named role_<org_id> with USAGE and CREATE on org_<org_id>
    schema only. The asyncpg pool connects as this role. The role has no access to platform schema or
    any other org schema.
  use_case: Database-level role scoping provides a second isolation layer below search_path. A malformed
    query using an unqualified table name cannot accidentally read another org's data even if search_path
    is misconfigured.
  integration_test: not_required
  integration_test_reason: not active
  stakeholders:
  - ops
  competitive_position:
    status: ahead
    rationale: Dedicated PostgreSQL role per org with schema-only access enforces DB-level tenant isolation
      beyond row-level security used by Hasura Cloud.
  code:
  - provisa/core/db.py
  tests:
  - tests/unit/test_org_isolation.py
  related_reqs:
  - REQ-828
- id: REQ-700
  status: complete
  since: 2026-07
  group: 11. Platform, Infrastructure & Delivery
  category: Multi-Tenancy
  priority: MUST
  type: structural
  description: Extends REQ-595 for on-prem multi-org deployments. Redis cache keys, hot table keys, APQ
    keys, and query result keys are all prefixed with org_id (org:<org_id>:...) rather than tenant_id.
    Redis ACL rules scope each org's credentials to their key prefix. One org's cache flush or eviction
    never touches another org's keys. Requires wiring org_id through check_cache/store_result in cache/middleware.py
    (the same gap blocking REQ-595 completion).
  use_case: Redis key prefix isolation complements PG schema isolation (REQ-695). Both layers must be
    independently correct — ACL misconfiguration in one should not cascade to the other.
  integration_test: not_required
  integration_test_reason: not active
  stakeholders:
  - ops
  - data-engineer
  competitive_position:
    status: ahead
    rationale: org_id-prefixed Redis keys with ACL rules per org provides cache isolation at the Redis
      ACL layer — stronger than namespace-only separation used by most multi-tenant SaaS platforms.
  code:
  - provisa/cache/middleware.py
  - provisa/cache/store.py
  - provisa/core/org_provisioning.py
  tests:
  - tests/integration/test_cache_store.py
  - tests/unit/test_cache_store.py
  - tests/unit/test_org_isolation.py
- id: REQ-701
  status: complete
  since: 2026-07
  group: 11. Platform, Infrastructure & Delivery
  category: Multi-Tenancy
  priority: SHOULD
  type: structural
  description: Org provisioning creates all required infrastructure atomically — org PG schema, PG role,
    Redis ACL user, and Trino cache schema — in a single provisioning transaction or compensating workflow.
    Partial provisioning leaves no orphaned resources. Deprovisioning drops all org resources in reverse
    order.
  use_case: Atomic provisioning prevents half-initialised orgs from entering a queryable but misconfigured
    state. Clean deprovisioning enables tenant offboarding without manual cleanup.
  integration_test: not_required
  integration_test_reason: not active
  stakeholders:
  - ops
  competitive_position:
    status: ahead
    rationale: Atomic org provisioning (PG schema + role + Redis ACL + Trino cache schema) with compensating
      workflow on partial failure is more robust than Hasura Cloud's sequential provisioning.
  code:
  - provisa/core/org_provisioning.py
  tests:
  - tests/unit/test_org_isolation.py
- id: REQ-702
  status: complete
  since: 2026-07
  group: 11. Platform, Infrastructure & Delivery
  category: Multi-Tenancy
  priority: MUST
  type: structural
  description: Demo init code and static demo assets are org-scoped. _seed_built_in_sources() passes state.org_id
    to _seed_meta_domain(); _seed_ops_pg() inherits the correct schema via the asyncpg connection pool
    search_path set by REQ-695. Static demo file assets (Parquet, SQLite, CSV) contain no schema references.
    No migration tool is required — org scoping is handled entirely through the connection pool and seed
    function wiring.
  use_case: Demo environments must participate in org isolation (REQ-695 through REQ-701) without special-casing.
    Seeded data lands in org_<org_id> automatically via the pool init hook, ensuring demo deployments
    and production multi-tenant deployments use the same code path.
  dependencies:
  - REQ-695
  - REQ-697
  integration_test: not_required
  integration_test_reason: not active
  stakeholders:
  - ops
  - data-engineer
  competitive_position:
    status: neutral
    rationale: ''
  code:
  - provisa/api/app.py
  tests:
  - tests/unit/test_org_isolation.py
  related_reqs:
  - REQ-828
- id: REQ-703
  status: complete
  group: 6. Execution, Routing, Caching & Performance
  category: High Availability
  priority: MUST
  type: infrastructure
  description: Provisa implements a two-tier recovery model for all deployment modes. Tier 1 retries read
    operations for up to 30 seconds on transient errors using exponential backoff with full jitter (configurable
    via PROVISA_RETRY_BUDGET_SECS). Tier 2 uses an internal watcher to detect and restart failed software
    components within 2–3 minutes. Write operations are never retried internally. Machine-level and cluster-level
    failures are the customer's responsibility.
  use_case: Read-heavy query workloads may encounter transient coordinator unavailability, brief network
    partitions, or temporary query queue saturation. Automatic retry provides resilience without requiring
    client-side retry logic for all read operations. Software component failures are self-healing within
    the deployment boundary; cluster-wide HA is configured by the customer based on their failure domain
    (single-node, VM/metal, K8s).
  code:
  - provisa/executor/trino.py
  - provisa/executor/direct.py
  - provisa/scheduler/jobs.py
  - terraform/aws/main.tf
  - terraform/gcp/main.tf
  - terraform/azure/main.tf
  tests:
  - tests/unit/test_high_availability.py
  integration_test: deferred
  integration_test_reason: 'End-to-end Trino container restart timing (2-3 min) requires a live Docker
    environment. [deferred: requires external infrastructure not in CI stack]'
  e2e: false
  e2e_reason: 'The two-tier retry model (exponential backoff on transient errors, component watcher restart)
    can only be verified by injecting real transient failures into a running server and observing retry
    and recovery behavior over actual network connections. [deferred: requires full UI/installer stack
    not available in CI]'
  scenario: 'Given a read operation is issued against Provisa

    When a transient error occurs (e.g., COORDINATOR_NOT_AVAILABLE, ConnectionError)

    Then Provisa internally retries with exponential backoff for up to PROVISA_RETRY_BUDGET_SECS (default
    30s)


    Given a software component fails (e.g., executor crashes)

    When the internal watcher detects the failure

    Then the component is restarted within 2–3 minutes

    And in-flight operations are lost (client must retry)


    Given a write operation fails for any reason

    Then it is never retried internally

    And the error is returned to the client immediately

    '
  since: 2026-06
  stakeholders:
  - ops
  - executive
  competitive_position:
    status: ahead
    rationale: 'Consistent with Trino, Spark, Presto, Snowflake, and BigQuery — none guarantee coordinator
      HA out of the box. Provisa improves on the baseline: built-in read retry with configurable budget
      means every client gets resilience for free without client-side implementation; DNS-stable coordinator
      address removes a manual ops step competitors leave to the customer; write-safety guarantee (never
      retry writes internally) is a correctness property that naive client retry implementations routinely
      miss.'
- id: REQ-711
  status: complete
  since: 2026-07
  group: 8. Client Access & Protocols
  category: ADBC Client
  priority: SHOULD
  type: structural
  description: adbc_connect() must accept an optional port parameter (default 8815) so callers can connect
    to Arrow Flight servers running on non-default ports. The hardcoded port 8815 must be replaced with
    this configurable value throughout provisa_client/adbc.py.
  use_case: Operators who cannot run the Flight server on port 8815 (due to port conflicts or network
    policy) can connect via ADBC without falling back to DB-API or SQLAlchemy.
  code:
  - provisa_client/adbc.py
  stakeholders:
  - ops
  - app-developer
  competitive_position:
    status: gap
    rationale: Dremio's Flight client and DuckDB's ADBC drivers allow the port to be specified at connection
      time. Provisa currently hardcodes 8815, forcing operators to use an alternative client if port 8815
      is unavailable.
  e2e: true
  e2e_reason: The configurable port for adbc_connect() can only be verified by establishing a real Arrow
    Flight connection to a server running on a non-default port, which requires a live stack.
  unit_test: false
  unit_test_reason: The unit test lives in the provisa-client package (provisa-client/tests/test_adbc.py),
    which ships and is versioned independently of the server; the repo tests/unit/ scan does not cover
    it.
  tests:
  - provisa-client/tests/test_adbc.py
- id: REQ-712
  status: complete
  superseded_by: REQ-595
  group: 6. Execution, Routing, Caching & Performance
  category: Cache
  priority: MUST
  type: constraint
  description: check_cache and store_result in provisa/cache/middleware.py pass tenant_id (org_id) through
    to RedisCacheStore.get/set, so the per-tenant cache key prefix implemented in the store is applied
    at the call site and multi-tenant cache key isolation is active for query result caching.
  use_case: Tenant-prefixed cache keys at the middleware layer ensure one tenant's cached query results
    can never be returned to another tenant.
  code:
  - provisa/cache/middleware.py
  since: 2026-06
  stakeholders:
  - ops
  - compliance
  competitive_position:
    status: ahead
    rationale: Redis-backed query result caches that do not partition by tenant are a known security risk
      in multi-tenant SaaS deployments. Provisa partitions every query result cache key by tenant_id at
      the middleware layer, matching the tenant-account result isolation Snowflake and BigQuery enforce.
  tests:
  - tests/integration/test_cache_store.py
  - tests/unit/test_apq_cache.py
  - tests/unit/test_cache_key.py
  - tests/unit/test_cache_store.py
  - tests/unit/test_mv_tenant_isolation.py
- id: REQ-713
  status: complete
  group: 7. Result Delivery
  category: Neo4j Export
  priority: SHOULD
  type: behavioral
  description: Neo4j export endpoint accepts graph nodes and edges via POST /data/neo4j-export, including
    target URL, credentials, database, and node/edge lists.
  use_case: Provisa users can export subgraphs queried via Cypher to external Neo4j servers for analysis,
    backup, or integration with other tools.
  code:
  - provisa/api/rest/cypher_router.py
  tests:
  - provisa-ui/e2e/neo4j-docker-export.spec.ts
  - tests/unit/test_neo4j_export.py
  since: 2026-06
  e2e: true
  e2e_reason: Full round-trip verification requires schema discovery, batch Cypher queries, export to
    real Neo4j instance, and count verification against the target server.
  scenario: 'Given a list of nodes with id, tableLabel, and properties, and edges with start, end, and
    type

    When POST /data/neo4j-export is called with url, username, password, database, nodes, and edges

    Then the nodes and edges are transmitted to the Neo4j HTTP transactional API

    '
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
- id: REQ-714
  status: complete
  group: 7. Result Delivery
  category: Neo4j Export
  priority: SHOULD
  type: behavioral
  description: Nodes are MERGE'd into Neo4j using _provisa_id as the deduplication key, with labels derived
    from tableLabel or compound "Domain:Table" label fields.
  use_case: Preserves node identity across export runs and handles domain-scoped node types.
  code:
  - provisa/api/rest/cypher_router.py
  tests:
  - provisa-ui/e2e/neo4j-docker-export.spec.ts
  - tests/unit/test_neo4j_export.py
  since: 2026-06
  scenario: 'Given a node with tableLabel "User" and properties {name: "Alice", age: 30}

    When the node is exported via POST /data/neo4j-export

    Then Neo4j contains a node with label "User", property _provisa_id set, and properties SET via +=
    operator

    '
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
- id: REQ-715
  status: complete
  group: 7. Result Delivery
  category: Neo4j Export
  priority: SHOULD
  type: behavioral
  description: Node properties are written to Neo4j using SET n += {...} with Cypher literal encoding
    to safely handle nested objects, arrays, nulls, and special types.
  use_case: Ensures property values (booleans, numbers, strings, nested structures) are correctly transmitted
    to Neo4j without injection or serialization errors.
  code:
  - provisa/api/rest/cypher_router.py
  tests:
  - provisa-ui/e2e/neo4j-docker-export.spec.ts
  - tests/unit/test_neo4j_export.py
  since: 2026-06
  scenario: 'Given a node with properties {active: true, count: 42, name: "Test", nested: {key: "value"}}

    When the node is exported

    Then all properties are SET in Neo4j with correct Cypher literal types

    '
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
- id: REQ-716
  status: complete
  group: 7. Result Delivery
  category: Neo4j Export
  priority: SHOULD
  type: behavioral
  description: Edges are MERGE'd by matching source and target nodes on _provisa_id, with relationship
    type preserved from the source graph.
  use_case: Creates or updates edges in Neo4j with correct source and target identity, supporting graph
    structure reconstruction.
  code:
  - provisa/api/rest/cypher_router.py
  tests:
  - provisa-ui/e2e/neo4j-docker-export.spec.ts
  - tests/unit/test_neo4j_export.py
  since: 2026-06
  scenario: 'Given an edge with start: 101, end: 202, type: "CONNECTS_TO"

    When the edge is exported

    Then Neo4j contains a relationship matching (a:Label{_provisa_id: 101})-[r:CONNECTS_TO]->(b:Label{_provisa_id:
    202})

    '
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
- id: REQ-717
  status: complete
  group: 7. Result Delivery
  category: Neo4j Export
  priority: SHOULD
  type: behavioral
  description: Neo4j export endpoint uses HTTP Basic authentication via Authorization header (base64-encoded
    username:password) to authenticate with the target server.
  use_case: Securely authenticates to external Neo4j instances without embedding credentials in the URL.
  code:
  - provisa/api/rest/cypher_router.py
  tests:
  - provisa-ui/e2e/neo4j-docker-export.spec.ts
  - tests/unit/test_neo4j_export.py
  since: 2026-06
  scenario: 'Given credentials username: "neo4j", password: "secret"

    When POST /data/neo4j-export is called

    Then Authorization header contains "Basic " + base64("neo4j:secret")

    '
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
- id: REQ-718
  status: complete
  group: 7. Result Delivery
  category: Neo4j Export
  priority: MUST
  type: constraint
  description: Connection errors to Neo4j return HTTP 502; timeouts return 504; authentication failures
    return 401.
  use_case: Clients can distinguish transient (502, 504) from permanent (401) failures and retry or fix
    credentials appropriately.
  code:
  - provisa/api/rest/cypher_router.py
  tests:
  - provisa-ui/e2e/neo4j-docker-export.spec.ts
  - tests/unit/test_neo4j_export.py
  since: 2026-06
  scenario: 'Given Neo4j is unreachable

    When POST /data/neo4j-export is called

    Then the endpoint returns HTTP 502 with error message

    '
- id: REQ-719
  status: complete
  group: 7. Result Delivery
  category: Neo4j Export
  priority: SHOULD
  type: behavioral
  description: 'Neo4j export endpoint returns {imported: N, errors: [...]} structure, allowing partial
    success where some statements fail while others succeed.'
  use_case: Clients can detect which nodes/edges failed to import and retry or log them without treating
    the entire export as a failure.
  code:
  - provisa/api/rest/cypher_router.py
  tests:
  - provisa-ui/e2e/neo4j-docker-export.spec.ts
  - tests/unit/test_neo4j_export.py
  since: 2026-06
  scenario: 'Given an export with 10 nodes, where 1 node fails due to a Neo4j constraint violation

    When POST /data/neo4j-export completes

    Then the response contains imported: 9, errors: ["constraint violation message"]

    '
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
- id: REQ-720
  status: complete
  group: 7. Result Delivery
  category: Neo4j Export
  priority: SHOULD
  type: behavioral
  description: E2E export flow batches nodes and edges separately to avoid transaction timeouts, with
    default batch size of 200 nodes per request.
  use_case: Allows exporting large graphs by chunking them into manageable transaction sizes, avoiding
    Neo4j timeout errors on large statements.
  code:
  - provisa/api/rest/cypher_router.py
  tests:
  - provisa-ui/e2e/neo4j-docker-export.spec.ts
  - tests/unit/test_neo4j_export.py
  since: 2026-06
  scenario: 'Given a graph with 500 nodes and 300 edges

    When the E2E export test runs

    Then nodes are sent in 3 batches (200, 200, 100) and edges in 1 batch

    '
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
- id: REQ-721
  status: complete
  group: 4. Source Connectors
  category: Splunk Connector
  priority: SHOULD
  type: behavioral
  description: Splunk is registered as a source type and exposed via Apache Calcite connector (kenstott/calcite),
    enabling Splunk search results to be queried as tables through Trino.
  use_case: Allows users to register Splunk instances as data sources and query their search results via
    the Provisa GraphQL API.
  code:
  - provisa/core/models.py
  - provisa/core/catalog.py
  tests:
  - provisa-ui/e2e/splunk-connector.spec.ts
  - tests/unit/test_splunk_connector.py
  since: 2026-06
  e2e: true
  e2e_reason: 'Query results are only verifiable through full live-stack E2E test: Splunk container must
    run, credentials passed to Trino, and schema enumeration/query execution observed.'
  scenario: 'Given a Splunk instance with admin credentials

    When a user registers the Splunk source with host, port, and auth token

    Then the source is added to the catalog and Splunk''s search tables (internal_server)

    are enumerable

    '
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
- id: REQ-722
  status: complete
  group: 4. Source Connectors
  category: Splunk Connector
  priority: SHOULD
  type: behavioral
  description: Splunk connector accepts a URL (from base_url or constructed via https://host:port, default
    port 8089) to connect to the Splunk management API.
  use_case: 'Provides a flexible connection method: users can specify a complete base_url or rely on host:port
    defaults to simplify configuration.'
  code:
  - provisa/core/catalog.py
  tests:
  - provisa-ui/e2e/splunk-connector.spec.ts
  - tests/unit/test_splunk_connector.py
  since: 2026-06
  e2e: true
  e2e_reason: URL construction behavior can only be verified through end-to-end Splunk connector registration
    and schema enumeration.
  scenario: 'Given Splunk host=splunk and port=8089

    When the source is registered

    Then the connector receives url=https://splunk:8089

    '
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
- id: REQ-723
  status: complete
  group: 4. Source Connectors
  category: Splunk Connector
  priority: SHOULD
  type: behavioral
  description: 'Splunk authentication: when use_token=true (default), the password field is passed as
    token; when use_token=false, username and password fields are passed as separate credentials.'
  use_case: Splunk supports both token-based (preferred for API automation) and username/password authentication,
    allowing users to choose their auth method.
  code:
  - provisa/core/catalog.py
  tests:
  - provisa-ui/e2e/splunk-connector.spec.ts
  - tests/unit/test_splunk_connector.py
  since: 2026-06
  scenario: 'Given a source with use_token=true and a password field

    When the connector properties are built

    Then props contains token=<password> and no user/password keys

    '
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
- id: REQ-724
  status: complete
  group: 4. Source Connectors
  category: Splunk Connector
  priority: SHOULD
  type: behavioral
  description: Splunk connector supports optional app and datamodel-filter properties from source.database
    and source.mapping, plus disable-ssl-validation for self-signed certificates.
  use_case: Allows filtering queries to a specific Splunk app and disabling SSL validation for development/test
    environments with self-signed certs.
  code:
  - provisa/core/catalog.py
  tests:
  - provisa-ui/e2e/splunk-connector.spec.ts
  - tests/unit/test_splunk_connector.py
  since: 2026-06
  scenario: 'Given a source with database=search_app and mapping.disable_ssl_validation=true

    When the connector properties are built

    Then props contains app=search_app and disable-ssl-validation=true

    '
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
- id: REQ-725
  status: complete
  group: 4. Source Connectors
  category: Splunk Connector
  priority: SHOULD
  type: constraint
  description: Splunk connector always sets case-insensitive-name-matching=true, allowing case-insensitive
    table and column lookups.
  use_case: Ensures consistent behavior when users reference Splunk table names regardless of case, matching
    Splunk's own case-insensitive semantics.
  code:
  - provisa/core/catalog.py
  since: 2026-06
  scenario: 'Given a Splunk source and a query against table InternalServer

    When the query is executed

    Then case-insensitive matching resolves it to internal_server table

    '
  tests:
  - tests/unit/test_splunk_connector.py
- id: REQ-726
  status: complete
  group: 4. Source Connectors
  category: SharePoint Connector
  priority: SHOULD
  type: behavioral
  description: SharePoint is registered as a source type in the Provisa data connector registry and can
    be configured as a queryable data source via the Apache Calcite sharepoint connector.
  use_case: Users can register SharePoint lists as tables in their Provisa domain, enabling SQL queries
    and GraphQL access to SharePoint data.
  code:
  - provisa/core/models.py
  tests:
  - provisa-ui/e2e/sharepoint-connector.spec.ts
  - tests/unit/test_sharepoint_connector.py
  since: 2026-06
  scenario: 'Given a user creates a new source

    When they select type "sharepoint"

    Then the source is created and can be queried via Trino using the sharepoint connector

    '
  e2e: true
  e2e_reason: End-to-end verification requires UI source creation, schema enumeration, and table registration
    through the full GraphQL stack.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
- id: REQ-727
  status: complete
  group: 4. Source Connectors
  category: SharePoint Connector
  priority: SHOULD
  type: behavioral
  description: 'SharePoint connector supports two authentication methods: CLIENT_CREDENTIALS (default)
    and certificate-based authentication via PFX certificate.'
  use_case: Different organizational security policies require different auth methods; CLIENT_CREDENTIALS
    suits service accounts while certificates support PKI-based auth.
  code:
  - provisa/core/catalog.py
  tests:
  - provisa-ui/e2e/sharepoint-connector.spec.ts
  - tests/unit/test_sharepoint_connector.py
  since: 2026-06
  scenario: 'Given a SharePoint source with auth-type CLIENT_CREDENTIALS

    When queries are executed

    Then Provisa sends client-id and client-secret to the Calcite connector

    And when auth-type is CERTIFICATE

    Then Provisa sends certificate-path and certificate-password instead

    '
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
- id: REQ-728
  status: complete
  group: 4. Source Connectors
  category: SharePoint Connector
  priority: SHOULD
  type: behavioral
  description: SharePoint connector builds connection properties from source base_url/host (site-url),
    username (client-id), password (client-secret), database field (tenant-id), and optional mapping fields
    (certificate_path, certificate_password, auth_type).
  use_case: Consolidates SharePoint auth and connection configuration into the standard Provisa Source
    model, allowing centralized secret management.
  code:
  - provisa/core/catalog.py
  tests:
  - provisa-ui/e2e/sharepoint-connector.spec.ts
  - tests/unit/test_sharepoint_connector.py
  since: 2026-06
  scenario: 'Given a SharePoint source with base_url="https://kenstott.sharepoint.com",

    username="client-id-value", password="secret", database="tenant-uuid"

    When catalog properties are built

    Then props contains site-url, auth-type, client-id, client-secret, tenant-id

    And certificate_path/certificate_password are included when present in mapping

    '
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
- id: REQ-729
  status: complete
  group: 4. Source Connectors
  category: SharePoint Connector
  priority: MUST
  type: constraint
  description: Secret values in SharePoint connector mapping (auth_type, certificate_path, certificate_password)
    are resolved via the secrets engine before being passed to the Calcite connector.
  use_case: Prevents plaintext credentials from being hardcoded in configuration, enabling vault/environment
    variable secret injection.
  code:
  - provisa/core/catalog.py
  tests:
  - provisa-ui/e2e/sharepoint-connector.spec.ts
  - tests/unit/test_sharepoint_connector.py
  since: 2026-06
  scenario: 'Given mapping contains certificate_path="${env:CERT_PATH}"

    When catalog properties are built

    Then certificate-path in props contains the resolved value from the env var

    '
- id: REQ-730
  status: complete
  group: 4. Source Connectors
  category: SharePoint Connector
  priority: SHOULD
  type: constraint
  description: SharePoint connector always enables case-insensitive-name-matching when building connection
    properties to the Calcite connector.
  use_case: SharePoint list names are case-insensitive; enabling this flag ensures consistent behavior
    across queries regardless of case variation in table/column references.
  code:
  - provisa/core/catalog.py
  since: 2026-06
  scenario: 'Given a SharePoint source with a list named "Calendar"

    When queries use "calendar" (lowercase) or "CALENDAR" (uppercase)

    Then both resolve to the same list due to case-insensitive matching

    '
  tests:
  - tests/unit/test_sharepoint_connector.py
- id: REQ-731
  status: complete
  group: 4. Source Connectors
  category: SharePoint Connector
  priority: SHOULD
  type: behavioral
  description: SharePoint lists are enumerated as schemas and exposed as queryable tables via Trino, allowing
    users to discover available lists and register them as Provisa tables.
  use_case: Users can browse SharePoint lists through the Provisa UI without manual configuration, improving
    UX for data source discovery.
  code:
  - provisa/core/catalog.py
  tests:
  - provisa-ui/e2e/sharepoint-connector.spec.ts
  - tests/unit/test_sharepoint_connector.py
  since: 2026-06
  scenario: 'Given a SharePoint source is added in the Provisa UI

    When a user navigates to add a table and selects this source

    Then available SharePoint lists (e.g., "calendar", "events") appear in the table dropdown

    '
  e2e: true
  e2e_reason: 'Requires full UI workflow: source creation, schema enumeration via GraphQL, and list discovery
    through the table registration form.'
  integration_test: deferred
  integration_test_reason: 'Behavior verifiable only end-to-end with real SharePoint connector instance
    and schema enumeration through the full Trino/GraphQL stack. [deferred: requires external infrastructure
    not in CI stack]'
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
- id: REQ-732
  status: complete
  group: 4. Source Connectors
  category: SharePoint Connector
  priority: SHOULD
  type: behavioral
  description: SharePoint tables can be registered in Provisa with known column definitions when the Calcite
    connector does not expose information_schema.columns.
  use_case: Allows users to manually define columns obtained from the Microsoft Graph API, bypassing the
    connector limitation and enabling table registration and queries.
  code:
  - provisa/core/catalog.py
  tests:
  - provisa-ui/e2e/sharepoint-connector.spec.ts
  - tests/unit/test_sharepoint_connector.py
  since: 2026-06
  scenario: 'Given the Calcite sharepoint connector does not expose information_schema.columns

    When a user registers a table via GraphQL registerTable mutation with columns=[{name, visibleTo, writableBy}]

    Then the table is created with the supplied column definitions

    '
  e2e: true
  e2e_reason: Verification requires the full GraphQL mutation stack and table registration workflow to
    confirm columns are persisted and accessible.
  integration_test: deferred
  integration_test_reason: 'Behavior verifiable only end-to-end through the GraphQL registerTable mutation
    stack with table persistence and column definition storage. [deferred: requires external infrastructure
    not in CI stack]'
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
- id: REQ-733
  status: complete
  group: 3. Source Registration & Data Modeling
  category: Aggregate Configuration
  priority: SHOULD
  type: ui
  description: Table edit form exposes `enable_aggregates` and `enable_group_by` as checkboxes in the
    admin UI. Stewards toggle these per-table settings to enable or disable aggregate and group-by root
    fields for the schema.
  use_case: Admin UI checkboxes give stewards an intuitive way to manage aggregate/group-by settings without
    manually editing YAML configuration files.
  code:
  - provisa/api/admin/types.py
  - provisa/api/admin/db_queries.py
  - provisa-ui/src
  tests:
  - provisa-ui/e2e/tables-enable-aggregates.spec.ts
  since: 2026-06
  scenario: 'Given a table with enable_aggregates=true and enable_group_by=true

    When the steward opens the table edit form in the admin UI

    Then both "Enable Aggregates" and "Enable Group By" checkboxes are checked

    '
- id: REQ-734
  status: complete
  group: 3. Source Registration & Data Modeling
  category: Aggregate Configuration
  priority: SHOULD
  type: structural
  description: Table configuration persists `enable_aggregates` and `enable_group_by` boolean columns
    in the `registered_tables` database table. Both columns default to `false`. Values are readable via
    the table edit API endpoint and reflect the current configuration state.
  use_case: Persistent storage of aggregate/group-by settings ensures steward configuration choices survive
    across deployments and are accessible via admin APIs.
  code:
  - provisa/api/admin/db_queries.py
  - provisa/core/models.py
  tests:
  - provisa-ui/e2e/tables-enable-aggregates.spec.ts
  - tests/unit/test_aggregate_config.py
  since: 2026-06
  scenario: 'Given a table with enable_aggregates=true persisted to the database

    When the admin API fetches table configuration

    Then the returned table object includes enable_aggregates=true and enable_group_by=false

    '
- id: REQ-740
  status: complete
  group: 1. Access Governance & Security
  category: Security
  priority: MUST
  type: behavioral
  description: Masking SELECT expressions only; predicates that remain in the query (JOIN ON, RLS-injected
    filters) use physical unmasked columns unchanged — user-written WHERE/HAVING references to masked
    columns never reach this stage, rejected at validation time by V005 (REQ-531). ORDER BY, LIMIT, GROUP
    BY, and all other clauses likewise remain unchanged, and masking returns a new immutable CompiledQuery,
    never mutating the input (absorbs REQ-744). Masking injection operates on the SELECT projection list
    exclusively, preserving query filtering and join semantics.
  use_case: Separating masked SELECT projections from unmasked predicates ensures query filters remain
    executable while users see masked values.
  code:
  - provisa/compiler/mask_inject.py
  - provisa/compiler/stage2.py
  tests:
  - tests/unit/test_masking_edge_cases.py
  - tests/e2e/test_masking.py
  - tests/integration/test_security_integration_extra.py
  since: 2026-06
  scenario: 'Given a masked column also referenced in WHERE or JOIN ON

    When masking is injected

    Then SELECT projects the masked expression; WHERE and JOIN ON reference the physical unmasked column

    '
  integration_test: required
  integration_test_reason: Separating masked SELECT from unmasked predicates requires SQL AST rewriting
    and execution to verify filter correctness.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - compliance
  - data-engineer
  competitive_position:
    status: ahead
    rationale: Applying masking only to SELECT projection while preserving unmasked column references
      in WHERE/JOIN predicates is a deliberate security design that prevents leakage via predicate manipulation
      while keeping queries functional.
- id: REQ-741
  status: complete
  group: 1. Access Governance & Security
  category: Security
  priority: MUST
  type: behavioral
  description: Column masking output uses ANSI SQL dialects (REGEXP_REPLACE, DATE_TRUNC, SQL literals)
    independent of source type. Dialect-specific transpilation via SQLGlot occurs downstream, after masking
    injection.
  use_case: Dialect-agnostic masking generation ensures consistent governance behavior across heterogeneous
    federated sources (PostgreSQL, MySQL, Snowflake, BigQuery).
  code:
  - provisa/security/masking.py
  - provisa/compiler/mask_inject.py
  tests:
  - tests/unit/test_masking_edge_cases.py
  - tests/e2e/test_masking.py
  since: 2026-06
  scenario: 'Given a masked column in queries against different source types

    When build_mask_expression generates the mask

    Then output is ANSI REGEXP_REPLACE/DATE_TRUNC regardless of source dialect

    '
  integration_test: not_required
  integration_test_reason: Dialect-agnostic masking output is pure SQL generation logic verifiable via
    unit tests.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - data-engineer
  competitive_position:
    status: neutral
    rationale: ''
- id: REQ-742
  status: complete
  group: 1. Access Governance & Security
  category: Security
  priority: MUST
  type: behavioral
  description: Masking respects column type constraints — NULL masks allowed only on nullable columns;
    regex and truncate masks validated for applicable types (regex on text, truncate on temporal). Validation
    enforced at config load time.
  use_case: Type-aware masking validation at config time prevents runtime errors and ensures masks produce
    valid SQL.
  code:
  - provisa/security/masking.py
  tests:
  - tests/unit/test_masking_edge_cases.py
  - tests/e2e/test_masking.py
  since: 2026-06
  scenario: 'Given a masking rule configured with an incompatible type

    When config is loaded

    Then validation rejects the rule (e.g., regex on integer, truncate on varchar, NULL on NOT NULL)

    '
  integration_test: not_required
  integration_test_reason: Type validation is pure logic verifiable via unit tests.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - compliance
  - data-engineer
  competitive_position:
    status: neutral
    rationale: ''
- id: REQ-743
  status: complete
  group: 1. Access Governance & Security
  category: Security
  priority: MUST
  type: behavioral
  description: Masking constant expressions must emit syntactically valid SQL for their type — numeric
    literals without quotes, string literals with single quotes (escaped apostrophes doubled), NULL keyword
    (not quoted), boolean TRUE/FALSE keywords (not Python string literals). Special constants MAX/MIN
    resolve to integer subtype bounds (tinyint=127, smallint=32767, integer=2147483647, bigint=9223372036854775807).
  use_case: Correct literal syntax ensures masked values are valid SQL across all dialects without requiring
    downstream escaping.
  code:
  - provisa/security/masking.py
  tests:
  - tests/unit/test_masking_edge_cases.py
  - tests/e2e/test_masking.py
  since: 2026-06
  scenario: 'Given various constant mask values (null, boolean, numeric, string with apostrophe)

    When build_mask_expression generates the SQL literal

    Then output is syntactically valid (NULL keyword, TRUE/FALSE, numeric unquoted, strings single-quoted
    with escaped apostrophes)

    '
  integration_test: not_required
  integration_test_reason: Literal SQL generation is pure string formatting verifiable via unit tests.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - data-engineer
  competitive_position:
    status: neutral
    rationale: ''
- id: REQ-744
  status: complete
  superseded_by: REQ-740
  group: 1. Access Governance & Security
  category: Security
  priority: MUST
  type: behavioral
  description: Masking preserves query structure — ORDER BY, LIMIT, GROUP BY, and other clauses remain
    unchanged; only SELECT projection is rewritten. Masking returns a new CompiledQuery object (immutable
    transformation), never mutating the input.
  use_case: Immutable masking injection and preservation of query structure ensure correct sorting, pagination,
    and aggregation behavior.
  code:
  - provisa/compiler/mask_inject.py
  tests:
  - tests/unit/test_masking_edge_cases.py
  - tests/e2e/test_masking.py
  - tests/integration/test_security_integration_extra.py
  since: 2026-06
  scenario: 'Given a query with ORDER BY, LIMIT, GROUP BY, or other clauses

    When masking is injected

    Then the clauses remain unchanged; result is a new object, input is unchanged

    '
  integration_test: required
  integration_test_reason: Immutability and clause preservation require execution to verify query semantics
    remain correct.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - data-engineer
  competitive_position:
    status: neutral
    rationale: ''
- id: REQ-745
  status: complete
  group: 1. Access Governance & Security
  category: Security
  priority: MUST
  type: behavioral
  description: Role-based masking — different roles can have different masks for the same column; admin
    roles with no masking rules see raw values; other roles see role-specific masks (regex, constant,
    or truncate). Selection determined by (table_id, role) key in masking rules dictionary.
  use_case: Role-specific masking lets different users see different representations of the same data
    based on their role permissions.
  code:
  - provisa/compiler/mask_inject.py
  - provisa/compiler/stage2.py
  tests:
  - tests/unit/test_masking_edge_cases.py
  - tests/e2e/test_masking.py
  - tests/integration/test_security_integration_extra.py
  since: 2026-06
  scenario: 'Given the same column with different masking rules per role

    When inject_masking is called for two different roles

    Then admin sees raw values; analyst sees regex mask; masked_viewer sees constant mask

    '
  integration_test: required
  integration_test_reason: Role-based masking behavior requires real role context and query execution
    to verify mask selection per role.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - compliance
  - data-engineer
  competitive_position:
    status: parity
    rationale: Role-based column masking is a standard feature in modern governance platforms (Hasura,
      Denodo, Trino).
- id: REQ-746
  status: complete
  group: 1. Access Governance & Security
  category: Security
  priority: MUST
  type: behavioral
  description: Capability enforcement via check_capability and has_capability functions — capabilities
    are independently assigned per role (query_development, source_registration, table_registration, relationship_definition,
    security_configuration, query_authorization, ignore_relationships). Admin capability grants all permissions.
    InsufficientRightsError raised on missing capability.
  use_case: Fine-grained, independently verifiable capabilities prevent privilege escalation.
  code:
  - provisa/security/rights.py
  tests:
  - tests/unit/test_rights.py
  since: 2026-06
  scenario: 'Given a role with a specific capability (e.g., query_development)

    When check_capability is called for that capability

    Then no exception is raised; for missing capability, InsufficientRightsError is raised

    '
  integration_test: not_required
  integration_test_reason: Capability enforcement is pure function logic verifiable via unit tests.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - compliance
  - data-engineer
  competitive_position:
    status: ahead
    rationale: Seven independently assigned capabilities (source registration, table registration, relationship
      definition, security configuration, query development, query authorization, ignore-relationships)
      plus universal admin grant is more granular than role-based access control in competitors.
- id: REQ-747
  status: complete
  group: 1. Access Governance & Security
  category: Security
  priority: MUST
  type: behavioral
  description: SQL validator bypass for remote same-source relationship pairs — when bypass_uncovered_relationships=True
    and both tables share the same remote source (graphql_remote or grpc_remote from same source_id),
    uncovered joins pass V002 validation. Cross-source joins (local↔remote or different remote sources)
    always require a covered_pair registration. Registered pairs always require correct column matches
    regardless of bypass flag.
  use_case: Remote relationship bypass allows federated GraphQL/gRPC sources to define their own relationship
    model without Provisa relationship registration.
  code:
  - provisa/compiler/sql_validator.py
  tests:
  - tests/unit/test_sql_validator_bypass.py
  since: 2026-06
  scenario: 'Given two remote tables from the same source_id with bypass_uncovered_relationships=True

    When SQL validator checks the join

    Then V002 violation is not raised; cross-source joins still require coverage

    '
  integration_test: not_required
  integration_test_reason: SQL validation rules are logic-based constraints verifiable via unit tests.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - data-engineer
  competitive_position:
    status: neutral
    rationale: ''
- id: REQ-748
  status: complete
  group: 1. Access Governance & Security
  category: Security
  priority: MUST
  type: behavioral
  description: Inverse relationship collision fix — joins producing inverse relationships (e.g., pets←assignments,
    assignments→pets) must emit distinct rel_types based on target label, not just column pairs. Keyed
    by (source_label, target_label, join_columns) to ensure reverse joins resolve to correct relationship
    types.
  use_case: Correct inverse relationship resolution prevents relationship type confusion in bidirectional
    federation scenarios.
  code:
  - provisa/cypher/sql_to_cypher.py
  tests:
  - tests/unit/test_sql_validator_bypass.py
  - tests/integration/test_security_integration_extra.py
  since: 2026-06
  scenario: 'Given two tables with inverse relationships sharing the same column names

    When semantic_sql_to_cypher converts joins in both directions

    Then forward join emits correct rel_type; inverse join emits distinct rel_type

    '
  integration_test: required
  integration_test_reason: Inverse relationship collision fix requires full Cypher generation and semantic
    model verification.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - data-engineer
  competitive_position:
    status: neutral
    rationale: ''
- id: REQ-749
  status: complete
  group: 1. Access Governance & Security
  category: Security
  priority: MUST
  type: behavioral
  description: Domain policy tri-state mode (legacy/single-domain/namespaced) — legacy mode (use_domains
    absent) stores declared domain_id verbatim; single-domain mode (use_domains=False) coerces all tables
    to default_domain; namespaced mode (use_domains=True) stores domain_id as declared. Reload validation
    sweep rejects pre-existing foreign domains when switching to single-domain mode.
  use_case: Domain policy modes enable multi-tenancy architectures without breaking legacy single-domain
    deployments.
  code:
  - provisa/core/domain_policy.py
  - provisa/core/config_loader.py
  tests:
  - tests/integration/test_domain_policy_integration.py
  - tests/unit/test_domain_policy.py
  since: 2026-06
  scenario: 'Given a config with use_domains mode specified

    When load_config_from_yaml processes the config

    Then domain_id is stored according to the tri-state mode (legacy/single/namespaced) and reload validates
    existing domains

    '
  integration_test: required
  integration_test_reason: Domain policy modes and validation sweep require real database table operations
    and config load/reload verification.
  e2e: false
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - ops
  - data-engineer
  competitive_position:
    status: neutral
    rationale: ''
- id: REQ-750
  status: complete
  group: 5. Query Languages, Compilation & Operations
  category: Cypher Query Frontend (Phase AU)
  priority: MUST
  type: behavioral
  description: Cypher graph variables (nodes, edges, paths) returned in the RETURN clause are serialized
    as JSON objects with canonical shape. Nodes include `id`, `label`, `tableLabel`, and `properties`.
    Edges include `identity`, `start`, `end`, `type`, `properties`, `startNode`, and `endNode`. Paths
    include `nodes`, `edges`, and `length`/`hops`.
  use_case: Consistent JSON serialization of graph objects enables client libraries to deserialize typed
    node/edge/path representations.
  code:
  - provisa/cypher/assembler.py
  - provisa/cypher/graph_rewriter.py
  tests:
  - tests/unit/test_cypher_assembler.py
  - tests/unit/test_cypher_graph_rewriter.py
  - tests/integration/test_cypher_integration_extra.py
  - provisa-ui/e2e/cypher-api.spec.ts
  since: 2026-06
  scenario: 'Given a Cypher query RETURN n, r, p where n is a node, r is an edge, p is a path

    When the Cypher router executes the query

    Then the response includes JSON objects for each graph variable with the canonical keys

    '
  integration_test: required
  integration_test_reason: Graph object serialization requires HTTP transport and real Cypher execution
    to verify JSON shape correctness.
  e2e: true
  e2e_reason: JSON serialization is only verifiable through a complete HTTP round-trip from query submission
    to response parsing.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - app-developer
  - data-engineer
  competitive_position:
    status: parity
    rationale: Neo4j driver libraries provide graph object serialization; Provisa's JSON serialization
      matches the Neo4j wire protocol format for compatibility.
- id: REQ-751
  status: complete
  group: 5. Query Languages, Compilation & Operations
  category: Cypher Query Frontend (Phase AU)
  priority: MUST
  type: behavioral
  description: Variable-length relationship patterns `[*1..n]` are compiled to recursive CTEs in Trino
    SQL. The CTE enforces max-hop bounds and returns edge sequences as JSON arrays. Multiple edge hops
    are preserved in the array for flat-join paths; recursive paths project NULL (intermediate edges not
    tracked in CTE).
  use_case: Variable-length traversals enable clients to explore multi-hop relationships without writing
    nested joins.
  code:
  - provisa/cypher/translator.py
  tests:
  - tests/unit/test_cypher_translator_paths.py
  - provisa-ui/e2e/cypher-variable-length-path.spec.ts
  - tests/integration/test_cypher_integration_extra.py
  since: 2026-06
  scenario: 'Given a Cypher query with [*1..5] pattern between two node types

    When the translator processes it

    Then it emits a WITH RECURSIVE CTE with hop-count guards and JSON_ARRAY edges

    '
  integration_test: required
  integration_test_reason: Recursive CTE compilation requires Trino execution to verify hop ordering and
    array structure correctness.
  e2e: true
  e2e_reason: Variable-length path execution is only verifiable through HTTP submission to a live Cypher
    endpoint with real data.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - app-developer
  - data-engineer
  competitive_position:
    status: ahead
    rationale: Cypher variable-length paths over federated relational sources with recursive CTE compilation
      is not available in competitors (Hasura, Denodo) which lack Cypher support.
- id: REQ-752
  status: complete
  group: 5. Query Languages, Compilation & Operations
  category: Cypher Query Frontend (Phase AU)
  priority: SHOULD
  type: behavioral
  description: Intermediate node property access in multi-hop MATCH patterns (e.g., `(p:Person)-[:WORKS_AT]->(c:Company)-[:HAS_DEPT]->(d:Department)`)
    resolves all three table aliases and preserves property references in WHERE and RETURN clauses. No
    aliasing conflicts occur.
  use_case: Complex traversals require filtering and returning properties from intermediate nodes without
    manual projection.
  code:
  - provisa/cypher/translator.py
  tests:
  - tests/unit/test_cypher_translator_exprs.py
  - tests/integration/test_cypher_integration_extra.py
  since: 2026-06
  scenario: 'Given a Cypher query with 3+ node variables in a path

    When the translator processes it

    Then all node aliases are available in WHERE and RETURN, and intermediate property access resolves
    correctly

    '
  integration_test: required
  integration_test_reason: Multi-hop alias resolution requires SQL generation and Trino execution to verify
    join correctness.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - data-engineer
  - app-developer
  competitive_position:
    status: neutral
    rationale: Multi-hop traversals are standard in graph databases; Provisa's translation to federated
      SQL joins is the unique aspect.
- id: REQ-753
  status: complete
  group: 5. Query Languages, Compilation & Operations
  category: Cypher Query Frontend (Phase AU)
  priority: SHOULD
  type: behavioral
  description: Path object RETURN (e.g., `RETURN p`) emits a JSON_OBJECT with `nodes` (array of node objects),
    `edges` (array of edge objects), `start`/`end` (node identities), and `length` (hop count). For recursive
    CTE paths, `hops` field counts intermediate hops.
  use_case: Returning entire paths as objects enables clients to navigate graph traversals without unpacking
    individual nodes/edges.
  code:
  - provisa/cypher/translator.py
  - provisa/cypher/graph_rewriter.py
  tests:
  - tests/unit/test_cypher_translator_exprs.py
  - provisa-ui/e2e/cypher-variable-length-path.spec.ts
  - tests/integration/test_cypher_integration_extra.py
  since: 2026-06
  scenario: 'Given a Cypher query MATCH p = (...) RETURN p

    When the translator processes it

    Then it emits JSON_OBJECT with nodes, edges, and length fields

    '
  integration_test: required
  integration_test_reason: Path object JSON structure requires SQL generation and Trino execution to verify
    output format.
  e2e: true
  e2e_reason: Path object serialization is only verifiable through HTTP response inspection.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - app-developer
  - data-engineer
  competitive_position:
    status: parity
    rationale: Path objects are standard in graph databases; Provisa matches Neo4j wire format.
- id: REQ-754
  status: complete
  superseded_by: REQ-573
  group: 5. Query Languages, Compilation & Operations
  category: Cypher Query Frontend (Phase AU)
  priority: SHOULD
  type: behavioral
  description: Correlated CALL subqueries (e.g., `CALL { WITH x MATCH (x)-[:REL]->(y) RETURN y }`) translate
    to CROSS JOIN LATERAL subqueries in Trino, preserving the outer variable binding.
  use_case: Correlated subqueries enable filtering inner results based on outer row context without materializing
    cartesian joins.
  code:
  - provisa/cypher/translator.py
  tests:
  - tests/unit/test_cypher_translator_exprs.py
  - tests/integration/test_cypher_integration_extra.py
  since: 2026-06
  scenario: 'Given a Cypher query with CALL { WITH x ... } correlated subquery

    When the translator processes it

    Then it emits CROSS JOIN LATERAL with the outer variable bound in the join condition

    '
  integration_test: required
  integration_test_reason: Lateral join compilation requires Trino SQL generation and execution to verify
    binding correctness.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - data-engineer
  - app-developer
  competitive_position:
    status: neutral
    rationale: Lateral joins are advanced SQL; the unique aspect is Cypher syntax support over federated
      sources.
- id: REQ-755
  status: complete
  group: 5. Query Languages, Compilation & Operations
  category: Cypher Query Frontend (Phase AU)
  priority: SHOULD
  type: behavioral
  description: Node label alternation (e.g., `(n:Label1|Label2)`) translates to UNION ALL branches in
    SQL, one per label candidate. Each branch includes only the relevant node type and relationships that
    originate from it.
  use_case: Label alternation enables flexible node matching without explicit OR conditions.
  code:
  - provisa/cypher/translator.py
  tests:
  - tests/unit/test_cypher_translator_exprs.py
  - tests/integration/test_cypher_integration_extra.py
  since: 2026-06
  scenario: 'Given a Cypher query MATCH (n:TypeA|TypeB) RETURN n

    When the translator processes it

    Then it emits UNION ALL with one branch per type, each selecting from the appropriate table

    '
  integration_test: required
  integration_test_reason: Label alternation requires SQL UNION generation and Trino execution to verify
    result merging.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - data-engineer
  - app-developer
  competitive_position:
    status: neutral
    rationale: Label alternation via UNION is a standard SQL pattern; Cypher syntax support is the unique
      aspect.
- id: REQ-756
  status: complete
  group: 5. Query Languages, Compilation & Operations
  category: Cypher Query Frontend (Phase AU)
  priority: SHOULD
  type: behavioral
  description: Subquery predicates — EXISTS { ... }, COUNT { ... }, COLLECT { ... } — translate to correlated
    SQL subqueries. EXISTS becomes EXISTS (SELECT ...), COUNT becomes (SELECT count(...)), COLLECT becomes
    ARRAY(...).
  use_case: Subquery predicates enable complex filtering based on related-node aggregations without intermediate
    MATCH clauses.
  code:
  - provisa/cypher/translator.py
  tests:
  - tests/unit/test_cypher_translator_exprs.py
  - tests/integration/test_cypher_integration_extra.py
  since: 2026-06
  scenario: 'Given a Cypher query MATCH (n) WHERE EXISTS { ... } RETURN n

    When the translator processes it

    Then it emits a correlated EXISTS subquery in the WHERE clause

    '
  integration_test: required
  integration_test_reason: Subquery predicates require Trino SQL execution to verify result correctness.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - data-engineer
  - app-developer
  competitive_position:
    status: neutral
    rationale: Cypher subquery predicates are standard graph query features; translation to federated
      SQL is the unique aspect.
- id: REQ-757
  status: complete
  group: 5. Query Languages, Compilation & Operations
  category: Cypher Query Frontend (Phase AU)
  priority: SHOULD
  type: behavioral
  description: 'Map projections — `n { .prop1, .prop2 }`, `n { .* }`, `n { key: expr }` — translate to
    Trino MAP(...) function calls with ARRAY keys and ARRAY values. Star projections expand schema properties;
    custom keys are added as named entries.'
  use_case: Map projections enable client-friendly reshaping of returned objects without manual column
    aliasing.
  code:
  - provisa/cypher/translator.py
  tests:
  - tests/unit/test_cypher_translator_exprs.py
  - tests/integration/test_cypher_integration_extra.py
  since: 2026-06
  scenario: 'Given a Cypher query MATCH (n:Person) RETURN n { .name, .age }

    When the translator processes it

    Then it emits MAP(ARRAY[''name'',''age''], ARRAY[n."name",n."age"])

    '
  integration_test: required
  integration_test_reason: Map projection compilation requires Trino execution to verify MAP structure
    and value extraction.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - app-developer
  - data-engineer
  competitive_position:
    status: neutral
    rationale: Map projections are a Neo4j feature; translation to Trino MAP is the unique aspect.
- id: REQ-758
  status: complete
  superseded_by: REQ-575
  group: 5. Query Languages, Compilation & Operations
  category: Cypher Query Frontend (Phase AU)
  priority: SHOULD
  type: behavioral
  description: Bidirectional edge traversal (e.g., `(a)-[]-(b)` without direction marker) expands to UNION
    ALL when multiple relationship directions exist in the schema. Each branch covers one direction; single-direction
    schemas skip the UNION.
  use_case: Bidirectional traversal enables undirected graph exploration without explicit forward/backward
    branches.
  code:
  - provisa/cypher/translator.py
  tests:
  - tests/unit/test_cypher_translator_exprs.py
  - tests/integration/test_cypher_integration_extra.py
  since: 2026-06
  scenario: 'Given a Cypher query MATCH (a:Person)-[]-(b:Company) where forward and backward relationships
    exist

    When the translator processes it

    Then it emits UNION ALL with one branch per direction

    '
  integration_test: required
  integration_test_reason: Bidirectional expansion requires SQL generation and Trino execution to verify
    UNION merging.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - data-engineer
  - app-developer
  competitive_position:
    status: neutral
    rationale: Bidirectional traversal via UNION ALL is standard SQL; Cypher syntax support over federated
      sources is the unique aspect.
- id: REQ-759
  status: complete
  group: 5. Query Languages, Compilation & Operations
  category: Cypher Query Frontend (Phase AU)
  priority: SHOULD
  type: behavioral
  description: Backward relationship traversal (e.g., `(c:Company)<-[:WORKS_AT]-(p:Person)`) inverts the
    join condition from the forward relationship definition. Join columns are swapped; ON condition evaluates
    to tgt.id = src.join_column.
  use_case: Backward traversal enables reverse-direction path queries without schema bidirectionality.
  code:
  - provisa/cypher/translator.py
  tests:
  - tests/unit/test_cypher_translator_paths.py
  - tests/integration/test_cypher_integration_extra.py
  since: 2026-06
  scenario: 'Given a Cypher query MATCH (c:Company)<-[:WORKS_AT]-(p:Person)

    When the translator processes it

    Then it emits a JOIN with swapped ON condition: p.company_id = c.id

    '
  integration_test: required
  integration_test_reason: Backward traversal requires SQL generation and Trino execution to verify inverted
    join correctness.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - data-engineer
  - app-developer
  competitive_position:
    status: neutral
    rationale: Backward traversal is standard in graph databases; translation to inverted SQL joins is
      the unique aspect.
- id: REQ-760
  status: complete
  group: 5. Query Languages, Compilation & Operations
  category: Cypher Query Frontend (Phase AU)
  priority: SHOULD
  type: behavioral
  description: String, list, and numeric functions (LEFT, RIGHT, TRIM, SIZE, COUNT DISTINCT, REDUCE) translate
    directly to Trino equivalents or adapters. REDUCE syntax is normalized to Trino lambda form. Legacy
    `{param}` syntax is normalized to `$param` before parsing.
  use_case: Function support enables rich query expressions without learning Trino-specific syntax.
  code:
  - provisa/cypher/translator.py
  tests:
  - tests/unit/test_cypher_translator_exprs.py
  - tests/integration/test_cypher_integration_extra.py
  since: 2026-06
  scenario: 'Given a Cypher query MATCH (n) RETURN left(n.name, 3), size(collect(n.age))

    When the translator processes it

    Then it emits Trino functions: left(...), cardinality(array_agg(...))

    '
  integration_test: required
  integration_test_reason: Function translation requires Trino execution to verify result correctness.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - app-developer
  - data-engineer
  competitive_position:
    status: neutral
    rationale: Function translation is standard compiler work; the unique aspect is coverage across Cypher-to-SQL
      bridge.
- id: REQ-761
  status: complete
  group: 5. Query Languages, Compilation & Operations
  category: Cypher Query Frontend (Phase AU)
  priority: SHOULD
  type: behavioral
  description: Implicit GROUP BY is inferred in the RETURN clause when non-aggregated columns are mixed
    with aggregate functions. All non-aggregated columns in RETURN are included in GROUP BY without explicit
    declaration.
  use_case: Implicit GROUP BY matches Neo4j Cypher semantics and reduces query verbosity.
  code:
  - provisa/cypher/translator.py
  tests:
  - tests/unit/test_cypher_translator_exprs.py
  - tests/integration/test_cypher_integration_extra.py
  since: 2026-06
  scenario: 'Given a Cypher query MATCH (n) RETURN n.type, count(*) AS cnt

    When the translator processes it

    Then it emits GROUP BY n.type inferred from non-aggregated columns

    '
  integration_test: required
  integration_test_reason: Implicit GROUP BY inference requires Trino execution to verify result grouping.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - app-developer
  - data-engineer
  competitive_position:
    status: parity
    rationale: Implicit GROUP BY is standard in graph databases; Provisa matches Neo4j Cypher semantics.
- id: REQ-762
  status: complete
  group: 5. Query Languages, Compilation & Operations
  category: Cypher Query Frontend (Phase AU)
  priority: SHOULD
  type: behavioral
  description: CASE expressions (both searched `CASE WHEN ... THEN ... ELSE ... END` and simple `CASE
    x WHEN ... THEN ... ELSE ... END`) translate directly to Trino CASE syntax. Multiple WHEN branches
    and optional ELSE are preserved.
  use_case: CASE expressions enable conditional logic in projections and filters without external control
    flow.
  code:
  - provisa/cypher/translator.py
  tests:
  - tests/unit/test_cypher_translator_paths.py
  - tests/integration/test_cypher_integration_extra.py
  since: 2026-06
  scenario: 'Given a Cypher query MATCH (n) RETURN CASE WHEN n.age > 18 THEN ''adult'' ELSE ''minor''
    END

    When the translator processes it

    Then it emits Trino CASE...WHEN...THEN...ELSE...END syntax

    '
  integration_test: required
  integration_test_reason: CASE expression translation requires Trino execution to verify result correctness.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - app-developer
  - data-engineer
  competitive_position:
    status: neutral
    rationale: CASE translation is standard compiler work; unique aspect is Cypher-to-SQL mapping.
- id: REQ-763
  status: complete
  group: 5. Query Languages, Compilation & Operations
  category: Cypher Query Frontend (Phase AU)
  priority: SHOULD
  type: behavioral
  description: UNWIND clauses (e.g., `UNWIND [1,2,3] AS x`) translate to Trino UNNEST with CROSS JOIN.
    Parameters in UNWIND (e.g., `$list`) are passed through and referenced as positional parameters in
    the SQL output.
  use_case: UNWIND enables iteration over lists without explicit array expansion in the application.
  code:
  - provisa/cypher/translator.py
  tests:
  - tests/unit/test_cypher_translator_paths.py
  - tests/integration/test_cypher_integration_extra.py
  since: 2026-06
  scenario: 'Given a Cypher query UNWIND [1, 2, 3] AS x RETURN x

    When the translator processes it

    Then it emits CROSS JOIN (SELECT ... FROM UNNEST(ARRAY[...]))

    '
  integration_test: required
  integration_test_reason: UNWIND translation requires Trino execution to verify UNNEST behavior.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - app-developer
  - data-engineer
  competitive_position:
    status: neutral
    rationale: UNWIND translation to UNNEST is standard; unique aspect is Cypher syntax support.
- id: REQ-764
  status: complete
  group: 5. Query Languages, Compilation & Operations
  category: Cypher Query Frontend (Phase AU)
  priority: SHOULD
  type: behavioral
  description: IN list predicates (e.g., `n.age IN [25, 30, 35]`) translate directly to SQL IN clauses
    with literal or parameter values.
  use_case: IN predicates enable efficient filtering on multi-value sets without repeated OR conditions.
  code:
  - provisa/cypher/translator.py
  tests:
  - tests/unit/test_cypher_translator_paths.py
  - tests/integration/test_cypher_integration_extra.py
  since: 2026-06
  scenario: 'Given a Cypher query MATCH (n) WHERE n.age IN [25, 30, 35] RETURN n.name

    When the translator processes it

    Then it emits SQL IN (25, 30, 35) in the WHERE clause

    '
  integration_test: required
  integration_test_reason: IN predicate translation requires Trino execution to verify filtering correctness.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - app-developer
  - data-engineer
  competitive_position:
    status: neutral
    rationale: IN predicate translation is standard SQL compiler work.
- id: REQ-765
  status: complete
  group: 5. Query Languages, Compilation & Operations
  category: Cypher Query Frontend (Phase AU)
  priority: SHOULD
  type: behavioral
  description: Pattern comprehensions (e.g., `[(p)-[:WORKS_AT]->(c:Company) | c.name]`) translate to ARRAY(...SELECT...)
    subqueries with the pattern matched inside and the pipe expression projected.
  use_case: Pattern comprehensions enable inline aggregation of related rows without explicit GROUP BY.
  code:
  - provisa/cypher/translator.py
  tests:
  - tests/unit/test_cypher_translator_paths.py
  - tests/integration/test_cypher_integration_extra.py
  since: 2026-06
  scenario: 'Given a Cypher query MATCH (p) RETURN [(p)-[:WORKS_AT]->(c:Company) | c.name]

    When the translator processes it

    Then it emits ARRAY(SELECT c."name" FROM ... WHERE ...)

    '
  integration_test: required
  integration_test_reason: Pattern comprehension translation requires Trino execution to verify ARRAY
    subquery behavior.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - app-developer
  - data-engineer
  competitive_position:
    status: neutral
    rationale: Pattern comprehensions are advanced Cypher features; translation to ARRAY subqueries is
      unique.
- id: REQ-766
  status: complete
  group: 5. Query Languages, Compilation & Operations
  category: Cypher Query Frontend (Phase AU)
  priority: SHOULD
  type: behavioral
  description: length(p) function on a path variable returns the hop count (number of edges). For recursive
    CTE paths, it returns the `hops` field value; for flat-join paths, it returns 1 or the explicit edge
    count.
  use_case: length(p) enables path-based filtering and ordering without explicit edge counting.
  code:
  - provisa/cypher/translator.py
  tests:
  - tests/unit/test_cypher_translator_paths.py
  - tests/integration/test_cypher_integration_extra.py
  since: 2026-06
  scenario: 'Given a Cypher query MATCH p = (...) RETURN length(p)

    When the translator processes it

    Then it extracts the `hops` field from the path object or returns 1 for single-hop paths

    '
  integration_test: required
  integration_test_reason: length(p) function requires SQL generation and Trino execution to verify hop
    counting.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - app-developer
  - data-engineer
  competitive_position:
    status: neutral
    rationale: length(p) is standard graph query functionality; translation to hop counting is the unique
      aspect.
- id: REQ-767
  status: complete
  group: 5. Query Languages, Compilation & Operations
  category: Cypher Query Frontend (Phase AU)
  priority: SHOULD
  type: behavioral
  description: Recursive CTE shortestPath and allShortestPaths functions enforce max-hop bounds at compile
    time. shortestPath injects LIMIT 1 and ORDER BY hops; allShortestPaths orders by hops without LIMIT.
  use_case: Shortest-path functions enable efficient shortest-path queries without client-side traversal
    algorithms.
  code:
  - provisa/cypher/translator.py
  tests:
  - tests/unit/test_cypher_translator_paths.py
  - tests/integration/test_cypher_integration_extra.py
  since: 2026-06
  scenario: 'Given a Cypher query MATCH p = shortestPath((a:Person)-[:KNOWS*..5]->(b:Person))

    When the translator processes it

    Then it emits a WITH RECURSIVE CTE with ORDER BY hops LIMIT 1

    '
  integration_test: required
  integration_test_reason: Shortest-path function compilation requires Trino execution to verify hop ordering
    and limiting.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - data-engineer
  - app-developer
  competitive_position:
    status: ahead
    rationale: Shortest-path functions over federated relational sources via recursive CTE compilation
      is not available in competitors lacking Cypher support.
- id: REQ-768
  status: complete
  group: 5. Query Languages, Compilation & Operations
  category: Cypher Query Frontend (Phase AU)
  priority: SHOULD
  type: behavioral
  description: Node graph variable deserialization from SQL result rows. JSON columns marked as NODE in
    graph_vars are parsed into Node objects with id, label, tableLabel, and properties extracted.
  use_case: Node deserialization enables type-safe access to node objects in the assembler stage before
    HTTP response serialization.
  code:
  - provisa/cypher/assembler.py
  tests:
  - tests/unit/test_cypher_assembler.py
  - tests/integration/test_cypher_integration_extra.py
  since: 2026-06
  scenario: 'Given a SQL result with a JSON column marked as NODE

    When the assembler processes the rows

    Then it deserializes the JSON into typed Node objects

    '
  integration_test: required
  integration_test_reason: Node deserialization requires actual SQL execution and JSON column handling
    to verify object construction.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - app-developer
  - data-engineer
  competitive_position:
    status: neutral
    rationale: Graph object deserialization is internal framework work; unique aspect is JSON-to-object
      bridge for Cypher results.
- id: REQ-769
  status: complete
  group: 5. Query Languages, Compilation & Operations
  category: Cypher Query Frontend (Phase AU)
  priority: SHOULD
  type: behavioral
  description: Edge graph variable deserialization from SQL result rows. JSON columns marked as EDGE are
    parsed into Edge objects with identity, type, start/end node references, and properties. Legacy (id/startNode/endNode)
    and Neo4j (identity/start/end) formats are both supported.
  use_case: Edge deserialization enables type-safe access to relationship objects with start/end node
    tracking.
  code:
  - provisa/cypher/assembler.py
  tests:
  - tests/unit/test_cypher_assembler.py
  - tests/integration/test_cypher_integration_extra.py
  since: 2026-06
  scenario: 'Given a SQL result with a JSON column marked as EDGE

    When the assembler processes the rows

    Then it deserializes the JSON into typed Edge objects with start/end node objects

    '
  integration_test: required
  integration_test_reason: Edge deserialization requires SQL execution and Neo4j format handling to verify
    object construction.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - app-developer
  - data-engineer
  competitive_position:
    status: neutral
    rationale: Edge deserialization is internal framework work; Neo4j format compatibility is the unique
      aspect.
- id: REQ-770
  status: complete
  group: 5. Query Languages, Compilation & Operations
  category: Cypher Query Frontend (Phase AU)
  priority: SHOULD
  type: behavioral
  description: Path graph variable deserialization from SQL result rows. Rows with _path_id and _depth
    columns are collapsed into single Path objects with nodes and edges arrays. Multiple paths with different
    _path_ids produce separate Path objects.
  use_case: Path deserialization enables client-friendly navigation of multi-hop graph results without
    manual array unpacking.
  code:
  - provisa/cypher/assembler.py
  tests:
  - tests/unit/test_cypher_assembler.py
  - tests/integration/test_cypher_integration_extra.py
  since: 2026-06
  scenario: 'Given SQL result rows with _path_id and _depth columns marking path hops

    When the assembler processes the rows

    Then rows with matching _path_id are collapsed into a single Path object

    '
  integration_test: required
  integration_test_reason: Path deserialization requires SQL execution with recursive CTE results to verify
    collapsing behavior.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - app-developer
  - data-engineer
  competitive_position:
    status: neutral
    rationale: Path deserialization is internal framework work; CTE integration is the unique aspect.
- id: REQ-771
  status: complete
  group: 5. Query Languages, Compilation & Operations
  category: Cypher Query Frontend (Phase AU)
  priority: SHOULD
  type: behavioral
  description: Variable-length relationship edge columns (e.g., `[c*..5]` variable) are deserialized as
    lists of Edge objects from JSON_ARRAY columns. NULL values from CTE null-projection are preserved
    as None. Empty lists result from zero-edge paths.
  use_case: Variable-length edge deserialization enables structured access to multi-hop relationship sequences.
  code:
  - provisa/cypher/assembler.py
  tests:
  - tests/unit/test_cypher_assembler.py
  - tests/integration/test_cypher_integration_extra.py
  since: 2026-06
  scenario: 'Given a SQL result with a JSON_ARRAY column containing edge objects from [*..n] pattern

    When the assembler processes the rows

    Then it deserializes the array into a list of Edge objects

    '
  integration_test: required
  integration_test_reason: Variable-length edge deserialization requires SQL execution to verify array
    parsing and null handling.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - app-developer
  - data-engineer
  competitive_position:
    status: neutral
    rationale: Variable-length edge deserialization is internal framework work.
- id: REQ-772
  status: complete
  superseded_by: REQ-349
  group: 5. Query Languages, Compilation & Operations
  category: Cypher Query Frontend (Phase AU)
  priority: SHOULD
  type: behavioral
  description: Graph rewriter applies JSON object wrapping to all graph variables in the SELECT clause.
    Scalar columns remain unwrapped. The wrapping ensures nodes/edges/paths are serializable to JSON.
  use_case: Graph rewriter preserves data integrity by ensuring all graph variables are JSON-compatible
    before HTTP serialization.
  code:
  - provisa/cypher/graph_rewriter.py
  tests:
  - tests/unit/test_cypher_graph_rewriter.py
  - tests/integration/test_cypher_integration_extra.py
  since: 2026-06
  scenario: 'Given an SQL query with both scalar and graph variable columns

    When the graph rewriter processes it

    Then scalar columns are left unchanged, graph variables are wrapped in JSON_OBJECT

    '
  integration_test: required
  integration_test_reason: Graph rewriting requires SQL generation and Trino execution to verify wrapping
    and data extraction.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - data-engineer
  - app-developer
  competitive_position:
    status: neutral
    rationale: Graph rewriting is internal framework work enabling Cypher support.
- id: REQ-773
  status: complete
  group: 5. Query Languages, Compilation & Operations
  category: Cypher Query Frontend (Phase AU)
  priority: SHOULD
  type: behavioral
  description: Domain-scoped node projections (e.g., `MATCH (n:PetStore) RETURN n`) include all properties
    from nodes in the domain in the JSON_OBJECT output. Multiple node types within a domain produce UNION
    ALL branches, each populating the full property set for that type.
  use_case: Domain-scoped queries enable exploration of all entities within a business domain without
    explicit type specification.
  code:
  - provisa/cypher/graph_rewriter.py
  - provisa/cypher/translator.py
  tests:
  - tests/unit/test_cypher_graph_rewriter.py
  - tests/integration/test_cypher_integration_extra.py
  since: 2026-06
  scenario: 'Given a Cypher query MATCH (n:DomainLabel) RETURN n where DomainLabel groups multiple node
    types

    When the translator processes it

    Then it emits UNION ALL with one branch per node type, each projecting all domain properties

    '
  integration_test: required
  integration_test_reason: Domain projection requires SQL generation and Trino execution to verify UNION
    merging and property coverage.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - data-engineer
  - app-developer
  competitive_position:
    status: neutral
    rationale: Domain-scoped exploration is a Provisa-specific feature leveraging label mappings.
- id: REQ-774
  status: complete
  group: 5. Query Languages, Compilation & Operations
  category: Cypher Query Frontend (Phase AU)
  priority: MAY
  type: behavioral
  description: Duplicate JSON key prevention. When a table's id_column differs from 'id' but the table
    has a column named 'id', the JSON_OBJECT projection emits only one 'id' key (the primary identity
    key, not the column).
  use_case: Duplicate key prevention ensures well-formed JSON output from federated tables with unconventional
    id column naming.
  code:
  - provisa/cypher/graph_rewriter.py
  tests:
  - tests/unit/test_cypher_graph_rewriter.py
  since: 2026-06
  scenario: 'Given a table with id_column=''inquiry_id'' but also a column named ''id''

    When the graph rewriter projects the node

    Then the JSON_OBJECT contains only one ''id'' key corresponding to the id_column

    '
  integration_test: required
  integration_test_reason: Duplicate key prevention requires SQL generation and Trino execution to verify
    JSON_OBJECT output.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - data-engineer
  - app-developer
  competitive_position:
    status: neutral
    rationale: Duplicate key prevention is a correctness detail for federated sources.
- id: REQ-775
  status: complete
  group: 5. Query Languages, Compilation & Operations
  category: Cypher Query Frontend (Phase AU)
  priority: MUST
  type: behavioral
  description: Anonymous all-rels path pattern `MATCH p=()-->()` (no node/edge labels or variables) generates
    a valid SQL subquery referencing all relationships without crashing. No empty table-prefix column
    references are emitted.
  use_case: Anonymous paths enable exploratory graph queries without schema knowledge.
  code:
  - provisa/cypher/translator.py
  tests:
  - tests/unit/test_cypher_translator_paths.py
  - tests/integration/test_cypher_integration_extra.py
  since: 2026-06
  scenario: 'Given a Cypher query MATCH p=()-->() RETURN p LIMIT 25

    When the translator processes it

    Then it emits a valid _all_rels subquery with JSON_OBJECT path serialization

    '
  integration_test: required
  integration_test_reason: Anonymous path translation requires SQL generation and Trino execution to verify
    _all_rels subquery validity.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - app-developer
  - data-engineer
  competitive_position:
    status: neutral
    rationale: Anonymous path support is a convenience feature for exploratory queries.
- id: REQ-776
  status: complete
  group: 5. Query Languages, Compilation & Operations
  category: Cypher Query Frontend (Phase AU)
  priority: MUST
  type: behavioral
  description: OPTIONAL MATCH chains (e.g., `OPTIONAL MATCH (a)-[r1]->(b) OPTIONAL MATCH (b)-[r2]->(c)`)
    translate to left-joined multi-hop paths. Each OPTIONAL MATCH becomes a separate LEFT JOIN in sequence,
    preserving null propagation for unmatched branches.
  use_case: OPTIONAL MATCH chains enable exploratory traversals where intermediate results may be absent
    without failing the entire query.
  code:
  - provisa/cypher/translator.py
  tests:
  - provisa-ui/e2e/cypher-breed-filter.spec.ts
  - tests/unit/test_cypher_frontend.py
  - tests/integration/test_cypher_integration_extra.py
  since: 2026-06
  scenario: 'Given a Cypher query with a chain of OPTIONAL MATCH clauses

    When the translator processes it

    Then it emits sequential LEFT JOINs with null-aware WHERE conditions

    '
  integration_test: required
  integration_test_reason: OPTIONAL MATCH translation requires Trino execution to verify left-join semantics
    and null handling.
  e2e: true
  e2e_reason: OPTIONAL MATCH behavior with federation errors is only verifiable through HTTP submission
    to a live Cypher endpoint.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - app-developer
  - data-engineer
  competitive_position:
    status: neutral
    rationale: OPTIONAL MATCH is standard graph query syntax; translation to federated LEFT JOIN is the
      unique aspect.
- id: REQ-777
  status: complete
  group: 5. Query Languages, Compilation & Operations
  category: Cypher Query Frontend (Phase AU)
  priority: MUST
  type: behavioral
  description: UNION ALL queries (e.g., `MATCH (n) RETURN ... UNION ALL MATCH (m) RETURN ...`) preserve
    distinct column aliases in both branches. Result column order matches the first UNION branch. Queries
    with property filters on both nodes and edges execute without FederationError.
  use_case: UNION ALL queries enable multi-entity result merging and cross-entity property filtering in
    a single query.
  code:
  - provisa/cypher/translator.py
  tests:
  - provisa-ui/e2e/cypher-union-all-email.spec.ts
  - tests/unit/test_cypher_frontend.py
  - tests/integration/test_cypher_integration_extra.py
  since: 2026-06
  scenario: 'Given a Cypher UNION ALL query with property filters on both node and edge properties

    When the translator processes it

    Then it emits a valid SQL UNION ALL with matching column aliases and unified WHERE conditions

    '
  integration_test: required
  integration_test_reason: UNION ALL translation requires Trino execution to verify column alignment and
    federation property filtering.
  e2e: true
  e2e_reason: UNION ALL queries with federation require HTTP submission to verify property resolution
    across entity types.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - app-developer
  - data-engineer
  competitive_position:
    status: neutral
    rationale: UNION ALL is standard SQL; Cypher syntax support over federated sources is the unique aspect.
- id: REQ-778
  status: complete
  group: 5. Query Languages, Compilation & Operations
  category: Cypher Query Frontend (Phase AU)
  priority: MUST
  type: behavioral
  description: Cypher /query/cypher endpoint returns a typed response with `columns` (array of column
    names), `rows` (array of result objects), `error` (null or error detail), and `type` (always "cypher").
    No row truncation or implicit result limiting occurs beyond query-specified LIMIT.
  use_case: Consistent response shape enables client libraries to deserialize typed Cypher query results.
  code:
  - provisa/api/rest/cypher_router.py
  - provisa/api/data/endpoint.py
  tests:
  - provisa-ui/e2e/cypher-breed-filter.spec.ts
  - provisa-ui/e2e/cypher-variable-length-path.spec.ts
  - provisa-ui/e2e/cypher-union-all-email.spec.ts
  - tests/unit/test_cypher_frontend.py
  - tests/integration/test_cypher_integration_extra.py
  since: 2026-06
  scenario: 'Given a Cypher query submitted to POST /data/cypher

    When the endpoint processes and executes it

    Then the response includes columns, rows, and error fields with the correct shape

    '
  integration_test: required
  integration_test_reason: Response shape verification requires HTTP transport and actual Cypher execution
    to inspect output structure.
  e2e: true
  e2e_reason: Response shape validation is only verifiable through HTTP round-trip with live Cypher endpoint.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - app-developer
  - data-engineer
  competitive_position:
    status: parity
    rationale: Consistent response shape is standard API design; Provisa's Cypher endpoint matches expected
      graph database patterns.
- id: REQ-779
  status: complete
  group: 10. UI & Admin Surfaces
  category: Graph Explorer
  priority: SHOULD
  type: ui
  description: Graph Explorer Favorites panel persists and displays user-saved Cypher queries with custom
    labels, stored in browser localStorage.
  use_case: Users need quick access to frequently-used Cypher queries without re-typing them.
  code:
  - provisa-ui/src/components/graph-explorer
  tests:
  - provisa-ui/e2e/graph-favorites.spec.ts
  since: 2026-06
  scenario: 'Given the Graph Explorer is open

    When favorites have been stored in localStorage as a JSON array with id, query, label, and ts fields

    And the user clicks the Favorites tab

    Then the favorites panel displays all stored favorites with their labels visible

    '
  e2e: true
  e2e_reason: Favorites display and localStorage persistence can only be verified through browser interaction
    and page reload.
  stakeholders:
  - app-developer
  - data-engineer
- id: REQ-780
  status: complete
  group: 10. UI & Admin Surfaces
  category: Graph Explorer
  priority: SHOULD
  type: ui
  description: 'Graph Explorer Favorites: hovering over a favorite item reveals action buttons (run, rename,
    delete) that were previously hidden.'
  use_case: Hover-reveal of action buttons keeps the favorites panel uncluttered while making operations
    discoverable.
  code:
  - provisa-ui/src/components/graph-explorer
  tests:
  - provisa-ui/e2e/graph-favorites.spec.ts
  since: 2026-06
  scenario: 'Given the Favorites panel is open with at least one favorite visible

    When the user hovers over a favorite item

    Then the run, rename, and delete action buttons become visible

    And when hover is removed, the buttons are hidden again

    '
  e2e: true
  e2e_reason: Hover state and button visibility are only testable through Playwright browser interaction.
  stakeholders:
  - app-developer
- id: REQ-781
  status: complete
  group: 10. UI & Admin Surfaces
  category: Graph Explorer
  priority: SHOULD
  type: behavioral
  description: 'Graph Explorer Favorites: clicking a favorite''s label loads the Cypher query into the
    editor for execution or modification.'
  use_case: Users can quickly load and run a saved query or modify it before execution.
  code:
  - provisa-ui/src/components/graph-explorer
  tests:
  - provisa-ui/e2e/graph-favorites.spec.ts
  - tests/unit/test_graph_explorer_favorites.py
  since: 2026-06
  scenario: 'Given the Favorites panel displays a favorite with a Cypher query

    When the user clicks the favorite''s label

    Then the query is loaded into the editor header

    And the editor is ready to execute or modify the query

    '
  e2e: true
  e2e_reason: Query loading and editor interaction are only verifiable through end-to-end browser testing.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - app-developer
- id: REQ-782
  status: complete
  group: 10. UI & Admin Surfaces
  category: Graph Explorer
  priority: SHOULD
  type: behavioral
  description: 'Graph Explorer Favorites: inline rename input allows editing a favorite''s label with
    Enter to commit and Escape to cancel; label persists in localStorage.'
  use_case: Users can update favorite labels to keep them organized and meaningful as their work evolves.
  code:
  - provisa-ui/src/components/graph-explorer
  tests:
  - provisa-ui/e2e/graph-favorites.spec.ts
  - tests/unit/test_graph_explorer_favorites.py
  since: 2026-06
  scenario: 'Given the Favorites panel with a visible favorite and action buttons on hover

    When the user clicks the rename button

    Then a text input appears focused with the current label

    When the user types a new label and presses Enter

    Then the input closes and the favorite displays the new label

    And the change persists in localStorage

    '
  e2e: true
  e2e_reason: Inline input behavior, focus management, and localStorage persistence can only be verified
    through live browser interaction.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - app-developer
- id: REQ-783
  status: complete
  group: 10. UI & Admin Surfaces
  category: Graph Explorer
  priority: SHOULD
  type: behavioral
  description: 'Graph Explorer Favorites: delete button removes a favorite from the panel and localStorage
    immediately.'
  use_case: Users can clean up outdated or unwanted saved queries.
  code:
  - provisa-ui/src/components/graph-explorer
  tests:
  - provisa-ui/e2e/graph-favorites.spec.ts
  - tests/unit/test_graph_explorer_favorites.py
  since: 2026-06
  scenario: 'Given the Favorites panel with at least one visible favorite

    When the user hovers over a favorite and clicks the delete button

    Then the favorite is removed from the panel immediately

    And the removal persists in localStorage (empty panel shows placeholder)

    '
  e2e: true
  e2e_reason: Delete behavior and localStorage synchronization are only verifiable through end-to-end
    browser testing.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - app-developer
- id: REQ-784
  status: complete
  group: 5. Query Languages, Compilation & Operations
  category: Cypher Graph Analytics
  priority: SHOULD
  type: behavioral
  description: Auto-impute endpoint generates relationship edges for visible graph nodes based on their
    labels and the schema relationship map, executing one Cypher query per relationship pair.
  use_case: Graph Explorer users need to automatically discover edges between visible nodes without manually
    querying each relationship type.
  code:
  - provisa/api/rest/cypher_router.py
  - provisa/cypher/assembler.py
  tests:
  - provisa-ui/e2e/cypher-impute-edges.spec.ts
  - tests/unit/test_cypher_analytics.py
  since: 2026-06
  scenario: 'Given a set of visible graph nodes with known labels

    When the auto-impute endpoint receives the visible node set with stable integer ids

    Then it queries each relationship pair (src_label)-[rel_type]->(tgt_label) where both endpoints are
    visible

    And returns all discovered edges merged with the input nodes in standard Cypher response format

    '
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
- id: REQ-785
  status: complete
  group: 5. Query Languages, Compilation & Operations
  category: Cypher Graph Analytics
  priority: MUST
  type: constraint
  description: Imputed edge startNode/endNode ids must be stable integers (registered via node_ids table),
    not raw database primary keys, to enable canvas lookup and correct relationship rendering.
  use_case: Graph Explorer relies on stable node ids (as registered in node_ids.id) to correlate imputed
    edges with visible nodes; raw PKs cause canvas lookup failures.
  code:
  - provisa/api/rest/cypher_router.py
  - provisa/cypher/assembler.py
  tests:
  - provisa-ui/e2e/cypher-impute-edges.spec.ts
  - tests/unit/test_cypher_analytics.py
  since: 2026-06
  scenario: 'Given imputed edges with startNode and endNode references

    When the edge startNode.id and endNode.id are examined

    Then they must be integers (typeof id === "number")

    And they must correspond to the stable integer ids in the node_ids table

    And NOT the raw composite_id or physical primary key

    '
- id: REQ-786
  status: complete
  group: 5. Query Languages, Compilation & Operations
  category: Cypher Graph Analytics
  priority: SHOULD
  type: behavioral
  description: Impute-relationships endpoint accepts visible node set with stable integer ids and resolves
    them to database primary keys via node_ids table lookup before executing relationship queries.
  use_case: Graph Explorer passes only the stable integer ids (already loaded); the endpoint must map
    them to raw PK values for correct relationship filtering.
  code:
  - provisa/api/rest/cypher_router.py
  tests:
  - provisa-ui/e2e/cypher-impute-edges.spec.ts
  - tests/unit/test_cypher_analytics.py
  since: 2026-06
  scenario: 'Given a request with nodes: [{label: "Meta", id: 10}, {label: "Meta", id: 11}, ...]

    When the endpoint fetches rows from node_ids WHERE id = ANY([10, 11, ...])

    Then it extracts the raw PK from composite_id ("label|pk_value")

    And uses the raw PK values in the WHERE clause for relationship queries

    And returns stable integer ids in the result edges (via register_node_ids)

    '
  e2e: true
  e2e_reason: Correct endpoint behavior requires live node_ids table lookup, graph node resolution, and
    verifying that returned edges have integer ids; unit mocking cannot ensure the full round-trip from
    stable id → raw PK → query → registered result.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
- id: REQ-788
  status: complete
  group: 4. Source Connectors
  category: File & Lake Sources
  priority: SHOULD
  type: behavioral
  description: File connector sources accept a directory glob pattern to enumerate CSV files. Discovered
    files are introspected to extract schema (column names and types). Multiple CSV files matching the
    glob are consolidated into a single logical table when registered.
  use_case: File glob patterns enable users to query across multiple CSV files without manual discovery
    or registration of individual files.
  code:
  - provisa/source_adapters/file_connector.py
  tests:
  - provisa-ui/e2e/file-connector.spec.ts
  - tests/unit/test_file_lake_sources.py
  since: 2026-06
  scenario: 'Given a directory containing multiple CSV files matching a glob pattern

    When a file connector source is registered with the directory glob pattern

    Then all matching files are discovered and enumerated as available tables

    And the table schema is extracted from CSV headers

    '
  e2e: true
  e2e_reason: File discovery, CSV introspection, and table enumeration require a real filesystem and live
    file scanning, verifiable only through end-to-end UI interaction and GraphQL schema introspection.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - data-engineer
  - app-developer
- id: REQ-789
  status: complete
  group: 4. Source Connectors
  category: File & Lake Sources
  priority: SHOULD
  type: behavioral
  description: 'CSV column headers are automatically mapped to GraphQL field names using LINQ4J convention:
    camelCase headers are converted to snake_case column names. This mapping is transparent at the schema
    definition level.'
  use_case: Automatic header normalization allows CSV files with mixed naming conventions to produce consistent,
    queryable schemas without manual column mapping.
  code:
  - provisa/source_adapters/file_connector.py
  tests:
  - provisa-ui/e2e/file-connector.spec.ts
  - tests/unit/test_file_lake_sources.py
  since: 2026-06
  scenario: 'Given a CSV file with camelCase headers (e.g., "companyName", "customerId")

    When the file is introspected by the file connector

    Then headers are automatically converted to snake_case (e.g., "company_name", "customer_id")

    And GraphQL field names reflect the snake_case conversion

    '
  e2e: true
  e2e_reason: Header conversion is only verifiable through full table registration and GraphQL schema
    introspection, requiring live CSV file processing.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - app-developer
  - data-engineer
- id: REQ-790
  status: complete
  group: 4. Source Connectors
  category: File & Lake Sources
  priority: SHOULD
  type: behavioral
  description: File connector table enumeration (via directory glob discovery) is accessible through the
    Provisa UI table registration form. When a file source is selected in the form, a schema dropdown
    is populated with discovered schemas, and a table dropdown lists all tables available in the selected
    schema.
  use_case: UI-driven table enumeration allows users to discover and register file-based tables without
    knowledge of directory structure or manual schema definitions.
  code:
  - provisa-ui/src/pages/tables
  - provisa/api/rest/tables_router.py
  tests:
  - provisa-ui/e2e/file-connector.spec.ts
  - tests/unit/test_file_lake_sources.py
  since: 2026-06
  scenario: 'Given the Sources & Tables UI with a registered file connector source

    When a user clicks "Add Table" and selects the file source

    Then the schema dropdown is populated with discovered schemas

    And the table dropdown lists all CSV-derived tables in the selected schema

    '
  e2e: true
  e2e_reason: UI form rendering, dropdown population, and schema/table enumeration require a live running
    server, file scanning, and browser interaction.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - app-developer
  - data-engineer
- id: REQ-791
  status: complete
  group: 4. Source Connectors
  category: File & Lake Sources
  priority: SHOULD
  type: behavioral
  description: Registered file-based tables are queryable via the data GraphQL endpoint. Tables appear
    in the GraphQL schema with domain-prefixed field names. Queries execute read-only against the underlying
    CSV files with all columns visible (subject to column masking and RLS rules if defined).
  use_case: File-based data becomes immediately consumable via standard GraphQL queries, enabling analytics
    and reporting without additional transformation.
  code:
  - provisa/api/data/endpoint.py
  - provisa/compiler/sql_gen.py
  tests:
  - provisa-ui/e2e/file-connector.spec.ts
  - tests/unit/test_file_lake_sources.py
  since: 2026-06
  scenario: 'Given a registered customers table created from CSV files via file connector

    When a GraphQL query is issued against the data endpoint for customers

    Then the query returns all rows from the CSV files with all columns

    And the response matches the CSV schema with snake_case column names

    '
  e2e: true
  e2e_reason: End-to-end query execution from GraphQL through file data fetching requires a live server,
    registered tables, and actual file I/O.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - app-developer
  - data-engineer
- id: REQ-792
  status: complete
  group: 7. Result Delivery
  category: Neo4j Export
  priority: SHOULD
  type: behavioral
  description: 'GET /data/graph-schema endpoint returns the current graph schema including all node labels
    (as array of {label: "Domain:Table"} objects) and relationship types (as array of {type, source, target}
    objects indicating source and target node labels).'
  use_case: E2E export clients can discover which node labels and relationship types exist in the current
    graph before querying and exporting them.
  code:
  - provisa/api/data/endpoint.py
  tests:
  - provisa-ui/e2e/neo4j-docker-export.spec.ts
  - tests/unit/test_neo4j_cypher_endpoint.py
  since: 2026-06
  scenario: 'Given a graph with multiple node labels and relationship types

    When GET /data/graph-schema is called

    Then the response includes node_labels array with all node labels

    And the response includes relationship_types array with source/target pairs

    '
  e2e: true
  e2e_reason: Schema discovery requires a live server with registered tables and relationships, verifiable
    only through a full E2E stack.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - app-developer
  - data-engineer
- id: REQ-793
  status: complete
  group: 7. Result Delivery
  category: Neo4j Export
  priority: SHOULD
  type: behavioral
  description: 'POST /data/cypher endpoint accepts parameterless Cypher queries and returns row data.
    Queries requiring parameters are rejected. Endpoint returns {rows: [...], error?: string} structure.'
  use_case: E2E export clients can execute discovery and validation queries against the graph without
    parameter substitution complexity.
  code:
  - provisa/api/rest/cypher_router.py
  tests:
  - provisa-ui/e2e/neo4j-docker-export.spec.ts
  - tests/unit/test_neo4j_cypher_endpoint.py
  since: 2026-06
  scenario: 'Given a parameterless Cypher query like "MATCH (n:Domain:Table) RETURN n"

    When POST /data/cypher is called with the query

    Then the response returns {rows: [...]}} with query results

    And a query requiring parameters returns error in response

    '
  e2e: true
  e2e_reason: Query execution verifiable only through live server with actual graph data and Cypher execution
    engine.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - app-developer
  - data-engineer
- id: REQ-794
  status: complete
  group: 7. Result Delivery
  category: Neo4j Export
  priority: SHOULD
  type: behavioral
  description: Query result values are introspected for node and edge structures. Nodes are identified
    by presence of {id, tableLabel, properties} fields. Edges are identified by {identity, startNode,
    endNode, type} fields. Both are recursively extracted from arbitrary result objects.
  use_case: E2E export clients can query arbitrary graph patterns and automatically extract nodes and
    edges regardless of result structure nesting.
  code:
  - provisa/api/rest/cypher_router.py
  tests:
  - provisa-ui/e2e/neo4j-docker-export.spec.ts
  - tests/unit/test_neo4j_cypher_endpoint.py
  since: 2026-06
  scenario: 'Given a Cypher query returning nested nodes and edges

    When result rows are introspected

    Then nodes with {id, tableLabel, properties} are extracted

    And edges with {identity, startNode, endNode, type} are extracted

    And extraction works on deeply nested result values

    '
  e2e: true
  e2e_reason: Result introspection and extraction verifiable only through live queries returning actual
    graph data structures.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - app-developer
  - data-engineer
- id: REQ-795
  status: complete
  group: 7. Result Delivery
  category: Neo4j Export
  priority: SHOULD
  type: behavioral
  description: POST /data/neo4j-export endpoint accepts edge-only requests (empty nodes array) and exports
    relationships without requiring node re-export. Edges reference nodes via start/end node IDs (matching
    deduplication key).
  use_case: E2E export workflows can batch export large graphs by sending nodes first, then edges in a
    separate request without resending node data.
  code:
  - provisa/api/rest/cypher_router.py
  tests:
  - provisa-ui/e2e/neo4j-docker-export.spec.ts
  - tests/unit/test_neo4j_cypher_endpoint.py
  since: 2026-06
  scenario: 'Given nodes already exported to Neo4j

    When POST /data/neo4j-export is called with empty nodes array and populated edges

    Then edges are successfully exported using start/end node IDs

    And Neo4j relationship count matches expected count

    '
  e2e: true
  e2e_reason: Edge-only export verified only through live Neo4j instance with actual relationship creation.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - app-developer
  - data-engineer
- id: REQ-796
  status: complete
  group: 7. Result Delivery
  category: Neo4j Export
  priority: SHOULD
  type: behavioral
  description: X-Role header (e.g., "DEV") grants access to /data/cypher and /data/graph-schema endpoints.
    Requests without valid role are rejected.
  use_case: Data export workflow endpoints require explicit role-based access control distinct from standard
    query execution permissions.
  code:
  - provisa/api/rest/cypher_router.py
  - provisa/api/data/endpoint.py
  tests:
  - provisa-ui/e2e/neo4j-docker-export.spec.ts
  - tests/unit/test_neo4j_cypher_endpoint.py
  since: 2026-06
  scenario: 'Given an export client with X-Role: DEV header

    When POST /data/cypher is called

    Then the request succeeds

    And a request without X-Role header is rejected

    '
  e2e: true
  e2e_reason: Role-based access control verified only through live server requests with actual header
    validation.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - app-developer
  - data-engineer
- id: REQ-797
  status: complete
  group: 7. Result Delivery
  category: Neo4j Export
  priority: SHOULD
  type: behavioral
  description: 'E2E neo4j export validates exported graph integrity: node and relationship counts in target
    Neo4j instance match expected counts. MERGE deduplication on _provisa_id ensures node count ≤ exported
    count; relationship count must equal exported edge count.'
  use_case: E2E export clients can verify that export was complete and accurate by querying node and relationship
    counts in the target Neo4j instance.
  code:
  - provisa/api/rest/cypher_router.py
  tests:
  - provisa-ui/e2e/neo4j-docker-export.spec.ts
  - tests/unit/test_neo4j_cypher_endpoint.py
  since: 2026-06
  scenario: 'Given a completed export with N nodes and E edges to Neo4j

    When node and relationship counts are queried in target Neo4j

    Then node count is ≤ N (due to MERGE deduplication on _provisa_id)

    And relationship count exactly equals E

    And all node counts are > 0

    '
  e2e: true
  e2e_reason: Graph integrity verification requires live Neo4j instance with actual relationship creation
    and count queries.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - app-developer
  - data-engineer
- id: REQ-798
  status: complete
  superseded_by: REQ-818
  group: 5. Query Languages, Compilation & Operations
  category: Cypher Mutations
  priority: MUST
  type: behavioral
  description: Cypher mutations (CREATE/DELETE/UPDATE) must be transpiled through the full semantic SQL
    write pipeline, applying RLS injection, dialect transpilation, and all post-mutation hooks (response
    cache invalidation, MV stale marking, Kafka change events, Kafka sink triggers, hot-table reload).
  use_case: Cypher mutations must enforce the same security policies, maintain consistency across multiple
    backends, and trigger the same change-propagation workflows as SQL and GraphQL mutations.
  code:
  - provisa/api/rest/cypher_router.py
  tests:
  - tests/unit/test_cypher_mutations.py
  - tests/integration/test_mutations.py
  - tests/steps/steps_cypher_mutations.py
  since: 2026-06
  scenario: 'Given a Cypher CREATE/DELETE/UPDATE mutation

    When the mutation is transpiled through WriteTranslator and wrapped in MutationResult

    Then RLS is injected via inject_rls_into_mutation

    And the mutation is transpiled to the target dialect

    And the mutation is executed via execute_direct

    And all post-mutation hooks fire (cache invalidation, MV stale marking, Kafka events, hot-table reload)

    '
  e2e: false
  e2e_reason: Backend write pipeline with no browser surface; transpilation/RLS covered by unit, full
    hook execution covered by integration round-trip.
  related_reqs:
  - REQ-666
  - REQ-667
  - REQ-668
  - REQ-670
  stakeholders:
  - app-developer
  - data-engineer
- id: REQ-799
  status: complete
  group: 10. UI & Admin Surfaces
  category: UI Explorer Pages
  priority: SHOULD
  type: ui
  description: Browser-based gRPC Explorer UI page at /grpc allows interactive query execution against
    gRPC service methods. Proto schema displayed inline, request bodies edited as JSON, responses rendered
    as JSON. Method selection filters by operation type (query vs mutation). Auto-selects first query
    method on navigation.
  use_case: Developers can explore and test gRPC queries without native gRPC client tools, using the same
    governance pipeline as the gRPC servicer.
  code:
  - provisa/api/data/endpoint_grpc_proxy.py
  - provisa-ui/src/pages/GrpcPage.tsx
  since: 2026-06
  scenario: 'Given a role with read access to a table

    When navigating to /grpc

    Then the proto schema is fetched and parsed

    And methods are grouped by operation type (query vs mutation)

    And selecting a method displays a JSON request template

    And submitting a request routes through /data/grpc/{TypeName} endpoint

    And response is rendered as formatted JSON

    '
  integration_test: not_required
  integration_test_reason: UI-only; backend proxy compilation and governance tested under REQ-803.
  e2e: true
  e2e_reason: UI interaction with proto parsing, method selection, request submission, and response rendering
    requires live browser execution.
  tests:
  - provisa-ui/e2e/grpc-explorer.spec.ts
  stakeholders:
  - app-developer
- id: REQ-800
  status: complete
  group: 10. UI & Admin Surfaces
  category: UI Explorer Pages
  priority: SHOULD
  type: ui
  description: Browser-based JSON:API Explorer UI page at /jsonapi allows interactive query composition
    with sparse fieldsets, filtering (eq, neq, gt, gte, lt, lte, like), sorting, pagination, and relationship
    inclusion. Table selection grouped by domain, field/include selectors with checkbox dropdowns, result
    summary view with resource cards and pagination controls.
  use_case: Developers can explore and test JSON:API endpoints without manual URL construction, inspecting
    governance-filtered data across all domains and relationships.
  code:
  - provisa/api/jsonapi/spec.py
  - provisa-ui/src/pages/JsonApiPage.tsx
  since: 2026-06
  scenario: 'Given a role with read access to tables across multiple domains

    When navigating to /jsonapi

    Then tables are listed grouped by domain

    And selecting a table allows field selection and include (relationship) selection

    And filter/sort/pagination controls compose a query URL

    And executing the query fetches from /data/jsonapi/{domain}/{table}

    And response is rendered in summary view (resource cards) or raw JSON

    And pagination links (first, prev, next, last) are clickable

    '
  integration_test: not_required
  integration_test_reason: UI-only; JSON:API endpoint and spec generation tested under REQ-805.
  e2e: true
  e2e_reason: UI state management, table/field selection, query composition, and response rendering with
    pagination require live browser execution and network interaction.
  tests:
  - provisa-ui/e2e/jsonapi-explorer.spec.ts
  stakeholders:
  - app-developer
- id: REQ-801
  status: complete
  group: 10. UI & Admin Surfaces
  category: UI Explorer Pages
  priority: SHOULD
  type: ui
  description: Browser-based OpenAPI Explorer UI page at /openapi embeds Swagger UI iframe serving dynamically
    generated OpenAPI 3.1 spec from /data/rest/docs. Supports auto-run navigation to pre-selected REST
    endpoints with method/path detection and try-it-out activation via Swagger UI DOM manipulation.
  use_case: Developers can browse REST API endpoints with interactive try-it-out experience powered by
    Swagger UI, with support for navigation from other explorers (e.g., NL query results linking to generated
    REST endpoints).
  code:
  - provisa/api/rest/openapi_spec.py
  - provisa-ui/src/pages/OpenApiPage.tsx
  since: 2026-06
  scenario: 'Given a role with governance applied to REST endpoints

    When navigating to /openapi

    Then Swagger UI loads the /data/rest/docs spec

    And per-table paths are visible with parameter documentation

    And optional autoRun navigation targets a specific method+path

    And the opblock is scrolled into view and expanded

    And try-it-out is activated and execute is clicked

    Then response is displayed in Swagger UI

    '
  integration_test: not_required
  integration_test_reason: UI-only iframe embed; spec generation tested under REQ-804.
  e2e: true
  e2e_reason: Iframe loading, DOM manipulation to find/expand opblocks, and Swagger UI interaction require
    live browser execution.
  tests:
  - provisa-ui/e2e/openapi-explorer.spec.ts
  stakeholders:
  - app-developer
- id: REQ-802
  status: complete
  group: 8. Client Access & Protocols
  category: Protocol Support
  priority: MUST
  type: behavioral
  description: Bolt protocol (Neo4j binary protocol) TCP server at port 5251 accepts Cypher queries and
    mutations, transpiles through Provisa query pipeline (compile, govern, route, execute). PackStream
    codec encodes/decodes scalars, lists, dicts, nodes, relationships, and messages. Framing layer chunks
    messages for TCP transport. Bolt authenticates under every configured auth provider. The driver's
    HELLO/LOGON `scheme` field selects the provider validator (`basic` presents principal+credentials,
    `bearer` presents a token), an unaccepted presentation is refused outright rather than retried against
    another validator, and a personal access token is accepted as an ordinary bearer credential (REQ-124,
    REQ-1263). Selectable roles derive from the validated identity — the claim-mapped role plus the identity's
    own assignments, intersected with compiled contexts (REQ-273, REQ-551) — never from a user record
    read behind the provider. Auth state is read fail-closed, so active auth middleware with no auth_config
    raises rather than degrading to trust mode. The WebSocket upgrade enforces a browser Origin allowlist
    from PROVISA_BOLT_ALLOWED_ORIGINS; an upgrade carrying an unlisted Origin gets 403, and with nothing
    listed every browser origin is refused (a header-less driver upgrade is unaffected).
  use_case: Neo4j-compatible clients (DBeaver, Cypher shells, drivers) can execute Cypher queries against
    Provisa schema with full governance applied, expanding query language surface beyond GraphQL/SQL/gRPC/REST.
    An OIDC- or basic-secured deployment is reachable from a Neo4j driver instead of getting a blanket
    refusal.
  code:
  - provisa/bolt/server.py
  - provisa/bolt/packstream.py
  - provisa/bolt/session.py
  - provisa/bolt/messages.py
  - provisa/bolt/framing.py
  - provisa/bolt/websocket.py
  tests:
  - tests/unit/test_bolt_packstream.py
  - tests/unit/test_bolt_auth.py
  - tests/unit/test_bolt_websocket_origin.py
  - tests/integration/test_bolt_server.py
  - tests/e2e/test_bolt_cypher.py
  since: 2026-06
  scenario: 'Given a Cypher client connecting to Bolt port 5251

    When the client sends Cypher query "MATCH (n:Person) RETURN n"

    Then the server accepts the handshake (magic + version negotiation)

    And the query is transpiled to SQL via WriteTranslator

    And governance (RLS, masking, visibility) is applied at compile time

    And results are executed and returned as Bolt structures (nodes, relationships)

    And response is serialized via PackStream and framed for TCP

    '
  integration_test: required
  integration_test_reason: Bolt handshake, query compilation, governance application, and PackStream serialization
    require live Bolt client and backend state interaction.
  e2e: false
  e2e_reason: Bolt is a TCP binary protocol with no browser surface; full Cypher→SQL pipeline verified
    by live-client integration test (tests/integration/test_bolt_server.py).
  stakeholders:
  - app-developer
  - data-engineer
- id: REQ-803
  status: complete
  group: 8. Client Access & Protocols
  category: Protocol Support
  priority: MUST
  type: structural
  description: HTTP→gRPC proxy endpoint POST /data/grpc/{TypeName} translates JSON request body into GraphQL
    query using the gRPC schema, compiles and executes through the full query pipeline, returns JSON rows.
    Supports read_mask with dot-notation for field filtering and native filter args (path_param, query_param)
    from request body["filter"].
  use_case: Browser-based gRPC Explorer and programmatic gRPC clients can query via HTTP+JSON without
    requiring native gRPC/Protobuf tooling, while applying the same governance and backend routing as
    native gRPC.
  code:
  - provisa/api/data/endpoint_grpc_proxy.py
  since: 2026-06
  scenario: 'Given a gRPC type "Pets" with Query method and fields (id, name, age, owner)

    When POST /data/grpc/Pets with body {filter: {owner: "Alice"}, limit: 20, read_mask: {paths: ["name",
    "age"]}}

    Then the type is resolved to GraphQL field name (pascal→snake)

    And field selections are built for scalars (and scalar sub-fields for nested types)

    And read_mask paths filter top-level and nested fields

    And native filter args are extracted from body["filter"]

    And GraphQL query is compiled and executed with governance

    Then response rows are serialized and returned as JSON

    '
  integration_test: required
  integration_test_reason: JSON→GraphQL translation, read_mask filtering, and governance pipeline require
    live backend with compiled schema.
  e2e: false
  e2e_reason: UI-driven execution covered by REQ-799 gRPC Explorer e2e.
  tests:
  - tests/unit/test_grpc_proxy_translation.py
  - tests/integration/test_grpc_proxy.py
  stakeholders:
  - app-developer
- id: REQ-804
  status: complete
  group: 8. Client Access & Protocols
  category: Protocol Support
  priority: MUST
  type: structural
  description: Dynamic OpenAPI 3.1 spec generator for /data/rest endpoints. Generates per-table paths,
    typed query parameters (limit, offset, fields, filter, orderBy), response schemas with attributes
    and relationships. Supports domain filtering, describes limit/offset pagination and sparse fieldsets.
  use_case: Swagger UI and REST API consumers can discover and understand auto-generated REST endpoints
    with parameter types, filter operators, pagination, and output schemas derived from the GraphQL schema.
  code:
  - provisa/api/rest/openapi_spec.py
  since: 2026-06
  scenario: "Given a schema with tables in multiple domains\nWhen GET /data/rest/openapi.json?role=<role_id>&domains=<domain_list>\n\
    Then spec is generated with one path per visible table\nAnd parameters include limit (int, min 1),\
    \ offset (int, min 0), fields (array of column names),\n    filter (array of field/comparator/value\
    \ objects), orderBy (array of field/direction objects)\nAnd response schema includes data (array of\
    \ resources) with attributes and relationships\nAnd domain filtering restricts paths to requested\
    \ domains\nThen Swagger UI renders spec with try-it-out support\n"
  integration_test: required
  integration_test_reason: Spec generation from live GraphQL schema with domain filtering requires live
    backend.
  e2e: false
  e2e_reason: Spec rendering in Swagger UI covered by REQ-801 OpenAPI Explorer e2e.
  tests:
  - tests/unit/test_openapi_spec.py
  - tests/integration/test_openapi_spec.py
  stakeholders:
  - app-developer
- id: REQ-805
  status: complete
  group: 10. UI & Admin Surfaces
  category: UI Explorer Pages
  priority: SHOULD
  type: ui
  description: Dynamic JSON:API OpenAPI 3.1 spec generator accessible at /data/jsonapi/openapi.json. Generates
    per-table paths with sparse fieldsets (fields[table]), filtering (filter[col], filter[col][op] for
    all filter operators), sorting (sort ± prefix), pagination (page[size], page[number]), and include
    (relationship names). Returns application/vnd.api+json in spec.
  use_case: Tools and developers can discover JSON:API endpoints and their constraints (filter operators,
    field lists, relationship names) without manual inspection.
  code:
  - provisa/api/jsonapi/spec.py
  since: 2026-06
  scenario: "Given a schema with Pets table having columns (id, name, owner_id)\nWhen GET /data/jsonapi/openapi.json?role=<role_id>&domains=<domain_list>\n\
    Then spec includes path /domain/Pets with parameters:\n    page[size] (int, min 1), page[number] (int,\
    \ min 1)\n    fields[Pets] (comma-separated column names)\n    sort (field name, ± prefix for desc)\n\
    \    include (comma-separated relationship names, e.g. owner)\n    filter[name], filter[name][eq|neq|gt|gte|lt|lte|like]\
    \ for each column\nAnd response schema describes JSON:API document with data array and included array\n"
  integration_test: required
  integration_test_reason: Spec generation from live schema with field discovery, filter operators, and
    relationship names requires live backend.
  e2e: false
  e2e_reason: Spec usage in JSON:API Explorer covered by REQ-800 e2e.
  tests:
  - tests/unit/test_jsonapi_spec.py
  - tests/integration/test_jsonapi_spec.py
  stakeholders:
  - app-developer
- id: REQ-806
  status: complete
  group: 8. Client Access & Protocols
  category: Bolt Protocol
  priority: MUST
  type: structural
  description: Bolt relationships receive durable integer IDs via a `rel_ids` Postgres table (BIGSERIAL
    id, composite_id "Type:startPk-endPk" UNIQUE, rel_type, properties JSONB), mirroring the `node_ids`
    design. The `register_rel_ids` function upserts edges and replaces composite identities with integer
    IDs.
  use_case: Bolt clients require stable relationship identity across browser sessions and multiple query
    paths (Bolt native, REST cypher, REST impute); numeric IDs enable efficient indexing and relationship
    traversal.
  code:
  - provisa/core/schema.sql
  - provisa/bolt/session.py
  - provisa/bolt/packstream.py
  tests:
  - tests/unit/test_bolt_rel_ids.py
  - tests/integration/test_bolt_rel_ids.py
  since: 2026-06
  scenario: 'Given a Bolt relationship created by cypher MATCH (a)-[r:OWNS]->(b) RETURN r

    When register_rel_ids is called with edge (node_start_pk, node_end_pk, rel_type)

    Then the composite_id "OWNS:pk1-pk2" is upserted into rel_ids table

    And the relationship''s id field is populated with the BIGSERIAL value

    And subsequent queries return the same numeric id for that relationship edge

    '
- id: REQ-807
  status: complete
  group: 1. Access Governance & Security
  category: Protocol Support
  priority: MUST
  type: structural
  description: Bolt auth model establishes principal = user, database = role. A user holds a set of roles;
    SHOW DATABASES lists one database per (view × role) pair. Database names follow the pattern `provisa_<role>`
    (business domains only) and `provisa_ops_<role>` (includes system/meta/ops domains). The Bolt Browser's
    database dropdown / `:use` command selects the active role and view. Home database defaults to the
    user's first role.
  use_case: Bolt Browser users can switch contexts by role and view (ops vs business) without reauthenticating.
    The database abstraction maps cleanly to role-based domain visibility.
  code:
  - provisa/bolt/session.py
  - provisa/bolt/packstream.py
  tests:
  - tests/unit/test_bolt_show_databases.py
  since: 2026-06
  scenario: 'Given a user "alice" holding roles ["sales", "finance"]

    When SHOW DATABASES is issued

    Then the result includes: provisa_sales, provisa_ops_sales, provisa_finance, provisa_ops_finance

    And HOME DATABASE = provisa_sales

    When :use provisa_ops_sales is issued

    Then subsequent queries execute in the ops view with finance role

    '
- id: REQ-808
  status: complete
  group: 1. Access Governance & Security
  category: Query Governance
  priority: MUST
  type: constraint
  description: The Bolt endpoint must never subvert the connecting user's domain rights. The active role's
    `domain_access` is the hard ceiling for every query, catalog call (db.labels), relationship counts,
    and relationship imputation. The ops/business view only narrows within that ceiling. Unknown or unauthorized
    `db` values return DatabaseNotFound error (no silent fallback). System, meta, and ops domains are
    identified by domain_id in {"", "meta", "ops"}.
  use_case: Bolt's flexible query language (cypher) could bypass view/role filtering if not enforced at
    the session/catalog level. This requirement ensures domain visibility is the immovable constraint,
    not an advisory filter.
  code:
  - provisa/bolt/session.py
  - provisa/cypher/assembler.py
  - provisa/api/rest/cypher_router.py
  tests:
  - tests/unit/test_bolt_domain_ceiling.py
  - tests/integration/test_bolt_domain_ceiling.py
  since: 2026-06
  scenario: 'Given a user in role "sales" with domain_access ["public", "sales"]

    When a cypher query attempts MATCH (n:Node) in ops domain

    Then the query is rejected or the result set excludes ops nodes

    When :use provisa_ops_unknown is issued

    Then DatabaseNotFound error is returned (not a fallback to provisa_sales)

    When db.labels is called

    Then only labels in domain_access ∩ view are returned

    '
- id: REQ-809
  status: complete
  group: 5. Query Languages, Compilation & Operations
  category: SQLGlot Transpilation
  priority: SHOULD
  type: behavioral
  description: Correlated scalar subqueries in PG-style canonical SQL are lifted into CTEs before the
    query is forwarded to Trino. rewrite_correlated_subqueries_for_trino in provisa/transpiler/transpile.py
    rewrites any correlated scalar subquery into a set-based CTE join so the federation engine receives
    a form it can execute across sources.
  use_case: Correlated-subquery-to-CTE rewriting keeps cross-source SQL correct on Trino, which does not
    execute arbitrary correlated scalar subqueries the way a single RDBMS would.
  code:
  - provisa/transpiler/transpile.py
  tests:
  - tests/unit/test_transpile.py
  since: 2026-03
  scenario: 'Given a PG-style query containing a correlated scalar subquery

    When the query is transpiled to Trino SQL

    Then the correlated subquery is lifted into a CTE joined on the correlation key

    And the forwarded SQL contains no correlated scalar subquery

    '
  stakeholders:
  - data-engineer
- id: REQ-810
  status: complete
  group: 6. Execution, Routing, Caching & Performance
  category: Materialized Views
  priority: SHOULD
  type: behavioral
  description: Materialized-view transparent rewriting supports partial join-pattern matches. rewrite_if_mv_match
    in provisa/mv/rewriter.py matches a query's joins against an MV per-join; when the MV covers only
    a subset of the query's joins, _partial_rewrite_to_mv removes the covered joins and preserves the
    remaining joins so the rewritten query reads the MV for the matched portion and joins the rest live.
  use_case: Partial MV matching lets a materialized view accelerate queries that join additional tables
    beyond the MV's definition, instead of only queries whose join pattern matches the MV exactly.
  code:
  - provisa/mv/rewriter.py
  tests:
  - tests/integration/test_execution_routing.py
  - tests/e2e/test_mv_optimization.py
  - tests/unit/test_mv_partial_rewrite.py
  since: 2026-03
  scenario: 'Given a materialized view covering a subset of a query''s joins

    When the query is compiled

    Then the joins covered by the MV are rewritten to read the MV

    And the joins not covered by the MV are preserved and executed live

    '
  stakeholders:
  - data-engineer
- id: REQ-811
  status: complete
  group: 6. Execution, Routing, Caching & Performance
  category: Federation Performance
  priority: SHOULD
  type: behavioral
  description: The `# @provisa key=value` GraphQL comment hint vocabulary includes a `route=federated|direct`
    directive parsed by extract_graphql_hints in provisa/compiler/hints.py. `route=federated` forces the
    query through the federation engine; `route=direct` forces single-source direct execution. This is
    distinct from the `/*+ BROADCAST(...) */` SQL block-hint family (REQ-279) and source-level federation_hints
    (REQ-281).
  use_case: A per-query route hint lets query authors override automatic routing for a specific query
    without changing source configuration.
  code:
  - provisa/compiler/hints.py
  tests:
  - tests/unit/test_routing.py
  since: 2026-03
  scenario: 'Given a GraphQL query annotated with the comment hint "# @provisa route=direct"

    When the query is compiled

    Then the query is routed to single-source direct execution

    And a query annotated with "# @provisa route=federated" is routed through the federation engine

    '
  related_reqs:
  - REQ-279
  - REQ-281
  stakeholders:
  - data-engineer
- id: REQ-812
  status: complete
  group: 8. Client Access & Protocols
  category: API & Integration
  priority: SHOULD
  type: behavioral
  description: The `X-Provisa-Sink` request header on a subscription request redirects the subscription's
    change-event output to a Kafka sink instead of the SSE response stream. subscription_sse.py parses
    the header and launches a Kafka sink consumer for the subscription, returning 202 Accepted rather
    than an open SSE stream.
  use_case: An X-Provisa-Sink header lets a client wire a live subscription directly into a Kafka topic
    for downstream stream processing without holding an SSE connection.
  code:
  - provisa/api/data/subscription_sse.py
  tests:
  - tests/unit/test_subscription_sink_header.py
  - tests/integration/test_kafka_sink.py
  since: 2026-04
  scenario: 'Given a subscription request with the header "X-Provisa-Sink" set to a Kafka target

    When the request is accepted

    Then the response status is 202 Accepted

    And subscription change events are delivered to the Kafka sink instead of an SSE stream

    '
  stakeholders:
  - data-engineer
  - app-developer
- id: REQ-813
  status: complete
  group: 9. Live Data & Events
  category: Subscriptions
  priority: MUST
  type: structural
  description: 'The `live` block (LiveDeliveryConfig on Table) is a unified change-feed / CDC abstraction.
    The binary `delivery: poll|cdc` field is replaced by a multi-strategy model: `strategy: poll|native|debezium|kafka`.
    Per-table params: poll → watermark_column/poll_interval; native → none; kafka → LiveKafkaParams (topic/format/key_column/field_mapping).
    Reconciled with REQ-824: Debezium/Kafka delta-transport (bootstrap_servers/topic_prefix/schema_registry_url/consumer_group_id)
    is inherited from Source.cdc, entered once per source — NOT repeated in per-table debezium params.
    query_id and outputs are optional so `live` covers both raw table change-feeds and live persisted-query
    output fan-out. Debezium topic names are derived from the source transport: {topic_prefix}.{schema}.{table}.'
  use_case: A single `live` config abstraction unifies all capture mechanisms (polling, PostgreSQL LISTEN/NOTIFY,
    MongoDB change streams, Debezium, arbitrary Kafka deltas), eliminating the misleading binary of "poll
    vs CDC" where poll IS a change-feed mechanism. Strategy-specific params prevent schema pollution.
    Optional query_id and outputs enable both low-level table subscriptions and high-level live persisted-query
    delivery.
  code:
  - provisa/core/models.py
  tests:
  - tests/unit/test_live_delivery_config.py
  since: 2026-07
  related_reqs:
  - REQ-565
  - REQ-814
  - REQ-824
  - REQ-260
  - REQ-261
  - REQ-285
  - REQ-176
  - REQ-258
  stakeholders:
  - data-engineer
  - ops
- id: REQ-814
  status: complete
  group: 9. Live Data & Events
  category: Subscriptions
  priority: MUST
  type: behavioral
  description: 'Provider selection (_resolve_provider_type in provisa/api/data/subscribe.py + get_provider
    in provisa/subscriptions/registry.py) dispatches on `live.strategy`, NOT on source_type. Validation
    capability-gates strategy by source_type: warehouses → poll only; MongoDB → native/poll; PostgreSQL
    → native/debezium/poll/kafka; non-PG RDBMS (MySQL, MariaDB, SQL Server, Oracle) → debezium/poll/kafka
    (debezium/kafka require source-level cdc transport per REQ-824); Kafka topics → kafka only. No source
    is registered with a synthetic "debezium" type; strategy=debezium routes to the Debezium provider.'
  use_case: Dispatching on strategy instead of source_type correctly models that strategy is a choice
    of delta-capture mechanism (independent of where the data is queried from), not a data source type.
    Capability-gating per source_type prevents misconfiguration (e.g., native change streams on Snowflake).
  code:
  - provisa/api/data/subscribe.py
  - provisa/subscriptions/registry.py
  - provisa/core/config_loader.py
  - provisa/core/models.py
  tests:
  - tests/unit/test_source_cdc_config.py
  - tests/unit/test_live_delivery_config.py
  - tests/steps/steps_subscriptions.py
  since: 2026-07
  scenario: 'Given a PostgreSQL table with live.strategy=native

    When get_provider() is called

    Then PgNotificationProvider is instantiated

    And the source_type is not used to dispatch

    '
  related_reqs:
  - REQ-565
  - REQ-260
  - REQ-261
  - REQ-285
  stakeholders:
  - data-engineer
  - ops
- id: REQ-815
  status: complete
  since: 2026-07
  group: 11. Platform, Infrastructure & Delivery
  category: Docker Compose Organization
  priority: MUST
  type: infrastructure
  description: Telemetry sink services (otel-collector, prometheus, tempo, grafana, otlp2parquet) are
    organized into an independently start/stoppable compose project (provisa-observability) separate from
    the core runtime stack. The app degrades gracefully (fire-and-forget) when no OTLP collector is listening.
    MinIO base definitions move to docker-compose.core.yml; Trino and trino-worker remain core with observability-specific
    env/exporter overlays applied only when telemetry is enabled.
  use_case: Telemetry infrastructure is optional overhead for test and CI runs. Isolating telemetry sinks
    reduces startup time and resource consumption while ensuring app correctness does not depend on collector
    availability.
  code:
  - docker-compose.core.yml
  - docker-compose.observability.yml
  - provisa/telemetry/exporter.py
  related_reqs:
  - REQ-813
  - REQ-814
  stakeholders:
  - ops
  tests:
  - tests/unit/test_infra_requirements.py
- id: REQ-816
  status: complete
  since: 2026-07
  group: 11. Platform, Infrastructure & Delivery
  category: Docker Compose Organization
  priority: SHOULD
  type: infrastructure
  description: Demo/sample upstream sources (graphql-demo, petstore-mock) are organized into an independently
    start/stoppable compose project separate from the core runtime, categorized alongside other test-only
    services (neo4j, elasticsearch, kafka, schema-registry, fuseki, mongodb). Integration tests requiring
    these fixtures bring them up via harness markers rather than assuming they are already running in
    the dev stack.
  use_case: Demo sources serve as test fixtures for federation and remote-source integration tests. Organizing
    them with other test services ensures test harness controls their lifecycle and reduces assumptions
    about the dev environment state.
  code:
  - docker-compose.demo.yml
  - docker-compose.test.yml
  - tests/conftest.py
  tests:
  - tests/integration/test_openapi_pet_by_status.py
  - tests/integration/test_graphql_remote_integration.py
  related_reqs:
  - REQ-813
  - REQ-814
  stakeholders:
  - ops
  - data-engineer
- id: REQ-817
  status: complete
  group: 11. Platform, Infrastructure & Delivery
  category: Trino Memory Management
  priority: MUST
  type: infrastructure
  description: Trino memory overflow handling uses Fault-Tolerant Execution (FTE) with task.retry-policy=TASK
    and task.low-memory-killer.policy=total-reservation-on-blocked-nodes. Docker deployments use a shared
    named-volume filesystem exchange manager (trino/etc/exchange-manager.properties, /data/trino/exchange).
    Helm deployments require an S3/MinIO-backed exchange manager with a MinIO bucket-init Job or external
    S3 endpoint+credentials, enforced by a fail-loud guard.
  use_case: FTE replaces legacy spill-to-disk (deprecated in Trino 481) and mitigates noisy-neighbor OOM
    in multi-tenant deployments by isolating task memory failures and enabling graceful task retry.
  code:
  - docker-compose.core.yml
  - trino/etc/config.properties
  - trino/etc/worker/config.properties
  - trino/etc/exchange-manager.properties
  - helm/provisa/templates/trino-exchange-secret.yaml
  - helm/provisa/templates/trino-exchange-bucket-job.yaml
  tests:
  - tests/integration/test_trino_fte_exchange.py
  since: 2026-07
  stakeholders:
  - ops
  - data-engineer
- id: REQ-818
  status: complete
  since: 2026-07
  group: 5. Query Languages, Compilation & Operations
  category: Cypher Mutations
  priority: MUST
  type: behavioral
  description: Cypher supports WRITES via the /data/cypher endpoint. CREATE, DELETE, and SET statements
    execute as direct table writes through the same write pipeline as GraphQL and SQL mutations, with
    RLS injection, dialect transpilation, and all post-mutation hooks (response cache invalidation, MV
    stale marking, Kafka change events, Kafka sink triggers, hot-table reload — absorbs REQ-798). Non-direct
    write patterns (MERGE, DETACH, REMOVE) are unsupported and rejected. All writes are governed by table/view
    write rights like any other write. This supersedes REQ-346 (read-only constraint), which is now inaccurate.
  use_case: Write support enables graph users to mutate data via Cypher while maintaining governance consistency
    with SQL and GraphQL mutations.
  code:
  - provisa/api/rest/cypher_router.py
  - provisa/cypher/write_translator.py
  - provisa/cypher/parser.py
  tests:
  - tests/integration/test_cypher_endpoint.py
  - tests/unit/test_cypher_parser.py
  - tests/unit/test_cypher_translator.py
  related_reqs:
  - REQ-346
  - REQ-670
  - REQ-345
  - REQ-003
  target: 2026-Q3
  scenario: 'Given a valid CREATE statement targeting a table with write rights

    When executed via the /data/cypher endpoint

    Then it executes as a direct table write, returns affected_rows, and applies RLS + post-mutation hooks


    Given a MERGE or DETACH statement

    When parsed

    Then it is rejected at parse time with a precise error

    '
  stakeholders:
  - data-engineer
  - compliance
- id: REQ-819
  status: complete
  group: 9. Live Data & Events
  category: Live Delivery Configuration
  priority: MUST
  type: behavioral
  description: 'Per-table live delivery configuration is now configurable via the admin GraphQL API and
    admin UI (TablesPage). Configuration is persisted on registered_tables.live (JSONB column). Configuration
    includes: query_id (identifies the live query), watermark_column (tracks delivery progress), poll_interval
    (frequency in seconds), delivery mode ("poll" or "cdc"), and outputs array (SSE or Kafka sinks). Previously
    configuration was YAML-only.'
  use_case: UI-based configuration makes live delivery settings accessible to non-YAML-fluent operators
    and enables runtime changes without server restart.
  code:
  - provisa/api/admin/types.py
  - provisa/api/admin/schema.py
  - provisa/core/repositories/table.py
  - provisa/core/schema.sql
  - provisa-ui/src/pages/TablesPage.tsx
  tests:
  - tests/unit/test_live_delivery_config.py
  since: 2026-07
  scenario: 'Given the admin GraphQL API for table mutations

    When updateTable is called with live configuration (query_id, watermark_column, poll_interval, delivery,
    outputs)

    Then the configuration is persisted to registered_tables.live and the live engine is notified


    Given the admin UI TablesPage

    When an operator edits live config for a table

    Then changes are reflected in the database and take effect without server restart

    '
  stakeholders:
  - ops
  - data-engineer
- id: REQ-820
  status: complete
  group: 9. Live Data & Events
  category: Live Query Routing
  priority: MUST
  type: behavioral
  description: Live poll execution routes through Trino (federated query), not the PostgreSQL connection
    pool. Any federated SQL source with a watermark column is pollable. The engine's pg_pool is used only
    for watermark bookkeeping (live_query_state table). This enables polling over remote sources (not
    just PostgreSQL) via Trino's federation layer.
  use_case: Routing through Trino enables live polling of any federated SQL source (e.g., BigQuery, Snowflake,
    S3 via Iceberg) without embedding source-specific clients into the live engine.
  code:
  - provisa/live/engine.py
  - provisa/api/app.py
  tests:
  - tests/unit/test_live_engine.py
  since: 2026-07
  scenario: 'Given a live poll query targeting a federated BigQuery table

    When the poll interval triggers

    Then the query executes through Trino (not the PostgreSQL pool)

    And the watermark is persisted to live_query_state in PostgreSQL

    '
  stakeholders:
  - data-engineer
- id: REQ-821
  status: complete
  group: 9. Live Data & Events
  category: Live Delivery Mechanisms
  priority: MUST
  type: constraint
  description: PostgreSQL data is never polled for live delivery. PostgreSQL live delivery uses delivery=cdc
    (LISTEN/NOTIFY with database triggers). Poll delivery (delivery=poll) is reserved for non-push federated
    sources reached through Trino. This prevents duplicate load on PostgreSQL replication and uses push
    mechanisms where available.
  use_case: PostgreSQL's native LISTEN/NOTIFY is more efficient than polling. Restricting poll to federated
    sources avoids duplicate query load on the primary database.
  code:
  - provisa/live/engine.py
  - provisa/core/config_loader.py
  tests:
  - tests/unit/test_live_delivery_config.py
  since: 2026-07
  scenario: 'Given a registered table backed by PostgreSQL

    When live delivery is configured

    Then delivery must be set to "cdc", not "poll"

    And a parse-time or runtime validation rejects delivery=poll for PostgreSQL sources

    '
  stakeholders:
  - data-engineer
  - ops
- id: REQ-822
  status: complete
  group: 9. Live Data & Events
  category: Live Delivery Mechanisms
  priority: MUST
  type: constraint
  description: 'delivery=cdc is restricted to source types with a real push provider in the subscription
    registry: postgresql (LISTEN/NOTIFY triggers), debezium and kafka (Kafka consumers), mongodb (change
    streams). All other sources default to delivery=poll. CDC connection config is inherited from the
    source definition, never per-table. This ensures CDC is only used when a push mechanism is available.'
  use_case: Restricting CDC to sources with native push mechanisms prevents attempts to use CDC on sources
    that don't support it. Connection config inheritance avoids credential duplication per table.
  code:
  - provisa/core/config_loader.py
  - provisa/subscriptions/registry.py
  tests:
  - tests/unit/test_live_delivery_config.py
  since: 2026-07
  scenario: 'Given a source of type S3 (no native push mechanism)

    When live delivery config is created with delivery=cdc

    Then it is rejected or silently downgraded to delivery=poll


    Given a PostgreSQL source with connection credentials

    When CDC is enabled on any table from that source

    Then the CDC provider uses the source''s connection config, not per-table overrides

    '
  stakeholders:
  - data-engineer
  - ops
- id: REQ-823
  status: complete
  group: 9. Live Data & Events
  category: Live Delivery Configuration
  priority: MUST
  type: behavioral
  description: The LiveEngine reconciles poll jobs from the database at startup and after every admin
    mutation via _rebuild_schemas(). This ensures that admin-set live config takes effect without server
    restart. Configuration changes trigger immediate engine reconciliation.
  use_case: Startup reconciliation catches live jobs even if the engine crashes mid-execution. Admin mutations
    trigger immediate reconciliation so operators see changes reflected in real-time without restart overhead.
  code:
  - provisa/live/engine.py
  - provisa/api/app.py
  tests:
  - tests/unit/test_live_engine.py
  - tests/unit/test_live_delivery_config.py
  since: 2026-07
  scenario: 'Given live config stored in registered_tables.live

    When the LiveEngine starts

    Then it queries the database for all active live configs and rebuilds poll jobs


    Given live config modified via admin GraphQL API

    When the mutation completes

    Then _rebuild_schemas() is called to reconcile the engine immediately

    And the new poll schedule takes effect without restart

    '
  stakeholders:
  - ops
  - data-engineer
- id: REQ-824
  status: complete
  group: 9. Live Data & Events
  category: Live Delivery Mechanisms
  priority: MUST
  type: behavioral
  since: 2026-07
  description: Source-level CDC transport configuration. Debezium/Kafka delta-transport (bootstrap_servers,
    topic_prefix, schema_registry_url, consumer_group_id) is entered ONCE per source via a `cdc` block
    on the Source model (persisted to sources.cdc JSONB), never repeated per-table. Per-table live config
    only selects delivery=cdc; the runtime inherits transport from the source. A source-level cdc block
    is only valid on CDC-capable RDBMS sources (postgresql uses native LISTEN/NOTIFY and needs no transport;
    mysql/mariadb/sqlserver/oracle require it to enable delivery=cdc via Debezium). Provider dispatch
    routes non-PG RDBMS sources with a cdc block to the Debezium provider. Polling remains per-table (watermark_column,
    poll_interval).
  use_case: Eliminates repeated data entry of Debezium/Kafka connection details across every table of
    a source; transport is a per-source connector property, not a per-table one.
  code:
  - provisa/core/models.py
  - provisa/core/config_loader.py
  - provisa/core/repositories/source.py
  - provisa/core/schema.sql
  - provisa/api/app.py
  - provisa/api/data/subscribe.py
  - provisa/api/admin/types.py
  - provisa/api/admin/schema.py
  - provisa-ui/src/liveCapability.ts
  - provisa-ui/src/pages/SourcesPage.tsx
  - provisa-ui/src/types/admin.ts
  - provisa-ui/src/hooks/admin.graphql
  tests:
  - tests/unit/test_source_cdc_config.py
  - provisa-ui/src/__tests__/liveCapability.test.ts
  scenario: 'Given a MySQL source with a source-level cdc block (bootstrap_servers, topic_prefix)

    When a table from that source sets live.delivery=cdc

    Then validation passes and the runtime routes the subscription to the Debezium provider using the
    source''s transport


    Given a MySQL source WITHOUT a cdc block

    When a table sets live.delivery=cdc

    Then config validation rejects it (no push mechanism)


    Given a warehouse source (e.g. snowflake)

    When a cdc block is set on it

    Then validation rejects the cdc block as unsupported for that source type

    '
  related_reqs:
  - REQ-813
  - REQ-814
  - REQ-819
  - REQ-821
  - REQ-822
  - REQ-565
- id: REQ-825
  status: complete
  since: 2026-07
  group: 6. Execution, Routing, Caching & Performance
  category: Execution & Routing
  priority: SHOULD
  type: structural
  description: 'The Provisa FederationEngine is a pluggable, abstract execution substrate behind a single
    contract. It is one of four primitives composed as a data-flow pipeline: DATASOURCES → FEDERATION
    ENGINE → SEMANTIC LAYER → EXECUTION PIPELINE. DATASOURCES are external systems with a capability profile.
    The FEDERATION ENGINE (this requirement) federates those datasources into one raw unified queryable
    surface; the method per source is the federation strategy (REQ-826). The SEMANTIC LAYER is the governed
    virtual model projected over that surface — registered tables/columns, relationships, domains, RLS,
    masking — engine-independent; it is where governance is DEFINED and the thing queries target. The
    EXECUTION PIPELINE turns a query into results as a sequence of total IR→IR passes (nanopass discipline),
    bookended by transport: (1) TRANSPORT ingress — receive the query over the client protocol (pgwire,
    Bolt, Arrow Flight, GraphQL, SQL); (2) TRANSPILE TO IR — frontend parse of the source language into
    a dialect-neutral logical IR (plural frontends converge here); (3) TRANSPILE TO GOVERNED IR — apply
    the semantic layer: inject RLS predicates, masking expressions, and relationship joins (engine-independent;
    the IP pass); (4) PLAN — the one IMPURE stage: not a pure transpile but a planner that (a) chooses
    the ROUTE (direct native-driver execution for a single reachable source vs handing the query to the
    federation engine), (b) resolves RESIDENCY PREREQUISITES — for each source whose federation strategy
    is MATERIALIZED and stale, emit a side-effecting prep step that loads/refreshes it into the DB cache
    before execute (REQ-826), and (c) CODEGENs the engine-dialect SQL via SQLGlot for the chosen route.
    The output is not a string but an ordered plan: a prep phase (0..n cache materializations, may be
    empty) feeding a single terminal step (DIRECT driver or federation-engine execute); this is where
    today''s router (DIRECT/TRINO/API) and the cache tiers (hot/warm/MV) converge. (5) EXECUTE — the federation
    engine runs the terminal step and does its own opaque physical planning (NOT Provisa''s plan); (6)
    TRANSPORT egress — stream results back over the same client protocol. Stages 1–3 are pure and engine-independent;
    stage 4 reads mutable freshness state and emits effects, and stages 4–5 vary per engine — so governance-parity
    conformance (REQ-827) must PIN residency (pre-warm the prep phase) and then test only the codegen
    + execute. No governance or compilation logic is duplicated per engine — governance is defined in
    the semantic layer and ENFORCED at stage 3; the engine only executes.

    There is no "federation vs warehouse" dichotomy: EVERY engine federates; only the METHOD of federation
    changes with capability (see REQ-826). An engine advertises a capability profile with two axes: (a)
    REACH — for each datasource, which federation strategies it can support; and (b) SCALE — single-node/memory-bound
    vs distributed. The profile RANKS transports per source (native > ADBC > ODBC), not a has_connectors
    boolean.

    Concrete engines: DuckDB is a full federation engine whose reach nearly matches Trino''s — native
    ATTACH for postgres/mysql/sqlite; ODBC (community nanodbc odbc_attach, or the DuckDB-team odbc/odbc_scanner
    functions) for any ODBC source; ADBC (community adbc_scanner, Arrow-native, preferred over ODBC);
    NoSQL via community extensions (mongo ATTACH TYPE MONGO, cassandra scan/query/ATTACH, elasticsearch_query);
    and zero-copy file scans. Its ONLY differentiator vs Trino is SCALE (single-node, memory-bound, no
    distributed shuffle), making it ideal on a developer desktop with no Trino dependency. Trino has broad
    reach and distributed scale. Snowflake/Databricks/Dremio are reused as engines to leverage a customer''s
    existing infrastructure. A pure RDBMS (e.g. SQL Server) has near-zero reach and federates only by
    the materialized strategy. The engine is selected at deployment via top-level config (engine: duckdb
    | trino | snowflake | databricks | dremio | ...), not per-source. All query dispatch that today reads
    the hardcoded Trino singleton (provisa/api/app.py, provisa/api/data/endpoint.py, provisa/executor/trino.py)
    must route through the FederationEngine contract. The IR->physical mapping is intentionally thin —
    the divergence is confined to dialect transpilation and the per-source federation strategy (REQ-826).
    DONE (2026-07): the PLAN stage OUTPUT is modelled — provisa/federation/plan.py::build_execution_plan
    composes the pure part of stage 4 into an ordered Plan: a prep phase (0..n PrepStep residency loads/refreshes,
    one per source whose federate() strategy is MATERIALIZED and stale) feeding a single terminal Route
    (DIRECT for a single VIRTUAL source, ENGINE otherwise). This is the four-primitive flow''s planner
    surface built on the connector/federate contract (REQ-840/826). REMAINING (kept in-progress): the
    other pipeline stages as engine-agnostic passes (transport ingress/egress, transpile to IR, transpile
    to governed IR, codegen, execute) and routing the hardcoded-Trino dispatch through the FederationEngine
    contract.'
  use_case: 'Decouples Provisa''s governed semantic layer from any single execution vendor. The same semantic
    layer, execution pipeline, governance, IR, and multi-protocol query surface run on an embedded desktop
    engine (DuckDB, no Trino), the Trino federation engine, or a customer''s existing warehouse (Snowflake/Databricks/Dremio)
    — making the engine a swappable deployment choice and concentrating Provisa''s IP in the semantic
    layer, pipeline, governance, and federation-strategy logic rather than the engine. Intended model:
    develop on DuckDB (laptop, zero infra, full governance and full source reach), then scale out to Trino/Snowflake/Databricks
    in production, unchanged.'
  code:
  - provisa/federation/plan.py
  - provisa/api/app.py
  - provisa/api/data/endpoint.py
  - provisa/executor/trino.py
  - provisa/transpiler/router.py
  - provisa/core/config_loader.py
  tests:
  - tests/unit/test_federation_plan.py
  related_reqs:
  - REQ-826
  - REQ-830
- id: REQ-826
  status: complete
  since: 2026-07
  group: 6. Execution, Routing, Caching & Performance
  category: Caching
  priority: SHOULD
  type: behavioral
  description: 'Federation strategy and freshness management — the datasources → federation engine arrow
    of REQ-825. The binding between a datasource table and the engine is a FEDERATION STRATEGY: the method
    by which that source joins the engine''s unified surface. federate(datasource, table) resolves each
    table to one of three strategies and returns which was chosen, because freshness differs per strategy:
    (1) VIRTUAL — the engine reaches the source live (a connector, or DuckDB ATTACH to postgres/mysql/sqlite,
    or a Trino connector); no copy, always fresh, cache_ttl irrelevant. (2) SCAN — the source is a file/object
    the engine reads in place, exposed as a CREATE VIEW with no data moved; freshness follows the underlying
    file, cache_ttl irrelevant. For DuckDB this spans read_parquet/read_csv/read_json, httpfs over S3/HTTP/GCS/Azure,
    Iceberg/Delta via extension, ATTACH of external DuckDB/SQLite, and other scanner extensions; the engine
    declares what it can scan. (3) MATERIALIZED — no virtual or scan representation (live APIs, NoSQL,
    or an RDBMS deliberately cached for latency); data is INSERTed/COPYed in. MATERIALIZED is the ONLY
    strategy where cache_ttl means a reload interval and the load-scheduling/invalidation logic runs;
    VIRTUAL and SCAN are effectively free.

    The federation strategy is STATEFUL — it owns residency, reload state, and freshness; the execution
    pipeline (transpile → transport) is stateless per query and must not carry this state. The chosen
    strategy depends on BOTH datasource capability and engine capability, so the same source may federate
    by different strategies on different engines. This unifies today''s three separate mechanisms — OpenAPI
    PG response cache (provisa/openapi/pg_cache.py), NoSQL mapping catalogs, and warm-tables/MV materialization
    — into one federate() operation. NoSQL scanners (mongo, cassandra, elasticsearch) infer schema on
    read; because governance in the semantic layer binds to columns, federate() MUST pin the schema on
    the CREATE VIEW from the semantic layer''s mapping (REQ-251), never trust re-inference. The router
    must not attempt a live/VIRTUAL route when federate() reports only a MATERIALIZED strategy for that
    source; it uses the strategy federate() returns. Only the TENANT MATERIALIZATION STORE (durable —
    API/GraphQL/gRPC results, warm tables, MVs; REQ-830) is a true MATERIALIZED strategy: the data becomes
    a real relation the engine reads, so it PARTICIPATES in federation (joinable/scannable like any source).
    The TENANT HOT CACHE is NOT a federation strategy and is orthogonal to virtual|scan|materialized —
    it is a codegen-time VALUES-CTE injection inlined into the SQL at the PLAN stage, never an engine
    relation and never federated; tier exclusivity (REQ-241) chooses hot over materialization store. DONE
    (2026-07): federate(source, engine) is implemented (provisa/federation/strategy.py), resolving each
    source to VIRTUAL | SCAN | MATERIALIZED and returning which was chosen. The choice depends on BOTH
    source capability and engine capability: an ATTACH connector on a live DB → VIRTUAL, an ATTACH connector
    on a file/object type (csv/parquet/iceberg/...) → SCAN, a LAND connector (warehouse-native) → MATERIALIZED,
    and an API/NoSQL source with no connector → MATERIALIZED (loaded into the store). The same source
    therefore federates differently per engine (csv SCANs on DuckDB but is UnreachableSource on a Trino
    build without a csv connector). prefer_materialized forces MATERIALIZED for a live source deliberately
    cached for latency. requires_residency(strategy) is the REQ-825 stage-4b signal (only MATERIALIZED
    needs a prep step). REMAINING (kept in-progress): the STATEFUL side — wiring residency / reload-scheduling
    / invalidation and unifying the three existing mechanisms (openapi pg_cache, NoSQL mapping catalogs,
    warm-tables/MV) behind this one federate() operation, and having the router consume the returned strategy
    — lands with the materialization store (REQ-844-848/830) and the PLAN stage.'
  use_case: Concentrates the real complexity of federation — strategy selection, residency, reload scheduling,
    and invalidation — in one stateful federate() operation, so any datasource (RDBMS, files, NoSQL, APIs)
    joins the engine's surface by the best available strategy with predictable freshness, independent
    of which engine is deployed.
  code:
  - provisa/federation/strategy.py
  - provisa/federation/engine.py
  - provisa/executor/trino.py
  - provisa/transpiler/router.py
  - provisa/openapi/pg_cache.py
  - provisa/mv/registry.py
  - provisa/cache/warm_tables.py
  - provisa/cache/hot_tables.py
  - provisa/core/config_loader.py
  tests:
  - tests/unit/test_federation_strategy.py
  scenario: 'Given an engine that can only materialize an OpenAPI datasource

    When a query references that source

    Then federate() returns the MATERIALIZED strategy, loads/refreshes the data, and the query reads it
    in place

    And the router does not attempt a live/VIRTUAL route


    Given a MATERIALIZED strategy with cache_ttl=300

    When the table has not been reloaded within 300 seconds

    Then federate() reloads it before the query executes (cache_ttl means reload interval)


    Given an engine that can ATTACH an RDBMS datasource live

    When a query references that source

    Then federate() returns the VIRTUAL strategy and the query reads it live with no copy


    Given a NoSQL datasource federated by SCAN or MATERIALIZED

    When the view is created

    Then the schema is pinned from the semantic layer mapping, not inferred

    '
  related_reqs:
  - REQ-825
  - REQ-827
  - REQ-830
- id: REQ-827
  status: complete
  since: 2026-07
  group: 6. Execution, Routing, Caching & Performance
  category: Execution & Routing
  priority: MUST
  type: constraint
  description: 'Governance-parity conformance across federation engines. Any engine admitted to the FederationEngine
    contract (REQ-825) MUST produce byte-identical governed results — RLS filtering, column masking, relationship-join
    enforcement, and row/aggregate output — as the reference engine for the same query and same identity.
    Because governance is defined in the semantic layer and enforced when the query compiles to IR, only
    the transpile → transport stages differ per engine; SQLGlot transpiles syntax, but semantic edges
    still differ per engine (NULL ordering, type coercion, decimal/numeric precision, regex and collation
    semantics, timezone and timestamp handling, string comparison case sensitivity), and these must not
    change which rows are visible, which cells are masked, or which joins are enforced. An engine is certified
    only by passing a shared governance-parity conformance suite run against a fixed golden dataset and
    identity matrix; results are diffed against the reference engine and any divergence fails certification.
    This is a hard gate: an uncertified engine MUST NOT be selectable in production config (REQ-825).
    The suite is authoritative for DuckDB, Trino, Snowflake, Databricks, Dremio, and any future engine.
    DONE (2026-07): the comparator + certification GATE are implemented — provisa/federation/conformance.py::
    compare_governed_results diffs a candidate engine''s governed rows against the reference as an order-INDEPENDENT
    multiset (NULL ordering is an allowed semantic edge) but visibility/masking-EXACT: a row visible in
    exactly one side (an RLS leak or over-filter) or a differently-masked cell is a Divergence, and any
    divergence fails certification. ConformanceRegistry tracks certified engines (the reference is always
    certified) and require_certified raises UncertifiedEngineError — the hard gate that an uncertified
    engine must not be selectable. REMAINING (kept in-progress): the conformance SUITE itself — the fixed
    golden dataset + identity matrix and the runner that executes each query on every candidate engine
    and feeds the diff — which needs the live multi-engine execution paths.'
  code:
  - provisa/federation/conformance.py
  - provisa/security/
  - provisa/compiler/
  - provisa/executor/trino.py
  - provisa/transpiler/router.py
  tests:
  - tests/unit/test_conformance.py
  - tests/integration/test_governance_integration.py
  - tests/integration/test_governance_parity_e2e.py
  scenario: 'Given a fixed golden dataset and an identity with role "analyst" subject to RLS and column
    masking

    When the same query is executed on the reference engine and on a candidate engine (DuckDB, Snowflake,
    Databricks, Dremio, or Trino)

    Then the returned rows, masked cells, and enforced relationship joins are byte-identical between engines


    Given a candidate engine whose semantic differences (NULL ordering, type coercion, collation) change
    which rows are visible or which cells are masked

    When the governance-parity conformance suite is run

    Then the engine fails certification and MUST NOT be selectable in production config

    '
  related_reqs:
  - REQ-825
  - REQ-826
- id: REQ-828
  status: complete
  since: 2026-07
  group: 11. Platform, Infrastructure & Delivery
  category: Deployment & Infrastructure
  priority: SHOULD
  type: structural
  description: 'Pluggable admin/metadata store, decoupled from Postgres, selected via a SQLAlchemy-compliant
    URI. The control-plane store (sources, domains, registered tables, columns, naming rules, governance
    config) plus the OpenAPI/GraphQL cache substrate must run on an embedded engine (DuckDB or SQLite)
    with zero external infra on a developer desktop, and on Postgres in production — same schema, same
    behavior. This is required for the desktop deployment model in REQ-825 (PG today requires Docker,
    which breaks the zero-infra laptop story). Three couplings must be removed: (1) DRIVER — repositories
    take asyncpg.Connection directly (provisa/core/repositories/source.py) and the pool factory is asyncpg-only
    (provisa/core/db.py); these move behind a store abstraction driven by a SQLAlchemy URI. (2) DIALECT
    — schema.sql uses Postgres-specific DDL (SERIAL, JSONB); metadata DDL must be dialect-neutral. (3)
    META-GOVERNANCE RLS — the hard gate: _init_meta_rls (provisa/api/app.py, REQ-041/REQ-402) enforces
    control-plane governance via Postgres row-level security, which DuckDB/SQLite lack. Meta-RLS enforcement
    MUST move from the database into the app layer so control-plane governance is store-independent (the
    same parity principle as REQ-827, applied to the admin DB). Until meta-RLS is app-enforced, embedded
    stores cannot be certified for production. On desktop the OpenAPI/GraphQL cache lands in the embedded
    store rather than a Docker Postgres; where Postgres exists (prod) the existing pg_cache path is unchanged
    and DuckDB may ATTACH it (REQ-826). This store is the TENANT CONTROL PLANE REGISTRY (REQ-830) — per-tenant,
    distinct from the PLATFORM CONTROL PLANE REGISTRY (tenant directory, federation-engine registry, platform
    governance). The meta-RLS move generalizes to a single app-layer tenant-isolation boundary enforced
    across all tenant-scoped stateful components, since swappable media (Redis, DuckDB, S3) have no native
    RLS.'
  use_case: Makes the full Provisa deployment — control plane and cache, not just the query engine — infra-free
    on a developer laptop, completing the develop-on-DuckDB / scale-out-in-prod model in REQ-825 without
    requiring Docker or an external Postgres.
  code:
  - provisa/core/meta_rls.py
  - provisa/core/db.py
  - provisa/core/database.py
  - provisa/core/duckdb_async.py
  - provisa/core/repositories/source.py
  - provisa/core/schema.sql
  - provisa/core/schema_org.py
  - provisa/api/app.py
  - provisa/openapi/pg_cache.py
  tests:
  - tests/unit/test_admin_store.py
  related_reqs:
  - REQ-825
  - REQ-826
  - REQ-827
  - REQ-830
- id: REQ-829
  status: complete
  since: 2026-07
  group: 11. Platform, Infrastructure & Delivery
  category: Deployment & Infrastructure
  priority: SHOULD
  type: structural
  description: 'Pluggable Redis medium with a lightweight, code-identical embedded realization for desktop.
    Sibling to REQ-828: where REQ-828 makes the SQL-store medium of the TENANT CONTROL PLANE REGISTRY
    pluggable (Postgres -> DuckDB/SQLite), this makes the Redis medium pluggable (Redis -> embedded fakeredis),
    so both halves of the REQ-830 desktop collapse (stateful components 1-4 into one process, zero infra)
    are covered. Redis is REQ-830''s named production medium for the TENANT HOT CACHE (component #4, hot
    tables REQ-230, provisa/cache/hot_tables.py) and additionally backs the fast result/APQ caches (provisa/cache/store.py,
    provisa/apq/cache.py) and the Redis-runtime governance state that is orthogonal to the five data-flow
    components — the sliding-window rate limiter and concurrency gauge (provisa/api/rate_limit.py) and
    the table-based cache-invalidation index (provisa/cache/store.py). When no REDIS_URL is configured,
    every one of these MUST transparently use an embedded fakeredis (FakeAsyncRedis) client that is API-identical
    to redis.asyncio.Redis. All four current connection factories (rate_limit.py:120, cache/store.py:138,
    apq/cache.py:97, cache/hot_tables.py:174) route through a single make_redis(url, decode_responses)
    factory that returns redis.asyncio.from_url when a URL is set and FakeAsyncRedis otherwise, sharing
    one process-wide fakeredis.FakeServer so the four clients (which differ in decode_responses) see the
    same in-memory store. Zero changes to existing call sites. TENANT ISOLATION for the Redis medium is
    enforced in the app layer via key namespacing (existing tenant_id key prefixes: provisa:cache:<tenant_id>:...,
    provisa:apq:<tenant_id>:...), NOT any store-native RLS — the same app-layer isolation boundary REQ-828/REQ-830
    mandate for all swappable media (Redis, DuckDB, S3); the single shared FakeServer is acceptable because
    desktop is single-tenant. The stub is not a KVS: it MUST support the full command surface actually
    invoked — get/set/delete, expire/TTL, sorted sets (zadd/zcard/zremrangebyscore/zrange for the sliding-window
    rate limiter), sets (sadd/smembers/scan_iter for the invalidation index), incr/decr (concurrency gauge),
    and pipelines. An aiocache/cashews-style KVS abstraction is explicitly rejected because it cannot
    express the sorted-set and set operations without rewriting rate limiting and invalidation, and the
    pure get/set caches it could serve already degrade to no-ops without Redis, so it buys almost nothing.
    Rationale — desktop already runs without Redis today, but silently: build_rate_limiter(None) returns
    NoopRateLimiter (rate_limit.py:118) and NoopCacheStore no-ops invalidate_by_table/invalidate_by_pattern
    (store.py:93-96), so rate limiting and cache invalidation are simply disabled and those code paths
    are never exercised on the laptop. Yet both surfaces are live in production: rate limiting runs on
    every HTTP request (rate_limit_middleware.py:28) plus Arrow Flight, subscriptions, and NL endpoints
    (flight/server.py:248, data/subscribe.py:404, nl_router.py:58), and table invalidation fires after
    every write (cypher_router.py:560, data/endpoint.py:3307, admin/schema.py:2458). The purpose is therefore
    NOT to add missing Redis features but to make desktop exercise the SAME hot-cache, result-cache, and
    rate-limit code paths as production instead of silently falling back to no-ops — so that behavior
    is testable on a laptop with zero infra. Production with a real REDIS_URL is unchanged; in production
    the same medium scales out to Redis (REQ-830). Completes the infra-free desktop model (REQ-825/828/830)
    by removing Redis as the last mandatory Docker dependency alongside the SQL store.'
  use_case: Lets a developer run the full Provisa backend on a laptop with no Redis server and no Docker,
    collapsing the Redis medium of the TENANT HOT CACHE and Redis-runtime state into an embedded fakeredis
    while exercising the identical hot-cache, result-cache, APQ, rate-limit, and invalidation code paths
    that production runs against a real Redis — rather than the current silent no-op fallback — so behavior
    is the same, and testable, in both environments, with tenant isolation enforced in the app layer.
  code:
  - provisa/core/redis_factory.py
  - provisa/api/rate_limit.py
  - provisa/cache/store.py
  - provisa/apq/cache.py
  - provisa/cache/hot_tables.py
  tests:
  - tests/unit/test_redis_factory.py
  related_reqs:
  - REQ-825
  - REQ-826
  - REQ-828
  - REQ-830
  - REQ-831
- id: REQ-830
  status: complete
  since: 2026-07
  tests:
  - tests/unit/test_arch_invariants.py
  group: 11. Platform, Infrastructure & Delivery
  category: Deployment & Infrastructure
  priority: SHOULD
  type: structural
  target: 2026-Q4
  description: 'Stateful-component topology. The four data-flow primitives (datasources -> federation
    engine -> semantic layer -> execution pipeline; REQ-825) describe a STATELESS per-query flow. Orthogonal
    to them, a deployment holds FIVE PLUGGABLE STATEFUL COMPONENTS, each independently swappable and —
    except the platform registry — TENANT-ISOLATED: (1) PLATFORM CONTROL PLANE REGISTRY — platform-scoped,
    shared: tenant directory, federation-engine registry, platform-level governance (REQ-041/REQ-402),
    and per-tenant routing (which registry/caches/engine each tenant uses). (2) TENANT CONTROL PLANE REGISTRY
    — per-tenant: persists that tenant''s datasource definitions and semantic layer (registered tables/columns,
    relationships, domains, RLS, masking, naming); the store REQ-828 makes pluggable. (3) TENANT MATERIALIZATION
    STORE — per-tenant durable store realizing the MATERIALIZED federation strategy (REQ-826): API/GraphQL/gRPC
    result caches, warm tables, MVs; disk/object-backed. It is a real relation the federation engine reads,
    so materialized/MV data PARTICIPATES in federation (joinable/scannable like any source). (4) TENANT
    HOT CACHE — per-tenant fast tier realizing hot tables (REQ-230): small, low-latency, ephemeral. STRICTLY
    a CTE-injection mechanism — its data is inlined as a VALUES CTE into the generated SQL at codegen
    and NEVER becomes an engine relation, so it does NOT participate in federation (cannot be joined or
    scanned by the engine as a table) and is orthogonal to the virtual|scan|materialized strategies. Tier
    exclusivity (REQ-241) chooses it over the materialization store. (5) FEDERATION ENGINE — the execution
    substrate (REQ-825); tenant-dedicated or shared. Each is independently pluggable: on a developer desktop
    components 1-4 collapse into one embedded engine (DuckDB/SQLite) and #5 is DuckDB (zero infra); in
    production they scale out independently (platform registry in Postgres, tenant registries sharded,
    materialization store in Iceberg/S3, hot cache in Redis, engine in Trino/Snowflake). Tenant isolation
    for components 2-5 MUST be enforced in the app layer (REQ-828), not delegated to any single store''s
    native RLS, so isolation holds across every medium.'
  use_case: Names the stateful components multi-tenancy must isolate and deployment must place, separating
    them from the stateless execution pipeline, and makes each independently pluggable so the same platform
    collapses to zero-infra on a laptop and scales each tier independently in production with tenant isolation
    enforced uniformly in the app layer.
  code:
  - provisa/api/app.py
  - provisa/core/db.py
  - provisa/core/repositories/source.py
  - provisa/openapi/pg_cache.py
  - provisa/cache/hot_tables.py
  - provisa/cache/warm_tables.py
  - provisa/mv/registry.py
  related_reqs:
  - REQ-825
  - REQ-826
  - REQ-828
  - REQ-829
  - REQ-831
- id: REQ-831
  status: complete
  group: 11. Platform, Infrastructure & Delivery
  category: Deployment & Infrastructure
  priority: SHOULD
  type: infrastructure
  since: 2026-07
  description: Demo datasources run as host processes with no Docker. The demo GraphQL service (Strawberry
    + Starlette ASGI, demo/graphql_server/server.py, port 4000) and the demo OpenAPI petstore service
    (Starlette ASGI, demo/petstore_server/server.py, port 18080) are launched as background uvicorn subprocesses
    by demo/run-demo-servers.sh {start|stop}, replacing the former docker-compose services (swaggerapi/petstore3
    'petstore-mock' and the built 'graphql-demo' container in docker-compose.demo.yml). start-ui.sh --demo
    starts and stops them and exports PETSTORE_BASE_URL=http://localhost:18080/api/v3 and GRAPHQL_DEMO_URL=http://localhost:4000/graphql;
    config/provisa.yaml reads those env vars with defaults that preserve the old Docker service names
    for containerized runs. This removes the demo-source Docker dependency for local/desktop development,
    advancing the zero-infra deployment model (REQ-829, REQ-830).
  use_case: Lets a developer run the full demo (governed GraphQL + OpenAPI sources) on a laptop without
    Docker, consistent with the embedded-engine / embedded-store zero-infra desktop model.
  code:
  - demo/run-demo-servers.sh
  - demo/graphql_server/server.py
  - demo/petstore_server/server.py
  - start-ui.sh
  - start-ui-install.sh
  - config/provisa.yaml
  related_reqs:
  - REQ-829
  - REQ-830
  tests:
  - tests/unit/test_demo_servers.py
- id: REQ-832
  status: complete
  group: 11. Platform, Infrastructure & Delivery
  category: Data Model & Persistence
  priority: MUST
  type: structural
  since: 2026-07
  description: 'Control-plane database is accessed through a SQLAlchemy-Core abstraction (provisa/core/database.py:
    Database/Connection/Row) backed by an AsyncEngine, replacing raw asyncpg. Connection pool replaces
    asyncpg.create_pool with equivalent behavior.'
  use_case: Enables multi-database support (PostgreSQL Tier-1, SQLite/MySQL Tier-2) by centralizing SQL
    dialect translation and connection management in one module.
  code:
  - provisa/core/database.py
  tests:
  - tests/unit/test_admin_database.py
- id: REQ-833
  status: complete
  group: 11. Platform, Infrastructure & Delivery
  category: Data Model & Persistence
  priority: MUST
  type: structural
  since: 2026-07
  description: Control-plane data model is split into two SQLAlchemy MetaData registries—platform control
    plane (orgs, user_profiles, user_org_memberships, local_users, org_invites, tenants, tenant_config)
    and per-org tenant control plane (sources, registered_tables, table_columns, relationships, roles,
    rls_rules, domains, materialized_views, kafka_*, api_*, creation_requests, tracked_functions/webhooks,
    live_query_state, node_ids, rel_ids, query_audit_log, etc.). Exposed at runtime as state.admin_db
    and state.org_db.
  use_case: Separates multi-tenant platform metadata from per-tenant schema, enabling per-tenant state
    isolation and independent schema evolution.
  code:
  - provisa/core/schema_admin.py
  - provisa/core/schema_org.py
  tests:
  - tests/unit/test_schema_metadata_parity.py
- id: REQ-834
  status: complete
  group: 11. Platform, Infrastructure & Delivery
  category: Data Model & Persistence
  priority: MUST
  type: constraint
  since: 2026-07
  description: Portable-type policy for cross-backend support maps PG-specific types to portable equivalents
    (JSONB/TEXT[] → JSON, SERIAL/BIGSERIAL → Integer/BigInteger identity, TIMESTAMPTZ → DateTime(timezone),
    UUID → Uuid, BYTEA → LargeBinary). Dialect-aware upsert() and insert_returning() helpers replace PG-only
    ON CONFLICT/RETURNING.
  use_case: Ensures Tier-1 PostgreSQL, Tier-2 SQLite ≥3.35, and Tier-2 MySQL 8 can all execute the same
    control-plane schema without migration.
  code:
  - provisa/core/database.py
  tests:
  - tests/unit/test_admin_database.py
- id: REQ-835
  status: complete
  group: 11. Platform, Infrastructure & Delivery
  category: Data Model & Persistence
  priority: MUST
  type: constraint
  since: 2026-07
  description: A Capabilities flag object gates database features by dialect—LISTEN/NOTIFY, advisory locks,
    arrays, append-only RULEs, RETURNING are PG-only and must not be used on SQLite or MySQL backends.
  use_case: Prevents runtime errors when control plane is deployed on non-PostgreSQL backends by enforcing
    compile-time feature gating.
  code:
  - provisa/core/database.py
  tests:
  - tests/unit/test_admin_database.py
- id: REQ-836
  status: complete
  group: 11. Platform, Infrastructure & Delivery
  category: Docker Composition & Deployment
  priority: MUST
  type: infrastructure
  since: 2026-07
  description: 'Trino FTE exchange spooling directory (provisa_exchange Docker named volume, mounted at
    /data/provisa/exchange) must be owned by uid 1000 before Trino starts. A trino-exchange-init busybox
    one-shot service in docker-compose.core.yml chowns the volume and runs before both trino coordinator
    and trino-worker via condition: service_completed_successfully.'
  use_case: Trino runs as uid 1000 but freshly-created named volumes are owned root:root, causing write
    failures and GENERIC_INTERNAL_ERROR on every FTE query (retry-policy=TASK). Pre-initialization via
    service dependency ensures filesystem readiness.
  code:
  - docker-compose.core.yml
  tests:
  - tests/unit/test_trino_exchange_init.py
  - tests/integration/test_trino_fte_exchange.py
- id: REQ-837
  status: complete
  group: 11. Platform, Infrastructure & Delivery
  category: Data Model & Persistence
  priority: MUST
  type: structural
  since: 2026-07
  description: 'Platform control plane and tenant control plane use two independent SQLAlchemy engines
    and connection pools (state.admin_db for platform registry; state.org_db for per-tenant data). Platform
    database is configured by PLATFORM_DATABASE_URL env var (full SQLAlchemy URI supporting any async
    backend: postgresql+asyncpg, sqlite+aiosqlite, mysql+aiomysql); required at startup with no fallback.'
  use_case: Enables deployment models where platform registry and per-tenant data live in separate databases
    on different backends (platform on SQLite/MySQL, tenant on PostgreSQL, or vice versa), allowing independent
    lifecycle and scaling.
  code:
  - provisa/core/database.py
  - provisa/core/schema_admin.py
  - provisa/api/app.py
  - provisa/auth/wiring.py
  - provisa/auth/middleware.py
  - provisa/auth/providers/basic.py
  tests:
  - tests/unit/test_platform_tenant_engines.py
- id: REQ-838
  status: complete
  group: 11. Platform, Infrastructure & Delivery
  category: Data Model & Persistence
  priority: MUST
  type: constraint
  since: 2026-07
  description: 'Tenant control-plane isolation is a CONTRACT, not a mechanism: within a tenant-scoped
    control-plane connection (state.org_db bound to one org_id), no other org''s rows are reachable —
    reads and writes see only the connecting org''s data, and cross-org access requires explicitly binding
    a different org. The ENFORCEMENT MECHANISM is backend-relative, selected by the control-plane store''s
    dialect and never assumed to be PostgreSQL-specific: PostgreSQL realizes it with a per-connection
    SET search_path to org_<org_id> plus a per-org role (physical schema isolation); single-tenant portable
    backends (SQLite / MySQL bootstrapped from schema_org) realize it as the sole default schema of a
    per-tenant database; a backend offering row-security may realize it via a tenant predicate. The invariant
    is identical across backends — only the realization differs, and unavailable mechanisms degrade explicitly,
    never silently (REQ-889).'
  use_case: Per-tenant state is isolated without row-level filtering logic on every query, on whatever
    control-plane store the deployment chooses. The mechanism is chosen from the backend's capabilities
    (PG schema+role, single-tenant default schema, or row-security predicate) so the same isolation guarantee
    holds when the control-plane store is swapped (REQ-850).
  code:
  - provisa/core/database.py
  - provisa/core/db.py
  - provisa/api/app.py
  tests:
  - tests/unit/test_org_isolation.py
  - tests/unit/test_tenant_isolation_contract.py
- id: REQ-839
  status: complete
  group: 11. Platform, Infrastructure & Delivery
  category: Data Model & Persistence
  priority: MUST
  type: constraint
  since: 2026-07
  description: Platform registry tables (orgs, user_profiles, user_org_memberships, local_users, org_invites,
    tenants, tenant_config) are created from portable SQLAlchemy metadata (schema_admin.init_registry_schema
    via metadata.create_all), not raw SQL schema files. No cross-database foreign key constraints between
    platform and tenant planes (FKs from domains.org_id, roles.org_id, etc. dropped to plain columns).
    id/token generation for local_users and org_invites moved app-side (uuid4) instead of PG-specific
    gen_random_uuid() server defaults.
  use_case: Ensures platform registry can run on any SQLAlchemy-supported async backend (PostgreSQL, SQLite
    ≥3.35, MySQL 8) without migration or schema.sql maintenance. Plain columns (no FK enforcement) allow
    platform and tenant planes to exist in separate physical databases with independent schemas.
  code:
  - provisa/core/schema_admin.py
  - provisa/api/auth_router.py
  - provisa/api/setup_router.py
  - provisa/api/admin/orgs_router.py
  - provisa/api/admin/local_users_router.py
  - provisa/api/admin/invites_router.py
  tests:
  - tests/unit/test_admin_database.py
  - tests/integration/test_sqlalchemy_dialect.py
- id: REQ-840
  status: complete
  since: 2026-07
  group: 4. Source Connectors
  category: Federation Engine Abstraction
  priority: MUST
  type: infrastructure
  target: 2026-Q3
  description: 'The federation engine is a pluggable abstraction with per-engine drivers. Three driver
    classes exist: broad federators (Trino, reaches many external source types), partial federators (DuckDB,
    attaches a subset of external databases via extensions/scanners), and self-only warehouse-native engines
    (Snowflake, Oracle, Databricks — reach only their own store). Reachability is connector-presence-defined
    and binary: reachable(table) is true iff a connector exists for (selected_engine, table.source.type).
    The connector''s mechanism — attach (reference in place) or land (materialize into the engine''s reachable
    store) — is an internal property of the connector, not a reachability tier; warehouse-native engines
    have only land mechanisms (into self), broad/partial federators use attach where a connector supports
    it and land otherwise.

    A federation engine INSTANCE owns its connector collection — a map keyed by source_type (connectors:
    dict[source_type -> Connector]). Reachability is a lookup into that collection: reachable(table) ==
    (table.source.type in engine.connectors). The three driver classes are defined by collection contents
    — broad federator (Trino: many source_types), partial federator (DuckDB: postgres/csv/json/parquet
    where an extension/scanner exists), self-only warehouse-native (Snowflake: own store only). Swapping
    the engine swaps the connector collection; planner/cache/freshness-gate logic is unchanged. Each Connector
    projects CatalogEntry rows into the engine catalog. DONE (2026-07): provisa/federation/engine.py::FederationEngine
    owns a connector collection keyed by source_type; reachable(source_type) is the binary connector-presence
    lookup; swapping the engine swaps the collection. driver_class() classifies by collection contents
    — build_trino_engine (postgres/mysql/sqlserver → BROAD), build_duckdb_engine (postgres + csv scanner
    → PARTIAL), build_snowflake_engine (land-into-self only → SELF_ONLY).'
  use_case: Support deployments whose engine ranges from a broad federator to a self-only data warehouse
    without changing planner or cache logic.
  code:
  - provisa/federation/engine.py
  - provisa/federation/connector.py
  tests:
  - tests/unit/test_federation_engine.py
  competitive_position:
    status: ahead
    rationale: Engine-pluggable federation (not hardwired to one engine) is uncommon; Denodo, Dremio,
      and Trino each hardwire their own engine.
  related_reqs:
  - REQ-841
  - REQ-842
- id: REQ-841
  status: complete
  since: 2026-07
  group: 4. Source Connectors
  category: Federation Engine Abstraction
  priority: MUST
  type: behavioral
  target: 2026-Q3
  description: A reachable source (one with a connector for the selected engine) is exposed by that connector's
    mechanism — attach (reference the source in place, no data movement) or land (physically materialize
    the source into the engine's reachable store). The mechanism is fixed by the connector, not chosen
    per query; a source with no connector is rejected as unreachable.
  scenario: 'Given a source reference and the configured federation engine

    When the planner resolves it

    Then if a connector exists it is exposed by that connector''s mechanism

    (attach in place, or land into materialization_store), otherwise it is rejected as

    unreachable.

    '
  code:
  - provisa/federation/engine.py
  - provisa/federation/connector.py
  tests:
  - tests/unit/test_federation_engine.py
  related_reqs:
  - REQ-840
- id: REQ-842
  status: complete
  since: 2026-07
  group: 4. Source Connectors
  category: Connector Abstraction
  priority: MUST
  type: structural
  description: 'A Connector, indexed by (federation_engine, source_type), encapsulates all engine-specific
    catalog operations — capability(), catalog_add(asset), catalog_remove(asset), land(source), typemap(source->engine).
    For Trino, catalog_add updates the Trino catalog; for DuckDB it issues ATTACH/scanner setup plus view
    DDL; for warehouse-native engines it is a no-op (the asset is already a native table). Adding a source
    type, a federation engine, or a cache backend is a connector registration; planner, cache-role, and
    freshness-gate logic are unchanged.

    The connector''s derived engine-catalog entry has an explicit persisted row shape: (name: text, engine/platform:
    text, source_type: text, mechanism: attach|land, details: jsonb). The connector exposes catalog_add,
    catalog_update, and catalog_remove operations over individual entries, plus a catalog_refresh operation
    that re-projects the entire engine catalog from the asset registry (REQ-843 reconcile). The details
    jsonb field is engine+source_type specific — e.g., Trino postgresql = connection-url/user /password
    .properties; DuckDB postgresql = ATTACH dsn; DuckDB csv/json = read_csv_auto/read_json_auto view DDL
    over file paths+columns; warehouse -native (Snowflake) = no-op land-into-self. Per the V1 no-migrations
    constraint, the catalog-entry table is derived/rebuildable engine state, not a migrated Postgres table.
    DONE (2026-07): provisa/federation/connector.py defines the Connector ABC keyed by (engine, source_type)
    with capability() and details(source), projecting the persisted CatalogEntry shape exactly — (name,
    engine, source_type, mechanism: attach|land, details: dict/jsonb). Concrete connectors realize the
    three engine families: Trino postgres/mysql/sqlserver (attach, connection-url/user/password .properties
    details), DuckDB postgres (attach dsn) + csv (read_csv_auto view DDL), and a warehouse-native land-into-self
    no-op. catalog_add/remove/refresh are the EngineCatalog.add/remove/refresh operations over these projected
    entries. REMAINING (kept in-progress): typemap(source→engine) per connector, and the live APPLICATION
    of a catalog entry to a running engine (writing Trino .properties / issuing DuckDB ATTACH+view DDL)
    — wired with the executor in the federate() work (REQ-825-827).'
  use_case: A single extensibility seam so new engines/sources/backends require only a connector, not
    core changes.
  code:
  - provisa/federation/connector.py
  - provisa/federation/engine.py
  tests:
  - tests/unit/test_federation_engine.py
  related_reqs:
  - REQ-840
  - REQ-843
- id: REQ-843
  status: complete
  since: 2026-07
  group: 4. Source Connectors
  category: Connector Abstraction
  priority: MUST
  type: constraint
  target: 2026-Q3
  description: 'The asset registry is the source of truth for asset existence; the engine catalog is derived,
    rebuildable state. Connectors project registry state into the engine catalog on asset create/drop
    and on a full startup reconcile. A missing or stale catalog entry triggers re-projection from the
    registry — never a fallback or error-and-continue. DONE (2026-07): EngineCatalog is derived state
    (name → CatalogEntry). FederationEngine.reconcile rebuilds the whole projection from the source registry
    (only reachable sources project); ensure_entry re-projects when the entry is missing or stale (!=
    the fresh projection) — never keeps stale, never falls back. on_asset_create/on_asset_drop keep the
    catalog in step with registry mutations.'
  code:
  - provisa/federation/engine.py
  - provisa/federation/connector.py
  tests:
  - tests/unit/test_federation_engine.py
  related_reqs:
  - REQ-842
  - REQ-135
- id: REQ-844
  status: complete
  since: 2026-07
  group: 4. Source Connectors
  category: Materialization Store
  priority: SHOULD
  type: structural
  description: 'materialization_store — materialized (landed physically by ANY write means: engine-native
    CTAS/load, SQLAlchemy upsert, or app-side land; REQ-848) into a durable STORE. Configured through
    the same ${env:...} secrets chokepoint as ControlPlaneConfig. Its members are characterized by three
    properties that together exclude every other cache: FEDERATABLE (the engine reads it as a real relation
    it can join/scan — REQ-826; excludes hot tables, which are VALUES-CTE plan injections, and the APQ/result
    memos, which are not relations); DURABLE (survives restart, disk/object-backed — excludes the ephemeral
    tier); and REFRESHABLE (actively kept in sync by the one centralized freshness gate — REQ-855/856/857/858;
    excludes a passive scanned source, which is federatable and durable but unmanaged). It holds TWO ROLES
    on ONE categorical axis — whether the entry has a semantic-SQL definition: (1) REPLICA — undefined
    verbatim copies of source rows, no standalone identity; governance/derivation applied at query time
    on top, never baked in. Two population profiles: reactive (source is UNREACHABLE — landed so it is
    queryable at all; on-miss, single-flighted pull-through; REQ-845/847; formerly api_cache, renamed
    as the role is source-agnostic) and warm (source is REACHABLE — an optional latency copy; usage-promoted/demoted
    by query frequency; REQ-238/239). (2) VIEW — mv: governed, named datasets each defined by semantic
    SQL (view_sql; implicit relationship-generated or explicit view materialize:true). The definition
    IS the identity and the basis for lineage, provenance (REQ-862), and definition-versioning. Two orthogonal,
    ENGINE-RELATIVE attributes apply to any entry and are NOT roles: PLACEMENT — where the entry lands,
    chosen relative to engine capability (object store, a low-latency fast-read tier, engine-native store,
    or Iceberg); warm''s "fast tier" is just placement=engine''s-low-latency-store (Trino realizes it
    as the Iceberg results catalog + local-SSD file cache, other engines differently), never a Trino-specific
    definition. FRESHNESS POLICY — TTL / probe / transitive (REQ-855). Population trigger and eviction
    are NOT roles: both roles are lazy and use the one freshness method; the view background refresh loop
    and warm''s frequency promotion are optional warm-ahead only. SUBSTRATE is selected independently
    per role (roles may occupy distinct schemas of one backend OR distinct backends), engine-capability-gated
    (REQ-846): replica → a relational/engine-native store suffices (undefined copies have no forensic
    value); view → SQLAlchemy DB OR engine-native Iceberg, defaulting to Iceberg whenever a reachable
    Iceberg catalog exists, because the derived dataset''s snapshot history is the forensic/bitemporal
    value (time-travel, provenance, restatement; REQ-372/862). Iceberg view refresh MUST use overwrite
    semantics — one clean snapshot per refresh (not delete+insert) — so time-travel maps exactly one snapshot
    to one refresh state. When no Iceberg catalog is reachable the substrate degrades to the relational/native
    store as an explicit, surfaced capability signal (never a silent fallback; REQ-847).'
  use_case: 'One engine-agnostic materialization store replacing per-protocol hardcoded caches, defined
    by three properties (federatable, durable, refreshable) that cleanly separate it from the ephemeral
    hot/APQ/result tiers and from passive scan sources. The replica-vs-view axis drives substrate: cheap
    relational storage for undefined source copies (reactive/warm), snapshot-capable Iceberg for the governed
    derived datasets whose history underpins audit, provenance, and bitemporal restatement. Supports security-conscious
    tenants pointing the store at their own data plane.'
  notes: 'DONE (2026-07): the store''s engine-relative foundations are in place on the federation contract
    — membership is derived (a member is a source that federate() resolves to MATERIALIZED, i.e. the reactive
    set; provisa/federation/materialization.py::reactive_sources), backend validity is enforced (REQ-846),
    and the write face is selected per backend (REQ-848). REMAINING (kept in-progress): the concrete store
    lifecycle — the two roles (replica vs view) over their selected substrate (relational vs Iceberg with
    overwrite-snapshot semantics), durable load/refresh, and participation in federation as a real joinable/scannable
    relation — lands with the freshness gate (REQ-855) and MV refresh path.'
  code:
  - provisa/federation/materialization.py
  - provisa/federation/engine.py
  tests:
  - tests/unit/test_federation_materialization.py
  - tests/integration/test_materialization_store_lifecycle_e2e.py
  related_reqs:
  - REQ-135
  - REQ-158
  - REQ-238
  - REQ-239
  - REQ-309
  - REQ-318
  - REQ-327
  - REQ-372
  - REQ-424
  - REQ-826
  - REQ-830
  - REQ-845
  - REQ-846
  - REQ-848
  - REQ-855
  - REQ-862
- id: REQ-845
  status: complete
  since: 2026-07
  group: 4. Source Connectors
  category: Materialization Store
  priority: SHOULD
  type: behavioral
  description: 'The set of sources served by reactive replicas is engine-relative — reactive = { source
    : reach(source) == land } for the configured federation engine — generalizing the previously hardcoded
    REST/GraphQL/gRPC pull-through set (renamed from api_cache; the role is source-agnostic). Pull-through
    is TTL/freshness-gated (per the REQ-855 freshness policy) and single-flighted to prevent upstream
    stampede on concurrent misses. DONE (2026-07): the engine-relative reactive set is derived — provisa/federation/materialization.py::
    reactive_sources returns { source.id : federate(source, engine) is MATERIALIZED }, so the pull-through
    set is a function of engine reach (a VIRTUAL/SCAN source is never a reactive replica; an unreachable
    source is excluded). REMAINING (kept in-progress): the TTL/freshness-gated, single-flighted pull-through
    execution that lands rows into the store on miss — lands with the write face (REQ-848) and the freshness
    gate (REQ-855).'
  scenario: 'Given a source with no attach connector for the active engine

    When it is referenced concurrently after TTL expiry

    Then a single coalesced pull lands its rows into materialization_store and the queries

    are rewritten to read the cached rows.

    '
  code:
  - provisa/federation/materialization.py
  - provisa/federation/strategy.py
  tests:
  - tests/unit/test_federation_materialization.py
  - tests/integration/test_materialization_store_lifecycle_e2e.py
  competitive_position:
    status: ahead
    rationale: Treating the cached-source set as a function of engine reach (rather than a fixed protocol
      list) is not present in competitors.
  related_reqs:
  - REQ-309
  - REQ-318
  - REQ-327
  - REQ-841
  - REQ-855
- id: REQ-846
  status: complete
  since: 2026-07
  group: 4. Source Connectors
  category: Materialization Store
  priority: MUST
  type: constraint
  target: 2026-Q3
  description: 'The engine''s usable materialized-store set is DERIVED from its connectors — a `materialized_store`
    flag on a Connector marks a reachable backend that can also be a store; FederationEngine.materialize_stores
    is a computed property over the connectors so flagged. PG-only today. Native→IR type translation for
    MATERIALIZED source landing occurs at the landing boundary in `resolve_landing_args` (provisa/federation/residency.py),
    not in the shared config/repository write path. Each column''s stored engine-normalized native type
    is translated to a canonical IR name via `provisa.core.ir_types.to_ir(native, platform)`, where `platform`
    is the federation engine''s dialect. This containment is necessary because stored `data_type` across
    the system is engine-physical and consumed as such by GraphQL/SQL generation, masking, and filters.
    Unmapped native types raise (IR vocabulary gap), never silent varchar defaults. DONE (2026-07): provisa/federation/connector.py
    (materialized_store flag on PG connectors), provisa/federation/engine.py (materialize_stores property),
    provisa/federation/residency.py (resolve_landing_args with to_ir translation).'
  code:
  - provisa/federation/materialization.py
  - provisa/federation/engine.py
  - provisa/federation/residency.py
  tests:
  - tests/unit/test_federation_materialization.py
  - tests/unit/test_residency.py
  related_reqs:
  - REQ-844
  - REQ-840
- id: REQ-847
  status: complete
  since: 2026-07
  group: 4. Source Connectors
  category: Materialization Store
  priority: MUST
  type: constraint
  description: 'Read-through and write-back discipline. On a pull-through read, a pull failure with no
    fresh cached data is a hard error; serving stale data is permitted only under an explicit per-source
    freshness policy (REQ-855), never as a silent fallback. Mutations on a pulled-through relation target
    the upstream source of truth and invalidate the cache entry; the cache is never written as if it were
    the system of record. DONE (2026-07): the discipline is implemented as pure policy — provisa/federation/read_through.py::
    resolve_read returns SERVE_FRESH (re-pull ok, or cache still fresh), SERVE_STALE (only when stale
    cache exists AND an explicit per-source stale policy allows it), or HARD_ERROR (pull failed with no
    fresh data — never a silent stale fallback); plan_mutation states a write targets "upstream" and invalidates
    the cache entry, never the cache as system of record. REMAINING (kept in-progress): wiring these decisions
    into the live pull-through read path and the mutation executor.'
  code:
  - provisa/federation/read_through.py
  tests:
  - tests/unit/test_read_through.py
  - tests/integration/test_materialization_store_lifecycle_e2e.py
  related_reqs:
  - REQ-844
  - REQ-855
- id: REQ-848
  status: complete
  since: 2026-07
  group: 4. Source Connectors
  category: Materialization Store
  priority: SHOULD
  type: structural
  description: 'The federation engine NEVER writes a materialization store; it merely attaches/links and
    READS the landed replica. ALL data-source landing goes through ONE write-face abstraction (store_writer.land
    / ensure_table / reconcile_table), vanilla SQLAlchemy Core today, with pluggable platform branches
    behind it (pyiceberg for an Iceberg store). The "collapse into the engine" applies only to an engine
    writing its OWN native store, never a separate/attached one. Read-only engines (Databricks/Snowflake
    external-linking Iceberg) are first-class readers. DONE (2026-07): provisa/federation/store_writer.py,
    provisa/federation/materialize_exec.py, provisa/api_source/engine_cache.py (land_api_cache), provisa/federation/duckdb_runtime.py.'
  code:
  - provisa/federation/materialization.py
  - provisa/federation/engine.py
  tests:
  - tests/unit/test_federation_materialization.py
  - tests/integration/test_materialization_store_lifecycle_e2e.py
  related_reqs:
  - REQ-844
- id: REQ-849
  status: complete
  group: 3. Source Registration & Data Modeling
  category: Views (Governed Computed Datasets)
  priority: SHOULD
  type: behavioral
  since: 2026-07
  description: 'After SQL compilation, the query rewriter inspects FROM/JOIN clauses and transparently
    rewrites the query to read from a materialized view target table when a matching MV exists — without
    the query author referencing the MV by name. Rewrites are gated by MV enabled status, freshness, and
    TTL. Match selection is coverage-optimal and independent of candidate ordering: a full match (all
    query JOINs covered → entire query redirected to the MV) is always preferred over any partial match,
    and among partial matches the widest coverage (most MV-covered JOINs replaced, remainder preserved)
    wins. A fully-matching MV is never preempted by an earlier partially-matching one.'
  use_case: Transparent MV substitution improves query performance without requiring application changes;
    TTL gating ensures stale data is not served without explicit policy.
  code:
  - provisa/mv/rewriter.py
  - provisa/mv/registry.py
  tests:
  - tests/unit/test_mv_partial_rewrite.py
  related_reqs:
  - REQ-135
  - REQ-198
  - REQ-199
  - REQ-844
  - REQ-845
  - REQ-855
  scenario: 'Given a fresh, enabled MV covering a query''s root table (and optionally some joins)

    When a query is compiled that references the underlying source tables without naming the MV

    Then the rewriter transparently redirects the FROM/JOIN to the MV target (full or partial),

    otherwise the query runs against the live source tables.

    '
- id: REQ-850
  status: complete
  group: 11. Platform, Infrastructure & Delivery
  category: Control-Plane Store Portability
  priority: MUST
  type: structural
  since: 2026-07
  description: Control-plane store is SQLAlchemy-URI-pluggable across dialects, bootstrapped from portable
    SQLAlchemy Core metadata (provisa/core/schema_org.py) via metadata.create_all, replacing PG-only raw
    schema.sql (which uses SERIAL, JSONB, DO $$/pg_advisory_lock, CREATE SCHEMA, search_path). PostgreSQL
    retains the raw schema.sql path unchanged.
  use_case: Allows control plane to deploy on non-PostgreSQL backends (SQLite, MySQL, DuckDB, Oracle)
    without migration, supporting infra-free deployments while maintaining backward compatibility with
    Postgres.
  code:
  - provisa/core/db.py
  - provisa/core/schema_org.py
  - provisa/audit/query_log.py
  related_reqs:
  - REQ-820
  - REQ-821
  - REQ-822
  scenario: 'Given a non-PostgreSQL backend (e.g., SQLite, MySQL) configured as the control-plane store

    When Provisa initializes

    Then init_schema calls metadata.create_all with portable SQLAlchemy types, and org control plane is
    created without raw Postgres DDL

    '
  tests:
  - tests/unit/test_schema_metadata_parity.py
- id: REQ-851
  status: complete
  group: 11. Platform, Infrastructure & Delivery
  category: Multi-Tenant Org Isolation
  priority: MUST
  type: structural
  since: 2026-07
  description: Org isolation is polymorphic on a schema-capable vs not-schema-capable axis. Schema-capable
    backends (PostgreSQL, MySQL, Oracle) scope an org as a namespace on a single shared connection via
    SQL commands (SET search_path TO org_<id>, USE org_<id>, ALTER SESSION SET CURRENT_SCHEMA). Not-schema-capable
    backends (SQLite, DuckDB) carry org in the database file; org selection happens at engine construction
    via per-org file paths.
  use_case: Provides unified multi-tenant abstraction across heterogeneous databases while respecting
    native schema isolation capabilities, enabling schema-based multi-tenancy on Postgres/MySQL and file-per-org
    isolation on SQLite/DuckDB.
  code:
  - provisa/core/database.py
  related_reqs:
  - REQ-818
  scenario: 'Given a PostgreSQL backend in multi-tenant mode

    When org context is activated

    Then enter_org_sql returns "SET search_path TO org_<id>"; on SQLite the org file is selected at engine
    construction.

    '
  tests:
  - tests/unit/test_org_isolation.py
  - tests/unit/test_tenant_isolation_contract.py
- id: REQ-852
  status: complete
  group: 11. Platform, Infrastructure & Delivery
  category: Multi-Tenant Org Isolation
  priority: MUST
  type: structural
  since: 2026-07
  description: OrgRouter (provisa/core/database.py) is the multi-tenant mechanism for not-schema-capable
    backends, mapping org_id → Database and lazily caching one engine per org_<id> database file. OrgRouter
    guards against construction on schema-capable backends and enforces that single-tenant SQLite uses
    the default namespace with no router. Cross-org queries within a single not-schema-capable connection
    are impossible (file boundary).
  use_case: Enables transparent multi-tenant routing for file-based backends (SQLite, DuckDB) while preventing
    misconfiguration on schema-capable systems.
  code:
  - provisa/core/database.py
  related_reqs:
  - REQ-818
  - REQ-851
  scenario: 'Given a multi-tenant SQLite deployment with org_1 and org_2 database files

    When a query is executed in org_1 context

    Then OrgRouter routes to the org_1 engine; switching org_id requires a new connection.

    '
  tests:
  - tests/unit/test_org_router.py
- id: REQ-853
  status: complete
  group: 11. Platform, Infrastructure & Delivery
  category: Control-Plane Store Portability
  priority: MUST
  type: constraint
  since: 2026-07
  description: NOT NULL JSON columns in schema_org.py carry server_default values ('[]' for list, '{}'
    for dict) so raw-text INSERTs that omit them succeed on portable backends (previously only Python-side
    Core defaults existed, causing raw inserts to fail NOT NULL constraints on SQLite).
  use_case: Ensures compatibility with raw SQL inserts across all backends, preventing NOT NULL violations
    when JSON columns are omitted in direct SQL statements.
  tests:
  - tests/unit/test_schema_metadata_parity.py
  - tests/unit/test_admin_database.py
  code:
  - provisa/core/schema_org.py
  related_reqs:
  - REQ-850
  scenario: 'Given a raw INSERT statement that omits a NOT NULL JSON column (e.g., metadata)

    When executed on SQLite via SQLAlchemy

    Then server_default (''{}'' or ''[]'') is applied and the insert succeeds.

    '
- id: REQ-854
  status: complete
  group: 11. Platform, Infrastructure & Delivery
  category: Release Artifact Packaging
  priority: MUST
  type: infrastructure
  since: 2026-07
  description: Core runtime image set (docker-compose.core.yml + OVA/Lima bundles) must include busybox
    as a core dependency; no third-party billing mock service is bundled in any compose (dev/core/airgap);
    python:3.12-slim is build-time only and excluded from packaged/airgapped runtime images.
  use_case: Busybox is required at runtime (trino-exchange-init service uses it to chown /data/provisa/exchange
    before Trino starts). Lemon Squeezy is called over the public REST API; tests stub it via HTTP fixtures
    (LEMONSQUEEZY_BASE_URL), not bundled mocks. Python:3.12-slim exists only as a build base for OVA/Lima
    source builds. This separation ensures airgapped deployments include all runtime dependencies and
    excludes unnecessary dev/mock components.
  code:
  - docker-compose.core.yml
  - docker-compose.dev.yml
  - .github/workflows/build-dmg.yml
  related_reqs:
  - REQ-836
  scenario: 'Given an airgapped OVA or Lima deployment built from docker-compose.core.yml

    When the Trino service starts

    Then busybox image is present to support trino-exchange-init initialization

    And no third-party billing mock service is bundled in the package.

    '
  tests:
  - tests/unit/test_release_packaging.py
- id: REQ-855
  status: complete
  since: 2026-07
  group: 4. Source Connectors
  category: Materialization Store
  priority: SHOULD
  type: behavioral
  description: 'materialization_store freshness policy — the ONE centralized freshness gate referenced
    by REQ-845 (reactive replica pull-through) and applicable to REQ-844''s view role; both roles evaluate
    it lazily on access. Per cache entry (per-table, keyed like the existing replica/view targets, NOT
    per-query-slice) the steward selects one of three modes: (1) TTL only — bounded staleness, current
    behavior (reactive-replica _mem_fresh expiry / view refresh interval); (2) probe — re-probe the upstream
    on access and rebuild only on change, ignoring TTL; (3) TTL+probe — probe only after a TTL floor elapses,
    capping probe frequency. The probe returns an OPAQUE token; Provisa never interprets it, only compares
    stored vs. fresh by equality. Unchanged token → keep the materialized rows, skip the re-pull/rebuild
    (zero lag at probe cost); changed token → invalidate the entry and re-pull, then store the new token.
    MUTUAL EXCLUSIVITY with delta (REQ-874): Probe (opaque-token change detection) and delta (monotonic-cursor
    incremental reload) are MUTUALLY EXCLUSIVE PER ENTRY, selected by token type. Opaque-token entries
    ⇒ REQ-855 probe + full re-pull, no delta. Monotonic entries ⇒ delta-as-probe (delta query IS the freshness
    evaluation), no separate probe runs. Probe transport is source-type-agnostic behind a single dispatch
    (freshness_token(source, table) -> str | None); None means the source cannot produce a token and the
    entry degrades to TTL (a capability signal, not a silent fallback — REQ-847). Transports: graphql_remote
    posts a steward GQL query (generalizing the REQ-673 count_query/graph_counts prototype); openapi uses
    ETag/Last-Modified or a steward-configured endpoint; DB-SQL sources run a steward SQL query via the
    engine; file-based sources use file mtime.'
  use_case: 'Lets stewards trade probe cost against TTL lag per cached source/table — zero-lag freshness
    for volatile pull-through sources and materialized views where lag is unacceptable, cheap TTL for
    static ones — inside the one materialization_store gate (REQ-845) rather than a per-protocol bolt-on.
    view targets (REQ-844 derivation role) may carry both a TTL/refresh interval and a freshness gate;
    the gate suppresses needless CTAS rebuilds when the upstream token is unchanged. DONE (2026-07): the
    centralized gate is implemented — provisa/federation/freshness_gate.py:: evaluate_freshness(mode,
    now, last_refresh_at, ttl, stored_token, probe) returns a FreshnessDecision (fresh + new_token) for
    the three modes: TTL (bounded staleness), PROBE (probe every access, ignore TTL), TTL_PROBE (probe
    only past the TTL floor). The probe yields an opaque token compared by equality — unchanged keeps
    the rows (zero lag), changed marks not-fresh with the new token to persist — and a probe returning
    None degrades the entry to TTL (a capability signal, never a silent stale fallback, REQ-847). Pure
    and reused by both the reactive-replica (REQ-845) and view (REQ-844) roles. REMAINING (kept in-progress):
    the per-source probe TRANSPORTS behind freshness_token(source, table) -> str | None (graphql query,
    openapi ETag/Last-Modified, DB-SQL steward query, file mtime) and wiring the gate into the store''s
    lazy-on-access path + delta mutual-exclusivity (REQ-874).'
  code:
  - provisa/federation/freshness_gate.py
  tests:
  - tests/unit/test_freshness_gate.py
  - tests/integration/test_materialization_store_lifecycle_e2e.py
  related_reqs:
  - REQ-844
  - REQ-845
  - REQ-847
  - REQ-849
  - REQ-135
  - REQ-199
  - REQ-673
  - REQ-874
  scenario: 'Given a reactive replica pull-through source configured with the TTL+probe mode and a TTL
    floor

    When a query reads the cached rows before the floor elapses

    Then the materialized rows are served without probing the upstream

    When a later query arrives after the TTL floor has elapsed

    Then freshness_token(source, table) is evaluated and compared to the stored token; if equal the

    existing rows are kept, and if different the entry is invalidated, re-pulled, rematerialized, and

    the new token stored

    And a view materialization with a freshness gate skips its scheduled CTAS rebuild while the

    upstream token is unchanged.

    '
- id: REQ-856
  status: complete
  since: 2026-07
  group: 4. Source Connectors
  category: Freshness Module
  priority: MUST
  type: structural
  description: 'Freshness gate (REQ-855: three modes TTL/probe/TTL+probe, source-agnostic freshness_token
    dispatch, transports incl. DB-SQL steward query and file mtime) is generalized into a standalone,
    context-agnostic module answering one question: given a subject and its observable state, is it fresh?
    Returns fresh/stale/failed + reason. Pure decision function with no triggering or refresh semantics
    (caller responsibilities). Enables MV, Source, cache, pgvector all consume one implementation.'
  use_case: Unifies freshness logic currently scattered across MV (TTL-based, provisa/mv/models.py:94-102),
    Source introspection, and ad-hoc cache checks (provisa/openapi/pg_cache.py:49). Single implementation
    enables consistent freshness gating across all data paths.
  code:
  - provisa/freshness/__init__.py
  - provisa/freshness/decision.py
  - provisa/freshness/predicate.py
  related_reqs:
  - REQ-844
  - REQ-845
  - REQ-855
  tests:
  - tests/unit/test_freshness.py
- id: REQ-857
  status: complete
  since: 2026-07
  group: 4. Source Connectors
  category: Freshness Module
  priority: MUST
  type: structural
  description: 'FreshnessSubject protocol exposes: (1) last-refresh timestamp; (2) last-refresh outcome
    (success/failure); (3) upstream-subject handles for transitive freshness; (4) content-query capability
    — this IS REQ-855''s freshness_token(source,table) -> str | None dispatch (opaque token; None = source
    cannot produce one, degrades to TTL per REQ-847).'
  use_case: Enables FreshnessPredicate to evaluate any subject uniformly—MV, Source, cache all conform
    to the same interface. Transitive strategy can check upstream freshness by following handles.
  code:
  - provisa/freshness/subject.py
  related_reqs:
  - REQ-856
  - REQ-855
  tests:
  - tests/unit/test_freshness.py
- id: REQ-858
  status: complete
  since: 2026-07
  group: 4. Source Connectors
  category: Freshness Module
  priority: MUST
  type: structural
  description: 'FreshnessPredicate with pluggable strategies that unify REQ-855''s three modes plus a
    boolean content predicate: TTL (time-based, exists today); PROBE (REQ-855 opaque- token change detection
    via freshness_token — the same mechanism as REQ-855''s DB-SQL steward-query transport, do not treat
    as a separate "SQL strategy"); TRANSITIVE (fresh iff own predicate holds AND all upstream subjects
    are transitively fresh — the one net-new capability beyond REQ-855, which is per-entry only); composable
    (TTL+PROBE floors probe frequency). Caller supplies subject and strategy; predicate returns decision
    without side effects.'
  use_case: Decouples freshness evaluation from refresh logic. Strategies are swappable and composable,
    enabling rich policies (e.g., TTL for cost-sensitive tables, PROBE for zero-lag accuracy, TRANSITIVE
    for derived caches/medallion chains).
  code:
  - provisa/freshness/predicate.py
  related_reqs:
  - REQ-856
  - REQ-857
  - REQ-855
  tests:
  - tests/unit/test_freshness.py
- id: REQ-859
  status: complete
  since: 2026-07
  group: 4. Source Connectors
  category: Freshness Module
  priority: MUST
  type: behavioral
  description: MV and the ad-hoc API/pg cache freshness checks (provisa/openapi/pg_cache.py) become FreshnessSubjects
    (via the StateSubject adapter) and delegate their TTL decision to the one unified FreshnessPredicate.
    MVDefinition.is_fresh_at keeps its FRESH-status lifecycle gate and delegates the TTL comparison; pg_cache._is_fresh
    delegates likewise. Behaviour-preserving unification of the scattered TTL logic. Source-level conformance
    is the gating work in REQ-860 (its interesting PROBE mode depends on the REQ-855 probe transport).
  use_case: 'Consistency: the same freshness semantics and strategies apply to every data path, and there
    is one place to change freshness policy instead of per-consumer copies.'
  scenario: 'Given a materialized view and an API/pg cache entry, each with its own last-refresh state

    When their freshness is evaluated

    Then both expose that state as a FreshnessSubject and the TTL decision is produced by the one shared
    FreshnessPredicate, yielding the same fresh/stale result as the prior per-consumer checks.

    '
  code:
  - provisa/freshness/adapters.py
  - provisa/mv/models.py
  - provisa/openapi/pg_cache.py
  tests:
  - tests/unit/test_freshness_adapters.py
  related_reqs:
  - REQ-856
  - REQ-857
  - REQ-858
  - REQ-844
- id: REQ-860
  status: complete
  since: 2026-07
  target: 2026-Q3
  code:
  - provisa/core/models.py
  - provisa/freshness/source_gate.py
  - provisa/federation/plan.py
  - provisa/federation/native_backend.py
  tests:
  - tests/unit/test_source_freshness_gate.py
  scenario: Given a source S with freshness_gate=true declaring change_signal=ttl and cache_ttl When a
    query reads a table from S whose last residency landing is older than cache_ttl Then S is scheduled
    into the execution plan residency prep and refreshed before the read, while a source within its TTL
    is read with no refresh
  group: 4. Source Connectors
  category: Freshness Gating
  priority: SHOULD
  type: behavioral
  description: Freshness gating is now available at the Source level (provisa/core/models.py), not just
    on MV. A source can declare a freshness predicate; queries reading from that source are gated by freshness
    before execution.
  use_case: Prevents serving stale data from pull-through sources. Stewards can enforce freshness without
    materializing into an MV, reducing latency and compute overhead.
  related_reqs:
  - REQ-856
  - REQ-859
- id: REQ-861
  status: complete
  since: 2026-07
  group: 4. Source Connectors
  category: Freshness Gating
  priority: MAY
  type: behavioral
  target: 2026-Q3
  description: File-based sources (CSV, Parquet, etc.) may carry an optional producer command (argv) that
    runs on-stale (gate-triggered) before the file is read. Allows refresh without modifying the file
    path or materializing into an MV. DONE (2026-07) - a file source (csv/parquet/sqlite/files) carries
    an optional producer_command argv (provisa/core/models.py); provisa/freshness/producer.py runs it
    with shell=False (injection-safe) and fails loud on a non-zero exit. The read path (provisa/events/source_loader.py
    SourceRowLoader.load) is invoked only after the REQ-860 source gate reports stale, so it runs the
    producer BEFORE the file read - freshening the file in place without touching path or defining an
    MV.
  use_case: 'Supports workflows where stale file detection should trigger a script to refresh the file
    in place. Example: fetch latest data from an upstream API and write to the same CSV path when staleness
    is detected. Pairs with REQ-855: REQ-855 file mtime DETECTS staleness, this producer command is the
    ACT-on-stale response.'
  code:
  - provisa/core/models.py
  - provisa/freshness/producer.py
  - provisa/events/source_loader.py
  tests:
  - tests/unit/test_file_producer_command.py
  scenario: Given a file source S (type csv) with freshness_gate=true and a producer_command argv When
    the REQ-860 gate reports S stale and its residency read runs Then the producer command executes (shell=False)
    before the file is read, a non-zero exit fails loud and the stale file is not read, while a source
    within its TTL runs no producer and a source with no producer_command is read as-is
  related_reqs:
  - REQ-856
  - REQ-860
  - REQ-855
- id: REQ-862
  status: complete
  since: 2026-07
  group: 1. Access Governance & Security
  category: Column-Level Trace Instrumentation
  priority: MUST
  type: structural
  target: 2026-Q3
  description: 'MV creation/refresh is instrumented with detailed column-level trace records: for each
    refresh, the emitted OTel spans capture the resolved per-output-column derivation (upstream source
    column(s) and transform, from the view_sql), the input snapshot ids/watermark epochs consumed, the
    active definition-version, and the refresh trace_id. These records land in the governed telemetry
    substrate (OTel→Parquet, otel catalog). The system does not provide a lineage-resolution engine; lineage
    is DERIVED BY THE USER querying these trace records (optionally materialized as an MV), which — combined
    with Iceberg time-travel — yields point-in-time, column-level lineage as a query result. DONE (2026-07):
    the mv.refresh.column_lineage span now carries all four store-independent stamps — per-output-column
    derivation (provisa/lineage/columns.py, sqlglot), the definition-version content hash (provisa/lineage/versions.py),
    the resolved input-version + fidelity kind, and the trace_id. The final piece — actual per-source
    signal gathering — is wired: at refresh (provisa/mv/input_signals.py::gather_input_signals) each source
    table is asked, through the same Trino connection, for the strongest signal it offers — an Iceberg
    snapshot id from "<table>$snapshots", else an RDB watermark MAX(watermark_column) with the column
    discovered from the provisa_admin registry catalog (REQ-260) — so input_version reflects the real
    data version rather than always degrading to the refresh epoch. Best-effort telemetry: sources offering
    neither contribute nothing and any query error is skipped, never failing the refresh; resolve_input_version
    grades iceberg_snapshot > watermark > freshness_token > refresh_epoch. Enables audit, impact analysis,
    and compliance workflows without a bespoke lineage service: because the trace substrate is itself
    queryable and time-travelable, users answer "where did this column value come from, as of time T"
    by querying the column-level MV-refresh traces rather than by invoking a dedicated lineage API.'
  code:
  - provisa/lineage/columns.py
  - provisa/lineage/versions.py
  - provisa/mv/refresh.py
  - provisa/mv/input_signals.py
  - provisa/mv/registry.py
  - provisa/transpiler/
  - provisa/mv/aggregate_catalog.py
  tests:
  - tests/unit/test_lineage_columns.py
  - tests/unit/test_mv_input_signals.py
  related_reqs:
  - REQ-844
  - REQ-857
  - REQ-858
  - REQ-372
  - REQ-302
  - REQ-303
  - REQ-815
  - REQ-194
  - REQ-198
  - REQ-199
  scenario: 'Given an MV "revenue_summary" defined as SELECT customer_id, SUM(amount) AS total

    FROM orders WHERE region = ''US''

    When it refreshes, the MV-refresh spans record: output column "total" derived from

    input column "orders.amount" via SUM under the region=''US'' predicate, input snapshot

    (watermark epoch E2), definition-version V, refresh trace_id X

    Then a user reconstructs point-in-time column lineage by querying those trace records

    (joined to the data snapshot via as_of) — the system resolves no lineage itself

    '
- id: REQ-863
  status: complete
  since: 2026-07
  group: 6. Execution, Routing, Caching & Performance
  category: Query Planning Pipeline
  priority: SHOULD
  type: structural
  target: 2026-Q3
  description: 'Query planning phases are ordered so that source-set-mutating transforms complete before
    routing. The pipeline is: query language -> semantic SQL (IR) -> governance (which may ADD sources
    via RLS subquery predicates) -> a post-governance optimization stage (which may REMOVE sources, e.g.
    hot-table VALUES-CTE inlining, API-cache rewrites, union-branch pruning) -> route decision + physical
    lowering -> execute. Routing (extract_sources / decide_route) MUST consume the output of the post-governance
    optimization stage, never the pre-optimization governed SQL. Consequence: when hot-CTE inlining collapses
    a federated (multi-source) query to a single remaining live source, routing observes one source and
    selects DIRECT execution instead of Trino federation; the VALUES CTEs are then lowered in the chosen
    route''s dialect (source dialect for direct pushdown, Trino for federated).'
  use_case: 'Direct single-source execution is materially faster than Trino federation. Today the route
    is decided on the governed SQL BEFORE hot-CTE inlining runs (inlining is nested inside the Route.TRINO
    branch), so a query whose second source is fully inlined still executes federated. Correct phase ordering
    makes the DIRECT collapse fall out naturally instead of requiring special-case logic, and unblocks
    the pushdown of inlined lookups on the direct path. The optimization stage sits after governance because
    hot inlining depends on the governed logical table reference (REQ-233: governance binds to the table
    name, then inlining swaps in the CTE, and the governed wrapper filters/masks the CTE rows).'
  code:
  - provisa/pgwire/_pipeline.py
  - provisa/transpiler/router.py
  - provisa/compiler/sql_gen.py
  - provisa/cache/hot_tables.py
  - provisa/compiler/stage2.py
  - provisa/compiler/sql_rewrite.py
  tests:
  - tests/unit/test_pipeline_order.py
  related_reqs:
  - REQ-230
  - REQ-232
  - REQ-233
  - REQ-027
  - REQ-028
  - REQ-030
  scenario: 'Given a query joining live source A to hot-cached lookup table B in source C

    When the post-governance optimization stage inlines B as a VALUES CTE

    Then extract_sources over the optimized SQL observes only source A

    And decide_route selects DIRECT (single-source) rather than Trino federation

    And the VALUES CTE is lowered in source A''s dialect for direct pushdown

    '
- id: REQ-864
  status: complete
  since: 2026-07
  group: 6. Execution, Routing, Caching & Performance
  category: Query Result Cache
  priority: SHOULD
  type: structural
  description: 'The query result cache key is computed from a NORMALIZED IR — a canonical, AST-normalized
    form of the GOVERNANCE-NORMALIZED IR (the post-Stage-2 governed SQL — REQ-263 — after RLS, masking,
    and column visibility are applied with per-identity values resolved, REQ-041/REQ-402) — rather than
    the serialized compiled-SQL string it hashes today (provisa/cache/key.py hashes compiled.sql as text).
    Normalization canonicalizes cosmetic variation that does not change results (whitespace, alias naming/ordering,
    literal formatting, commutable predicate/JOIN ordering, projection ordering) so two semantically-identical
    queries resolve to the SAME cache entry instead of missing. Because the keyed form is the governed
    IR, the resolved persona — role, RLS predicate values, masking, and column visibility — is ALREADY
    embedded in it (no separate concatenation), and the per-tenant prefix (REQ-595) is applied on top;
    the RLS-unresolved guard (key.py ValueError) is retained. CRITICAL: normalization canonicalizes cosmetic/structural
    form ONLY and MUST preserve every identity-encoding element — the RESOLVED RLS predicate values, role,
    masking, and column visibility — so it can never collapse two personas onto one key (the hypercritical
    isolation constraint REQ-866). Refines, does not replace, the existing result cache (REQ-544).'
  use_case: Raises cache hit-rate and eliminates duplicate entries for queries that differ only in text,
    making the result cache robust to cosmetic query variation from different clients, formatters, or
    GraphQL shapes that compile to equivalent IR.
  code:
  - provisa/cache/key.py
  - provisa/api/data/endpoint.py
  tests:
  - tests/unit/test_cache_key.py
  related_reqs:
  - REQ-544
  - REQ-536
  - REQ-595
  - REQ-262
  - REQ-865
  - REQ-866
- id: REQ-865
  status: complete
  since: 2026-07
  group: 6. Execution, Routing, Caching & Performance
  category: Query Routing
  priority: SHOULD
  type: structural
  description: 'The route decision includes CACHED as a first-class route alongside DIRECT (REQ-027) and
    FEDERATED/Trino (REQ-028): the Route enum gains Route.CACHE and decide_route (provisa/transpiler/router.py)
    evaluates the result cache — keyed per REQ-864/REQ-544 — as the first candidate route. A cache hit
    resolves to Route.CACHE and serves the stored result with no direct or federated execution; a miss
    falls through to DIRECT/FEDERATED as today. This elevates the current outer-wrapper cache check (which
    runs before decide_route in endpoint.py and is invisible to routing) into the routing model, so every
    query is served by exactly one uniformly-decided route: {cached, direct, federated}. CACHED belongs
    in the route decision precisely BECAUSE the routing point is where the query is GOVERNANCE-NORMALIZED:
    routing consumes the post-Stage-2 governed IR (REQ-263, persona-resolved via REQ-041/REQ-402), so
    the cache key derived there ALREADY encodes the requester''s persona and a cache serve is inherently
    isolated (REQ-866) — a pre-governance wrapper cannot guarantee that structurally. The no-cache bypass
    (@noCache / no_cache / @cached(ttl:0); REQ-544) removes CACHED from the candidate set for that query.
    Routing consumes the post-optimization IR (REQ-863), so cache-key computation and route selection
    operate on the same normalized form.'
  use_case: Unifies the serving model — one decision point owns how every query is served — makes cache
    participation observable in the route/plan (traceable like any route), and lets routing policy reason
    about the cache alongside direct and federated execution instead of it being a hidden pre-step.
  code:
  - provisa/transpiler/router.py
  - provisa/api/data/endpoint.py
  - provisa/cache/store.py
  related_reqs:
  - REQ-544
  - REQ-536
  - REQ-864
  - REQ-027
  - REQ-028
  - REQ-863
  - REQ-866
  tests:
  - tests/unit/test_routing.py
- id: REQ-866
  status: complete
  since: 2026-07
  group: 1. Access Governance & Security
  category: Cache Isolation
  priority: MUST
  type: constraint
  description: 'HYPERCRITICAL isolation invariant for the query result cache. The cache key MUST fully
    partition by the RESOLVED governance identity of the requester, so a cache hit can NEVER serve rows
    computed for a different persona. This matters because personas can switch on the same device, connection,
    or pooled session; a mis-scoped key would leak one user''s data to another. The key MUST bind to:
    the per-tenant prefix (REQ-595), role_id, and the fully-RESOLVED RLS predicates plus masking and column
    visibility — i.e. RLS current_setting/session values substituted to their actual per-identity values
    (REQ-041/REQ-402), NOT the unresolved rule template (identical across users, which would collapse
    distinct personas onto one key). This isolation is STRUCTURAL when the cache is a first-class route
    (REQ-865): the route decision operates on the governance-normalized IR, so the key intrinsically encodes
    the persona. IR/SQL normalization (REQ-864) canonicalizes cosmetic and structural form ONLY and MUST
    preserve every identity-encoding element. FAIL-CLOSED: if any identity dimension is unresolved or
    unknown, the cache MUST NOT be read or written for that query (the key.py RLS-unresolved guard), and
    CACHED (REQ-865) is removed from the candidate routes — never a silent fallback that could serve another
    persona''s data.'
  use_case: Prevents cross-persona / cross-user data leakage through the result cache when identities
    change on a shared device, connection, or pooled session — a hard multi-tenant and RLS isolation guarantee,
    consistent with the two-layer governance model (REQ-038) and Stage-2 RLS/masking enforcement (REQ-263).
  code:
  - provisa/cache/key.py
  - provisa/api/data/endpoint.py
  tests:
  - tests/unit/test_cache_key.py
  related_reqs:
  - REQ-544
  - REQ-595
  - REQ-864
  - REQ-865
  - REQ-041
  - REQ-402
  - REQ-038
  - REQ-263
- id: REQ-867
  status: complete
  since: 2026-07
  group: 2. Authentication & Identity
  category: Authorization
  priority: MUST
  type: structural
  description: Table-scoped mutation registration with per-mutation grants across all remote schema adapters
    (GraphQL remote, OpenAPI, gRPC, Hasura). Mutations are registered as first-class sub-resources, each
    with its own writable_by list. Empty writable_by = default-deny. The authorization model is protocol-agnostic;
    shared/universal layer includes table-scoped mutation sub-resources, per-mutation writable_by, global
    WRITE capability, execute-time enforcement, read-statement taint, ranked-candidate suggestion interface,
    admin-confirm, and default-deny. What is per-adapter is only the signals used for classification and
    association.
  use_case: Enables fine-grained authorization of table mutations independent of the table's read visibility
    across all query sources (GraphQL, OpenAPI, gRPC, Hasura), supporting mutation execution as UDFs in
    the semantic layer.
  code:
  - provisa/security/mutation_authz.py
  - provisa/security/rights.py
  - provisa/core/models.py
  tests:
  - tests/unit/test_mutation_authz.py
  related_reqs:
  - REQ-868
  - REQ-869
  - REQ-870
  - REQ-871
  - REQ-872
- id: REQ-868
  status: complete
  since: 2026-07
  group: 2. Authentication & Identity
  category: Authorization
  priority: MUST
  type: structural
  description: Global WRITE (alias EXECUTE_MUTATION) capability added to the Capability enum. Acts as
    a coarse master switch; role must hold WRITE AND appear in the specific mutation's writable_by to
    invoke a mutation.
  use_case: 'Two-gate authorization model: coarse capability check (does role have WRITE at all?) plus
    fine-grained mutation-level grant check (is role in this mutation''s writable_by?). Analogous to Postgres
    role-level rights + per-table GRANT.'
  code:
  - provisa/security/rights.py
  tests:
  - tests/unit/test_mutation_authz.py
  related_reqs:
  - REQ-867
  - REQ-869
  - REQ-870
- id: REQ-869
  status: complete
  since: 2026-07
  group: 2. Authentication & Identity
  category: Authorization
  priority: MUST
  type: constraint
  description: 'Mutations are classified as WRITES by contract (not caller declaration) via protocol-specific
    classifiers. Per-adapter read/write classifiers: GraphQL uses operation type (type Mutation), OpenAPI
    uses HTTP method (POST/PUT/PATCH/DELETE = write; GET = read) plus x-provisa-kind override, gRPC uses
    MethodOptions.idempotency_level (NO_SIDE_EFFECTS = read; IDEMPOTENCY_UNKNOWN and IDEMPOTENT = write;
    AIP naming and server-streaming are non-normative heuristics only), Hasura uses exposed_as/action_type.
    Universal default: unknown/unclassifiable → treat as write (default-deny). Any read statement (SELECT)
    referencing a kind=mutation UDF is promoted to write and subject to both WRITE capability + writable_by
    check. Execute-time enforcement in _execute_action_field validates role_id ∈ mutation.writable_by.'
  use_case: Prevents callers from invoking mutations by disguising them as SELECT statements. Protocol-agnostic
    classification with per-adapter signals ensures mutations cannot bypass write-level authorization
    regardless of source.
  code:
  - provisa/security/mutation_authz.py
  - provisa/api/data/endpoint.py
  tests:
  - tests/unit/test_mutation_authz.py
  related_reqs:
  - REQ-867
  - REQ-868
  - REQ-870
- id: REQ-870
  status: complete
  since: 2026-07
  group: 2. Authentication & Identity
  category: Authorization
  priority: MUST
  type: behavioral
  description: 'Admin-only reclassification: legitimate reclassification of a mutation to read-safe is
    gated by ACCESS_CONFIG capability and recorded as a governance decision. No per-operation read_only
    opt-out for callers; only admin acts can demote a mutation. Re-introspection of discovered mutations
    registers with empty writable_by; existing grants preserved by name. DONE (2026-07): two enforceable
    behaviors. (1) reclassify_kind (provisa/security/ mutation_authz.py) is the single reclassification
    gate — only a role holding ACCESS_CONFIG (ADMIN bypasses, per has_capability) may demote a mutation
    to read-safe; the reverse (promoting a read to a write) is rejected for everyone, and there is no
    caller-supplied read_only flag anywhere in the request path, so classification stays under governance
    control. A no-op transition is idempotent. (2) upsert_function (provisa/core/repositories/ function.py)
    now preserves an existing writable_by on re-introspection: a discovered mutation upserts with an empty
    writable_by that keeps the current admin grant (grants preserved by name), while an explicit non-empty
    grant still applies. Proven with unit tests for the gate and a live-Postgres test for grant preservation
    across re-introspection.'
  use_case: Ensures authorization policy remains under control of governance (admin), not individual callers.
    Prevents callers from overriding mutation classification, supports safe discovery/re-registration
    of mutations without accidentally granting them.
  code:
  - provisa/security/mutation_authz.py
  - provisa/core/repositories/function.py
  tests:
  - tests/unit/test_mutation_authz.py
  - tests/integration/test_mutation_writable_by_preservation.py
  target: 2026-Q4
  scenario: 'Given a discovered remote mutation "createOrder" registered with an empty writable_by

    And an admin grants it to the "ops" role (writable_by = [''ops''])

    When introspection re-runs and upserts createOrder by name with an empty writable_by

    Then the ops grant is preserved (writable_by stays [''ops'']) — discovery never wipes grants

    And when a role WITHOUT the ACCESS_CONFIG capability attempts to reclassify createOrder

    to read-safe, the attempt is rejected

    And an ACCESS_CONFIG (or admin) role may demote it to read, but no one may promote a

    read back to a write

    '
  related_reqs:
  - REQ-867
  - REQ-868
  - REQ-869
  - REQ-871
- id: REQ-871
  status: complete
  since: 2026-07
  group: 2. Authentication & Identity
  category: Authorization
  priority: MUST
  type: behavioral
  description: 'Mutation↔table association suggestions at registration time via protocol-specific association-suggesters.
    Universal rule: adapter emits ranked (mutation → table) candidates; suggestions are hints only; admin-confirmed;
    confirmed associations register with empty writable_by (default-deny); any mutation may bind to any
    table (manual override always); unmatched mutations remain registerable. GraphQL: walk mutation return
    type''s fields, unwrap NON_NULL/LIST, match leaf types against type_to_table; LIST-valued field =
    changed-records collection; prioritize single object > list-of-table-type; ignore scalar/stats fields.
    OpenAPI: align via URL path template, operationId stem, and tags (e.g., POST /users → users table);
    response schema is tiebreaker only, not primary signal. gRPC: response-message repeated field type
    + method-name entity stem (weakest signal). Fallbacks (all adapters): operation name affixes (create*/update*/delete*/upsert*)
    and input-type stem for operations whose response carries no queryable type. False negatives expected;
    no suggestion auto-binds. DONE (2026-07): the suggester engine (provisa/security/association_suggester.py)
    implements all three protocols and the shared fallbacks exactly as specified, returning a ranked list
    of TableCandidate(table, score, reason) deduplicated per table (strongest signal wins, corroborating
    signals add a small tiebreak). Signal strengths encode the spec order: GraphQL single-object return
    (1.0) > list-of-table-type (0.8); OpenAPI path resource (0.9) > operationId stem (0.7) > tag (0.6)
    with response schema as a 0.05 tiebreak; gRPC repeated response-field type (0.6) > method-name stem
    (0.4); name-affix / input-type stem fallback (0.3). Nothing auto-binds — the functions are pure and
    return hints; an empty list is the honest false-negative case. The GraphQL remote mapper (provisa/graphql_remote/mapper.py)
    invokes it during registration so every mapped mutation entry carries suggested_associations = [{table,
    score, reason}] computed from its unwrapped return-type leaves + input-type stem, with writable_by
    left empty (default-deny). Consuming these hints into an admin confirmation surface is REQ-870; projecting/binding
    across surfaces is REQ-872.'
  use_case: Registration of mutations across heterogeneous sources requires protocol-aware association
    signals. Per-adapter suggesters reduce admin friction by ranking likely alignment; unmatched mutations
    remain registerable as global functions or require manual binding.
  code:
  - provisa/security/association_suggester.py
  - provisa/graphql_remote/mapper.py
  tests:
  - tests/unit/test_association_suggester.py
  - tests/unit/test_graphql_remote_mapper.py
  target: 2026-Q4
  scenario: 'Given a remote GraphQL source with a query "users" returning [User] (so type_to_table

    maps User → the users table) and a mutation "createUser(input: UserInput): User"

    When the schema is registered and mapped

    Then the createUser tracked-function entry carries suggested_associations whose top

    candidate is the users table (score 1.0, reason "return type User"), and its writable_by

    stays empty — the suggestion is a hint that no code auto-binds

    And a mutation "sendTelemetry: Boolean" with no table-typed return yields an empty

    suggested_associations list rather than an error

    '
  related_reqs:
  - REQ-867
  - REQ-870
- id: REQ-872
  status: complete
  superseded_by: REQ-1156
  since: 2026-07
  group: 2. Authentication & Identity
  category: Authorization
  priority: MUST
  type: behavioral
  description: 'Registered remote-schema mutations/functions (tracked_functions, tracked_webhooks) MUST
    be projected into each query surface''s native function catalog and be invocable from each surface,
    routing through the shared executor and enforcing per-mutation writable_by authorization. Single source
    of truth: tracked_functions + tracked_webhooks registry. Per-surface fidelity: pgwire populates _pg_proc
    and supports SELECT fn(...), SQL/Explore projects information_schema.routines and supports table-valued-function
    invocation, Cypher/Bolt binds CALL <registeredFn>(args) YIELD ... with optional discovery. DONE (2026-07)
    — the SQL-surface DISCOVERY projection: the pgwire catalog builds pg_proc and information_schema.routines
    + information_schema.parameters from the tracked_functions registry (provisa/pgwire/catalog.py::_populate_functions,
    called from _build_catalog_db), so psql \\df, DBeaver and Explore now see registered functions. The
    registry is the single source of truth; projection is role-scoped by visible_to (unrestricted or the
    role is granted); table-valued functions (a return_schema) are set-returning (proretset/record); arguments
    become information_schema.parameters rows in ordinal order with SQL data types; the same function
    registered under multiple keys (bare + domain-prefixed alias) projects once. DONE (2026-07) — the
    SHARED EXECUTOR + Cypher INVOCATION: the tracked-function execution core is extracted from the GraphQL
    action path into invoke_tracked_function(name, args, state, role_id) (provisa/api/data/endpoint.py)
    — surface-agnostic, enforcing per-mutation writable_by by contract (REQ-869) then running SELECT *
    FROM "schema"."fn"(args) through the function''s source pool. The GraphQL path now routes through
    it, and the Cypher surface binds CALL <registeredFn>(args) YIELD col [AS alias] to it (provisa/api/rest/cypher_router.py::_detect_registered_call
    / _handle_registered_call): the call is intercepted before parse, args (literals / $params) are coerced
    positionally, writable_by is enforced in the executor (403 on deny), and YIELD projects the returned
    columns. DONE (2026-07) — the pgwire / SQL INVOCATION: execute_pgwire_sql intercepts a bare SELECT-of-a-registered-function
    before governance/routing and runs it through the same executor (provisa/pgwire/function_call.py).
    Both the table-valued form SELECT * FROM fn(args) and the scalar form SELECT fn(args) are recognized
    via sqlglot (an Anonymous func whose name is a registered tracked function), literal args are coerced
    positionally, and the executor''s row dicts are adapted back to a pgwire QueryResult — so psql / DBeaver
    / Explore invoke registered functions exactly like native ones, with writable_by enforced. All surfaces
    (GraphQL, Cypher CALL, pgwire/ SQL SELECT) now discover AND invoke registered functions through the
    one shared executor.'
  use_case: UDF registry currently exposed only through GraphQL; pgwire, SQL/Explore, and Cypher surfaces
    cannot discover or invoke registered functions. Cross-surface projection and execution binding unifies
    function access across all query surfaces and enforces consistent authorization.
  code:
  - provisa/pgwire/catalog.py
  - provisa/pgwire/function_catalog.py
  - provisa/pgwire/function_call.py
  - provisa/pgwire/_pipeline.py
  - provisa/api/data/action_exec.py
  - provisa/api/rest/registered_call.py
  - provisa/api/rest/cypher_router.py
  tests:
  - tests/unit/pgwire/test_function_projection.py
  - tests/unit/pgwire/test_function_call.py
  - tests/unit/test_tracked_function_invocation.py
  target: 2026-Q4
  scenario: 'Given a tracked_function "createOrder(customer_id integer, total number)" returning a table

    (return_schema set), visible to all roles, in the registry

    When a SQL-surface client queries information_schema.routines and information_schema.parameters

    over pgwire as a role that can see it

    Then routines lists createOrder as a set-returning FUNCTION and parameters lists customer_id

    and total in ordinal order with their SQL data types (pg_proc shows proname=createOrder,

    pronargs=2, proretset=true)

    And a function whose visible_to excludes the querying role does not appear in the catalog

    '
  related_reqs:
  - REQ-867
  - REQ-868
- id: REQ-873
  status: rejected
  rejection_reason: Duplicates existing watermark/probe change-signal work (REQ-855, REQ-260, REQ-282).
    Its only net-new capability (incremental delta fetch) is superseded by REQ-874.
  group: 9. Live Data & Events
  category: Subscriptions
  priority: MUST
  type: behavioral
  description: SUPERSEDED BY REQ-874. This requirement duplicated existing watermark/probe change-signal
    work already captured in REQ-855 (probe transport, opaque token comparison), REQ-260 (RDB watermark_column
    polling), and REQ-282 (Live Query Engine polling). Its only net-new capability (incremental delta
    fetch/append to materialized replicas instead of full re-pull) is now carried by REQ-874, which generalizes
    delta-fetch to any federated dataset and separates WATERMARK (existing change-signal) from DELTA (the
    new incremental append). This entry is retired; use REQ-874 and REQ-855 instead.
  related_reqs:
  - REQ-874
  - REQ-855
  - REQ-260
  - REQ-282
- id: REQ-874
  status: complete
  since: 2026-07
  group: 4. Source Connectors
  category: Materialization Store
  priority: SHOULD
  type: behavioral
  description: 'Delta fetch for materialization_store REPLICA incremental refresh: an INCREMENTAL RELOAD
    strategy for datasets whose federation strategy is MATERIALIZED (REQ-826) — data is landed/cached,
    so full re-pull is the cost delta avoids. Remote schemas (graphql_remote/ openapi/grpc) are the PRIMARY
    DRIVER but not the sole case; also RDB cached-for-latency, NoSQL, live APIs landed via MATERIALIZED.
    VIRTUAL and SCAN strategies EXCLUDED (always-fresh / read-in-place; nothing to delta-refresh per REQ-826).
    KEY SIMPLIFICATION: PROBE == DELTA for monotonic entries. For a monotonic-cursor entry the DELTA QUERY
    IS THE FRESHNESS EVALUATION. Run the delta query: non-empty result ⇒ changed ⇒ apply the rows; empty
    ⇒ fresh ⇒ no-op. No separate watermark/probe query. No scalar_path. DEFINITION: ONE authored query
    (delta_query) — authored, source-native, fully-formed filter with two PLACEHOLDERS that Provisa substitutes:
    (a) watermark placeholder ($wm bind variable) bound to the stored CURSOR VALUE, (b) fields placeholder
    ({{fields}}) injected from the table''s registered selection set. Provisa substitutes only; never
    parses the filter. CURSOR FIELD IMPLICIT: the cursor field is the field $wm filters on (single query,
    single field); after applying delta rows, the stored cursor advances to max(cursor-field) over returned
    rows (delta_query ordered by cursor field). Same-field alignment therefore trivially guaranteed; no
    separate field declaration required. Cursor and monotonicity are the registrant''s responsibility;
    Provisa does no dedup or boundary-inclusivity logic. SOURCE-TYPE-SPECIFIC AUTHORING AND EXECUTION:
    delta_query is AUTHORED IN THE SOURCE''S NATIVE QUERY LANGUAGE, syntax and filter/cursor binding PER-SOURCE-TYPE:
    GraphQL `where: {field: {_gt: $wm}}` + `{{fields}}`; OpenAPI query-param `?updated_since=$wm` + selection
    headers; gRPC request-message filter + `$wm` + selection; SQL `WHERE field > $wm` + projection. Only
    $wm/{{fields}} placeholder substitution and cursor-advance logic are uniform across types. DELTA EXECUTION
    ROUTES THROUGH THE ADAPTER''S NATIVE CALLER, NOT THE FEDERATION ENGINE: GraphQL delta POSTs the native
    query to the source (predicate pushed down AT source) and lands rows into materialization_store; does
    NOT execute as Trino `WHERE field > x` scan over the federated relation. Native-pushdown path is the
    efficiency win and reason delta exists rather than relying on Trino watermark_column poll (which re-scans
    with no pushdown). Per source type: (1) delta template = native query syntax + filter/cursor binding
    (author-supplied, source-specific); (2) delta execution = routes through source type''s existing native
    caller (GraphQL/HTTP/gRPC/SQL), bypassing federation engine. Pattern mirrors per-adapter mutation
    classification/suggestion (REQ-869/871): uniform contract with per-source-type native implementation.
    APPLY: upsert returned rows on the replica''s ALREADY-REGISTERED PRIMARY KEY (entity identity from
    table registration) to replace prior row state; degrades to plain insert for append-only/immutable
    sources. REPLICA-ONLY: VIEW/mv role is delta-ineligible—REQ-844 Iceberg overwrite-snapshot semantics
    (one clean snapshot per refresh) for time-travel/provenance/ bitemporal (REQ-372/862) would be violated
    by upsert. Mutable relational substrate only. RELATIONSHIP TO FRESHNESS: Freshness (REQ-855/856) is
    pure stale/fresh DECISION with no refresh semantics (REQ-856). Delta is REFRESH ACTION under REQ-826''s
    MATERIALIZED reload — incremental sibling of full re-pull. But for monotonic entries delta query serves
    as freshness evaluation, so REQ-855''s opaque probe is NOT also run: PROBE (opaque token) and DELTA
    (monotonic cursor) are MUTUALLY EXCLUSIVE PER ENTRY, selected by token type. Opaque-token entries
    ⇒ REQ-855 probe + full re-pull, no delta. Monotonic entries ⇒ delta-as-probe, no separate probe. Validation
    gates: (a) delta_query contains both $wm and {{fields}} placeholders; (b) parses; (c) $wm bind-var
    type is orderable/monotonic-compatible; (d) injected selection valid against row type. DONE (2026-07):
    the UNIFORM part is implemented — provisa/federation/delta.py: delta_applies gates delta to MATERIALIZED
    (VIRTUAL/SCAN excluded); render_delta_fields substitutes {{fields}} textually (Provisa never parses
    the source-native filter) leaving $wm for native binding; has_wm_placeholder is the $wm validation
    gate; delta_is_fresh is PROBE==DELTA (empty result ⇒ fresh/no-op, non-empty ⇒ changed/apply); advance_cursor
    advances the stored cursor to max(cursor-field) over the returned rows (empty keeps it). REMAINING
    (kept in-progress): the per-source-type delta authoring + native execution (GraphQL/HTTP/gRPC/SQL
    callers, predicate pushed down at source), keyed-upsert apply on the replica, and the delta/opaque-probe
    mutual-exclusivity wiring with REQ-855.'
  use_case: 'For REPLICAS (any MATERIALIZED-strategy dataset whose federation strategy lands data), full
    re-pull on every refresh is expensive and unnecessary. Delta fetch via monotonic watermark keeps replicas
    fresh with zero lag (REQ-855 probe) without repeated full upstream fetches. Primary use: remote-schema
    sources (graphql_remote/openapi/gRPC) avoid full-relation re-fetch and re-materialize. Also benefits
    RDB sources presently using watermark_column only for subscriptions (REQ-260) — they gain incremental
    replica refresh: keyed upsert instead of full re-pull, no full-CTAS + mutation-triggered staleness
    cycle. Strategy-agnostic scope avoids VIRTUAL (live) and SCAN (in-place read) per REQ-826. Replicas
    in mutable stores absorb deltas; views cannot.'
  code:
  - provisa/federation/delta.py
  - provisa/subscriptions/
  - provisa/materialization/
  - provisa/materialization_store/
  tests:
  - tests/unit/test_delta.py
  - tests/integration/test_materialization_store_lifecycle_e2e.py
  scenario: 'Given a MATERIALIZED-strategy dataset with a monotonic watermark, a delta query authored
    with

    $wm and {{fields}} placeholders, and a replica in a mutable relational store

    When the watermark signals a change (REQ-855 probe)

    And Provisa substitutes $wm with the cursor value and {{fields}} with the table''s selection set

    Then the rendered delta query fetches only rows changed since the last watermark

    And those rows are upserted on the replica''s registered primary key to replace prior state,

    or inserted if the source is append-only

    And the replica remains fresh without full re-materialization

    '
  related_reqs:
  - REQ-855
  - REQ-856
  - REQ-844
  - REQ-826
  - REQ-260
  - REQ-282
  - REQ-845
- id: REQ-875
  status: complete
  since: 2026-07
  group: 6. Execution, Routing, Caching & Performance
  category: Query Routing
  priority: SHOULD
  type: behavioral
  description: 'Cheap-count route: a count(*)-shaped query over an UNMATERIALIZED source that exposes
    an EXACT native count (REQ-673 cardinality capability) is routed to the native count call instead
    of materializing the full dataset to count it. Parallels Route.CACHE (REQ-865) — a query shape that
    resolves to a first-class route bypassing the expensive path. Today SELECT count(*) over a cold API
    source pulls the entire dataset (endpoint.py _materialize_api_to_trino_cache) to count it; with an
    exact native count it becomes a single call. THREE GUARDS, all required. (1) SHAPE: a bare count(*)
    with no other projection and no WHERE the native call cannot honor; a filtered count the source cannot
    filter falls back to materialize. (2) EXACTNESS: fires only when the cardinality Estimate is exact;
    approximate statistics stay in the planner/human-hint lane and are never served as the answer to count(*).
    (3) GOVERNANCE: the route MUST NOT bypass Stage-2 RLS/masking (REQ-263) — when an RLS predicate applies
    to the table for the requesting persona a native total count would over-count the visible subset,
    so the route is disabled and falls back to materialize+count. Fail-closed, the same discipline as
    cache isolation (REQ-866). DONE (2026-07): the three-guard route decision is implemented — provisa/federation/cardinality.py::
    can_route_cheap_count(is_bare_count_star, estimate, rls_applies) fires only when the shape is a bare
    count(*), the REQ-673 estimate is EXACT with a value, and NO RLS predicate applies to the persona;
    any guard failing returns False (fail-closed → materialize+count). REMAINING (kept in-progress): wiring
    it into the endpoint count(*) path so an exact-native-count source answers with one call instead of
    _materialize_api_to_trino_cache.'
  code:
  - provisa/federation/cardinality.py
  tests:
  - tests/unit/test_cardinality.py
  use_case: Turns SELECT count(*) over a cold remote/API table from a full-dataset HTTP pull into a single
    cheap call, while preserving exact-answer semantics and governance, so the volume question is answerable
    interactively without warming the cache.
  scenario: 'Given an unmaterialized API source with an exact cardinality capability and no RLS predicate

    When a user issues a bare SELECT count(*) query with no WHERE clause

    Then the query is routed to the native count call instead of materializing the full dataset

    And when the same query applies to a table where RLS rules restrict the user''s visible rows

    Then the cheap-count route is disabled and the query falls back to materialize+count to preserve governance

    '
  related_reqs:
  - REQ-673
  - REQ-865
  - REQ-826
  - REQ-263
  - REQ-866
  stakeholders:
  - data-engineer
  - app-developer
  competitive_position:
    status: ahead
    rationale: Routing an unmaterialized-source count(*) to a native count call — gated by shape, exactness,
      and RLS — is a federation execution optimization absent in Hasura and Dremio.
- id: REQ-876
  status: complete
  since: 2026-07
  code:
  - docker-compose.dev.yml
  - docker-compose.observability.yml
  - docker-compose.demo.yml
  - docker-compose.dev-install.yml
  - docker-compose.mvmvp.yml
  tests:
  - tests/unit/test_infra_requirements.py
  group: 11. Platform, Infrastructure & Delivery
  category: Deployment & Infrastructure
  priority: SHOULD
  type: infrastructure
  description: 'Compose host-port parameterization for stack coexistence. Every published host port in
    docker-compose.core.yml (and the dev/observability overlays) MUST be expressed as ${VAR:-default}
    so the default preserves current behavior while a second stack can bind a different host port. This
    lets the dev (provisa), e2e (provisa-e2e), and integration (provisa-test) compose projects run simultaneously
    instead of colliding on 5432/8080/ 9000/6379/8480 — the failure mode observed when a second project''s
    minio could not bind 9000, started detached from its network, and its minio-init could not resolve
    minio, hanging the stack at N-1/N. THREE parts: (1) parameterize the published ports in core.yml/dev.yml/observability.yml
    (left side only; container ports fixed). (2) each test harness project (tests/conftest.py integration,
    tests/e2e/conftest.py) sets its own offset port set (e.g. PG_PORT/TRINO_PORT/REDIS_PORT/MINIO_PORT/ZAYCHIK_PORT)
    before compose up. (3) the in-process app under test connects to the matching ports — TRINO_PORT/ZAYCHIK_PORT
    are already env-driven (app.py:1119/1160); the PROVISA_CONFIG fixture''s Postgres host:port must become
    env-interpolated (or the harness must template it) so the app reaches the offset postgres. Isolation
    of the compose PROJECT (‑p) already exists; this closes the host-port dimension so lifecycle-independent
    stacks are also bind-independent.'
  use_case: A developer or CI can run the dev stack, the e2e stack, and an integration test run at the
    same time without tearing one down — each provisions its own services on its own host ports, so no
    run blocks another and no half-attached container corrupts a shared network.
  related_reqs:
  - REQ-815
  - REQ-816
  - REQ-828
  - REQ-830
- id: REQ-877
  status: complete
  since: 2026-07
  scenario: Given an MV with row-level delta capture enabled When the MV refreshes with rows inserted/updated/deleted
    since the prior refresh Then the delta ledger records the insert/update/delete rows under that refresh
    version, and an opt-out MV writes no ledger entries
  group: 1. Access Governance & Security
  category: Row-Level Delta Capture
  priority: MAY
  type: behavioral
  target: 2026-Q4
  description: 'Opt-in, per-table row-level delta capture for materialized views — the row-level companion
    to the column-level lineage trace (REQ-862). Each MV refresh can record which rows changed (insert
    / update / delete) since the prior refresh, so downstream consumers get change-data-capture on a derived
    dataset and audit gets row-granular provenance. NATIVE-FIRST — hashing is the fallback, never the
    default: (1) an Iceberg-target MV derives deltas from a snapshot-to-snapshot changelog scan (no hashing,
    no retained state); (2) a watermarked source (REQ-260 watermark_column) derives the delta from the
    watermark-bounded slice — this IS the output-side mirror of REQ-874 delta-fetch; (3) ONLY an RDB target
    with no watermark falls back to full-MV row hashing. QUERY-GENERATED — MAKE THE DB THE COMPUTE: the
    row hash and the diff are pushed down as SQL (hash via the engine''s md5/sha over the canonical row
    projection; the delta via a set operation — EXCEPT / anti-join — between the current and the previous
    hash relation, the previous snapshot persisted as a relation). Provisa issues the query and lands
    the result (INSERT…SELECT / CTAS in the engine); it MUST NOT SELECT rows out and hash/diff them in
    Python. The delta is (key, change_type, old_hash, new_hash). COLUMN EXCLUSION: the opt-in specifies
    which columns are EXCLUDED from the row hash (e.g. volatile audit/updated_at columns), so a change
    to an ignored column is not reported as a row change — the hash is computed over the included projection
    only. The hash path is the expensive case and is strictly OPT-IN per table (default off). A SYSTEM-WIDE
    config sets the row-count ceiling for the hash path — `max_row_hashing` (global default in provisa.yaml,
    per-table override) — above which hash-based delta capture is SKIPPED and logged (mirrors the existing
    MV materialization max_rows guard); never sampled (sampling would make an audit delta lossy and untrustworthy).
    Deltas land in a SEPARATE append-only ledger keyed by (mv_id, definition_version, refresh_trace_id)
    — the same store-independent principle as mv_refresh_log (REQ-862), never stamped on the target table.
    NON-BLOCKING: delta capture — especially the full-MV hash path — MUST run off the refresh critical
    path in a background thread/task; the refresh completes and marks fresh without waiting on delta computation,
    and a slow or failed delta capture never delays or fails the refresh (best-effort provenance, logged
    on failure).'
  use_case: 'Gives audit and incremental-consumption workflows row-level "what changed between refresh
    N and N+1" without a bespoke CDC service, while bounding cost: capable targets/sources (Iceberg, watermarked)
    get deltas cheaply from the engine, and the expensive full-MV hash path is opt-in and ceiling-guarded
    so very large MVs never silently pay for it.'
  code:
  - provisa/mv/delta.py
  - provisa/mv/refresh.py
  - provisa/mv/models.py
  - provisa/core/schema_org.py
  tests:
  - tests/unit/test_mv_delta_capture.py
  implementation_note: 'Opt-in per-MV row-level delta ledger (mv_delta_ledger) written best-effort off
    the refresh critical path — capture_row_deltas/delta_key/delta_exclude_columns on the MV; diff is
    BY-KEY over a hash of the non-excluded projection. SCOPE vs spec: value-delta tier is implemented
    (events carry old/new row values, enabling REQ-878 content reconstruction); the diff is currently
    computed over the landed row snapshots in-process rather than pushed down as an engine EXCEPT/anti-join,
    and the native-first Iceberg-changelog / watermark-slice derivation and the max_row_hashing ceiling
    remain future optimizations of the same ledger contract.'
  related_reqs:
  - REQ-862
  - REQ-874
  - REQ-826
  - REQ-260
- id: REQ-878
  status: complete
  since: 2026-07
  scenario: Given an MV with row-level delta capture enabled and a delta ledger spanning refresh versions
    1..N When a caller reconstructs the MV as of version K (K<N) via forward-fold from base or reverse
    from the live table Then the reconstructed rows equal the MV state as of version K, and reconstructing
    an unknown version fails loud
  group: 1. Access Governance & Security
  category: Point-In-Time Reconstruction
  priority: MAY
  type: behavioral
  target: 2026-Q4
  description: 'Point-in-time MV reconstruction from the delta ledger (REQ-877). An append-only delta
    ledger is a temporal substrate: folding change events up to a refresh version N (or reversing them
    from the live table back to N) reconstructs the view as of N — time-travel for targets that lack it
    natively. TWO OPT-IN TIERS by fidelity, matching what the ledger stores: (1) HASH-DELTA (REQ-877 default)
    reconstructs MEMBERSHIP and change history only — which row keys existed as of N, and that/when a
    row changed — because a hash proves change but does not invert to old values; (2) VALUE-DELTA stores
    the changed row VALUES, enabling FULL CONTENT reconstruction as of N (true time-travel for an RDB
    target). Value-delta is a second, heavier opt-in — it reimplements what Iceberg gives natively (checkpoint
    snapshots + a changelog), so Iceberg targets get content time-travel for free and value-delta buys
    it for RDB targets at a storage cost. CHECKPOINT-BOUNDED REPLAY: do not fold all history — persist
    a periodic full snapshot plus deltas between; reconstruct = nearest checkpoint <= N, then apply deltas
    up to N (Iceberg''s model). RECONSTRUCTION IS A QUERY — MAKE THE DB THE COMPUTE (same discipline as
    REQ-877): as-of-N is a windowed greatest-per-key query over the ledger (last event with version <=
    N per key, excluding deletes), executed in the engine, never a Python replay. Non-blocking and system-config
    bounded like REQ-877.'
  use_case: Lets a user query "the view as of refresh N / time T" for MVs whose target is not Iceberg,
    without a bespoke temporal store — membership-and-audit reconstruction cheaply from hashes, or full
    content reconstruction when the value-delta tier is opted in, with replay cost bounded by checkpoints
    and the whole reconstruction expressed as a single ledger query.
  code:
  - provisa/mv/delta.py
  - provisa/mv/refresh.py
  - provisa/core/schema_org.py
  tests:
  - tests/unit/test_mv_delta_capture.py
  implementation_note: 'reconstruct_forward folds the ledger from base as the windowed greatest-per-key
    query over refresh_version <= N (executed in the store, deletes excluded); reconstruct_reverse derives
    the same as-of-N set from the live rows by correcting only keys touched after N (restoring updates,
    resurrecting deletes, dropping after-N inserts). Value-delta tier gives full content reconstruction.
    Unknown version fails loud. SCOPE vs spec: checkpoint-bounded replay (periodic full snapshot + deltas-between)
    is not yet persisted — reconstruction folds from ledger base; membership- only hash tier is covered
    by the value tier superset.'
  related_reqs:
  - REQ-877
  - REQ-862
  - REQ-826
  - REQ-874
- id: REQ-879
  status: complete
  since: 2026-07
  group: 4. Source Connectors
  category: Materialization Store
  priority: MUST
  type: structural
  target: 2026-Q4
  description: 'Cross-instance MV refresh coordination for a load-balanced fleet sharing one materialization
    store. Today the live refresh state is per-instance in-memory (MVRegistry._mvs; mark_refreshing flips
    an in-process status), so instance A''s REFRESHING is invisible to B and both refresh the same MV
    — redundant compute plus concurrent writers racing on the same relation. FIX: promote the materialized_views
    catalog into the AUTHORITATIVE SHARED refresh-state and drive the refresh loop off it (not the per-instance
    registry). Add columns: writer, lease_until, materialized_definition_version, materialized_input_version,
    snapshot_id. PROTOCOL: (1) ATOMIC CLAIM — a single conditional UPDATE that BOTH dedups by version
    (skip when materialized_input_version == target, the REQ-862 stamp is the already-written key) AND
    excludes concurrent writers (skip when status=refreshing AND lease_until>now). 0 rows = skip; 1 row
    = this instance owns the refresh. This is the per-MV election — DECENTRALIZED, no global leader; a
    heartbeat renews lease_until during a long refresh, and lease expiry reclaims after a crash. (2) FENCED
    COMMIT — finalize (set materialized_version/status/ snapshot_id) only WHERE writer=me AND lease_until>=now;
    0 rows means the lease was lost (slow/crashed-then-revived) so the result is DISCARDED — prevents
    a stale writer clobbering a newer refresh (fencing token). (3) SOURCE OF TRUTH — the store''s committed
    snapshot (atomic commit, e.g. Iceberg) is authoritative for what is materialized; the catalog is coordination
    metadata reconciled to the store on lease expiry, so a crash mid-write leaves no partial data. NOT
    held pessimistic locks (SELECT FOR UPDATE over a long refresh holds a transaction open and a crash
    leaves a stuck lock) — leases + fencing. The catalog is the same structure that holds freshness (REQ-855)
    and the version/lineage stamps (REQ-862). CONSISTENCY KNOB: a per-MV `consistency` setting selects
    the tier — `shared` (default) engages this coordination catalog (one snapshot-consistent copy); `distributed`
    per-instance-materializes and does NOT engage it. The distributed tier is only eventually consistent
    under TWO preconditions: (a) a DETERMINISTIC view_sql — enforced at registration (reject now()/random()/uuid/unordered-LIMIT;
    provisa/mv/determinism.py); and (b) source QUIESCENCE — the source must stop changing long enough
    within a refresh cycle for all instances to materialize the same source state. A never-settling high-churn
    source never converges (each instance perpetually reflects a different snapshot), so the distributed
    tier is inappropriate for it — determinism is necessary but not sufficient. Quiescence is a workload
    property, not statically checkable, so it is an operational constraint on choosing `distributed`,
    whereas determinism is validated.'
  use_case: Lets an arbitrary number of control-plane/engine instances run behind a load balancer against
    one shared materialization store without redundant refreshes, write races, or a single-leader bottleneck
    — each instance races to claim due MVs, exactly one wins per MV, and crashes are handled by lease
    expiry and fenced commits.
  code:
  - provisa/mv/registry.py
  - provisa/mv/refresh.py
  - provisa/mv/coordination.py
  - provisa/core/schema_org.py
  tests:
  - tests/unit/test_mv_fleet_coordination.py
  related_reqs:
  - REQ-826
  - REQ-830
  - REQ-855
  - REQ-862
  - REQ-844
- id: REQ-880
  status: complete
  since: 2026-07
  scenario: 'Given the materialized-view store is configured with a SQLAlchemy URI (e.g. mysql://host/db)

    When a materialized view or relationship is refreshed into the store

    Then the store write face is derived from the URI backend, not the hardcoded Trino "postgresql" catalog


    Given a self-only (sqlalchemy) engine whose native store is mysql

    When a separate postgresql LAND-only backend is validated against it

    Then the materialization backend is rejected as a reachability regression

    '
  tests:
  - tests/unit/test_federation_materialization.py
  group: 4. Source Connectors
  category: Materialization Store
  priority: MUST
  type: constraint
  target: 2026-Q4
  description: The materialized-view store (MV cache target for materialized relationships and views)
    must be configurable as a SQLAlchemy URI instead of pinned to a specific database type. Today it is
    hardcoded to the Trino catalog literal "postgresql", preventing the materialization backend from using
    any SQLAlchemy-supported database.
  use_case: Supports flexible deployment scenarios where the materialization backend may be a dedicated
    data warehouse, distributed datastore, or cloud analytics system independent of the source connectors.
  code:
  - provisa/api/app.py
  - provisa/mv/refresh.py
  - provisa/core/schema_org.py
  related_reqs:
  - REQ-158
  - REQ-159
  - REQ-135
  - REQ-879
- id: REQ-881
  status: complete
  since: 2026-07
  group: 4. Source Connectors
  category: Materialization Store
  priority: SHOULD
  type: behavioral
  target: 2026-Q4
  description: 'Probe-based freshness gate on the MV REFRESH path, opt-in per MV — the refresh-time application
    of REQ-855''s centralized freshness gate to the materialized-view role. MOTIVATION: relationship-based
    (join-pattern) MVs are expensive to rebuild — a full Trino CTAS / DELETE+INSERT of the join every
    cycle. Today the background refresh loop gates purely on TTL (MVRegistry.get_due_for_refresh compares
    now - last_refresh_at >= refresh_interval), so a due MV pays the full rebuild every interval EVEN
    WHEN NO SOURCE CHANGED. FIX: add a per-MV freshness_mode (ttl [default, current behavior] | probe
    | ttl_probe). For probe / ttl_probe, before the expensive materialization the refresh computes an
    opaque per-MV change token from the REQ-862 per-source input signals (Iceberg snapshot id / RDB watermark
    MAX, already gathered by provisa/mv/input_signals.py::gather_input_signals) and compares it by equality
    to the token stored at last refresh: unchanged → skip the rebuild, reset the TTL, keep the materialized
    rows and FRESH status (zero lag at probe cost); changed → rebuild and store the new token. ttl_probe
    probes only after the TTL floor elapses (caps probe frequency); probe re-checks every loop ignoring
    TTL. SAFETY: a token is usable only when EVERY source produces one (len(signals) == len(source_tables));
    if any source is token-less the entry degrades to TTL and rebuilds normally — never skip on partial
    signal (REQ-855 None-degrades-to-TTL, REQ-847 capability-signal-not-silent-fallback). Opt-in is per
    MV so cheap MVs keep plain TTL and only expensive relationship MVs pay the probe. Mutual exclusivity
    with delta (REQ-874) and the shared coordination catalog (REQ-879) follow REQ-855: the same materialized_input_version
    stamp is the probe token and the dedup key.'
  use_case: Lets a steward gate an expensive relationship-based MV so its costly join is rebuilt only
    when a source actually changed, trading a cheap per-source probe (snapshot id / watermark) for skipping
    a full re-materialization every interval — zero-lag freshness where rebuild cost is high, plain TTL
    where it is not.
  code:
  - provisa/mv/models.py
  - provisa/mv/registry.py
  - provisa/mv/refresh.py
  - provisa/mv/input_signals.py
  tests:
  - tests/unit/test_mv_probe_freshness.py
  scenario: 'Given a relationship MV with freshness_mode="probe" over an Iceberg-backed source and a

    last_input_token equal to the source''s current snapshot id

    When the refresh loop processes it

    Then the refresh computes the source token, finds it unchanged, and skips the rebuild

    (no DELETE/CREATE) — resetting the TTL and keeping the materialized rows FRESH

    And when the source snapshot id later differs, the same MV rebuilds and stores the new token

    And an MV where any source yields no token degrades to plain TTL (never skips on partial signal)

    '
  notes: 'DONE (2026-07): MVDefinition gains freshness_mode (ttl | probe | ttl_probe) + last_input_token;
    MVRegistry.get_due_for_refresh returns probe-mode MVs every loop (ttl_probe keeps the TTL floor);
    refresh_mv gathers the REQ-862 per-source signals first and, for probe/ttl_probe, builds an input_token
    (usable only when EVERY source produced a signal, else degrade to TTL) and skips the expensive rebuild
    + size-count probe via MVRegistry.mark_unchanged when it equals last_input_token, storing the fresh
    token after a real rebuild. Single-instance token store today; fleet-wide persistence of the token
    is REQ-879''s shared refresh-coordination catalog (by design — the same stamp is the dedup key there).'
  related_reqs:
  - REQ-855
  - REQ-862
  - REQ-874
  - REQ-879
  - REQ-847
- id: REQ-882
  status: complete
  since: 2026-07
  group: 4. Source Connectors
  category: Materialization Store
  priority: SHOULD
  type: behavioral
  target: 2026-Q4
  description: 'Wire the aggregate MV rewrite path into the live query execution path. Populate AggregateMVCatalog
    from MVRegistry on startup, invoke aggregate rewrite in the endpoint query path, and enforce filter
    subset-safety before rewriting. DONE (2026-07): (1) POPULATE — MVRegistry.register/unregister now
    sync the process aggregate-MV catalog (provisa/mv/aggregate_catalog.get_aggregate_catalog()), so every
    MV registered at startup is visible to rewriting; register() is a no-op for non-aggregate MVs. (2)
    INVOKE — the endpoint query path calls rewrite_if_aggregate_match after the join-MV rewriter (provisa/mv/rewriter.py);
    only when the join rewrite did not already fire. It extracts a single-table aggregate query via sqlglot
    (base table, columns inside aggregate functions, top-level AND filter fragments; queries with a JOIN,
    multiple tables, or no aggregate are left alone) and, on a covering MV, rewrites FROM to the MV target
    and sets sources to the MV catalog. (3) SUBSET-SAFETY — find_aggregate_mv now enforces it (the prior
    code skipped the check its docstring promised): a new MVDefinition.filters records the predicates
    the aggregate MV was pre-computed with, and an MV is usable only when set(mv.filters) is a SUBSET
    of the query''s filters (an unfiltered MV is always safe); the query''s remaining filters (query −
    MV) are re-applied on the MV at rewrite so no rows the query wants are silently dropped.'
  use_case: Enables query acceleration via pre-computed aggregates stored in materialized views, allowing
    queries requesting SUM/AVG/etc over MV-backed tables to read pre-aggregated results instead of materializing
    raw rows and computing aggregates at query time.
  code:
  - provisa/mv/aggregate_catalog.py
  - provisa/mv/registry.py
  - provisa/mv/rewriter.py
  - provisa/mv/models.py
  - provisa/api/data/endpoint.py
  tests:
  - tests/unit/test_aggregate_mv_catalog.py
  scenario: 'Given an aggregate MV over "orders" pre-computing SUM(amount), registered with no filters,

    and the aggregate catalog populated from the MV registry

    When a query "SELECT SUM(amount) FROM orders WHERE region = ''us''" reaches the endpoint and the

    join-MV rewriter did not fire

    Then the query is rewritten to read the MV target table with region = ''us'' re-applied, and its

    sources become the MV catalog

    And an MV pre-computed WITH status = ''active'' is NOT used for a query that lacks that filter

    (subset-safety), so no rows are silently dropped

    '
  related_reqs:
  - REQ-198
  - REQ-199
  - REQ-849
- id: REQ-883
  status: complete
  since: 2026-07
  group: 4. Source Connectors
  category: Federation Engine Abstraction
  priority: SHOULD
  type: behavioral
  target: 2026-Q4
  description: 'Provisa''s pgwire server must satisfy the capability set that DuckDB''s `postgres` extension
    exercises when attaching Provisa via `ATTACH ... AS x (TYPE postgres)`, enabling DuckDB federation
    engines and external DuckDB clients to mount a governed Provisa instance with RLS/masking intact.
    Concrete capabilities enumerated: (1) libpq connection + auth handshake; (2) catalog introspection
    queries for schemas/tables/ columns/types/PKs over pg_namespace, pg_class, pg_attribute, pg_type,
    pg_index, pg_constraint with correct atttypmod and pg_type OIDs; (3) bulk read via `COPY (SELECT ...)
    TO STDOUT (FORMAT binary)` with real PostgreSQL binary COPY wire encoding per column type; (4) transaction-control
    statements (`BEGIN`/`COMMIT`/`ROLLBACK`, incl. `SET TRANSACTION READ ONLY` / `REPEATABLE READ`) accepted
    as no-ops — NO real snapshot isolation is provided or needed; (5) single-stream scan ONLY. Snapshot
    export, server-side cursors, and ctid-range parallelism are explicit NON-GOALS, validated empirically
    against DuckDB in the sibling pgwire-calcite project (PGW-004 / PGW-023): DuckDB''s ctid parallelism
    splits a table by physical page range, but Provisa exposes no physical row address, so honoring a
    ctid range would force a `ROW_NUMBER() OVER (ORDER BY ...)` partition that scans+sorts the whole table
    per stream — strictly worse than one stream. The server reports relpages/reltuples ~ 0 in pg_class
    so DuckDB never elects parallelism and reads a single correct binary-COPY stream. Server version >=
    14 (DuckDB''s gate) is already met (server_version ''14.0.provisa''). DONE (2026-07) capability (3)
    — BINARY COPY output: COPY ... TO STDOUT (FORMAT binary) now emits a real PostgreSQL binary COPY stream
    (provisa/pgwire/copy_handler.py): the 11-byte PGCOPY signature + flags + header-extension, per-row
    int16 field count with length-prefixed per-field bodies (int32 -1 for NULL), and the int16 -1 trailer.
    Per-column encoding is selected from the column''s advertised type (Arrow schema on the Trino Flight
    bulk path; DuckDB type strings via QueryResult.column_types on the direct path) — bool/int2/int4/int8/float4/float8/bytea
    encode to their PG binary representations, everything else to UTF-8 text matching the text OID — and
    the CopyOutResponse overall-format flag is set to 1 (binary). Capabilities (1) libpq handshake and
    (2) catalog introspection are served by the existing pgwire server + catalog; (4) transaction-control
    statements are accepted as no-ops by the txn-tag path. DONE (2026-07) the COPY parser now accepts
    DuckDB''s actual `(FORMAT "binary")` syntax — optional WITH and a quoted format word — which the older
    regex rejected (all three COPY regexes in provisa/pgwire/copy_handler.py); and a live end-to-end DuckDB
    1.5.3 `ATTACH ... TYPE postgres` test drives all five capabilities against a running server (tests/integration/test_duckdb_attach_pgwire.py):
    ATTACH + catalog discovery, binary-COPY row/type/NULL round-trip, transaction no-ops via postgres_execute,
    and relpages=0 single-stream. Capability (5) is confirmed a non-goal, not remaining work. COMPLETE.'
  use_case: Lets DuckDB (as the REQ-825 federation engine or any external DuckDB/BI client) mount a governed
    Provisa instance via `ATTACH ... TYPE postgres` and query it with RLS/masking intact, completing the
    develop-on-DuckDB story where DuckDB reaches Provisa itself the same way it reaches any Postgres.
  code:
  - provisa/pgwire/server.py
  - provisa/pgwire/copy_handler.py
  - provisa/pgwire/catalog.py
  - provisa/pgwire/_pipeline.py
  tests:
  - tests/unit/pgwire/test_copy_binary.py
  - tests/integration/test_duckdb_attach_pgwire.py
  scenario: 'Given a client issues COPY (SELECT id, name FROM t) TO STDOUT (FORMAT binary) over pgwire

    When the result has an INTEGER id and a VARCHAR name column

    Then the server replies with a binary-mode CopyOutResponse and a byte stream beginning with the

    PGCOPY signature, encoding id as a 4-byte int and name as UTF-8 text per row, a NULL as field

    length -1, and terminating with the int16 -1 trailer

    And the same COPY in text/csv format is byte-for-byte unchanged from before

    '
  related_reqs:
  - REQ-825
  - REQ-826
  - REQ-842
  - REQ-527
- id: REQ-884
  status: complete
  since: 2026-07
  code:
  - provisa/api/_meta_views.py
  - provisa/api/startup_seed.py
  - provisa/api/app_loaders.py
  - provisa/audit/query_log.py
  tests:
  - tests/unit/test_ops_domain.py
  group: 1. Access Governance & Security
  category: Observability
  priority: MUST
  type: behavioral
  description: All Provisa internal logs—starting with the query audit log (REQ-074) and extending to
    any other operational/observability logs—must be materialized and exposed as first-class registered
    tables within a dedicated "ops" domain in Provisa's federated catalog.
  use_case: Makes operational telemetry queryable through the governed pipeline (pgwire/SQL/GraphQL/Cypher)
    subject to role + domain access control, instead of only via the Python export path (export_audit_log)
    or raw control-plane JDBC access which bypass governance. Enables self-observability/dogfooding where
    an MCP/AI planner can read observability signals through the same governed surface as business data.
    Closes the gap that query_audit_log is not currently exposed in the federated pgwire catalog.
  scenario: 'Given a Provisa instance with query_audit_log records and a user with role r that has ops
    domain access

    When the user queries "SELECT query, user, status FROM ops.query_audit_log WHERE timestamp > ..."

    over pgwire

    Then the query returns only the audit log rows that satisfy the user''s domain access policy,

    enforced by the governed access control layer

    '
  related_reqs:
  - REQ-074
  - REQ-527
- id: REQ-885
  status: complete
  since: 2026-07
  group: 5. Query Languages, Compilation & Operations
  category: Extensible Functions
  priority: SHOULD
  type: behavioral
  description: 'Generalize tracked functions (REQ-205–208: source-resident stored procedures exposed as
    GraphQL mutations) to support multiple implementation kinds: source procedure (existing), script (local
    subprocess), http, grpc, and optionally python (in-process). All transports now functional: gRPC uses
    proto-less generic JSON unary bridge (_grpc_call in function_dispatch.py) via grpc.aio insecure/secure
    channels, with method path normalization and tls/timeout_s binding keys honored. Implementation kind
    becomes a selectable dimension at registration; addressing (catalog name) is decoupled from binding
    (transport+location, swappable). Provisa-hosted functions take relation arguments (kinds: table_ref=lazy,
    result_set=eager materialized Arrow, column_value=scalar row-wise) and return Arrow buffers materialized
    as relations, governed at the read-time boundary. Identity model (DEFINER/admin vs INVOKER/user) selected
    by materialize flag: true ⇒ admin/output-governed; false ⇒ user/input-governed. Composition via view_sql
    + materialize; no new MV subsystem—reuse MV refresh (REQ-879), CTAS sink, governance, lineage primitives.'
  use_case: Enables diverse computation platforms (hosted scripts, external services, Python libraries)
    to act as governed data transformations, returning relations (not just scalar functions). Maintains
    config-as-truthful-model property with URI addressing and spec-driven discovery (OpenAPI/proto/ script-manifest).
    Preserves existing source-procedure tracked-function contract while extending to Provisa-hosted and
    external implementations.
  code:
  - provisa/executor/function_dispatch.py
  - provisa/api/data/action_exec.py
  - provisa/api/app.py
  - provisa/core/models.py
  - provisa/core/repositories/function.py
  - provisa/core/schema_org.py
  - provisa/api/admin/actions_router.py
  - provisa-ui/src/pages/commands/CommandFormFields.tsx
  - provisa-ui/src/pages/commands/CommandsPage.tsx
  - provisa-ui/src/pages/commands/types.ts
  - provisa-ui/src/pages/commands/api/actions.ts
  tests:
  - tests/unit/test_extensible_functions.py
  scenario: 'Given a user registers a function with implementation kind=http and argument kinds=[table_ref,
    column_value]

    and a view references it as SELECT * FROM myfn(tbl, param) WITH (materialize=true)

    When Provisa materializes the view (e.g. via CTAS or scheduled MV refresh)

    Then the function invokes the http endpoint as admin (definer), passing table metadata/hint

    and row-wise scalar, receives Arrow buffer, materializes as a relation, and applies row-level

    access governance on output before serving to end users

    '
  open_decisions:
  - Eager vs streamed as default carrier for result_set arguments
  - Reentrancy/cycle guard when a function queries its own dependents (engine cannot see dependency graph
    statically)
  - Exact credential-minting mechanism for invoker-identity functions (scoped, single-use, short-TTL pgwire
    session tokens)
  security_constraints: 'Registration requires admin privilege for system-processed (materialize=true)
    functions; users may register only invoker-context (materialize=false) functions bounded by their
    own rights. Hosted functions receive minted, scoped, single-use, short-TTL credentials (no ambient
    passwords in env/ config). Egress deny-by-default allow-list enforced: _check_egress refuses http/grpc
    calls whose host is not in state.udf_egress_allowlist (host or host:port entries); loopback (Provisa
    pgwire) always allowed. Config sourced from server.udf_egress_allowlist in provisa.yaml and PROVISA_UDF_EGRESS_ALLOWLIST
    env (comma-separated), loaded in provisa/api/app.py. Process isolation + resource limits close DoS.
    Row-wise calls to external services are refused/ hard-warned; set-wise/batched required for expensive
    or external work. Honest staging: ship-now requires trusted-author-only trust model (same as stored
    procedures/dbt); opening to less-trusted authors requires confinement (egress control + process isolation).'
  performance_constraint: 'Row-wise (column_value kind) permitted only for cheap pure local scalars; expensive
    or external work must be set-wise and batched (function receives relation, makes batched outbound
    calls). External row-wise refusal enforced: _reject_rowwise_external refuses an http/grpc function
    whose declared arguments are all column_value scalars (no table_ref/result_set relation argument).
    Row-wise kills pushdown (opaque projection) and prevents engine optimization. Detection via ops-domain
    audit (REQ-884) duration_ms; prevention via refusal for external row-wise and cardinality warning
    for row-wise applied past N estimated rows.'
  observability: Engine-emitted I/O-boundary trace (REQ-886) is the non-bypassable floor; function-emitted
    OTel tracing via W3C traceparent propagation is encouraged ceiling. Correlation ID stamped into minted
    pgwire session joins invocation to governed reads for precise lineage.
  ui: No new top-level nav category or actionType noun. Generalized existing Commands "function" type
    with implementation/body selector (source procedure | script | http | grpc). Referenced from Views/Materialized
    Views as definition source.
  related_reqs:
  - REQ-205
  - REQ-206
  - REQ-207
  - REQ-208
  - REQ-879
  - REQ-884
  - REQ-886
- id: REQ-886
  status: complete
  since: 2026-07
  group: 1. Access Governance & Security
  category: Observability
  priority: MUST
  type: behavioral
  target: 2026-Q4
  description: Engine-emitted I/O tracing for UDF/transformer invocations must be mandatory and non-bypassable,
    recording UDF name, transport type (script/http/grpc/sql/python), identity context (admin/definer
    vs user/invoker), declared input refs, output cardinality/size, duration_ms, and status. A trace/correlation
    ID must be stamped into the UDF's minted pgwire session so audit rows join to the invocation trace
    for precise lineage reconstruction. UDF-emitted tracing (via W3C traceparent context propagation)
    is encouraged but optional, allowing UDF interiors and egress calls to nest under the engine-side
    invocation span.
  use_case: Establishes a non-bypassable observability floor (relied on for governance and audit) while
    encouraging voluntary UDF-emitted interior/egress tracing as the ceiling for debugging and performance
    analysis. Enables precise lineage reconstruction by correlating UDF invocation traces with pgwire
    data-access audit rows.
  code:
  - provisa/otel_compat.py
  - provisa/executor/function_dispatch.py
  tests:
  - tests/unit/test_udf_tracing.py
  scenario: 'Given a user invokes a UDF (transformer or custom function) via pgwire or SQL

    When the UDF executes and accesses tables via pgwire data derefs

    Then the engine records an invocation trace with UDF name, transport, identity, input

    refs, output size, duration, and status, stamping a trace ID into the UDF session so

    that pgwire audit rows for the UDF''s data access join to the invocation trace via

    correlation ID, reconstructing the full data lineage precisely

    And if the UDF implements W3C traceparent propagation, its interior spans nest under

    the invocation span automatically via OTEL context

    '
  related_reqs:
  - REQ-884
  - REQ-885
- id: REQ-887
  status: complete
  since: 2026-07
  group: 4. Source Connectors
  category: RDBMS Stored Procedure Auto-Discovery
  priority: SHOULD
  type: behavioral
  target: 2026-Q3
  description: Extend database source introspection to auto-discover stored procedures and routines (via
    information_schema.routines, pg_proc, or vendor equivalents) and auto-register them, rather than requiring
    hand-registration. Classify discovered procedures as read-returning (registered as parameterized relations;
    proc arguments become query arguments) or side-effecting (registered as mutations/tracked functions).
    Discovered proc results flow through Stage-2 governance identically to tables.
  use_case: 'Meet legacy enterprise estates (Oracle/SQL Server shops with large proc inventories) at their
    current state, ensuring nothing in the estate is invisible to the catalog. Mirrors the OpenAPI discovery
    pattern (REQ-316/317): introspection + auto-registration avoiding hand-registration. Value scales
    with proc inventory; low build cost because it reuses existing introspection and OpenAPI-style registration.'
  code:
  - provisa/api/admin/introspect.py
  - provisa/discovery/catalog_cache.py
  tests:
  - tests/unit/test_stored_proc_discovery.py
  scenario: 'Given a user connects a Postgres source with stored procedures (identified via pg_proc)

    When introspection runs and analyzes prokind (p=procedure, f=function) and provolatile (stable/immutable
    vs volatile)

    Then Provisa auto-registers stable/immutable procedures as parameterized relations (proc arguments
    as query params, same shape as OpenAPI GET tables) and volatile procedures as mutations/tracked functions,
    making results queryable via GraphQL and SQL with governance applied identically to hand-registered
    procs

    '
  related_reqs:
  - REQ-205
  - REQ-206
  - REQ-207
  - REQ-208
  - REQ-316
  - REQ-317
  - REQ-319
  - REQ-885
  competitive_position:
    status: parity
    rationale: Cloud data warehouses (Snowflake, BigQuery) auto-discover and expose stored procedures
      in their catalogs. This requirement brings Provisa to parity for legacy database sources that rely
      on stored procedures as a primary abstraction.
- id: REQ-888
  status: complete
  since: 2026-07
  group: 6. Execution, Routing, Caching & Performance
  category: Execution & Routing
  priority: MUST
  type: constraint
  description: Federation engine contract is wired into the live query path via EngineRuntime, bound mandatorily
    at AppState construction as state.federation_engine. Engine-specific consumer transports are capability-gated
    via EngineCapability enum (ROWS/ARROW/ARROW_STREAM); missing capabilities fail closed via UnsupportedCapabilityError
    with no fallback.
  use_case: Mandatory EngineRuntime binding and capability-gating defines the defensible abstraction boundary
    for platform-specific escape hatches. Named, advertised capabilities prevent arbitrary injected connections
    and enforce consistent routing (DIRECT → native driver, ENGINE → federated execution) across all consumer
    paths (pgwire, GraphQL, Arrow Flight, COPY handler, NL executor).
  code:
  - provisa/federation/runtime.py
  - provisa/api/graphql/
  - provisa/bolt/
  - provisa/arrow_flight/
  - provisa/query_helpers.py
  tests:
  - tests/integration/test_engine_runtime_binding.py
  - tests/unit/test_engine_capabilities.py
  related_reqs:
  - REQ-825
  - REQ-840
- id: REQ-889
  status: complete
  since: 2026-07
  tests:
  - tests/unit/test_arch_invariants.py
  target: 2026-Q4
  group: 11. Platform, Infrastructure & Delivery
  category: Deployment Tier Architecture
  priority: MUST
  type: constraint
  description: 'The metadata/config/roles store MUST be the embedded single source of truth in every deployment
    tier. The embedded tier (SQLite or DuckDB) is the metadata home; container tier adds Trino + observability
    strictly as COMPUTE, never as metadata home. Tier changes MUST be additive and reversible: Trino can
    be toggled on/off without migrating the catalog.'
  use_case: Tier changes must be reversible without catalog migrations. If Postgres becomes the metadata
    home in a container tier, it reintroduces a container dependency and creates a migration problem on
    every tier toggle, breaking the design invariant that compute is pluggable while metadata stays embedded.
  notes: 'Scope clarification (what is and is not a metadata-home blocker): Provisa governance roles (analyst/admin,
    RLS, masking, capabilities) already live entirely in metadata tables + the governance layer and never
    touch a database role system — no gap. The PG `CREATE ROLE role_<org_id>` (db.py:82) is per-org PHYSICAL
    schema isolation invoked only by provision_org() from the multi-tenant SaaS admin endpoint (POST /admin/orgs),
    not at base startup; it is compute/deployment-tier hardening and is now a NO-OP on every non-PostgreSQL
    control-plane backend (create_org_role/provision_org/deprovision_org gate on dialect=="postgresql"),
    so it is structurally unreachable on an embedded home. PG LISTEN/NOTIFY (database.py:454) is a DATASOURCE
    feature (subscriptions/CDC/triggers on a PG source, via pg_provider) correctly gated on source type
    — NOT a metadata-home dependency. The genuine remaining metadata-home item is the control-plane bootstrap
    DDL (schema.sql is PG-only), which already has a portable path (_init_schema_portable via the schema_org
    SQLAlchemy metadata) for embedded backends.'
  code:
  - provisa/core/db.py
  - provisa/core/database.py
  - provisa/core/desktop_profile.py
  related_reqs:
  - REQ-825
  - REQ-840
  - REQ-888
- id: REQ-890
  status: in-progress
  since: 2026-07
  scenario: 'Given pgwire launched with the ''oidc'' auth provider (issuer URL + audience configured)

    When a client presents an OIDC ID token (JWT) as the password

    Then the token is verified against the issuer JWKS and mapped to a role via resolve_role


    Given an invalid or tampered OIDC token

    When authentication is attempted

    Then a FATAL 28P01 is returned and no session is established


    Given the cleartext/simple provider is not explicitly enabled

    When a client attempts cleartext auth

    Then the connection is refused


    Given any configured auth provider

    When a client presents an ordinary password in the startup packet

    Then it is validated through the provider''s basic presentation and the role comes from the validated
    identity, not the supplied username


    Given a client presents a personal access token as the password

    When authentication is attempted

    Then the token prefix selects the bearer presentation and the token validates as an ordinary bearer
    credential


    Given auth.default_role is not configured

    When an authenticated identity matches no role-mapping rule

    Then the connection fails rather than landing on an arbitrary role

    '
  tests:
  - tests/unit/test_pgwire_requirements.py
  - tests/unit/test_auth_providers.py
  - tests/integration/test_pgwire_integration.py
  target: 2026-Q4
  group: 4. Source Connectors
  category: Federation Engine Abstraction
  priority: SHOULD
  type: behavioral
  description: Pgwire auth must be a pluggable provider interface selected at launch (trust | local |
    oidc), superseding today's fixed MD5/cleartext wiring while keeping trust/cleartext as the baseline.
    (a) A local-accounts provider stores SCRAM-SHA-256 verifiers at rest (RFC 5802 — random salt, iterations,
    StoredKey/ServerKey; no cleartext stored or required on the wire, secure without TLS) with a CLI to
    add/remove/list users, and offers SCRAM-SHA-256 as a wire auth mechanism. (b) An external-token provider
    accepts an OIDC ID token (JWT) presented as the password and verifies it against the issuer's JWKS
    (signature, exp, aud, iss); issuer-generic (Firebase/Auth0/Google/Entra) via configured issuer URL
    + audience. (c) Every configured provider authenticates pgwire, not only oidc and simple. The startup
    packet carries a username and one cleartext secret and no scheme field, so the presentation is decided
    once from what the secret is — a personal access token by its prefix and a bearer/JWT provider''s
    secret are presented as `bearer` (REQ-1263), everything else as `basic` — and a credential the chosen
    validator refuses is never retried against another. Validators are submitted to the pgwire event loop
    rather than a private one, because the token store and DB-backed providers hold loop-bound handles.
    The session role is resolved from the validated identity via resolve_role against auth.default_role,
    which is required configuration; a provider that cannot be constructed answers FATAL 28P01 on the
    wire instead of dropping the connection.
  use_case: Pgwire connections to Provisa today use MD5 or cleartext auth, neither of which meet enterprise
    security standards. SCRAM-SHA-256 (salted, iterated, server-side verification) secures local accounts
    without requiring TLS. OIDC integration allows enterprises to reuse their identity provider, eliminating
    separate credential management for database access.
  code:
  - provisa/pgwire/server.py
  related_reqs:
  - REQ-883
  - REQ-527
- id: REQ-891
  status: complete
  since: 2026-07
  scenario: 'Given an authenticated role whose grant excludes table ''secret''

    When the role queries information_schema/pg_catalog through the catalog intercept

    Then ''secret'' is filtered out of discovery results


    Given the same role issues a query referencing an object outside its grant

    When the query executes

    Then execution rejects the access rather than returning rows

    '
  tests:
  - tests/unit/pgwire/test_catalog.py
  target: 2026-Q4
  group: 4. Source Connectors
  category: Federation Engine Abstraction
  priority: SHOULD
  type: behavioral
  description: 'Pgwire per-role authorization maps an authenticated account to Calcite/source schema+table
    visibility, enforced in BOTH discovery and execution by construction: the catalog intercept (provisa/pgwire/catalog.py)
    filters pg_catalog/information_schema discovery to the role''s allowed objects, and query execution
    rejects access to objects outside the role''s grant. Complements existing RLS/masking (which govern
    rows/columns) with object-level visibility over the wire.'
  use_case: Pgwire clients (DBeaver, DataGrip, DuckDB) discover database schemas via pg_catalog and information_schema
    queries. Without object-level authorization filtering, a Pgwire client will display all accessible
    Calcite schemas/tables to the user even if the user's role should not see them. Enforcing role visibility
    in both discovery and execution prevents information leakage and aligns object access with existing
    RLS/masking governance.
  code:
  - provisa/pgwire/catalog.py
  - provisa/pgwire/_pipeline.py
  related_reqs:
  - REQ-883
- id: REQ-892
  status: complete
  since: 2026-07
  group: 4. Source Connectors
  category: Connector Abstraction
  priority: MAY
  type: structural
  description: 'Pgwire may present popular PostgreSQL extension surfaces backed by the federation engine,
    as a pluggable opt-in-per-deployment mechanism: each surface (a) advertises itself in pg_extension
    so clients see it "installed", (b) declares its types/OIDs in the single normalization module, (c)
    maps its operators/functions to an engine function or rejects them explicitly (convert-or-reject-loudly
    — a surface must NOT claim a capability, e.g. index acceleration, it does not implement). Concrete
    surfaces: pgvector-compatible (vector type + distance operators <-> L2, <=> cosine, <#> inner product,
    including ORDER BY emb <-> $1 similarity search, NOT advertising ivfflat/hnsw index acceleration);
    PostGIS-subset (geometry/geography + ST_Distance/ST_Contains/ST_DWithin/ST_Intersects/&&); JSON operators
    (-> ->> #> #>> mapped to JSON_VALUE/JSON_QUERY); and compatibility functions (pg_trgm similarity()/%,
    gen_random_uuid(), pgcrypto digest()).'
  use_case: Pgwire clients assume PostgreSQL extensions are available (pgvector for ML/embedding workloads,
    PostGIS for geospatial, JSON operators for document processing). Making these surfaces available over
    the federation engine eliminates the need to add specialized backends or custom UDFs; a single Provisa
    pgwire endpoint can serve diverse analytics workloads (ML, geospatial, document) via compatibility
    operators mapped to federation engine functions.
  code:
  - provisa/pgwire/ext_surfaces.py
  - provisa/pgwire/catalog_data.py
  - provisa/pgwire/catalog.py
  - provisa/pgwire/catalog_populate.py
  - provisa/pgwire/_pipeline.py
  tests:
  - tests/unit/test_pgwire_ext_surfaces.py
  notes: pgvector / json-ops / compat-fns live; postgis-subset partial (geometry/geography types + ST_*
    pass-through + && -> ST_Intersects; geography aliased to geometry, no SRID transforms). Opt-in via
    PROVISA_PGWIRE_EXT_SURFACES. ivfflat/hnsw index acceleration rejected loudly and never advertised.
  related_reqs:
  - REQ-883
  - REQ-842
- id: REQ-893
  status: complete
  since: 2026-07
  group: 4. Source Connectors
  category: Federation Engine Abstraction
  priority: MAY
  type: structural
  description: 'PostgreSQL can act as a pluggable FederationEngine (DriverClass.PARTIAL) via postgres_fdw,
    enabling Postgres servers to attach foreign tables from other Postgres servers (ATTACH mechanism —
    no data movement, references in place). FDW types (postgres_fdw, mysql_fdw, file_fdw, mongo_fdw, tds_fdw,
    etc.) are provisioned at install time via CREATE EXTENSION; per-source connections are created at
    runtime via CREATE SERVER + CREATE USER MAPPING + IMPORT FOREIGN SCHEMA / CREATE FOREIGN TABLE. This
    mirrors Trino''s architecture: connector types bundled at provision, catalogs dynamically created
    at runtime (REQ-251).'
  use_case: 'Enables Tier 1 deployment: a single PostgreSQL instance serves as both the metadata home
    and the compute engine for PG-to-PG and cross-FDW federation, eliminating the need for Trino until
    heterogeneous/broad federation is required. Maintains containerless deployment spirit and completes
    the multi-tier deployment story. Queries pass through the semantic→IR layer (RLS/masking applied before
    transpile), ensuring Postgres engine passes governance-parity conformance suite (REQ-827) by construction.'
  notes: 'Scale constraint (single-node, no MPP): like DuckDB, a Postgres+postgres_fdw engine executes
    on ONE node — there is no massively-parallel execution. postgres_fdw pushes down predicates and same-server
    joins/aggregates, but a cross-SERVER join (data from two different remotes) cannot be pushed down
    and materializes locally, bounded by the single node''s memory/CPU. This makes DuckDB and Postgres
    the SINGLE-NODE federators (embedded / small-to-medium tiers, low latency, no cluster); Trino is the
    only MPP federator (distributes federated joins/aggregations across a worker cluster). The tier boundary
    is therefore about SCALE, not just breadth: run DuckDB/Postgres compute until federation volume or
    concurrency outgrows one node, then graduate to Trino for MPP. The metadata home never moves (REQ-889);
    only the compute engine scales up. Implication for DriverClass: classification should reflect single-node
    vs MPP in addition to connector breadth.

    Value positioning (deprioritized to MAY): a Postgres engine adds NO federation-scale advantage over
    DuckDB — both are single-node, and horizontal scale is already achieved by running multiple stateless
    Provisa instances behind a load balancer with DuckDB. Postgres''s value is purely OPERATIONAL, not
    federation: (1) inspectable — a real listening server (psql, pg_stat_*, EXPLAIN, standard monitoring/backup)
    vs DuckDB''s opaque in-process file; (2) hardened/mature — MVCC, WAL, replication/HA/PITR, auth/roles;
    (3) shared concurrent state — N stateless Provisa app instances can share ONE Postgres as both metadata
    home and single-node engine, whereas DuckDB is embedded/ single-writer so each instance needs its
    own file (no shared home across a fleet). So DuckDB is the PRIMARY embedded single-node federator
    (Tier 0, per-instance); Postgres is the OPTIONAL shared/hardened/inspectable state+compute tier for
    fleets that want one concurrent backend. Build only if that ops profile is needed; not required for
    federation capability.

    Additional Postgres advantages for the SHARED-backend role: (a) connection pooling — PG is a multi-client
    server built for pooled clients (pgbouncer, server-side max_connections, driver pools), whereas DuckDB
    has no server-side pool (Provisa multiplexes threads/cursors to one file); (b) stability/maturity
    matters more for a shared backend than a per-instance file; (c) STANDARDIZED federation surface —
    postgres_fdw implements SQL/MED (ISO SQL''s standard for external data: CREATE FOREIGN TABLE, IMPORT
    FOREIGN SCHEMA), while DuckDB''s ATTACH + read_* scanners are DuckDB-specific. DuckDB concurrency
    model (for contrast): one Provisa process owns one embedded DuckDB and IS the multi-user multiplexer
    — concurrent readers are cheap (MVCC snapshots), writers/ materialization serialize and are coordinated
    by Provisa''s execution layer; DuckDB is not a multi-client server.

    IMPLEMENTATION: PostgresFdwConnector and FileFdwConnector (Mechanism.ATTACH) implemented in provisa/federation/connector.py.
    build_postgres_engine() in provisa/federation/engine.py creates a DriverClass.PARTIAL single-node
    engine. E2E tests pass: postgres_fdw attaches remote PG source with RLS masking (non-NY customers
    hidden), file_fdw attaches CSV; both joined to local engine table and executed via compile→apply_governance→rewrite_semantic_to_physical→transpile→execute
    on Postgres. Companion DuckDB engine also passes e2e (test_duckdb_federation_engine_e2e.py): RLS filter
    on customers.csv (15→5 rows) and CSV+SQLite relationship join on DuckDB.

    CORE FDW SET (bundled in standard PG, verified working): postgres_fdw (remote PG), file_fdw (CSV/text).
    EXTERNAL FDW CANDIDATES (not in standard image): sqlite_fdw (SQLite files), parquet_fdw (Parquet),
    iceberg_fdw (Apache Iceberg lakehouse), mysql_fdw, mongo_fdw, tds_fdw.

    ADDING EXTERNAL FDWs TO EMBEDDED PG: FDW provisioning is a BUILD/BUNDLING step, not runtime CREATE
    EXTENSION. Each external FDW is a shared library (.so) compiled against the embedded PG''s exact version;
    SQL files placed in that PG''s lib/ and share/extension/ dirs. Only then does CREATE EXTENSION succeed.
    Curated FDWs must be compiled into the embedded-PG distribution (pgserver bundles prebuilt PG binaries).
    Adding FDWs means shipping a customized embedded-PG build — directly parallel to how Trino bundles
    connector plugins in its image. This is the design guarantee: FDW set is fixed at distribution build
    time, not provisioned at install time.'
  code:
  - provisa/federation/connector.py
  - provisa/federation/engine.py
  tests:
  - tests/unit/test_postgres_federation_engine.py
  - tests/integration/test_postgres_federation_engine_e2e.py
  - tests/integration/test_duckdb_federation_engine_e2e.py
  e2e: true
  e2e_reason: 'RLS masking on joined federation tables is only verifiable through a live-stack round-trip:
    semantic governance layer must apply before transpile, then Postgres executor confirms rows are hidden/shown
    correctly based on user context.'
  related_reqs:
  - REQ-825
  - REQ-840
  - REQ-841
  - REQ-888
  - REQ-889
  - REQ-827
  - REQ-251
  - REQ-894
  - REQ-897
- id: REQ-894
  status: complete
  since: 2026-07
  code:
  - provisa/federation/engine.py
  tests:
  - tests/unit/test_arch_invariants.py
  target: 2026-Q4
  group: 11. Platform, Infrastructure & Delivery
  category: Deployment Tier Architecture
  priority: SHOULD
  type: constraint
  description: DuckDB federation engine is a DESKTOP/EDGE/DEV tier embedded federator, not a production
    multi-user backend. DuckDB is single-process with no server-side connection pooling and no HA; one
    Provisa process owns one DuckDB file and serves as the multi-user multiplexer (concurrent readers
    cheap via MVCC snapshots; writers/materialization serialize under Provisa coordination). The "multiple
    Provisas behind a load balancer with DuckDB" pattern works only for fully independent/partitioned
    workloads because DuckDB provides no shared state and is single-writer.
  use_case: DuckDB is recommended for zero-install single-node desktop/edge/dev deployments with one Provisa
    instance per DuckDB file. Production multi-user federation at scale requires either Trino (MPP, distributes
    across a worker cluster) or PostgreSQL with postgres_fdw (single-node shared backend, operational
    hardening). The tier boundary is SCALE (single-node vs MPP), not just federation breadth.
  notes: 'Design invariant: DuckDB and Postgres are the SINGLE-NODE federators (embedded/small-to-medium
    tiers, low latency, no cluster); Trino is the only MPP federator. The metadata home never moves (REQ-889);
    only the compute engine scales up. DuckDB''s concurrency model: one Provisa process owns one embedded
    DuckDB file and IS the multi-user multiplexer — concurrent readers are cheap (MVCC snapshots), writers/materialization
    serialize and are coordinated by Provisa''s execution layer. DuckDB is not a multi-client server and
    cannot be a shared production backend.'
  related_reqs:
  - REQ-825
  - REQ-840
  - REQ-841
  - REQ-888
  - REQ-889
  - REQ-893
- id: REQ-895
  status: complete
  unit_test: false
  unit_test_reason: Cross-vendor SQL/MED design pattern (MAY); no code artifact or mockable interface
    to assert.
  since: 2026-07
  target: 2026-Q4
  group: 4. Source Connectors
  category: Federation Engine Abstraction
  priority: MAY
  type: structural
  description: 'RDBMS-as-single-node-federation-engine pattern generalizes across DB vendors via native
    foreign-data mechanisms (SQL/MED or equivalent). Faithful SQL/MED: PostgreSQL (most complete, postgres_fdw
    and ecosystem FDWs) and IBM Db2 (federation via CREATE NICKNAME). Equivalent proprietary mechanisms:
    Oracle (database links + Heterogeneous Gateways), SQL Server (linked servers + PolyBase external tables),
    MySQL/MariaDB (FEDERATED / MariaDB CONNECT engine). All are single-node ATTACH-mechanism connectors
    and belong to the same federation-engine tier as PG/DuckDB; Trino remains the only MPP/broad federator.'
  use_case: 'Pluggable "warehouse/RDBMS-native" engine family: a customer''s existing warehouse (Db2/Oracle/
    SQL Server/PG) could itself be the federation engine via its native foreign-data mechanism, requiring
    no new infrastructure. These engines are OPTIONAL/MAY and operations-driven, not scale-driven. Unifies
    the single-node federator family under the SQL/MED abstraction and enables ecosystem partnerships
    (e.g., Db2, Oracle, SQL Server connectors as plugins).'
  notes: 'All RDBMS foreign-data engines share the single-node scale constraint: each executes on ONE
    node and cannot push distributed joins/aggregations across two different remote servers. Cross-SERVER
    joins materialize locally. This makes them equivalent tiers: DuckDB and Postgres (via postgres_fdw)
    are embedded/shared examples; Db2 federation, Oracle database links, SQL Server linked servers, and
    MariaDB CONNECT are the vendor-native equivalents. All are SINGLE-NODE (REQ-894); only Trino is MPP.
    The tier boundary is SCALE, not breadth. Implementation: each RDBMS engine would be a pluggable DriverClass.PARTIAL
    connector wired via EngineRuntime (REQ-888).'
  related_reqs:
  - REQ-825
  - REQ-840
  - REQ-841
  - REQ-888
  - REQ-889
  - REQ-893
- id: REQ-896
  status: complete
  since: 2026-07
  code:
  - provisa/compiler/stage2.py
  - provisa/pgwire/_pipeline.py
  tests:
  - tests/unit/test_arch_invariants.py
  target: 2026-Q4
  group: 0. Architecture & Design Principles
  category: Federation Architecture
  priority: MUST
  type: structural
  description: Provisa's defensible value is the GOVERNED-FEDERATION MODEL and consistent governance philosophy,
    not any compute engine or infrastructure. Federation engines (Trino, DuckDB, Postgres, Snowflake,
    warehouse-native) are interchangeable commodity COMPUTE substrates; governance (RLS, column masking,
    relationship-join enforcement) is applied in the semantic→IR layer BEFORE any engine sees the query,
    ensuring governance-parity across all engines regardless of architecture.
  use_case: Engine independence allows Provisa to swap compute substrates (Trino ↔ DuckDB ↔ Postgres)
    with the entire governed federation model unchanged. The connector, CatalogEntry, materialization-store
    abstractions and single-node→MPP tier graduation remain engine-agnostic; governance invariants hold
    across deployment tiers and engine boundaries.
  notes: 'Design consequence: keep governance and the semantic model strictly above the engine seam. Never
    let engine-specific behavior leak into governance logic. Engines must remain hot-swappable. This principle
    is validated by the fact that Provisa can demonstrate identical governed-federation behavior across
    fundamentally different compute architectures (Trino/MPP cluster vs DuckDB/embedded in-process vs
    Postgres/single-node shared backend).'
  related_reqs:
  - REQ-827
  - REQ-825
  - REQ-840
  - REQ-841
  - REQ-888
  - REQ-889
  - REQ-893
  - REQ-894
  - REQ-895
- id: REQ-897
  status: complete
  since: 2026-07
  code:
  - provisa/federation/engine.py
  - provisa/federation/connector_base.py
  - provisa/federation/plan.py
  - provisa/transpiler/router.py
  tests:
  - tests/unit/test_engine_capability_traits.py
  target: 2026-Q4
  group: 0. Architecture & Design Principles
  category: Federation Architecture
  priority: SHOULD
  type: structural
  description: 'Federation engines are declared-capability objects; capabilities are planner inputs. Each
    FederationEngine carries a set of DECLARED capability traits used to make routing/planning/tier decisions.
    Established traits: driver_class (reach: BROAD/PARTIAL/SELF_ONLY) and mpp (scale-out; orthogonal to
    reach). Candidate additional traits: file_native, pooled, transactional, streaming (Arrow transport
    modes), and connector-level pushdown (predicate/join/aggregate).'
  use_case: 'Consolidating engine capabilities into one coherent descriptor enables planner decisions
    such as: push a large federated join to an MPP engine; land API/remote sources into a pooled/transactional
    store; prefer a file_native engine when sources are files; gate single-node→MPP tier graduation. Capabilities
    become planner inputs rather than being inferred from incidental plumbing (e.g., connector count or
    driver class alone).'
  notes: 'Current seeds are scattered across EngineCapability (transports, provisa/federation/runtime.py),
    connector Capability (pushdown), and engine driver_class/mpp fields. Direction: consolidate into one
    coherent engine-capability descriptor that orthogonally captures: (a) reach dimension — BROAD (native
    federation), PARTIAL (via connectors), SELF_ONLY (single-node); (b) scale dimension — mpp/distributed
    vs single-node; (c) storage dimension — file_native (scans files directly), pooled (server-side connection
    pooling), transactional support; (d) compute dimension — pushdown capabilities (predicate/join/aggregate
    at connector level). This consolidation is informational today; it enables decision-driving planner
    logic and capability-aware cost models in future versions.'
  related_reqs:
  - REQ-825
  - REQ-840
  - REQ-841
  - REQ-888
  - REQ-889
  - REQ-893
  - REQ-894
  - REQ-895
- id: REQ-898
  status: complete
  since: 2026-07
  code:
  - provisa/pg_extensions/catalog.py
  - config/pg_extension_catalog.yaml
  tests:
  - tests/unit/test_pg_extension_catalog.py
  target: 2026-Q4
  group: 4. Source Connectors
  category: PostgreSQL Deployment
  priority: SHOULD
  type: infrastructure
  description: Curated, prebuilt catalog of PostgreSQL extensions (sqlite_fdw, parquet_fdw/parquet_s3_fdw,  pg_lake,
    pg_duckdb, pg_analytics) indexed by pinned PostgreSQL major version and target OS/platform  (manylinux,
    macOS, Windows), layered onto the pip-installed pgserver runtime to avoid runtime  build dependencies.
  use_case: Prebuilt extension artifacts eliminate runtime compilation and external build toolchains,  preserving
    pgserver's "no root, pip install" deployment model. Enables sqlite_fdw and Iceberg-oriented  FDWs
    (pg_lake, pg_duckdb, pg_analytics) to work out-of-box with Provisa's Postgres federation engine  without
    requiring libsqlite3, libarrow/libparquet, or DuckDB compilation by the end-user.
  related_reqs:
  - REQ-893
  - REQ-894
  - REQ-895
- id: REQ-899
  status: complete
  tests:
  - tests/unit/test_duckdb_community_connectors.py
  since: 2026-07
  target: 2026-Q4
  group: 4. Source Connectors
  category: Federation Engine Abstraction
  priority: MAY
  type: structural
  description: Catalog of DuckDB extensions eligible to back a federation-source connector, each providing
    either a relation-producing table function usable as CREATE VIEW ... SELECT * FROM fn(...), or an
    ATTACH catalog of tables. Only these two mechanisms qualify; scalar/index-only extensions are excluded.
  use_case: Enables connector authors to identify which DuckDB extensions can materially contribute federated
    sources via scanner table-function views or ATTACH catalogs, excluding pure utility/scalar/index extensions
    that cannot produce relations.
  notes: 'CORE extensions (stable, duckdb.org/docs/stable/core_extensions):

    - postgres — ATTACH + TF (postgres_scan, postgres_query)

    - mysql — ATTACH + TF (mysql_scan, mysql_query)

    - sqlite — ATTACH + TF (sqlite_scan) [IMPLEMENTED: DuckDBSqliteConnector]

    - ducklake — ATTACH

    - motherduck — ATTACH

    - unity_catalog — ATTACH

    - iceberg — ATTACH + TF (iceberg_scan, iceberg_metadata)

    - odbc — ATTACH + TF

    - delta — TF (delta_scan)

    - lance — TF (lance_scan)

    - vortex — TF (vortex file scan)

    - avro — TF (read_avro)

    - parquet — TF (read_parquet) [IMPLEMENTED: DuckDBParquetConnector]

    - json — TF (read_json / read_json_auto)

    - excel — TF (read_xlsx)

    - spatial — TF (ST_Read, GDAL/OGR)

    - httpfs — TF (remote file scans; composes with parquet/json/csv)

    - azure — TF (Azure blob scans)

    - csv (built-in read_csv_auto) [IMPLEMENTED: DuckDBCsvConnector]


    COMMUNITY extensions (duckdb.org/community_extensions, v1.5.x):

    - bigquery — ATTACH + TF (bigquery_scan)

    - firebird — ATTACH + TF

    - hive_metastore — ATTACH

    - delta_classic — ATTACH

    - altertable — ATTACH + TF

    - airport — ATTACH + TF (Arrow Flight)

    - duckdb_delta_sharing / duck_delta_share — TF (Delta Sharing scan)

    - adbc / adbc_scanner — TF (ADBC source scan)

    - gsheets — TF (Google Sheets scan)

    - gcs — TF (GCS file scans)

    - fire_duck_ext — TF (Firestore query)

    - nanoarrow — TF (Arrow IPC scan)

    - h5db / anndata / duckdb_zarr / dbn / dta / dicom / fit / duckhts — TF (file-format scanners)

    - crawler / http_client / dns — TF (web/network scans)

    - fsquery / hostfs / gh — TF (filesystem-as-table scans)

    - eurostat / gaggle / blockduck / brew / aixchess — TF (domain data-source scans)

    - duckpgq — TF (graph query scans)


    Excluded (do not qualify): ai, llm, flock, crypto, inet, icu, encodings, fts, vss, aws, quack, autocomplete
    (scalar/index-only, no relation producers).

    Excluded (already covered by a superior native path): erpl_web (OData/GraphQL/REST) and salesforce

    (REST/Bulk API) — the API-ecosystem surfaces they target are already reached via Provisa''s

    non-reachable-source path (openapi/graphql_remote LANDed into the materialization store), which carries

    our caching and governance; a raw scanner would bypass both and is redundant.

    '
  code:
  - provisa/federation/connector.py
  - provisa/federation/engine.py
  related_reqs:
  - REQ-893
  - REQ-894
  - REQ-895
  - REQ-888
  - REQ-840
- id: REQ-900
  status: complete
  since: 2026-07
  target: 2026-Q4
  group: 4. Source Connectors
  category: PostgreSQL Deployment
  priority: MUST
  type: infrastructure
  description: 'Postgres federation engine''s curated v1 connector set: prebuilt FDW/extension bindings
    for csv, sqlite, sqlserver, oracle, duckdb, and parquet/iceberg/delta_lake (via pg_duckdb), indexed
    by (pg_major, platform, libc) triple and layered onto the embedded pgserver runtime. Catalog resolution
    fails closed: no artifact for a triple means the source is unreachable on that deployment (no silent
    fallback).'
  use_case: A deployed Postgres federation engine connects to CSV (file_fdw), SQLite (sqlite_fdw), SQL
    Server (tds_fdw), Oracle (oracle_fdw), and lakehouse sources (parquet/iceberg/delta via pg_duckdb)
    without runtime build or external toolchain dependencies. The fixed v1 set avoids sprawl; future connectors
    (mysql_fdw, mongo_fdw) or write-heavy operations route via separate engines or deferred tiers.
  notes: 'V1 connector set: csv→file_fdw (core, read-only), sqlite→pgspider/sqlite_fdw (read/write; libsqlite3),
    sqlserver→tds_fdw (read-only; freetds), oracle→laurenz/oracle_fdw (read/write; Instant Client supplied
    by deployment, not redistributable), parquet/iceberg/delta→pg_duckdb (read; object-store S3/GCS/Azure),
    duckdb→pg_duckdb ATTACH of a local .duckdb file (READ-WRITE; local files default to read-write, add
    READ_ONLY to restrict). pg_duckdb does NOT require MotherDuck — MotherDuck is an optional cloud add-on
    only (off unless duckdb.enable_motherduck is called); the v1 connector set is fully local/embedded.
    pg_duckdb constraints: one writable ATTACH per transaction; ATTACH definitions do not survive session,
    so the engine re-ATTACHes on each connection startup. Parquet write and MinIO endpoint control deferred
    — pg_duckdb covers read; revisit parquet_s3_fdw only if write or fine S3 control needed. Each extension
    built per (pg_major, platform, libc) triple as a separate pip-installable wheel; absence of an artifact
    for a deployment''s triple makes the source unreachable — no degradation, no fallback to DuckDB engine.'
  code:
  - provisa/federation/connector.py
  - provisa/federation/engine.py
  - docs/arch/fdw_catalog.md
  tests:
  - tests/integration/test_postgres_federation_engine_e2e.py
  related_reqs:
  - REQ-893
  - REQ-894
  - REQ-895
  - REQ-898
  - REQ-899
- id: REQ-901
  status: complete
  since: 2026-07
  group: 4. Source Connectors
  category: PostgreSQL Deployment
  priority: MUST
  type: infrastructure
  description: pg_duckdb v1.0.0 (bundles DuckDB v1.3.2) is the compatible build for embedded PostgreSQL
    16.2; later pg_duckdb versions require backend symbols absent from PG16.2. Extension built from pg
    source against matching major version and layered into pgserver install via build script.
  use_case: Pinned PostgreSQL 16.2 in pgserver 0.1.4 runtime requires pg_duckdb v1.0.0 to avoid symbol
    resolution failures at extension load. Build process (scripts/build_pg_duckdb.sh) compiles against
    PG16 source, and the resulting .dylib/.so is dropped alongside other core FDWs (file_fdw, postgres_fdw).
  notes: 'Build constraint: pg_duckdb v1.0.0 only; later HEAD versions introduce ExecCheckOneRelPerms
    backend symbol not present in PG16.2. pg_duckdb requires shared_preload_libraries=''pg_duckdb'' in
    PostgreSQL config and rejects NUMERIC columns with unset precision.'
  code:
  - scripts/build_pg_duckdb.sh
  - provisa/federation/engine.py
  tests:
  - tests/integration/test_embedded_pg_duckdb_engine_e2e.py
  related_reqs:
  - REQ-900
  - REQ-898
- id: REQ-902
  status: complete
  since: 2026-07
  group: 5. Query Languages, Compilation & Operations
  category: Query Compilation
  priority: MUST
  type: constraint
  description: pg_duckdb's transparent execution path cannot emit PostgreSQL 16 nested-JSON syntax (JSON_OBJECT
    with colon separators); nested-relationship GraphQL federation through pg_duckdb requires a pg_duckdb-specific
    JSON emission in the transpiler to collapse JSON_OBJECT into flat json_build_object.
  use_case: Governed federation of nested GraphQL pipelines (with JOIN-enforced relationships) through
    pg_duckdb. Today pg_duckdb engine handles flat federated joins and governance filtering; nested-relationship
    queries route to other engines until JSON output is adapted.
  scenario: 'Given a federated query with nested relationships (e.g., author.posts)

    When the planner selects pg_duckdb as the federation engine

    Then the transpiler emits DuckDB-compatible json_build_object syntax instead of PG16 JSON_OBJECT

    And the nested objects are correctly formed in the output.

    '
  notes: 'Limitation is DuckDB-specific: PG emits JSON_OBJECT(k : v RETURNING json); DuckDB rejects colon
    syntax and requires json_build_object(k, v). pg_duckdb maps PG syntax transparently for most operations
    but not for nested JSON construction in the output layer. Workaround: route nested queries to non-pg_duckdb
    engines or defer nested-relationship federation to future version.'
  code:
  - provisa/transpiler/transpile.py
  - provisa/federation/pg_backend.py
  tests:
  - tests/unit/test_transpile.py
  related_reqs:
  - REQ-900
  - REQ-896
- id: REQ-903
  status: complete
  superseded_by: REQ-904
  since: 2026-07
  group: 4. Source Connectors
  category: PostgreSQL Deployment
  priority: MUST
  type: behavioral
  description: Postgres federation engines validate connector availability and fail explicitly when sources
    require unavailable connectors. Sources whose connector is unavailable resolve to UnreachableSource
    (explicit error, never silent fallback). Superseded by REQ-904 (probe-based discovery with runtime
    functional validation).
  use_case: Operators need clear diagnostics when sources are unreachable due to missing connectors, rather
    than silent downgrade to alternative engines.
  scenario: 'Given a source requiring an unavailable connector

    When query planning occurs

    Then the source resolves to UnreachableSource with explicit error.

    '
  code:
  - provisa/federation/engine.py
  tests:
  - tests/unit/test_isolated_engine_routing.py
  related_reqs:
  - REQ-900
  - REQ-901
  - REQ-904
- id: REQ-904
  status: complete
  since: 2026-07
  group: 4. Source Connectors
  category: PostgreSQL Deployment
  priority: MUST
  type: behavioral
  description: 'Federation connectors are self-describing prebuilt definitions; each has an async probe()
    reporting FUNCTIONAL truth (FDW/extension actually loads/works), not mere presence. build_pg_engine()
    registers all prebuilt Postgres connectors as candidates in precedence order (postgres_fdw; pg_duckdb_csv
    preferred over file_fdw; pg_duckdb_parquet; pg_duckdb_json). At startup, FederationEngine.discover(fetch,
    disabled=frozenset(keys)) probes each candidate and sets the active set: a connector struck by the
    override (key in disabled) is not probed; every other candidate is probed and only kept if available;
    per source_type the first available candidate wins (richer preferred, stock fallback). A source with
    no available connector is UnreachableSource (explicit, no silent fallback). The override config is
    now only a strike-list, not a presence declaration. Runtime probe correctly reports pg_duckdb unavailable
    until preloaded (shared_preload_libraries check), where a static flag would get it wrong.'
  use_case: Runtime discovery ensures only functionally available connectors are registered, avoiding
    silent failures from misconfigured deployments. The strike-list override provides fallback control
    without requiring static presence declarations.
  scenario: 'Given a FederationEngine with pg_duckdb not in shared_preload_libraries

    When discover() is called

    Then pg_duckdb''s probe reports unavailable

    And CSV sources fall back to file_fdw

    And post-preload, discover() re-probes and activates pg_duckdb.

    '
  code:
  - provisa/federation/connector.py
  - provisa/federation/engine.py
  tests:
  - tests/unit/test_federation_engine.py
  - tests/integration/test_embedded_pg_duckdb_engine_e2e.py
  related_reqs:
  - REQ-903
- id: REQ-905
  status: complete
  since: 2026-07
  group: 6. Execution, Routing, Caching & Performance
  category: Query Execution
  priority: SHOULD
  type: behavioral
  description: Cost-based promotion of VIRTUAL/SCAN sources to MATERIALIZED when the source connector
    cannot push down row-reducing operators (filter/aggregate) the query requires, and the estimated scan
    size exceeds the threshold (default 1,000,000 rows). Promoted sources delegate reduction to the engine
    store, amortized across the freshness TTL. UNKNOWN cardinality never promotes (fail-open).
  use_case: When source connectors lack required row-reducing pushdown capability but scans are large,
    promoting to MATERIALIZED allows the engine store (with full pushdown) to handle reduction efficiency,
    amortizing the cost across the source's freshness window. Join-only gaps do not promote (non row-reducing).
  scenario: 'Given a federated query with row-reducing operators (filter/aggregate) over a VIRTUAL/SCAN
    source

    When the source connector cannot push down these operators

    And the estimated row count >= 1,000,000

    Then the source is promoted to MATERIALIZED

    And the engine store handles the reduction, amortized across the TTL

    And UNKNOWN cardinality does not trigger promotion.

    '
  code:
  - provisa/federation/promote.py
  - provisa/federation/strategy.py
  - provisa/federation/plan.py
  related_reqs:
  - REQ-826
  tests:
  - tests/unit/test_promote.py
- id: REQ-906
  status: complete
  since: 2026-07
  group: 4. Source Connectors
  category: Source Connectors
  priority: SHOULD
  type: structural
  description: 'Seven DuckDB community-extension federation connectors implemented: MSSQL, MongoDB, Snowflake,
    BigQuery, Firebird, Google Sheets (gsheets), and Airport. Each connector references an external source
    via ATTACH mechanism and includes a load-only startup probe that verifies the extension loads and
    registers its scanner/attach symbol without opening a live connection to a source.'
  use_case: Extends federation to support additional cloud and enterprise source types (MSSQL, MongoDB,
    Snowflake, BigQuery) plus specialty connectors (Firebird, Google Sheets, Arrow Flight via Airport),
    each verified at startup via extension probe to ensure engine capability is installed and reachable
    offline.
  code:
  - provisa/federation/connector.py
  - provisa/federation/engine.py
  - provisa/federation/duckdb_extensions.py
  - provisa/core/models.py
  - scripts/stage_duckdb_extensions.py
  scenario: 'Given a DuckDB federation engine with community extensions staged into the extension directory

    When the federation runtime starts

    Then each of the seven connectors (MSSQL, MongoDB, Snowflake, BigQuery, Firebird, Gsheets, Airport)

    is probed via LOAD to verify the extension loads and registers its scanner/attach symbol

    And sources using these connectors can be registered for federation

    And no live connection is attempted during probe.

    '
  related_reqs:
  - REQ-899
  - REQ-904
  tests:
  - tests/unit/test_duckdb_community_connectors.py
- id: REQ-907
  status: complete
  since: 2026-07
  group: 4. Source Connectors
  category: Source Connectors
  priority: MUST
  type: behavioral
  description: SqliteFdwConnector and MysqlFdwConnector enable the embedded/BYO Postgres federation engine
    to attach SQLite files and remote MySQL servers via sqlite_fdw and mysql_fdw, respectively. Each connector
    declares runtime_deps (tuple of shared libs tagged system/bundled/operator) documenting the packaging
    surface; probes verify functional availability at startup.
  use_case: Federation of SQLite file-based data stores (in-place via sqlite_fdw; libsqlite3 system-provided)
    and remote MySQL databases (via mysql_fdw; libmysqlclient/mariadb-connector-c bundled) extends the
    Postgres v1 connector set beyond CSV/Parquet/Iceberg, enabling embedded deployments to query across
    multiple relational sources without data movement.
  scenario: 'Given a source configured with sqlite_fdw (source_type "sqlite", key "sqlite_fdw") or mysql_fdw

    (source_type "mysql", key "mysql_fdw")

    When the federation engine initializes

    Then the connector probes functional availability (sqlite_fdw via loaded FDW, mysql_fdw via probe

    and library bundling)

    And if available, the connector attaches the SQLite file (CREATE SERVER OPTIONS(database path) +

    IMPORT FOREIGN SCHEMA) or remote MySQL (CREATE SERVER + CREATE FOREIGN TABLE IMPORT FOREIGN SCHEMA)

    And queries route through the attached foreign schema to the source.

    '
  notes: 'SqliteFdwConnector uses pgspider/sqlite_fdw (read/write); runtime dep = system libsqlite3 (OS-provided).
    Proven end-to-end: reads demo orders.sqlite through the real compiler pipeline. MysqlFdwConnector
    uses mysql_fdw; runtime dep = libmysqlclient/mariadb-connector-c (must be bundled). Connector wired
    and probe-gated; load-on-macOS + live-MySQL federation not yet proven — probe gates discover() reporting
    unavailable until functional. Each Connector declares runtime_deps in the prebuilt definition.'
  code:
  - provisa/federation/connector.py
  - provisa/federation/engine.py
  tests:
  - tests/integration/test_embedded_pg_sqlite_fdw_e2e.py
  - tests/unit/test_fdw_connectors.py
  related_reqs:
  - REQ-900
  - REQ-901
  - REQ-904
- id: REQ-908
  status: complete
  since: 2026-07
  group: 4. Source Connectors
  category: Source Connectors
  priority: MUST
  type: behavioral
  description: PgDuckdbIcebergConnector reads Apache Iceberg tables in-place via pg_duckdb's iceberg_scan
    (DuckDB iceberg extension) inside the stock embedded Postgres. Extension is STATIC-linked into libduckdb
    (aws-sdk-cpp, avro-c, roaring) with no extra runtime dylib; build requires USE_MERGED_VCPKG_MANIFEST=1
    and iceberg pinned to branch v1.3-ossivalis.
  use_case: Federation of Apache Iceberg lakehouse tables stored on S3/GCS/Azure without data movement.
    Iceberg's time-travel and ACID guarantees are preserved through the query; governance predicates filter
    rows at the scan level before federation.
  scenario: 'Given a source configured with pg_duckdb_iceberg (source_type "iceberg", key "pg_duckdb_iceberg")

    When the federation engine initializes

    Then the connector probes that iceberg_scan is registered (pg_duckdb without iceberg lacks the function,

    so probes correctly disable it)

    And if available, queries emit iceberg_scan(''<root>'', allow_moved_paths := true)

    And governance predicates are pushed down into the scan

    And results are correctly federated with other sources.

    '
  notes: 'pg_duckdb is built (via vcpkg) to include the iceberg extension; aws-sdk-cpp[sso,sts,identity-management]
    + avro-c + roaring are STATIC-linked into libduckdb (no extra runtime dylib; libduckdb links only
    macOS system frameworks). Build requires USE_MERGED_VCPKG_MANIFEST=1 (DuckDB generates merged extension
    vcpkg manifest and runs vcpkg install) + unshallowed vcpkg for pinned baseline + CMAKE_POLICY_VERSION_MINIMUM=3.5,
    iceberg pinned to branch v1.3-ossivalis. Proven end-to-end: iceberg_scan reads a real pyiceberg-generated
    Iceberg table on embedded PG 16.2 with governance predicate. Fixed globally: patched conda''s incomplete
    aws-sdk-cpp (missing sso) cmake config hard-erroring unrelated CMake builds; /opt/miniconda3/lib/cmake/AWSSDK/AWSSDKConfig.cmake
    now graceful on missing components.'
  code:
  - provisa/federation/connector.py
  - provisa/federation/engine.py
  - scripts/build_pg_duckdb.sh
  tests:
  - tests/integration/test_embedded_pg_duckdb_iceberg_e2e.py
  - tests/unit/test_pg_duckdb_iceberg_connector.py
  related_reqs:
  - REQ-900
  - REQ-901
  - REQ-904
- id: REQ-909
  status: complete
  since: 2026-07
  group: 4. Source Connectors
  category: Direct-Route Dialect Expansion
  priority: SHOULD
  type: structural
  description: Generic SQLAlchemy fallback driver provides direct-route write capability for any source
    type with a SQLAlchemy dialect, without requiring a bespoke async driver. Writable sources are the
    intersection of {has_driver} AND {sqlglot_write_dialect}, where has_driver now includes the SQLAlchemy
    fallback when no native driver exists.
  use_case: Broadens the writable RDBMS set on the direct route without per-source driver implementations.
    Any SQLAlchemy dialect (whose DBAPI is installed) + sqlglot write dialect support enables writes to
    that source type without a native async driver.
  code:
  - provisa/executor/writable.py
  - provisa/executor/drivers/sqlalchemy_driver.py
  - provisa/executor/drivers/registry.py
  - provisa/federation/connector.py
  - provisa/core/models.py
  - provisa/api/app.py
  related_reqs:
  - REQ-229
  - REQ-550
  tests:
  - tests/unit/test_writable.py
  - tests/unit/test_drivers.py
- id: REQ-910
  status: complete
  since: 2026-07
  group: 4. Source Connectors
  category: Multi-Route Write Dispatch
  priority: MUST
  type: behavioral
  description: 'Preference-ordered write-path resolution routes mutations across three tiers: NATIVE (bespoke
    async driver, direct), SQLALCHEMY (generic fallback driver, direct), and ENGINE (federation engine
    writes upstream via a write-capable connector). NATIVE and SQLALCHEMY require both a direct driver
    and SQLGlot write dialect; ENGINE requires only the connector''s Capability.write. ENGINE is the only
    route that reaches sources with no direct driver and no SQLGlot dialect (e.g., NoSQL sources exposed
    as writable relations by their connector).'
  use_case: Enables mutation routing to any writable source regardless of per-source driver availability
    or SQLGlot dialect support. Maximizes write coverage by cascading through direct routes (native first,
    then SQLAlchemy) before falling back to engine-routed mutations through connectors.
  scenario: 'Given a federation engine with connectors for multiple source types (postgresql, sqlite,
    cassandra)

    When resolve_write_path is called for each source with the engine

    Then postgresql returns NATIVE (native asyncpg driver + dialect available)

    And sqlite returns SQLALCHEMY (no native driver, SQLAlchemy fallback + dialect available)

    And cassandra returns ENGINE (no direct driver/dialect, only connector write=True)

    And if engine is None, only NATIVE and SQLALCHEMY remain as possible routes

    '
  code:
  - provisa/executor/writable.py
  - provisa/executor/drivers/registry.py
  tests:
  - tests/unit/test_writable.py
  related_reqs:
  - REQ-909
  - REQ-842
  - REQ-229
- id: REQ-911
  status: complete
  since: 2026-07
  group: 4. Source Connectors
  category: ClickHouse Federation Engine
  priority: SHOULD
  type: behavioral
  description: ClickHouse is a selectable federation engine (PROVISA_ENGINE=clickhouse) alongside trino/pg/duckdb/sqlalchemy.
    It is a single-node-reach PARTIAL federator, MPP, native_store="clickhouse" that ATTACHes external
    sources via ClickHouse's native integration engines (Mechanism.ATTACH) rather than an FDW.
  use_case: Federate PostgreSQL, MySQL, CSV, Parquet, and MongoDB sources as ClickHouse tables without
    data movement. PostgreSQL/MySQL sources mount as DATABASE engines exposing all remote tables; CSV/Parquet
    mount as TABLE engines (S3, URL, or File based on path scheme) with columns inferred by ClickHouse;
    MongoDB mounts as MongoDB engine with columns supplied from registry metadata.
  scenario: 'Given PROVISA_ENGINE=clickhouse with registered sources (postgresql, mongodb, s3://bucket/file.parquet)

    When the federation engine initializes

    Then postgresql source mounts as CREATE DATABASE ... ENGINE=PostgreSQL

    And mongodb source mounts as CREATE TABLE ... ENGINE=MongoDB with supplied columns

    And s3 parquet source mounts as CREATE TABLE ... ENGINE=S3 with inferred columns

    And all sources support predicate pushdown where available (postgresql, mysql, parquet)

    And query execution transpiles to ClickHouse dialect and routes through ClickHouseFederationRuntime

    '
  code:
  - provisa/federation/engine.py
  - provisa/federation/connector.py
  - provisa/federation/clickhouse_backend.py
  - provisa/federation/clickhouse_runtime.py
  - provisa/transpiler/transpile.py
  tests:
  - tests/integration/test_clickhouse_runtime_e2e.py
  - tests/unit/test_clickhouse_connectors.py
  related_reqs:
  - REQ-900
  - REQ-901
  - REQ-904
- id: REQ-912
  status: complete
  since: 2026-07
  group: 4. Source Connectors
  category: ClickHouse Federation Engine
  priority: SHOULD
  type: behavioral
  description: 'The ClickHouse federation runtime supports three interchangeable execution backends selected
    by URL scheme at initialization: clickhouse:// (HTTP via clickhouse-connect, port 8123), clickhouse+native://
    (native TCP via clickhouse-driver, port 9000), or chdb:// (embedded in-process engine, optional filesystem
    persistence). All backends expose identical SQL and integration engine surfaces.'
  use_case: Operators can choose between a remote ClickHouse server (HTTP or native protocol), in-process
    embedded ClickHouse, or persistent embedded store depending on deployment topology and latency requirements.
  scenario: 'Given ClickHouseFederationRuntime.from_url with URL scheme

    When the URL scheme is clickhouse://

    Then connect via clickhouse-connect HTTP client to port 8123

    When the URL scheme is clickhouse+native://

    Then connect via clickhouse-driver native TCP client to port 9000

    When the URL scheme is chdb:// or chdb:///path

    Then initialize embedded chdb in-process, optionally persisting to path

    And all three backends support identical SQL execution and integration engines

    '
  code:
  - provisa/federation/runtime.py
  tests:
  - tests/unit/test_clickhouse_connectors.py
  - tests/integration/test_clickhouse_runtime_e2e.py
  related_reqs:
  - REQ-911
- id: REQ-913
  status: complete
  since: 2026-07
  group: 0. Architecture & Design Principles
  category: AST-Based Transforms
  priority: MUST
  type: structural
  description: Every query transform — governance (RLS, masking, visibility, row-cap), correlated- subquery
    rewriting, and dialect lowering — MUST operate on a parsed SQL/relational AST, never on SQL text via
    regex or string substitution. Each surface language (GraphQL, Cypher, SQL) parses to an AST that is
    carried through governance and planning and serialized to a dialect string only at final emission;
    no stage may re-derive query structure (SELECT scope, WHERE injection point, alias binding, LIMIT)
    from text.
  use_case: 'String/regex transforms silently mis-apply governance: a scalar subquery in the projection
    moves the masking boundary (PII leak), a non-t0 alias convention skips RLS qualification (scope-ambiguous
    predicate), and a parameterized LIMIT bypasses the row-cap ceiling. AST transforms make "governance
    applied" a structural property of the tree rather than a textual coincidence each frontend must keep
    satisfying.'
  code:
  - provisa/compiler/stage2.py
  - provisa/compiler/rls.py
  - provisa/compiler/mask_inject.py
  - provisa/transpiler/transpile.py
  - provisa/cypher/translator.py
  tests:
  - tests/unit/test_ast_transform_invariants.py
  - tests/unit/test_ast_transforms.py
  - tests/integration/test_ast_transform_e2e.py
  target: 2026-Q4
  competitive_position:
    status: parity
    rationale: Trino, Spark, and Calcite-based engines apply predicate/column governance as logical-plan
      rewrites, not text edits; AST-based transforms bring Provisa to that baseline and are prerequisite
      to first-class dynamic obligations.
  related_reqs:
  - REQ-040
  - REQ-041
  - REQ-087
  - REQ-263
  - REQ-264
- id: REQ-914
  status: complete
  since: 2026-07
  group: 1. Access Governance & Security
  category: Pipeline Stage Tracing
  priority: SHOULD
  type: behavioral
  description: 'Each query-pipeline boundary (govern.in, govern.rls, govern.mask, govern.out, and transpile.<dialect>)
    emits an OpenTelemetry span event carrying the canonical SQL, so the transform sequence is inspectable
    per request and the pre/post-governance pair serves as a compliance receipt. SQL capture is OFF by
    default; PROVISA_TRACE_SQL=redacted blanks every literal to a placeholder before attaching, and =full
    attaches raw SQL for dev only. Telemetry never fails the request: a redaction parse error is surfaced
    as a span attribute. PROVISA_TRACE_AST=1 attaches the SQLGlot AST repr, redacted the same way (literals
    blanked), so it is safe to enable independently of the SQL mode.'
  use_case: Debugging special-case query transforms previously meant reading regex and guessing where
    text was mangled; per-stage canonical SQL turns that into a diff. The redaction default prevents literal
    values (RLS predicate values, PII) from leaking into traces in a governance product.
  scenario: 'Given the query pipeline processes a request

    When PROVISA_TRACE_SQL is unset or off

    Then stage span events carry the stage name but no SQL text

    When PROVISA_TRACE_SQL is redacted

    Then each stage event carries SQL with every literal replaced by a placeholder

    And no literal value (e.g. an RLS predicate value or SSN) appears in any attribute

    When the SQL is unparseable in redacted mode

    Then the failure is recorded as a span attribute and the request is not aborted

    '
  code:
  - provisa/observability/stage_trace.py
  - provisa/compiler/stage2.py
  - provisa/compiler/rls.py
  - provisa/compiler/mask_inject.py
  - provisa/transpiler/transpile.py
  tests:
  - tests/unit/test_stage_trace.py
  related_reqs:
  - REQ-913
  - REQ-302
  - REQ-303
- id: REQ-920
  status: complete
  since: 2026-07
  group: 12. Migration & Compatibility (Hasura)
  category: Config Generation
  priority: MUST
  type: structural
  description: 'The DDN-to-Provisa connector schema converter (ddn/mapper.py, _map_connectors) emits editable
    placeholder connection strings when an HML subgraph connector lacks a source-override. Placeholder
    format: localhost/5432/default/postgres/${env:DB_PASSWORD}. Intentional fallback: DDN is a config-generation
    tool whose output is reviewed before deployment, not a runtime system.'
  use_case: Configuration generation tools output templates for human review and edit; the placeholder
    guides the operator to fill in the correct connection URI with environment variable substitution.
  code:
  - provisa/ddn/mapper.py
  tests:
  - tests/unit/test_ddn.py
  related_reqs: []
- id: REQ-921
  status: complete
  since: 2026-07
  group: 6. Execution, Routing, Caching & Performance
  category: Promotion Coercion
  priority: MUST
  type: behavioral
  description: 'An unmapped promotion target_type in API JSONB promotions (provisa/api_source/promotions.py,
    generate_promotion_ddl) yields an empty cast (no explicit JSONB→target coercion). Intentional: only
    types requiring explicit coercion are in _PG_CAST_MAP; unmapped types are already representable in
    JSONB without casting.'
  use_case: Promotion coercion maps only the types that need explicit PostgreSQL casts; types not requiring
    coercion do not need a cast and the absence signals correct representation.
  scenario: 'Given a promotion with a target_type that is in _PG_CAST_MAP

    When generate_promotion_ddl generates the coercion SQL

    Then the output includes an explicit CAST expression

    When a promotion with a target_type not in _PG_CAST_MAP

    Then the output omits the CAST and relies on JSONB native representation

    '
  code:
  - provisa/api_source/promotions.py
  tests:
  - tests/unit/test_api_promotions.py
  related_reqs: []
- id: REQ-922
  status: complete
  since: 2026-07
  group: 9. Live Data & Events
  category: Change Data Capture
  priority: MUST
  type: behavioral
  description: 'Debezium CDC provider (provisa/subscriptions/debezium_provider.py) uses a stable sentinel
    (datetime.min) for missing or unparseable ts_ms, mirroring REQ-343 (RSS provider sentinel). Intentional
    fallback: ts_ms is optional in Debezium envelopes (snapshot/tombstone records may omit it); using
    now() would advance the watermark and drop later real events.'
  use_case: Change-data-capture watermarks must monotonically advance only with real event timestamps,
    not clock time; sentinel values preserve event ordering when optional fields are absent.
  scenario: 'Given a Debezium envelope with missing or unparseable ts_ms

    When the CDC provider processes the record

    Then it uses datetime.min as the event timestamp

    And the watermark does not advance beyond real events

    '
  code:
  - provisa/subscriptions/debezium_provider.py
  tests:
  - tests/unit/test_debezium_provider.py
  - tests/integration/test_debezium_cdc.py
  related_reqs:
  - REQ-343
- id: REQ-923
  status: complete
  since: 2026-07
  group: 5. Query Languages, Compilation & Operations
  category: Trino Introspection
  priority: MUST
  type: behavioral
  description: 'Trino column introspection (provisa/compiler/introspect.py, introspect_column_types /
    _fetch_with_startup_retry) retries with backoff when the coordinator reports SERVER_STARTING_UP, up
    to the ready timeout. Every other Trino error propagates. Intentional resilience: transient coordinator
    boot can last minutes; retry captures this operational reality without masking genuine errors.'
  use_case: Coordinator restarts are transient operational events; introspection must tolerate boot time
    without becoming a silent mask for genuine connection or permission errors.
  scenario: 'Given Trino introspection is invoked during coordinator startup

    When the coordinator reports SERVER_STARTING_UP

    Then the introspection retries with backoff up to the ready timeout

    When any other Trino error occurs

    Then the error propagates without retry

    '
  code:
  - provisa/compiler/introspect.py
  tests:
  - tests/unit/test_compiler_introspect.py
  - tests/integration/test_introspect.py
  related_reqs: []
- id: REQ-924
  status: complete
  since: 2026-07
  group: 3. Source Registration & Data Modeling
  category: Source Watermarking
  priority: MUST
  type: structural
  description: A source table has at most one watermark column, selected from existing columns only. Sources
    have no derived/synthetic columns; watermark value is a column name or none.
  use_case: The watermark column enables incremental refresh and subscriptions; limiting to one existing
    column simplifies source configuration and avoids synthetic stamping.
  code:
  - provisa/core/models.py
  - provisa/federation/residency.py
  tests:
  - tests/unit/test_residency.py
  - tests/unit/test_watermark_validation.py
  related_reqs: []
- id: REQ-925
  status: complete
  since: 2026-07
  group: 3. Source Registration & Data Modeling
  category: Source Watermarking
  priority: MUST
  type: constraint
  description: If a watermark column is set, it must be monotonic non-decreasing (timestamp or incrementing
    integer). The column type is validated at selection time; non-monotonic columns are rejected.
  use_case: Incremental refresh and subscription reads rely on WHERE wm > cursor predicates; non-monotonic
    values would miss or duplicate rows, violating correctness.
  code:
  - provisa/core/config_loader.py
  - provisa/core/ir_types.py
  tests:
  - tests/unit/test_watermark_validation.py
  related_reqs:
  - REQ-924
- id: REQ-926
  status: complete
  since: 2026-07
  group: 9. Live Data & Events
  category: Change Subscriptions
  priority: MUST
  type: behavioral
  description: 'The watermark column gates refresh mode and subscribability: watermark set → APPEND refresh
    (incremental, WHERE wm > cursor) and subscriptions allowed; watermark unset → REPLACE refresh (full
    DELETE+INSERT) and subscriptions forbidden.'
  use_case: Subscribability and incremental refresh are coupled; only watermarked sources can reliably
    serve incremental reads and change notifications.
  scenario: 'Given a source with a watermark column set

    When refresh is triggered

    Then the system executes APPEND refresh (incremental WHERE wm > cursor)

    And subscriptions to this source are permitted

    Given a source with no watermark column

    When refresh is triggered

    Then the system executes REPLACE refresh (full DELETE+INSERT)

    And subscriptions to this source are forbidden'
  code:
  - provisa/core/change_signal.py
  - provisa/federation/residency.py
  - provisa/events/injector.py
  tests:
  - tests/unit/test_change_signal.py
  - tests/unit/test_residency.py
  - tests/unit/test_injector.py
  related_reqs: []
- id: REQ-927
  status: complete
  superseded_by: REQ-926
  since: 2026-07
  group: 9. Live Data & Events
  category: Change Subscriptions
  priority: MUST
  type: constraint
  description: Subscriptions require a watermark column. No watermark = no subscriptions and no incremental
    refresh.
  use_case: Subscriptions deliver rows WHERE wm > cursor; without a watermark, there is no monotonic reference
    point for incremental delivery.
  code:
  - provisa/core/change_signal.py
  - provisa/core/config_loader.py
  - provisa/live/reconcile.py
  tests:
  - tests/unit/test_change_signal.py
  - tests/unit/test_injector.py
  related_reqs:
  - REQ-926
- id: REQ-928
  status: complete
  since: 2026-07
  group: 9. Live Data & Events
  category: Change Subscriptions
  priority: MUST
  type: constraint
  description: Delivered subscription grain is INSERTS and UPDATES only; HARD DELETES are not supported.
    An append + (wm > cursor) model cannot observe removed rows; a watermarked landed source is a watermark-ordered
    append log, not deduplicated current-state.
  use_case: Subscribers receive a changelog stream ordered by watermark; deletes leave no advancing watermark
    and cannot be signaled within the append-log model.
  code:
  - provisa/core/change_signal.py
  - provisa/events/injector.py
  tests:
  - tests/unit/test_injector.py
  - tests/unit/test_change_signal.py
  related_reqs:
  - REQ-926
- id: REQ-929
  status: complete
  since: 2026-07
  group: 9. Live Data & Events
  category: Change Signals
  priority: MUST
  type: structural
  description: 'The notification/inbound change signal is independent from the watermark column. Allowed
    values: ttl | probe | probe+ttl | native | debezium | kafka. When a watermark exists, probe defaults
    to MAX(watermark); otherwise probe is a source-native query or TTL is used. Push variants (native/debezium/kafka)
    rely on the operator to maintain feed health.'
  use_case: Change signals are orthogonal to watermarking; different sources may use different notification
    mechanisms (polling vs. push, automatic vs. operator-managed).
  code:
  - provisa/core/change_signal.py
  tests:
  - tests/unit/test_change_signal.py
  related_reqs: []
- id: REQ-930
  status: complete
  since: 2026-07
  group: 6. Execution, Routing, Caching & Performance
  category: Materialization
  priority: MUST
  type: behavioral
  description: Materialization is derived from federation-engine reachability, not a user knob. A source
    the engine cannot reach live is materialized/landed (mandatory). A reachable source is queried live,
    with optional perf-caching (TTL-based). First land of an unreachable source is unconditional; the
    change signal governs subsequent refresh.
  use_case: Materialization ensures correctness when live access is unavailable; it is driven by infrastructure
    capability, not user preference, eliminating misconfiguration.
  scenario: 'Given a source that the federation engine cannot reach live

    When the source is first added

    Then it is materialized/landed unconditionally

    And subsequent refreshes follow the change signal (ttl/probe/push)

    Given a source that the engine can reach live

    When queries reference it

    Then it is queried live without materialization

    And optional perf-caching may be applied via TTL'
  code:
  - provisa/federation/strategy.py
  - provisa/federation/plan.py
  tests:
  - tests/unit/test_federation_strategy.py
  related_reqs: []
- id: REQ-931
  status: complete
  since: 2026-07
  group: 9. Live Data & Events
  category: Change Subscriptions
  priority: MUST
  type: structural
  description: Derived/synthetic watermarks are manufactured in the SQL/view layer, never at the source.
    A view can project a computed watermark column (e.g. GREATEST(created_at, updated_at), MAX of inputs'
    watermarks, ROW_NUMBER) and downstream subscribes to that derived column. Source-level synthetic-watermark
    stamping is intentionally unnecessary.
  use_case: Views provide full SQL tooling for composing derived watermarks; sourcing synthetic columns
    at ingest would duplicate this capability and complicate source registration.
  code:
  - provisa/federation/residency.py
  tests:
  - tests/unit/test_residency.py
  related_reqs: []
- id: REQ-932
  status: complete
  since: 2026-07
  group: 9. Live Data & Events
  category: Change Detection Strategy
  priority: MUST
  type: structural
  description: 'The change_signal→landing-shape mapping (push→CDC upsert/tombstone by PK, poll+watermark→APPEND,
    else REPLACE) is implemented as store_writer.select_landing_shape + the DB-agnostic Core ops in materialize_exec
    (land_replace/land_append/apply_cdc via the dialect-agnostic Connection.upsert), NOT engine CTAS in
    mv/refresh. The named integration tests (test_landing_replace/append_delete/publish_downstream) do
    not exist; coverage lives in unit tests (test_change_signal, test_materialize_landing, test_subscribe_publish).
    DONE (2026-07): provisa/federation/store_writer.py, provisa/federation/materialize_exec.py, provisa/core/change_signal.py.'
  use_case: Eliminates redundant encoding of change-detection intent across three independent fields (`change_signal`,
    `freshness_mode`, `live.strategy`). Centralizes the authoritative source and clarifies orthogonal
    axes (detection method, append/replace mode, materialization).
  code:
  - provisa/core/change_signal.py
  - api/app.py
  - api/admin/schema.py
  - api/data/subscribe.py
  - live/reconcile.py
  - core/config_loader.py
  - core/models.py
  - mv/refresh.py
  tests:
  - tests/unit/test_change_signal.py
  - tests/unit/test_materialize_landing.py
  - tests/unit/test_subscribe_publish.py
  related_reqs:
  - REQ-931
  - REQ-929
- id: REQ-933
  status: complete
  since: 2026-07
  group: 9. Live Data & Events
  category: Execution & Routing
  priority: MUST
  type: structural
  description: 'SQLAlchemy generic types are the ONE canonical engine-independent IR type registry (provisa/core/ir_types.py).
    Sources map native→IR (to_ir), fed engines map IR→physical dialect, the write face maps IR→SQLAlchemy
    — so the fed engine is never the type authority. to_ir normalizes native/dialect spellings to one
    canonical IR name (varchar→text, int4→integer, jsonb→text), strips length/precision, and RAISES on
    an unknown type (no silent varchar default). data_type is an IR name: authoritative for a LANDED table
    (drives store DDL), informational for a LIVE source (engine''s live resolution wins). Types resolved
    at registration, never lazily backfilled. DONE (2026-07): provisa/core/ir_types.py, provisa/federation/materialize_exec.py;
    tests: tests/unit/test_ir_types.py.'
  code:
  - provisa/core/ir_types.py
  - provisa/federation/materialize_exec.py
  tests:
  - tests/unit/test_ir_types.py
- id: REQ-934
  status: complete
  since: 2026-07
  group: 4. Source Connectors
  category: Materialization Store
  priority: MUST
  type: behavioral
  description: 'The materialization store is RECONCILED, not blindly created: an existing landing table
    whose columns match config is KEPT (survives restart, data intact); a config/table/engine drift that
    changes the column set RECREATES it (drop+create, re-landed on next refresh); absent → create. Convergent
    + idempotent. Two triggers, one primitive: boot (after config load) and runtime UI (re)registration.
    Eager-creates landing tables (DDL) + attaches the engine''s read view WITHOUT landing data — DDL/DML
    split so the catalog is complete at startup; landed sources only, live sources attach live. DONE (2026-07):
    provisa/federation/store_writer.py (reconcile_table), provisa/federation/native_backend.py (reconcile_landed_tables),
    provisa/federation/duckdb_runtime.py (attach_landed_source), provisa/federation/runtime.py, provisa/api/app.py,
    provisa/api/admin/schema.py; tests: tests/unit/test_reconcile_landed.py, tests/unit/test_store_writer.py.'
  scenario: 'Given a source with a landing table matching config columns

    When the engine boots

    Then the table is kept intact and reused

    Given a config/table drift that changes columns

    When reconcile_table is called

    Then the table is dropped and recreated

    Given a source without a landing table

    When reconcile is triggered

    Then DDL creates the table without landing data

    '
  code:
  - provisa/federation/store_writer.py
  - provisa/federation/native_backend.py
  - provisa/federation/duckdb_runtime.py
  - provisa/federation/runtime.py
  - provisa/api/app.py
  - provisa/api/admin/schema.py
  tests:
  - tests/unit/test_reconcile_landed.py
  - tests/unit/test_store_writer.py
- id: REQ-935
  status: complete
  since: 2026-07
  group: 4. Source Connectors
  category: Materialization Store
  priority: SHOULD
  type: behavioral
  description: 'At land time, incoming rows are mapped to the target columns by NAME (matched land, unmatched
    target NULL, extra keys dropped — best-effort); the land is REFUSED when the fraction of target columns
    present is <= a match floor (default 0.0 fails only at 0% overlap), turned by the caller into an error
    event — never silently land a mangled table. Distinct from config/design drift (which recreates, authoritative).
    DONE (2026-07): provisa/federation/store_writer.py (check_source_drift); tests: tests/unit/test_store_writer.py.'
  scenario: 'Given incoming rows with columns matching the target schema by name

    When check_source_drift is called

    Then rows are mapped to target columns with unmatched targets set to NULL

    Given incoming rows below the match floor threshold

    When check_source_drift is called

    Then the land is refused and an error event is emitted

    '
  code:
  - provisa/federation/store_writer.py
  tests:
  - tests/unit/test_store_writer.py
- id: REQ-936
  status: complete
  since: 2026-07
  group: 9. Live Data & Events
  category: Live Data & Events
  priority: MUST
  type: structural
  description: 'One event loop over one queue; base sources and MVs are homogeneous nodes. FIVE event
    kinds in three categories: APPLY {delta, append, replace} → landing shapes; ADVISE {warn} → alert/at-risk/DAG-tint,
    no enforcement, data still flows; HALT {error} → freeze (landed) / detach (live). Delta is UPSERT
    by PK (insert/update + tombstone delete); append is INSERT (rows + cursor WHERE watermark > last).
    Append carries the cursor/rows; a delta payload above the jsonb size ceiling degrades to replace.
    The emitted kind is set by the node''s incremental-maintainability (may differ from consumed; non-maintainable
    → replace).'
  use_case: Unified event model provides a single, homogeneous abstraction for all data-movement primitives
    (sources, MVs, subscriptions) with clear semantics for change operations (delta, append, replace),
    advisory warnings, and error handling.
  tests:
  - tests/unit/test_event_queue.py
  - tests/unit/test_injector.py
- id: REQ-937
  status: complete
  since: 2026-07
  group: 6. Execution, Routing, Caching & Performance
  category: Execution & Routing
  priority: MUST
  type: structural
  description: 'Every node = generate (fetch for a source | SQL for an MV — the only difference) → post-process
    (enrich and/or validate/gate) → emit one of the 5 events. Post-processing (enrich and/or validate)
    applies to sources AND MVs alike. Landing is OPTIONAL: a change event can trigger validation/reaction
    on LIVE data without materializing.'
  use_case: Single-threaded node model (generate→post-process→emit) provides a clear, composable architecture
    for incremental data flows while keeping all processing layers visible and auditable.
  tests:
  - tests/unit/test_processor.py
  - tests/unit/test_handlers.py
- id: REQ-938
  status: complete
  since: 2026-07
  group: 6. Execution, Routing, Caching & Performance
  category: Execution & Routing
  priority: MUST
  type: structural
  description: Every MV is SQL, authored to REFERENCE its inputs (SQLGlot extracts the dependency edges
    — reuse the exp.Table extraction already in provisa/api/admin/_table_ops.py) and DECLARE its full
    output schema; columns the SQL doesn't derive are constant placeholders completed by post-processing.
    The input reference (count/key/max(watermark)) doubles as the node's change/freshness token. Guarantees
    lineage is always derivable — no node ever has opaque inputs. DAG is acyclic (cycle detection at registration);
    re-parsed on registration. One event fans out to many dependents → the exclusive unit of work is (event
    × dependent-table).
  use_case: SQL lineage extraction from MV definitions ensures the DAG is always derivable and transparent;
    combining with change tokens enables correct cascading materialization and versioning.
  tests:
  - tests/unit/test_processor.py
- id: REQ-939
  status: complete
  since: 2026-07
  group: 6. Execution, Routing, Caching & Performance
  category: Execution & Routing
  priority: SHOULD
  type: structural
  description: 'SQL is always the lineage/schema authority; processors are SUBORDINATE, attached to COLUMNS
    (post-process completing declared columns, or a UDF sugar; external UDFs deferred to a batched post-process).
    Processor contract: pure function of declared inputs → declared output model, no side effects, deterministic
    (or a declared best-effort). Tiers by robustness: in-engine SQL → in-process UDF → external env. MV
    compute resources are OPERATOR-SUPPLIED (the fed engine, sized to need — MPP for big views; Provisa
    builds no distributed compute and orchestrates the supplied engine). DQ metrics / ML features / enrichment
    are external processors — bring your compute, Provisa governs the OUTPUT as a first-class governed
    data product.'
  use_case: Processors provide an imperative escape hatch when SQL is insufficient, while keeping lineage
    and schema declaration authoritative; tiers enable plugging any compute backend.
  tests:
  - tests/unit/test_lineage.py
- id: REQ-940
  status: complete
  since: 2026-07
  target: 2026-Q4
  group: 6. Execution, Routing, Caching & Performance
  category: Execution & Routing
  priority: MAY
  type: infrastructure
  description: 'The external processor tier is a toolkit of transport adapters — shell (stdin/stdout),
    HTTP (streamed request/response), gRPC (bidi-stream) — all conforming to ONE streaming input→output
    contract: a stream of schema-declared rows in, a stream of schema-declared rows out; pure; no side
    effects; schema-validated both ends. NDJSON default framing (Arrow-stream for throughput is a future
    framing under the same contract). The toolkit owns framing, streaming, and fail-loud schema validation;
    the author implements only the transform. Extends the in-process REQ-957 preprocess hook to external
    processors reachable over any transport. A declarative spec (transport + address) selects the adapter
    via provisa.processors.build_adapter.'
  use_case: Pluggable transport adapters allow external processors to be invoked over any mechanism (shell,
    HTTP, gRPC) while maintaining a single streaming contract and declarative schema validation.
  code:
  - provisa/processors/contract.py
  - provisa/processors/framing.py
  - provisa/processors/shell.py
  - provisa/processors/http.py
  - provisa/processors/grpc.py
  - provisa/processors/factory.py
  integration_test: not_required
  integration_test_reason: The shell adapter test invokes a real subprocess (a live process boundary);
    HTTP/gRPC transports are exercised through injected transports. No external service to stand up.
  tests:
  - tests/unit/test_processors.py
- id: REQ-941
  status: complete
  since: 2026-07
  group: 9. Live Data & Events
  category: Live Data & Events
  priority: MUST
  type: constraint
  description: 'A gate is a validate-processor with halt authority; outcome ∈ {pass → data event; warn
    → advisory warn event (no enforcement, data flows); fail → error event (halt)} — declared per node.
    Hard-fail enforcement: landed → freeze last-good; live → detach + alert (declared-severe and rare;
    eventual + catalog-granular, no per-row/in-flight block over federation). error is emitted by ANY
    hard node failure (source-land failure, hard gate, drift-below-floor, unreachable source, unknown
    type), propagates blocked-upstream transitively, downstream freezes last-good, vintage stays honest
    ("as of last good land, known-blocked since T"). Recovery = re-registration through the reconcile
    controller (no dedicated re-attach event, no default background loop); auto-recovery is an opt-in
    health-probe policy that owns cadence + hysteresis.'
  use_case: Gates provide declarative data validation and enforcement, with clear semantics for warnings
    (advisory) and errors (halt), ensuring data correctness while maintaining transparent recovery paths
    and vintage honesty.
  tests:
  - tests/unit/test_processor.py
  - tests/unit/test_supervisor.py
  - tests/unit/test_boot.py
  - tests/unit/test_app_wiring.py
- id: REQ-942
  status: complete
  since: 2026-07
  group: 9. Live Data & Events
  category: Live Data & Events
  priority: MUST
  type: infrastructure
  description: 'A shared event substrate is the third fleet-shared pillar (with control-plane DB + shared
    cache), PLUGGABLE: in-process (single-node/embedded) → durable log (fleet). Default backend is a control-plane
    EVENT TABLE as a transactional outbox (the event is enqueued in the SAME transaction as the state
    change → atomic; superior to a broker for correctness); SELECT FOR UPDATE SKIP LOCKED claim + LISTEN/NOTIFY
    wake + a reaper; graduate to Kafka only for raw-event volume. Two tables: events (posted once) + event_status
    (one row per event × dependent-table = the fanout work item). Claim by TARGET TABLE (not per-event):
    cross-table parallel, same-table serialized, coalesce at drain; heartbeat lease, stale → reclaim,
    idempotent apply = effectively-once. High-volume raw CDC is digested at the injector edge into coalesced
    node-level events; the table holds only digested events. Three roles: INJECTORS (external → post events,
    one per modality; delta/append payloads above jsonb ceiling degrade to replace), TABLE PROCESSORS
    (claim → generate/land → post-process/gate → complete → re-post downstream events; homogeneous/stateless/horizontally-scaled),
    REPEATERS (cursor-based fanout read for SSE-to-local-clients; forward, never claim). Dispatch rule
    (the N-boxes test): CLAIM exactly-once for anything whose duplication across the fleet is wrong —
    landing, side-effecting reactions (warn/error handlers: email/ticket/page), Kafka-sink produce; FANOUT
    (cursor read-all) ONLY for per-box-local delivery (SSE). In-loop = pure/convergent (constrained);
    out-of-loop reactions = arbitrary side-effecting automation (free), including error/warn-event automation
    — but to change the governed DATA you must re-enter the governed loop (inject/re-register/re-ingest),
    never a side-channel store write.'
  use_case: A shared event substrate provides the scalability and correctness foundation for fleet-wide
    data propagation, with clear role separation (injector, processor, repeater) and dispatch semantics
    (claim for mutations, fanout for broadcast).
  tests:
  - tests/unit/test_event_queue.py
  - tests/unit/test_injector.py
- id: REQ-943
  status: complete
  since: 2026-07
  group: 9. Live Data & Events
  category: Live Data & Events
  priority: MUST
  type: behavioral
  description: The event-loop source-land path fetches current rows from a materialized source table through
    the federation engine's SQL terminal. SourceRowLoader.load(source, table) issues a qualified/quoted
    SELECT * via engine.execute_engine and returns row dicts. Row-oriented API/push source types (openapi,
    ingest, websocket, rss, grpc_remote, prometheus, google_sheets) have no engine-scannable table; load()
    raises UnsupportedSourceFetch (explicit, never silent) for types without a wired adapter (openapi
    is wired via REQ-945; others require per-adapter fetch implementation). The boot wiring lands nothing
    for that node (logged once) until a per-adapter fetch is wired.
  use_case: The event-loop source-land path requires a unified interface to fetch current snapshots of
    materialized source tables through the federated SQL engine, with explicit failure signaling for source
    types that lack engine-scannable tables and deferred adapter-specific fetch logic.
  code:
  - provisa/events/source_loader.py
  - provisa/events/app_wiring.py
  tests:
  - tests/unit/test_source_loader.py
  scenario: 'Given a materialized source with current rows in the federation catalog

    When SourceRowLoader.load(source, table) is invoked

    Then engine.execute_engine issues SELECT * FROM "<catalog>"."<schema>"."<table>" and returns row dicts

    Given a row-oriented API/push source type with no engine-scannable table

    When SourceRowLoader.load(source, table) is invoked

    Then UnsupportedSourceFetch is raised and no scan is issued

    '
- id: REQ-944
  status: complete
  since: 2026-07
  group: 10. UI & Admin Surfaces
  category: Schema Discovery
  priority: SHOULD
  type: ui
  description: The schema-discovery admin UI assigns canonical IR data-types to source columns. The backend
    exposes the IR vocabulary via GET /admin/schema-discovery/ir-types (returns sorted IR_TYPES from provisa/core/ir_types.py).
    The SchemaDiscovery UI component fetches that vocabulary to populate per-column type dropdowns instead
    of hardcoded engine-specific types. New manually-added columns default to IR "text". Discovered native
    columns are normalized to IR names via toIrType (mirroring backend native→IR aliases) so introspected
    columns land on valid dropdown values. Steward-assigned IR data-types persist through registration
    via GraphQL ColumnInput.data_type field, which flows into Column model and is saved via table_repo.save.
    Type assignments are engine-independent; the canonical IR vocabulary maps every IR name to a GraphQL
    scalar, and the landing write path maps IR → the store's physical type.
  use_case: Stewards can standardize column types to a canonical vocabulary independent of source engine
    during schema discovery, ensuring consistent data modeling across heterogeneous sources.
  code:
  - provisa/api/admin/discovery_schema.py
  - provisa/api/admin/types.py
  - provisa/api/admin/_table_ops.py
  - provisa/compiler/type_map.py
  - provisa/core/ir_types.py
  - provisa-ui/src/components/SchemaDiscovery.tsx
  - provisa-ui/src/api/admin.ts
  - provisa-ui/src/irTypes.ts
  tests:
  - tests/unit/test_ir_types_endpoint.py
  - tests/unit/test_ir_type_persistence.py
  - provisa-ui/src/__tests__/schemaDiscovery-irtype.test.ts
  scenario: 'Given a steward discovering a source with mixed native type names (e.g., Postgres int8, Snowflake
    BIGINT)

    When the SchemaDiscovery UI loads the column list

    Then toIrType normalizes each native type to its IR equivalent (e.g., both → IR "int")

    And the type dropdown displays only valid IR vocabulary from GET /admin/schema-discovery/ir-types

    And the steward can assign or override any IR type per column

    Given a new column added manually to the source

    When the column is created

    Then the default type is IR "text"

    '
- id: REQ-945
  status: complete
  since: 2026-07
  group: 9. Live Data & Events
  category: Live Data & Events
  priority: MUST
  type: behavioral
  description: The openapi adapter fetch is wired into SourceRowLoader via make_openapi_loader, which
    accepts injectable per-type adapter_loaders. make_openapi_loader resolves a table's registered ApiEndpoint
    and its ApiSource (base_url + auth) from live app state (state.api_endpoints / state.api_sources),
    calls the operation with its default_params via api_source.caller.call_api, and flattens the response
    pages into row dicts via api_source.flattener.flatten_response — the same call_api → flatten chain
    the API-cache path uses. app_wiring builds it from state and registers it under "openapi" source type,
    so openapi source nodes now land real data. A table with no registered endpoint raises UnsupportedSourceFetch
    (never a silent empty snapshot). Remaining API/push types (ingest, websocket, rss, grpc_remote, prometheus,
    google_sheets) still raise UnsupportedSourceFetch until each is wired.
  use_case: Openapi sources need a real-time fetch path that reuses the same authentication and response
    flattening logic as the API-cache write path, without requiring an engine-scannable table.
  code:
  - provisa/events/source_loader.py
  - provisa/events/app_wiring.py
  tests:
  - tests/unit/test_source_loader.py
  scenario: 'Given an openapi source with a registered ApiEndpoint and ApiSource in live state

    When SourceRowLoader.load(source, table) is invoked

    Then make_openapi_loader resolves the ApiEndpoint and ApiSource from state

    And calls the operation with default_params via api_source.caller.call_api

    And flattens the response pages via api_source.flattener.flatten_response

    And returns row dicts without issuing an engine SELECT

    Given an openapi source with no registered ApiEndpoint

    When SourceRowLoader.load(source, table) is invoked

    Then UnsupportedSourceFetch is raised

    '
- id: REQ-946
  status: complete
  since: 2026-07
  group: 4. Source Connectors
  category: Connector Abstraction
  priority: MUST
  type: structural
  description: 'Trino Connector class is the single source of truth for a source type''s Trino catalog
    and its reach (realizes REQ-842). Each catalogable source type has a Trino Connector (provisa/federation/connector.py)
    declaring trino_connector (the Trino connector.name for CREATE CATALOG ... USING) and details() (the
    catalog .properties). build_trino_engine registers the full set via build_trino_connectors(); provisa/core/catalog.py
    create_catalog/_build_catalog_properties derive name+props from the TRINO_CONNECTORS registry — a
    source type with no Trino connector gets NO catalog. sqlite/openapi are Mechanism.LAND (data landed
    into local PG cache, then Trino reads it); all other catalogable types are ATTACH. Consequence: federate()
    classifies Trino''s connector types as VIRTUAL (federated live) rather than MATERIALIZED — engine.connectors
    is the engine''s true reach.'
  use_case: Centralizing connector registration through a single registry eliminates parallel type→name
    maps and hardcoded property branches, ensuring consistent catalog generation and reach classification
    across all source types.
  code:
  - provisa/federation/connector.py
  - provisa/core/catalog.py
  tests:
  - tests/unit/test_federation_strategy.py
  - tests/unit/test_federation_engine.py
  - tests/unit/test_federation_materialization.py
  - tests/unit/test_sharepoint_connector.py
  - tests/unit/test_splunk_connector.py
  - tests/unit/test_trino_catalog_files.py
  related_reqs:
  - REQ-842
- id: REQ-947
  status: complete
  tests:
  - tests/unit/test_source_registry_contract.py
  - tests/unit/test_engine_reach_faces.py
  since: 2026-07
  target: 2026-Q3
  group: 4. Source Connectors
  category: Connector Abstraction
  priority: MUST
  type: structural
  description: 'engine.connectors (the unified connector registry per federation engine) must include
    a connector for EVERY source type the engine can offer: (a) federated via engine''s own connectors,
    (b) direct-route via Provisa''s native async drivers (executor/drivers/registry.py), and (c) adapter-materializable
    (source_adapters/_ADAPTER_MAP). Only then is the source-creation dropdown a pure projection of engine.connectors;
    today direct-only sources (e.g., duckdb when used alone) and adapter-only sources (govdata) are absent
    from engine.connectors and would be dropped from that projection. Introduce a third Mechanism (FETCH)
    for (c): Provisa''s adapter fetches rows; write face lands them; engine reads the replica. This retires
    the parallel SOURCE_TO_CONNECTOR map and adapter_loaders special-casing.'
  use_case: engine.connectors must be complete — federatable ∪ Provisa-direct ∪ adapter-materializable
    — so that the source-creation dropdown dynamically reflects exactly what each federation engine can
    reach, whether through its native connectors, direct drivers, or adapters. Eliminates hardcoded source-type
    lists and parallel registry maps.
  code:
  - provisa/federation/connector.py
  - provisa/federation/engine.py
  - provisa/events/source_loader.py
  - provisa/executor/drivers/registry.py
  - provisa/source_adapters/_ADAPTER_MAP
- id: REQ-948
  status: complete
  since: 2026-07
  scenario: 'Given a connector runtime_deps entry tagged provider="bundled"

    When the source list is rendered

    Then the source is enabled because Provisa ships and relocates its driver


    Given a runtime_deps entry tagged provider="operator" whose driver is not installed

    When probe() reports it unavailable

    Then the source appears in the dropdown but disabled with its operator remediation

    '
  tests:
  - tests/unit/test_connector_reach_modes.py
  target: 2026-Q3
  group: 4. Source Connectors
  category: Driver Management
  priority: SHOULD
  type: behavioral
  description: 'Driver availability is per-connector data, not new machinery: Connector.runtime_deps entries
    are tagged by who provides the driver — "system" (OS-provided), "bundled" (Provisa ships + relocates
    it), "operator" (BYO, must install) — and Connector.probe() (REQ-904) reports functional availability
    + operator remediation. A source is shown in the dropdown but rendered disabled with its remediation
    when its driver is operator-provided and not installed. Bundled subset Provisa distributes: postgres
    (libpq), duckdb (libduckdb), sqlite (system libsqlite3), csv, parquet, iceberg, sqlserver, mysql,
    plus any source reachable via SQLAlchemy fallback driver. Niche drivers (e.g. oracle, some SaaS) stay
    operator/BYO.'
  use_case: Tagging driver availability per-connector allows stewards to see which sources are available,
    which require operator installation, and what remediation is needed — without silent failures or hardcoded
    availability checks.
  code:
  - provisa/federation/connector.py
  related_reqs:
  - REQ-904
- id: REQ-949
  status: complete
  since: 2026-07
  code:
  - provisa/federation/trino_connectors.py
  - provisa/federation/clickhouse_connectors.py
  - provisa/federation/connector.py
  - provisa/federation/engine.py
  tests:
  - tests/unit/test_federation_engine.py
  - tests/unit/test_clickhouse_connectors.py
  - tests/unit/test_connector_reach_modes.py
  group: 4. Source Connectors
  category: Connector Abstraction
  priority: SHOULD
  type: structural
  description: 'Structural debt: provisa/federation/connector.py and provisa/federation/engine.py are
    monoliths that do not scale as more federation engines are added. Split them into per-engine modules
    (e.g. provisa/federation/connectors/<engine>.py and per-engine engine builders) before/as new engines
    land.'
  use_case: As Provisa adds support for additional federation engines (DuckDB, ClickHouse, etc.), centralizing
    all engine-specific code in monolithic modules becomes unmaintainable. Per-engine module organization
    enables independent engine contributions without merge conflicts or coupling.
- id: REQ-950
  status: complete
  group: 4. Source Connectors
  category: Wire-compatible RDB Families
  priority: MUST
  type: structural
  description: Wire-compatible RDB reuses its base wire's native async driver, SQLGlot dialect, and Trino
    connector via registry data only. Implemented for CockroachDB/YugabyteDB/Greenplum (Postgres-wire)
    and TiDB (MySQL-wire) via SourceType + SOURCE_TO_CONNECTOR + SOURCE_TO_DIALECT + _PG_WIRE_TYPES/_MYSQL_WIRE_TYPES
    + jdbc_url + _TRINO_JDBC_TYPES + _DRIVER_FACTORIES. Each federates VIRTUAL on Trino and is direct-route
    capable.
  code:
  - provisa/core/models.py
  - provisa/federation/connector.py
  - provisa/executor/drivers/registry.py
  tests:
  - tests/unit/test_wire_compatible_rdbs.py
  since: 2026-07
- id: REQ-951
  status: complete
  since: 2026-07
  target: 2026-Q3
  group: 4. Source Connectors
  category: Connector Abstraction
  priority: MUST
  type: structural
  description: 'Connector declares a CAPABILITY SET of reach modes (mechanisms: frozenset[Mechanism])
    instead of a single fixed mechanism. Extend Mechanism to include SCAN and a Provisa-direct materialize
    member (FETCH). Examples: Trino postgresql = {ATTACH, LAND}; API source (openapi/graphql_remote/grpc_remote)
    = {FETCH} (Provisa fetches + write-face lands; engine reads replica); lake file on DuckDB = {SCAN};
    warehouse-native = {LAND into self}. federate(), strategy.py, CatalogEntry.mechanism, materialization.py,
    and promote.py read the declared capability set and pick a reach mode per policy/cost instead of a
    single mechanism plus scattered heuristics (prefer_materialized, cost-based promotion). Materializer''s
    flag (materialize_store) and planner logic are simplified.'
  use_case: Declaring a full set of reach modes per connector enables federate() and the planner to intelligently
    select reach modes per policy and cost, eliminating ad-hoc heuristics and making reach flexibility
    explicit and testable.
  code:
  - provisa/federation/connector.py
  - provisa/federation/strategy.py
  - provisa/federation/materialization.py
  - provisa/federation/promote.py
  - provisa/core/catalog.py
  tests:
  - tests/unit/test_connector_reach_modes.py
  related_reqs:
  - REQ-947
  - REQ-841
  - REQ-846
- id: REQ-952
  status: complete
  since: 2026-07
  group: 4. Source Connectors
  category: Source Type Availability
  priority: MUST
  type: ui
  description: 'The source-creation type dropdown displays the UNION of all possible source types, gated
    and annotated against the currently-configured federation engine to educate users on the impact of
    engine choice. Backend engine_registry() exposes per-engine reach faces: live_source_types (types
    read LIVE via attach connector — ATTACH_RW/ATTACH_R) and reachable_source_types (every configurable
    type = live-attach ∪ _MATERIALIZE_ONLY ∪ Provisa-direct drivers/adapters that land a refreshed replica).
    UI tags each option: LIVE (attach, in place, fresh) or REPLICA (Provisa lands a refreshed copy). An
    unreachable type is disabled and annotated "LIVE w/ {engine labels}" naming the engines that reach
    it live.'
  use_case: Users can see at source-creation time which types are available live on the current engine
    and which would require materialization, enabling informed connectivity choices without trial-and-error
    or missing sources in the dropdown.
  code:
  - provisa/federation/engine.py
  - provisa-ui/src/pages/SourcesPage.tsx
  tests:
  - tests/unit/test_engine_reach_faces.py
  scenario: 'Given a federation engine with live_source_types = [postgres, mysql] and reachable_source_types
    = [postgres, mysql, oracle, csv]

    When the source-creation dropdown renders

    Then postgres and mysql are tagged LIVE and enabled

    And oracle and csv are tagged REPLICA and enabled

    Given a different federation engine where oracle is live but csv is not reachable

    When the source-creation dropdown renders

    Then csv is disabled and annotated "LIVE w/ Oracle, JDBC Engines"

    '
  related_reqs:
  - REQ-947
- id: REQ-953
  status: complete
  since: 2026-07
  group: 9. Live Data & Events
  category: Replica Lifecycle
  priority: MUST
  type: behavioral
  description: Source replicas are built at boot by posting replace events for each source, and refreshed
    by events based on their cache_ttl cadence or push listeners.
  use_case: Ensures all source data is available immediately at startup and stays current during runtime
    through event-driven updates.
  code:
  - provisa/events/boot.py
  tests:
  - tests/unit/test_boot.py
  - tests/unit/test_app_wiring.py
  scenario: 'Given a Provisa instance with source specs configured

    When the application boots

    Then register_runtime schedules a one-shot "events:boot" job that posts replace events for each source

    And supervisor.drain lands every source replica and fans to materialized views once, idempotently

    And register_runtime schedules refresh injectors for each source at its cache_ttl cadence

    And push sources are refreshed by their listeners

    '
- id: REQ-954
  status: complete
  since: 2026-07
  target: 2026-Q3
  group: 4. Source Connectors
  category: Replica Strategy
  priority: MUST
  type: behavioral
  description: To materialize files/sharepoint/splunk sources, Provisa starts the connector's bundled
    Calcite pgwire server (pgwire-file, pgwire-sharepoint, pgwire-splunk from github.com/kenstott/calcite
    release engine-v0.28.0), connects to it as a generic PostgreSQL endpoint, and SELECTs from the connector
    schema to land rows into the materialize store.
  use_case: Enables replica materialization for connector-backed sources independent of the federation
    engine's own connectors, making them reachable via pgwire bridge.
  code:
  - provisa/federation/strategy.py
  - provisa/federation/pgwire_replica.py
  - provisa/events/source_loader.py
  - provisa/events/app_wiring.py
  tests:
  - tests/unit/test_replica_strategy.py
  scenario: 'Given a files/sharepoint/splunk source registered in federation config

    When the replica strategy selects _CONNECTOR_PGWIRE_REPLICA

    Then Provisa launches the source''s bundled pgwire server and connects as generic PostgreSQL

    And SELECTs from the connector schema (files.*, sharepoint.*, splunk.* tables)

    And lands rows into the materialize store as the replica

    '
  related_reqs:
  - REQ-947
- id: REQ-955
  status: complete
  since: 2026-07
  target: 2026-Q3
  group: 4. Source Connectors
  category: Replica Strategy
  priority: MUST
  type: structural
  description: 'Each pgwire server is configured via model/model.json in the bundle with source-specific
    credentials and storage paths: files → directory (or S3 storageType + storageConfig + AWS env vars);
    sharepoint → siteUrl, tenantId, clientId, clientSecret (or cert/device-code); splunk → url (or host/port/protocol),
    token or username/password, app. Provisa manages server lifecycle (start/health/stop) per source.
    Each server is allocated a unique --port (default 5433) and --calcite-child host:port (default 127.0.0.1:5533)
    to enable concurrent execution without collision; port allocation is handled transparently by the
    pgwire-<connector> CLI wrapper (./bin/pgwire-<connector> --port <N> copies args into python -m pgwire_calcite.launcher).
    No PGWIRE_PORT env var exists; only --port CLI flag and PGWIRE_PYTHON/PGWIRE_HOME/PGWIRE_CALCITE_CLASSPATH*
    env vars.'
  use_case: Centralizes per-source configuration and lifecycle management so each connector's pgwire server
    is properly authenticated, monitored, and port-isolated for concurrent operation across multiple federated
    sources.
  code:
  - provisa/federation/strategy.py
  - provisa/federation/pgwire_replica.py
  tests:
  - tests/unit/test_replica_strategy.py
  scenario: 'Given a files source with backing-store config (directory or S3)

    When the replica strategy starts the pgwire server

    Then model/model.json is written with the source''s storage path and executionEngine

    And the server starts with those credentials

    And Provisa allocates a unique --port and --calcite-child port for this source

    And Provisa monitors and can stop the server on demand

    '
- id: REQ-956
  status: complete
  since: 2026-07
  target: 2026-Q3
  group: 4. Source Connectors
  category: Replica Strategy
  priority: MUST
  type: infrastructure
  description: Pgwire bundles (pgwire-file, pgwire-sharepoint, pgwire-splunk) are fetched on demand from
    github.com/kenstott/calcite release engine-v0.28.0 at runtime and cached locally; they are not bundled
    with Provisa. Version pinning and artifact download are managed via runtime_deps system.
  use_case: Decouples Provisa release cycles from pgwire connector releases, and keeps the Provisa distribution
    lean by fetching connector bundles on-demand.
  code:
  - provisa/runtime_deps
  - provisa/federation/pgwire_replica.py
  tests:
  - tests/unit/test_replica_strategy.py
  scenario: 'Given a Provisa instance that needs to materialize a files/sharepoint/splunk source

    When the pgwire replica strategy is triggered

    Then the system downloads pgwire-<connector> from engine-v0.28.0 if not cached

    And caches it locally for future use

    And launches from the cached bundle

    '
  related_reqs:
  - REQ-951
- id: REQ-957
  status: complete
  since: 2026-07
  group: 9. Live Data & Events
  category: Table Processor (Shared)
  priority: SHOULD
  type: behavioral
  description: 'A node''s processing envelope collapses to ONE optional user hook: preprocess(rows, ctx)
    -> rows, run after produce (source fetch or MV SQL) and before land. Absent = identity. Returning
    rows proceeds to land; returning [] is a row-level no-op (nothing lands, no re-post); ctx.warn(reasons)
    emits an advisory warn event and keeps going (non-fatal); raising emits an error event and short-circuits
    the land (fatal), fanning the error forward for poison propagation. Reject is not a new outcome —
    it is an error (REQ-941), just emitted before landing. warn/error severity replaces any separate accept/reject
    channel. preprocess MUST be deterministic (its output feeds the content hash that gates the re-post;
    non-determinism ripples forever). Built-ins NOT in the hook: pre-produce poison short-circuit (a claimed
    upstream error skips produce) and Defer (a lease action, not an emitted event). ctx is read-only:
    node name/kind, claimed-event summary, columns/schema, per-input frontier, forced flag, last-landed
    hash/epoch, and window bounds.'
  use_case: Authors express custom data prep, enrichment, validation, quarantine, and rejection through
    a single deterministic transform that folds into the existing event-type vocabulary (delta/append/replace/warn/error),
    with no bespoke gate machinery.
  code:
  - provisa/events/processor.py
  - provisa/events/handlers.py
  - provisa/events/boot.py
  - provisa/mv/preprocess.py
  - provisa/mv/models.py
  - provisa/core/models.py
  tests:
  - tests/unit/test_live_core_loop.py
  - tests/unit/test_mv_preprocess.py
  scenario: 'Given an MV node with a preprocess(rows, ctx) hook

    When the node is claimed and produce yields rows

    Then preprocess runs before land

    And returning rows lands them and re-posts the node''s change

    And returning [] lands nothing and re-posts nothing

    And ctx.warn(reasons) emits a warn event yet still lands the rows

    And raising emits an error event, lands nothing, and fans the error to dependents

    And a claimed upstream error short-circuits to error before produce runs

    '
  related_reqs:
  - REQ-941
  - REQ-940
  - REQ-939
- id: REQ-958
  status: complete
  since: 2026-07
  target: 2026-Q4
  group: 9. Live Data & Events
  category: Derived / MV Processor
  priority: SHOULD
  type: behavioral
  description: 'Temporal MVs are expressed as completeness-gated processing windows, not a separate snapshot
    pipeline. A node''s required-input set is its declared expected-events list (REQ-961), defaulting
    to all SQL-lineage inputs (extract_inputs, REQ-939), verified as a freshness contract at fire time
    (no NO_CHANGE events). A window opens on the first fanned-in event, gathers and coalesces subsequent
    events (fan-in collapse), and fires at the claim''s deadline (window.end + allowed_lateness). produce
    reads as-of ctx.window.end (never now(), to stay deterministic and replayable); the result lands as
    an append keyed on the window boundary (accumulating lineage), so a point-in-time snapshot (e.g. daily
    sales) is a read-time filter over the accumulating MV. Matching "received for this window" REQUIRES
    a window/epoch tag stamped on each event at emit time. Late data policy is per-node: drop, or emit
    a correction partition (never rewrite a sealed one). A window pegs by time and aligns time-keyed inputs;
    joins across inputs with no common time key still need frontier machinery. The accumulating-append
    + read-time-filter form serves PIT as a VIEW over retained RT history and therefore stays INSIDE the
    NRT ideal (REQ-966) — it is reconstructible, not a departure; the departure line is reconstructible-vs-frozen,
    and a PIT dataset that CANNOT be rebuilt from current state plus retained event history is a preserved,
    sealed snapshot (REQ-983), not this.'
  use_case: Snapshot/temporal MVs (daily sales, period reporting) and torn-read-free joins are served
    by one primitive — wait for the lineage-required inputs, then compute as-of the window boundary —
    collapsing the trigger, the as-of peg, the finality proxy, and partial-recompute cost into a single
    windowing option.
  code:
  - provisa/events/lineage.py
  - provisa/events/processor.py
  - provisa/events/deadlines.py
  - provisa/federation/store_writer.py
  tests:
  - tests/unit/test_live_temporal.py
  scenario: 'Given an MV whose lineage requires inputs A and B

    And a processing window with a deadline

    When A posts a windowed event but B has not

    Then the window stays open and does not fire

    When B posts its windowed event before the deadline

    Then the window fires once, computing as-of ctx.window.end

    And the result lands as an append keyed on the window boundary

    When a window reaches its deadline with the set incomplete

    Then it fires partial with a warn, or defers, per the node''s policy

    '
  related_reqs:
  - REQ-957
  - REQ-959
  - REQ-939
  - REQ-932
- id: REQ-959
  status: complete
  since: 2026-07
  target: 2026-Q4
  group: 9. Live Data & Events
  category: Table Processor (Shared)
  priority: SHOULD
  type: behavioral
  description: 'Claim scheduling and failover rest on ONE correctness anchor — a compare-and-set on the
    claim''s processor_name at commit — with every other timer demoted to a heuristic that is safe to
    be wrong. A claim declares a per-claim deadline (fire-by); there is no global lease. Three independent
    recovery paths, all resolved by the ownership CAS: (1) reassert-on-restart — a returning owner CASes
    its remembered claims; matched = resume (rehydrate deadline/window from the row), zero rows = a peer
    took it, drop; (2) optional heartbeat — a lapsed lease lets a peer take over a CRASHED owner before
    the deadline (failover-latency knob only; false positives cost a wasted recompute, never a double
    effect); (3) deadline-miss reclaim — deadline+grace passed and not completed lets a peer take over
    a STUCK-BUT-ALIVE owner (the failure a heartbeat structurally cannot catch). reclaimable = (heartbeat
    lapsed) OR (deadline+grace passed AND not completed). Deadline is availability-mode (fire partial
    at deadline) or consistency-mode (fire only on completeness; deadline is an SLA alert, not a trigger),
    per node. deadline may be self-declared at claim or derived as min(fixed, time-to-deadline).'
  use_case: Long-held batch/window claims survive worker crash, restart, and stuck-but-alive workers with
    no coordination service and no false-positive double-processing, because the ownership CAS at commit
    — not any lease's accuracy — decides who commits.
  code:
  - provisa/events/queue.py
  - provisa/events/supervisor.py
  - provisa/events/processor.py
  tests:
  - tests/unit/test_live_core_loop.py
  - tests/unit/test_processor.py
  scenario: 'Given a claim owned by a worker with a deadline and optional heartbeat

    When the owner crashes and its heartbeat lapses

    Then a peer may CAS-take-over before the deadline

    When the owner restarts and reasserts a still-owned claim

    Then the CAS matches and it resumes; if a peer took it the CAS matches zero and it drops

    When the owner is alive but missed deadline+grace without completing

    Then a peer reclaims it and fires

    And a superseded owner''s late commit fails the ownership CAS and applies nothing

    '
  related_reqs:
  - REQ-958
  - REQ-960
  - REQ-941
- id: REQ-960
  status: complete
  since: 2026-07
  target: 2026-Q4
  group: 9. Live Data & Events
  category: Table Processor (Shared)
  priority: MUST
  type: constraint
  description: 'The processing loop''s commit ordering and landing MUST be crash-safe under at-least-once
    resume. post + fan_out + complete run AFTER land and in one ownership-CAS-guarded control-plane transaction
    (post-before-complete): a crash between land and commit re-claims and re-runs, never losing the downstream
    ripple. Because land hits the store DB — a different database from the control-plane queue, so no
    shared transaction — land MUST be idempotent on the window key (replace overwrites; append upserts
    by window key, never blind-appends), so a re-run or a rare double-land from a reclaim race is harmless.
    Combined with recompute-to-current/window-scoped generate (idempotent) and the ownership CAS at commit,
    this yields exactly-one committed effect from any number of resumed attempts.'
  use_case: Guarantees no lost fan-out and no duplicate partitions across worker crash, restart, and failover,
    without distributed transactions — the two code gaps (complete-before-post ordering; land not keyed
    for idempotency) are closed as an invariant.
  code:
  - provisa/events/processor.py
  - provisa/federation/store_writer.py
  - provisa/federation/materialize_exec.py
  tests:
  - tests/unit/test_live_core_loop.py
  - tests/unit/test_processor.py
  related_reqs:
  - REQ-959
  - REQ-958
- id: REQ-961
  status: complete
  since: 2026-07
  target: 2026-Q4
  group: 9. Live Data & Events
  category: Derived / MV Processor
  priority: SHOULD
  type: behavioral
  description: 'A periodic MV''s processing trigger is the CALENDAR boundary (REQ-962), bounded by the
    claim deadline (window.end + allowed_lateness) — NOT a completeness barrier and NOT a stream of NO_CHANGE
    events. There is no NO_CHANGE event type. The DECLARED expected-events list is the report''s FRESHNESS
    CONTRACT: the inputs that must be fresh-through-window.end for the output to be trusted, verified
    by a PULL against per-input freshness state at fire time — not by receiving events. List length is
    the trust/latency dial: a fact-table subset = periodic fact-gated (dimensions read as-of window.end,
    never listed); all lineage inputs = strong; empty = calendar-only (compute the closed period, verify
    nothing). Undeclared default = all lineage inputs. At fire (the deadline; earlier only if a node opts
    into completeness firing): produce as-of window.end from landed data, then verify each listed input
    is fresh-through-window.end (its source successfully refreshed to cover the window, with zero or more
    rows). All fresh → trusted; zero rows on a fresh input → a trustworthy zero. A listed input NOT fresh-through-window.end
    at the deadline → outage → warn/hold (expected-but-absent) — the accountability the existence gate
    provides without a human — UNLESS the calendar says the window should not exist (holiday), which removes
    the expectation and the alarm. Early completeness firing is an optional optimization with little value
    for periodic reports; the push-completeness signal it needs is only justified for a continuous always-current
    MV requiring torn-read-free early firing, out of scope here.'
  use_case: A single declarative freshness contract replaces gating/reference classification and NO_CHANGE
    punctuation — the author lists which inputs must be current for the report to be trusted, matching
    a human's expectation of whether an output is due, so an absent output is unambiguously an outage
    (required input stale) or a legitimate no-op (holiday / fresh zero), decided by input freshness rather
    than by an event.
  code:
  - provisa/events/processor.py
  - provisa/events/freshness_contract.py
  - provisa/events/deadlines.py
  - provisa/events/lineage.py
  tests:
  - tests/unit/test_live_temporal.py
  scenario: 'Given a daily-sales MV with expected-events list [transactions] and a business-day calendar

    When the day is a business day and transactions are fresh-through-end-of-day

    Then at the deadline the window generates the day''s partition, trusted

    When the day is a business day with genuinely no sales but transactions refreshed cleanly

    Then transactions is fresh-through-end-of-day with zero rows and a trustworthy zero is sealed

    When the day is a business day but transactions is not fresh-through-end-of-day at the deadline

    Then an outage is raised as warn/hold (not a silent skip)

    When the calendar marks the day a holiday

    Then no window exists, the MV does not generate, and no alarm is raised

    '
  related_reqs:
  - REQ-958
  - REQ-962
  - REQ-932
- id: REQ-962
  status: complete
  since: 2026-07
  target: 2026-Q4
  group: 9. Live Data & Events
  category: Derived / MV Processor
  priority: SHOULD
  type: behavioral
  description: 'Temporal window boundaries come from named, shared, versioned CALENDARS — not fixed intervals.
    A calendar is a registered definition: base system (Gregorian / fiscal / retail 4-4-5), timezone (DST-aware
    — days are 23/25h, never a fixed offset), fiscal anchor, and a versioned business-day/holiday set.
    An MV declares (calendar, grain ∈ daily/weekly/monthly/quarterly/annual/…); the calendar deterministically
    yields [start,end), a calendar-addressable window id (e.g. 2026-Q1), and the next boundary — driving
    the as-of peg (window.end), the claim deadline (boundary + per-grain lateness), the scheduler wake,
    and forced-regen addressing. Grains NEST (day ⊂ week ⊂ month ⊂ quarter ⊂ year) so a coarse roll-up
    MV''s expected set is its constituent sealed sub- windows, and a shared calendar guarantees the nesting
    is consistent across the DAG. The calendar also GATES WINDOW EXISTENCE: a business-day grain has no
    window on a non- business-day (holiday) → the MV deterministically does not generate and raises no
    deadline alarm; a calendar-day grain still opens a window (zero/empty row) — the grain picks which,
    because "no sales row" and "zero-stock row" are both valid. Holiday data is versioned/immutable-per-window
    for replay fidelity — never silently drifted. Per-grain: allowed_lateness and the availability-vs-consistency
    deadline policy (REQ-959).'
  use_case: Quarterly/annual/daily/fiscal/retail MVs get correct, nesting, DST- and holiday-aware boundaries
    from one shared calendar registry, and holidays become a deterministic "no window" rather than a silent
    gap or a spurious deadline alarm.
  code:
  - provisa/events/calendars.py
  - provisa/events/deadlines.py
  - provisa/events/processor.py
  - provisa/events/boot.py
  tests:
  - tests/unit/test_live_temporal.py
  scenario: 'Given an MV declaring (fiscal-us calendar, quarterly grain)

    When fiscal Q1 closes

    Then the window [Q1-start, Q1-end) fires with id 2026-Q1, pegged as-of Q1-end

    And its expected set is the sealed daily sub-windows of Q1

    Given a daily-sales MV on a business-day grain

    When the day is a holiday in the calendar

    Then no window is created and the MV does not generate, with no alarm

    Given a daily-inventory MV on a calendar-day grain

    When the day is a holiday

    Then a window still opens and seals a zero/empty snapshot

    '
  related_reqs:
  - REQ-958
  - REQ-961
  - REQ-959
- id: REQ-963
  status: complete
  since: 2026-07
  target: 2026-Q4
  group: 9. Live Data & Events
  category: Derived / MV Processor
  priority: SHOULD
  type: behavioral
  description: 'Live (always-current) MVs debounce input churn using the SAME claim+deadline primitive
    as periodic MVs (REQ-959) — only the deadline SOURCE differs. Periodic: deadline = calendar boundary
    + lateness (REQ-962). Live: deadline = min(last_change + quiet, first_change + max_delay), a trailing-edge
    debounce with a hard cap. The max_delay cap is MANDATORY, not optional: pure quiet-reset starves under
    continuous churn (e.g. a report over 10 frequently-changing tables never goes quiet), so the cap guarantees
    a refresh and IS the staleness SLA — "wait for a quiet lull, but never be more than max_delay stale";
    quiet=0 disables debounce (real-time). The first triggering change opens a claim; it absorbs and coalesces
    subsequent changes (fan-in collapse — a cross-table burst of N events becomes ONE recompute); it fires
    once at the deadline. Safe ONLY because recompute- to-current is idempotent — debounce deliberately
    drops intermediate states and recomputes the final state once. Live MV specifics vs periodic: computes
    as-of now (no window.end peg) and REPLACE-lands (forgetful current view, not append/accumulate); the
    expected- events list is the TRIGGER SET (which inputs'' changes debounce-fire the recompute), not
    a freshness contract — unlisted inputs are read at recompute time but do not trigger. quiet and max_delay
    are per-MV. Debounce controls recompute FREQUENCY, not cost — per-fire cost is the orthogonal incremental-maintenance
    axis. All REQ-959 failover (reassert, deadline- takeover, ownership CAS) applies unchanged since the
    debounce deadline is the claim deadline.'
  use_case: A live report over many frequently-changing tables refreshes without churn — a burst of input
    changes collapses into a single recompute after a quiet lull, bounded by a max_delay staleness SLA
    — reusing the periodic MV's claim+deadline machinery with a debounce-derived deadline instead of a
    calendar-derived one.
  code:
  - provisa/events/processor.py
  - provisa/events/deadlines.py
  - provisa/events/queue.py
  tests:
  - tests/unit/test_live_temporal.py
  - tests/unit/test_processor.py
  scenario: 'Given a live MV over 10 frequently-changing tables with debounce quiet=2s max_delay=30s

    When a burst of changes arrives across several tables within 2s

    Then one claim opens, coalesces the burst, and fires a single recompute after the lull

    When the tables change continuously and never go quiet for 2s

    Then the max_delay cap fires a recompute at least every 30s

    When quiet is 0

    Then every change recomputes immediately (debounce disabled)

    And each recompute replaces the current-state result (no window peg, no append)

    '
  related_reqs:
  - REQ-959
  - REQ-961
  - REQ-958
- id: REQ-964
  status: complete
  since: 2026-07
  target: 2026-Q4
  group: 0. Architecture & Design Principles
  category: Data-Model-Driven Estate
  priority: MUST
  type: structural
  description: 'The materialization estate is DATA-MODEL-DRIVEN, not process-driven — the key differentiator.
    You declare the authoritative, governed data you want represented (each MV as SQL) and the RULES for
    processing/reprocessing (expected-events freshness contract REQ-961, calendar/grain REQ-962, deadline/debounce
    REQ-959/REQ-963, preprocess REQ-957); the system DERIVES the process — lineage from SQL (REQ-939),
    fan-out, scheduling, and failover — with nothing hand-orchestrated. This inverts the imperative pipeline
    model (dbt/Airflow author the process; the data model is emergent) and yields a single, almost- PROVABLE
    data estate: a deterministic function of (governed sources, SQL definitions, rules). "Almost" is bounded
    by five explicit PROOF OBLIGATIONS, each pushable toward mechanical enforcement: (1) transform determinism
    — MV SQL and preprocess must be pure (reject now()/random at registration; sandbox from wall-clock);
    (2) source freshness honesty — the one irreducible trust boundary, "trusted" is relative to what a
    source reports; (3) land idempotency on the window key — exactly-one-effect under the store/queue
    two-database split rests on this + ownership CAS (REQ-960), not a distributed transaction; (4) temporal-join
    correctness — the barrier guarantees input availability, not point-in- time-correct (SCD-2) joins,
    which are the SQL author''s obligation (lintable); (5) calendar immutability — replay fidelity requires
    holiday/fiscal data immutable-per-window (REQ-962). Close 1/3/5 mechanically, lint 4, and the estate
    is provable except where a source lies — the strongest guarantee a federated estate can make.'
  use_case: 'Governance and reproducibility are structural properties of the estate, not process hygiene:
    because lineage is derived (cannot drift), outputs are addressable and deterministically replayable,
    and trust is explicit via the freshness contract, the whole data estate is auditable as a function
    of its declared inputs and rules.'
  code:
  - provisa/events/lineage.py
  - provisa/events/boot.py
  - provisa/events/handlers.py
  tests:
  - tests/unit/test_live_selfdescribing.py
  scenario: 'Given an MV declared as SQL over shop.orders When it is registered Then lineage, fan-out,
    output schema, and PK are DERIVED from the SQL (nothing hand-orchestrated) Given an MV whose SQL reads
    now()/current_timestamp/random() Then registration is rejected — proof-obligation #1 (transform determinism)
    is enforced mechanically so materialization stays addressable and replayable'
  related_reqs:
  - REQ-939
  - REQ-957
  - REQ-960
  - REQ-961
  - REQ-962
- id: REQ-965
  status: complete
  since: 2026-07
  target: 2026-Q4
  group: 9. Live Data & Events
  category: Derived / MV Processor
  priority: SHOULD
  type: behavioral
  description: 'An MV has TWO INDEPENDENT operator-declared outcomes, not one: a PERSISTENCE outcome (how
    the compute result is applied to the MV''s own store table — replace / append / upsert, via land_replace
    / land_append / apply_cdc already in materialize_exec) and a SET of EVENT outcomes emitted downstream:
    per fire an MV may emit ANY, ALL, or NONE of {replace, append, delta} (alongside warn / error), each
    shape routed to the dependents that consume it — persistence is a single choice, the event side is
    a set, not a scalar. Emission is DEMAND- DRIVEN and pay-per-consumer: a shape is produced only if
    a dependent (or an explicit sink) subscribes to it, so delta''s diff cost is paid ONLY when delta
    has a subscriber. This is what makes operational datasets emergent — a History dataset is the subscriber
    of the append stream, a DQ dataset the subscriber of the warn/error stream, incremental dependents
    the subscribers of the delta stream, all from the same MV fire; the estate becomes self- describing
    (quality, history, freshness, lineage, audit are just more MVs over the streams the machinery already
    emits). The event outcomes are symmetric with the input side (select_landing_shape / change_signal,
    REQ-932), so a dependent MV cannot distinguish a base source from an upstream MV — the uniform DAG
    completed on the output side. The two axes are decoupled along BOTH cost and semantics: (cost) persist=replace
    + emit=delta recomputes the whole table but diffs new-vs-old ONCE at emit to hand every dependent
    a precise delta instead of a recompute; persist=upsert + emit=replace maintains precisely but tells
    downstream only "recompute"; (semantics) persist=replace + emit=append lets a forgetful current-state
    MV feed a downstream that KEEPS HISTORY — it overwrites itself yet emits its output as an append batch
    the downstream accumulates. The event outcome is a downstream-facing contract chosen for downstream''s
    needs, not derived from persistence. Emit shapes form a cost/semantics ladder: replace = cheapest,
    a recompute signal → downstream recomputes; append = emit produced rows as a batch, no diff → downstream
    accumulates; delta = needs row identity (PK) and possibly a diff at emit if persistence did not already
    yield the changed rows → downstream applies precisely. Governing rule: event granularity <= (what
    persistence yields) + an optional emit-diff, whose cost — like the persistence cost — is a DESIGN
    CHOICE only the operator can make (dataset scale, incrementalizability, prior-state storage, recompute-vs-diff
    SLA); for very large datasets a diff/delta may be infeasible and the operator declares coarser shapes.
    The system NEVER infers a shape nor silently downgrades: a declared-but-infeasible outcome on either
    axis (emit=delta or persist=upsert without a PK, a diff exceeding a declared bound) is an EXPLICIT
    ERROR surfaced to the operator, never a silent fallback — that would hide churn/cost and break provability
    (REQ-964). A maintenance mode MAY supply defaults (periodic → append, live → replace) but the operator''s
    declarations are authoritative. Concretely, persist=replace (a limited, performance-sized hot/current
    table) + emit=append (a downstream history accumulator) keeps PIT DERIVABLE from RT and therefore
    INSIDE the NRT ideal (REQ-966) — no departure; the frozen, non-reconstructible snapshot is the separate
    departure (REQ-983).'
  use_case: 'An MV is a source to its dependents, and how it stores itself is separable from what it tells
    them: the operator declares a persistence outcome for its own storage economics and an event outcome
    for the downstream contract — enabling a forgetful view to feed a historical accumulator, or a full
    recompute to propagate as a precise delta — so incremental (or historical) change flows through the
    DAG exactly as the operator chose and can afford, or the estate errors explicitly rather than degrading
    silently.'
  code:
  - provisa/events/outcomes.py
  - provisa/events/handlers.py
  - provisa/events/processor.py
  - provisa/events/boot.py
  - provisa/events/app_wiring.py
  - provisa/federation/materialize_exec.py
  - provisa/federation/store_writer.py
  - provisa/core/change_signal.py
  - provisa/mv/models.py
  tests:
  - tests/unit/test_live_mv_outcomes.py
  scenario: 'Given an MV whose operator declares persistence=replace and event=append

    When it fires

    Then it overwrites its own store table with the current-state result

    And it emits an append event carrying that result as a batch

    And a downstream history MV appends each emission to accumulate a time series

    Given an MV whose operator declares persistence=replace and event=delta

    When it fires

    Then it recomputes the whole table and diffs new-vs-old once to emit only the changed rows

    Given an MV whose operator declares event=delta on a result with no primary key

    Then registration errors — delta needs row identity — and never silently emits replace

    Given a very large MV whose operator declares persistence=replace and event=replace

    Then each fire replaces the table and signals recompute, with no diff attempted

    '
  related_reqs:
  - REQ-932
  - REQ-957
  - REQ-958
  - REQ-964
  - REQ-983
- id: REQ-966
  status: complete
  since: 2026-07
  target: 2026-Q4
  group: 0. Architecture & Design Principles
  category: Data-Model-Driven Estate
  priority: MUST
  type: structural
  description: 'The zero-config default is the HAPPY PATH of an always-on, real-time enterprise, and it
    is the model''s canonical IDEAL: an MV needs only its SQL — lineage is auto-derived (REQ-939), fan-out
    auto-wired, failover (ownership CAS + reassert, REQ-959/REQ-960) always-on and config-free — and everything
    self-organizes into the DAG and reprocesses in near-real-time (event-driven, recompute-to-current,
    replace-persist, demand-emit). EVERY other primitive is PROGRESSIVE OPT-IN CONFIG that expresses a
    justified DEPARTURE from that ideal. Every departure traces to exactly TWO ROOT CAUSES, and config
    SHOULD be tagged with which: (1) RESOURCE constraint — "can''t afford NRT" — cost/scale/latency limits
    behind debounce (REQ-963), deadlines/batch (REQ-959), fact-gating (REQ-961), delta/incremental (REQ-965),
    and (at the storage level) even pre-cut rollups vs filtering the live stream (REQ-958/ REQ-962); (2)
    BUSINESS MANDATE — "the biz wants EOD reports and that''s the way it is" — period boundaries and history
    the organization requires (REQ-958/REQ-962/REQ-965). The two have OPPOSITE TRAJECTORIES: resource
    concessions are DEBT that retires as infrastructure improves (cheaper compute retires debounce, reliable
    sources retire deadlines, incremental engines make NRT-at-scale free). Business mandates stratify:
    ENDOGENOUS mandates — internal habits (EOD/monthly cadences) formed because real-time was infeasible
    — dissolve as NRT normalizes, because capability RESHAPES demand (free live output makes people stop
    asking for EOD); EXOGENOUS mandates — market close, filing deadlines, counterparty cutoffs — are imposed
    from outside and no compute or culture-shift retires them (the genuine floor). Defaults are designed,
    documented baselines, NOT silent fallbacks — the operator always knows the NRT default and departs
    explicitly. The why-tag makes the config surface a SELF-DESCRIBING LEDGER (itself queryable as a dataset):
    resource-tagged = retirement backlog / infra-ROI map ("what real-time maturity costs to buy"); endogenous-mandate
    = adapts as culture catches up; exogenous-mandate = the permanent business-requirement registry ("what
    you can never buy out of, and who owns it") — and it surfaces FOSSILIZED constraints (EOD because
    an overnight mainframe batch once required it) as retireable debt masquerading as mandate. But the
    knobs are PERMANENT for a reason bigger than any floor: business culture and technical capability
    are two independently-moving frontiers that NEVER FULLY SYNC — tech outruns adoption, biz outruns
    capability, and the gap is never zero, never static, and differs per dataset. So the config is not
    merely retireable compromise but the permanent ALIGNMENT LAYER coupling two co-evolving systems —
    which is WHY it must be declarative (biz expresses intent), granular / per-node (the gap differs per
    dataset), continuously retunable (the gap moves), and non- destructive (retune re-aligns without re-authoring
    process). Config measures distance from the ideal (sharpening REQ-964); the knobs keep intent and
    capability coupled while both move.'
  use_case: Onboarding is SQL-only and the platform degrades from the NRT ideal one justified, why-tagged
    config at a time — so the sum of an org's config is a queryable map of its distance from always-on
    real-time, split into retireable-with-investment vs permanent-business-truth, and reducing that distance
    is a matter of retiring resource-driven config as infrastructure allows (never re-authoring process)
    down to the irreducible mandate floor.
  code:
  - provisa/events/boot.py
  - provisa/events/lineage.py
  - provisa/events/processor.py
  tests:
  - tests/unit/test_live_selfdescribing.py
  scenario: Given an MV declared with ONLY its SQL (every other knob defaulted) When a source it reads
    changes Then lineage auto-derives, fan-out auto-wires, and the MV reprocesses in near-real-time (replace-persist,
    single-shape emit, real-time fire) with no configuration beyond its SQL
  related_reqs:
  - REQ-964
  - REQ-939
  - REQ-958
  - REQ-959
  - REQ-961
  - REQ-962
  - REQ-963
  - REQ-965
- id: REQ-967
  status: complete
  since: 2026-07
  target: 2026-Q4
  group: 0. Architecture & Design Principles
  category: Data-Model-Driven Estate
  priority: SHOULD
  type: structural
  description: 'The estate is SELF-DESCRIBING: because every change is an event and every derived dataset
    is an MV, the system''s OWN operational metadata is emergent — each operational concern is just an
    MV over streams the machinery already emits, with no bespoke subsystem. Catalog: DQ / quality = an
    MV over the warn/error emissions (REQ-957, REQ-941), append-keyed → a scorecard and quality history;
    HISTORY / SCD-2 = an MV over an append emission from a replace-persist upstream (REQ-965) → a time
    series, which also RETIRES proof-obligation #4 (REQ-964) — the SCD-2 dataset IS the point-in-time-correct
    dimension, so temporal joins become a declared dataset instead of an unenforced author burden (this
    reconstructible SCD-2 view is PIT inside the ideal; a frozen, non-reconstructible snapshot is the
    REQ-983 departure); FRESHNESS / VINTAGE = an MV over the freshness-contract state (REQ-961) → an SLA
    / as-of dashboard with queryable outage detection; LINEAGE / IMPACT = an MV over the SQL-derived DAG
    edges (REQ-939) → impact analysis; AUDIT / PROVENANCE = an MV over the durable event log (REQ-960
    queue) → a replayable operational trail; CHURN / COST = an MV over fire and debounce-collapse events
    (REQ-963) → a tuning dashboard; CONFIG MATURITY = an MV over why-tagged config (REQ-966) → the distance-from-NRT
    map. The meta-level (how the estate operates) folds into the object- level (governed MVs) — the estate
    observes itself with its own machinery, so quality, history, freshness, lineage, audit, and cost are
    first-class governed data, governed and lineage-tracked like any other dataset. This reflexivity is
    the COMPLETENESS TEST for the primitive set (you cannot reach outside it to describe it) and the strongest
    form of the REQ-964 thesis: a new operational capability is a DECLARATION — an MV over an existing
    stream — never a new code path or a separate DQ / history / audit subsystem.'
  use_case: Observability, quality, history, audit, lineage, and cost are not separate subsystems bolted
    on — they are declared MVs over the streams the platform already emits, so they inherit the same governance,
    lineage, and reprocessing guarantees as the business data, and new operational views are added by
    declaration rather than engineering.
  code:
  - provisa/events/handlers.py
  - provisa/events/processor.py
  tests:
  - tests/unit/test_live_selfdescribing.py
  scenario: Given the warn/error events the machinery emits When a DQ scorecard is declared as an MV over
    that stream Then quality is a query over the emissions — no bespoke DQ subsystem Given a replace-persist
    upstream that emits an append per fire When a history MV persists that append Then it accumulates
    each snapshot into an SCD-2 time series — no bespoke history subsystem
  related_reqs:
  - REQ-964
  - REQ-965
  - REQ-957
  - REQ-961
  - REQ-960
  - REQ-966
- id: REQ-968
  status: complete
  since: 2026-07
  target: 2026-Q4
  group: 9. Live Data & Events
  category: Derived / MV Processor
  priority: SHOULD
  type: behavioral
  description: 'Forced regen / replay recomputes a derived node — or an addressable calendar window (e.g.
    2026-Q1, REQ-962) — ON DEMAND, bypassing the token/no-op gate (REQ-958). Scope selection: by SOURCE
    (regen the roots and let lineage fan-out cascade forward — the natural, always-correct form), by MV
    NODE (regen a specific derived node without re-landing its sources — for a changed SQL def), or by
    WINDOW-ID (regen one sealed period). It posts synthetic events marked forced ({forced:true, reason,
    scope}); a forced event MUST bypass the no-op gate (it recomputes regardless of change) yet still
    runs preprocess (REQ-957) and honors the output outcomes (REQ-965). The forward cascade is the normal
    fan-out — dependents recompute in topological order. Idempotent land on the window key (REQ-960) makes
    replay exactly-once; determinism (REQ-964 obligation 1) + calendar-addressable windows (REQ-962) make
    a window regen EXACT — it recomputes precisely that period from its as-of inputs. Forced events are
    marked for audit (REQ-967) so a replay is distinguishable from an organic change.'
  use_case: Operators can restate a period, rebuild after a definition change, or recover from a bad load
    by declaring a scope to regenerate — sources, a node, or a window-id — and the estate replays deterministically
    and exactly-once through the existing DAG, with no bespoke backfill job.
  code:
  - provisa/events/processor.py
  - provisa/events/injector.py
  - provisa/events/queue.py
  - provisa/events/deadlines.py
  tests:
  - tests/unit/test_live_regen_snapshots.py
  scenario: 'Given a sealed daily window 2026-07-08 with a corrected upstream

    When an operator forces regen by window-id 2026-07-08

    Then that period recomputes exactly from its as-of inputs, bypassing the no-op gate

    And the forced event is marked in the audit stream

    Given a changed MV SQL definition

    When an operator forces regen by MV node

    Then the node recomputes without re-landing its sources and cascades to dependents

    '
  related_reqs:
  - REQ-958
  - REQ-962
  - REQ-960
  - REQ-965
  - REQ-967
- id: REQ-969
  status: complete
  since: 2026-07
  target: 2026-Q4
  group: 9. Live Data & Events
  category: Derived / MV Processor
  priority: MAY
  type: behavioral
  description: 'Incremental maintenance computes a derived node''s output from an upstream DELTA instead
    of a full recompute — the cost axis that makes emit=delta (REQ-965) worth declaring. When an input
    arrives as delta/append (REQ-932/REQ-965), an incrementally-maintained MV applies only the changed
    rows to its prior landed state (persist=upsert/append) and emits the resulting delta, rather than
    re-running the full SELECT. Feasibility is per-MV and OPERATOR-DECLARED: it is expressible only when
    the SQL admits an incremental form (many aggregations/joins do via delta rules; some do not) and a
    PK exists for row identity — a declared-but-infeasible incremental MV is an EXPLICIT ERROR, never
    a silent fall-back to full recompute (REQ-964; a silent fall-back hides cost). Realized either as
    author-provided incremental SQL or a system delta-rule derivation where the SQL shape permits. Recompute-to-current
    stays the DEFAULT (REQ-966 NRT baseline); incremental is the resource-concession config that lowers
    per-fire cost for large or hot derived nodes. Conflict to declare around: an incrementally-maintained
    MV cannot freely drop intermediate deltas the way recompute-to-current can, so it partially conflicts
    with debounce (REQ-963) — the operator chooses per node.'
  use_case: Large or high-churn derived nodes refresh by applying upstream deltas instead of full recomputation,
    making delta emission affordable and incremental propagation real — declared per MV where feasible,
    erroring explicitly where not, never silently degrading.
  code:
  - provisa/events/handlers.py
  - provisa/events/lineage.py
  - provisa/events/boot.py
  - provisa/federation/materialize_exec.py
  - provisa/mv/models.py
  tests:
  - tests/unit/test_live_regen_snapshots.py
  scenario: 'Given an incrementally-maintained MV over a fact table with a PK

    When the fact posts a delta of changed rows

    Then the MV applies only those rows to its prior state and emits a delta, with no full recompute

    Given an operator declares incremental on an MV whose SQL has no incremental form or no PK

    Then registration errors, and never silently falls back to full recompute

    '
  related_reqs:
  - REQ-965
  - REQ-963
  - REQ-964
  - REQ-932
- id: REQ-970
  status: complete
  since: 2026-07
  target: 2026-Q4
  group: 9. Live Data & Events
  category: Derived / MV Processor
  priority: SHOULD
  type: behavioral
  description: A derived node's store-table schema is DERIVED from its SQL SELECT (output column names
    + types), not taken from a source — the structural contrast with a replica, whose schema comes from
    its source (REQ-846). At registration the SELECT is analyzed (SQLGlot + type inference, or a describe
    against the federation engine) to produce the output columns; the MV store table is created/reconciled
    to that schema via the existing reconcile_table machinery (REQ-846). A definition change that drifts
    the output schema → RECREATE (drop + reland on next fire), symmetric to source-drift reconcile. A
    PK for the derived table — required for persist=upsert / emit=delta / incremental (REQ-965/REQ-969)
    — is operator-declared or inferred from an unambiguous GROUP BY / distinct key; required-but-absent
    is an explicit error, never a silent replace. The derived schema flows into lineage (REQ-939) so downstream
    MVs type-check their inputs against it.
  use_case: 'An MV needs only its SQL: its output schema, store table, and PK are derived and reconciled
    automatically and published into lineage so the DAG is type-checked end to end — no hand- authored
    DDL, symmetric to but distinct from a replica''s source-derived schema.'
  code:
  - provisa/federation/store_writer.py
  - provisa/events/lineage.py
  - provisa/events/app_wiring.py
  - provisa/compiler
  tests:
  - tests/unit/test_live_mv_outcomes.py
  scenario: 'Given an MV defined by a SELECT with computed columns

    When it is registered

    Then its output columns and types are derived from the SELECT and its store table is reconciled

    When the SELECT changes shape

    Then the store table is recreated and relanded on the next fire

    Given an MV declared persist=upsert with no derivable PK

    Then registration errors rather than silently landing a replace

    '
  related_reqs:
  - REQ-846
  - REQ-939
  - REQ-965
  - REQ-969
- id: REQ-971
  status: complete
  since: 2026-07
  target: 2026-Q4
  group: 6. Execution, Routing, Caching & Performance
  category: Data Masking & Expression Pushdown
  priority: MUST
  type: constraint
  description: Column-level data masking MUST be expressed as semantic SQL projection expressions within
    the governed IR so that masking participates in expression/projection pushdown exactly as RLS predicates
    participate in predicate pushdown. Where a connector can evaluate the mask expression at the source,
    the mask MUST push down so the raw, unmasked column value never crosses the wire into the middle tier.
    Any mask that does NOT push down to the source MUST be evaluated in a bounded-memory streaming stage
    — either engine-side (the fed engine computes the mask expression and streams results) or per-event/per-batch
    in the middle tier (as the subscription path already does via _mask_row over an async event stream).
    It is FORBIDDEN to materialize the full unmasked relation in the middle tier to mask it (no fetchall()-style
    buffer-then-mask). This streaming constraint ensures masking correctness never depends on fed-engine
    pushdown capability — a capability gap must never become a data leak — and bounds middle-tier memory
    to O(batch), not O(relation), minimizing the amount of cleartext resident in the tier at any instant.
  use_case: 'Masking today injects scalar SQL expressions (REGEXP_REPLACE, DATE_TRUNC, constant) into
    the governed SELECT projection after RLS injection. This works correctly but lacks first-class pushdown
    capability: the planner cannot reason about whether a mask pushed to the source, and no Capability
    flag tracks whether the source can evaluate the mask''s functions. Without projection pushdown, the
    raw cleartext value must transit to the engine even if the source could mask it — a confidentiality
    gap analogous to forcing RLS evaluation server-side when the source supports it. The design must model
    masking as a pushdown-capable expression so confidentiality-aware masking (including a governance
    guard that acknowledges non-pushable masks) becomes explicit in the planner. For masks that cannot
    push down, the fallback must evaluate them in a streaming stage to prevent the middle tier from buffering
    the full unmasked relation into memory — rationale: masking correctness must never depend on pushdown
    capability (a gap is never a data leak), and the middle tier must bound memory to O(batch), not O(relation),
    with minimal cleartext residence time.'
  code:
  - provisa/compiler/mask_inject.py
  - provisa/compiler/stage2.py
  - provisa/federation/connector.py
  - provisa/federation/promote.py
  - provisa/federation/cardinality.py
  - provisa/api/data/subscribe.py
  tests:
  - tests/unit/test_mask_pushdown.py
  scenario: 'Given a column with a masking rule that uses functions the source supports (e.g., DATE_TRUNC)

    When a query projects that column

    Then the planner detects the mask is pushable and delegates it to the source

    And the raw unmasked value does not cross the wire to the middle tier

    Given a column with a masking rule using a function the source does not support

    Then the planner accepts the mask will execute engine-side but logs/flags this as a confidentiality
    fallback

    Given a mask that cannot push down to the source

    Then the mask is evaluated in a streaming stage (engine-side or per-batch)

    And the full unmasked relation is never buffered in the middle tier

    Given a code path that would fetchall() an unmasked relation into the middle tier before masking

    Then that path is rejected as a violation of REQ-971

    '
  related_reqs:
  - REQ-087
  - REQ-088
  - REQ-089
  - REQ-090
  - REQ-091
  - REQ-263
  - REQ-264
  - REQ-826
  - REQ-842
  - REQ-673
  - REQ-875
- id: REQ-972
  status: complete
  superseded_by: REQ-989
  since: 2026-07
  group: 11. Platform, Infrastructure & Delivery
  category: Desktop Installation
  priority: MUST
  type: infrastructure
  description: 'Desktop installers default to a self-contained native install: DuckDB federation engine
    + sqlite control plane + in-memory (fakeredis) cache, with no Docker, VM, Trino, Redis, or MinIO.
    A `native` preset in config/capabilities.yaml declares this base tier.'
  use_case: Enables zero-dependency installation for lightweight desktop and demo scenarios without external
    infrastructure.
  target: 2026-Q3
  code:
  - config/capabilities.yaml
  - provisa/core/desktop_profile.py
  tests:
  - tests/unit/test_desktop_profile.py
- id: REQ-973
  status: complete
  since: 2026-07
  group: 11. Platform, Infrastructure & Delivery
  category: Desktop Installation
  priority: SHOULD
  type: infrastructure
  description: 'Federation engine is a wizard choice, not a fixed tier: DuckDB (native, default) | Trino-on-Docker
    | External engine (URL or host+port). Trino-on-Docker and external engines are opt-in overrides layered
    on the native base.'
  use_case: Allows users to upgrade from native DuckDB to Trino or an external federation engine without
    reinstalling the base system.
  target: 2026-Q3
  code:
  - provisa/core/desktop_profile.py
  - packaging/macos/first-launch.sh
  - packaging/windows/first-launch-native.ps1
  tests:
  - tests/unit/test_desktop_profile.py
- id: REQ-974
  status: complete
  since: 2026-07
  group: 11. Platform, Infrastructure & Delivery
  category: Desktop Installation
  priority: SHOULD
  type: infrastructure
  description: desktop_profile.load_profile gains engine/engine_url/materialize_url/trino_endpoint/otlp_endpoint
    overrides mapping to PROVISA_ENGINE / PROVISA_ENGINE_URL / PROVISA_MATERIALIZE_URL / TRINO_HOST+PORT
    / OTEL_EXPORTER_OTLP_ENDPOINT environment variables.
  use_case: Enables container orchestrators and deployment tools to override engine and observability
    endpoints at runtime without modifying the installer-created configuration.
  target: 2026-Q3
  code:
  - provisa/core/desktop_profile.py
  tests:
  - tests/unit/test_desktop_profile.py
- id: REQ-975
  status: complete
  since: 2026-07
  group: 11. Platform, Infrastructure & Delivery
  category: Desktop Installation
  priority: MUST
  type: infrastructure
  description: Observability is always-on built-in (self-telemetry in Admin). The optional Docker observability
    stack (otel-collector + prometheus + grafana) is an external demonstration, not an on/off switch;
    selecting it only redirects OTLP export to external infrastructure.
  use_case: Ensures telemetry is always collected and visible in Admin UI; external obs-integration is
    optional for enterprise monitoring without adding complexity to the base install.
  target: 2026-Q3
  code:
  - provisa/core/desktop_profile.py
  tests:
  - tests/unit/test_desktop_profile.py
- id: REQ-976
  status: complete
  since: 2026-07
  group: 11. Platform, Infrastructure & Delivery
  category: Desktop Installation
  priority: SHOULD
  type: constraint
  description: 'Docker and VMs are provisioned only when needed: engine == Trino-on-Docker, OR the obs-integration
    demo runs on Docker, OR the demo runs on Docker. Otherwise the install is fully native.'
  use_case: Minimizes resource footprint for users who do not require Trino or external observability,
    reducing memory and disk overhead on desktop systems.
  target: 2026-Q3
  code:
  - packaging/macos/first-launch.sh
  tests:
  - tests/unit/test_desktop_install.py
- id: REQ-977
  status: complete
  since: 2026-07
  group: 11. Platform, Infrastructure & Delivery
  category: Desktop Installation
  priority: SHOULD
  type: infrastructure
  description: 'All heavy add-ons (Trino image set, observability stack, demo) are downloadable and resolved
    local-first: installer-adjacent directory, then mounted volumes, then ~/Downloads, then GitHub release.
    Enterprise can pre-stage tarballs for airgapped installation.'
  use_case: Enables fully airgapped deployment by allowing enterprises to pre-stage container images and
    demo datasets alongside the installer.
  target: 2026-Q3
  code:
  - packaging/macos/first-launch.sh
  tests:
  - tests/unit/test_desktop_install.py
- id: REQ-978
  status: complete
  since: 2026-07
  group: 11. Platform, Infrastructure & Delivery
  category: Desktop Installation
  priority: SHOULD
  type: behavioral
  description: Installing the demo is optional and OFF by default. When installed, the launcher opens
    the UI at ?tour=1 to auto-start the guided tour.
  use_case: Separates the base product from demo data; guided tour auto-starts for new users who opt into
    the demo without manual navigation.
  target: 2026-Q3
  code:
  - packaging/macos/ProvisaLauncher/Sources/ProvisaLauncher/Views/Setup/SetupWizardView.swift
  - packaging/windows/provisa-native.ps1
  - packaging/macos/first-launch.sh
  tests:
  - tests/unit/test_desktop_install.py
  scenario: 'Given a fresh desktop installation without demo data

    When the user completes the wizard and launches the app

    Then the UI loads without demo sources

    And tour mode is not active

    Given a user selects "Install demo" during the wizard

    When the launcher starts the UI

    Then the UI loads with demo sources

    And tour mode is active at ?tour=1

    '
- id: REQ-979
  status: complete
  since: 2026-07
  group: 11. Platform, Infrastructure & Delivery
  category: Desktop Installation
  priority: MUST
  type: infrastructure
  description: The native tier builds a Python VENV at first launch from PyPI (pinned to release). The
    DMG/EXE bundles a bare python-build-standalone interpreter + a wheelhouse. First-launch creates ~/.provisa/venv
    and installs provisa[embedded] online from PyPI, or offline via pip --no-index --find-links from the
    bundled wheelhouse. The native tier runs with no Docker or container dependencies.
  use_case: Ensures the entire native-tier stack is self-contained and runs on any OS (macOS, Windows,
    Linux) without requiring external Python environments or container runtimes.
  target: 2026-Q3
  code:
  - packaging/macos/build-dmg.sh
  - packaging/windows/build-sfx.ps1
  - packaging/windows/first-launch-native.ps1
  - packaging/windows/provisa-native.ps1
  tests:
  - tests/unit/test_desktop_install.py
  - tests/unit/test_infra_requirements.py
  notes: Windows native builder (build-sfx.ps1) downloads python-build-standalone for x86_64-pc-windows-msvc,
    bundles it with a wheelhouse, and stages the built UI at a static path. first-launch-native.ps1 creates
    the VENV at %USERPROFILE%\.provisa\venv, installs from wheelhouse or PyPI, and provisa-native.ps1
    runs the two uvicorn processes (API factory + ui_server). The Windows base installer ships NO VirtualBox,
    OVA, or Trino; the container/compute tier (WSL2 + containerd, Trino image) is a separate on-demand
    download per REQ-889 (additive, reversible tier toggle).
- id: REQ-980
  status: complete
  since: 2026-07
  group: 5. Query Languages, Compilation & Operations
  category: Query Compilation
  priority: SHOULD
  type: structural
  description: Add `json` as a first-class IR type so native JSON in source columns preserves through
    to materialized stores as native JSON where supported, instead of collapsing to `text`. The write
    face maps IR `json` to SQLAlchemy's generic JSON (native on Postgres/MySQL/DuckDB, TEXT-affinity on
    SQLite via the dialect compiler); the land path parses a JSON column's serialized-text value into
    a Python object before insert so the SQLAlchemy JSON column does not double-encode; and the read path
    converts an already-parsed dict/list consistently with the string path. Composite/array types (row/array/list/struct/map)
    remain collapsed to `text`.
  use_case: Federation queries over JSON columns maintain type information and enable native JSON operations
    in target stores, rather than forcing serialization fallbacks.
  code:
  - provisa/core/ir_types.py
  - provisa/federation/materialize_exec.py
  - provisa/executor/serialize.py
  tests:
  - tests/unit/test_ir_types.py
  - tests/unit/test_materialize_landing.py
  related_reqs:
  - REQ-846
  - REQ-932
  - REQ-902
- id: REQ-981
  status: complete
  since: 2026-07
  group: 9. Live Data & Events
  category: Live Data & Events
  priority: MUST
  type: behavioral
  description: 'The event loop''s content-hash output gate — an always-on no-op ripple suppressor for
    replace-shaped landings. After a source-land or MV-generate produces its result, the node hashes the
    landed content (canonical, order-stable over the primary key) and compares it to the hash stored from
    the prior land. Unchanged → the node does NOT re-post its change event, so no downstream MV recompute
    cascades; the redundant store write is also skipped. Changed → land, persist the new hash, re-post.
    This makes a change_signal=ttl / probe_type=none poll (which always re-fetches) cheap on the DOWNSTREAM
    side: the full fetch still happens, but an unchanged result never ripples the DAG. Applies to replace
    shapes only (append/CDC deltas are new rows by definition and pass through). The hash is the correctness
    backstop that probe_type (REQ-981) optimizes the fetch in front of.'
  use_case: A TTL-polled source or MV that re-queries on cadence but whose content rarely changes no longer
    forces its entire dependent MV subtree to recompute on every tick — the DAG only ripples when landed
    content actually differs, eliminating wasted downstream work.
  target: 2026-Q3
  code:
  - provisa/events/processor.py
  - provisa/events/handlers.py
  - provisa/events/content_hash.py
  - provisa/events/queue.py
  - provisa/core/schema_org.py
  tests:
  - tests/unit/test_content_hash.py
  - tests/unit/test_live_change_gates.py
  - tests/unit/test_handlers.py
  related_reqs:
  - REQ-941
  - REQ-855
  - REQ-982
  scenario: 'Given a materialized source with change_signal=ttl and probe_type=none

    And its prior land stored a content hash H

    When the TTL poll fires and the re-fetched rows hash to H

    Then the node skips the store write

    And the node does not re-post its change event

    And no dependent MV recomputes

    Given the same source

    When the re-fetched rows hash to a value other than H

    Then the node lands the rows and persists the new hash

    And re-posts its change event to its dependents

    '
- id: REQ-982
  status: complete
  since: 2026-07
  group: 9. Live Data & Events
  category: Live Data & Events
  priority: MUST
  type: behavioral
  description: 'probe_type is the event loop''s input-side change-detection axis, orthogonal to the change_signal
    cadence (ttl | probe | ttl_probe). Values: watermark | hash | count | none. The type both selects
    the detection transport AND implies the landing shape: watermark -> append (fetch WHERE wm > cursor,
    needs watermark_column), hash/count/none -> replace. This REPLACES the prior "watermark_column presence
    implies append" heuristic in select_landing_shape with an explicit probe_type -> shape map. Availability
    is gated by a per-source capability matrix probe_capabilities(source_type): SQL/engine-scannable sources
    support {watermark, hash, count, none} (hash = full-scan checksum, expensive, degenerates to the REQ-980
    output hash); HTTP/API sources support {hash via ETag/Last-Modified, count via total-header, watermark
    if a sortable cursor, none}; file/object sources support {hash via mtime+size, none}; push/streaming
    sources are not on the probe axis. Config validation rejects a probe_type outside the source''s capability
    set; when change_signal in {probe, ttl_probe} and probe_type is unset the default resolves per class
    (SQL -> watermark if watermark_column else count; API/file -> hash). Each transport implements freshness_token(source,
    table) -> str | None; a None token degrades the node to TTL cadence (REQ-847 capability signal, never
    a silent stale fallback). The token baseline is persisted per node and compared through evaluate_freshness
    (REQ-855). The real probe factory replaces the always-changed _poll_probe_factory in the event-loop
    boot wiring.'
  use_case: An operator declares how each source detects change appropriate to its type — a watermark
    column for an append-only table, an ETag for an HTTP API, mtime for a file — so the loop skips the
    fetch entirely when the source is unchanged, rather than re-querying every cadence and relying only
    on the downstream hash gate.
  target: 2026-Q3
  code:
  - provisa/core/change_signal.py
  - provisa/core/models.py
  - provisa/events/injector.py
  - provisa/events/boot.py
  - provisa/events/app_wiring.py
  - provisa/events/probes.py
  - provisa/events/handlers.py
  - provisa/federation/residency.py
  - provisa/federation/store_writer.py
  - provisa/federation/freshness_gate.py
  tests:
  - tests/unit/test_probes.py
  - tests/unit/test_injector.py
  - tests/unit/test_probe_type_config_validation.py
  - tests/unit/test_live_change_gates.py
  - tests/mvmvp/test_mv_triggers_integration.py
  related_reqs:
  - REQ-941
  - REQ-855
  - REQ-847
  - REQ-929
  - REQ-932
  - REQ-981
  scenario: 'Given a postgresql source table with change_signal=probe and probe_type=watermark and watermark_column=updated_at

    When the poll fires and MAX(updated_at) exceeds the stored cursor

    Then the node posts an append event and lands only the rows past the cursor

    Given an openapi source with change_signal=probe and probe_type=hash

    When the poll fires and the endpoint returns the same ETag as stored

    Then the node does not fetch, land, or re-post

    Given a csv file source configured with probe_type=watermark

    When the config is parsed

    Then validation rejects it because file sources do not support the watermark probe type

    '
- id: REQ-983
  status: complete
  since: 2026-07
  target: 2026-Q4
  group: 9. Live Data & Events
  category: Derived / MV Processor
  priority: SHOULD
  type: behavioral
  description: 'A preserved snapshot is a point-in-time dataset that is MATERIALIZED-AND-SEALED because
    it is NOT reconstructible from current state plus retained event history — the one PIT form that is
    a genuine DEPARTURE from the NRT ideal (REQ-966), as opposed to the reconstructible PIT of REQ-958/REQ-965/REQ-967
    (accumulating append + read-time filter) which stays inside the ideal. It is DECLARED, never inferred,
    and MUST be why-tagged with its REQ-966 root cause: RESOURCE — the deriving history was never retained
    or the source cannot be replayed (debt that retires as retention/replay capability improves); or BUSINESS
    MANDATE — the output must be frozen exactly as computed at a moment (a definition or upstream changed
    and the prior result must be preserved as-is; an external filing/attestation fixes the number). The
    snapshot is landed as an IMMUTABLE, addressable partition (a REQ-962 window-id or an explicit snapshot-id),
    sealed like a calendar window: never rewritten, corrected only by a new partition, replay-exact via
    determinism (REQ-964) where the inputs still exist, and otherwise preserved verbatim because re-derivation
    is impossible. Distinct from REQ-958''s accumulating-append history (a reconstructible view over RT)
    and from a live replace-persist hot table (REQ-965): a preserved snapshot is chosen precisely when
    derive-from-RT is NOT available. Never a silent fallback — the operator declares it with its root
    cause, or the estate serves PIT as a view.'
  use_case: Real-world estates cannot always retain full event history or replay every source, and some
    outputs must be frozen as-computed for audit/attestation — so the platform offers PIT datasets built
    and preserved at a moment as an explicit, why-tagged departure, keeping derivable PIT the default
    and making every frozen snapshot a visible entry in the distance-from-NRT ledger (REQ-966).
  scenario: 'Given an MV whose upstream event history is not retained

    When an operator needs its state as-of a past moment

    Then a preserved snapshot must be declared with a why-tag (resource: history-not-retained) and landed
    as an immutable, addressable partition

    And it is never rewritten, only superseded by a new partition

    When the deriving history IS retained instead

    Then PIT is served as a read-time view over the accumulating MV (REQ-958) and no preserved snapshot
    is declared

    '
  code:
  - provisa/events/snapshots.py
  - provisa/core/schema_org.py
  - provisa/federation/materialize_exec.py
  tests:
  - tests/unit/test_live_regen_snapshots.py
  competitive_position:
    status: ahead
    rationale: dbt/Airflow snapshots are imperative jobs with no distinction between derivable and frozen
      point-in-time data; Provisa makes the frozen case an explicit, why-tagged departure that self-documents
      as retireable-vs-permanent in the distance-from-NRT ledger.
  related_reqs:
  - REQ-966
  - REQ-958
  - REQ-965
  - REQ-967
  - REQ-962
  - REQ-968
  - REQ-964
- id: REQ-984
  status: complete
  since: 2026-07
  group: 6. Execution, Routing, Caching & Performance
  category: Materialized View Lifecycle
  priority: SHOULD
  type: structural
  description: The legacy periodic CTAS materialized-view refresh loop is retired in favor of the event
    loop as the sole MV compute path. The refresh_loop function is replaced by reclamation_loop, which
    handles only storage reclamation (drop removed MVs + reap orphaned tables per REQ-234); periodic refresh
    cadence is now driven by each MV's event-loop poll job, reframing periodic refresh as the REQ-963
    debounce deadline source.
  use_case: Consolidating MV compute into a single event-driven path eliminates double-computation and
    simplifies the refresh lifecycle while preserving operator-triggered refresh mutations.
  code:
  - provisa/mv/refresh.py
  - provisa/api/app_startup.py
  - provisa/events/boot.py
  related_reqs:
  - REQ-234
  - REQ-135
  - REQ-966
  - REQ-963
  tests:
  - tests/unit/test_mv_reclamation_loop.py
- id: REQ-985
  status: complete
  since: 2026-07
  group: 6. Execution, Routing, Caching & Performance
  category: Cache
  priority: SHOULD
  type: structural
  description: 'When `cache.enabled` is omitted from config, caching defaults to ENABLED. A CacheStore
    always exists: `RedisCacheStore(None)` falls back to embedded fakeredis when no Redis URL is configured.
    Operators must explicitly set `cache.enabled: false` to disable caching via `NoopCacheStore`.'
  use_case: Operators have safe defaults where caching is active unless explicitly disabled, eliminating
    silent no-cache states and ensuring consistent cache behavior across hot tables and response caches.
  code:
  - provisa/api/app.py
  - provisa/cache/hot_tables.py
  related_reqs:
  - REQ-829
  - REQ-544
  tests:
  - tests/unit/test_cache_default_enabled.py
- id: REQ-986
  status: complete
  since: 2026-07
  scenario: 'Given the ClickHouse federation engine advertising ARROW and ARROW_STREAM

    When a query executes through the Provisa Arrow Flight server via run_arrow

    Then columnar data is returned as an Arrow table without row materialization


    When the same query executes via run_arrow_stream

    Then it yields Arrow record batches lazily

    '
  tests:
  - tests/unit/test_engine_capabilities.py
  - tests/unit/test_native_arrow_transport.py
  - tests/integration/test_clickhouse_runtime_e2e.py
  - tests/integration/test_arrow_flight_integration.py
  group: 6. Execution, Routing, Caching & Performance
  category: Arrow Flight Transport
  priority: MUST
  type: behavioral
  description: ClickHouse federation engine must expose Arrow-native transport through the Provisa Arrow
    Flight server to honor its declared ARROW and ARROW_STREAM capabilities, enabling columnar data streaming
    without row materialization.
  use_case: ClickHouse can deliver Arrow data natively over HTTP and native TCP protocols, but currently
    Provisa collapses these to row tuples. Exposing this capability via Arrow Flight matches Trino's architecture
    and enables efficient columnar streaming for high-throughput federated queries.
  code:
  - provisa/federation/engine.py
  - provisa/federation/backend.py
  - provisa/federation/clickhouse_runtime.py
  - provisa/api/flight/server.py
  target: 2026-Q4
  integration_test: required
  competitive_position:
    status: parity
    rationale: Trino natively streams Arrow over Flight; ClickHouse as a standalone system does not expose
      Arrow Flight. Once implemented, Provisa matches Trino's federated Arrow capability, providing parity
      with the industry standard for columnar transport in federated query engines.
- id: REQ-987
  status: complete
  since: 2026-07
  scenario: 'Given the Databricks federation engine advertising ROWS/ARROW/ARROW_STREAM

    When a batch at or above COPY_INTO_ROW_THRESHOLD is landed into the store

    Then the batch is staged as Parquet and ingested via COPY INTO, never a per-row INSERT loop


    Given a batch below the threshold

    When it is landed

    Then a single multi-row INSERT is used, never row-by-row

    '
  tests:
  - tests/unit/test_databricks_store.py
  - tests/unit/test_engine_capabilities.py
  - tests/integration/test_databricks_federation_engine_e2e.py
  - tests/integration/test_databricks_external_link_e2e.py
  group: 6. Execution, Routing, Caching & Performance
  category: Arrow Flight Transport
  priority: MUST
  type: behavioral
  description: 'Databricks SQL warehouse must be supported as a first-class Provisa federation engine
    with Arrow-native transport capabilities for both READ and WRITE paths. Read-side: Arrow columnar
    data streaming through the Provisa Arrow Flight server via execute_arrow/execute_stream. Write-side:
    materialization-store landing, residency reconciliation, and CDC snapshot seed must use Arrow-native
    columnar bulk writes (via ADBC adbc_ingest() or Parquet -> cloud object stage -> COPY INTO / Delta
    register), never row-oriented INSERT. End-to-end columnar path avoids row materialization overhead
    in Python and enables Databricks'' distributed ingest parallelization. Row INSERT acceptable only
    for tiny writes where per-file staging overhead dominates.'
  use_case: Organizations using Databricks SQL warehouses require direct federation capability to Provisa
    without intermediary routing through Trino. Arrow-native transport for both read and write paths avoids
    row materialization overhead and matches Trino's architecture for efficient federated query execution
    over Databricks. Materialized views and landed federation outputs must remain columnar end-to-end,
    enabling downstream queries to execute at warehouse speed.
  code:
  - provisa/federation/engine.py
  - provisa/federation/backend.py
  - provisa/federation/store_writer.py
  - provisa/api/flight/server.py
  - provisa/transpiler/transpile.py
  target: 2026-Q4
  integration_test: required
  validation_notes: 'Databricks live-tested successfully on Serverless SQL Warehouse with ARROW_STREAM
    via Cloud Fetch EXTERNAL_LINKS, parsed by pyarrow. Python databricks-sql-connector / urllib fail on
    TLS in dev environment due to TLS-intercepting proxy; CA cert is in macOS keychain (curl trusts it)
    but not in certifi. Workaround: point REQUESTS_CA_BUNDLE / SSL_CERT_FILE at bundle including proxy
    CA. This is an environment configuration detail, not a Databricks platform limitation.'
  competitive_position:
    status: parity
    rationale: Trino natively connects to Databricks as a source. Provisa with Databricks as a first-class
      federation engine (like Trino, ClickHouse, DuckDB) achieves parity with Trino's federation reach
      and enables customers to develop on DuckDB locally while scaling to Databricks warehouses in production.
      Columnar write path via ADBC/Parquet matches best practice (avoids Python row materialization).
  related_reqs:
  - REQ-986
  - REQ-988
- id: REQ-988
  status: complete
  since: 2026-07
  scenario: 'Given Snowflake promoted to a first-class federation engine peer to Trino/DuckDB/ClickHouse

    When it declares capabilities

    Then ROWS, ARROW, and ARROW_STREAM are advertised


    Given a single-source query targeting Snowflake

    When physical SQL is transpiled

    Then it targets the Snowflake dialect and routes directly with no Trino detour

    '
  tests:
  - tests/unit/test_engine_capabilities.py
  - tests/unit/test_transpiler.py
  - tests/integration/test_snowflake_federation_engine_e2e.py
  target: 2026-Q4
  group: 6. Execution, Routing, Caching & Performance
  category: Arrow Flight Transport
  priority: MUST
  type: behavioral
  description: Snowflake federation engine must be promoted from a source-only connector (federated through
    Trino) to a first-class Provisa federation engine peer to Trino/DuckDB/ClickHouse/Postgres, with Arrow-native
    transport wired through the Provisa Arrow Flight server. Engine declares ROWS, ARROW, and ARROW_STREAM
    capabilities; backend implements execute_arrow() and execute_stream() to deliver Arrow columnar data
    and lazy record-batch streaming without row materialization. Physical SQL transpilation must target
    Snowflake dialect. Governance (RLS, masking, rate-limiting) applies unchanged above the backend.
  use_case: Snowflake is an MPP warehouse with native Arrow support; exposing its columnar capabilities
    via Provisa's Arrow Flight server enables direct, efficient federated queries without row materialization
    and eliminates the current Trino-as-middleman routing. Matches architectural parity with Trino (REQ-986
    ClickHouse parallel) and serves enterprises whose primary warehouse is Snowflake.
  code:
  - provisa/federation/engine.py
  - provisa/federation/backend.py
  - provisa/api/flight/server.py
  - provisa/executor/trino_flight.py
  - provisa/transpiler/transpile.py
  - provisa/source_registry.py
  - provisa/source_connectors/snowflake_connector.py
  integration_test: required
  competitive_position:
    status: ahead
    rationale: Snowflake natively supports Arrow data export (fetch_arrow_all / fetch_arrow_batches);
      Provisa will be the first to expose this capability via Arrow Flight as a federated engine, matching
      industry best practice (Trino). Competitors typically access Snowflake as a read-only source through
      ODBC or REST, not as a columnar-streaming federation peer.
  related_reqs:
  - REQ-986
  - REQ-987
- id: REQ-989
  status: complete
  tests:
  - tests/unit/test_duckdb_store_native.py
  - tests/unit/test_federation_engine.py
  since: 2026-07
  target: 2026-Q3
  group: 11. Platform, Infrastructure & Delivery
  category: Platform Defaults
  priority: MUST
  type: structural
  description: 'Zero-config default stack must be fully embedded and in-process with no external dependencies:
    default federation engine is duckdb (not trino), default materialize store is duckdb''s native embedded
    store (not platform tenant DB via Postgres), and default control-plane store is sqlite (not embedded_pg).
    Desktop installers default to this stack via the `native` preset declared in config/capabilities.yaml
    (absorbs REQ-972). External engines and stores (Trino, Postgres, ClickHouse, Snowflake, Databricks)
    remain selectable via PROVISA_ENGINE / PROVISA_ENGINE_URL / PROVISA_MATERIALIZE_URL / control_plane_store
    overrides.'
  use_case: Organizations and developers need instant local startup with zero Docker, no Trino cluster,
    no Postgres dependency — everything embedded for true out-of-the-box instant start. The zero-config
    path must serve as an approachable entry point while preserving full enterprise engine/store pluggability
    via configuration.
  code:
  - provisa/federation/engine.py
  - provisa/core/desktop_profile.py
  - provisa/core/capabilities.yaml
  e2e: true
  e2e_reason: Default engine and store behavior requires a full integration round-trip to confirm duckdb
    is selected, materialization occurs in-process, and control-plane persists to sqlite without external
    services.
  related_reqs:
  - REQ-986
  - REQ-987
  - REQ-988
- id: REQ-990
  status: complete
  since: 2026-07
  scenario: 'Given a materialization target declaring bulk-COPY/columnar ingest support

    When a batch at or above the bulk threshold is landed

    Then the bulk/columnar ingest path is used, never row-by-row INSERT


    Given a target without bulk support or a tiny write

    When the batch is landed

    Then row INSERT is used, capability-gated and explicit, never a silent fallback

    '
  tests:
  - tests/unit/test_store_writer.py
  - tests/unit/test_databricks_store.py
  group: 6. Execution, Routing, Caching & Performance
  category: Materialization Writes
  priority: MUST
  type: behavioral
  description: Materialization-store writes MUST use the target store's columnar or bulk-COPY ingest path
    wherever available, instead of row-by-row INSERT. Row INSERT is permitted only for targets without
    bulk support or tiny writes where staging overhead dominates, and this choice must be explicit/capability-gated,
    never a silent fallback.
  use_case: Row-by-row INSERT serializes writes, defeats vectorization, and forces Python row tuples through
    the network. Bulk columnar paths (Arrow, COPY, adbc_ingest) maintain end-to-end columnar efficiency,
    reduce network overhead, and leverage target-store parallelization. This is especially critical for
    high-volume materialization, residency reconciliation, and CDC snapshot seeding.
  code:
  - provisa/federation/store_writer.py
  - provisa/federation/store_connection.py
  - provisa/federation/backend.py
  target: 2026-Q4
  related_reqs:
  - REQ-986
  - REQ-987
  - REQ-988
  - REQ-989
- id: REQ-991
  status: complete
  since: 2026-07
  group: 9. Live Data & Events
  category: Live Delivery Strategy
  priority: MUST
  type: constraint
  description: 'Live Delivery mechanism (push repeat, append poll, or full-replace poll) is DERIVED SOLELY
    from the effective change_signal plus watermark column presence. The Live Delivery UI exposes no independent
    Strategy selector; the delivery mechanism mirrors change_signal.select_landing_shape: push signal
    → repeat the change stream; poll signal WITH watermark → append; poll signal WITHOUT watermark → full-replace.'
  use_case: Eliminates redundant strategy selection across detection method and delivery mode. Centralizes
    authoritative control in Change Signal, reducing cognitive load and preventing inconsistent strategy/detection
    pairs. The watermark presence is the determining factor for append vs. replace, not a separate configuration.
  code:
  - provisa/live/engine.py
  - provisa/live/reconcile.py
  - provisa-ui/src/pages/tables/LiveDeliveryFieldset.tsx
  tests:
  - tests/unit/test_live_engine.py
  - tests/unit/test_live_engine_full.py
  - tests/unit/test_live_delivery_config.py
  related_reqs:
  - REQ-932
- id: REQ-992
  status: complete
  since: 2026-07
  group: 9. Live Data & Events
  category: Live Delivery Strategy
  priority: MUST
  type: behavioral
  description: 'Poll signals without a watermark column execute a full-replace poll mode: LiveEngine._poll_replace
    re-scans the full result set at each interval and emits a replace-mode LiveSpec only when an order-independent
    content hash (stored per output in live_query_state) changes. Unchanged scan results do not emit a
    snapshot, suppressing ripples in small or non-monotonic tables.'
  use_case: Non-monotonic or small tables scanned via poll (no watermark) need refresh semantics without
    the churn of repeated identical snapshots. Content-hash-based delivery suppresses ripples while ensuring
    consistency after real data changes.
  code:
  - provisa/live/engine.py
  - provisa/live/reconcile.py
  scenario: 'Given a poll-signal table with no watermark column and 100 rows

    When the full table is re-scanned and the content hash matches the previous scan

    Then no snapshot is delivered to downstream subscribers


    When the content hash differs (row added, deleted, or modified)

    Then a replace snapshot is delivered

    '
  tests:
  - tests/unit/test_content_hash.py
  - tests/unit/test_live_engine.py
  - tests/unit/test_live_engine_full.py
  related_reqs:
  - REQ-932
  - REQ-991
- id: REQ-993
  status: complete
  since: 2026-07
  group: 9. Live Data & Events
  category: Live Delivery Strategy
  priority: MUST
  type: constraint
  description: The content hash used for full-replace ripple suppression is order-independent (e.g., XOR
    or sorted multiset hash), stored per-output in live_query_state, and is recomputed on each full-table
    re-scan. A change in hash always triggers a replace snapshot delivery; an unchanged hash always suppresses
    delivery.
  use_case: Order-independent hashing ensures that logical content changes (row addition/deletion) trigger
    snapshots even if rows appear in different order, while avoiding spurious ripples when only row order
    differs. Persistent storage in live_query_state allows next interval's scan to detect real changes.
  code:
  - provisa/live/engine.py
  tests:
  - tests/unit/test_content_hash.py
  related_reqs:
  - REQ-932
  - REQ-991
  - REQ-992
- id: REQ-994
  status: complete
  since: 2026-07
  group: 4. Source Connectors
  category: Data Sources
  priority: SHOULD
  type: structural
  description: Trino/Presto clusters are read as first-class remote SOURCES via SQLAlchemy's trino dialect
    and landed as REPLICA tables, reaching replication parity with GraphQL, REST, and gRPC sources. Source
    classification is by read mechanism, not data-at-rest residency; Trino qualifies as both a source
    and a federation engine, mirroring the dual roles of Snowflake, Databricks, and BigQuery.
  use_case: 'Users need to query live Trino coordinators without deploying a separate federation engine.
    Trino-as-source unifies the source registration surface: a remote Trino cluster lands via the standard
    replication path, identical to web-service sources, enabling analytics workloads to federate without
    ETL.'
  code:
  - provisa/executor/drivers/registry.py
  - provisa-ui/src/pages/sources/constants.ts
  tests:
  - tests/unit/test_trino_source_driver.py
- id: REQ-995
  status: complete
  tests:
  - tests/unit/test_mssql_warehouse_connectors.py
  since: 2026-07
  group: 4. Source Connectors
  category: Federation Engine Capabilities
  priority: SHOULD
  type: behavioral
  description: Microsoft Fabric federation engine attaches Apache Iceberg data LIVE via OneLake Delta
    metadata virtualization (OPENROWSET FORMAT='DELTA' over virtualized/shortcut paths), extending live-attach
    reach from {parquet, csv, delta_lake} to include iceberg. Azure Synapse serverless lacks OneLake virtualization,
    so Iceberg tables are NOT attachable there and must land as REPLICA materialization.
  use_case: 'Fabric''s OneLake Delta virtualization layer enables direct live queries over Iceberg tables
    without requiring separate Iceberg compute. Synapse''s absence of this virtualization necessitates
    materializing Iceberg data as REPLICA, preserving correctness while leveraging different engine strengths:
    Fabric for live lakehouse queries, Synapse for post-materialization analytics.'
  code:
  - provisa/federation/mssql_warehouse_connectors.py
  scenario: 'Given an Iceberg table registered in OneLake on Microsoft Fabric

    When a live attachment is configured for Fabric engine with _FABRIC_ONLY_FORMAT constraint

    Then the table is queryable LIVE via OPENROWSET FORMAT=''DELTA'' without materialization


    Given the same Iceberg table targeted for Azure Synapse serverless

    When attachment is attempted without OneLake virtualization

    Then the table is materialized as a REPLICA instead of attached live

    '
- id: REQ-996
  status: complete
  since: 2026-07
  target: 2026-Q3
  group: 6. Execution, Routing, Caching & Performance
  category: Physical Data Transfer
  priority: MUST
  type: behavioral
  description: Support one-time physical data move expressed as standard SQL CREATE TABLE {schema}.{table}
    AS SELECT ... FROM {schema2}.{table2}. This physically creates and populates a table in a writable
    source. It does NOT create or register a Provisa model entity (no Domain, no Table model, no creation_request)
    — the new table is invisible to the federated model until separately introspected/registered.
  use_case: 'High-speed cross-data-source bulk copy and migration: users express a one-time physical move
    as a single SQL statement over the federated namespace without external ETL orchestrator, per-source
    connector code, or intermediate file staging. The CTAS operates at two speed tiers: same-engine zero-copy
    native CTAS pushdown, and cross-engine bulk-load via the landing face bounded by target bulk-insert
    driver performance. No semantic model registration required. Primary derived pattern: snapshot/archive
    via scheduled CTAS with date templating (e.g., CREATE TABLE archive.orders_{{yyyymmdd}} AS SELECT
    * FROM live.orders on cron). Each run produces distinct table name, so global schema.table uniqueness
    (REQ-998) holds automatically with no collision/overwrite; snapshots remain unregistered, ideal for
    time-windowed archives.'
  code:
  - provisa/executor/ctas.py
  - provisa/executor/writable.py
  - provisa/federation/store_writer.py
  - provisa/pgwire/server.py
  tests:
  - tests/unit/test_ctas.py
  scenario: 'Given a federated view SELECT col1, col2 FROM schema_a.table_a

    When a user executes CREATE TABLE schema_b.new_table AS SELECT col1, col2 FROM schema_a.table_a

    Then a physical table is created in the source owning schema_b

    And the new table does not appear in the Domain or Table model until explicitly registered

    '
  competitive_position:
    status: ahead
    rationale: Trino and Snowflake support CTAS within a catalog. Provisa's differentiator is catalog-free
      globally-unique schema.table addressing combined with physical cross-source move independent of
      semantic model registration, enabling users to manage physical data layout outside the federated
      schema.
- id: REQ-997
  status: complete
  since: 2026-07
  target: 2026-Q3
  group: 6. Execution, Routing, Caching & Performance
  category: Execution Routing
  priority: MUST
  type: behavioral
  description: 'Execution routing for CTAS: resolve the write path via existing resolve_write_path(target_source_type,
    engine). If source and target are on the same engine, emit engine-native CTAS pushed down (zero-copy).
    If cross-engine, the engine runs the SELECT and rows are bulk-loaded into the target source via the
    landing write face (provisa/federation/store_writer.py) — the engine is never on the write path. Writes
    get no retry budget (provisa/executor/direct.py) to avoid duplicate side effects.'
  use_case: 'Routing strategy for high-speed bulk migration: same-engine CTAS achieves zero-copy pushdown;
    cross-engine routes through landing write face to isolate engine from write side effects. No retry
    budget prevents accidental duplicate table creation. This two-tier routing model is the performance
    guarantor for federation-wide bulk copy without intermediate staging.'
  code:
  - provisa/executor/ctas.py
  - provisa/executor/writable.py
  - provisa/federation/store_writer.py
  - provisa/executor/direct.py
  tests:
  - tests/unit/test_ctas.py
  scenario: 'Given source_a on Snowflake and target schema on Snowflake

    When CREATE TABLE target_schema.new_table AS SELECT ... FROM source_a.table

    Then the entire CTAS is pushed to Snowflake engine (zero-copy)


    Given source_a on Snowflake and target schema on BigQuery

    When CREATE TABLE target_schema.new_table AS SELECT ... FROM source_a.table

    Then Snowflake executes the SELECT, lands rows in a temporary table

    Then rows are bulk-loaded to BigQuery via store_writer landing write face

    And the BigQuery engine is never on the write path

    '
- id: REQ-998
  status: complete
  since: 2026-07
  target: 2026-Q3
  group: 6. Execution, Routing, Caching & Performance
  category: Name Resolution
  priority: MUST
  type: constraint
  description: 'The load-bearing invariant: schema.table must be globally unique across the entire model.
    This is the language contract. A new CTAS target whose schema.table collides with an existing asset
    is rejected (name taken model-wide).'
  use_case: 'Provisa''s catalog-free addressing relies on schema.table as the sole global identifier.
    Allowing collisions would create ambiguity in later reads: schema.table could refer to multiple physical
    tables across different sources, breaking the semantic model.'
  code:
  - provisa/executor/ctas.py
  tests:
  - tests/unit/test_ctas.py
  scenario: 'Given existing table schema_a.users in source_a

    When CREATE TABLE schema_a.users AS SELECT ...

    Then the operation is rejected with "schema.table already exists in model"


    Given no existing table with name schema_a.new_table

    When CREATE TABLE schema_a.new_table AS SELECT ...

    Then the table is created and the operation succeeds

    '
- id: REQ-999
  status: complete
  since: 2026-07
  target: 2026-Q3
  group: 6. Execution, Routing, Caching & Performance
  category: Name Resolution
  priority: MUST
  type: constraint
  description: Catalog is optional in the grammar and, when present, always names the source. It is never
    used for resolution when absent (schema.table uniqueness resolves it). When present, catalog MUST
    equal the source that schema.table resolves to — matches proceed (self-documenting), contradiction
    is an error, never an override. Grammar stays standard three-part catalog.schema.table so SQLGlot
    parses it natively (read placement off exp.Table.catalog); the model itself stays catalog-free.
  use_case: Catalog in the SQL statement provides optional documentation/validation of placement intent
    without overriding the model's catalog-free resolution. Explicit catalog allows users to self-document
    which source they are writing to; mismatch is an error, preventing accidental writes to wrong sources.
  code:
  - provisa/executor/ctas.py
  tests:
  - tests/unit/test_ctas.py
  scenario: 'Given schema_a maps to source_snowflake

    When CREATE TABLE snowflake.schema_a.new_table AS SELECT ...

    Then the catalog snowflake matches the resolved source

    And the table is created successfully (self-documenting, no override)


    When CREATE TABLE bigquery.schema_a.new_table AS SELECT ...

    Then the catalog bigquery contradicts the resolved source snowflake

    And the operation is rejected with "catalog mismatch"


    When CREATE TABLE schema_a.new_table AS SELECT ...

    Then no catalog is specified, schema.table uniqueness resolves placement

    And the table is created in source_snowflake

    '
- id: REQ-1000
  status: complete
  since: 2026-07
  target: 2026-Q3
  group: 6. Execution, Routing, Caching & Performance
  category: Name Resolution
  priority: MUST
  type: behavioral
  description: 'Placement disambiguation at CREATE only: a new unique schema.table normally lands in the
    single writable source its schema maps to. In the rare case a schema maps to more than one writable
    source, catalog becomes required to disambiguate placement; absent an unambiguous target the CREATE
    is rejected (unique-match-or-error). Never silently pick a target.'
  use_case: Schemas can map to multiple writable sources in a federation (e.g., a schema with replicas
    on two different engines). Explicit catalog requirement prevents silent mis-targeting while allowing
    valid operations when placement is unambiguous.
  code:
  - provisa/executor/ctas.py
  tests:
  - tests/unit/test_ctas.py
  scenario: 'Given schema_shared maps to sources [snowflake_prod, snowflake_staging]

    When CREATE TABLE schema_shared.new_table AS SELECT ...

    Then the operation is rejected with "schema maps to multiple writable sources; use catalog to disambiguate"


    When CREATE TABLE snowflake_prod.schema_shared.new_table AS SELECT ...

    Then the operation succeeds, table created in snowflake_prod


    Given schema_unique maps to single writable source snowflake_only

    When CREATE TABLE schema_unique.new_table AS SELECT ...

    Then the operation succeeds without requiring catalog (unambiguous placement)

    '
- id: REQ-1001
  status: complete
  since: 2026-07
  target: 2026-Q3
  group: 6. Execution, Routing, Caching & Performance
  category: Transaction Safety
  priority: SHOULD
  type: constraint
  description: 'Transactional safety for the move: create-temp → bulk-load → atomic swap so a mid-stream
    failure leaves no partial target table.'
  use_case: Long-running or unreliable bulk-load operations may fail partway through. Atomic swap ensures
    target table is either completely populated or not created at all, preventing partial or corrupted
    data.
  code:
  - provisa/federation/store_writer.py
  - provisa/executor/ctas.py
  tests:
  - tests/unit/test_ctas.py
  scenario: 'Given a cross-engine CTAS with large result set

    When rows are bulk-loading and an error occurs at 50% completion

    Then the temporary table is cleaned up and target table is not created

    And the operation fails atomically with no partial table left behind

    '
- id: REQ-1002
  status: complete
  since: 2026-07
  target: 2026-Q3
  group: 6. Execution, Routing, Caching & Performance
  category: Type Coercion
  priority: SHOULD
  type: behavioral
  description: 'Cross-engine result-schema → target DDL type coercion policy: the SELECT result schema
    must map to valid target-dialect DDL (handling engine types like ROW/array/DECIMAL scale). Reuse store_writer
    ensure_table where possible.'
  use_case: Source engines emit result schemas with types that may not be natively supported by target
    engines (e.g., Snowflake ROW types, complex arrays, variable-precision DECIMAL). Type coercion ensures
    that the target table DDL is valid and preserves logical semantics.
  code:
  - provisa/federation/store_writer.py
  - provisa/executor/ctas.py
  - provisa/core/ir_types.py
  tests:
  - tests/unit/test_ctas.py
  scenario: 'Given SELECT returns ARRAY<STRUCT<name STRING, value INT>> from Snowflake

    When creating target table in BigQuery

    Then complex type is coerced to BigQuery-native ARRAY<STRUCT<name STRING, value INT64>>


    Given SELECT returns DECIMAL(38,10) from source engine

    When creating target table with different DECIMAL scale limits

    Then type is coerced to target''s supported DECIMAL range

    '
- id: REQ-1003
  status: complete
  since: 2026-07
  code:
  - provisa/scheduler/jobs.py
  - provisa/scheduler/templating.py
  - provisa/core/models.py
  tests:
  - tests/unit/test_scheduled_sql.py
  group: 6. Execution, Routing, Caching & Performance
  category: Scheduled Execution
  priority: SHOULD
  type: behavioral
  description: Extend the APScheduler-backed ScheduledTrigger (provisa/core/models.py:640) to support
    execution of a user-supplied SQL statement on its cron schedule, not only webhook POST. When a trigger
    fires, the SQL statement executes against the federated engine. This enables scheduled CTAS snapshot/archive
    patterns (REQ-996).
  use_case: Scheduled CTAS (CREATE TABLE ... AS SELECT) is the primary derived pattern from one-time CTAS
    (REQ-996), enabling snapshot and archive tables that update on a regular schedule without requiring
    external ETL orchestrators or manual user intervention. Users express scheduled snapshots as declarative
    triggers whose SQL statements run on cron.
  scenario: 'Given a ScheduledTrigger with a SQL statement and a cron expression

    When the cron fires at the scheduled time

    Then the SQL statement is executed against the federated engine

    And the result (table creation, inserts, etc.) persists in the target source

    '
- id: REQ-1004
  status: complete
  since: 2026-07
  code:
  - provisa/scheduler/templating.py
  tests:
  - tests/unit/test_scheduled_sql.py
  group: 6. Execution, Routing, Caching & Performance
  category: Template Substitution
  priority: SHOULD
  type: behavioral
  description: Scheduled SQL statements support run-time substitution of date/timestamp tokens (e.g.,
    {{yyyymmdd}}, {{YYYY-MM-DD}}, {{iso8601}}, {{timestamp}}) into the statement text before execution.
    Each run substitutes tokens with that run's execution date/time, enabling scheduled CTAS to produce
    distinct date-varied table names (e.g., archive.orders_{{yyyymmdd}}) without collision or overwrite,
    preserving the global schema.table uniqueness invariant (REQ-998) automatically. Depends on REQ-1003.
  use_case: The snapshot/archive pattern requires each run to produce a uniquely-named table. Date tokens
    allow users to express target table names declaratively (e.g., archive.orders_{{yyyymmdd}}) so scheduled
    CTAS automatically creates archive.orders_20260712, archive.orders_20260713, etc. without manual collision
    detection or user-supplied name-generation logic. This is essential for autonomous time-windowed archives
    that preserve the model's global schema.table uniqueness (REQ-998) while remaining unregistered semantic
    entities.
  scenario: 'Given a ScheduledTrigger with scheduled CTAS statement: CREATE TABLE archive.orders_{{yyyymmdd}}
    AS SELECT * FROM live.orders

    When the cron fires on 2026-07-12

    Then the token {{yyyymmdd}} is substituted with 20260712 and the table archive.orders_20260712 is
    created


    When the cron fires again on 2026-07-13

    Then the token {{yyyymmdd}} is substituted with 20260713 and the table archive.orders_20260713 is
    created

    And both archive.orders_20260712 and archive.orders_20260713 exist in the model with distinct names

    '
- id: REQ-1005
  status: complete
  since: 2026-07
  target: 2026-Q3
  group: 11. Platform, Infrastructure & Delivery
  category: Desktop Installation
  priority: SHOULD
  type: infrastructure
  description: 'Windows native tier first-launch (after setup completes and before/after opening the UI)
    presents next-steps guidance telling the user that the federation engine (Trino), observability stack,
    and demo data pack are NOT part of the native tier and are added via separate layered installers:
    Container installer (Provisa-Container-*.exe → WSL2 + containerd + Trino), then Obs installer (requires
    container tier), then Demo installer (requires Core + Obs). Guidance includes how to initialize the
    federation engine by running the Container installer.'
  use_case: The native tier installer provides a standalone local query engine (core + duckdb) but ships
    no federation engine (Trino), observability stack, or demo data. Users who install native tier receive
    no indication of how to access these optional features and the app opens directly to the UI without
    guidance, creating confusion about what Provisa can do. This requirement ensures users understand
    the tier model and how to extend their installation.
  code:
  - packaging/windows/first-launch-native.ps1
  tests:
  - tests/unit/test_desktop_install.py
  related_reqs:
  - REQ-979
  - REQ-889
  - REQ-633
- id: REQ-1006
  status: complete
  since: 2026-07
  group: 11. Platform, Infrastructure & Delivery
  category: Infrastructure
  priority: MUST
  type: behavioral
  description: 'The SPA static server routing is deterministic and driven by the browser Sec-Fetch-Dest
    request header (never by a maintained path allowlist). Requests with Sec-Fetch-Dest: document or GET
    whose Accept contains text/html are top-level SPA navigation and receive index.html; all other requests
    (fetch/XHR/EventSource, iframe subresource, non-GET) are proxied to the API and their real HTTP status
    — including 404 — is surfaced.'
  use_case: The previous path-allowlist heuristic silently served index.html (HTTP 200 HTML) for any unlisted
    API path, causing JSON parse errors and UI hangs on "Loading...". Deterministic header-driven routing
    eliminates both failure classes and prevents silent fallbacks. It also enables SPA deep-link refreshes
    on client routes without collision with API prefix handlers like /admin.
  code:
  - provisa/ui_server.py
  tests:
  - provisa-ui/e2e/infrastructure.spec.ts
  - tests/e2e/test_health_checks.py
  - tests/unit/test_ui_server_routing.py
  scenario: 'Given a browser navigates directly to the SPA deep link /graph/node/42

    When the request includes Sec-Fetch-Dest: document

    Then index.html is served with HTTP 200


    Given the UI calls fetch(''/api/setup/status'')

    When the API endpoint is unlisted and returns 404

    Then the 404 status is proxied to the browser (never silently replaced with index.html)


    Given a browser makes a XHR request to /api/v1/query

    When the request lacks Sec-Fetch-Dest: document header

    Then the request is proxied to the API and its real HTTP status is returned

    '
  e2e: true
  e2e_reason: The Sec-Fetch-Dest routing decision is only verifiable through a full live-stack round-trip
    where a browser navigates the SPA and makes API calls; the 404 propagation behavior (without silent
    HTML fallback) cannot be verified through unit tests alone.
  related_reqs:
  - REQ-057
  - REQ-058
  - REQ-559
- id: REQ-1007
  status: complete
  tests:
  - tests/unit/test_desktop_install.py
  since: 2026-07
  group: 11. Platform, Infrastructure & Delivery
  category: Multi-Tier Deployment
  priority: MUST
  type: infrastructure
  description: Deployment choices — federation engine (DuckDB/Trino/external-SQLAlchemy), observability
    mode (none/Docker-incluster/collector), and demo install — are exposed uniformly across all deployment
    surfaces (macOS SwiftUI, Linux AppImage, install.sh, Windows native, Helm, Terraform) with environment-variable
    overrides and interactive prompts where applicable.
  use_case: Deployment parity ensures users can select the same configuration across desktop, server,
    container, and cloud deployments without relearning the choice model for each platform.
  code:
  - packaging/linux/first-launch.sh
  - packaging/macos/build-dmg.sh
  - packaging/windows/first-launch-native.ps1
  - packaging/windows/install-container.ps1
  - install.sh
  - helm/provisa/values.yaml
  - helm/provisa/templates/demo.yaml
  - terraform/aws/main.tf
  - terraform/azure/main.tf
  - terraform/gcp/main.tf
  - terraform/deploy.sh
  related_reqs:
  - REQ-972
  - REQ-973
  - REQ-974
  - REQ-975
  - REQ-976
  - REQ-977
  - REQ-978
  - REQ-979
- id: REQ-1008
  status: complete
  since: 2026-07
  group: 8. Client Access & Protocols
  category: MCP Server
  priority: SHOULD
  type: behavioral
  description: Provisa exposes a Model Context Protocol (MCP) server so external AI agents (Claude Desktop,
    claude.ai connectors, Claude Code) can discover catalog metadata and execute SQL through the same
    governed pipeline as every other client. The server is a protocol adapter with zero new governance
    logic — every SQL path routes through the existing choke point _govern_and_route(sql, role_id) in
    provisa/pgwire/_pipeline.py, and catalog reads reuse build_catalog_tables(state). It runs in-process
    in the FastAPI app (shared state) via the official mcp Python SDK (FastMCP). Two transports remote
    Streamable HTTP with OAuth (token→provisa role via existing pgwire OIDC) and local stdio with a pinned
    role for development. A role is REQUIRED on every call; the server never defaults to admin. Tools
    list_schemas, list_tables(schema), describe_table(schema,table), run_sql(sql) (row-limited/paged),
    explain_sql(sql) (plan-only), and search_catalog(nl_text). Structured drill-down (list_schemas → list_tables
    → describe_table) is deterministic over the meta views (registered_tables_meta, table_columns_meta)
    and needs no vector store. search_catalog is semantic bottom-up detail search using small-to-big retrieval
    embed narrow column-leaf chunks, match on the detail, then resolve up through deterministic drill-down
    to return the parent table branch (all columns + FKs) plus schema breadcrumb. Three chunk tiers schema
    (schema+desc+table names), table (table+desc+column names), column (column+desc+table+schema); all
    columns embedded. A single formatter get_chunk(schema, table?, column?) is the sole chunk-text contract
    address depth selects the tier (schema | schema+table | schema+table+column), column requires table,
    and it returns embed-text as labeled plain prose (NOT markdown — markdown chrome is token noise to
    the embedder; the pretty structured view is describe_table's job for a different consumer). A separate
    iter_entities() enumerator walks the meta views yielding addresses that drive get_chunk, keeping the
    formatter pure (address in, text out). The address IS the provenance the args (schema, table, column)
    equal the {level, schema_id, table_id, column_id} stored on the vector row, so a hit resolves straight
    back through get_chunk/describe_table with no separate chunk-id→entity mapping. Schema questions are
    never answered from stale chunk text. Because one formatter serves both initial index and incremental
    reindex, indexed and re-indexed text cannot drift. Vector store is DuckDB VSS, full stop — no second
    backend. The index runs in-process with the always-running Provisa server, alongside the meta views
    and pg_duckdb it derives from, so there is no external vector store to provision. DuckDB's HNSW index
    is memory-resident (disk persistence is still experimental), so it is treated as a server-lifetime
    artifact rebuilt/loaded at startup by the same walk-the-meta-views code path as incremental reindex
    (a cold build is just the full case). Sizing the index scales with schema cardinality, not row volume,
    and the catalog is curated, so it stays single-node across the range (fed ~10K, enterprise ~140K,
    largest curated+transaction ~1.4M column chunks) — well inside DuckDB VSS's range, which is why no
    pgvector/multi-million fallback is needed. Phase 2 (search_catalog semantic explore) is implemented
    with full rebuild-on-reload; incremental re-embed (keyed off MV/catalog refresh) remains a future
    optimization.
  use_case: Makes Provisa pluggable into Claude and other MCP-speaking agents for governed metadata discovery
    and SQL execution, without exposing a second ungoverned surface. Extends the governed-surface-for-AI-planners
    motivation of REQ-884 from "queryable telemetry" to a first-class agent-facing protocol. Bottom-up
    search answers detail-topic questions ("which dataset holds the SOFR reset rate") that structured
    top-down navigation cannot at enterprise catalog scale where the flat table list exceeds an agent's
    context window.
  code:
  - provisa/pgwire/_pipeline.py
  - provisa/api/flight/catalog.py
  - provisa/api/_meta_views.py
  - provisa/mv/refresh.py
  - provisa/api/mcp/search.py
  - provisa/api/mcp/tools.py
  - provisa/api/mcp/server.py
  tests:
  - tests/unit/test_mcp_server.py
  - tests/unit/test_mcp_search.py
  - tests/unit/test_mcp_status.py
  scenario: 'Given a Provisa instance whose curated catalog is indexed and an MCP client authenticated
    as role r with access to a subset of domains

    When the agent calls search_catalog("customer lifetime value") then describe_table on the top hit
    and finally run_sql("SELECT ... LIMIT 100") against that table

    Then search_catalog returns only entities in domains r may access, ranked by column-detail relevance
    with {schema,table,column} provenance, describe_table returns the authoritative table structure from
    the meta views, and run_sql executes through _govern_and_route under role r — enforcing the same domain
    access policy as pgwire and returning a row-limited result

    '
  related_reqs:
  - REQ-884
  - REQ-267
  - REQ-369
- id: REQ-1009
  status: complete
  since: 2026-07
  group: 10. UI & Admin Surfaces
  category: Component Library Standardization
  priority: MUST
  type: structural
  description: Standardize provisa-ui on the Mantine component library (MIT/OSS) as the single modern
    UI component system. Remove unused @mui/material and Emotion dependencies (dead deps, imported in
    0 source files).
  use_case: A single, well-maintained component library provides consistent UX, reduces bundle size, and
    simplifies maintenance. Mantine's hooks-first architecture aligns with React best practices and TypeScript
    integration.
  code:
  - provisa-ui/src/
  tests:
  - tests/unit/test_ui_modernization_requirements.py
- id: REQ-1010
  status: complete
  since: 2026-07
  group: 10. UI & Admin Surfaces
  category: Design System
  priority: SHOULD
  type: structural
  description: Establish a consistent visual design theme driven by CSS-variable design tokens, mapped
    into Mantine's theme. Primary color is indigo (#6366f1); existing App.css token set is the source
    of truth.
  use_case: CSS variables enable dynamic theming (light/dark), reduce redundancy, and provide a single
    point of truth for colors, spacing, and typography across the application.
  code:
  - provisa-ui/src/App.css
  tests:
  - tests/unit/test_ui_modernization_requirements.py
- id: REQ-1011
  status: complete
  since: 2026-07
  group: 10. UI & Admin Surfaces
  category: Theming
  priority: SHOULD
  type: behavioral
  description: Support both Dark and Light color schemes via Mantine's color-scheme system, with a user-facing
    theme toggle. Current state is dark-only.
  use_case: Light mode improves accessibility for users with certain vision conditions; user choice increases
    accessibility and user satisfaction.
  code:
  - provisa-ui/src/main.tsx
  - provisa-ui/src/theme/ColorSchemeToggle.tsx
  e2e: true
  e2e_reason: Theme toggle is a UI interaction that must be verified end-to-end to ensure the app correctly
    switches and persists the user's choice across page reloads.
  scenario: 'Given the app rendered with the Mantine color-scheme manager

    When the user activates the theme toggle

    Then the color scheme switches between dark and light and persists across reloads via localStorage

    '
  tests:
  - provisa-ui/e2e/
  - tests/unit/test_ui_modernization_requirements.py
- id: REQ-1012
  status: complete
  since: 2026-07
  group: 10. UI & Admin Surfaces
  category: Internationalization
  priority: SHOULD
  type: behavioral
  description: 'Internationalize the entire frontend: no hardcoded user-facing strings (~536 exist today).
    Use react-i18next with an ''en'' base catalog; include locale-aware date and number formatting. Pipeline
    must support adding additional locales.'
  use_case: i18n enables Provisa to serve global enterprises in their native languages, reducing adoption
    friction and supporting compliance requirements in non-English jurisdictions.
  code:
  - provisa-ui/src/main.tsx
  - provisa-ui/src/i18n/
  e2e: true
  e2e_reason: Locale switching, date/number formatting, and catalog consistency must be verified end-to-end
    with actual locale-aware rendering in the browser.
  scenario: 'Given the frontend wired with react-i18next and an en base catalog

    When a component renders user-facing text

    Then the text resolves from the i18n catalog (no hardcoded literals) with locale-aware date/number
    formatting

    '
  tests:
  - provisa-ui/e2e/
  - tests/unit/test_ui_modernization_requirements.py
- id: REQ-1013
  status: complete
  since: 2026-07
  group: 10. UI & Admin Surfaces
  category: Accessibility
  priority: MUST
  type: constraint
  description: 'Meet enterprise accessibility requirements: WCAG 2.1 Level AA across the app. Includes
    focus management and focus-trap in dialogs, full keyboard operability, correct ARIA roles/attributes
    (e.g. role="dialog"/aria-modal on the 65 modal usages that currently lack them), 4.5:1 text contrast
    in both color schemes, visible focus indicators, reduced-motion support, form label/error association,
    and live regions for async notifications.'
  use_case: WCAG 2.1 AA is required for enterprise and government adoption; accessibility is a legal and
    ethical requirement, not optional.
  e2e: false
  e2e_reason: Accessibility (keyboard navigation, focus trapping, ARIA/modal semantics) is guaranteed
    by the external component library (Mantine), whose components ship WCAG-compliant behaviour; the unit
    test asserts the library is adopted rather than re-verifying the library's own accessibility end-to-end.
  tests:
  - tests/unit/test_ui_modernization_requirements.py
- id: REQ-1014
  status: complete
  since: 2026-07
  group: 10. UI & Admin Surfaces
  category: Accessibility
  priority: MUST
  type: behavioral
  description: 'Accessibility is a tested, enforced requirement: add automated axe-core checks (@axe-core/playwright)
    into the Playwright e2e coverage fixture so every e2e run asserts zero accessibility violations.'
  use_case: Automated accessibility testing catches WCAG violations early, prevents regressions, and enforces
    compliance as a gate condition for test passage.
  code:
  - provisa-ui/e2e/coverage.ts
  tests:
  - provisa-ui/e2e/coverage.ts
  - tests/unit/test_ui_modernization_requirements.py
  e2e: true
  e2e_reason: Accessibility scanning must run against the real DOM tree rendered in the browser; static
    analysis cannot detect color contrast, focus order, or semantic issues.
  scenario: 'Given the Playwright e2e coverage fixture with @axe-core/playwright wired in

    When any e2e spec runs against a rendered page

    Then axe-core scans the DOM and the run asserts zero accessibility violations

    '
- id: REQ-1016
  status: complete
  since: 2026-07
  group: 10. UI & Admin Surfaces
  category: Testing
  priority: MUST
  type: constraint
  description: Migrate the provisa-ui test suite (27 Playwright e2e specs + 22 unit/component tests) in
    lockstep with component modernization (REQ-1009/REQ-1012/REQ-1014). Stop relying on hand-rolled CSS-class
    selectors (59 locator('.…') uses) and hardcoded literal UI text (58 text-based assertions). Adopt
    role-based selectors (getByRole) plus stable data-testid attributes on migrated components; text assertions
    must reference i18n catalog keys (t('key')) rather than literal strings. Migration done per-component
    in the same pass as that component's Mantine/i18n/a11y conversion, keeping the suite green incrementally.
  use_case: Mantine migration removes original classNames and i18n moves strings to catalogs, breaking
    CSS-class and hardcoded-text selectors. Role-based and data-testid strategies are robust to component
    library changes; i18n-key assertions reflect the requirements contract. Incremental per-component
    migration prevents big-bang test breakage and ensures continuous test coverage.
  code:
  - provisa-ui/e2e/
  - provisa-ui/src/
  tests:
  - provisa-ui/e2e/
  - tests/unit/test_ui_modernization_requirements.py
  relates_to:
  - REQ-1009
  - REQ-1012
  - REQ-1014
- id: REQ-1017
  status: proposed
  superseded_by: REQ-1271
  group: 11. Platform, Infrastructure & Delivery
  category: Self-Service Provisioning
  priority: MUST
  type: behavioral
  target: 2026-Q3
  description: Any authenticated Firebase user with no existing org membership may create an org via a
    non-superadmin endpoint; the creator is inserted into user_org_memberships as role 'admin'.
  use_case: Enables self-service SaaS adoption without platform admin involvement. Lifts the superadmin
    gate for the self-serve path only; superadmin endpoints in orgs_router remain for platform administration.
  code:
  - provisa/api/auth_router.py
  - provisa/api/orgs_router.py
  relates_to:
  - REQ-833
  - REQ-839
- id: REQ-1075
  status: complete
  group: 11. Platform, Infrastructure & Delivery
  category: SaaS Billing
  priority: MUST
  type: behavioral
  since: 2026-07
  description: 'SaaS billing is provided by Lemon Squeezy as Merchant of Record. `provisa/api/billing/`
    integrates over the Lemon Squeezy REST API (JSON:API) and signed webhooks. Checkout: POST /v1/checkouts
    and returns `checkout.data.attributes.url`; the tenant_id is carried in checkout `custom_data` and
    echoed back in webhook `meta.custom_data`. Webhook signature verification: HMAC-SHA256 over the RAW
    request body keyed by env `LEMONSQUEEZY_SIGNING_SECRET`, compared in constant time to the hex `X-Signature`
    header. Tenant→customer linkage stored as `tenants.ls_customer_id`. Plan lifecycle from `meta.event_name`:
    `subscription_created`/`subscription_updated` set plan+source_limit from the LS variant name (mapped
    via PLAN_LIMITS by `plan_from_variant`); `subscription_cancelled`/`subscription_expired` revert to
    the trial plan. Customer portal URL comes from the LS customer object `data.attributes.urls.customer_portal`.
    Env: `LEMONSQUEEZY_API_KEY`, `LEMONSQUEEZY_STORE_ID`, `LEMONSQUEEZY_SIGNING_SECRET` (and `LEMONSQUEEZY_BASE_URL`
    to override the endpoint in tests). An unrecognized variant name is a hard error, never a silent default.
    This replaces the prior Stripe integration. Subscription webhooks are idempotent and order-independent:
    Lemon Squeezy retries any delivery that did not answer 2xx and does not order retries against live
    traffic, so the same event arrives twice and an older one can arrive after a newer one -- a redelivered
    "active" landing after a cancellation would re-entitle a cancelled org. Each subscription event is
    claimed against `orgs.subscription_event_at` in a single UPDATE that both tests and stamps the watermark
    (applied only when strictly newer), so a duplicate or stale retry is a 200 no-op carrying `ignored:
    stale_event` rather than a 4xx, which Lemon Squeezy would retry until it disabled the webhook. A subscription
    payload with no parseable `updated_at` cannot be ordered and is rejected 400.'
  use_case: Lemon Squeezy MoR consolidates global sales-tax/VAT compliance, chargebacks, and fraud handling
    in one system Provisa does not build; per-tenant plan gating via source_limit maps to LS subscription
    variants.
  code:
  - .claude/commercial/provisa_commercial/router.py
  - .claude/commercial/provisa_commercial/org_db.py
  - .claude/commercial/provisa_commercial/schema.py
  tests:
  - tests/unit/test_billing_lemonsqueezy.py
  - .claude/commercial/tests/test_billing_webhook_idempotency.py
  scenario: 'Given a tenant initiates checkout with tenant_id in custom_data

    When POST /v1/checkouts is called to Lemon Squeezy API

    Then a checkout URL is returned; the webhook subsequently updates tenant.ls_customer_id and applies
    plan/source_limit from the subscription variant


    And Given a subscription_created webhook from Lemon Squeezy with valid HMAC-SHA256 signature

    When the webhook signature is verified against LEMONSQUEEZY_SIGNING_SECRET

    Then the tenant''s plan and source_limit are updated from the variant name; unrecognized variants
    reject the webhook

    '
- id: REQ-1076
  status: complete
  group: 11. Platform, Infrastructure & Delivery
  category: Federation Configuration
  priority: MUST
  type: structural
  since: 2026-07
  description: 'Databricks federation TLS trust is configurable via environment variables to enable the
    databricks-sql connector to work behind a TLS-intercepting proxy. Honored in precedence order: DATABRICKS_TLS_NO_VERIFY=1
    (disable verification, local dev only), DATABRICKS_TLS_CA_FILE (custom CA certificate), REQUESTS_CA_BUNDLE,
    SSL_CERT_FILE. If all are absent, connector keeps its default verified behavior. Values passed as
    _tls_trusted_ca_file and _tls_verify to dbsql.connect().'
  use_case: Organizations with TLS-intercepting proxies (compliance/security gateways) require the ability
    to supply custom certificate authorities or disable verification in development environments. Provisa
    must support this pattern to work in corporate proxy environments.
  code:
  - provisa/federation/databricks_tls.py
  - provisa/federation/databricks_runtime.py
  - provisa/executor/drivers/databricks.py
  tests:
  - tests/unit/test_warehouse_config.py
- id: REQ-1077
  status: complete
  group: 11. Platform, Infrastructure & Delivery
  category: Warehouse Configuration
  priority: SHOULD
  type: structural
  since: 2026-07
  description: MSSQL warehouse (Azure Synapse/Fabric) login timeout is configurable via PROVISA_MSSQL_LOGIN_TIMEOUT
    environment variable (seconds, default 120). Applied at both pyodbc connection sites (provisa/federation/mssql_warehouse_runtime.py
    and provisa/executor/drivers/mssql_warehouse.py) via the ConnectionTimeout keyword argument.
  use_case: Serverless SQL pools in Azure Synapse auto-pause and require wake-up time before accepting
    connections. A configurable timeout prevents false connection failures when the pool is resuming from
    auto-pause.
  code:
  - provisa/federation/mssql_warehouse_runtime.py
  - provisa/executor/drivers/mssql_warehouse.py
  tests:
  - tests/unit/test_warehouse_config.py
- id: REQ-1078
  status: complete
  group: 11. Platform, Infrastructure & Delivery
  category: Testing Infrastructure
  priority: SHOULD
  type: structural
  since: 2026-07
  description: The scripts/test-all runner auto-loads .env file before executing test lanes, with caller-exported
    variables taking precedence. This allows credential-gated warehouse test lanes (e.g., Databricks,
    Snowflake, MSSQL) to run instead of skipping.
  use_case: Enables CI/CD pipelines to load external warehouse credentials from .env (not checked in)
    and run full warehouse integration tests. Caller-exported variables can still override .env values
    for CI/CD systems that inject credentials via environment.
  code:
  - provisa/api/billing/router.py
  - provisa/api/billing/lemonsqueezy_client.py
  - provisa/api/billing/models.py
  - provisa/api/billing/tenant_db.py
  - provisa/core/schema_admin.py
  tests:
  - tests/unit/test_billing_lemonsqueezy.py
  - tests/unit/test_org_isolation.py
- id: REQ-1026
  status: proposed
  group: 11. Platform, Infrastructure & Delivery
  category: First-Customer Deployment
  priority: MUST
  type: infrastructure
  target: 2026-Q3
  description: 'First-customer deployment target is a single GCP Compute Engine VM (e2-standard-4: 4 vCPU
    / 16GB, trimmable to e2-standard-2 / 8GB) running the core docker-compose stack. GCP chosen for colocation
    with Firebase auth (single vendor/single bill) and one-time $300 free-tier credit.'
  use_case: Trino's ~8GB JVM footprint rules out per-service PaaS (Fly/Railway/Render). A raw VM is the
    cheapest usable deployment platform. GCP colocation with Firebase minimizes operational complexity
    and bill consolidation.
  relates_to:
  - REQ-854
  - REQ-121
- id: REQ-1027
  status: proposed
  group: 11. Platform, Infrastructure & Delivery
  category: First-Customer Deployment
  priority: MUST
  type: infrastructure
  target: 2026-Q3
  description: Object storage externalized to Cloudflare R2 (Trino exchange/spill and any object storage),
    replacing bundled MinIO for first-customer profile. R2 chosen for zero egress fees at low volume.
  use_case: MinIO adds operational overhead and state complexity. R2's zero-egress pricing aligns first-customer
    cost to actual usage.
  relates_to:
  - REQ-854
- id: REQ-1028
  status: proposed
  group: 11. Platform, Infrastructure & Delivery
  category: First-Customer Deployment
  priority: MUST
  type: infrastructure
  target: 2026-Q3
  description: 'Trimmed first-customer runtime profile: single-node Trino (coordinator-only, no separate
    trino-worker; heap reduced to 3-4GB), R2 for exchange, no MinIO. Deliverable: docker-compose.first-customer.yml.
    The trino-worker service remains defined in compose (unused) for later multi-node scaling without
    re-architecture.'
  use_case: Reduces memory footprint to fit e2-standard-2 (8GB). Preserves architecture for horizontal
    scaling when load justifies multi-node deployment.
  relates_to:
  - REQ-854
  - REQ-1027
- id: REQ-1029
  status: proposed
  group: 11. Platform, Infrastructure & Delivery
  category: First-Customer Deployment
  priority: MUST
  type: infrastructure
  target: 2026-Q3
  description: Postgres data persists on a separate GCP persistent disk, mounted into the VM, enabling
    VM resize/reattach without control-plane data loss.
  use_case: Decouples compute lifecycle from data lifecycle. Allows VM scaling or migration while preserving
    schema and operational state.
  relates_to:
  - REQ-696
  - REQ-697
- id: REQ-1030
  status: proposed
  group: 11. Platform, Infrastructure & Delivery
  category: Email Delivery
  priority: MUST
  type: infrastructure
  target: 2026-Q3
  description: 'Email transport for first customer: AWS SES (or equivalent SMTP) at low volume, matching
    the SMTP-only EmailProvider (REQ-1330).'
  use_case: Removes dependency on GCP-only email services. SMTP provides portable, low-volume email delivery
    aligned with the pluggable EmailProvider abstraction.
  relates_to:
  - REQ-1330
  - REQ-1021
  - REQ-1022
- id: REQ-1031
  status: proposed
  group: 11. Platform, Infrastructure & Delivery
  category: First-Customer Deployment
  priority: SHOULD
  type: infrastructure
  target: 2026-Q3
  description: Documented lift-and-shift portability to Vultr (flat pricing + bundled bandwidth) as the
    exit hatch when GCP steady-state cost is no longer preferred. Deployment is plain docker-compose on
    any VM; only the Firebase project is GCP-pinned, compute is host-agnostic.
  use_case: Reduces vendor lock-in. Preserves operator freedom to migrate to lower-cost platforms as workload
    stabilizes and predictable costs justify optimization.
  relates_to:
  - REQ-1026
  - REQ-121
- id: REQ-1032
  status: proposed
  group: 11. Platform, Infrastructure & Delivery
  category: Cost Management
  priority: MUST
  type: constraint
  target: 2026-Q3
  description: No cloud infrastructure is provisioned until a customer is signed; pre-customer sunk cost
    limited to a domain and a free Firebase project. Cold-start relies on REQ-854 OVA/Lima bundles for
    sub-hour spin-up.
  use_case: Minimizes pre-revenue spending. Defers fixed infrastructure costs until revenue justifies
    them, reducing financial risk during customer acquisition.
  relates_to:
  - REQ-854
  - REQ-121
- id: REQ-1033
  status: proposed
  group: 11. Platform, Infrastructure & Delivery
  category: Demo Tiers & Onboarding
  priority: MUST
  type: infrastructure
  target: 2026-Q3
  description: 'Provision two demo tiers on shared first-customer VM: (a) anonymous ephemeral sandbox
    requiring no signup; (b) persistent quota-limited free tier for Firebase-signed-up users. Both leverage
    schema-per-org tenancy on shared Trino/PG/Redis infrastructure; no per-user VMs provisioned.'
  use_case: Converts cold-start friction into zero-friction "try it now" landing page. Ephemeral sandbox
    captures prospects instantly; persistent free tier captures those ready to commit an email. Scales
    to thousands of free accounts on single shared VM because idle orgs consume only disk (no compute).
  relates_to:
  - REQ-696
  - REQ-697
  - REQ-1017
  - REQ-1026
- id: REQ-1034
  status: proposed
  group: 11. Platform, Infrastructure & Delivery
  category: Demo Tiers & Onboarding
  priority: MUST
  type: behavioral
  target: 2026-Q3
  description: On ephemeral sandbox session start, clone a pre-seeded golden template schema (org_template,
    containing REQ-414 demo federated sources and sample data) into a session-scoped org schema (e.g.
    org_sandbox_<uid>) via the REQ-697 init_schema provisioning path.
  use_case: Pre-seeding the template avoids re-running full data registration and source discovery on
    every sandbox session. Prospects can immediately explore and create sources, views, and roles without
    setup lag.
  scenario: 'Given a prospect visits the public landing page

    When they click "Try Now" without signup

    Then a Firebase anonymous session is created and a cloned schema is provisioned with demo sources
    + data

    And the UI connects to that session-scoped org schema

    '
  relates_to:
  - REQ-414
  - REQ-696
  - REQ-697
  - REQ-121
- id: REQ-1035
  status: proposed
  group: 2. Authentication & Identity
  category: Demo Tiers & Onboarding
  priority: MUST
  type: behavioral
  target: 2026-Q3
  description: Ephemeral sandbox sessions are scoped by Firebase anonymous auth (REQ-121) without email
    collection. Prospects get a uid-based session identity with zero friction.
  use_case: Removes signup friction (email validation, password, ToS acceptance) for the "just looking"
    funnel. Firebase anonymous auth is stateless and session-scoped, aligning with ephemeral sandbox lifecycle
    (no user record persists).
  scenario: 'Given a prospect clicks "Try Now"

    When Firebase anonymous auth completes

    Then the browser receives an anonymous auth token scoped to a uid

    And the backend routes all org/schema lookups through that uid

    '
  relates_to:
  - REQ-121
  - REQ-1033
- id: REQ-1036
  status: proposed
  group: 11. Platform, Infrastructure & Delivery
  category: Demo Tiers & Onboarding
  priority: MUST
  type: behavioral
  target: 2026-Q3
  description: Ephemeral sandbox schemas are dropped on explicit logout OR after an idle-TTL of inactivity.
    Teardown is self-resetting; a reaper process periodically sweeps expired sandbox schemas.
  use_case: Ensures sandbox cleanup without manual intervention. Eliminates abandonment debt (orphaned
    schemas)—each expired sandbox reclaims Postgres and Trino resources immediately. Prospect abandonment
    is costless.
  scenario: 'Given a user logs out from an ephemeral sandbox

    When the session ends

    Then DROP SCHEMA CASCADE removes the org_sandbox_<uid> schema and all contents

    Or: Given a sandbox reaches its idle-TTL

    When the reaper runs

    Then expired sandboxes are identified and dropped

    '
  relates_to:
  - REQ-1034
- id: REQ-1037
  status: proposed
  group: 11. Platform, Infrastructure & Delivery
  category: Demo Tiers & Onboarding
  priority: MUST
  type: constraint
  target: 2026-Q3
  description: 'Ephemeral sandbox sessions are subject to: (a) session-scoped TTL (e.g., 24h); (b) per-session
    query row limit and query cost caps; (c) data source registration restricted to bundled demo allowlist
    (no arbitrary outbound live sources). Platform-schema isolation (REQ-696) already guarantees tenant
    isolation.'
  use_case: Prevents sandbox abuse (runaway queries, scanning arbitrary external data warehouses). Demo
    allowlist ensures predictable resource footprint and simplifies compliance.
  relates_to:
  - REQ-696
  - REQ-1034
- id: REQ-1038
  status: proposed
  group: 11. Platform, Infrastructure & Delivery
  category: Demo Tiers & Onboarding
  priority: SHOULD
  type: behavioral
  target: 2026-Q4
  description: 'Free tier orgs follow a reversible archive lifecycle to reclaim disk and Postgres resources
    without destructive loss: idle orgs are email-nudged (REQ-1330/1022) before any action; on continued
    inactivity, pg_dump''d to Cloudflare R2 (REQ-1027) and live schema dropped; restored on re-login from
    R2 archive; hard tombstone (delete R2 archive) only after long tail (e.g. 12 months) with final nudge.'
  use_case: Dormant free accounts consume disk only (no idle compute on shared Trino/PG); archiving is
    disk-pressure-driven, not urgent. Reversible archive (vs. destructive delete) preserves user goodwill
    and simplifies re-engagement campaigns.
  scenario: 'Given a free-tier org is inactive for 3 months

    When the archival check runs

    Then an email nudge is sent to the registered user

    And if no response after 1 month, pg_dump is stored in R2 and the live Postgres schema is dropped

    When the user logs in again

    Then the schema is restored from R2 seamlessly

    And after 12 months of archival with no login, a final retention email is sent

    Then after 1 more month, the R2 archive is deleted (hard tombstone)

    '
  relates_to:
  - REQ-1330
  - REQ-1022
  - REQ-1027
  - REQ-696
- id: REQ-1039
  status: proposed
  group: 11. Platform, Infrastructure & Delivery
  category: Demo Tiers & Onboarding
  priority: MUST
  type: behavioral
  target: 2026-Q3
  description: 'Funnel routing policy: "just looking" traffic (cold-start landing page) is routed to anonymous
    ephemeral sandbox (self-reaping, zero abandonment debt); persistent free tier requires deliberate
    Firebase signup, minimizing abandoned-account volume on shared infrastructure.'
  use_case: Biases toward ephemeral first-contact (low infrastructure cost, self-cleaning) and requires
    a signal (email signup) before allocating a persistent schema. Maximizes conversion rate while minimizing
    accumulation of dormant free accounts.
  scenario: 'Given a prospect visits the landing page without signin

    When they click "Try Now"

    Then they are routed to ephemeral sandbox without email collection

    When they click "Create Account"

    Then they are routed to Firebase signup, and on success, a persistent free-tier org and schema are
    provisioned

    '
  relates_to:
  - REQ-1033
  - REQ-1034
  - REQ-1035
  - REQ-1017
- id: REQ-1040
  status: proposed
  group: 5. Query Languages, Compilation & Operations
  category: Trino Resource Management
  priority: MUST
  type: structural
  target: 2026-Q4
  description: Per-tenant Trino resource groups enforce isolated CPU/memory ceilings and concurrency limits
    (maxRunning, maxQueued) per org_id, preventing noisy-neighbor contention on the shared cluster. Resource
    group configuration is generated via trino_setup.py keyed to org tier.
  use_case: Isolates tenant workloads within a shared Trino cluster. Runaway queries from one org are
    queued/throttled rather than starving interactive queries from other orgs. Tier-based tuning (free
    tier gets tight caps; premium gets relaxed limits) enables cost control at the resource-allocation
    layer.
  relates_to:
  - REQ-696
  - REQ-1017
- id: REQ-1041
  status: proposed
  group: 11. Platform, Infrastructure & Delivery
  category: Trino Scaling & HA
  priority: SHOULD
  type: infrastructure
  target: 2026-Q4
  description: 'Horizontal Trino worker scale-out path: staged from single-node (coordinator executes,
    worker replicas=0) -> dedicated workers (include-coordinator=false, trino-worker replicas>0, coordinator
    remains single) -> k8s HPA on workers autoscaling on CPU/queue depth. Coordinator is never scaled;
    workers are stateless and horizontally elastic.'
  use_case: Allows cost-effective first-customer single-node deployment (coordinator handles query execution)
    while providing a clear path to scale workers independently as workload grows. Worker statelesness
    and HPA integration enable elastic resource response to query volume spikes.
  relates_to:
  - REQ-1026
  - REQ-1028
- id: REQ-1042
  status: proposed
  group: 5. Query Languages, Compilation & Operations
  category: Trino Resource Management
  priority: SHOULD
  type: structural
  target: 2026-Q4
  description: 'Workload separation: interactive user queries and MV/batch-refresh workloads run under
    distinct Trino resource groups (and eventually distinct worker pools), preventing heavy MV rebuilds
    from inflating interactive query latency SLO.'
  use_case: MV fleet (REQ-942/966) refresh cycles can spike memory and CPU; isolating batch workloads
    into a separate resource group with relaxed limits ensures interactive queries maintain predictable
    tail latency. Distinct worker pools (future) enable physical separation and independent scaling.
  relates_to:
  - REQ-942
  - REQ-966
- id: REQ-1043
  status: proposed
  group: 11. Platform, Infrastructure & Delivery
  category: Multi-Tenancy & Scaling
  priority: SHOULD
  type: behavioral
  target: 2026-Q4
  description: 'Tiered cluster routing for physical isolation: sandbox/free tenants route to a shared
    cluster with tight resource-group caps (best-effort); standard paid tenants route to a better-resourced
    shared cluster; whale/premium tenants route to a dedicated Trino cluster (or dedicated worker pool)
    pointed at that org''s data sources. Routing is org-tier-aware.'
  use_case: 'Isolates resource demand: free tier cannot degrade premium SLA. Enables differentiated pricing
    (shared cluster = included in all tiers; dedicated cluster = premium SKU). Simplifies compliance (isolated
    VPC, isolated infra, isolated audit logs for high-security tenants).'
  relates_to:
  - REQ-1017
  - REQ-696
- id: REQ-1044
  status: proposed
  group: 5. Query Languages, Compilation & Operations
  category: Query Cost Control & Monetization
  priority: MUST
  type: constraint
  target: 2026-Q4
  description: 'Hard query-cost caps as a monetization gate: each org tier enforces ceilings on query
    cost (CPU time, peak memory, bytes scanned, execution time) AND on OUTPUT ROWS returned. Queries exceeding
    the tier''s ceiling are REJECTED with an explanatory error message indicating the operation requires
    a higher tier/premium SKU. The first four are scan-side ceilings enforced via Trino resource-group
    limits and per-query guards (query.max-memory-per-node, query.max-execution-time, query.max-scan-physical-bytes)
    keyed to org entitlement; they bound the massive-aggregate case, where a query reads a warehouse to
    return one number, and the cost lands on compute. The output-row cap is the egress-side ceiling and
    is a SEPARATE exposure the scan-side four do not catch: a full-table SELECT * over a modest table
    passes every scan guard while shipping gigabytes, and the cost lands on the egress line ($0.48/GB
    past the tier allowance, REQ-1452). Trino exposes no output-row property, so the cap is enforced by
    Provisa on the result path rather than by the engine. It is a PLAN ENTITLEMENT and is distinct from
    the REQ-005 per-role max_rows, which is an org-authored governance knob that truncates by injecting
    LIMIT: the tier cap REJECTS instead, because a silently truncated result makes the tier boundary invisible
    to the customer hitting it and forfeits the conversion the cap exists to drive. Where both apply the
    stricter bound wins, and only the tier bound raises a tier error. The cap is also what makes the REQ-1454
    active-hour meter safe: that meter charges occupancy and is deliberately blind to volume, so without
    an output ceiling an org can drain the estate inside one $1.30 hour. Free/standard tiers carry restrictive
    caps; expensive queries require acquiring a premium SKU. The confirmed per-tier ceilings are: Trial
    — 100,000 output rows, 10 GB scanned, 8 GB peak memory, 120s execution; Starter — 1,000,000 output
    rows, 100 GB scanned, 32 GB peak memory, 300s execution; Pro — uncapped on all four, because a Pro
    org runs on its own engine and its ceiling is its own hardware. Each tier admits strictly more than
    the one below it, so the upgrade a rejection names always runs the query it was rejected for.'
  use_case: 'Primary monetization lever: caps are hard rejections, not silent degradation. Prevents free-tier
    abuse in both directions — scanning the entire data warehouse (scan-side) and downloading it (egress-side).
    Drives tier-upgrade conversion when users hit caps. Project rule: rejection must be explicit, never
    a silent cap-and-continue.'
  relates_to:
  - REQ-005
  - REQ-1017
  - REQ-1040
  - REQ-1452
  - REQ-1454
- id: REQ-1045
  status: proposed
  group: 11. Platform, Infrastructure & Delivery
  category: Demo Tiers & Onboarding
  priority: MUST
  type: constraint
  target: 2026-Q3
  description: Sandbox and free-tier orgs receive the tightest resource-group and query-cost ceilings,
    enforced via the same tier-cap mechanism (REQ-1044) mapped to sandbox/free entitlements. Caps are
    applied at session provisioning and validated on every query submission.
  use_case: Limits abuse surface of sandbox/free tiers without manual intervention. Sandbox sessions (REQ-1034/1035/1036)
    are especially constrained (query-row limits, data-source allowlist, idle-TTL) to minimize resource
    drain from abandoned demo sessions. Free-tier orgs receive soft caps that nudge users toward upgrade
    without forcing abandonment.
  relates_to:
  - REQ-1033
  - REQ-1034
  - REQ-1035
  - REQ-1036
  - REQ-1044
- id: REQ-1046
  status: complete
  since: 2026-08
  tests:
  - tests/unit/test_storage_quota.py
  - .claude/commercial/tests/test_storage_allowance.py
  - tests/unit/test_org_storage_router.py
  group: 11. Platform, Infrastructure & Delivery
  category: Storage Management & Quotas
  priority: MUST
  type: constraint
  target: 2026-Q4
  description: 'Per-tier storage quotas: each org tier enforces a hard ceiling on the org''s footprint
    in the platform''s relational materialization store — the per-org schemas holding landed rows, MV
    outputs, and the API/GraphQL result caches. Redirect result objects are outside the ceiling: they
    are written under a per-query prefix and TTL-deleted, so they are a rate rather than an accumulation,
    and the egress meter (REQ-1452) prices them. Free tiers receive the tightest ceilings; paid tiers
    larger; enforced and metered per org.'
  use_case: Tiered storage model parallels query-cost caps (REQ-1044) as a primary monetization and resource-isolation
    lever. Platform storage (Postgres schema data, materialized rows, MV outputs, R2 object bytes) is
    the metered resource; federation keeps most source data external. Free tiers get tight bounds to prevent
    abuse accumulation; premium tiers permit larger workloads. Storage must be explicitly capped and enforced
    at ingestion/materialization time, never silently evicted.
  relates_to:
  - REQ-1027
  - REQ-1044
  - REQ-1045
  - REQ-1037
  - REQ-1033
  - REQ-696
  - REQ-942
  - REQ-966
- id: REQ-1047
  status: complete
  since: 2026-08
  tests:
  - tests/unit/test_storage_quota.py
  scenario: 'Given an org whose footprint in the platform materialization store is at its plan''s allowance

    When it refreshes a materialized view or lands a source

    Then the operation is refused with an error naming both a larger plan and bring-your-own storage

    And nothing is truncated, evicted, or partially written

    '
  group: 11. Platform, Infrastructure & Delivery
  category: Storage Management & Quotas
  priority: MUST
  type: behavioral
  target: 2026-Q4
  description: 'Explicit rejection on storage-cap breach: operations that would push an org past its storage
    ceiling (MV materialization, landing/ingest, result persistence) are REJECTED with an explanatory
    error indicating a higher SKU or bring-your-own (BYO) storage is required — never silently truncated
    or evicted.'
  use_case: 'Mirrors REQ-1044''s monetization-gate semantics: hard rejection, not silent degradation.
    Prevents free-tier abuse and drives tier-upgrade conversion. Project rule (CRITICAL): enforcement
    must be explicit rejection, never a silent cap-and-continue. Error message guides customer toward
    upgrade path or BYO storage option.'
  relates_to:
  - REQ-1044
  - REQ-1046
  - REQ-1048
- id: REQ-1048
  status: complete
  since: 2026-08
  tests:
  - tests/unit/test_storage_quota.py
  - tests/unit/test_org_storage_router.py
  group: 11. Platform, Infrastructure & Delivery
  category: Storage Management & Quotas
  priority: SHOULD
  type: structural
  target: 2026-Q4
  description: 'Bring-your-own storage (BYO): an org may register its own materialization store (their
    cloud account, their bill), configured per org and stored encrypted at rest. Once registered it outranks
    the deployment''s configured store for every MV output and landed table the org writes — a precedence,
    not a fallback. Their bytes never count against platform storage and are neither metered (REQ-1049)
    nor capped (REQ-1046). The org''s store credentials are scoped to that org only and are never returned
    by any read surface.'
  use_case: Enables premium customers with large or unpredictable storage needs to avoid platform quota
    limits by supplying their own cloud storage. Isolates security/compliance (customer controls bucket
    permissions, encryption, audit logging) and cost (customer's infrastructure bill, not platform cost).
    Reduces platform resource strain from whales/large enterprises.
  relates_to:
  - REQ-1027
  - REQ-1046
- id: REQ-1049
  status: complete
  since: 2026-08
  tests:
  - tests/unit/test_storage_quota.py
  scenario: 'Given an org materializing into the platform store

    When a write seam checks the org''s storage headroom

    Then the footprint measured off the live store is recorded as that hour''s storage level

    And a later observation in the same hour replaces it rather than adding to it

    '
  group: 11. Platform, Infrastructure & Delivery
  category: Storage Management & Quotas
  priority: MUST
  type: behavioral
  target: 2026-Q4
  description: 'Per-org storage metering/accounting: platform meters each org''s footprint in the relational
    materialization store — measured on demand off the live store rather than accumulated in a counter,
    so the figure tracks the disk the operator is billed for — to drive quota enforcement (REQ-1046),
    tier upgrade prompts, and billing. Recorded as a LEVEL per hour (the last observation replaces the
    previous), not a running total.'
  use_case: Storage metering is the storage analog of per-query cost accounting behind REQ-1044. Enables
    accurate billing per org (platform storage), identifies orgs approaching quota limits (triggering
    upgrade nudges), and feeds resource-capacity planning (platform storage forecasting). BYO orgs (REQ-1048)
    exclude their external bytes from platform metering.
  relates_to:
  - REQ-1044
  - REQ-1046
  - REQ-1047
  - REQ-1048
- id: REQ-1050
  status: complete
  since: 2026-07
  group: 11. Platform, Infrastructure & Delivery
  category: Marketing & Presence
  priority: SHOULD
  type: structural
  unit_test: false
  unit_test_reason: Static marketing site (framework-free HTML/CSS deployed to Cloudflare Pages) — no
    in-process interface to unit test. Verified by code review.
  target: 2026-Q3
  code:
  - site/
  description: 'Marketing website surface: a static marketing site (landing, features, pricing) hosted
    on Cloudflare Pages. Zero marginal cost at any traffic level; deployed independently of the app so
    it cannot affect app availability. Colocated with the Cloudflare R2 object storage vendor.'
  use_case: Public-facing marketing presence decoupled from app infrastructure. Cloudflare Pages provides
    global CDN distribution and cost-free hosting (unlimited traffic, automatic DDoS mitigation). Static
    deployment eliminates operational overhead and risk of app unavailability cascading to marketing presence.
    R2 colocation reduces cross-zone latency for asset serving.
  relates_to:
  - REQ-1027
  - REQ-1026
- id: REQ-1051
  status: proposed
  group: 11. Platform, Infrastructure & Delivery
  category: Documentation & Developer Experience
  priority: SHOULD
  type: structural
  target: 2026-Q3
  description: 'Hosted documentation surface: product docs and API reference published on Cloudflare Pages
    (same deployment or docs subsite), sourced from doc-writer agent output. Static, free-hosted, independent
    of the app.'
  use_case: Product documentation and API reference must be independently accessible and performant. Cloudflare
    Pages provides zero-cost, globally distributed static hosting. Decoupling from the app UI ensures
    docs remain available even during app maintenance or outages, improving developer experience and reducing
    support burden.
  relates_to:
  - REQ-1050
  - REQ-1027
- id: REQ-1052
  status: proposed
  group: 11. Platform, Infrastructure & Delivery
  category: Onboarding & Entry Points
  priority: MUST
  type: behavioral
  target: 2026-Q3
  description: 'Website-to-SaaS entry-point wiring: the marketing site''s primary CTAs (calls-to-action)
    map to the specified onboarding paths — "Try it now" initiates an anonymous ephemeral sandbox session
    (REQ-1034/1036, no signup required); "Sign up free" routes to Firebase self-serve org provisioning
    (REQ-1017); "Contact sales" triggers SES/SMTP email delivery to sales (REQ-1330).'
  use_case: Converts marketing interest into activation (sandbox demo, free-tier signup, sales engagement).
    Clear entry-point routing ensures users experience the intended onboarding flow without friction.
    Marketing messaging and product capability must align (e.g., "try now" genuinely allows zero-friction
    exploration; "sign up free" uses self-serve Firebase, not manual approval).
  scenario: 'Given a user is on the marketing site

    When the user clicks "Try it now"

    Then an anonymous ephemeral sandbox session (REQ-1034) is provisioned and the browser is redirected
    to the session


    Given a user is on the marketing site

    When the user clicks "Sign up free"

    Then the browser redirects to the Firebase self-serve org signup flow (REQ-1017)


    Given a user is on the marketing site

    When the user submits a "Contact sales" form

    Then an email is sent to the sales inbox via SES/SMTP (REQ-1330)

    '
  relates_to:
  - REQ-1017
  - REQ-1330
  - REQ-1034
  - REQ-1036
  - REQ-1050
  e2e: true
  e2e_reason: Entry-point routing requires a live marketing site pointing to live app/sandbox infrastructure;
    clicking CTAs and verifying downstream provisioning is only testable end-to-end.
- id: REQ-1053
  status: proposed
  group: 11. Platform, Infrastructure & Delivery
  category: Pricing & Tiering
  priority: MUST
  type: behavioral
  target: 2026-Q4
  description: 'Pricing page reflects tier entitlements: the public pricing table renders the free/standard/premium
    tier limits directly from the query-cost caps (REQ-1044) and storage caps (REQ-1046) — the same entitlement
    definitions that gate runtime enforcement drive the marketing pricing presentation, eliminating manual
    sync.'
  use_case: 'Single source of truth for tier limits: tier definitions (cost caps, storage quotas) exist
    once in the runtime entitlement system (REQ-1044/1046); the pricing page programmatically queries
    those limits and renders them to users. Eliminates marketing/product drift (manually updated pricing
    page drifting from actual runtime caps). Ensures all users see accurate tier terms before signup.'
  scenario: 'Given a tier definition specifies a query-cost cap and storage quota (via REQ-1044/1046)

    When the pricing page is rendered

    Then the tier card displays the same cap and quota values without manual entry or copy-paste


    Given a tier definition is updated (e.g., free storage increased)

    When the pricing page is refreshed

    Then the new limit is visible without separate marketing update

    '
  relates_to:
  - REQ-1044
  - REQ-1046
  - REQ-1050
  e2e: true
  e2e_reason: Accurate tier-limit rendering from runtime definitions requires the full stack (tier store,
    pricing page, live presentation); unit tests cannot verify marketing/product alignment without end-to-end
    execution.
  tests:
  - tests/unit/test_entitlements.py
- id: REQ-1054
  status: proposed
  group: 11. Platform, Infrastructure & Delivery
  category: Multi-Tenancy & Routing
  priority: MUST
  type: infrastructure
  target: 2026-Q4
  description: 'Per-org wildcard subdomain routing: each org is addressable at {org}.provisa.io via a
    single wildcard DNS record (*.provisa.io) and wildcard TLS certificate, enabling zero-provisioning
    self-serve org creation (REQ-1017).'
  use_case: Eliminates per-org DNS and certificate provisioning overhead. Any newly created org is instantly
    reachable at its subdomain without manual infrastructure steps, reducing onboarding friction and operational
    toil.
  relates_to:
  - REQ-1017
  - REQ-594
- id: REQ-1055
  status: proposed
  group: 11. Platform, Infrastructure & Delivery
  category: Multi-Tenancy & Routing
  priority: MUST
  type: infrastructure
  target: 2026-Q4
  description: 'Fixed per-protocol ports shared across all orgs: HTTP 443 (GraphQL/REST/UI), pgwire 5439,
    Bolt 7687, Arrow Flight 8480 are shared across all orgs, never varying per org. Org resolution comes
    from connection parameters (hostname, database name, username, auth principal), not from port multiplexing.'
  use_case: Simplifies port management and connection strings. Standard ports improve discoverability
    and reduce client configuration complexity. Org is a logical namespace, not a network multiplexing
    dimension.
  relates_to:
  - REQ-1054
- id: REQ-1056
  status: proposed
  superseded_by: REQ-1234
  group: 11. Platform, Infrastructure & Delivery
  category: Multi-Tenancy & Routing
  priority: MUST
  type: behavioral
  target: 2026-Q4
  description: 'Per-surface org resolution: HTTP surfaces resolve org from Host subdomain and/or JWT tenant_id
    claim (REQ-594); pgwire and Bolt (which lack Host headers) resolve org from connection handshake parameters
    (database name, username, or auth principal).'
  use_case: Accommodates wire protocol constraints (no Host header) while leveraging HTTP metadata where
    available. Ensures all surfaces can correctly identify the tenant without ambiguity or silent fallback.
  scenario: 'Given an HTTP request with Host: org-a.provisa.io and a JWT without tenant_id claim

    When the request reaches TenantMiddleware

    Then the org is resolved to org-a


    Given an HTTP request with Host: provisa.io and a JWT with tenant_id: org-b

    When the request reaches TenantMiddleware

    Then the org is resolved to org-b


    Given a pgwire connection string specifying database: org-c

    When the connection handshake completes

    Then the org is resolved to org-c

    '
  relates_to:
  - REQ-594
  - REQ-1055
- id: REQ-1057
  status: proposed
  group: 11. Platform, Infrastructure & Delivery
  category: Multi-Tenancy & Routing
  priority: SHOULD
  type: infrastructure
  target: 2026-Q4
  description: 'Cloudflare wildcard routing: HTTP traffic is proxied via Cloudflare (*.provisa.io -> VM:443);
    raw-TCP wire protocols are routed via Cloudflare Spectrum (TCP-by-SNI) or a wildcard A record, with
    org derived from connection parameters.'
  use_case: Leverages Cloudflare's global edge for HTTP caching and DDoS protection. Spectrum enables
    low-latency TCP passthrough for pgwire and Bolt without origin-side port multiplexing. Single infrastructure
    path for all protocol surfaces.
  relates_to:
  - REQ-1054
  - REQ-1055
  - REQ-1056
- id: REQ-1058
  status: proposed
  group: 1. Access Governance & Security
  category: Public Data Publishing
  priority: SHOULD
  type: behavioral
  target: 2026-Q4
  description: 'Public-publish anonymous access: unauthenticated requests carry no tenant_id claim; the
    org is resolved from Host subdomain only, and the request is bound to a built-in read-only ''public''
    role scoped to that org. TenantMiddleware gains a Host-based resolution path for the no-claim case
    instead of returning 401.'
  use_case: Enables orgs to publish datasets anonymously without requiring end-user authentication. Public
    role allows intentional, opt-in data sharing without building separate anonymous infrastructure. Distinct
    from the Firebase-anonymous sandbox uid (REQ-1034).
  scenario: 'Given a request to org-a.provisa.io with no authentication

    When the request reaches TenantMiddleware

    Then the org is resolved to org-a and the user is bound to org-a''s ''public'' role (not 401)


    Given the public role has zero visibility initially

    When an asset is published to the public role

    Then unauthenticated requests can read that asset via visibility/RLS/masking (REQ-039/040)

    '
  relates_to:
  - REQ-594
  - REQ-039
  - REQ-040
  - REQ-1019
  - REQ-1034
  - REQ-1054
- id: REQ-1059
  status: proposed
  group: 1. Access Governance & Security
  category: Public Data Publishing
  priority: SHOULD
  type: behavioral
  target: 2026-Q4
  description: 'Publishing is a governance grant, not new machinery: each org has a built-in ''public''
    role starting with ZERO visibility. Publishing an asset means granting that role visibility to a specific
    table/view; access then flows through existing visibility/RLS/masking (REQ-039/040) uniformly across
    GraphQL/SQL/pgwire/Bolt. Nothing is public by default; opt-in and explicit.'
  use_case: Reuses existing access-control infrastructure (visibility, RLS, masking) uniformly across
    all surfaces. Eliminates special-case code for public access. Clear opt-in model prevents accidental
    data leaks.
  scenario: 'Given a table orders in org-a

    When an org admin grants the ''public'' role visibility to orders

    Then unauthenticated requests to org-a can query orders subject to its RLS and masking rules

    '
  relates_to:
  - REQ-039
  - REQ-040
  - REQ-1019
  - REQ-1058
- id: REQ-1060
  status: proposed
  group: 1. Access Governance & Security
  category: Public Data Publishing
  priority: MUST
  type: constraint
  target: 2026-Q4
  description: 'Hard caps on anonymous public traffic: unauthenticated published endpoints are internet-facing
    with no login gate, so they inherit the tightest rate limits and query-cost caps (REQ-1044/1045).
    High-volume public publishing requires a premium public-tier SKU.'
  use_case: Protects shared infrastructure from resource exhaustion by anonymous users. Free-tier public
    endpoints are rate-limited; high-volume use is gated to premium. Prevents adversarial abuse or accidental
    DoS by published datasets.
  relates_to:
  - REQ-1044
  - REQ-1045
  - REQ-1058
- id: REQ-1061
  status: proposed
  group: 11. Platform, Infrastructure & Delivery
  category: Multi-Tenancy & Branding
  priority: SHOULD
  type: behavioral
  target: 2026-Q4
  description: 'Subdomain-as-dedicated-channel value proposition: every org gets its own {org}.provisa.io
    endpoint, conveying dedicated tenancy and branding even though authenticated resolution does not require
    it.'
  use_case: Improves perceived value and tenant isolation narrative. Org-branded subdomain increases engagement
    and data ownership perception. Leverages the wildcard infrastructure (REQ-1054) to provide this for
    zero per-org cost.
  relates_to:
  - REQ-1054
  - REQ-1019
- id: REQ-1062
  status: proposed
  group: 11. Platform, Infrastructure & Delivery
  category: Multi-Tenancy & Branding
  priority: SHOULD
  type: behavioral
  target: 2026-Q4
  description: 'Custom domain as a premium gate: premium/enterprise tiers may bind a custom domain (e.g.
    data.acme.com via CNAME) with custom TLS and white-label (no Provisa branding). Free/standard tiers
    use {org}.provisa.io only. Tier entitlements are surfaced on the pricing page (REQ-1052).'
  use_case: Provides a premium branding differentiator. Custom domain + white-label appeals to enterprises
    embedding data apps in customer-facing portals. Clear tier gate monetizes the feature and simplifies
    operations (no per-org cert management for premium tenants).
  relates_to:
  - REQ-1052
  - REQ-1019
  - REQ-1054
- id: REQ-1063
  status: proposed
  group: 11. Platform, Infrastructure & Delivery
  category: Pricing & Tiering
  priority: SHOULD
  type: behavioral
  target: 2026-Q4
  description: 'Usage-credit billing meter: metered consumption is billed as universal compute credits
    whose consumption is uniform across tiers, with the dollar price per credit rising by tier (bundling
    features/support) — the Starburst Galaxy model. Built on Stripe billing (REQ-594).'
  use_case: Provides predictable, usage-based billing that aligns with customer consumption patterns.
    Tier-based credit pricing allows upsells (pay more per credit for premium support/features). Complements
    hard caps (REQ-1044/1046) which define ceilings; meter defines pay-as-you-go below them. Industry-proven
    model (Starburst, Apollo).
  relates_to:
  - REQ-594
  - REQ-1044
  - REQ-1046
- id: REQ-1064
  status: proposed
  group: 11. Platform, Infrastructure & Delivery
  category: Pricing & Tiering
  priority: SHOULD
  type: behavioral
  target: 2026-Q4
  description: 'Credit-boxed + time-boxed enterprise trial: a fixed-duration trial (e.g. 30 days) grants
    a fixed compute-credit balance; on expiry or non-payment, the account auto-downgrades to free tier
    (never silent charge).'
  use_case: Lowers barrier to enterprise evaluation. Fixed credit budget prevents runaway costs; explicit
    downgrade on expiry prevents surprise billing and maintains customer trust. Encourages pilot-to-production
    conversion.
  scenario: 'Given an enterprise trial is created with 30-day duration and 1000 credits

    When 29 days have passed and credits are consumed

    Then queries continue to work as long as credits remain


    Given the trial duration expires

    When the customer has not converted to a paid plan

    Then the account auto-downgrades to free tier with no charge

    '
  relates_to:
  - REQ-1063
  - REQ-594
- id: REQ-1065
  status: proposed
  group: 11. Platform, Infrastructure & Delivery
  category: Pricing & Tiering
  priority: MAY
  type: behavioral
  target: 2026-Q4
  description: 'Signup usage credit: a small usage-credit grant on signup (Apollo $50-style) provides
    a soft on-ramp to exploration without a time-boxed trial expiration.'
  use_case: Reduces friction for self-serve signup. Free users get compute credits to try real queries;
    if they exhaust credits, they can upgrade, not just expire. Increases conversion from free to paid
    by showcasing value through real usage.
  scenario: 'Given a user signs up for the free tier

    When signup completes

    Then the user is granted a fixed credit balance (e.g., 50 credits) with no expiration


    Given the user runs queries that consume credits

    When all credits are exhausted

    Then future queries fail with an upgrade prompt (not a time-based expiration)

    '
  relates_to:
  - REQ-1063
  - REQ-1017
- id: REQ-1066
  status: proposed
  group: 11. Platform, Infrastructure & Delivery
  category: Pricing & Tiering
  priority: SHOULD
  type: behavioral
  target: 2026-Q4
  description: 'Premium feature gates beyond compute: SSO/SAML + fine-grained access control (ABAC/SCIM,
    leveraging REQ-122/123/203/204), longer audit/observability retention, private networking, and SLA-backed
    support are gated to premium tiers.'
  use_case: Monetizes operational and compliance features that enterprises demand. Justifies premium tier
    beyond compute capacity alone. Clear feature gates drive upsell conversation and meet customer expectations
    for enterprise offerings.
  relates_to:
  - REQ-122
  - REQ-123
  - REQ-203
  - REQ-204
  - REQ-1019
  tests:
  - tests/unit/test_entitlements.py
- id: REQ-1067
  status: proposed
  group: 11. Platform, Infrastructure & Delivery
  category: Pricing & Tiering
  priority: SHOULD
  type: behavioral
  target: 2026-Q4
  description: 'Bring-your-own compute / dedicated cluster as premium gate: premium tenants may run on
    a dedicated Trino cluster (extends tiered routing REQ-1043) and/or bring their own compute, complementing
    BYO storage (REQ-1048).'
  use_case: Provides high-touch enterprise offering with dedicated resources and isolation. Extends tiered
    routing to allocate premium tenants to reserved clusters. Enables power customers to optimize and
    control compute independently of Provisa infrastructure. Competitive parity with Starburst Galaxy
    (dedicated clusters) and Apollo GraphOS (federation customization).
  relates_to:
  - REQ-1043
  - REQ-1048
  - REQ-1063
- id: REQ-1068
  status: accepted
  group: 11. Platform, Infrastructure & Delivery
  category: Data Catalog Integration
  priority: SHOULD
  type: structural
  target: 2026-Q4
  description: 'Pluggable metadata export provider pattern: an abstract MetadataExport interface (mirroring
    AuthProvider/EmailProvider pattern) enables organizations to publish Provisa''s governance metadata
    OUTBOUND to external data catalogs. Configured per org in YAML with vendor-specific credentials. Outbound
    only — Provisa never ingests an external catalog as source of truth.'
  use_case: Allows Provisa to act as the authoritative upstream feeding customers' existing data-governance
    catalogs (OpenMetadata, Atlan, Collibra, DataHub, Apache Atlas), rather than competing with them.
    Keeps Provisa as runtime enforcement engine while projecting metadata to the customer's catalog of
    record.
  code:
  - provisa/api/metadata_export/provider.py
  relates_to:
  - REQ-120
  - REQ-1330
  - REQ-1066
- id: REQ-1069
  status: accepted
  group: 11. Platform, Infrastructure & Delivery
  category: Data Catalog Integration
  priority: SHOULD
  type: structural
  target: 2026-Q4
  description: 'Standards-first metadata core: the MetadataExport provider emits OpenLineage for lineage
    and maps assets to the OpenMetadata ingestion API as first-class targets. Vendor-specific adapters
    (Atlan, Collibra, DataHub, Apache Atlas) are implemented as concrete subclasses of MetadataExport
    using the same internal metadata model.'
  use_case: Standards-based approach (OpenLineage, OpenMetadata) ensures portability and reduces vendor
    lock-in. First-class support for OpenMetadata reduces friction for OSS adopters; vendor adapters extend
    to enterprises already on Atlan or Collibra.
  code:
  - provisa/api/metadata_export/openlineage.py
  - provisa/api/metadata_export/openmetadata.py
  - provisa/api/metadata_export/registry.py
  tests:
  - tests/unit/test_metadata_export_standards.py
  - tests/integration/test_metadata_export_openlineage_e2e.py
  - tests/integration/test_metadata_export_openmetadata_e2e.py
  relates_to:
  - REQ-1068
- id: REQ-1070
  status: accepted
  group: 11. Platform, Infrastructure & Delivery
  category: Data Catalog Integration
  priority: SHOULD
  type: behavioral
  target: 2026-Q4
  description: Published metadata payload includes datasets/tables/columns, domains, stewards and ownership
    (REQ-609/020), approved relationships, and descriptions/aliases. Lineage is derived from actually-compiled
    queries and the MV DAG (provisa/lineage/, REQ-939/942), providing column-level + MV-DAG lineage more
    accurate than scanner/agent-based ingestion.
  use_case: Provisa's query-compilation and DAG-based lineage is derived from actual execution, making
    it more authoritative than external scanners or agent-based metadata collection. Publishing this accuracy
    to external catalogs elevates DG team's source-of-truth quality.
  relates_to:
  - REQ-609
  - REQ-020
  - REQ-039
  - REQ-040
  - REQ-939
  - REQ-942
  - REQ-1068
  - REQ-1069
  code:
  - provisa/api/metadata_export/model.py
  - provisa/api/metadata_export/builder.py
  scenario: 'Given a governed config with a stewarded domain, a table with described columns, an approved

    relationship, and a view derived from that table

    When a metadata snapshot is built for the org

    Then the snapshot publishes the source, domain, table and every column as addressable assets

    And the approved relationship carries its defining steward, version and review flag

    And the view''s lineage is published per column with the transform that produces it

    And a domain with no designated steward is published as pending rather than omitted

    '
- id: REQ-1071
  status: accepted
  group: 11. Platform, Infrastructure & Delivery
  category: Data Catalog Integration
  priority: SHOULD
  type: behavioral
  target: 2026-Q4
  description: 'Governance-signal projection: enforcement facts Provisa already computes (which columns/tables
    are masked, RLS-restricted, or visibility-restricted per REQ-039/040) are projected outward as tags,
    classifications, or metadata properties on the corresponding assets in the target external catalog.'
  use_case: Data-governance teams see Provisa's enforced governance policies reflected in their catalog
    of record, enabling data consumers to understand which data is restricted or transformed without context-switching
    to Provisa's UI. Strengthens audit and compliance narrative.
  relates_to:
  - REQ-039
  - REQ-040
  - REQ-1068
  - REQ-1070
  code:
  - provisa/api/metadata_export/governance.py
  tests:
  - tests/unit/test_metadata_export_governance.py
  scenario: 'Given a governed config with a masked column, a column hidden from one role, and an RLS rule

    When a metadata snapshot is built for the org

    Then each restricted asset carries a governance tag naming the signal and the rule

    And every tag names the roles the restriction applies to and the roles exempt from it

    And no mask pattern or RLS predicate appears anywhere in the snapshot

    '
- id: REQ-1072
  status: accepted
  group: 11. Platform, Infrastructure & Delivery
  category: Data Catalog Integration
  priority: SHOULD
  type: behavioral
  target: 2026-Q4
  description: 'Sync mechanism: metadata changes push to the external catalog event-driven via the REQ-942
    event substrate. A scheduled full reconcile (using the existing scheduler, provisa/scheduler/jobs.py)
    periodically re-syncs the complete metadata projection. Per-org configuration and credentials, scoped
    to that org only.'
  use_case: Event-driven sync ensures near-real-time metadata propagation for operational responsiveness;
    scheduled full reconcile handles dropped events and corrects drift. Per-org scoping ensures multi-tenant
    isolation and per-customer credential management.
  relates_to:
  - REQ-942
  - REQ-1068
  - REQ-1070
  code:
  - provisa/api/metadata_export/publishing.py
  tests:
  - tests/unit/test_metadata_export_publishing.py
  - tests/steps/steps_metadata_export_docs.py
  scenario: 'Given an org whose metadata export target is configured and entitled

    When the governed model changes

    Then a publish is queued for that org and no other

    And one drain publishes the whole pending change set as a single snapshot

    And a publish the catalog rejected stays claimable instead of being completed

    And the org''s scheduled reconcile republishes the full snapshot on its own cron

    '
- id: REQ-1073
  status: accepted
  group: 11. Platform, Infrastructure & Delivery
  category: Data Catalog Integration
  priority: MUST
  type: constraint
  target: 2026-Q4
  description: 'Premium-gated metadata export: the metadata export connector is an enterprise/premium
    tier entitlement, extending REQ-1066. Targeting regulated-industry data-governance teams with existing
    external catalog investments.'
  use_case: Metadata export to external catalogs is a high-touch enterprise feature. Restricted to premium
    tiers to simplify support and monetize integration value to enterprises already operating external
    DG systems.
  relates_to:
  - REQ-1066
  - REQ-1068
  - REQ-1069
  - REQ-1070
  - REQ-1071
  - REQ-1072
  code:
  - provisa/control_plane/entitlements.py
  tests:
  - tests/unit/test_entitlements.py
  - tests/unit/test_metadata_export_admin_surface.py
  - tests/unit/test_metadata_export_gate.py
- id: REQ-1074
  status: accepted
  group: 10. UI & Admin Surfaces
  category: Admin Configuration
  priority: MUST
  type: ui
  target: 2026-Q4
  description: 'Metadata export admin surface: an Admin tab that configures the per-org export target
    (provider, endpoint, credentials, reconcile schedule), runs a connection health check against the
    live catalog, triggers an on-demand full reconcile, and shows the last publish outcome including the
    per-asset errors a partial publish returns. Credentials are write-only in the UI — an existing secret
    renders as set/not-set, never as its value. The tab is hidden for orgs without the REQ-1073 entitlement.'
  use_case: A data-governance admin configures and operates the catalog connection without editing YAML,
    and diagnoses a partial publish from the assets the target rejected rather than from server logs.
  code:
  - provisa/api/admin/metadata_export_router.py
  - provisa-ui/src/components/admin/MetadataExportTab.tsx
  - provisa-ui/src/api/metadataExport.ts
  tests:
  - tests/unit/test_metadata_export_admin_surface.py
  - provisa-ui/src/__tests__/MetadataExportTab.test.tsx
  - provisa-ui/e2e/metadata-export-admin.spec.ts
  relates_to:
  - REQ-1068
  - REQ-1069
  - REQ-1072
  - REQ-1073
- id: REQ-1368
  status: accepted
  group: 11. Platform, Infrastructure & Delivery
  category: Data Catalog Integration
  priority: SHOULD
  type: behavioral
  target: 2026-Q4
  description: 'Metadata export user documentation: a published docs page (docs/metadata-export.md, navigated
    under Security & Governance) covering the supported targets, the YAML configuration, what the payload
    contains, how governance signals appear in the target catalog, and the event-driven vs scheduled-reconcile
    sync model. Outbound-only is stated explicitly so no reader expects a catalog-to-Provisa ingest path.'
  use_case: An admin evaluating or configuring catalog integration finds the supported targets and the
    exact configuration in the product docs instead of in the architecture notes.
  code:
  - docs/metadata-export.md
  - mkdocs.yml
  tests:
  - tests/steps/steps_metadata_export_docs.py
  scenario: 'Given the published documentation set

    When a reader looks for how to publish metadata to an external catalog

    Then the metadata export page is reachable from the Security and Governance navigation

    And the page states that publication is outbound only

    And the page names every registered export provider

    And the page documents each configuration setting the export config accepts

    And the page describes the sync model the scheduler actually runs

    '
  relates_to:
  - REQ-1068
  - REQ-1069
  - REQ-1070
  - REQ-1071
  - REQ-1072
- id: REQ-1079
  status: complete
  tests:
  - tests/unit/test_admin_config_tabs.py
  - provisa-ui/src/__tests__/SecurityTab.test.tsx
  group: 10. UI & Admin Surfaces
  category: Admin Configuration
  priority: SHOULD
  type: ui
  since: 2026-07
  description: 'Admin UI can view and edit the security posture (security.mode: standard | high) via GET/PUT
    /admin/security, with changes applied on restart.'
  use_case: Enables operators to adjust security enforcement level without code deployment.
  relates_to:
  - REQ-693
- id: REQ-1080
  status: complete
  tests:
  - tests/unit/test_admin_config_tabs.py
  - provisa-ui/src/__tests__/AiModelsTab.test.tsx
  group: 10. UI & Admin Surfaces
  category: Admin Configuration
  priority: SHOULD
  type: ui
  since: 2026-07
  description: Admin UI can view and edit AI model assignments (ai_models), the embedding/vector-model
    registry (vector_models), and the natural-language rate limit (nl.rate_limit) via GET/PUT /admin/ai-models,
    with changes applied on restart.
  use_case: Allows dynamic model and rate-limit tuning without redeployment.
  relates_to:
  - REQ-464
  - REQ-419
  - REQ-500
  - REQ-370
- id: REQ-1081
  status: complete
  tests:
  - tests/unit/test_admin_config_tabs.py
  group: 10. UI & Admin Surfaces
  category: Admin Configuration
  priority: SHOULD
  type: ui
  since: 2026-07
  description: Admin UI cache-storage settings now also cover warm-tier promotion and engine filesystem
    read-cache (warm_tables), plus the default materialized-view refresh TTL (materialized_views.default_ttl).
  use_case: Centralizes cache and materialized-view refresh configuration in the admin console.
  relates_to:
  - REQ-240
  - REQ-543
- id: REQ-1082
  status: complete
  tests:
  - tests/unit/test_admin_config_tabs.py
  group: 10. UI & Admin Surfaces
  category: Admin Configuration
  priority: SHOULD
  type: ui
  since: 2026-07
  description: Admin UI observability settings expose the full OpenTelemetry tracing-pipeline tuning surface
    (log_level, compact_cron, compact_batch_size, compact_file_chunk, ops_snapshot_retention_hours, span_export_delay_millis,
    otlp2parquet_max_age_secs, collector_batch_timeout_ms, s3_endpoint).
  use_case: Operators can fine-tune telemetry collection and export without redeployment.
  relates_to:
  - REQ-545
- id: REQ-1083
  status: complete
  tests:
  - tests/unit/test_admin_config_tabs.py
  group: 10. UI & Admin Surfaces
  category: Admin Configuration
  priority: SHOULD
  type: ui
  since: 2026-07
  description: Admin UI platform settings now expose remote-GraphQL traversal limits (graphql_remote.max_object_depth,
    max_list_depth, max_list_items) and make the default query sample size editable.
  use_case: Operators can constrain GraphQL complexity and adjust sampling without redeployment.
  relates_to:
  - REQ-165
- id: REQ-1084
  status: complete
  tests:
  - tests/unit/test_admin_config_tabs.py
  group: 10. UI & Admin Surfaces
  category: Admin Configuration
  priority: SHOULD
  type: ui
  since: 2026-07
  description: The federation-engine tab now exposes S3 exchange-spool fields (exchange_spool_s3_endpoint,
    exchange_spool_bucket, exchange_spool_s3_region, exchange_spool_s3_access_key, exchange_spool_s3_secret_key).
  use_case: Enables S3-backed exchange-spool configuration in the admin UI.
  relates_to:
  - REQ-916
- id: REQ-1085
  status: complete
  tests:
  - provisa-ui/src/__tests__/SecurityTab.test.tsx
  - provisa-ui/src/__tests__/EncryptionTab.test.tsx
  - provisa-ui/src/__tests__/LocalUsersTab.test.tsx
  group: 10. UI & Admin Surfaces
  category: Admin Navigation
  priority: SHOULD
  type: ui
  since: 2026-07
  description: Admin navigation consolidates security posture, encryption, authentication, and local users
    under a single Security section with sub-tabs.
  use_case: Improves discoverability and grouping of security-related admin settings.
- id: REQ-1086
  status: complete
  tests:
  - tests/unit/test_encryption_providers.py
  group: 1. Access Governance & Security
  category: Encryption
  priority: SHOULD
  type: structural
  since: 2026-07
  description: Extensible encryption provider registry (provisa/encryption/registry.py) with pluggable
    implementations. Each provider is a spec with UI config_fields, an availability probe, and a builder.
    build_encryption_service dispatches via the registry and fails closed on unknown/unavailable providers.
  use_case: Decouples provider implementation from core service, enabling built-in (LocalKeychain, AwsKms,
    AzureKeyVault, Vault) and custom providers (via env var, entry-point, or API) to coexist without code
    changes.
  code:
  - provisa/encryption/registry.py
  relates_to:
  - REQ-684
  - REQ-685
- id: REQ-1087
  status: complete
  tests:
  - tests/unit/test_encryption_providers.py
  group: 1. Access Governance & Security
  category: Encryption
  priority: SHOULD
  type: structural
  since: 2026-07
  description: AWS KMS encryption provider (envelope wrapping via KMS Encrypt/Decrypt) with support for
    custom endpoint_url for KMS-compatible private endpoints. Available whenever boto3 is present.
  use_case: Enterprises can use AWS-managed KMS keys without re-implementing envelope encryption logic.
    Private endpoint support enables airgapped deployments.
  code:
  - provisa/encryption/providers/aws_kms.py
  relates_to:
  - REQ-684
- id: REQ-1088
  status: complete
  tests:
  - tests/unit/test_encryption_providers.py
  group: 1. Access Governance & Security
  category: Encryption
  priority: SHOULD
  type: structural
  since: 2026-07
  description: HashiCorp Vault (Transit engine) encryption provider with auto-availability when hvac package
    is installed. Vault address is configurable as a custom endpoint.
  use_case: Enterprises using Vault can leverage existing HSM or encryption backends without provisioning
    cloud KMS. Custom Vault endpoint supports private/airgapped deployments.
  code:
  - provisa/encryption/providers/vault.py
  relates_to:
  - REQ-684
- id: REQ-1089
  status: complete
  tests:
  - tests/unit/test_encryption_providers.py
  group: 1. Access Governance & Security
  category: Encryption
  priority: SHOULD
  type: structural
  since: 2026-07
  description: Azure Key Vault encryption provider (RSA-OAEP-256 key wrap) with auto-availability when
    azure-keyvault-keys package is installed.
  use_case: Azure-native deployments can leverage customer-managed keys in Azure Key Vault without running
    separate KMS infrastructure.
  code:
  - provisa/encryption/providers/azure_keyvault.py
  relates_to:
  - REQ-684
- id: REQ-1090
  status: complete
  group: 1. Access Governance & Security
  category: Encryption
  priority: SHOULD
  type: behavioral
  since: 2026-07
  description: Enterprises can register custom encryption providers (e.g., in-house KMS/HSM endpoints)
    without core changes via register_encryption_provider() API, PROVISA_ENCRYPTION_PROVIDER_MODULES environment
    variable, or provisa.encryption_providers entry-point group.
  use_case: Organizations with proprietary KMS or HSM deployments can integrate them without maintaining
    a Provisa fork.
  code:
  - provisa/encryption/registry.py
  tests:
  - tests/unit/test_encryption_providers.py
  relates_to:
  - REQ-684
  scenario: 'Given a custom KMS provider module is installed

    When build_encryption_service is called with the custom provider name

    Then the custom provider is instantiated and used for encryption operations

    '
- id: REQ-1091
  status: complete
  tests:
  - tests/unit/test_admin_config_tabs.py
  - provisa-ui/src/__tests__/EncryptionTab.test.tsx
  group: 10. UI & Admin Surfaces
  category: Admin Configuration
  priority: SHOULD
  type: ui
  since: 2026-07
  description: Admin encryption tab derives its provider list live from the encryption registry. Built-in
    and custom providers appear automatically with their declared config fields. Unavailable providers
    are shown but not selectable.
  use_case: Operators can configure encryption without code changes; custom providers are discoverable
    in the UI without redeploy.
  relates_to:
  - REQ-1086
- id: REQ-1092
  status: complete
  tests:
  - tests/unit/test_mcp_chat.py
  group: 10. UI & Admin Surfaces
  category: Catalog Explore
  priority: SHOULD
  type: ui
  since: 2026-07
  description: Provisa provides an LLM chatbot at /explore (under the Explore nav group, branded as "Assistant")
    where users converse in natural language to explore and query the federated catalog. The interface
    is built with @chatscope/chat-ui-kit-react. A Claude model (default claude-opus-4-8, configurable
    via ai_models.mcp_chat) drives a manual tool-use loop with governed MCP tools—list_schemas, list_tables,
    describe_table, search_catalog, run_sql, explain_sql—via provisa/api/mcp/chat.py. Every tool executes
    under the caller's fixed role (from x-provisa-role), so the assistant respects domain access + governance
    boundaries; the model cannot choose or escalate the role. The backend streams assistant text and tool
    activity to the browser as Server-Sent Events via POST /admin/mcp/chat.
  use_case: Users can explore and query the federated catalog through conversational AI, discovering tables/columns
    and running SQL through natural language without schema knowledge, constrained by their role's visibility.
    The assistant is bound by the same governed pipeline as any client.
  code:
  - provisa/api/mcp/chat.py
  - provisa/api/mcp/status.py
  - provisa-ui/src/pages/McpExplorePage.tsx
  e2e: true
  e2e_reason: Behavior is only verifiable through browser chat interaction, API transport (SSE), and assistant
    response rendering.
  scenario: 'Given a user with an active role that can access catalog tables

    When the user navigates to /explore and submits a natural-language query in the chat interface

    Then the assistant uses search_catalog or structured drill-down tools to discover matching tables/columns

    And the assistant returns explanations or SQL results constrained to the user''s role''s accessible
    domains

    And the model can never choose or escalate the role, and tool errors are reported back to the model
    for recovery

    '
  relates_to:
  - REQ-1008
- id: REQ-1093
  status: complete
  group: 3. Source Registration & Data Modeling
  category: Unique Constraints
  priority: SHOULD
  type: behavioral
  since: 2026-07
  description: 'A registered Table carries a table-level list of UNIQUE constraints (`unique_constraints:
    [{name, columns[]}]`), each an ordered column set that may be single-column or composite. On registration
    Provisa introspects declared UNIQUE constraints from RDB sources (PostgreSQL/MySQL information_schema.table_constraints
    + key_column_usage grouped by constraint_name and ordered by ordinal_position; SQLite PRAGMA index_list/index_info
    with origin=''u'') and seeds the list. Uniqueness is only recorded from a source-declared constraint,
    never inferred from sampled data. The admin UI exposes an expandable "Uniques" panel in the register/edit
    table forms: a "+ Constraint" control adds a row with a Name text field and a multi-select checkbox
    of the table''s columns. pgwire emits each unique constraint as a pg_constraint row with contype ''u''
    (conkey = ordered attnums, no confrelid) and projects it into information_schema.table_constraints
    (constraint_type ''UNIQUE'') and key_column_usage, so standard clients (DBeaver, psql \d) render the
    keys. The MCP describe_table tool includes the table''s unique_constraints alongside foreign_keys,
    role-gated.'
  use_case: Foreign keys can legitimately target any unique key, not just the primary key; surfacing declared
    unique constraints improves relationship inference, lets the query planner reason about row identity,
    gives standard SQL clients and the MCP agent the natural keys users actually join and filter on, and
    makes the ERD/catalog show natural keys instead of only surrogate PKs.
  code:
  - provisa/core/models.py
  - provisa/discovery/fk_introspect.py
  - provisa/pgwire/catalog_constraints.py
  - provisa/api/mcp/tools.py
  - provisa/api/admin/discovery_schema.py
  - provisa/compiler/context.py
  - provisa-ui/src/components/admin/UniquesPanel.tsx
  - provisa-ui/src/pages/tables/RegisterTableForm.tsx
  - provisa-ui/src/pages/tables/TableEditForm.tsx
  tests:
  - tests/unit/test_unique_introspect.py
  - tests/unit/pgwire/test_unique_constraints.py
  - tests/unit/test_unique_constraints_wiring.py
  - provisa-ui/src/components/admin/__tests__/UniquesPanel.test.tsx
  scenario: 'Given a PostgreSQL source table with a declared UNIQUE constraint on (tenant_id, email)

    When the table is registered in Provisa

    Then the table''s unique_constraints list is seeded with {name, columns: [tenant_id, email]} from
    the source

    And the admin register/edit UI shows the constraint in the expandable "Uniques" panel with its name
    and checked columns

    And pgwire exposes it as a pg_constraint row with contype ''u'' and an information_schema.table_constraints
    row of type ''UNIQUE''

    And the MCP describe_table response for the table includes the unique_constraints entry

    '
  relates_to:
  - REQ-018
- id: REQ-1094
  status: complete
  tests:
  - tests/unit/pgwire/test_composite_fk_and_index.py
  group: 8. Client Access & Protocols
  category: PostgreSQL Catalog Compatibility
  priority: MUST
  type: structural
  since: 2026-07
  description: Composite foreign keys are reconstructed at pgwire catalog-build time from the FK→PK invariant.
    Provisa's relationship model stores each FK column as a separate join with no shared constraint ID;
    single-column many-to-one joins between the same source/target table pair whose target columns together
    form the target table's composite primary key are collapsed into one pg_constraint FK row (contype
    'f') with multi-element conkey/confkey arrays, ordered by target PK column order. Joins not covering
    a composite PK (single-column FKs, partial coverage, independent multi-FKs to the same table) remain
    one single-column row each.
  use_case: PostgreSQL clients and tools (e.g., DataGrip, psql) expect composite FKs as single pg_constraint
    rows with parallel conkey/confkey arrays; reconstructing them ensures correct relationship visualization
    and validation logic without requiring a V1-prohibited migration.
  code:
  - provisa/pgwire/catalog_constraints.py
  scenario: 'Given a target table with a composite primary key (col_a, col_b)

    When source table S has many-to-one relationships joining S.col_a → target.col_a and S.col_b → target.col_b

    Then pgwire emits one pg_constraint FK row with conkey=[attnum_a, attnum_b] and confkey=[attnum_a,
    attnum_b]

    ordered by target PK column order

    And a single-column FK from S to target on a different column remains one separate pg_constraint row

    '
- id: REQ-1095
  status: complete
  tests:
  - tests/unit/pgwire/test_composite_fk_and_index.py
  group: 8. Client Access & Protocols
  category: PostgreSQL Catalog Compatibility
  priority: MUST
  type: structural
  since: 2026-07
  description: pg_catalog.pg_index is populated with one row per primary-key and UNIQUE constraint (indrelid
    = table oid, indkey = ordered key attnums, indisprimary/indisunique flags set). This allows PostgreSQL
    clients (e.g., DataGrip) that resolve key columns via pg_index.indkey rather than pg_constraint to
    discover the correct columns via the standard pg_index → pg_attribute join.
  use_case: Standard SQL clients resolve primary key and unique constraint columns by joining pg_index
    to pg_attribute on indkey; populating pg_index ensures clients that use this path (rather than pg_constraint)
    get correct key column visibility and metadata.
  code:
  - provisa/pgwire/catalog_constraints.py
  scenario: 'Given a table with a primary key and a UNIQUE constraint

    When a PostgreSQL client queries pg_index for the table

    Then one pg_index row exists for the PK with indisprimary=true and indkey=[attnum list]

    And one pg_index row exists for the UNIQUE with indisunique=true and indkey=[attnum list]

    And clients resolving key columns via pg_index → pg_attribute get the correct columns

    '
- id: REQ-1096
  status: complete
  group: 10. UI & Admin Surfaces
  category: Admin & Configuration
  priority: SHOULD
  type: behavioral
  since: 2026-07
  description: 'Live config export/diff/patch — an OPT-IN admin capability, distinct from the plain download/upload
    of REQ-164, that lets an admin see and export exactly what changed since startup. The on-disk config
    is only the boot seed; admin mutations write to the control-plane DB, so the file goes stale (an MV
    or relationship created in the UI, or a changed primary key, never appears in it). When enabled, Provisa
    rebuilds the DB-backed config sections (tables incl. views/MVs, relationships, roles, rls_rules, domains)
    from live state and overlays them on the file base to produce the CURRENT config.

    The baseline for diff/patch is a BOOT SNAPSHOT captured ONCE at the end of boot — after all runtime
    auto-derivation (FK tracking, graphql-remote registration) — so the diff shows only changes made SINCE
    startup, not derived entities that were never in the file. This is the "lock the starting config after
    automated changes" behavior: the automated/derived setup is frozen as the baseline, and every subsequent
    admin edit shows as a clean diff against it.

    Both sides are normalized identically (deep key-sort + stable entity-sort) so section/key/entity ORDER
    never differs and the diff surfaces only real changes. The admin UI (Configuration File section) exposes:
    View Diff (a two-pane diff of baseline vs current, editable), Download Patch (a git-apply/patch-compatible
    unified diff from baseline to the curated current, for CI/CD reproduction), and Apply Revised (upload
    the curated config and reload). When the flag is on, uploaded config is normalized on consume and
    persisted in normalized form so the on-disk file stays byte-faithful to the patch baseline; when off,
    the file is written verbatim to preserve a hand-authored file''s comments/ordering.

    The feature is coherent only when the generated/normalized config is canonical (config built from
    installer/demo choices), NOT a hand-authored file whose comments/ordering a normalized patch could
    not stay faithful to — so it is GATED behind the live_config_export flag (config key live_config_export:
    true OR env PROVISA_LIVE_CONFIG_EXPORT in {1,true,yes}), exposed to the UI as features.live_config_export,
    which conditionally renders the View Diff / patch controls. In DEMO MODE the flag MUST be ON: the
    demo is the canonical-generated-config scenario, so demo builds force live_config_export regardless
    of an explicit config key. (Amended 2026-08-21, superseded by the repository for an org''s model:)
    Once an environment has the repository of REQ-1524 this surface stops being the way a change is seen.
    Its baseline is a BOOT SNAPSHOT and its two sides are one YAML document, and both assumptions fail
    against a write-through repository: there is no drift between a file and live state to reconcile,
    because every change was already committed as it happened, and there is no single document to diff,
    because REQ-1526 serializes the model as a directory of files. A diff is therefore between two REFS
    -- any commit against any other, not only against boot -- and it is rendered per file, which is also
    the only form in which a rename or a deletion can be shown at all. The right side stops being editable
    and the revert arrows go with it: editing a projection would make the repository an authority over
    the model, which REQ-1524 forbids, and undo is loading an earlier sha rather than reverting a chunk.
    The capability survives for the deployment''s own boot seed, which is still a file and still goes
    stale; it is the ORG''s model that moves to the repository.'
  use_case: Admins running the canonical/demo config need to see exactly what they changed in the UI since
    startup and export it as a patch for version control or CI/CD, instead of hand-diffing a stale on-disk
    file that never reflected DB-backed edits. Locking the boot snapshot after automated derivation keeps
    the diff free of noise. Demo users get the diff/patch surface out of the box because the demo config
    is generated and therefore always safe to normalize.
  code:
  - provisa/api/app.py
  - provisa/api/app_startup.py
  - provisa/api/admin/config_export.py
  - provisa/api/admin/settings_router.py
  - provisa-ui/src/pages/AdminPage.tsx
  - provisa-ui/src/components/admin/ConfigDiffView.tsx
  tests:
  - tests/unit/test_config_export.py
  - tests/integration/test_settings_router_api.py
  scenario: 'Given demo mode is active

    Then live_config_export is forced on and the admin UI shows View Diff / Download Patch / Apply Revised

    Given live_config_export is enabled

    When Provisa finishes booting (after FK tracking and graphql-remote registration)

    Then the generated config is captured once as the boot-snapshot baseline

    When an admin changes a table''s primary key (or creates an MV) in the UI

    And the admin opens View Diff

    Then the diff shows only that change against the boot snapshot, both sides normalized identically

    And Download Patch yields a git-apply-compatible unified diff from baseline to the curated current

    And Apply Revised uploads the normalized config and reloads

    Given live_config_export is disabled and demo mode is inactive

    Then the View Diff / patch controls are absent and an uploaded config is written verbatim

    '
  relates_to:
  - REQ-164
- id: REQ-1097
  status: complete
  since: 2026-07
  group: 4. Source Connectors
  category: Test Coverage
  priority: MUST
  type: constraint
  description: 'Every datasource type in SourceType enum (49 types: postgresql, mysql, mariadb, sqlserver,
    oracle, cockroachdb, yugabytedb, tidb, greenplum, singlestore, snowflake, bigquery, databricks, fabric,
    synapse, redshift, clickhouse, elasticsearch, pinot, druid, exasol, delta_lake, iceberg, hive, hive_s3,
    mongodb, cassandra, redis, firebird, duckdb, sqlite, openapi, graphql_remote, grpc_remote, govdata,
    kafka, neo4j, sparql, prometheus, rss, websocket, sharepoint, splunk, google_sheets, csv, parquet,
    files, airport, ingest) must have an integration or e2e test against a real (Docker or cloud-provisioned)
    instance. Unit tests that merely reference the type name do not satisfy this.'
  use_case: 'Ensures all datasource connectors work correctly in practice against real instances, not
    just as concept or identity-check in code. Covered with self-provisioning tests: direct-driver — mariadb,
    tidb, cockroachdb, yugabytedb, sqlserver, oracle, singlestore (license-gated skip); connector (federation
    engine → Trino catalog) — cassandra (reference pattern), exasol (arch-gated: amd64-only image), redshift
    + sharepoint (creds/instance-gated, no self-provision); DuckDB community extension — firebird, airport
    (live vs zaychik), google_sheets (opt-in gated: DuckDB gsheets extension SA-auth, PROVISA_GSHEETS_LIVE=1);
    Calcite/Trino — splunk (live, self-provisioned). Fixed a product bug found while covering exasol:
    Source.jdbc_url() omitted exasol/redshift, silently no-op''ing catalog creation. Filed issue #74 (zaychik
    Arrow-Flight auth/protocol gap, found via airport; the Provisa Flight→Trino transport itself is verified
    working — tests/integration/test_trino_flight_engine_e2e.py). The heavy multi-container analytics/lake
    ecosystems are now covered by self-provisioning e2e: pinot and druid (Trino catalog), hive and hive_s3
    (Trino hive connector + HMS, S3 on MinIO). kudu and accumulo were removed from SourceType — neither
    is a SQL-variant with a downloadable driver testable in this harness (Trino 481 ships no kudu/accumulo
    connector).'
  code:
  - provisa/core/models.py
  - provisa/federation/trino_connectors.py
  tests:
  - tests/integration/test_mariadb_source_e2e.py
  - tests/integration/test_tidb_source_e2e.py
  - tests/integration/test_cockroachdb_source_e2e.py
  - tests/integration/test_yugabytedb_source_e2e.py
  - tests/integration/test_greenplum_source_e2e.py
  - tests/integration/test_sqlserver_source_e2e.py
  - tests/integration/test_oracle_source_e2e.py
  - tests/integration/test_singlestore_source_e2e.py
  - tests/integration/test_cassandra_source_e2e.py
  - tests/integration/test_firebird_source_e2e.py
  - tests/integration/test_exasol_source_e2e.py
  - tests/integration/test_redshift_source_e2e.py
  - tests/integration/test_sharepoint_source_e2e.py
  - tests/integration/test_google_sheets_source_e2e.py
  - tests/integration/test_airport_source_e2e.py
  - tests/integration/test_splunk_source_e2e.py
  - tests/integration/test_pinot_source_e2e.py
  - tests/integration/test_druid_source_e2e.py
  - tests/integration/test_hive_source_e2e.py
  - tests/integration/test_hive_s3_source_e2e.py
  - tests/integration/test_trino_flight_engine_e2e.py
  unit_test: false
  unit_test_reason: This is an integration/e2e coverage requirement by definition — it mandates a real-instance
    integration or e2e test per SourceType; a unit test that only references the type name explicitly
    does not satisfy it.
  target: 2026-Q4
- id: REQ-1098
  status: complete
  since: 2026-07
  group: 11. Platform, Infrastructure & Delivery
  category: Native Tier Database
  priority: MUST
  type: structural
  description: The native (DuckDB) tier exposes the tenant control-plane as the `provisa_admin` catalog,
    enabling meta/ops entities (registered_tables, etc.) to be queryable with parity to the Trino tier.
    The catalog attaches the tenant SQLite DB READ_ONLY and exposes every table under the compiler-emitted
    schema `org_<id>` without a hardcoded table list.
  use_case: Provides full parity with Trino tier where provisa_admin is a real attached catalog. Allows
    native users to query system metadata and operational state via standard SQL, matching Trino/Presto
    multi-catalog semantics.
  code:
  - provisa/federation/duckdb_runtime.py
  - provisa/federation/native_backend.py
  tests:
  - tests/unit/test_duckdb_control_plane_attach.py
  relates_to:
  - REQ-979
- id: REQ-1099
  status: complete
  since: 2026-07
  group: 11. Platform, Infrastructure & Delivery
  category: Native Tier Database
  priority: MUST
  type: constraint
  description: The control-plane SQLite (platform + tenant) MUST run in WAL mode (journal_mode=WAL) with
    busy_timeout enabled. This is the concurrency guarantee that permits the native DuckDB engine to read
    the tenant DB READ_ONLY while aiosqlite writes config changes without transient "database is locked"
    errors.
  use_case: Enables concurrent read-write access to the control-plane SQLite without lock contention or
    stalling queries. Required for safe multi-client scenarios where the UI (aiosqlite) updates config
    while the native engine queries system state.
  code:
  - provisa/core/database.py
  tests:
  - tests/unit/test_admin_database.py
  relates_to:
  - REQ-1098
- id: REQ-1100
  status: complete
  since: 2026-07
  group: 11. Platform, Infrastructure & Delivery
  category: Desktop Installation
  priority: MUST
  type: infrastructure
  description: The native demo launcher MUST wait for the demo mock servers (petstore :18080, graphql
    :4000) to answer before starting the API. The launcher MUST gate the browser-open on the API's /health
    endpoint (served only after lifespan/config-load completes), not merely on the UI proxy port.
  use_case: Ensures the demo environment is fully initialized before opening the browser to the user.
    Prevents premature browser navigation to a not-yet-ready environment and reduces failed first-user
    experience.
  code:
  - packaging/windows/provisa-native.ps1
  tests:
  - tests/unit/test_desktop_install.py
  - packaging/windows/tests/provisa-native.Tests.ps1
  relates_to:
  - REQ-979
- id: REQ-1101
  status: complete
  since: 2026-07
  group: 11. Platform, Infrastructure & Delivery
  category: Desktop MCP Configuration
  priority: MUST
  type: infrastructure
  description: Native/desktop tier enables remote MCP server by default, bound to loopback (PROVISA_MCP_HOST=127.0.0.1),
    with the governed role pinned explicitly (native auth:none = one local admin). All settings overridable
    via environment variables.
  use_case: Provides secure local MCP connectivity for Claude Desktop on native installations without
    additional configuration, enabling tool use against local Provisa queries.
  code:
  - provisa/api/mcp/server.py
  - packaging/windows/provisa-native.ps1
  - scripts/provisa
  tests:
  - tests/unit/test_mcp_status.py
- id: REQ-1102
  status: complete
  since: 2026-07
  group: 11. Platform, Infrastructure & Delivery
  category: MCP Status Endpoint
  priority: MUST
  type: behavioral
  description: The MCP status endpoint returns a resolved, proxy-aware, editable connect URL derived from
    X-Forwarded-Host/Host headers plus PROVISA_MCP_PORT and /mcp path; PROVISA_MCP_EXTERNAL_URL overrides.
    Returns null when MCP is disabled.
  use_case: Enables UI to display and allow editing of the MCP connection URL, accounting for reverse
    proxies and external domains that the server cannot reliably infer.
  code:
  - provisa/api/mcp/status.py
  tests:
  - tests/unit/test_mcp_status.py
  scenario: 'Given MCP is enabled on the server with PROVISA_MCP_PORT=9999

    When the UI calls GET /api/mcp/status with X-Forwarded-Host: example.com

    Then the endpoint returns {url: "http://example.com:9999/mcp", enabled: true}

    '
- id: REQ-1103
  status: complete
  since: 2026-07
  group: 11. Platform, Infrastructure & Delivery
  category: Explore MCP UI Panel
  priority: MUST
  type: ui
  description: The Explore/MCP surface provides a "Connect Claude Desktop" panel that emits a copy-ready
    claude_desktop_config.json entry with URL prefilled and editable.
  use_case: Simplifies Claude Desktop setup by providing a complete, copy-paste configuration entry that
    users can add to their Claude Desktop config file.
  code:
  - provisa-ui/src/pages/McpExplorePage.tsx
  tests:
  - tests/unit/test_mcp_status.py
- id: REQ-1104
  status: complete
  since: 2026-07
  group: 11. Platform, Infrastructure & Delivery
  category: Claude Desktop Bridge
  priority: MUST
  type: infrastructure
  description: 'The Claude Desktop connector is Node-free: mcp-proxy (Python stdio-to-Streamable-HTTP
    bridge) is bundled into the native standalone runtime. The emitted config''s command references the
    bundled interpreter (PROVISA_MCP_BRIDGE_COMMAND). Feature is gated to native tier only; off-tier the
    panel degrades to "install mcp-proxy yourself". No npx, no Node, no user pip, fully airgapped.'
  use_case: Eliminates Node.js and npm as dependencies for Claude Desktop MCP connectivity in native installations,
    reducing distribution complexity and supporting offline/airgapped environments.
  code:
  - packaging/windows/build-sfx.ps1
  - packaging/macos/build-dmg.sh
  - pyproject.toml
  - provisa/api/mcp/status.py
  - packaging/windows/provisa-native.ps1
  - scripts/provisa
  tests:
  - tests/unit/test_mcp_status.py
  relates_to:
  - REQ-979
- id: REQ-1105
  status: complete
  since: 2026-07
  target: 2026-Q3
  group: 11. Platform, Infrastructure & Delivery
  category: MCP Authentication
  priority: MUST
  type: behavioral
  description: Wire resolve_token_role into the FastMCP Streamable HTTP transport to map remote bearer
    tokens to Provisa roles, enabling secure per-caller MCP authentication before exposing MCP off the
    loopback.
  use_case: Prerequisite for safely exposing MCP to remote callers with proper role-based access control
    and governance.
  scenario: 'Given the MCP Streamable HTTP transport bound off the loopback

    When a request arrives carrying an OIDC bearer token

    Then the token is resolved to a Provisa role via the same provider/claim mapping pgwire uses

    And that role governs the tool call for the duration of the request

    And a request with no bearer, or a token that resolves to no role, is rejected 401 (fail closed, never
    admin)

    '
  code:
  - provisa/api/mcp/server.py
  tests:
  - tests/unit/test_mcp_bearer_role.py
- id: REQ-1106
  status: complete
  since: 2026-07
  group: 11. Platform, Infrastructure & Delivery
  category: MCP Transport
  priority: MUST
  type: constraint
  description: MCP local-connector transport supports plain HTTP by default with optional TLS (PROVISA_MCP_TLS=1
    for public deployment). Per-machine self-signed certificates are generated on first start with OS
    user-store trust and http fallback if cert creation fails. When MCP binds non-loopback (0.0.0.0),
    DNS-rebinding Host-header protection is disabled (stdio bridges are not subject to browser-origin
    attacks); loopback binds retain strict protection.
  use_case: Enables local Provisa MCP servers to be reached by Claude Desktop via mcp-proxy stdio bridge
    without requiring bundled TLS. Supports optional TLS for publicly-exposed deployments while fixing
    421 Misdirected Request errors when clients connect via the machine's real IP (e.g. WSL VM IP).
  code:
  - provisa/api/mcp/server.py
  - provisa/api/mcp/tls.py
  - provisa/api/mcp/status.py
  tests:
  - tests/unit/test_mcp_status.py
  relates_to:
  - REQ-1104
- id: REQ-1120
  status: complete
  since: 2026-07
  group: 8. Client Access & Protocols
  category: Served Protocol Surface
  priority: SHOULD
  type: behavioral
  scenario: 'Given a DuckDB client with the airport community extension and a role

    When it ATTACHes Provisa (TYPE AIRPORT) and runs SELECT against a federated table

    Then governed rows stream back over the airport protocol via the shared query pipeline

    And row-level security is enforced server-side (deny-by-default when no session var)'
  description: Provisa serves the DuckDB Airport Arrow-Flight protocol as an outbound data-as-a-service
    surface, allowing external DuckDB clients with the `airport` community extension to ATTACH Provisa
    (TYPE AIRPORT) and query its governed federated catalog with server-side governance (RLS/masking)
    applied.
  use_case: DuckDB users can integrate Provisa's federated catalog directly into DuckDB workflows without
    separate ETL, leveraging DuckDB's in-process analytics while Provisa enforces row-level and column-level
    access control server-side. This peer protocol surface complements existing bolt (Neo4j), pgwire (Postgres),
    and Arrow Flight SQL surfaces.
  code:
  - provisa/api/airport/server.py
  - provisa/api/airport/wire.py
  - provisa/api/airport/query.py
  - provisa/api/airport/service.py
  - provisa/api/app_startup.py
  tests:
  - tests/integration/test_airport_service_e2e.py
  unit_test: false
  unit_test_reason: The airport protocol surface is a live-stack behavior (DuckDB airport extension ATTACH
    + governed streaming); it is verified end-to-end through the real DuckDB client against the running
    service, not by unit tests.
  target: 2026-Q4
  relates_to:
  - REQ-1097
- id: REQ-1121
  status: complete
  group: 8. Client Access & Protocols
  category: Query Pushdown & Optimization
  priority: MUST
  type: constraint
  description: The DuckDB airport extension delivers filter and projection pushdown via the endpoints
    DoAction parameters map (json_filters JSON expression tree + column_ids indices). The server translates
    these to semantic WHERE + projection, folds them into the DoGet ticket, and injects them through _govern_and_route
    so sources apply filters/projection with RLS/masking layered on.
  use_case: 'Pushdown minimizes network transfer and leverages source query optimization while maintaining
    governance invariants. Conformance rule: DoGet must return the FULL advertised schema; projected-out
    columns are null-filled client-side and DuckDB projects accordingly.'
  code:
  - provisa/api/airport/pushdown.py
  - provisa/api/airport/server.py
  tests:
  - tests/integration/test_airport_service_e2e.py
  unit_test: false
  unit_test_reason: Airport protocol behavior is verified end-to-end against the running service via the
    real DuckDB airport extension (live governed round-trip), not by unit tests.
  since: 2026-07
  relates_to:
  - REQ-1120
- id: REQ-1122
  status: complete
  group: 8. Client Access & Protocols
  category: Access Control & Projection
  priority: MUST
  type: constraint
  description: Airport catalog scan projects only registered semantic columns per role, never SELECT *.
    Unregistered physical columns (e.g. audit created_at) are neither leaked on read nor sent as NULL
    on INSERT.
  use_case: Ensures column-level access control is enforced at the protocol boundary and physical schema
    details remain hidden from federated consumers, preventing accidental data leakage or schema coupling.
  code:
  - provisa/api/airport/service.py
  - provisa/api/airport/query.py
  tests:
  - tests/integration/test_airport_service_e2e.py
  unit_test: false
  unit_test_reason: Airport protocol behavior is verified end-to-end against the running service via the
    real DuckDB airport extension (live governed round-trip), not by unit tests.
  since: 2026-07
  relates_to:
  - REQ-1120
- id: REQ-1123
  status: complete
  group: 8. Client Access & Protocols
  category: Transaction Management
  priority: SHOULD
  type: behavioral
  description: 'Airport transactions use a real in-memory coordinator (provisa/api/airport/transactions.py).
    create_transaction mints a UUID identifier; get_transaction_status reports active/committed/aborted.
    Best-effort read-committed isolation: each governed mutation auto-commits.'
  use_case: Provides transaction identity and status tracking for DuckDB clients performing multi-statement
    workflows against a governed federated catalog without requiring persistent transaction state across
    connections.
  scenario: 'Given a DuckDB client with airport extension connected to Provisa

    When the client calls create_transaction

    Then a unique transaction UUID is minted and returned

    And get_transaction_status reports the transaction as active

    And mutations within the transaction auto-commit with read-committed isolation

    '
  code:
  - provisa/api/airport/transactions.py
  - provisa/api/airport/service.py
  tests:
  - tests/integration/test_airport_service_e2e.py
  unit_test: false
  unit_test_reason: Airport protocol behavior is verified end-to-end against the running service via the
    real DuckDB airport extension (live governed round-trip), not by unit tests.
  since: 2026-07
  relates_to:
  - REQ-1120
- id: REQ-1124
  status: complete
  group: 8. Client Access & Protocols
  category: Mutation Enforcement
  priority: MUST
  type: constraint
  description: Governed DML (INSERT / UPDATE / DELETE) via do_exchange decodes the Arrow stream and submits
    semantic SQL through _compile_govern_execute (the single governed write pipeline), gated on engine
    writability (is_writable_on / writable.py). Never parallel writers. UPDATE/DELETE key rows by the
    table's PRIMARY KEY, advertised as the airport row identity (is_rowid pseudo-column filled with the
    JSON-encoded PK tuple), so the WHERE is formed only from PKs RLS already let the role read and is
    RLS-filtered again through the pipeline — a role can never mutate a row it cannot see (the attempt
    is a governed no-op).
  use_case: Ensures all mutations flow through the unified governance pipeline with consistent RLS/masking
    and audit enforcement, preventing bypass via direct insertion or uncontrolled parallel writes.
  code:
  - provisa/api/airport/service.py
  - provisa/api/airport/query.py
  - provisa/api/airport/server.py
  tests:
  - tests/integration/test_airport_service_e2e.py
  unit_test: false
  unit_test_reason: Airport protocol behavior is verified end-to-end against the running service via the
    real DuckDB airport extension (live governed round-trip), not by unit tests.
  since: 2026-07
  relates_to:
  - REQ-1120
- id: REQ-1125
  status: complete
  group: 8. Client Access & Protocols
  category: Protocol Compliance
  priority: MUST
  type: constraint
  description: 'Operations refused with a correct Flight protocol error (never a silent no-op). UPDATE/DELETE
    ARE supported via PRIMARY-KEY row identity: the table''s PK is advertised as the airport row identity
    (is_rowid pseudo-column filled with the JSON-encoded PK tuple) so DuckDB echoes it back on update/delete;
    the server forms a governed WHERE PK IN (...) submitted through the one write pipeline (RLS/masking/writable-ACL
    apply). They are refused ONLY per-table, when a table has no primary key (no safe row identity). create_table
    IS supported: it creates the physical table in the target domain''s single writable source (schema-mutation
    pipeline) and registers it, so the new table joins the catalog and accepts a governed INSERT; refused
    only when the domain has no writable source bound (or maps to more than one — ambiguous). column_statistics
    + table_function_flight_info are refused (governed federation has no precomputed stats and advertises
    no table functions). Physical column/struct ALTERs (add_column/rename_table/add_field/ …) are refused
    (they need the admin schema-mutation API''s source/governance context absent from the airport DDL
    payload). DDL create_schema/drop_schema map to Provisa domains via the control-plane domain repository.'
  use_case: 'Maintains protocol integrity and a consistent governance model: safe, governed operations
    (PK UPDATE/DELETE, create_table into a writable source) are served through the one pipeline, while
    operations that cannot be safely federated are rejected — preventing data inconsistency, uncontrolled
    mutations, or governance bypass.'
  code:
  - provisa/api/airport/server.py
  - provisa/api/airport/query.py
  tests:
  - tests/integration/test_airport_service_e2e.py
  unit_test: false
  unit_test_reason: Airport protocol behavior is verified end-to-end against the running service via the
    real DuckDB airport extension (live governed round-trip), not by unit tests.
  since: 2026-07
  relates_to:
  - REQ-1120
- id: REQ-1126
  status: complete
  since: 2026-07
  group: 11. Platform, Infrastructure & Delivery
  category: PyPI Distribution
  priority: MUST
  type: infrastructure
  description: 'Provisa ships as a pip-installable wheel via `pip install provisa[embedded]`. The wheel
    contains a fully functional embedded Provisa system (SQLite control plane + embedded DuckDB engine)
    ready to run immediately: `provisa run`. Supply-chain trust follows existing Artifactory-as-PyPI models
    already approved and CVE-scanned by regulated enterprises.'
  use_case: Airgapped/regulated enterprises already trust Artifactory-as-PyPI supply chains (whitelisted,
    CVE-scanned, hash-pinned via uv.lock) but cannot approve new registries (Docker Hub, notarization
    services, custom mirrors). Wheel distribution avoids new approval gates.
  code:
  - pyproject.toml
  - provisa/cli.py
  - scripts/build-wheel.sh
  target: 2026-Q4
  tests:
  - tests/e2e/test_pypi_packaging.py
  unit_test: false
  unit_test_reason: Wheel build/install is a packaging flow (python -m build artifact contents + console-script
    wiring), validated by packaging-marked tests deselected from the default lane, not by unit tests.
- id: REQ-1127
  status: complete
  since: 2026-07
  group: 11. Platform, Infrastructure & Delivery
  category: UI Packaging
  priority: MUST
  type: structural
  description: React UI is precompiled into the wheel. CI runs `vite build` (from provisa-ui/package.json),
    embeds the output into the package tree (e.g. provisa/_ui/), and declares it in [tool.setuptools.package-data]
    so `python -m build` includes it. ui_server.py serves from the packaged directory path rather than
    __file__.parent.parent/static, eliminating the need for npm/Node at runtime or post-install. The runtime
    config/ files are staged into provisa/_config/ the same way so the wheel is fully self-contained.
  use_case: Airgapped environments have no npm/Node installed. Packaging the precompiled UI into the wheel
    ensures a self-contained delivery that can be deployed to systems with only Python >=3.12.
  code:
  - provisa/ui_server.py
  - provisa/core/desktop_profile.py
  - provisa/pg_extensions/catalog.py
  - scripts/build-wheel.sh
  - pyproject.toml
  target: 2026-Q4
  tests:
  - tests/e2e/test_pypi_packaging.py
  unit_test: false
  unit_test_reason: UI/config packaging is verified against built wheel contents (python -m build artifact),
    a packaging-marked flow deselected from the default lane, not by unit tests.
- id: REQ-1128
  status: complete
  since: 2026-07
  group: 11. Platform, Infrastructure & Delivery
  category: Console Entry Point
  priority: SHOULD
  type: structural
  description: A `provisa` console entry point (provisa = provisa.cli:main) is added to [project.scripts]
    in pyproject.toml, allowing users to launch the system via the `provisa run` command at the shell
    without explicitly calling a module or uvicorn.
  use_case: Improves user experience and follows Python packaging conventions; users expect a single command
    (`provisa run`) rather than python -m invocations.
  code:
  - provisa/cli.py
  - pyproject.toml
  target: 2026-Q4
  tests:
  - tests/e2e/test_pypi_packaging.py
  unit_test: false
  unit_test_reason: The console-script wiring is a pyproject/entry_points declaration verified in the
    built-wheel packaging flow, deselected from the default lane.
- id: REQ-1129
  status: complete
  since: 2026-07
  group: 11. Platform, Infrastructure & Delivery
  category: Embedded Profile Dependencies
  priority: MUST
  type: constraint
  description: 'The [embedded] optional dependency extra pins SQLite control plane + embedded DuckDB engine
    (REQ-979: native/desktop tier). The bundled Trino JVM is omitted (PyPI cannot carry a JVM server).
    Full multi-engine federation is preserved by pointing to a customer-provided ("bring your own") external
    engine via TRINO_HOST/TRINO_PORT env or federation_engine_host/port config (already supported: provisa/federation/engine.py:1201,
    provisa/core/desktop_profile.py:153). Embedded DuckDB is the default when no external engine is configured.'
  use_case: Defines a complete, runnable tier for airgapped enterprises while preserving federation capability
    via bring-your-own external engines. Omitting the JVM avoids GPL licensing complications and PyPI
    size/upload limits.
  code:
  - provisa/federation/engine.py
  - provisa/core/desktop_profile.py
  - pyproject.toml
  target: 2026-Q4
  tests:
  - tests/e2e/test_pypi_packaging.py
  unit_test: false
  unit_test_reason: The [embedded] extra + bring-your-own-engine env are a packaging/config contract verified
    in the built-wheel packaging flow and embedded-env resolution test, deselected from the default lane.
- id: REQ-1130
  status: complete
  since: 2026-07
  group: 11. Platform, Infrastructure & Delivery
  category: Mirror Preparation
  priority: SHOULD
  type: constraint
  description: 'Python language pin: >=3.12,<3.13. Airgap Artifactory mirrors must pre-seed cp312 platform
    wheels for all native-wheel dependencies: duckdb, chdb, pyodbc, pyarrow, grpcio, cryptography, bcrypt,
    psycopg2-binary, snowflake-connector-python, etc. A requirements.lock and "mirror these wheels" checklist
    is delivered so the customer''s Artifactory admin can prepare in one pass.'
  use_case: Regulated enterprises run PyPI mirrors (Artifactory, Nexus) with whitelisted wheels. Pre-seeding
    the checklist avoids deployment surprises and allows the admin to verify all transitive deps are approved
    before go-live.
  code:
  - pyproject.toml
  - packaging/pypi/requirements-embedded.lock
  - packaging/pypi/MIRROR-CHECKLIST.md
  target: 2026-Q4
  tests:
  - tests/e2e/test_pypi_packaging.py
  unit_test: false
  unit_test_reason: Mirror preparation is deliverable artifacts (hash-pinned lock + checklist) validated
    for presence in the packaging-marked lane, not by unit tests.
- id: REQ-1131
  status: complete
  since: 2026-07
  group: 1. Access Governance & Security
  category: Column Visibility Control
  priority: MUST
  type: constraint
  description: A role with ADMIN capability bypasses per-column visible_to restrictions and always sees
    every column. The V003 column-visibility check in stage2.py implements this admin override.
  use_case: Ensures admins can fully inspect and manage the system without being restricted by user-facing
    visibility policies that apply to ordinary roles.
  code:
  - provisa/compiler/stage2.py
  tests:
  - tests/unit/test_stage2.py
  - tests/unit/test_governance.py
  scenario: 'Given an ADMIN-capability role and a table with columns restricted by per-role visible_to

    When the role queries the table

    Then all columns are returned regardless of visible_to restrictions

    '
- id: REQ-1132
  status: complete
  since: 2026-07
  group: 1. Access Governance & Security
  category: Semantic-Layer Meta Visibility
  priority: MUST
  type: behavioral
  code:
  - provisa/security/rights.py
  - provisa/compiler/stage2.py
  - provisa/core/models.py
  - provisa/core/schema_org.py
  - provisa/core/repositories/relationship.py
  - provisa/api/app.py
  tests:
  - tests/unit/test_meta_governance_visibility.py
  description: 'Metadata visibility is tiered by role. The meta domain exposes two column classes: CORE
    (table_name, column_name, data_type, description, alias, schema_name, domain_id, keys, relationship
    endpoints) and GOVERNANCE (visible_to, unmasked_to, writable_by, masking rules, view_sql, native_filter_type,
    scope). DEFAULT tier (every role): sees CORE meta for directly-accessible tables plus 1-hop relationship
    neighbors (user-defined/semantic relationships only, excluding synthetic catalog edges), bidirectional.
    Meta DOMAIN GRANT: sees CORE meta for all tables and relationships. Per-relationship opt-out suppresses
    target metadata from DEFAULT discovery.'
  use_case: Enables governed discovery over the reachable neighborhood without exposing data. Separates
    catalog structure knowledge (CORE) from governance configuration (GOVERNANCE), allowing each tier
    explicit control.
  scenario: 'Given a role that can query table t1 (connected via semantic relationship to t2 and t3)

    When querying registered_tables and table_columns

    Then sees CORE meta for t1, t2, t3; sees nothing for t4 (no relationship); sees no GOVERNANCE columns
    unless view_governance capability is granted

    '
- id: REQ-1133
  status: complete
  since: 2026-07
  group: 1. Access Governance & Security
  category: Domain Access Control
  priority: MUST
  type: constraint
  description: 'The `ops` domain behaves like any ordinary data domain: roles must be explicitly granted
    rights to access it. There is no implicit access to ops tables; admins bypass this via the admin capability
    override.'
  use_case: Prevents unintended exposure of operational/internal tables. Each role must be explicitly
    granted access according to their responsibility.
  code:
  - provisa/api/startup_seed.py
  tests:
  - tests/unit/test_ops_domain.py
  scenario: 'Given a role without explicit access to the ops domain

    When the role attempts to query ops tables

    Then the query is rejected; ops domain acts as a standard domain requiring explicit grants

    '
- id: REQ-1134
  status: complete
  since: 2026-07
  group: 1. Access Governance & Security
  category: Governance Column Visibility
  priority: MUST
  type: constraint
  code:
  - provisa/security/rights.py
  - provisa/compiler/schema_gen.py
  - provisa/compiler/stage2.py
  tests:
  - tests/unit/test_meta_governance_visibility.py
  description: The `view_governance` capability is required to view GOVERNANCE columns in the meta domain
    (visible_to, unmasked_to, writable_by, mask_type, mask_pattern, mask_replace, mask_value, mask_precision,
    view_sql, native_filter_type, scope). A role with the meta domain grant does NOT automatically see
    GOVERNANCE columns; view_governance must be granted independently.
  use_case: Separates discovery (CORE meta) from administration/audit (GOVERNANCE meta). Allows data stewards
    to see governance configuration without granting broad meta-domain access, and vice versa.
  scenario: 'Given a role with meta domain access but without view_governance capability

    When querying the meta domain

    Then sees CORE columns (table_name, column_name, data_type, etc.) but not GOVERNANCE columns (masking
    rules, role grants)

    Given the same role also granted view_governance capability

    When querying the meta domain again

    Then now sees both CORE and GOVERNANCE columns

    '
- id: REQ-1135
  status: complete
  since: 2026-07
  target: 2026-Q3
  group: 11. Platform, Infrastructure & Delivery
  category: Licensing
  priority: MUST
  type: behavioral
  code:
  - provisa/licensing/anchors.py
  - provisa/licensing/keys.py
  - provisa/licensing/machine_id.py
  tests:
  - tests/unit/test_licensing.py
  description: The installation records a tamper-evident first-use timestamp that survives reinstall.
    On first startup with no existing anchor, the current date is recorded as first_seen. The anchor payload
    {first_seen, machine_id} is Ed25519-signed and written to multiple locations outside the application
    install directory (e.g. ~/.provisa, an OS-native store, and a secondary marker). On every startup
    the system reads all anchors, takes the earliest (min) first_seen, and rewrites any missing anchor
    with that value. No network access is required — the mechanism is fully airgapped and time-based.
  use_case: Enables a 30-day trial clock that cannot be reset by uninstalling and reinstalling the application,
    without any license server or phone-home.
  scenario: 'Given a fresh installation with no existing anchor

    When the application first starts

    Then first_seen is set to today and written as a signed anchor to all anchor locations

    Given an existing installation whose app directory is removed and reinstalled

    When the application starts again

    Then the earliest surviving anchor''s first_seen is restored and the trial clock is not reset

    '
- id: REQ-1136
  status: complete
  since: 2026-07
  target: 2026-Q3
  group: 11. Platform, Infrastructure & Delivery
  category: Licensing
  priority: MUST
  type: constraint
  code:
  - provisa/licensing/monotonic.py
  tests:
  - tests/unit/test_licensing.py
  description: The trial-expiry evaluation is resistant to wall-clock rollback. The system persists a
    monotonic wall-clock high-water mark and evaluates elapsed time as max(now, high_water) - first_seen.
    The trial is considered expired when elapsed >= 30 days and no valid license is present. Setting the
    system clock backward does not extend the trial.
  use_case: Prevents a user from evading the nag by moving the machine clock backward.
  scenario: 'Given an installation whose first_seen was 31 days ago

    When the user sets the system clock back 10 days and starts the application

    Then max(now, high_water) - first_seen still exceeds 30 days and the trial is treated as expired

    '
- id: REQ-1137
  status: complete
  since: 2026-07
  target: 2026-Q3
  group: 11. Platform, Infrastructure & Delivery
  category: Licensing
  priority: MUST
  type: behavioral
  code:
  - provisa/licensing/nag.py
  - provisa/licensing/emit.py
  - provisa/licensing/state.py
  - provisa/api/app_startup.py
  - provisa/api/app.py
  - provisa/pgwire/server.py
  - provisa/bolt/session.py
  - provisa/grpc/server.py
  - provisa/api/flight/server.py
  - provisa/api/mcp/server.py
  tests:
  - tests/unit/test_licensing.py
  description: After the trial expires and no valid license is present, a self-contained nag message is
    surfaced on every access surface. It is emitted in the UI as a persistent shell banner, and through
    each served protocol's native non-fatal notice channel only (pgwire NoticeResponse, Bolt SUCCESS-metadata
    notifications, Arrow Flight app_metadata, gRPC trailing metadata, REST X-Provisa-License-Notice header
    plus warnings[] envelope, MCP notifications/message). The nag never modifies the result body, row
    stream, or any schema-typed field; if a protocol has no safe out-of-band channel the nag is skipped
    for that protocol. Emission is rate-limited to once per session/connection. The message states that
    the license is FREE and is used only to gather usage information because Provisa collects no telemetry,
    that the trial period has elapsed, lists the fields to send (company, position/title, role, first
    name, last name, email, phone optional), provides both the provisa.dev registration link and the license
    request email, includes the machine ID, and states how to apply the received license file. The nag
    never gates or degrades functionality.
  use_case: Nags the user into registering so end-user contact details are captured — the only means of
    gathering usage since Provisa ships with no telemetry. The license is free; nothing is charged or
    blocked.
  scenario: 'Given an expired trial with no valid license

    When a client connects over pgwire

    Then a single NoticeResponse carrying the license nag is sent and the query results are unaffected

    Given the same state

    When the UI loads any page

    Then a persistent license banner is shown in the shell chrome containing the machine ID, both contact
    routes, and the required fields

    '
- id: REQ-1138
  status: complete
  since: 2026-07
  target: 2026-Q3
  group: 11. Platform, Infrastructure & Delivery
  category: Licensing
  priority: MUST
  type: structural
  code:
  - provisa/licensing/license.py
  - provisa/licensing/keys.py
  tests:
  - tests/unit/test_licensing.py
  description: A license file is an Ed25519-signed token issued by provisa.dev (which holds the private
    key). Its payload contains company, position/title, role, first name, last name, email, optional phone,
    machine_id, and issued_at. The application embeds only the corresponding public key and verifies the
    license entirely offline. The license is a one-time registration capture and is the alternative to
    telemetry — applying it does NOT enable, start, or schedule any telemetry, usage reporting, or ongoing
    data collection. Provisa never phones home before, during, or after licensing.
  use_case: Allows offline verification of an authentic, installation-bound license without any runtime
    call to an issuing server. Registration at install time replaces telemetry entirely; there is no continuous
    collection.
- id: REQ-1139
  status: complete
  since: 2026-07
  target: 2026-Q3
  group: 11. Platform, Infrastructure & Delivery
  category: Licensing
  priority: MUST
  type: behavioral
  code:
  - provisa/licensing/license.py
  - provisa/licensing/machine_id.py
  - provisa/cli.py
  tests:
  - tests/unit/test_licensing.py
  description: A license file is applied via `provisa license apply <file>`, a UI upload in Settings →
    License, or by placing it at ~/.provisa/license.json. On apply the system verifies the Ed25519 signature
    against the embedded public key and checks that the license machine_id matches the local installation's
    stable machine_id (derived from hardware/OS identifiers, not the app directory, so it survives reinstall).
    A valid, matching license permanently silences the nag on all surfaces. A signature failure or machine_id
    mismatch is rejected and the nag continues.
  use_case: Lets a user remove the nag by applying the installation-bound license they received.
  scenario: 'Given an expired trial and a license file whose signature is valid and machine_id matches
    this installation

    When the user runs `provisa license apply <file>`

    Then the license is accepted and the nag no longer appears on any surface

    Given a license file whose machine_id does not match this installation

    When the user applies it

    Then it is rejected and the nag continues

    '
- id: REQ-1140
  status: complete
  since: 2026-07
  target: 2026-Q3
  group: 6. Execution, Routing, Caching & Performance
  category: Materialized View Governance
  priority: MUST
  type: constraint
  description: A materialized view may only be published if all relationships it depends on are approved.
    If the requesting user holds the necessary rights to create those relationships, the relationships
    are automatically created and approved; MV publication proceeds. If the user lacks the required rights,
    both the relationship creation and MV creation requests are submitted for approval, and MV publication
    is blocked until all relationships are approved.
  use_case: Ensures MVs cannot be published over unapproved relationships, preventing uncontrolled data
    lineage and access patterns. Distinguishes between users who can auto-provision (trusted data engineers)
    and those who must request approval (least-privilege roles).
  scenario: 'Given a user with relationship-creation rights attempting to publish an MV that depends on
    relationships X and Y

    When the user submits the MV publication request

    Then relationships X and Y are automatically created and approved, and the MV is published immediately

    Given a user without relationship-creation rights attempting to publish an MV that depends on relationships
    X and Y

    When the user submits the MV publication request

    Then both the relationship creation requests and the MV creation request are submitted as a single
    approval workflow

    And the MV is not published until all relationships have been approved

    '
  code:
  - provisa/api/admin/mv_relationship_gate.py
  - provisa/api/admin/schema_mutation_ops.py
  tests:
  - tests/unit/test_mv_relationship_gate.py
- id: REQ-1141
  status: complete
  since: 2026-07
  group: 6. Execution, Routing, Caching & Performance
  category: Caching
  priority: SHOULD
  type: behavioral
  description: 'A steward can mark an operational database source as load-sensitive and configure it for
    scheduled-refresh-only materialization, with a required constraint: at least one of three independent
    optional gates MUST be configured. The gates are off-peak window (a time range), refresh cadence (refresh_interval),
    and freshness probe (FreshnessMode.PROBE, checking for source change). The refresh fires on the AND
    of ONLY the configured gates — an unconfigured gate is vacuously true (ignored). Trigger clocks vary:
    window-only pulls once per window-open edge; cadence-only pulls every interval regardless of time;
    probe-only pulls on each evaluation if changed. Probe always gates further: if configured and returns
    unchanged, the scheduler resets its clock, zero data pull. The query path never touches the source:
    it serves the last snapshot via FreshnessMode.SCHEDULED (always fresh=true), decoupling query-path
    load from source access. REQ-1141''s contract is scheduler-driven refresh: without a configured gate,
    there is no scheduler discipline and the source is not load-protected. Boundary principle: MATERIALIZATION
    REQUIRES A REFRESH POLICY (a REQ-1141 gate OR a cache_ttl). Prefer_materialized + cache_ttl (no gates)
    is REQ-826 lazy read-through (query-triggered on staleness). Prefer_materialized + no gates + no cache_ttl
    splits on reachability: if the engine can ATTACH/VIRTUAL the source live, prefer_materialized is inert
    and the source degrades to live serving; if unreachable (_MATERIALIZE_ONLY type — APIs/NoSQL/streams
    with no live option), it is a load-once-then-frozen snapshot, only meaningful for static reference
    data or REQ-847 mutation-driven invalidation.'
  use_case: Lets a steward protect an operational database by serving it entirely from scheduled snapshots,
    with fine-grained control over refresh timing (maintenance windows, periodic intervals, or change-driven).
    The probe signal avoids wasted re-pulls when the source is unchanged. Query traffic never touches
    the source — only predictable background refresh traffic, bounded by configured gates and staleness
    by scheduler policy, not query load.
  code:
  - provisa/core/models.py
  - provisa/federation/scheduled_refresh.py
  - provisa/federation/freshness_gate.py
  - provisa/federation/scheduler.py
  - provisa/federation/read_through.py
  tests:
  - tests/unit/test_scheduled_refresh.py
  - tests/unit/test_scheduler_driver.py
  target: 2026-Q4
  scenario: 'Given a load_protected_policy: scheduled_refresh source with window-only config (off-peak:
    22:00-02:00 UTC, no cadence, no probe)

    When the off-peak window opens at 22:00

    Then the scheduler fires a refresh attempt (window-open edge trigger)

    And then idles until 02:00 when the window closes and opens again the next night


    Given the same source reconfigured with cadence-only (refresh_interval: 1h, no window, no probe)

    When the scheduler ticks every 1h at any time

    Then each tick drives a refresh attempt, regardless of local time


    Given the source with probe-only (no window, no cadence)

    When the scheduler continuously evaluates (e.g. on demand or via a background loop)

    And the probe reports unchanged (token == stored_token)

    Then the scheduler resets its clock without pulling, zero cost

    And when the probe reports changed (token != stored_token), a pull is triggered


    Given the source with window+cadence (window: 22:00-02:00, refresh_interval: 30m, no probe)

    When a cadence tick fires

    Then pull only if the tick landed inside the window


    Given the source with window+probe (window: 22:00-02:00, no cadence)

    When the off-peak window opens

    Then call the probe; pull only if it reports changed


    Given the source with all three (window, cadence, probe)

    When a cadence tick fires inside the window

    Then call the probe; pull only if changed


    When a query arrives at any time

    Then the query path serves the last materialized snapshot with FreshnessMode.SCHEDULED (always fresh=true)

    And the source never sees query-path traffic


    Given a source configured prefer_materialized: true, cache_ttl: 3600, with NO gates (no window, no
    cadence, no probe)

    Then this source is NOT a REQ-1141 scheduled refresh; it is REQ-826 lazy read-through

    And the query path triggers re-pulls on staleness (query-driven, not scheduler-driven)


    Given a source configured prefer_materialized: true, with NO gates AND NO cache_ttl

    Then MATERIALIZATION REQUIRES A REFRESH POLICY, so behavior splits on reachability:


    If the engine can ATTACH/VIRTUAL the source live (reachable):

    Then prefer_materialized is inert and the source degrades to live/VIRTUAL serving

    And no refresh policy = no scheduler discipline = live query-path access


    If the source is unreachable (_MATERIALIZE_ONLY type: APIs, NoSQL, streams, no live option):

    Then it is a load-once-then-frozen snapshot

    And it is only meaningful for static reference data or REQ-847 mutation-driven invalidation

    '
  competitive_position:
    status: ahead
    rationale: Denodo and Dremio cache for query latency and fall back to live source execution on cache
      miss or staleness. A declared load-protection policy combining optional independent gates (window,
      cadence, probe) with change detection — guaranteeing zero query-path load and snapshot-only serving
      with bounded staleness per configuration — is not a built-in feature in comparable products.
  related_reqs:
  - REQ-236
  - REQ-825
  - REQ-826
  - REQ-847
  - REQ-855
- id: REQ-1142
  status: complete
  since: 2026-07
  group: 11. Platform, Infrastructure & Delivery
  category: Embedded Tier Deployment
  priority: MUST
  type: behavioral
  description: DuckDB extensions (sqlite_scanner, postgres_scanner, iceberg, delta, community) required
    by the embedded engine are shipped offline via a separate PyPI package `provisa-duckdb-ext` (py3-none-any
    universal wheel) that embeds pinned, per-platform `.duckdb_extension` blobs. On embedded launch, the
    extensions are staged into a writable local directory without any call to extensions.duckdb.org. Missing
    extensions fail loud; silent network fallback is disabled.
  use_case: Behind enterprise firewalls where only PyPI/Maven/npm/NuGet are proxied (e.g. Artifactory),
    extensions.duckdb.org is unreachable, breaking the embedded tier and `provisa run --demo`. Offline
    delivery via PyPI—already reachable—ensures embedded deployments work in air-gapped environments without
    network access to DuckDB's extension registry.
  code:
  - provisa/federation/duckdb_extensions.py
  - provisa/federation/duckdb_runtime.py
  - packaging/duckdb-ext/
  tests:
  - tests/unit/test_duckdb_ext_staging.py
  scenario: 'Given an air-gapped environment with only PyPI proxy access

    And provisa[embedded] installed (which depends on provisa-duckdb-ext)

    When `provisa run --demo` launches the embedded DuckDB engine

    Then stage_bundled_extensions copies platform-specific .duckdb_extension blobs

    from the package into ~/.provisa/native/duckdb-ext (idempotent)

    And PROVISA_DUCKDB_EXT_DIR is set to that directory

    And the engine connects with extension_directory=PROVISA_DUCKDB_EXT_DIR

    and autoinstall_known_extensions=false

    And all required extensions load from local blobs without network access

    And a missing extension raises BundledExtensionsMissing, never a silent fallback

    '
- id: REQ-1143
  status: complete
  since: 2026-07
  group: 6. Execution, Routing, Caching & Performance
  category: Caching
  priority: SHOULD
  type: behavioral
  description: The table detail view shows a derived, human-readable refresh_policy_summary that expresses
    the effective refresh/serving behavior per (source, engine) pair, computed server-side from the same
    resolution logic (federate + freshness_gate + REQ-1141 gates) that the planner uses. The summary states
    the effective outcome (Live, Scheduled snapshot, or Lazy cache) and warns when prefer_materialized
    is inert or the table is accidentally frozen.
  use_case: A steward cannot decode six interacting config fields (prefer_materialized, gates, cache_ttl,
    cadence, freshness probe, off-peak window) to understand whether a table is served live, from a scheduled
    snapshot, or from a lazy cache. The server-derived summary, shown in one sentence, eliminates manual
    decoding and surface misconfiguration warnings at author time.
  code:
  - provisa/federation/policy_summary.py
  - provisa/api/admin/_refresh_summary.py
  - provisa-ui/src/pages/tables/TableEditForm.tsx
  tests:
  - tests/unit/test_policy_summary.py
  - tests/unit/test_refresh_summary_view.py
  target: 2027-Q1
  scenario: 'Given a steward viewing a table detail

    When the table detail renders at the top of the edit form

    Then a refresh_policy_summary is displayed, derived from

    federate(source, engine) resolution plus freshness_gate plus REQ-1141 gates

    And the summary shows one of: "served live" (VIRTUAL/SCAN reached directly),

    "refreshed [cadence] [window], only when changed; queries never touch the source"

    (load-protected snapshot), or "refreshed on access when older than [ttl]" (lazy cache)

    And misconfiguration warnings appear for: prefer_materialized with no gates/cache

    (reachable source → "inert on this engine — served live" | unreachable source →

    "frozen snapshot — loaded once, never refreshes")

    And the summary is recomputed per (source, engine) pair, showing different prose

    if reachability changes across engines

    '
  competitive_position:
    status: ahead
    rationale: Deriving a human-readable effective-policy summary from the real planner resolution (per
      source AND per engine), with author-time misconfiguration warnings, is not offered by Denodo, Dremio,
      or Hasura, which expose only raw cache configuration fields to the user.
  related_reqs:
  - REQ-1141
  - REQ-826
  - REQ-855
  - REQ-847
- id: REQ-1144
  status: complete
  group: 11. Platform, Infrastructure & Delivery
  category: Windows Native Launcher
  priority: MUST
  type: behavioral
  description: The Windows native launcher displays a visible WinForms splash screen (startup monitor)
    during first launch instead of running fully hidden. The monitor launches first-launch-native.ps1
    hidden, tails a progress breadcrumb file (%USERPROFILE%\.provisa\.startup-status), polls the /health
    endpoint for readiness, warms first-paint UI endpoints, then opens the browser and closes.
  use_case: The prior -WindowStyle Hidden chain showed nothing for tens of seconds during API startup
    and swallowed fatal staging errors to a hidden console, making failures appear as "nothing happened."
    A visible monitor surfaces startup progress and fatal errors immediately.
  code:
  - packaging/windows/startup-monitor.ps1
  since: 2026-07
  unit_test: false
  unit_test_reason: PowerShell WinForms UI automation requires Windows GUI environment; not reproducible
    in headless CI/cross-platform test runners. Behavior is verifiable via manual QA on Windows hosts.
  scenario: 'Given a user running the Windows native launcher for the first time

    When the launcher starts

    Then a WinForms splash screen appears immediately

    And the monitor process launches first-launch-native.ps1 hidden in the background

    And the splash reads .startup-status breadcrumbs in real-time

    And when /health becomes ready, the monitor warms the UI endpoints

    And once ready, the monitor opens the browser and closes


    Given a startup failure (e.g. staging error) written as ERROR to .startup-status

    When the monitor reads the ERROR breadcrumb

    Then the splash displays the error message and remains open

    And the user sees the exact failure instead of "nothing happened"

    '
- id: REQ-1145
  status: complete
  group: 11. Platform, Infrastructure & Delivery
  category: Windows Native Launcher
  priority: MUST
  type: behavioral
  description: The launcher surfaces startup progress via breadcrumbs. first-launch-native.ps1 and provisa-native.ps1
    write STATE|message lines (STAGING, CONFIG, DEMO, START, WAIT, ERROR) to %USERPROFILE%\.provisa\.startup-status
    for the monitor to display. Failed breadcrumb writes must never abort startup (best-effort).
  use_case: Breadcrumbs let the monitor and user track each stage of startup (staging runtime, configuring
    services, starting demo data, waiting for API, etc.), giving visibility into slow stages and enabling
    the monitor to display progress in real-time.
  code:
  - packaging/windows/first-launch-native.ps1
  - packaging/windows/provisa-native.ps1
  since: 2026-07
  unit_test: false
  unit_test_reason: PowerShell file I/O and breadcrumb state tracking requires Windows file system and
    process context; not reproducible in headless CI. Behavior is manual-QA verified on Windows hosts.
  scenario: 'Given a startup sequence beginning

    When first-launch-native.ps1 enters the staging phase

    Then it writes "STAGING|Extracting runtime..." to .startup-status

    And as each phase completes (CONFIG, DEMO, START, WAIT), a new breadcrumb is written


    Given a failed breadcrumb write (e.g. permission denied on .startup-status)

    When the launcher attempts to write

    Then the write failure is silently caught (best-effort)

    And startup continues without interruption


    Given an ERROR state

    When a fatal condition is detected (e.g. runtime staging failed)

    Then "ERROR|<message>" is written to .startup-status

    And the launcher halts gracefully

    And the monitor reads the ERROR and displays it to the user

    '
- id: REQ-1146
  status: complete
  group: 11. Platform, Infrastructure & Delivery
  category: Windows Native Launcher
  priority: MUST
  type: constraint
  description: Runtime upgrade must kill ALL processes holding the runtime before replacing it, including
    the demo mock servers tracked in .demo.pid, not just .native.pid. A leftover demo python.exe locked
    the runtime DLLs and made re-staging silently abort, pinning the install to its old runtime.
  use_case: Without killing all demo processes, a stale Python process holding the runtime directory prevents
    the upgrade from replacing DLLs, causing the installer to silently fail and continue using the old
    runtime. This constraint ensures clean, uninterrupted runtime upgrades.
  code:
  - packaging/windows/provisa-native.ps1
  since: 2026-07
  unit_test: false
  unit_test_reason: Process termination and file locking behavior requires Windows environment with actual
    DLL locks and process lifecycle control; not reproducible in CI. Verified via integration testing
    on Windows upgrade scenarios.
  scenario: 'Given a running Windows native install with runtime version 242

    And a background demo mock server (python.exe) holding a file lock on runtime DLLs

    When the upgrade to runtime 244 begins

    Then provisa-native.ps1 reads .demo.pid and .native.pid

    And kills ALL processes listed in both files

    And waits for process termination

    And only then replaces the runtime directory

    And the upgrade completes successfully to 244


    Given the old behavior where only .native.pid was killed

    When python.exe from the demo is still running

    Then DLL replacement silently fails

    And the install remains on runtime 242

    And the user observes no error (silent failure)

    '
- id: REQ-1147
  status: complete
  group: 11. Platform, Infrastructure & Delivery
  category: Windows Native Launcher
  priority: SHOULD
  type: behavioral
  description: When the startup monitor drives the launch (env PROVISA_STARTUP_UI=1), provisa-native.ps1
    must NOT open the browser itself — the monitor owns the readiness wait and browser open. This avoids
    a double-open race where both the monitor and the native script attempt to open the same URL simultaneously.
  use_case: A double-open race (monitor + native script both calling Start-Process browser) causes unpredictable
    behavior (duplicate tabs, missed readiness wait, flipped window focus). Centralizing browser open
    to the monitor ensures a single, coordinated open after confirmed readiness and UI warmup.
  code:
  - packaging/windows/provisa-native.ps1
  - packaging/windows/startup-monitor.ps1
  since: 2026-07
  unit_test: false
  unit_test_reason: Browser-open coordination between provisa-native.ps1 and startup-monitor.ps1 depends
    on PowerShell Start-Process and Windows process/env context; the double-open race is not reproducible
    in headless CI. Verified via manual QA on Windows hosts.
  scenario: 'Given env PROVISA_STARTUP_UI=1 (monitor is driving launch)

    When provisa-native.ps1 reaches the point where it normally opens the browser

    Then it skips the browser open (checks the env var first)

    And only waits for the monitor''s signal or /health readiness

    And the monitor, after confirming /health and warming UI endpoints, opens the browser once


    Given env PROVISA_STARTUP_UI not set (standalone native script launch)

    When provisa-native.ps1 reaches readiness

    Then it opens the browser directly (legacy behavior)

    And the user sees the application immediately

    '
- id: REQ-1148
  status: complete
  since: 2026-07
  group: 6. Execution, Routing, Caching & Performance
  category: Freshness / Change Detection
  priority: SHOULD
  type: behavioral
  scenario: 'Given a source with sentinel_path pointing at a zero-byte marker and change_signal=probe,
    probe_type=hash

    When the poll node evaluates and the marker''s mtime/size (or ETag/Last-Modified) is unchanged

    Then the freshness token equals the stored baseline and no data is pulled

    And when the producer touches the marker the token changes, so the node re-pulls from the source

    And a missing/unreachable marker yields a None token, degrading the node to its TTL cadence

    '
  description: 'A new freshness PROBE transport whose opaque token comes from a zero-byte SENTINEL marker
    rather than from scanning the data. Reuses the REQ-855 gate unchanged: freshness_token() -> str |
    None (provisa/events/probes.py:126) compared by equality; a new sentinel drop changes the token ->
    changed -> re-pull; unchanged -> keep. The sentinel lives at a configured path reachable over a file
    mount, HTTP, FTP, or SFTP; the token is exists+mtime+size for file/FTP/SFTP, or ETag/Last-Modified
    for HTTP -- the same HASH token shape files already use (provisa/events/probes.py:46-49). Adds a `sentinel`
    probe transport + a `sentinel_path` (with transport/credentials) config field; capability classes
    and the gate machinery are untouched. Selected via change_signal in {probe, ttl_probe} with probe_type=hash
    pointed at the sentinel.'
  use_case: Lets a steward detect upstream readiness from a published sentinel marker (a near-free poll)
    instead of scanning or querying the source -- and, combined with REQ-1141 load protection, the cheap
    sentinel poll replaces hammering the operational source while the heavy re-pull still defers to the
    off-peak window.
  code:
  - provisa/events/probes.py
  - provisa/events/sentinel_probe.py
  - provisa/events/boot.py
  - provisa/core/models.py
  tests:
  - tests/unit/test_sentinel_probe.py
  target: 2027-Q1
  competitive_position:
    status: ahead
    rationale: Marker-file readiness detection as a first-class freshness signal, decoupled from the data
      pull, is not a built-in in Denodo/Dremio/Hasura.
  related_reqs:
  - REQ-855
  - REQ-982
  - REQ-929
  - REQ-1141
- id: REQ-1149
  status: complete
  since: 2026-07
  group: 6. Execution, Routing, Caching & Performance
  category: Freshness / Change Detection
  priority: SHOULD
  type: behavioral
  scenario: 'Given a source with change_signal=signal, load-protected under REQ-1141

    When a data-less trigger arrives (a Kafka control message or an HTTP webhook), carrying no rows

    Then the change-trigger token for that source bumps, marking its snapshot stale

    And the load-protected scheduler treats the bumped token as a changed probe and re-pulls the

    rows from the source of truth on its own schedule (deferred to the off-peak window)

    And with no trigger the token is unchanged, so the scheduler pulls nothing

    '
  description: 'A new change_signal PUSH variant, `signal`, for a DATA-LESS external trigger (a Kafka
    control message on a topic, or an HTTP webhook) that says "the source changed / refresh now" WITHOUT
    carrying the rows. Distinct from the existing CDC push signals (kafka/debezium/native, provisa/core/change_signal.py:29)
    where the message CARRIES per-row events and lands as upserts (select_landing_shape -> CDC). A `signal`
    message instead INVALIDATES the materialized snapshot (marks it stale); the rows are then re-pulled
    from the SOURCE OF TRUTH on the next refresh -- it does not land the message. It sits between the
    axes: push-triggered timing, poll-style full/delta re-pull. Adds `signal` to the push-signal vocabulary
    + a receiver (kafka control topic / webhook endpoint) mapping a trigger to a staleness mark. Under
    REQ-1141 the signal is the change-detector and the load-protected scheduler still owns WHEN the heavy
    pull runs (deferred to the off-peak window) -- decoupling the cheap control signal from the data pull.'
  use_case: Lets an upstream system announce a change over Kafka/webhook without Provisa polling or the
    message carrying data; Provisa invalidates and re-pulls from the source on its own schedule -- ideal
    for load-protected operational sources (REQ-1141) where a control-plane signal must not translate
    into immediate source load.
  code:
  - provisa/core/change_signal.py
  - provisa/federation/change_trigger.py
  - provisa/federation/scheduled_refresh.py
  - provisa/federation/scheduler.py
  tests:
  - tests/unit/test_change_trigger_signal.py
  target: 2027-Q1
  competitive_position:
    status: ahead
    rationale: A data-less push trigger that invalidates-and-repulls, separate from CDC-carries-data,
      is not offered natively by competitors.
  related_reqs:
  - REQ-929
  - REQ-932
  - REQ-855
  - REQ-1141
- id: REQ-1156
  status: complete
  since: 2026-07
  group: 8. Client Access & Protocols
  category: Protocol Support
  priority: SHOULD
  type: behavioral
  description: 'Registered commands (tracked functions and webhooks -- REQ-205..208, REQ-242, REQ-885)
    MUST be invocable and discoverable across ALL client-facing protocol surfaces, not only the three
    that expose them today (GraphQL, pgwire SQL, REST). A command carries two orthogonal dimensions that
    every surface must honor: `kind` (query vs mutation) and set-vs-scalar return shape (`return_schema`
    / `returns = "schema.table"` -> set-returning / table-valued). Current state (verified): GraphQL exposes
    commands as query/mutation fields (provisa/compiler/actions_schema.py:183-185, wired at provisa/compiler/schema_gen.py:846-848);
    pgwire SQL invokes `SELECT fn(...)` / `SELECT * FROM fn(...)` and projects them into the pg_proc/routines
    catalog (provisa/pgwire/function_call.py:83-87, provisa/pgwire/function_catalog.py:67-90), but SQL
    invocation is gated by a SINGLE hook `maybe_invoke_registered_function` that lives ONLY in `execute_pgwire_sql`
    (provisa/pgwire/_pipeline.py:528); REST invokes via provisa/api/rest/registered_call.py:162-164. The
    following surfaces are DARK and MUST be lit: (1) MCP -- run_sql and any tool that runs SQL through
    `_govern_and_route`/`_execute_plan` (provisa/api/mcp/tools.py:270-272) bypasses the function hook,
    so even `SELECT fn(...)` does not invoke a command; expose commands as MCP tools and/or route MCP
    SQL through the shared function hook. (2) Arrow Flight -- ListFlights/catalog lists tables only (provisa/api/flight/catalog.py:87)
    and do_get runs SQL via `_govern_and_route` (provisa/api/flight/server.py:536-543), bypassing the
    hook; commands must be listed as Flights and invocable. (3) gRPC (Provisa''s OWN server) -- proto_gen
    emits only per-table Query{T}/Insert{T} RPCs (provisa/grpc/proto_gen.py:231-239) and never iterates
    si.functions; generate an RPC per command (query-kind -> unary/server-streaming for set returns, mutation-kind
    -> unary MutationResponse). (4) Cypher/Bolt -- no tracked-function path exists; APOC-style CALL is
    rejected (provisa/cypher/parser.py:864) and Bolt RUN routes only to _execute_cypher (provisa/bolt/session.py:284);
    expose commands via a CALL procedure surface (or equivalent) that reaches invoke_tracked_function.
    The single shared executor `invoke_tracked_function` (provisa/api/data/action_exec.py) MUST remain
    the one invocation path so per-mutation writable_by / governance (REQ-869) is enforced uniformly regardless
    of surface; the fix is to route each protocol''s function-call path through that executor rather than
    reimplementing dispatch.'
  use_case: A steward registers a command once and every client -- a GraphQL app, a psql / DBeaver session,
    a REST caller, an MCP-connected assistant, an Arrow Flight consumer, a gRPC service, and a Neo4j/Bolt/Cypher
    client -- can invoke and discover it with identical governance, instead of the command silently working
    on three surfaces and being invisible or a no-op on the rest.
  code:
  - provisa/api/data/action_exec.py
  - provisa/api/mcp/tools.py
  - provisa/api/mcp/server.py
  - provisa/api/flight/server.py
  - provisa/api/flight/catalog.py
  - provisa/grpc/proto_gen.py
  - provisa/grpc/server.py
  - provisa/cypher/parser.py
  - provisa/bolt/session.py
  - provisa/api/rest/registered_call.py
  tests:
  - tests/unit/test_command_surface_discovery.py
  - tests/unit/test_grpc_command_endpoint.py
  - tests/unit/test_bolt_command_call.py
  - tests/unit/test_proto_gen.py
  - tests/unit/test_tracked_function_invocation.py
  target: 2026-Q4
  scenario: Given a command `active_users` registered as a set-returning query-kind tracked function When
    it is invoked over MCP, Arrow Flight, gRPC, and Cypher/Bolt Then each surface returns the same governed
    row set and the command is discoverable in that surface's catalog/schema, with writable_by / governance
    enforced identically to the GraphQL, pgwire, and REST surfaces
  competitive_position:
    status: ahead
    rationale: Uniform command invocation with a single governed executor across GraphQL, SQL, REST, gRPC,
      MCP, Arrow Flight, and Cypher/Bolt from one registration is not offered by any competing federation
      or GraphQL-over-data product.
  related_reqs:
  - REQ-205
  - REQ-242
  - REQ-869
  - REQ-872
  - REQ-885
- id: REQ-1157
  status: complete
  group: 5. Query Languages, Compilation & Operations
  category: Materialized Views & Caching
  priority: SHOULD
  type: behavioral
  since: 2026-07
  description: 'A registered relation backed by `view_sql` (a Provisa-managed view or, with materialize=true,
    a materialized view -- REQ-885/REQ-543, provisa/core/models.py:496-498) is DERIVED, not a base table,
    and MUST be exposed query-only: the schema generator MUST NOT emit insert/upsert/update/delete GraphQL
    mutations for it, and raw-SQL surfaces (pgwire / Flight SQL / Bolt) MUST reject writes to it. Rationale:
    a write to a view either fails at the source (non-updatable view) or lands in the mv_cache snapshot
    that the next REQ-879 refresh silently overwrites -- data loss with no error, violating the project''s
    no-silent-failure rule. Implementation: fetch_tables carries `view_sql` (provisa/api/admin/db_queries.py:114),
    _build_visible_tables sets _TableInfo.read_only = bool(view_sql) (provisa/compiler/schema_types.py,
    provisa/compiler/schema_gen.py:_build_visible_tables), and the mutation-field loop skips read_only
    relations (provisa/compiler/schema_gen.py). The GraphQL read surface (query fields, aggregates, relationships)
    is UNCHANGED -- only the write surface is withdrawn. A base table is never affected (view_sql is null).
    Raw-SQL write-rejection: because every raw-SQL surface funnels through the one governed pipeline (REQ-1176),
    the guard lives in ONE place -- _reject_view_writes (provisa/pgwire/_pipeline.py), called from both
    _govern_and_route (pgwire / REST /data/sql / Flight SQL / MCP) and _govern_and_route_compiled (Bolt/Cypher
    / gRPC). It rejects any INSERT/UPSERT/UPDATE/DELETE/MERGE whose TARGET is a view_sql_map relation;
    a view read in the FROM/USING of a write, and all base-table writes, are unaffected.'
  use_case: A steward composes an MV over a Provisa-hosted function (REQ-885) and it appears in GraphQL
    for querying, but insert/update/delete mutations are no longer generated for it -- so a client cannot
    issue a write that would be lost on the next refresh. The relation reads like any table; it just cannot
    be written.
  code:
  - provisa/api/admin/db_queries.py
  - provisa/compiler/schema_types.py
  - provisa/compiler/schema_gen.py
  - provisa/pgwire/_pipeline.py
  tests:
  - tests/unit/test_view_readonly_writes.py
  - tests/unit/test_visibility.py
  scenario: Given a registered relation `daily_totals` whose view_sql materializes a Provisa-hosted function
    When a role introspects the GraphQL schema and issues SQL against it Then daily_totals exposes query
    fields (and aggregates) but NO insert/upsert/update/delete mutation, and a raw-SQL INSERT/UPDATE/DELETE
    against it is rejected rather than silently overwritten on the next refresh
  competitive_position:
    status: parity
    rationale: Read-only exposure of derived/materialized relations matches the expected behavior of mature
      data platforms; the value is preventing silent data loss.
  related_reqs:
  - REQ-885
  - REQ-543
  - REQ-879
  - REQ-032
- id: REQ-1158
  status: complete
  group: 11. Platform, Infrastructure & Delivery
  category: Package Distribution
  priority: MUST
  type: infrastructure
  since: 2026-07
  description: 'A universal py3-none-any wheel package `provisa-pg-ext` carrying the pinned per-platform
    PostgreSQL extension and FDW binaries, published to PyPI so the embedded tier acquires them via PyPI/Artifactory
    instead of github.com/kenstott/provisa/releases (unreachable behind PyPI/Maven/npm-only enterprise
    proxies). Payload (scripts/ci/build_pg_extensions.sh): core contrib file_fdw + postgres_fdw, external
    sqlite_fdw + mysql_fdw, and pg_duckdb — where pg_duckdb (csv/parquet/json + httpfs + iceberg via DuckDB)
    SUBSUMES the REQ-898 parquet_fdw / parquet_s3_fdw / pg_lake / pg_analytics functionality in one maintained
    extension rather than four heterogeneous build systems. Built by the `wheel` job in .github/workflows/build-pg-extensions.yml,
    which collects the per-platform build tarballs (packaging/pg-ext/collect.py merges them under provisa_pg_ext/_ext),
    builds ONE universal wheel (python -m build), and publishes to PyPI. Runtime discovery: packaging/pg-ext/provisa_pg_ext.ext_root()
    locates the bundle; provisa/pg_extensions/staging.py stage_bundled_pg_extensions() copies the running
    platform''s FDW modules into a pgserver pginstall and provisa/federation/fdw_artifact_catalog.py registers
    what landed (fails loud if the wheel lacks this platform''s bundle — no github.com fallback), keyed
    by the REQ-898 config/pg_extension_catalog.yaml inventory (provisa/pg_extensions/catalog.py). A pgserver
    configured with FDW sources thus loads the bundled modules via CREATE EXTENSION straight from the
    PyPI wheel — the offline analog of the DuckDB extension staging (REQ-1142). The FDW set IS the pgserver
    federation engine''s (PgFederationRuntime, provisa/federation/pg_runtime.py) REACHABILITY matrix:
    each FDW defines a reachable source type (postgres_fdw→postgres, sqlite_fdw→sqlite, file_fdw→csv,
    mysql_fdw→mysql, pg_duckdb→parquet/csv/json/httpfs/iceberg). Runtime staging is WIRED: provisa/core/control_plane_pg.py
    start() calls _stage_bundled_extensions() at embedded-pgserver boot, staging the wheel''s modules
    into the shared pginstall so the pg fed engine can CREATE EXTENSION them offline (best-effort: a BYO-Postgres
    tier without the wheel is a clean no-op — its FDWs come from the system PG, discovered via the STANDARD
    pg_available_extensions catalog, connector_duckdb._probe_pg_extension). Reachability is proven end-to-end
    per FDW through PgFederationRuntime.attach_source: file_fdw + postgres_fdw (test_embedded_pg_fdw_engine_e2e),
    sqlite_fdw (test_embedded_pg_sqlite_fdw_e2e), pg_duckdb csv/parquet (test_embedded_pg_duckdb_engine_e2e)
    and iceberg (test_embedded_pg_duckdb_iceberg_e2e), and the runtime directly (test_pg_runtime_e2e);
    mysql_fdw is best-effort (client lib), matching the CI smoke gate (scripts/ci/smoke_pg_extensions.py,
    wired in build-pg-extensions.yml, which CREATE EXTENSIONs each on a fresh pgserver). Declarative BYO
    custom-connector description (adding a source_type→attach mapping for a custom FDW / DuckDB ATTACH
    extension purely in config) is a SEPARATE requirement, not this packaging one.'
  use_case: Embedded tier (and enterprise deployments behind restricted proxies) can install PostgreSQL
    extensions offline via standard PyPI/Artifactory channels instead of fetching from unreachable GitHub
    releases.
  code:
  - packaging/pg-ext/
  - packaging/pg-ext/collect.py
  - scripts/ci/build_pg_extensions.sh
  - scripts/ci/smoke_pg_extensions.py
  - .github/workflows/build-pg-extensions.yml
  - provisa/pg_extensions/staging.py
  - provisa/pg_extensions/catalog.py
  - provisa/federation/fdw_artifact_catalog.py
  - provisa/core/control_plane_pg.py
  - provisa/federation/pg_runtime.py
  tests:
  - tests/unit/test_pg_ext_staging.py
  - tests/unit/test_pg_extension_catalog.py
  - tests/unit/test_control_plane_pg_staging.py
  - tests/integration/test_embedded_pg_fdw_engine_e2e.py
  - tests/integration/test_embedded_pg_sqlite_fdw_e2e.py
  - tests/integration/test_embedded_pg_duckdb_engine_e2e.py
  - tests/integration/test_pg_runtime_e2e.py
  competitive_position:
    status: neutral
    rationale: PyPI-distributed extension binaries enable standard enterprise packaging; the competitive
      dimension is deployment flexibility rather than feature parity.
  related_reqs:
  - REQ-1142
  - REQ-898
- id: REQ-1150
  status: complete
  group: 11. Platform, Infrastructure & Delivery
  category: Embedded CLI Startup
  priority: SHOULD
  type: behavioral
  description: 'The `provisa run` console command signals startup completion by polling the API /ready
    endpoint (the warm gate, not /health), prints a "✓ Provisa is ready — <url>" completion line, and
    opens the browser automatically with `?tour=1` for a `--demo` run (so the guided tour auto-starts).
    A new `--no-browser` flag opts out of the open while still printing the URL. Failures and 300s timeouts
    are best-effort and non-fatal: the servers keep running, and the URL is printed manually as a fallback.'
  use_case: Previously `provisa run` printed URLs then served forever with no completion signal and no
    browser open, so `--demo` appeared stuck even though the app was up. Readiness polling, completion
    line, and browser auto-open provide immediate visual feedback that the system is ready and reachable.
  code:
  - provisa/cli.py
  since: 2026-07
  unit_test: false
  unit_test_reason: Browser auto-open and readiness-poll behavior span the `provisa run` process, a live
    /ready endpoint, a 300s poll loop, and an OS browser invocation — no in-process seam to unit-test.
    Verified manually on a live launch (`provisa run --demo` / `--no-browser`).
  scenario: 'Given `provisa run` is launched with --demo

    When the embedded API and UI servers start

    Then _announce_ready polls /ready until 200 or 300s timeout

    And once /ready returns 200, "✓ Provisa is ready — http://127.0.0.1:3000/?tour=1" is printed

    And the browser opens to that URL automatically

    And the guided tour starts (App.tsx reads ?tour=1)


    Given `provisa run --no-browser` is launched

    When the servers become ready

    Then the completion line is still printed

    And no browser opens

    And the user can manually open the URL


    Given a failure during readiness polling (network error, etc.)

    When the failure occurs

    Then it is caught and silently logged

    And the URL is printed as a fallback

    And the servers keep running

    '
- id: REQ-1159
  status: complete
  group: 5. Query Languages, Compilation & Operations
  category: Extensible Functions
  priority: MUST
  type: behavioral
  description: 'Registered commands (tracked functions) MUST be composable INLINE within a larger SQL
    statement — e.g. `SELECT o.id, e.embedding FROM orders o JOIN enrich_cmd(''main.public.orders'') e
    ON o.id = e.id` — not only as a standalone top-level `SELECT * FROM fn(args)` / `SELECT fn(args)`,
    and this MUST work across ALL client-facing surfaces (GraphQL, pgwire SQL, REST, MCP run_sql, Arrow
    Flight, Provisa gRPC, Bolt/Cypher), routed through the one shared invoke_tracked_function executor
    and the shared _govern_and_route / _govern_and_route_compiled pipeline. Implemented as a command-localization
    rewrite pass (_localize_inline_commands in provisa/pgwire/_pipeline.py → provisa/executor/command_localize.py
    localize_commands / find_command_calls) inside the shared _govern_and_route pipeline. The standalone
    hook (provisa/pgwire/function_call.py maybe_invoke_registered_function) now returns None for any composed
    statement (joined/sub-queried), so it never mis-runs one command as the whole result; the localizer
    owns composition. Every raw-SQL surface gets it for free by routing through the one pipeline: pgwire,
    MCP run_sql, Arrow Flight SQL, and REST /data/sql. The drifted second pipeline (provisa/api/data/endpoint_dev.py::_compile_govern_execute)
    has been DELETED; REST /data/sql, airport writes, and table-profile now route through _govern_and_route/_execute_plan,
    with as_of (REQ-1163), __provisa__ view expansion, and govdata folded into the one pipeline. The single
    chokepoint is enforced: _govern_and_route mints an unforgeable provenance stamp on every plan and
    _execute_plan refuses any plan lacking one (tests/unit/test_governed_chokepoint.py). gRPC (typed proto
    → _govern_and_route_compiled, no raw-SQL entry), Bolt/Cypher (command invoked standalone via CALL,
    REQ-1156 — no inline-SQL syntax) and GraphQL (commands are generated fields, not raw SQL) have no
    raw-SQL inline-composition surface, so REQ-1159 is N/A there. The pass (a) detects every command call
    in the parsed tree, (b) executes each to a local relation typed by the command''s declared output
    dataset contract, substituting the call site via a size-adaptive mechanism (VALUES CTE below a row
    threshold, registered Arrow table above), (c) forces local federation execution (Route.ENGINE — a
    command cannot be pushed to a remote source), (d) applies governance at the correct DEFINER/INVOKER
    identity for the command''s input plus the caller''s governance for the outer SQL, and (e) validates
    rows in/out against the command''s declared input/output dataset schema (fail-loud). Prerequisite:
    a canonical per-dataset contract — one or more scalar (column_value) args plus one or more TYPED input
    datasets (result_set/table_ref args, each carrying an ordered relational field-list) and exactly one
    TYPED output dataset; canonical field-list is the source of truth (single GraphQL-scalar vocabulary),
    with JSON Schema and GraphQL SDL as edge projections (seed-in / render-out), not stored truth.'
  use_case: A steward composes a governed external transform directly in ordinary SQL joined against federated
    tables, on any surface, and — because the command declares and validates its input/output dataset
    columns — column-level lineage can close across the opaque RPC boundary via taint-closure splicing
    at the function node.
  code:
  - provisa/pgwire/_pipeline.py
  - provisa/pgwire/function_call.py
  - provisa/executor/command_localize.py
  - provisa/executor/function_dispatch.py
  - provisa/api/data/action_exec.py
  - provisa/api/data/endpoint_dev.py
  - provisa/api/mcp/tools.py
  - provisa/api/flight/server.py
  - provisa/processors/contract.py
  - provisa/core/models.py
  - provisa-ui/src/pages/commands/CommandFormFields.tsx
  - provisa-ui/src/api/actions.ts
  - provisa/api/admin/table_profile_router.py
  - provisa/api/airport/query.py
  tests:
  - tests/integration/test_command_compose_surfaces.py
  - tests/integration/test_command_compose_e2e.py
  - tests/unit/test_command_localize.py
  - tests/unit/test_mcp_server.py
  - tests/unit/test_governed_chokepoint.py
  - tests/e2e/test_transport_contract.py
  scenario: "Given a registered command with a typed input/output dataset contract\nWhen a client submits\
    \ SQL that composes the command inline (FROM cmd(args) e JOIN a source table)\nThen on every raw-SQL\
    \ surface (pgwire, REST /data/sql, MCP run_sql, Arrow Flight) the command is\n  localized to a typed\
    \ local relation via the one _govern_and_route/_execute_plan pipeline\nAnd the outer statement joins\
    \ against it and returns the enriched rows identically on each surface\nAnd the composed statement\
    \ is never handed to the federation engine as an unknown table function\nAnd no surface can bypass\
    \ the pipeline: _execute_plan refuses any plan lacking a top-minted stamp\n"
  since: 2026-07
  target: 2026-Q4
  related_reqs:
  - REQ-885
  - REQ-1156
  - REQ-862
  - REQ-940
- id: REQ-1160
  status: complete
  group: 1. Access Governance & Security
  category: Column-Level Lineage Graph
  priority: MUST
  type: behavioral
  description: 'From a SINGLE SQL statement — including one or more inline-composed commands (REQ-1159)
    — the platform MUST be able to produce a complete column-level lineage DAG for the entire view, from
    source inputs to final output columns, with command nodes as first-class, contract-validated (non-opaque)
    nodes in the graph. Computable STATICALLY from the view definition plus each command''s declared input/output
    dataset contract (no execution required); optionally reconciled with runtime refresh-version records
    (REQ-862 spans / input versions) to stamp which version flowed. Implemented as a full node+edge graph
    builder (provisa/lineage/graph.py build_column_graph / LineageGraph) that preserves intermediate relations
    (CTEs, subqueries, joins), alongside the pre-existing leaf-collapsed resolver (provisa/lineage/columns.py
    resolve_column_lineage, retained for the flat output->source view). Each inline command, once localized
    into a typed CTE/Arrow relation, contributes real edges: taint-closure by default (each declared output
    column derives from ALL declared input columns) or precise per-column edges when declared; command
    nodes are visually distinct opaque-transform boundaries tagged with the command name and its contract.
    Two grains from one graph: relation-level zoom-out and column-level drill-in. Surfaces (shipped):
    an API that takes a view/statement and returns graph JSON (nodes + edges) — POST /admin/lineage/graph
    (provisa/api/admin/lineage_router.py) — and a UI DAG visualization (provisa-ui/src/components/lineage/LineageDag.tsx,
    LineagePage.tsx); previously column lineage was only OTel span attributes emitted at MV refresh. Each
    transform (edge) MUST be NAMED and/or DEFINED, not left as an opaque SQL-expression string: the resolver
    decomposes each projection expression and matches every operation against the union vocabulary of
    SQL operators/standard functions (SQLGlot expression nodes), engine built-in scalar functions, and
    registered commands (projected into pg_proc/routines, each carrying its declared contract) — so e.g.
    lat,lng <- city, state is labeled as the chain "concat/+ (SQL op) -> geocode (command, contract as
    its def)". This is a sub-DAG inside the edge: the column-level graph states the derivation, and zooming
    into an edge reveals the named operation chain (expression-level command nodes reuse the same contract
    / taint-closure machinery). A transform matching no known op is itself a signal worth flagging.'
  use_case: A steward inspects the end-to-end derivation of any view's output columns — across SQL and
    opaque command boundaries alike — as a rendered DAG; the visualization doubles as a discipline mirror,
    since an over-declared "frankenstein" command renders as a wide fan-in node, making sloppy contracts
    visible.
  code:
  - provisa/lineage/columns.py
  - provisa/lineage/graph.py
  - provisa/lineage/versions.py
  - provisa/api/admin/lineage_router.py
  - provisa/pgwire/_pipeline.py
  - provisa-ui/src/components/lineage/LineageDag.tsx
  - provisa-ui/src/pages/LineagePage.tsx
  tests:
  - tests/unit/test_lineage_graph.py
  - provisa-ui/src/__tests__/LineagePage.test.tsx
  scenario: 'Given a view whose SQL projects output columns through CTEs, joins, and one inline-composed
    command

    When the column-lineage graph API is invoked for that view

    Then a node+edge DAG is returned preserving intermediate relations

    And the inline command appears as a first-class contract-validated node with taint-closure edges

    And each transform edge is named against the SQL-op / built-in / registered-command vocabulary

    '
  since: 2026-07
  target: 2026-Q4
  competitive_position:
    status: ahead
    rationale: Column-level, definitional (config-derived, not crawler-based), transparent through opaque
      inline transforms — versus peers offering table-level or externally batch-crawled lineage only.
  related_reqs:
  - REQ-1159
  - REQ-862
- id: REQ-1161
  status: complete
  group: 1. Access Governance & Security
  category: Federation-Wide Provenance Graph
  priority: MUST
  type: behavioral
  since: 2026-07
  description: 'The platform MUST be able to merge every per-view / per-statement column-level lineage
    DAG (REQ-1160) into ONE federation-wide provenance graph — spanning all registered views, MVs, and
    inline-composed commands across all sources — by unioning nodes on canonical resolved dataset identity
    + column name. Base source columns -> every derived column everywhere, with command boundaries transparent
    via their declared contracts. Node key = resolved dataset identity (physical/semantic, via the centralized
    dataset identity service, never ad-hoc string comparison — docs/dataset-handling-standards.md "Dataset
    Identity"): if two references to the same dataset do not resolve to one node, the merged graph fragments
    silently, which MUST NOT happen. Computed whole but RENDERED progressively: relation-level is the
    default view, column-level on drill-in, with focus/scoping tools required (ego-graph around a node,
    upstream-only / downstream-only slice, domain filter) — a literal full-graph render is unusable at
    federation scale. Built INCREMENTALLY: recompute a view''s sub-DAG on its definition change and union
    incrementally; never rebuild the entire graph. CYCLE handling is detect + CHARACTERIZE + surface,
    NOT auto-reject: a cycle that crosses a materialization boundary (an MV reading a prior snapshot of
    a downstream MV) is a LEGAL time-lagged feedback loop — the snapshot is the version boundary that
    makes it well-defined; a cycle with NO materialization boundary on it is the likely design error (no
    evaluation order, no stable value). The platform describes the distinction and surfaces it (in the
    graph and as a diagnostic) for operator judgment; it does not unilaterally reject. IMPLEMENTATION
    (provisa/lineage/merge.py, provisa/lineage/graph.py, provisa/api/admin/lineage_router.py): merge/union
    on canonical dataset identity (real-relation node keys, quoting normalized), base→derived stitching
    with transparent command-contract boundaries, cycle detect+characterize (feedback vs error), progressive
    slicing (ego-graph / upstream / downstream / depth / domain filter), and the GET /admin/lineage/federation
    API. Built INCREMENTALLY (build_federation_graph_incremental): each view''s parsed sub-DAG is memoized
    by its SQL, so a request recomputes ONLY the views whose definition changed and unions the rest from
    cache; an unchanged input returns the prior merged graph without re-unioning — the entire graph is
    never rebuilt from scratch.'
  use_case: An operator runs impact analysis (change a source column, see everything affected across every
    domain), traces any derived column back to its sources across the whole federation, and inspects circular
    definitions with enough context to judge whether a cycle is an intended time-lagged feedback loop
    or a design error.
  code:
  - provisa/lineage/merge.py
  - provisa/lineage/graph.py
  - provisa/api/admin/lineage_router.py
  - provisa-ui/src/
  tests:
  - tests/unit/test_lineage_merge.py
  - tests/unit/test_lineage_incremental.py
  - tests/unit/test_lineage_graph.py
  scenario: "Given multiple registered views/MVs whose per-statement column lineage DAGs share dataset\
    \ columns,\n  including a cross-view chain and a materialization boundary\nWhen the federation graph\
    \ is requested\nThen the per-view DAGs merge into ONE graph unioned on canonical dataset identity\
    \ (base source column\n  → every derived column, command boundaries transparent), cycles are characterized\
    \ as feedback (across\n  a materialized boundary) vs error, and it is sliceable by ego/upstream/downstream/depth/domain\n\
    And on a subsequent request only views whose SQL changed are re-parsed — the whole graph is never\n\
    \  rebuilt from scratch, and an unchanged input returns the prior merged graph\n"
  competitive_position:
    status: ahead
    rationale: A live, column-level, cross-source, command-transparent provenance map of the entire federation
      — versus catalogs offering only table-level or batch-crawled lineage.
  related_reqs:
  - REQ-1160
  - REQ-640
  - REQ-372
- id: REQ-1162
  status: complete
  group: 4. Source Connectors
  category: Materialization Store
  priority: SHOULD
  type: behavioral
  description: 'A materialized view''s time-travel capability is a property of its DEFINITION, guaranteed
    on any materializing substrate (Iceberg, PostgreSQL, DuckDB, ...). Maintenance is APPEND-ONLY — a
    version, once written, is never updated or deleted (no valid_to write-back), so it works on engines
    that cannot cheaply UPDATE a federated store. Each refresh appends one of two ways: `snapshot` appends
    the whole fresh dataset stamped with system time; `delta` appends only the rows the ENGINE computes
    as changed (anti-joins in INSERT ... SELECT) plus tombstones for removed keys — never folded row-by-row
    in Provisa. "As of" history is DERIVED at read time from the immutable append log (latest batch for
    snapshot; a ROW_NUMBER window per entity key for delta), so consumer-facing `as_of` semantics are
    identical across every substrate. On Iceberg each append is exactly one snapshot (REQ-844). Declared
    end to end via mv_bitemporal_mode / mv_bitemporal_key (TableInput -> core Table -> MVDefinition.bitemporal
    -> refresh_mv) with a UI control in TableEditForm. NOTE: exposing `as_of` as a query-language argument
    that routes bitemporal-MV reads through reconstruction is tracked as REQ-1163 (the consumer query
    surface, analogous to REQ-372 for sources).'
  use_case: Time-travel previously existed only on Iceberg; when Iceberg was unreachable the substrate
    degraded to replace/append/upsert, losing history. Append-only bitemporal materialization makes time-travel
    available on any substrate — the same definition retargets across engines without remodeling.
  code:
  - provisa/mv/bitemporal.py
  - provisa/mv/refresh.py
  - provisa/mv/models.py
  - provisa/api/admin/schema_common.py
  - provisa/core/schema_org.py
  - provisa/core/schema.sql
  - provisa/core/repositories/table.py
  - provisa/api/admin/schema_helpers.py
  - provisa/api/app_rebuild.py
  tests:
  - tests/unit/test_bitemporal.py
  - tests/unit/test_bitemporal_exec.py
  - tests/unit/test_bitemporal_pg.py
  - tests/unit/test_bitemporal_iceberg.py
  - tests/unit/test_bitemporal_refresh_e2e.py
  - tests/integration/test_bitemporal_http_e2e.py
  since: 2026-07
  scenario: 'Given a materialized view declared bitemporal (snapshot or delta) on an entity key

    When it is refreshed repeatedly as rows are inserted, updated, and deleted

    Then each refresh only APPENDS (no UPDATE/DELETE), reconstruction returns the correct state at any
    past point, snapshot and delta modes agree, and on Iceberg each refresh is exactly one snapshot

    '
  integration_test: required
  integration_test_reason: Covered — tests/integration/test_bitemporal_http_e2e drives the live app; registerTable
    (create) persists the config, rebuildSchemas reloads MVDefinition.bitemporal, refresh materializes
    the append log into an embedded-DuckDB store, and /data/sql reads it back.
  related_reqs:
  - REQ-372
  - REQ-844
  - REQ-874
- id: REQ-1163
  status: complete
  group: 4. Source Connectors
  category: Materialization Store
  priority: SHOULD
  type: behavioral
  since: 2026-07
  description: 'A bitemporal materialized view (REQ-1162) is READABLE through the query languages: consumers
    get current state by default and can travel in time. (1) Current-by-default — a bitemporal materialized
    view''s inline-expansion entry (state.view_sql_map, app_rebuild.py) is pointed at a reconstruction
    over the physical append log (bitemporal.py: view_read_sql), so a plain read returns the CURRENT reconstructed
    state (latest batch for snapshot; latest-version-per-business-key window dropping tombstones for delta),
    never the raw log. (2) Time travel — a request-level `X-Provisa-As-Of` header (validated to a safe
    SQL timestamp literal by parse_as_of; malformed → HTTP 400) applies to the whole query: _prepare_compiled
    overlays each bitemporal view''s expansion entry with an as-of reconstruction over its append log
    (as_of_view_map + view_read_sql(ts)), giving identical consumer-facing as-of semantics across every
    substrate. A whole-query as-of context (like a bitemporal system time) rather than a per-field arg
    keeps it out of the GraphQL schema. Analogous to REQ-372 (`as_of` for Iceberg/Delta SOURCES).'
  use_case: Without routing reads through reconstruction a bitemporal MV read would surface all historical
    versions instead of current state. Current-by-default makes bitemporal MVs usable; the X-Provisa-As-Of
    header adds opt-in, whole-query time travel.
  code:
  - provisa/api/app_rebuild.py
  - provisa/api/app.py
  - provisa/api/data/endpoint.py
  - provisa/api/data/endpoint_dev.py
  - provisa/mv/bitemporal.py
  tests:
  - tests/unit/test_bitemporal_exec.py
  - tests/unit/test_bitemporal_read_integration.py
  - tests/integration/test_bitemporal_http_e2e.py
  integration_test: required
  integration_test_reason: 'Covered — test_bitemporal_read_integration drives the real refresh -> log
    -> reconstruction -> expand_views -> engine chain (current + as-of, snapshot + delta), and test_bitemporal_http_e2e
    drives the LIVE app end to end: it materializes a bitemporal view into an embedded-DuckDB store and
    reads it through /data/sql current-by-default and via X-Provisa-As-Of (far-future -> current, pre-refresh
    -> empty, malformed -> HTTP 400).'
  scenario: 'Given a materialized bitemporal view that has been refreshed across several changes

    When it is queried with no header, then with X-Provisa-As-Of set to a past time

    Then the plain read returns current reconstructed state and the as-of read returns the state that
    was effective at that time; a malformed header is rejected with HTTP 400

    '
  related_reqs:
  - REQ-1162
  - REQ-372
  - REQ-844
- id: REQ-1164
  status: complete
  group: 4. Source Connectors
  category: Materialization Store
  priority: SHOULD
  type: behavioral
  since: 2026-07
  description: 'Declarative `entity` and `fact` shortcuts that LOWER to the existing MV / bitemporal /
    relationship primitives — the two constructs both a star schema and a Data Vault are built from, kept
    methodology-neutral. (1) `entity` — a keyed, deduplicated, optionally-historized projection of a source;
    lowers to a materialized view (view_sql projecting the key + attributes) with materialize=true, and
    when history is requested a bitemporal MV (REQ-1162): history=scd2 → mode=delta, history=snapshot
    → mode=snapshot, keyed on the entity key. Serves a star DIMENSION (SCD1/SCD2) and a Data Vault HUB+SATELLITE.
    (2) `fact` — join to entity keys, reduce to a declared grain, aggregate measures; lowers to a materialized
    view (SELECT grain + dimension FK columns + aggregated measures, GROUP BY grain+FKs) plus registered
    relationships to the entities. Serves a star FACT and a DV LINK (a degenerate fact of keys). The lowering
    is pure (a spec → the same registration a user would hand-write), so the whole warehouse retargets
    across engines as IR — the model GENERATES the star/vault, it does not decide the methodology (grain,
    conformance, SCD choice stay the modeler''s).'
  use_case: Star schemas and Data Vaults are compositions of a tiny primitive set (dimension/fact, hub/
    satellite/link). First-class entity/fact sugar lets a modeler declare them once and have Provisa generate
    the underlying portable MV definitions, instead of hand-writing each view_sql + flags.
  code:
  - provisa/mv/modeling.py
  tests:
  - tests/unit/test_modeling.py
  scenario: "Given an `entity` spec (entity key + attributes, optional history=scd2|snapshot) and a `fact`\
    \ spec\n  (grain + dimension FK refs + measures)\nWhen the specs are lowered\nThen the entity becomes\
    \ a materialize=true MV projecting key+attributes (bitemporal mode=delta for\n  scd2, mode=snapshot\
    \ for snapshot, keyed on the entity key) and the fact becomes an MV selecting\n  grain + FK columns\
    \ + aggregated measures grouped by grain+FKs, plus registered relationships to the\n  entities — the\
    \ same registrations a modeler would hand-write, portable across engines\n"
  related_reqs:
  - REQ-1162
  - REQ-1160
  - REQ-844
- id: REQ-1165
  status: complete
  group: 9. Live Data & Events
  category: Table Processor (Shared)
  priority: MUST
  type: constraint
  since: 2026-07
  description: 'The inline MV preprocess hook (REQ-957) is RESCOPED from a row transform (rows → rows)
    to a PREFLIGHT CHECK that returns a verdict (continue / abort / quarantine), not a mutated dataset.
    Row transforms are explicitly out of scope — they belong in SQL (engine pushdown) or external processors
    (REQ-940). Rationale: a gate does not mutate the landed set, so it does not feed the content hash
    (REQ-964), eliminating the justification for full in-memory materialization and the max_rows cap (SKIPPED_SIZE)
    that prevents scaling to large sources. Accepted preflight transports are SQL pushdown (for SQL-expressible
    checks, evaluated engine-side as probe queries) and Python+Arrow (for non-SQL checks, evaluated in-process
    with columnar batches). gRPC is out of scope — it only serves out-of-process/remote validators, which
    introduces unnecessary overhead (separate process, wire framing) disproportionate to a preflight gate''s
    simplicity and scale requirements. The Python preflight hook input contract is dict[str, pyarrow.RecordBatchReader],
    keyed by INPUT NODE NAME, providing lazy Arrow streams via FederationEngine.execute_engine_stream.
    This avoids full materialization of large inputs; the hook consumes batches lazily and short-circuits
    early. If an engine lacks EngineCapability.ARROW_STREAM and a preflight is declared, the system fails
    loud at wiring/boot time (no silent skip, no materialize fallback). The land/generate path remains
    unchanged, using independent streamed reads.'
  use_case: Rescoping from stateful row transform to stateless verdict gate simplifies the contract, improves
    memory efficiency, and allows evaluation to scale via engine pushdown (SQL assertions) or streaming
    short-circuit (non-SQL predicates) rather than buffering the entire dataset in memory before applying
    the gate. Providing lazy Arrow streams as input ensures the hook can evaluate large datasets without
    materializing them into Python dicts.
  scenario: 'Given an MV node with a Python preflight hook and an engine supporting ARROW_STREAM

    When the node is claimed and produce yields rows

    Then preflight evaluation occurs BEFORE landing: (1) if SQL-expressible,

    it is pushed down as a probe query (existing _probe_source_count pattern in

    provisa/mv/refresh.py) and evaluated engine-side without materializing to Python;

    (2) if not SQL-expressible, the hook receives dict[str, pyarrow.RecordBatchReader] keyed

    by MV input node names, evaluates batches lazily via FederationEngine.execute_engine_stream,

    short-circuits early, and emits a verdict without buffering; the verdict gates landing

    and re-posting. If ARROW_STREAM is unavailable and preflight is declared, boot fails.

    '
  code:
  - provisa/mv/preprocess.py
  - provisa/mv/preflight.py
  - provisa/mv/preflight_sql.py
  - provisa/mv/preflight_eval.py
  - provisa/mv/refresh.py
  - provisa/processors/arrow.py
  - provisa/events/handlers.py
  - provisa/events/boot.py
  - provisa/events/processor.py
  related_reqs:
  - REQ-957
  - REQ-964
  - REQ-940
  - REQ-939
  tests:
  - tests/unit/test_preflight_sql.py
  - tests/unit/test_preflight_verdict.py
  - tests/unit/test_preflight_refresh.py
  - tests/unit/test_processor_arrow.py
  - tests/unit/test_live_core_loop.py
  - tests/integration/test_preflight_streaming.py
  - tests/e2e/test_preflight_streaming_e2e.py
  - tests/features/REQ-1165-preflight-streaming.feature
  - tests/steps/steps_preflight_streaming.py
- id: REQ-1166
  status: complete
  since: 2026-07
  group: 9. Live Data & Events
  category: Materialization Store
  priority: SHOULD
  type: behavioral
  description: A materialized view can have a REPEATING CALENDAR TRIGGER attached that cuts a calendar-addressable
    version at each boundary (end-of-day, end-of-week, end-of-month, end-of-quarter, end-of-year, or custom
    nth-weekday recurrences). Each boundary is named with a stable window_id (e.g., 2026-Q1, 2026-W03).
    The calendar is a TRIGGER/ADDRESSING axis (when a version is cut + its stable identifier), orthogonal
    to the storage strategy used to preserve history. Full-snapshot-per-boundary is one option (expensive);
    delta append is preferred for most cases. Composes the calendar boundary trigger (REQ-961/962) and
    freshness-gated firing (per-input freshness contract).
  use_case: Time-series analytics and auditing require data captured at calendar milestones (e.g., end-of-month
    snapshots of customer state or materialized reports keyed by fiscal quarter). Calendar-addressed versions
    enable users to query "what was the state as-of the end of Q1 2026?" independent of the underlying
    storage mechanism.
  scenario: 'Given a materialized view with a repeating calendar trigger on a named boundary

    When each calendar boundary is reached and input freshness gates are satisfied

    Then a version is cut at that boundary with a stable window_id, and the version is addressable

    via that window_id in time-travel reads, regardless of whether the underlying storage is full

    snapshot or delta append

    '
  related_reqs:
  - REQ-961
  - REQ-962
  - REQ-1162
  - REQ-877
  - REQ-878
  - REQ-958
  - REQ-965
  - REQ-967
  code:
  - provisa/events/handlers.py
  - provisa/events/boot.py
  - provisa/mv/refresh.py
  tests:
  - tests/unit/test_periodic_snapshot_generate.py
  - tests/integration/test_periodic_snapshot_pipeline.py
  - tests/steps/steps_periodic_snapshot.py
- id: REQ-1167
  status: complete
  since: 2026-07
  group: 4. Source Connectors
  category: Materialization Store
  priority: MUST
  type: constraint
  description: 'A calendar-addressed TIME-TRAVEL materialized view MUST use one of the append-only or
    reconstructible storage strategies (bitemporal snapshot mode, bitemporal delta mode, row-level delta
    ledger with reconstruct_forward/reverse, or reconstructible PIT with accumulating append). A replace/overwrite
    MV cannot answer calendar-addressed as-of reads: each refresh overwrites the prior version, so only
    the current state exists. Snapshot (full dataset per boundary) vs delta (changes only) vs ledger vs
    reconstructible-PIT is a COST choice, not a capability choice: all four strategies enable time-travel
    reads, but differ in storage footprint and reconstruction latency. Note: snapshot ⟹ time-travel, but
    time-travel ⇏ snapshot. Preserved snapshots (REQ-983) are a distinct single-instant frozen form and
    are NOT a time-travel series.'
  use_case: Append-only and reconstructible-storage designs enable calendar-addressed versions to be queried
    across time. Replace MVs lose all previous versions on refresh, making time-travel impossible. The
    codebase supports four such strategies; the cost/latency tradeoff is a deployment choice.
  related_reqs:
  - REQ-1162
  - REQ-877
  - REQ-878
  - REQ-958
  - REQ-965
  - REQ-967
  - REQ-983
  code:
  - provisa/mv/bitemporal.py
  - provisa/events/handlers.py
  tests:
  - tests/unit/test_bitemporal_calendar.py
  - tests/integration/test_periodic_snapshot_pipeline.py
- id: REQ-1168
  status: complete
  since: 2026-07
  group: 9. Live Data & Events
  category: Calendar Boundary
  priority: SHOULD
  type: structural
  description: The Grain enum in provisa/events/calendars.py (daily/weekly/monthly/quarterly/annual) cannot
    express nth-weekday-of-period recurrences (e.g. "3rd Wednesday of each month"). An NthWeekday recurrence
    rule (weekday + ordinal 1..5 or -1/last) with occurrence math and a tiling window extends the grain
    vocabulary; parse_grain_spec resolves a nesting grain OR a recurrence spec ("3WE"/"LFR"), and window_for/next_boundary/PeriodicCalendar
    accept both.
  use_case: Business snapshots often occur on variable dates within a period (e.g., last business day
    of month, 3rd Friday for option expiry). The current fixed-grain enum is insufficient.
  code:
  - provisa/events/calendars.py
  tests:
  - tests/unit/test_live_temporal.py
  - tests/unit/test_periodic_snapshot_generate.py
  - tests/unit/test_bitemporal_calendar.py
- id: REQ-1169
  status: complete
  since: 2026-07
  group: 9. Live Data & Events
  category: Calendar Management
  priority: SHOULD
  type: structural
  description: Calendars are declarable through the admin surface and the calendar→MV binding is plumbed
    end to end. A calendar repository + createCalendar mutation + calendars query define named versioned
    calendars; the MV snapshot-schedule fields (mv_calendar/mv_grain/mv_allowed_lateness/ mv_expected_events/mv_business_day_grain)
    flow model→registry→event loop; and a collapsible Snapshot Schedule UI panel binds a calendar + grain
    (nesting or nth-weekday) to a materialized view, making the periodic-snapshot feature reachable without
    code changes.
  use_case: Without config-level calendar definition and UI binding, repeating snapshots cannot be declared
    by users without code changes. A user-facing config surface is essential for adoption.
  code:
  - provisa/core/repositories/calendar.py
  - provisa/api/admin/schema_query.py
  - provisa/api/admin/schema_mutation.py
  - provisa-ui/src/pages/tables/TableEditForm.tsx
  tests:
  - tests/unit/test_snapshot_schedule_wiring.py
  - tests/integration/test_periodic_snapshot_graphql_e2e.py
- id: REQ-1170
  status: complete
  group: 9. Live Data & Events
  category: Table Load Protection
  priority: SHOULD
  type: constraint
  since: 2026-07
  description: 'Load protection (REQ-1141 off-peak window) and snapshotting on the same table can conflict
    on timing: a snapshot boundary may fall outside the off-peak refresh window. The system emits a WARNING
    when both load protection and periodic snapshot(s) are configured on one table, alerting the user
    to potential misalignment between the protection window and snapshot schedule.'
  use_case: If a snapshot boundary coincides with a peak-hours-only window, the snapshot cannot fire.
    This warning surfaces the timing conflict so the user can adjust the protection window or snapshot
    schedule.
  code:
  - provisa/api/admin/schema_mutation.py
  scenario: 'Given a table with both off-peak load protection AND a periodic snapshot

    When the configuration is validated (e.g., during MV update)

    Then a warning is emitted if the snapshot boundary is incompatible with the off-peak window

    '
  tests:
  - tests/unit/test_load_protection_snapshot_conflict.py
  related_reqs:
  - REQ-1141
- id: REQ-1171
  status: complete
  since: 2026-07
  group: 9. Live Data & Events
  category: Calendar Boundary
  priority: SHOULD
  type: structural
  description: Snapshot-schedule grains extend to support the full RFC 5545 RRULE recurrence model (Outlook/iCalendar).
    RRuleRecurrence (a frozen dataclass with normalized RRULE string) is validated via python-dateutil.
    parse_grain_spec recognizes "RRULE:FREQ=..." or bare "FREQ=..." specs; window_for/next_boundary dispatch
    to dateutil.rrule-backed occurrence tiling yielding half-open [start,end) windows addressed by opening
    occurrence (e.g., "2026-02-18-M3WE"). INTERVAL phase anchors to civil epoch 2000-01-01 for query-independent
    tiling. Bounded rules (COUNT/UNTIL) are rejected; nesting grains and 4-4-5 fiscal calendars unchanged.
  use_case: Beyond nesting grains (daily..annual) and nth-weekday shorthand, users need full recurrence
    expressivity for complex business schedules (e.g., "every 3 weeks on Monday, until year-end"). RRULE
    standardizes this across Outlook, Google Calendar, and iCalendar ecosystems.
  code:
  - provisa/events/calendars.py
  - provisa-ui/src/pages/tables/RecurrenceBuilder.tsx
  scenario: 'Given a user configures a snapshot schedule with an RRULE grain (e.g., "FREQ=WEEKLY;INTERVAL=2;BYDAY=MO,FR")

    When the MV refresh cycle executes

    Then snapshot boundaries are calculated per the RRULE spec, yielding half-open windows tiled consistently

    And the system rejects RRULE specs with COUNT or UNTIL (unbounded requirement)

    '
  tests:
  - tests/unit/test_calendar_rrule.py
  related_reqs:
  - REQ-1168
  - REQ-1169
  e2e: true
  e2e_reason: 'RRULE parsing and occurrence tiling behavior is only verifiable end-to-end: UI RecurrenceBuilder
    emits RRULE string → server parses and registers via parse_grain_spec → event loop tiles occurrences
    and triggers snapshots on the computed boundaries.'
- id: REQ-1172
  status: complete
  since: 2026-07
  group: 5. Query Languages, Compilation & Operations
  category: Compiler & Schema
  priority: SHOULD
  type: behavioral
  description: Registered callable field names (tracked functions and webhooks) in GraphQL schemas now
    apply the active naming convention (apollo_graphql camelCase, hasura_graphql snake_case) at the emit
    site, matching table field name behavior. Previously callable fields retained their raw registered
    names regardless of the configured convention.
  use_case: Consistent naming across table fields and callable fields improves developer experience and
    ensures the GraphQL surface respects the configured naming convention uniformly.
  code:
  - provisa/compiler/actions_schema.py
  - provisa/api/app_loaders.py
  related_reqs:
  - REQ-1156
  - REQ-471
  - REQ-411
  - REQ-412
  scenario: 'Given a Provisa deployment with apollo_graphql (camelCase) naming convention active

    And a tracked function registered with name "add_pet"

    When the GraphQL schema is generated

    Then the callable field appears as "addPet" not "add_pet"

    And the reverse-lookup alias key routes "addPet" back to the "add_pet" command

    '
  tests:
  - tests/unit/test_callable_field_naming.py
- id: REQ-1173
  status: complete
  since: 2026-07
  group: 8. Client Access & Protocols
  category: Protocol Support
  priority: SHOULD
  type: behavioral
  description: 'Webhooks (tracked_webhooks from the remote schema registry) are now governed, discoverable
    commands routable across all protocol surfaces (pgwire, Bolt, Flight, REST, gRPC, MCP), extending
    REQ-872 from tracked_functions alone. A webhook: enforces writable_by authorization identically to
    tracked functions; appears in list_visible_commands discovery; is routed through the shared invoke_tracked_function
    executor (with a webhook-specific branch in invoke_tracked_webhook); and is invocable via SELECT webhook(...)
    on pgwire, CALL webhook(...) YIELD on Bolt, POST /{domain}/commands/{name} on REST, and as an MCP
    tool.'
  use_case: Webhooks (scalar-argument HTTP POST mutations) were previously GraphQL-only and lacked governance.
    Making webhooks governed, discoverable, and multi-surface-routable unifies command access patterns
    across all query surfaces and enforces consistent authorization.
  code:
  - provisa/api/data/action_exec.py
  - provisa/pgwire/function_call.py
  - provisa/bolt/session.py
  - provisa/api/rest/generator.py
  - provisa/api/rest/openapi_spec.py
  - provisa/grpc/proto_gen.py
  - provisa/grpc/server.py
  tests:
  - tests/unit/test_webhook_all_surfaces.py
  related_reqs:
  - REQ-872
  - REQ-1156
  scenario: 'Given a tracked webhook registered with name "notify_customer" in the schema registry

    When a pgwire client executes SELECT notify_customer(customer_id)

    Then the webhook routes through the shared invoke_tracked_function executor

    And writable_by authorization is enforced (403 if denied)

    And the HTTP POST fires to the registered URL with the arguments

    '
- id: REQ-1174
  status: complete
  group: 1. Access Governance & Security
  category: Query Complexity Limits
  priority: MUST
  type: constraint
  description: Per-role query complexity limits (max_query_depth, max_query_nodes, max_query_time_ms)
    complement per-role request-rate limits (REQ-369). Enforced at the GraphQL→IR compile boundary; over-limit
    requests return HTTP 413. Limits persist on the roles table and are configurable via admin API.
  use_case: Query complexity limits prevent resource exhaustion from complex or malicious queries, complementing
    request-rate limiting. Provides Hasura api_limits parity.
  code:
  - provisa/compiler/limits.py
  - provisa/api/models/role.py
  - provisa/admin/admin_api.py
  tests:
  - tests/unit/test_query_limits.py
  - tests/unit/test_hasura_api_limits.py
  - provisa-ui/e2e/security-query-limits.spec.ts
  since: 2026-07
  scenario: 'Given a role with max_query_depth=3, max_query_nodes=100, max_query_time_ms=5000

    When a GraphQL query is submitted that exceeds any limit

    Then the request is rejected with HTTP 413 at the GraphQL→IR compile boundary

    '
  competitive_position:
    status: parity
    rationale: Hasura provides api_limits (depth_limit, node_limit, time_limit) per role via api_limits.yaml.
      Provisa implements equivalent functionality in the admin GraphQL API, matching Hasura's governance
      model.
  e2e: true
  e2e_reason: 'Verification requires end-to-end validation: configuring limits via admin API (REST/GraphQL),
    then executing queries through the data endpoint to confirm 413 rejection when limits are exceeded.'
- id: REQ-1175
  status: complete
  group: 11. Platform, Infrastructure & Delivery
  category: Release Integrity
  priority: MUST
  type: constraint
  description: A GitHub Release must never be published in a partial state (missing any installer/download-link
    asset). All four tag-triggered workflows (build-dmg, release-exports, build-pg-extensions, build-duckdb-extensions)
    create the release as a DRAFT via ensure-release; sibling workflows attach assets only with gh release
    upload --clobber (never flipping draft/prerelease). Only build-dmg's publish-release job undrafts
    the release after verifying the complete installer manifest (macOS core/runtime/obs/demo DMGs, Linux
    AppImage, Windows native + container installers, JDBC jar) is present via fail_on_unmatched_files;
    missing assets cause immediate failure leaving the release draft.
  use_case: Prevents /dl redirector from serving incomplete builds. In alpha.257/258, a partial release
    was published when build-dmg failed while sibling workflows succeeded, causing end users to download
    missing installers.
  code:
  - .github/workflows/build-dmg.yml
  - .github/actions/ensure-release
  - site/functions/dl/[platform].js
  tests:
  - tests/unit/test_release_integrity.py
  since: 2026-07
  scenario: 'Given a tag is pushed triggering all four tag-triggered workflows

    When any workflow completes before build-dmg''s publish-release job

    Then assets are uploaded to a DRAFT release (not visible to /dl)

    And if build-dmg''s manifest verification fails

    Then the release remains DRAFT and is never published

    And /dl redirector ignores the draft, serving an older complete build

    '
- id: REQ-1176
  status: complete
  group: 1. Access Governance & Security
  category: Execution Integrity
  priority: MUST
  type: constraint
  description: All governed SQL execution flows through exactly one pipeline (_govern_and_route/_govern_and_route_compiled
    → _execute_plan) that mints unforgeable per-plan provenance stamps (256-bit process-private capability
    tokens). No surface may assemble its own govern+route+execute sequence. Plans lacking valid stamps
    are rejected before row access, making ungoverned data egress (RLS/masking/column-visibility/write-ACL
    bypass) mechanically impossible.
  use_case: Prevents side-door or hand-crafted execution plans from bypassing governance. A static AST
    ratchet test enforces that no NEW module co-locates decide_route + apply_governance, and runtime stamp
    validation rejects any plan without valid provenance.
  code:
  - provisa/pgwire/_pipeline.py
  tests:
  - tests/unit/test_governed_chokepoint.py
  - tests/e2e/test_transport_contract.py
  since: 2026-07
  scenario: 'Given a hand-crafted execution plan assembled outside _govern_and_route

    When the plan is submitted to _execute_plan without a valid provenance stamp

    Then the plan is rejected before any row is read, preventing RLS/masking bypass

    And given a plan routed through _govern_and_route with a valid stamp

    When submitted to _execute_plan

    Then the stamp is validated and execution proceeds with governance enforced

    '
  related_reqs:
  - REQ-1159
  - REQ-1163
  - REQ-272
  - REQ-825
- id: REQ-1177
  status: complete
  since: 2026-07
  group: 4. Source Connectors
  category: Connector Configuration
  priority: SHOULD
  type: behavioral
  description: 'Operators may declare custom source connectors (proprietary FDWs or ATTACH extensions)
    in config without code changes. A declarative descriptor per source_type specifies (a) required extension(s)
    to load, (b) the attach/import template, and (c) source_type mapping. Availability is verified at
    attach time against each engine''s standard extension catalog (pg_available_extensions for Postgres,
    duckdb_extensions() for DuckDB), failing loud (never silent) when the declared extension is not installable.
    The two native engines scope this differently because their extension APIs differ. POSTGRES is a GENERIC
    descriptor: PG FDW is SQL/MED (an ISO standard), so every FDW uses the same DDL shape — CREATE SERVER
    … FOREIGN DATA WRAPPER <fdw> OPTIONS(…) + CREATE USER MAPPING + IMPORT FOREIGN SCHEMA (or an explicit
    CREATE FOREIGN TABLE when the FDW lacks IMPORT, e.g. file_fdw). The descriptor supplies only the per-FDW
    variance: extension name, SERVER option keys (host vs servername vs dbserver), the USER MAPPING key
    (user vs username), and a supports_import flag. So an arbitrary standard-conforming FDW is drivable
    from config. DUCKDB is a CURATED UNION, not arbitrary: DuckDB has no single standard, so we support
    only extensions that fit one of the TWO mechanisms our engine already drives — (1) ATTACH-TYPE for
    database extensions (ATTACH ''<dsn>'' AS x (TYPE <t>), generalized today by _DuckDBExtensionConnector
    / REQ-899: INSTALL <ext> [FROM community] + LOAD + ATTACH), and (2) SCAN table-function for file/lake
    extensions (CREATE VIEW … AS SELECT * FROM read_parquet(…) / read_csv_auto(…) / iceberg_scan(…) /
    delta_scan(…)). The popular methods (postgres/sqlite/mssql/mongo/mysql; csv/parquet/iceberg/delta)
    are covered OOTB; the config admits a new DuckDB source_type only when it declares one of these two
    mechanisms (mechanism + TYPE-name or scan-function + connection/scan template). An extension exposing
    neither is not drivable and is unsupported by design — no bespoke per-extension code path.


    CONFORMANCE TARGETS (each is a real extension we do NOT ship an OOTB connector for, chosen to exercise
    a DIFFERENT descriptor branch than any hardcoded connector — passing them proves the descriptor is
    general, not a re-expression of what we already hardcode):

    (1) PG SQL/MED → mongo_fdw (EnterpriseDB/mongo_fdw). Reaches the mongo:7 service ALREADY in docker-compose.test.yml
    (seeded by db/mongo-init.js, requires_mongodb marker) — no new infra; only the FDW binary (libmongoc)
    is built. Descriptor: {extension: mongo_fdw, server_options:{address, port}, user_mapping:{username,
    password}, table_options:{database, collection}, supports_import: false}. Exercises the credentialed
    CREATE USER MAPPING path, a distinct SERVER key (address), NoSQL collection→foreign-table OPTIONS,
    and the explicit CREATE FOREIGN TABLE branch — none of which our OOTB PG connectors combine. Also
    proves cross-engine parity (mongo is reached OOTB by the DuckDB engine via DuckDBMongoConnector; here
    PG reaches it purely from config).

    (2) DuckDB ATTACH → ducklake (duckdb/ducklake). Self-contained: ATTACH ''ducklake:cat.ducklake'' AS
    lake (DATA_PATH ''data/'') over a local DuckDB/SQLite catalog + local parquet, no server. Exercises
    the URI-scheme-prefix DSN + extra ATTACH option (DATA_PATH) form, distinct from our stock ATTACH ''<dsn>''
    AS x (TYPE <t>).

    (3) DuckDB SCAN → excel / read_xlsx (duckdb/duckdb-excel). Self-contained: CREATE VIEW v AS SELECT
    * FROM read_xlsx(''f.xlsx'', sheet=''Q1'', header=true) over a local .xlsx, no server. Exercises a
    scan table-function with NAMED ARGS (sheet/range/header), distinct from our stock read_csv_auto/read_parquet.'
  scenario: 'Given an operator declares a custom source connector in config/custom_connectors.yaml with
    no code change

    When the descriptor is a duckdb_attach for a new source_type over a real ducklake catalog

    Then the DuckDB engine reaches that source_type and the runtime attaches it and returns its rows

    When the descriptor is a duckdb_scan for a new source_type over a real .xlsx via read_xlsx

    Then the DuckDB engine reaches that source_type and the runtime scans it in place and returns its
    rows

    When the descriptor is a generic pg_fdw for a new source_type

    Then the Postgres engine reaches that source_type and its connector emits standard SQL/MED IMPORT
    FOREIGN SCHEMA DDL

    And a descriptor naming an unknown kind fails loud at load rather than leaving the source_type silently
    unreachable'
  use_case: BYO PG-federated or DuckDB-federated deployments with custom/proprietary extensions can extend
    reachability to new source types purely via config, without code changes or rebuilds. Extends REQ-898
    config/pg_extension_catalog.yaml from a packaged-inventory model to an operator-authored connector
    descriptor.
  code:
  - provisa/federation/custom_connectors.py
  - provisa/federation/engine.py
  - provisa/federation/pg_runtime.py
  - provisa/federation/duckdb_runtime.py
  - config/custom_connectors.yaml
  tests:
  - tests/unit/test_custom_connectors.py
  - tests/integration/test_custom_connectors_e2e.py
  - tests/integration/test_custom_connectors_pg_e2e.py
  - tests/features/REQ-1177.feature
  related_reqs:
  - REQ-898
  - REQ-1158
  - REQ-1142
- id: REQ-1178
  status: complete
  since: 2026-07
  group: 4. Source Connectors
  category: Connector Configuration
  priority: SHOULD
  type: behavioral
  description: 'Expand ClickHouse federation engine reach beyond the 7 OOTB source types by (1) adding
    OOTB native engines — SQLite (DATABASE engine, file, no server) and Hudi (lakehouse, zero-copy) —
    and (2) enabling operators to define config-driven ClickHouse connectors without code, in three kinds
    mirroring the engine''s mechanisms: clickhouse_database (CREATE DATABASE ENGINE=… auto-expose, e.g.
    Redis), clickhouse_table (per-table CREATE TABLE ENGINE=…, columns from the registry, e.g. the JDBC/ODBC
    bridge), clickhouse_scan (CREATE TABLE ENGINE=… with ClickHouse inferring the schema, e.g. HDFS/URL).
    Availability is probed against ClickHouse''s STANDARD discovery catalog (system.table_engines), failing
    loud when a declared engine is absent from the build.'
  scenario: 'Given a ClickHouse federation engine

    When an operator registers a SQLite source (OOTB, no config)

    Then the engine reaches sqlite live and the runtime returns its federated rows

    When an operator declares a clickhouse_database/table/scan connector for a new source_type in config/custom_connectors.yaml

    Then the engine reaches that new source_type with no code change

    And an absent ClickHouse integration engine fails loud at attach time'
  use_case: ClickHouse's 50+ integration engines outreach today's OOTB types. Config-driven connector
    extensibility (parallel to REQ-1177) gives operators live federation to arbitrary databases without
    engineering. Streaming ingestion engines (Kafka/RabbitMQ/NATS) are out of scope — they follow the
    materialized-view ingestion model, not live pull reach.
  code:
  - provisa/federation/clickhouse_connectors.py
  - provisa/federation/clickhouse_runtime.py
  - provisa/federation/custom_connectors.py
  - provisa/federation/engine.py
  - config/custom_connectors.yaml
  tests:
  - tests/unit/test_clickhouse_connectors.py
  - tests/unit/test_custom_connectors.py
  - tests/integration/test_clickhouse_runtime_e2e.py
  - tests/features/REQ-1178.feature
  related_reqs:
  - REQ-1177
  - REQ-909
  - REQ-904
  - REQ-899
  - REQ-904
- id: REQ-1179
  status: complete
  since: 2026-07
  group: 4. Source Connectors
  category: Connector Configuration
  priority: MUST
  type: behavioral
  description: 'mongo_fdw (EnterpriseDB PG SQL/MED connector) reaches MongoDB live end-to-end as a conformance
    target for REQ-1177. The extension binary is built from scripts/build_mongo_fdw.sh (mongo_fdw + bundled
    libmongoc 1.30.2 and json-c linked against embedded PG 16.2). The E2E test installs the extension
    dylibs into pgserver''s embedded PG with @loader_path rpath so that mongo_fdw and libmongoc co-locate
    and resolve correctly. The descriptor (extension: mongo_fdw, server_options:{address, port}, user_mapping:{username,
    password}, table_options:{database, collection}, supports_import: false) drives config-only pg_fdw
    to read docker-seeded MongoDB.product_reviews without import.'
  scenario: 'Given the mongo_fdw extension is built and installed into pgserver''s embedded PG

    When a config-driven PG connector with extension: mongo_fdw targets a real MongoDB container

    Then the pgserver federation engine creates the foreign server with the address/port, emits

    CREATE USER MAPPING with username/password, and creates foreign tables over the specified database

    and collection

    And the engine returns federated rows from MongoDB without pre-importing schema'
  use_case: Proves that REQ-1177 config-driven descriptors generalize beyond hardcoded connectors. mongo_fdw
    exercises a distinct descriptor branch (credentialed, explicit CREATE FOREIGN TABLE, separate SERVER
    key, NoSQL collection options) not combined by any OOTB PG connector, validating descriptor composability
    and cross-engine parity (MongoDB is already reachable OOTB via DuckDB).
  code:
  - scripts/build_mongo_fdw.sh
  - provisa/federation/custom_connectors.py
  - provisa/federation/pg_runtime.py
  unit_test: false
  unit_test_reason: The requirement is that a BUILT mongo_fdw binary reaches a live MongoDB through embedded
    PG's SQL/MED layer. Every part of that — the build script's linkage against embedded PG 16.2, the
    extension loading, the federated round trip — exists only in a running stack, so a unit test could
    assert nothing beyond the descriptor text the e2e already renders and executes.
  tests:
  - tests/integration/test_custom_connectors_mongo_e2e.py
  related_reqs:
  - REQ-1177
  - REQ-1158
  - REQ-899
  e2e: true
  e2e_reason: Behavior is only verifiable end-to-end by building mongo_fdw, installing its dylibs into
    pgserver, seeding MongoDB, and running a full federation round-trip query.
- id: REQ-1180
  status: complete
  since: 2026-07
  group: 4. Source Connectors
  category: Connector Configuration
  priority: MUST
  type: constraint
  description: 'GenericPgFdwConnector (provisa/federation/custom_connectors.py) emits a bare `CREATE USER
    MAPPING ... SERVER s` with NO OPTIONS clause when user_mapping is present but empty (e.g., user_mapping:
    {}). If user_mapping is absent, no mapping is created. If keys are present, OPTIONS(...) is emitted.
    This constraint enables no-auth FDW scenarios (e.g., mongo_fdw against unauthenticated MongoDB), where
    an empty username would force the driver to attempt a failing auth handshake.'
  scenario: 'Given a GenericPgFdwConnector with user_mapping: {} (empty dict)

    When the connector compiles CREATE USER MAPPING SQL

    Then the SQL is `CREATE USER MAPPING FOR current_user SERVER s` with no OPTIONS clause

    Given a GenericPgFdwConnector with no user_mapping key

    Then no mapping is created

    Given a GenericPgFdwConnector with user_mapping: {username: ''u'', password: ''p''}

    Then the SQL is `CREATE USER MAPPING FOR current_user SERVER s OPTIONS (username ''u'', password ''p'')`'
  use_case: Unauthenticated FDW targets (no credentials needed) require the bare SQL/MED form to avoid
    triggering unnecessary auth. This constraint prevents driver-level failures when credentials are not
    applicable.
  code:
  - provisa/federation/custom_connectors.py
  tests:
  - tests/unit/test_custom_connectors.py
  related_reqs:
  - REQ-1177
  - REQ-1179
- id: REQ-1181
  status: complete
  since: 2026-07
  group: 11. Platform, Infrastructure & Delivery
  category: Terraform Cloud Deployment
  priority: SHOULD
  type: infrastructure
  description: Terraform GCP provisioning automatically stages release artifacts (AppImage and provisa-core-images-amd64-<version>.zip)
    from GitHub releases into the target GCS bucket before VM boot via a configurable null_resource.stage_artifacts,
    gated by a stage_from_github bool (default true). Requires authenticated gh and gsutil on the operator
    machine.
  use_case: Cloud deployments need release artifacts available in GCS before the VM starts. Automating
    the download and upload reduces manual operator steps and ensures artifact freshness for multi-region
    or repeated deployments.
  code:
  - terraform/gcp/main.tf
  - terraform/gcp/variables.tf
  target: 2026-Q3
- id: REQ-1182
  status: complete
  since: 2026-07
  group: 11. Platform, Infrastructure & Delivery
  category: Identity Provider Configuration
  priority: SHOULD
  type: behavioral
  description: Cloud deployments support automated identity-provider selection via an auth_provider Terraform
    variable (none|firebase|basic|keycloak|oauth|oidc). The variable is exported to the Compute Engine
    node as PROVISA_IDP, driving the server's env-based _auto_configure_idp logic. Firebase is a first-class
    option via firebase_project_id and firebase_service_account_key variables.
  scenario: 'Given a Terraform GCP deployment configured with auth_provider = "firebase"

    When the Compute Engine instance starts and PROVISA_IDP="firebase" is set

    Then the server automatically loads Firebase credentials from FIREBASE_PROJECT_ID and FIREBASE_SERVICE_ACCOUNT_KEY

    And the server initializes the Firebase IdP without manual config changes

    '
  use_case: Cloud deployments require identity providers to be configured at boot time. Environment-based
    auto-configuration enables one-shot, reproducible deployments without post-launch manual setup or
    Terraform output re-entry.
  tests:
  - tests/unit/test_idp_auto_configuration.py
  code:
  - terraform/gcp/variables.tf
  - terraform/gcp/main.tf
  - provisa/api/setup_router.py
  target: 2026-Q3
- id: REQ-1183
  status: complete
  since: 2026-07
  group: 11. Platform, Infrastructure & Delivery
  category: Linux Installation & Systemd Integration
  priority: MUST
  type: infrastructure
  description: The Linux first-launch installer persists all IdP-related environment variables (PROVISA_IDP,
    FIREBASE_PROJECT_ID, FIREBASE_SERVICE_ACCOUNT_KEY, KEYCLOAK_*, OAUTH_*) into a systemd EnvironmentFile
    at ~/.provisa/provisa.env, allowing the server running under systemd to resolve these variables for
    IdP auto-configuration without re-entry from the first-launch process.
  scenario: 'Given a non-interactive Linux cloud deployment with PROVISA_IDP and FIREBASE_* env vars set
    during first-launch

    When the first-launch installer completes and persists variables to ~/.provisa/provisa.env

    Then systemd starts the provisa service and the server resolves ${env:PROVISA_IDP} and ${env:FIREBASE_*}
    at runtime

    And the IdP auto-configuration succeeds without manual re-entry after system reboot

    '
  use_case: Cloud VMs running under systemd (not a user login shell) require environment variables to
    be persisted to a file readable by the systemd unit. This decouples the first-launch process from
    the long-running server.
  code:
  - provisa/cli/linux_installer.py
  - provisa/api/setup_router.py
  target: 2026-Q3
- id: REQ-1184
  status: complete
  since: 2026-07
  group: 11. Platform, Infrastructure & Delivery
  category: Linux Installation & Systemd Integration
  priority: SHOULD
  type: infrastructure
  description: Non-interactive (cloud) Linux deployments automatically enable and start the Provisa systemd
    service via `systemctl enable --now` instead of only installing the unit file. This ensures the server
    is running immediately after deployment without waiting for a manual start command or system reboot.
  scenario: 'Given a non-interactive Linux cloud deployment (no user login shell)

    When the installer completes

    Then systemctl enable --now provisa is called automatically

    And the server is running without manual intervention

    '
  use_case: Cloud deployments are unattended and need the server running immediately. Automatic enable
    and start reduces the gap between terraform apply and service readiness, eliminating the need for
    post-deployment SSH commands.
  code:
  - provisa/cli/linux_installer.py
  target: 2026-Q3
- id: REQ-1185
  status: in-progress
  since: 2026-07
  group: 6. Execution, Routing, Caching & Performance
  category: Result Handling & Streaming
  priority: MUST
  type: structural
  description: Engine-agnostic query results implement a ResultStream Protocol (column_names, column_types,
    batches(), iter_rows(), rows); two implementations separate fully-materialized QueryResult (bounded
    metadata/catalog paths) from lazy StreamingQueryResult (row batches, once-consumed iterator, stateless
    for shared-pool SaaS deployment).
  use_case: SaaS shared-pool load management requires small stateless Provisa instances behind a load
    balancer. In-process result materialization (fetchall) causes OOM on small pods when users request
    large result sets, so every result-bearing surface must stream and never materialize unbounded user
    data.
  code:
  - provisa/executor/result.py
  scenario: 'Given a QueryResult for bounded metadata (catalog, config, schema refresh)

    When the caller iterates .rows

    Then all rows are held in memory as a list[tuple] (fully materialized)

    And successive calls to .rows return the same list without re-execution

    Given a StreamingQueryResult for row-producing queries (user-facing engine output)

    When the caller iterates .batches() or .iter_rows()

    Then row batches are fetched lazily from a once-consumed iterator

    And the result object never buffers the full set in Provisa memory

    When .rows or .materialize() is called

    Then all buffered rows are collected into memory explicitly with user awareness

    When .rows or .materialize() is called a second time on the same StreamingQueryResult

    Then RuntimeError is raised (no silent replay of exhausted iterator)

    '
  related_reqs:
  - REQ-028
  target: 2026-Q3
- id: REQ-1186
  status: complete
  since: 2026-07
  group: 6. Execution, Routing, Caching & Performance
  category: Result Handling & Streaming
  priority: MUST
  type: behavioral
  description: pgwire ENGINE route streams the engine's result set lazily instead of materializing the
    full result in Provisa memory. ProvisaSession.execute_sql governs on the event loop via govern_pgwire_plan
    coroutine, then drains the engine's synchronous streaming terminal (execute_engine_sync) on the socketserver
    worker thread.
  use_case: Streaming prevents OOM on large result sets by deferring row materialization to the client's
    consumption rate. SaaS deployments with resource-constrained pods require all user-facing query results
    to stream unbounded data.
  code:
  - provisa/pgwire/session.py
  - provisa/executor/
  tests:
  - tests/integration/test_pgwire_integration.py
  - tests/integration/test_preflight_streaming.py
  - tests/e2e/test_preflight_streaming_e2e.py
  - tests/unit/test_pgwire_lazy_result.py
  related_reqs:
  - REQ-028
  - REQ-1176
  - REQ-1185
  scenario: 'Given a pgwire query on an ENGINE route

    When the query is governed and execution begins

    Then rows flow from the engine via DataRow messages without buffering the full result

    And the result streams at the client''s consumption rate via the socketserver worker thread

    When the client closes the connection mid-result

    Then the server terminates the engine cursor and cleans up without materializing remaining rows

    '
- id: REQ-1187
  status: complete
  since: 2026-07
  group: 6. Execution, Routing, Caching & Performance
  category: Result Handling & Streaming
  priority: MUST
  type: constraint
  description: ENGINE streaming branch calls require_governed_plan(plan) before the engine executes, ensuring
    a single chokepoint where the plan is stamped with governed-provenance guarantee (same contract Flight's
    streaming sink honors).
  use_case: Governance audit trail requires every executed plan to carry a deterministic provenance stamp.
    Single chokepoint enforcement prevents silent bypass of governance policy at the engine level.
  code:
  - provisa/executor/
  tests:
  - tests/integration/test_governance_integration.py
  - tests/unit/test_governance.py
  related_reqs:
  - REQ-1176
  - REQ-028
  scenario: 'Given an ENGINE route query with a compiled plan

    When the streaming executor is about to send rows to the engine

    Then require_governed_plan(plan) is called and verifies the plan is governed

    And execution proceeds only if the plan carries the provenance stamp

    When the plan is missing the stamp

    Then execution is blocked with a governance violation

    '
- id: REQ-1188
  status: complete
  since: 2026-07
  group: 6. Execution, Routing, Caching & Performance
  category: Result Handling & Streaming
  priority: SHOULD
  type: behavioral
  description: execute_engine_sync and backend execute_sync implementations accept a session_hints keyword
    parameter for per-plan Trino session properties, preventing silent loss of session-scoped directives
    (e.g., retry_policy=NONE for non-replayable sources) on the synchronous execution path.
  use_case: Trino session properties control engine behavior per query (e.g., retry_policy for Kafka sources).
    Without session_hints on the sync path, properties set at plan-governance time are silently dropped,
    causing non-deterministic behavior between async and sync execution paths.
  tests:
  - tests/unit/test_trino_session_hints.py
  code:
  - provisa/backends/
  - provisa/executor/
  related_reqs:
  - REQ-1186
  scenario: 'Given a Trino backend query with retry_policy=NONE in session properties

    When execute_engine_sync is called with session_hints on the sync path

    Then the Trino session properties are applied and not dropped

    When a non-replayable source (Kafka) fails mid-execution

    Then the engine respects retry_policy=NONE and does not retry

    When execute_engine_sync is called without session_hints

    Then session properties are ignored (native backends ignore them already)

    '
- id: REQ-1189
  status: complete
  since: 2026-07
  group: 6. Execution, Routing, Caching & Performance
  category: Result Handling & Streaming
  priority: MUST
  type: behavioral
  description: ProvisaQueryResult adapts a ResultStream (streaming or materialized) to the buenavista
    QueryResult ABC, pulling row batches lazily. When the engine supplies no per-column types, exactly
    one batch is buffered to infer wire types for RowDescription before DataRow transmission, never buffering
    the full result.
  use_case: pgwire RowDescription must precede DataRow and requires column type inference. Buffering only
    one batch instead of the full result minimizes memory overhead while providing complete type information
    to the client.
  code:
  - provisa/pgwire/
  - provisa/executor/result.py
  tests:
  - tests/integration/test_pgwire_integration.py
  - tests/integration/test_preflight_streaming.py
  - tests/e2e/test_preflight_streaming_e2e.py
  - tests/unit/test_pgwire_lazy_result.py
  related_reqs:
  - REQ-1185
  - REQ-1186
  scenario: 'Given a pgwire query result from an ENGINE route without pre-supplied column types

    When ProvisaQueryResult is created with the ResultStream

    Then exactly one batch is buffered to infer column types

    And RowDescription is sent to the client with inferred types

    And remaining rows are streamed lazily on demand

    When all result rows are consumed

    Then the ResultStream is exhausted and buffered batch is released

    When only partial results are consumed before client closes

    Then remaining rows are never fetched from the engine

    '
- id: REQ-1190
  status: complete
  since: 2026-07
  group: 6. Execution, Routing, Caching & Performance
  category: Result Handling & Streaming
  priority: MUST
  type: constraint
  description: DIRECT routes must stream for user-data scans via the source's own server-side cursor or
    Arrow reader, identically to ENGINE routes. Only metadata/admin/registered-function DIRECT routes
    (inherently bounded) materialize results via _execute_plan on the event loop. execute_native must
    return a lazy (schema, batch_gen) tuple, not a materialized QueryResult. See docs/arch/streaming-uniformity-gap.md
    (Defect 1).
  use_case: A DIRECT single-reachable-source passthrough (e.g., SELECT * FROM one_source) can be unbounded
    and must not materialize in Provisa RAM. Only bounded metadata queries (catalog, schema metadata,
    govdata, admin endpoints) benefit from materialization. Streaming the source's native cursor prevents
    OOM on large user-data scans.
  code:
  - provisa/executor/
  - provisa/graphql/
  tests:
  - tests/integration/test_catalog_integration.py
  - tests/integration/test_governance_integration.py
  - provisa-ui/e2e/governance-core.spec.ts
  - tests/unit/test_airport_streaming_terminal.py
  related_reqs:
  - REQ-1185
  - REQ-1186
  scenario: 'Given a DIRECT route query (catalog, config, schema refresh)

    When execute_plan is called on the event loop

    Then results are materialized in full via _execute_plan

    And the QueryResult is returned with .rows containing all rows

    When a subsequent call requests .rows

    Then the same materialized list is returned without re-execution

    Given an async GraphQL query for metadata

    When the query executes on the event loop

    Then materialization is accepted and results are cached in memory

    '
- id: REQ-1191
  status: proposed
  group: 7. Result Delivery
  category: Large Result Redirect & CTAS
  priority: SHOULD
  type: behavioral
  description: HTTP-addressable local materialization. Extend the existing local_dir file:// materialization
    to an http:// addressable serve endpoint (e.g. /data/redirect-file/<name>) so locally-materialized
    redirect results are downloadable over HTTP by a client, not only via desktop file:// paths.
  use_case: HTTP-addressable local materialization generalizes the Trino "engine writes the file, client
    receives only a URL" property to the embedded/no-object-store case, enabling web clients and remote
    consumers to retrieve materialized results.
  code:
  - provisa/executor/redirect.py
  target: 2026-Q3
  e2e: true
  e2e_reason: HTTP endpoint behavior and file serve correctness are only verifiable through a live server
    and HTTP transport.
- id: REQ-1192
  status: proposed
  group: 7. Result Delivery
  category: Large Result Redirect & CTAS
  priority: SHOULD
  type: constraint
  description: Per-username access scoping on served redirect files. Only the user who owns (created)
    a materialized redirect file may retrieve it. Authorization is derived from the requesting identity/bearer
    token per request, consistent with Provisa's stateless, token-derived governance.
  use_case: Per-username access scoping ensures users cannot retrieve redirect results materialized by
    other users, maintaining data isolation and governance.
  target: 2026-Q3
  scenario: 'Given a redirect file materialized by user A

    When user B requests the file via HTTP with their bearer token

    Then the request is rejected with 403 Forbidden

    '
- id: REQ-1193
  status: proposed
  group: 7. Result Delivery
  category: Large Result Redirect & CTAS
  priority: SHOULD
  type: behavioral
  description: Age-based reaping of local redirect files. Locally-materialized files are reaped after
    they exceed a maximum age (TTL). The S3 path already has cleanup scheduling; the local path needs
    an equivalent TTL reaper for long-running instances.
  use_case: TTL-based reaping prevents indefinite disk usage accumulation from materialized redirect files
    on long-running embedded or on-premise deployments.
  code:
  - provisa/executor/redirect.py
  target: 2026-Q3
  scenario: 'Given a locally-materialized redirect file created at time T

    When a cleanup job runs and current time exceeds T + TTL

    Then the file is deleted from disk

    '
- id: REQ-1194
  status: proposed
  group: 7. Result Delivery
  category: Large Result Redirect & CTAS
  priority: SHOULD
  type: structural
  description: Extend engine-native ctas_redirect beyond Trino to all object-store-capable engines. Databricks
    uses INSERT OVERWRITE DIRECTORY / CTAS to external volume; Snowflake uses COPY INTO <stage> FROM (query);
    ClickHouse uses INSERT INTO FUNCTION s3(...) SELECT ...; DuckDB uses COPY (query) TO 's3://...' via
    httpfs. Each engine implements its native result-to-object-store write and returns only the resulting
    URI; Provisa stays out of the data path.
  use_case: Engine-native CTAS to object stores removes Provisa from the materialization data path, enabling
    large result handling to scale with engine resources and object-store throughput rather than Provisa
    memory/bandwidth.
  code:
  - provisa/federation/backend.py
  target: 2026-Q3
  scenario: 'Given a large result query against Snowflake, Databricks, ClickHouse, or DuckDB

    When ctas_redirect is available and object store is configured

    Then the engine writes results directly to the object store and returns only the URI

    '
- id: REQ-1195
  status: proposed
  group: 7. Result Delivery
  category: Large Result Redirect & CTAS
  priority: SHOULD
  type: constraint
  description: Sink-tier selection rule. ctas_redirect targets the engine's native object-store sink when
    a reachable object store is configured (PROVISA_REDIRECT_ENDPOINT + credentials); otherwise it falls
    back to Provisa's local_dir + HTTP-serve (REQ-1191). For DuckDB, this fallback is a deployment gap
    (no guaranteed bucket), not a capability gap — configuring an object store promotes it to the native
    path. PostgreSQL core COPY cannot target object storage (extension required), so PG stays on the local/host-file
    tier absent an extension.
  use_case: Sink-tier selection ensures redirect results use the highest-fidelity, most scalable path
    available given the engine and deployment configuration.
  target: 2026-Q3
  scenario: 'Given PROVISA_REDIRECT_ENDPOINT is configured

    When a large result redirect is needed

    Then the engine-native CTAS to object store is used if available

    Given PROVISA_REDIRECT_ENDPOINT is NOT configured

    When a large result redirect is needed

    Then the local_dir + HTTP-serve fallback is used

    '
- id: REQ-1196
  status: accepted
  group: 7. Result Delivery
  category: Large Result Redirect & CTAS
  priority: SHOULD
  type: constraint
  description: Design decision — no Provisa-side byte-metering (Arrow buffer counting) and no compile-time
    cartesian product-budget control. Once materialization is engine-native, a cartesian/large-result
    blowup is bounded by the engine's own resource governors, the existing wall-time timeout, and engine-native
    CTAS, with Provisa never in the data path. The embedded in-process engine is desktop-dev / single-tenant
    only, so its lack of isolation is acceptable. The existing default row cap (100 rows unless full_results
    capability) remains the primary cheap safeguard against accidental full pulls.
  use_case: Deferring cartesian blowup control to engine-side governors (memory, CPU, concurrency limits)
    and timeouts keeps the Provisa result path stateless and avoids redundant metering. The default row
    cap provides sufficient protection for typical usage patterns.
  since: 2026-07
  scenario: 'Given a cartesian product that would produce 1M rows

    When executed against an engine with memory/timeout governors

    Then the engine terminates the query and Provisa never materializes the full result

    '
- id: REQ-1197
  status: complete
  group: 7. Result Delivery
  category: JSON:API Pagination
  priority: MUST
  type: behavioral
  description: JSON:API total_count (meta.total field for pagination) is computed by pushing a COUNT(*)
    aggregate down to the federated engine, not by materializing the full matching result set in Python
    and counting rows. Implementation wraps the governed count SELECT in `SELECT COUNT(*) AS total FROM
    (<compiled select>) AS _provisa_count`, routes it through the single governance pipeline (_govern_and_route_compiled
    + _execute_plan) so RLS/masking bind to the inner base tables, and reads the single scalar row.
  use_case: Pushing COUNT(*) to the engine avoids materializing large result sets in the API process and
    ensures RLS/masking rules bind correctly to base tables before aggregation. Part of the streaming
    initiative Phase 4 (cap/fix inherently-buffered transport formats).
  code:
  - provisa/api/jsonapi/generator.py
  tests:
  - tests/integration/test_jsonapi_integration.py
  - tests/unit/test_jsonapi.py
  - tests/e2e/test_jsonapi_meta_total.py
  since: 2026-07
  scenario: 'Given a registered table with RLS rules applied

    When a JSON:API paginated query requests total_count

    Then a COUNT(*) aggregate is pushed to the federated engine

    And the governing rules are applied to base tables before counting

    And Provisa receives a single scalar row with the total

    And no full result set is materialized in Python

    '
  e2e: true
  e2e_reason: COUNT(*) push-down behavior is only verifiable end-to-end through the API transport with
    actual governance rules applied and result materialization monitored.
- id: REQ-1198
  status: complete
  group: 11. Platform, Infrastructure & Delivery
  category: VM Cloud Deployment
  priority: MUST
  type: infrastructure
  description: VM cloud deploy runs rootful system Docker (Docker CE from Docker's official apt repo)
    with docker-compose-plugin for Compose v2, selected via PROVISA_DOCKER_MODE=system env var.
  use_case: Single-tenant VM is the isolation boundary; rootless daemon refuses to run as root; CLI requires
    docker compose v2.
  code:
  - packaging/linux/first-launch.sh
  - scripts/provisa
  since: 2026-07
- id: REQ-1199
  status: complete
  group: 11. Platform, Infrastructure & Delivery
  category: VM Cloud Deployment
  priority: MUST
  type: infrastructure
  description: Trino custom-connector jars ship as separate release asset provisa-trino-plugins-<version>.tar.gz;
    first-launch.sh extracts them into compose/trino/plugins/ so docker-compose.core.yml bind-mounts resolve.
  use_case: Slim AppImage stays under GitHub's 2 GB limit; without jars Trino crash-loops on startup ("No
    service providers of type io.trino.spi.Plugin").
  code:
  - packaging/linux/first-launch.sh
  - terraform/gcp
  since: 2026-07
- id: REQ-1200
  status: complete
  group: 11. Platform, Infrastructure & Delivery
  category: VM Cloud Deployment
  priority: MUST
  type: structural
  description: docker-compose.app.yml sets control-plane SQLAlchemy URIs TENANT_DATABASE_URL and PLATFORM_DATABASE_URL
    on the provisa service, built from PG_* vars for parity with Helm.
  use_case: Ensures database connectivity within the container network; aligns VM and Kubernetes deployment
    patterns.
  code:
  - docker-compose.app.yml
  - docker-compose.core.yml
  since: 2026-07
- id: REQ-1201
  status: complete
  group: 11. Platform, Infrastructure & Delivery
  category: VM Cloud Deployment
  priority: MUST
  type: infrastructure
  description: PLATFORM_DATABASE_URL has no fallback; app aborts at startup without it. TENANT_DATABASE_URL
    defaults to localhost.
  use_case: REQ-837 compliance; enforces explicit platform DB configuration and prevents silent misconfiguration
    inside container network.
  code:
  - docker-compose.app.yml
  since: 2026-07
- id: REQ-1202
  status: complete
  group: 11. Platform, Infrastructure & Delivery
  category: VM Cloud Deployment
  priority: MUST
  type: infrastructure
  description: GCP VM systemd unit for the stack is Type=oneshot with RemainAfterExit=yes.
  use_case: Ensures stack runs once at boot and persists in running state until explicit stop or reboot.
  code:
  - terraform/gcp
  since: 2026-07
- id: REQ-1203
  status: complete
  group: 7. Result Delivery
  category: Large Result Redirect & CTAS
  priority: SHOULD
  type: constraint
  description: Design decision — large-result redirect/materialize is an IR-level directive on the governed
    query plan (`_Plan.materialize` carrying a `provisa.executor.redirect.Delivery`), set by the one pipeline
    planners when a caller passes `deliver=`. The single execution terminal `_execute_plan` runs the shared
    sink terminal `provisa.executor.redirect.run_materialize` when the plan carries a materialize directive,
    returning a delivery handle on `QueryResult.redirect`. Planner forces ENGINE lowering when delivery
    is requested, ensuring the plan always carries engine-physical CTAS SQL. Sink-tier selection is centralized
    in `run_materialize` (REQ-1195), replacing the former transport-local CTAS branch; every transport
    that executes through the one pipeline now inherits redirect capability.
  use_case: Centralizing redirect through the one pipeline ensures uniform sink selection and result handling
    across all transports (HTTP /data, GraphQL, pgwire, Arrow Flight), eliminating per-transport CTAS
    branches while maintaining engine-native materialization when available.
  tests:
  - tests/unit/test_materialize_route.py
  - tests/unit/test_buffered_auto_threshold.py
  code:
  - provisa/pgwire/_pipeline.py
  - provisa/executor/redirect.py
  since: 2026-07
  scenario: 'Given a caller requests large-result delivery via deliver=

    When the one-pipeline planners (_govern_and_route or _govern_and_route_compiled) process the query

    Then a _Plan.materialize directive is set carrying sink-tier configuration

    And _execute_plan detects the directive and calls run_materialize

    And run_materialize selects the appropriate sink (engine-native CTAS or local fallback) per REQ-1195

    And the delivery handle is returned on QueryResult.redirect

    '
- id: REQ-1204
  status: proposed
  group: 7. Result Delivery
  category: Large Result Redirect & CTAS
  priority: SHOULD
  type: behavioral
  description: Buffered transports (GraphQL, JSON:API, Bolt) surface materialize/CTAS handles via protocol-native
    side-channels; streaming transports (pgwire streaming, Flight SQL, airport) opt out (deliver=None).
    Callers request materialization via transport-specific headers/metadata, translated to the shared
    _Plan.materialize IR directive by provisa.executor.redirect.delivery_from_request(...).
  use_case: Protocol-native side-channels preserve result data in response bodies while surfacing redirect
    handles separately, enabling async materialization for buffered transports while streaming transports
    skip materialization.
  code:
  - provisa/executor/redirect.py
  target: 2026-Q3
  scenario: 'Given a JSON:API caller sends X-Provisa-Redirect / X-Provisa-Redirect-Format / X-Provisa-Redirect-Threshold
    headers

    When the engine materializes the result

    Then the handle is surfaced in document top-level meta.redirect with data: null

    Given a Bolt caller sends RUN transaction metadata (provisa_redirect / provisa_redirect_format / provisa_redirect_threshold)

    When the engine materializes the result

    Then the handle is surfaced in trailing PULL SUCCESS metadata redirect key, with zero records streamed

    Given a streaming transport (pgwire, Flight SQL, airport) attempts materialization

    When delivery_from_request is called

    Then deliver=None silently opts out

    '
- id: REQ-1205
  status: complete
  superseded_by: REQ-1253
  group: 11. Platform, Infrastructure & Delivery
  category: Multi-Protocol Exposure (GCP)
  priority: MUST
  type: infrastructure
  description: GCP VM cloud deployment exposes each Provisa wire protocol through its own GCP External
    TCP passthrough NetLB (static IP + regional TCP backend service + HTTP-or-TCP health check + forwarding
    rule + instance-group named_port), driven data-driven from a single local.protocols map in terraform/gcp.
    Adding a protocol requires one row in the map.
  use_case: Data-driven protocol exposure allows adding new wire protocols to GCP deployments with a single
    config entry, avoiding per-protocol boilerplate NetLB resource duplication.
  code:
  - terraform/gcp
  since: 2026-07
  scenario: 'Given a configured protocol entry in local.protocols (pgwire, bolt, mcp, grpc)

    When terraform apply is run

    Then a dedicated External TCP NetLB is created with static IP and named instance group port

    And the NetLB exposes the protocol on its fixed client-expected port

    '
- id: REQ-1206
  status: complete
  group: 11. Platform, Infrastructure & Delivery
  category: Multi-Protocol Exposure (GCP)
  priority: MUST
  type: structural
  description: Terraform GCP interview booleans enable_pgwire, enable_bolt, enable_mcp, enable_grpc (all
    default true) and mcp_role (default admin) control which optional wire-protocol listeners are started
    on the instance. API (port 8000, HTTP /health probe), Arrow Flight (port 8815), and Web UI (port 3000)
    are always exposed; pgwire (5439), Bolt (7687), MCP (8009), and gRPC (50051) are opt-in via interview
    flags.
  use_case: Boolean interview flags give operators fine-grained control over which protocols are exposed
    on a deployed instance without modifying terraform code.
  code:
  - terraform/gcp
  since: 2026-07
  scenario: 'Given enable_pgwire=false in terraform interview

    When the instance starts

    Then the pgwire listener is not started

    And no NetLB forwards to pgwire port 5439

    Given enable_pgwire=true (default) in terraform interview

    When the instance starts

    Then the pgwire listener is started on port 5439

    And a NetLB forwards traffic to port 5439

    '
- id: REQ-1207
  status: complete
  group: 11. Platform, Infrastructure & Delivery
  category: Multi-Protocol Exposure (GCP)
  priority: MUST
  type: infrastructure
  description: Terraform base_startup script exports enabled wire-protocol listener environment variables
    to the instance (PROVISA_PGWIRE_PORT, PROVISA_BOLT_PORT, PROVISA_MCP_PORT, PROVISA_MCP_HOST, PROVISA_MCP_ROLE,
    GRPC_PORT) based on which protocols are enabled via interview booleans.
  use_case: Exporting protocol configuration as environment variables allows first-launch.sh and the application
    to discover which protocols are enabled at startup time.
  code:
  - terraform/gcp
  since: 2026-07
  scenario: 'Given enable_pgwire=true and enable_bolt=false in terraform interview

    When terraform base_startup runs

    Then PROVISA_PGWIRE_PORT=5439 is exported

    And PROVISA_BOLT_PORT is not set or empty

    '
- id: REQ-1208
  status: complete
  group: 11. Platform, Infrastructure & Delivery
  category: Multi-Protocol Exposure (GCP)
  priority: MUST
  type: infrastructure
  description: first-launch.sh persists enabled wire-protocol listener environment variables into the
    systemd EnvironmentFile (~/.provisa/systemd/provisa.env) and generates an extension compose overlay
    (~/.provisa/extensions/protocols/docker-compose.protocols.yml) that publishes each enabled container
    port 1:1 to the host and sets the listener environment variables for each enabled protocol.
  use_case: Persisting protocol configuration into systemd EnvironmentFile and generating a protocol-specific
    compose overlay ensures that enabled protocols remain configured across service restarts and are isolated
    from the base application compose file.
  code:
  - packaging/linux/first-launch.sh
  since: 2026-07
  scenario: 'Given PROVISA_PGWIRE_PORT=5439 and PROVISA_BOLT_PORT unset from terraform base_startup

    When first-launch.sh runs

    Then PROVISA_PGWIRE_PORT=5439 is appended to ~/.provisa/systemd/provisa.env

    And ~/.provisa/extensions/protocols/docker-compose.protocols.yml is generated

    And the overlay includes port 5439:5439 mapping for pgwire

    And the overlay does not include a port mapping for Bolt

    '
- id: REQ-1209
  status: complete
  group: 11. Platform, Infrastructure & Delivery
  category: Multi-Protocol Exposure (GCP)
  priority: MUST
  type: infrastructure
  description: scripts/provisa auto-includes every ~/.provisa/extensions/*/docker-compose.*.yml file during
    `provisa start`, so protocol overlays generated by first-launch.sh are automatically composed with
    the base stack without requiring manual docker-compose -f inclusion.
  use_case: Auto-inclusion allows the extension mechanism to work transparently; operators do not need
    to manually add compose files to the start command.
  code:
  - scripts/provisa
  since: 2026-07
  scenario: 'Given ~/.provisa/extensions/protocols/docker-compose.protocols.yml exists and defines port
    5439:5439 for pgwire

    When `provisa start` is invoked

    Then the protocols overlay is automatically included

    And the pgwire container port 5439 is published to the host as 5439

    '
- id: REQ-1210
  status: complete
  group: 11. Platform, Infrastructure & Delivery
  category: Multi-Protocol Exposure (GCP)
  priority: SHOULD
  type: constraint
  description: gRPC wire protocol listener only serves once a proto schema has been registered (state.proto_files
    populated). On a demo or fresh cluster with no registered protos, the gRPC NetLB backend remains unhealthy
    per its health check. This is expected behavior; the NetLB does not recover until a proto is registered.
  use_case: Gating gRPC service availability on proto registration ensures the protocol cannot serve partial
    or incomplete schema state; failure to register a proto keeps the protocol unhealthy as designed,
    signaling to operators that configuration is needed.
  tests:
  - tests/unit/test_grpc_listener_gate.py
  code:
  - provisa/grpc
  - terraform/gcp
  since: 2026-07
  scenario: 'Given a GCP instance with enable_grpc=true (default) and no registered protos

    When the instance starts and the gRPC listener begins accepting health check probes

    Then the listener responds unhealthy (probe fails) until a proto is registered

    Given a proto schema is registered via the registry API

    When subsequent health check probes are sent

    Then the listener responds healthy

    '
- id: REQ-1211
  status: accepted
  group: 11. Platform, Infrastructure & Delivery
  category: Linux AppImage Deployment
  priority: MUST
  type: constraint
  description: Linux AppImage deployments must stage the docker-compose tree out of the AppImage's self-mount
    into a persistent, writable location (${PROVISA_HOME}/compose) during first-launch and record project_dir
    there, as the AppImage mount (/tmp/.mount_Provis*) is read-only and ephemeral.
  use_case: The AppImage mount is read-only, blocking Trino plugin extraction and other write operations;
    it is also ephemeral and vanishes when the AppImage process exits, causing the systemd `provisa start`
    daemon to lose its compose files on restart.
  target: 2026-Q3
  scenario: 'Given a Linux AppImage deployment is launched for the first time

    When first-launch.sh executes

    Then the docker-compose tree is staged from the AppImage mount to ${PROVISA_HOME}/compose

    And project_dir is recorded in configuration

    And subsequent `provisa start` invocations find the compose files in the persistent location

    And plugin extraction succeeds on persistent writable storage

    '
- id: REQ-1212
  status: accepted
  group: 11. Platform, Infrastructure & Delivery
  category: Docker Image Build
  priority: MUST
  type: constraint
  description: The provisa/provisa:local core Docker image must have the built React SPA present in static/
    at image-build time; CI core-image jobs must run the vite build into static/ before `docker build`.
  use_case: Without the UI bundle in the image, the provisa-ui container serves HTTP 503 "UI not bundled",
    making Provisa inaccessible via the web interface.
  target: 2026-Q3
  scenario: 'Given a CI core-image job is triggered

    When the job builds the Docker image

    Then it first runs `npm run build` in provisa-ui to generate static/

    And the docker build command includes the populated static/ directory

    When the provisa-ui container is deployed and starts

    Then the React SPA is available and HTTP 200 is served on the web interface port

    And the UI is not unavailable with HTTP 503

    '
- id: REQ-1213
  status: complete
  group: 11. Platform, Infrastructure & Delivery
  category: VM Cloud Deployment
  priority: MUST
  type: infrastructure
  description: GCP VM startup-script writes /etc/apt/apt.conf.d/99provisa-resilient before the first apt-get
    update, setting Acquire::http::Timeout=30, Acquire::https::Timeout=30, Acquire::Retries=3, Acquire::http::Pipeline-Depth=0
    to prevent indefinite CLOSE-WAIT hangs when Canonical mirrors half-close sockets mid-response.
  use_case: apt's default HTTP pipelining wedges forever (no timeout) on half-closed mirror sockets; under
    set -euo pipefail, hung apt-get update never errors and blocks the entire node boot indefinitely.
    Configuring timeouts and disabling pipelining makes apt deterministic on first-launch.
  code:
  - terraform/gcp/main.tf
  since: 2026-07
  scenario: 'Given a GCP VM instance is created with Provisa terraform/gcp configuration

    When the base_startup script executes before the first apt-get update

    Then /etc/apt/apt.conf.d/99provisa-resilient is written with Acquire::http::Timeout=30, Acquire::https::Timeout=30,
    Acquire::Retries=3, Acquire::http::Pipeline-Depth=0

    And subsequent apt-get update commands respect the timeout and retry settings

    And the node boot completes deterministically without indefinite CLOSE-WAIT hangs

    '
- id: REQ-1214
  status: complete
  group: 7. Result Delivery
  category: Multi-Protocol Query Routing
  priority: MUST
  type: constraint
  description: Every transport (gRPC, Arrow Flight, airport, GraphQL, JSON:API, Bolt, pgwire) routes through
    the single governed pipeline (_govern_and_route / _govern_and_route_compiled / _execute_plan) regardless
    of protocol. For ENGINE-route plans, raw-passthrough transports drain the engine's streaming terminal
    directly without materializing via _execute_plan; only genuinely-bounded routes (DIRECT native driver,
    metadata/admin, registered-function output) and format-buffered transports materialize through _execute_plan.
  use_case: Centralizing routing through one pipeline ensures uniform governance (RLS, masking, audit),
    prevents per-transport SQL branches, and allows streaming transports to drain engine terminals directly
    while keeping buffered transports format-aware.
  code:
  - provisa/executor/_pipeline.py
  tests:
  - tests/unit/test_governed_chokepoint.py
  - tests/integration/test_grpc_execution.py
  since: 2026-07
  scenario: 'Given a gRPC streaming query request and an Arrow Flight query request against the same table

    When both are routed through the ONE pipeline (_govern_and_route)

    Then both receive the same governing stamp (audit, RLS, masking applied)

    And the gRPC transport drains the engine''s ResultStream directly without materializing

    And the Arrow Flight transport drains the engine''s RecordBatch generator directly without materializing

    And a JSON:API buffered transport materializes through _execute_plan before formatting

    '
- id: REQ-1215
  status: complete
  group: 11. Platform, Infrastructure & Delivery
  category: Multi-Protocol Exposure (gRPC)
  priority: MUST
  type: behavioral
  description: gRPC server-streaming query RPC (_handle_query in provisa/grpc/server.py) streams ENGINE-route
    query results lazily. Each row batch is pulled from the sync ResultStream (execute_engine_sync) through
    loop.run_in_executor, keeping the blocking cursor out of the event loop; peak memory is bounded to
    one batch. Governance stamp is verified via require_governed_plan before engine execution.
  use_case: gRPC aio runs as an async generator over blocking engine cursors; wrapping ResultStream pulls
    in executor ensures the event loop never blocks and memory never exceeds one batch, enabling large
    result sets over gRPC.
  code:
  - provisa/grpc/server.py
  tests:
  - tests/unit/test_grpc_server.py
  - tests/integration/test_grpc_execution.py
  since: 2026-07
  scenario: 'Given a gRPC query RPC request for a 10M-row ENGINE-route result

    When _handle_query pulls from execute_engine_sync via loop.run_in_executor

    Then each batch is yielded as it arrives from the cursor

    And the event loop remains unblocked

    And peak memory stays bounded to one batch

    And the governance stamp is verified before any engine execution

    '
- id: REQ-1216
  status: complete
  group: 11. Platform, Infrastructure & Delivery
  category: Multi-Protocol Exposure (Arrow Flight)
  priority: MUST
  type: behavioral
  description: 'Arrow Flight SQL do_get ENGINE route streams via execute_engine_stream. All Flight-serving
    engines must declare EngineCapability.ARROW_STREAM only if run_arrow_stream genuinely streams via
    server-side chunk APIs (fetch_arrow_batches, to_arrow_iterable, fetchmany→Arrow). CONFORMANCE GAP:
    Databricks, BigQuery, MSSQL declare ARROW_STREAM but call run_arrow then .to_batches(), materializing
    the whole table. See docs/arch/streaming-uniformity-gap.md Defect 2.'
  use_case: 'Streaming via execute_engine_stream replaces full materialization with lazy per-batch pulls,
    enabling large result sets over Flight. Engine capability declarations must be honest: only declare
    ARROW_STREAM when the terminal is genuinely lazy via the driver''s server-side chunk API.'
  code:
  - provisa/api/flight/server.py
  - provisa/executor/_engine_executor.py
  tests:
  - tests/unit/test_flight_modes.py
  - tests/integration/test_arrow_flight_integration.py
  since: 2026-07
  scenario: 'Given an Arrow Flight do_get query for a 10M-row ENGINE-route result

    When _do_get_sql_governed calls execute_engine_stream

    Then a schema is returned immediately

    And RecordBatch records are streamed as a generator

    And the license nag is attached as app_metadata on the first batch

    And the cursor closes when the generator drains or the consumer stops early

    And peak memory is bounded to one batch

    '
- id: REQ-1217
  status: complete
  group: 7. Result Delivery
  category: Engine Streaming Terminals
  priority: MUST
  type: constraint
  description: 'Per-engine Arrow streaming terminals (execute_engine_stream) and run_sync must be genuinely
    lazy. DuckDB uses fetch_record_batch (_ARROW_STREAM_BATCH_ROWS = 65536). Snowflake uses fetch_arrow_batches.
    CONFORMANCE GAP: Databricks, BigQuery, MSSQL fake laziness by calling run_arrow/.to_batches(), materializing
    before batching. These must use server-side chunk APIs (BigQuery to_arrow_iterable, Databricks Cloud
    Fetch iteration, MSSQL cursor fetchmany→Arrow). See docs/arch/streaming-uniformity-gap.md Defects
    2-3.'
  use_case: Genuinely lazy per-batch pulls prevent materializing full result sets in memory and allow
    the consumer to stop early without forcing the engine to compute remaining batches. Only server-side
    chunk APIs honor this for warehouse engines; materializing then rebatching violates the design.
  code:
  - provisa/executor/_engine_executor.py
  tests:
  - tests/unit/test_duckdb_stream_terminal.py
  - tests/integration/test_preflight_streaming.py
  since: 2026-07
  scenario: 'Given a DuckDB Arrow streaming query with 1M rows

    When the consumer pulls the first 10 batches and then disconnects

    Then DuckDB''s cursor is closed and the remaining 990 batches are never fetched

    And memory is not allocated for the full result set

    '
- id: REQ-1218
  status: complete
  superseded_by: REQ-1231
  group: 7. Result Delivery
  category: Protocol-Specific Result Handling
  priority: MUST
  type: constraint
  description: Airport Flight transport drains the single streaming terminal identically to Flight SQL
    (REQ-1216). Byte-stable schema advertisement comes from the plan's typed output columns (known pre-execution),
    not by scanning rows. The is_rowid pseudo-column derives from source key metadata, a streamed rowid
    column, or an off-heap CTAS side-table — never an in-Provisa full-table cache. Airport is a streaming
    transport, not a materializing catalog-scan transport. See docs/arch/streaming-uniformity-gap.md Defect
    5.
  use_case: Draining the streaming terminal prevents OOM on large table scans in airport. Schema and rowid
    metadata are catalog-identity concerns separable from the result-streaming path. Off-heap CTAS satisfies
    unbounded rowid mappings without materializing in Provisa RAM.
  code:
  - provisa/api/airport/query.py
  - provisa/api/airport/server.py
  tests:
  - tests/integration/test_airport_source_e2e.py
  - tests/integration/test_airport_service_e2e.py
  - tests/unit/test_airport_streaming_terminal.py
  since: 2026-07
  scenario: 'Given an airport metadata request for a table schema

    When the request arrives, the plan''s typed columns advertise the schema pre-execution

    Then subsequent requests return the same schema bytes (stable)

    When UPDATE/DELETE echo needs is_rowid, it derives from source key metadata or a side-table

    And the result stream drains unboundedly without buffering in Provisa RAM

    '
- id: REQ-1219
  status: complete
  group: 11. Platform, Infrastructure & Delivery
  category: First-Run Setup Wizard
  priority: MUST
  type: constraint
  description: 'The first-run setup wizard layers an auth section onto a base config. When no config file
    exists, read_config_for_setup() in provisa/api/admin/_config_io.py falls back to the shipped minimal
    skeleton provisa-install-base.yaml (system sources/domains + built-in admin role, auth: none). The
    skeleton guarantees ProvisaConfig can be parsed after the wizard writes its auth section, ensuring
    _load_and_build always has a valid configuration to parse.'
  use_case: First-run installs with no prior config file must produce a valid configuration that _load_and_build
    can parse. The skeleton provides sources/domains/tables/roles (no defaults possible) and allows the
    wizard to focus on auth layering, guaranteeing config validity on startup.
  code:
  - provisa/api/admin/_config_io.py
  - provisa/cli.py
  - scripts/build-wheel.sh
  tests:
  - tests/unit/test_setup_base_config.py
  since: 2026-07
  scenario: 'Given a first-run install with no ~/.provisa/config.yaml

    When the setup wizard is invoked

    Then read_config_for_setup() loads the shipped provisa-install-base.yaml skeleton

    And the wizard layers an auth section onto the skeleton

    And the resulting config is written to ~/.provisa/config.yaml

    When _load_and_build parses the config file on startup

    Then it succeeds with a valid ProvisaConfig (sources, domains, tables, roles, auth all populated)

    '
- id: REQ-1220
  status: complete
  group: 11. Platform, Infrastructure & Delivery
  category: Demo Deployment Parity
  priority: MUST
  type: infrastructure
  description: On demo deployment, the app container (uvicorn main:app) loads a complete config rather
    than dropping into the first-run wizard. The Dockerfile bakes config/ and demo SQLite sample data
    into /app/config (demo data under /app/config/demo/files to avoid the ./demo compose bind-mount shadowing).
    packaging/linux/first-launch.sh::write_demo_overlay() writes ~/.provisa/extensions/demo/docker-compose.demo.yml
    exporting PROVISA_CONFIG=/app/config/provisa-install.yaml, PROVISA_DEMO=1, PROVISA_DEMO_DIR=/app/config/demo/files
    when INSTALL_DEMO=y and ROLE=primary (secondaries pull shared config from the primary DB). scripts/provisa
    auto-includes the overlay.
  use_case: Demo deployments must start with a populated config to avoid the first-run wizard and provide
    users with immediate sample data to explore. Docker/Linux demo parity with native launch (commit f7289d27)
    requires baking config and demo data into the image and wiring environment variables through the overlay
    mechanism.
  code:
  - Dockerfile
  - packaging/linux/first-launch.sh
  - scripts/provisa
  tests:
  - tests/unit/test_infra_requirements.py
  since: 2026-07
  scenario: 'Given a demo deploy with INSTALL_DEMO=y and ROLE=primary

    When the app container starts

    Then PROVISA_CONFIG points to /app/config/provisa-install.yaml (baked into image)

    And PROVISA_DEMO=1 and PROVISA_DEMO_DIR=/app/config/demo/files (baked demo data)

    And the overlay ~/.provisa/extensions/demo/docker-compose.demo.yml is written by first-launch.sh

    And scripts/provisa auto-includes the overlay on next `provisa start`

    And the app loads the config without entering the first-run wizard

    And demo data is immediately available for exploration

    '
- id: REQ-1221
  status: complete
  group: 7. Result Delivery
  category: Arrow Streaming Adapter
  priority: MUST
  type: behavioral
  description: The generic row→Arrow-batch adapter (arrow_batches_from_rows in provisa/federation/runtime_support.py)
    enables ARROW and ARROW_STREAM transports on ROWS-only federation engines (Postgres via psycopg2,
    SQLAlchemy). It packs Python rows into memory-bounded Arrow RecordBatches (65536 rows/batch default),
    locks the Arrow schema from the first batch and casts subsequent batches to it, converts Decimal→float,
    and maps empty results to a null-typed schema.
  use_case: Postgres and SQLAlchemy engines have no native Arrow reader (only row-based results), but
    clients expect Arrow streaming via Flight-SQL and airport transports. The adapter bridges this gap
    by materializing batches incrementally instead of buffering the entire result set, enabling lazy streaming
    over large result sets without requiring zero-copy native Arrow support.
  code:
  - provisa/federation/runtime_support.py
  - provisa/federation/native_backend.py
  - provisa/federation/engine.py
  tests:
  - tests/unit/test_runtime_support.py
  - tests/unit/test_native_arrow_transport.py
  - tests/unit/test_engine_capability_traits.py
  since: 2026-07
  scenario: 'Given a Postgres federation engine (ROWS-only, no native Arrow reader)

    When Flight-SQL requests Arrow record batches (execute_stream)

    Then native_backend.execute_stream branches on hasattr(rt, "run_arrow_stream")

    And pg_backend has no run_arrow_stream method, so the adapter is used

    And arrow_batches_from_rows adapts rt.run_sync''s lazy row stream into (schema, batch_generator)

    And batches are yielded incrementally (65536 rows/batch) instead of materializing the full result

    And Flight-SQL client receives bounded Arrow RecordBatches over the wire

    '
- id: REQ-1222
  status: complete
  group: 7. Result Delivery
  category: Engine Streaming Terminals
  priority: MUST
  type: behavioral
  description: PgFederationRuntime gains run_arrow(sql, params) and run_arrow_stream(sql, params) methods
    for zero-copy Arrow transport via adbc_driver_postgresql. run_arrow returns a materialized pyarrow.Table;
    run_arrow_stream returns (schema, batch_generator) with record batches fetched on-demand from a dedicated
    short-lived ADBC connection. Postgres rows decode directly into Arrow RecordBatches without Python
    row materialization; streaming variant is memory-bounded by one batch size. Dependency on adbc-driver-postgresql
    added to pyproject.toml.
  use_case: PostgreSQL federation engine can expose Arrow streaming to Flight SQL and airport transports,
    enabling large result sets without buffering rows in Python memory or materializing the full result
    set client-side.
  code:
  - provisa/federation/pg_backend.py
  tests:
  - tests/integration/test_streaming_memory_bounded_e2e.py
  - tests/unit/test_pg_arrow_transport.py
  since: 2026-07
  scenario: 'Given a PgFederationRuntime instance with a PostgreSQL connection

    When run_arrow_stream(sql, params) is called for a 10M-row result

    Then a dedicated ADBC connection is opened

    And a (schema, batch_generator) tuple is returned immediately

    And record batches are fetched on-demand (65536 rows/batch default) from adbc_driver_postgresql''s
    fetch_record_batch

    And peak memory is bounded to one batch

    And the ADBC connection closes when the generator drains or the consumer stops early

    And rows are decoded directly into Arrow with no Python row list materialization

    '
- id: REQ-1223
  status: complete
  group: 7. Result Delivery
  category: Engine Streaming Terminals
  priority: MUST
  type: behavioral
  description: SqlAlchemyFederationRuntime.run_sync classifies SQL statements by leading keyword (after
    stripping leading comments) to identify ROW-RETURNING queries (SELECT, WITH, VALUES, TABLE, SHOW,
    EXPLAIN). Row-returning statements stream via server-side named cursor using SQLAlchemy's stream_results
    execution option over psycopg2, replacing the prior unnamed cursor that buffered the entire result
    on execute(). Non-row-returning statements (DDL, DML) execute buffered in a committing transaction
    because psycopg2 syntax-errors on DECLARE CURSOR FOR DDL/DML. The streaming read runs on a dedicated
    connection closed when the stream drains.
  use_case: Server-side named cursors decouple result fetching from statement execution, allowing large
    result sets to remain on the server and be pulled incrementally without buffering client-side, enabling
    memory-bounded streaming on federation engines (Trino, Snowflake, Databricks, etc.) that use SQLAlchemy.
  code:
  - provisa/federation/sqlalchemy_backend.py
  tests:
  - tests/integration/test_streaming_memory_bounded_e2e.py
  - tests/unit/test_sqlalchemy_statement_classification.py
  since: 2026-07
  scenario: 'Given a SqlAlchemyFederationRuntime instance with a SQLAlchemy engine connection

    When run_sync(sql, params) is called with sql="SELECT * FROM large_table"

    Then the statement is classified as ROW-RETURNING

    And a named server-side cursor is declared via stream_results execution option

    And rows are fetched on-demand from the cursor without buffering the full result

    And peak memory is bounded to one fetch batch

    Given run_sync(sql, params) is called with sql="CREATE TABLE foo AS SELECT * FROM bar"

    When a DDL statement is detected

    Then it executes buffered in a committing transaction (not via named cursor)

    And the statement completes before returning (DDL cannot use server-side cursor)

    '
- id: REQ-1224
  status: complete
  group: 7. Result Delivery
  category: Large Result Redirect & CTAS
  priority: SHOULD
  type: behavioral
  description: Automatic stream↔CTAS threshold at the single terminal (_execute_plan). Buffered transports
    (JSON:API, GraphQL, Bolt) land an engine-native CTAS via run_materialize above a configurable row-count/byte
    threshold and inline the body below it — automatically, without requiring a caller to pass deliver=
    via side-channel. Threshold is a system configuration (e.g., 10K rows, 100MB). See docs/arch/streaming-uniformity-gap.md
    Defect 4.
  use_case: Buffered transports cannot stream incrementally to the wire and require materialization. An
    automatic threshold at the terminal (not per-caller) prevents memory OOM on unexpectedly large results
    by using engine-native CTAS for large sets while keeping small results inline for latency.
  code:
  - provisa/executor/redirect.py
  - provisa/pgwire/_pipeline.py
  - provisa/api/jsonapi/generator.py
  - provisa/api/rest/generator.py
  - provisa/bolt/session.py
  tests:
  - tests/unit/test_buffered_auto_threshold.py
  since: 2026-07
  related_reqs:
  - REQ-1203
  - REQ-1204
  scenario: 'Given a JSON:API query result with 5K rows

    When _execute_plan executes with threshold=10K rows

    Then the result materializes inline and returns body directly

    Given a GraphQL query result with 50K rows

    When _execute_plan executes with threshold=10K rows

    Then a CTAS is landed via run_materialize

    And a materialization handle is returned (not streamed rows)

    '
- id: REQ-1225
  status: in-progress
  group: 7. Result Delivery
  category: Engine Streaming Terminals
  priority: MUST
  type: behavioral
  description: Warehouse federation runtimes (Snowflake, Databricks, BigQuery, MSSQL, ClickHouse) must
    implement genuinely streaming `run_sync` terminals using their respective server-side chunk APIs.
    Each runtime builds `run_sync` on its single lazy Arrow primitive via `stream_rows_from_arrow(schema,
    batches)` in provisa/federation/runtime_support.py, returning a `StreamingQueryResult` (single-consume,
    one batch pulled at a time) rather than a fully materialized `QueryResult`. The async `run` terminal
    materializes via `run_async_materialized`, which drains the streaming `run_sync` on an executor thread,
    mirroring the PostgreSQL runtime split. `stream_rows_from_arrow` is the inverse primitive of `arrow_batches_from_rows`.
  use_case: Warehouse `run_sync` currently materializes the full result set, breaking memory boundedness
    on the pgwire ENGINE route despite REQ-1186. A genuinely streaming `run_sync` keeps the pgwire route
    OOM-safe for large results. See docs/arch/streaming-uniformity-gap.md Defect 3.
  code:
  - provisa/federation/snowflake_runtime.py
  - provisa/federation/databricks_runtime.py
  - provisa/federation/bigquery_runtime.py
  - provisa/federation/mssql_warehouse_runtime.py
  - provisa/federation/clickhouse_runtime.py
  - provisa/federation/runtime_support.py
  tests:
  - tests/integration/test_streaming_memory_bounded_e2e.py
  target: 2026-Q3
  scenario: 'Given a warehouse engine (Snowflake, Databricks, BigQuery, MSSQL, ClickHouse) with a large
    result set (1M+ rows)

    When pgwire ENGINE route calls run_sync(sql, params)

    Then a StreamingQueryResult is returned immediately (not a materialized QueryResult)

    And batches are fetched on-demand from the warehouse via its server-side chunk API

    And memory consumption is bounded by one batch size (~64KB-1MB)

    When a consumer stops early after 10 batches

    Then the warehouse cursor/fetch context is closed

    And remaining batches are never fetched

    And memory allocated for the full result set is not consumed

    '
- id: REQ-1226
  status: proposed
  group: 11. Platform, Infrastructure & Delivery
  category: HTTPS/TLS Configuration
  priority: MUST
  type: infrastructure
  description: GCP/Docker cluster deployment must serve all endpoints over HTTPS with TLS encryption.
    If the operator does not supply TLS certificates, the deployment must auto-generate temporary self-signed
    certificates to enable HTTPS out of the box without manual certificate provisioning.
  use_case: HTTPS is a security baseline for production deployments. Auto-generating self-signed certificates
    removes the operational friction of manual certificate management for development and test environments
    while still enforcing encrypted transport in production-like setups.
  target: 2026-Q4
  scenario: 'Given a GCP/Docker cluster deployment with no TLS certificates supplied

    When the deployment starts

    Then HTTPS endpoints are served over automatically generated self-signed certificates

    And TLS encryption is active without operator intervention

    Given a GCP/Docker cluster deployment with operator-supplied TLS certificates

    When the deployment starts

    Then the supplied certificates are used instead of self-signed ones

    And HTTPS endpoints are served using the provided TLS credentials

    '
- id: REQ-1228
  status: complete
  group: 11. Platform, Infrastructure & Delivery
  category: HTTPS/TLS Configuration
  priority: MUST
  type: infrastructure
  description: 'HTTPS/TLS encryption (REQ-1226) applies to ALL protocol endpoints in a cluster deployment
    — not just the web API and UI, but also pgwire, Bolt, Arrow Flight, gRPC, and MCP endpoints. TLS terminates
    at each protocol server inside the container. The cluster''s L4 load balancers pass TCP through without
    termination, allowing each protocol server to handle its own encryption. Self-signed certificates
    are auto-generated when none are supplied. In production SaaS deployments, REQ-1239 supplies a real
    wildcard `*.provisa.dev` certificate via ACME DNS-01, written to PROVISA_TLS_CERT/PROVISA_TLS_KEY
    paths that all protocol servers read.

    TLS alone authenticates only the server, so each protocol endpoint also supports client-certificate
    verification (mutual TLS). One policy is resolved in provisa/security/mtls.py and translated into
    each transport''s spelling — ssl.SSLContext.verify_mode plus load_verify_locations for pgwire and
    Bolt, root_certificates/require_client_auth for gRPC, verify_client/root_certificates for Flight.
    Configuration mirrors the TLS certificate resolution — a per-protocol variable (PROVISA_PGWIRE_CLIENT_CA,
    PROVISA_BOLT_CLIENT_CA, PROVISA_GRPC_CLIENT_CA, PROVISA_FLIGHT_CLIENT_CA), else the node-wide PROVISA_MTLS_CLIENT_CA.
    PROVISA_MTLS_MODE selects `required` or `optional`; naming a CA with no mode means required. PROVISA_MTLS_BIND_PRINCIPAL
    additionally requires the certificate''s common name to equal the username the connection then authenticates
    as, checked on pgwire before the password is examined and on Bolt before HELLO or LOGON resolves the
    principal. Misconfiguration fails startup rather than degrading — a mode with no CA, a CA path that
    does not exist, and an unrecognized mode each raise.'
  use_case: Encrypting all protocol endpoints (not just HTTP) ensures security across every client access
    path — database drivers (pgwire), graph traversal (Bolt), columnar streaming (Arrow Flight), service
    meshes (gRPC), and model context protocols (MCP) — without requiring separate external TLS termination
    per protocol. Self-signed dev stand-in allows local testing; production uses a real trusted cert.
    Client-certificate verification moves the first check to the handshake, so a caller without a certificate
    the deployment's CA signed never reaches the credential layer to start guessing; principal binding
    makes a stolen password useless without that user's certificate.
  since: 2026-07
  code:
  - provisa/security/mtls.py
  - provisa/api/app_startup.py
  - provisa/grpc/server.py
  - provisa/pgwire/server.py
  - provisa/bolt/session.py
  tests:
  - tests/unit/test_mtls.py
  scenario: 'Given a cluster deployment with no TLS certificates supplied

    When the deployment starts

    Then self-signed certificates are auto-generated for all protocol endpoints

    And the API server serves HTTPS

    And the pgwire server serves TLS-wrapped connections on port 5439

    And the Bolt server serves TLS-wrapped connections

    And the Arrow Flight server serves TLS-wrapped connections

    And the gRPC server serves TLS-wrapped connections

    And the MCP server serves TLS-wrapped connections

    And the L4 load balancer passes TCP through without intercepting TLS

    Given a cluster deployment with operator-supplied TLS certificates

    When the deployment starts

    Then all protocol servers use the supplied certificates

    And each endpoint is served over the operator''s TLS credentials

    Given a production SaaS deployment with ACME DNS-01 provisioned wildcard cert

    When the deployment starts

    Then the wildcard cert (*.provisa.dev) is read from PROVISA_TLS_CERT/PROVISA_TLS_KEY

    And all protocol servers use the same wildcard cert across all subdomains

    Given PROVISA_MTLS_CLIENT_CA names a CA bundle and no mode is set

    When a wire server starts

    Then client certificates are required

    And a client presenting none is refused at the handshake, before any credential is read

    Given PROVISA_MTLS_MODE is optional

    When a client presents no certificate

    Then the handshake completes and the credential layer decides

    And a client presenting a certificate the CA did not sign is still refused

    Given PROVISA_MTLS_BIND_PRINCIPAL is true

    When a client whose certificate names carol authenticates as alice over pgwire or Bolt

    Then the connection is refused as unauthorized rather than as a wrong password

    Given PROVISA_MTLS_MODE is set with no client CA configured

    When the node starts

    Then startup fails rather than serving unverified connections

    '
- id: REQ-1229
  status: complete
  since: 2026-07
  group: 11. Platform, Infrastructure & Delivery
  category: Clustered Deployment Configuration
  priority: MUST
  type: constraint
  code:
  - provisa/core/config_loader.py
  - provisa/api/app.py
  tests:
  - tests/unit/test_config_replace_mode.py
  description: In a clustered Provisa deployment, only the primary node may modify the config row store
    (DELETE, INSERT, UPDATE). Secondary nodes set PROVISA_ROLE=secondary, which forces load_config replace=OFF
    to prevent them from overwriting shared config rows. Concurrent identical config upserts across the
    cluster are serialized by a PostgreSQL advisory lock and are idempotent no-ops. All nodes load a byte-identical
    baked config file at startup; no runtime "pull config from primary PG" path exists.
  use_case: The single-writer invariant prevents secondaries from accidentally corrupting the shared config
    store. Advisory lock serialization ensures concurrent upserts are safe and idempotent. Byte-identical
    baked configs guarantee schema consistency across the cluster at startup.
  target: 2026-Q3
  scenario: 'Given a clustered deployment with primary and secondary nodes

    When the primary upserts a config row

    Then the advisory lock serializes the write

    And the row is written to the shared PostgreSQL

    When a secondary attempts to DELETE or REPLACE a config row

    Then PROVISA_ROLE=secondary forces load_config replace=OFF

    And the DELETE/REPLACE is rejected

    When multiple nodes attempt identical upserts concurrently

    Then the advisory lock serializes them

    And each upsert becomes an idempotent no-op

    When any node starts

    Then it loads the byte-identical baked config file

    And no runtime pull from primary PG occurs

    '
- id: REQ-1230
  status: proposed
  group: 11. Platform, Infrastructure & Delivery
  category: Clustered Deployment Configuration
  priority: MUST
  type: infrastructure
  description: Secondary nodes in a cluster deployment run application tier only (Provisa API/UI + in-process
    protocol listeners on ports 3000, 5439, 7687, etc.). They do NOT run local postgres, redis, minio,
    or trino instances. Instead, control-plane, cache, object-store, and Trino-coordinator endpoints are
    repointed at the primary via intra-cluster networking. Secondary topology is selected via a standalone
    compose-secondary.yml base (not an additive overlay, since compose depends_on cannot be removed by
    overlay merge).
  use_case: Eliminating stateful services on secondaries reduces deployment complexity and resource footprint,
    centralizes all state on the primary, and ensures a clean restart topology where secondaries are ephemeral
    compute without persistent service dependencies.
  target: 2026-Q3
  scenario: 'Given a cluster deployment with a secondary node

    When compose-secondary.yml is applied

    Then the Provisa API tier starts on port 3000

    And pgwire listener starts on port 5439

    And Bolt listener starts on port 7687

    And Arrow Flight listener starts

    And gRPC listener starts

    And MCP listener starts

    And no local postgres instance is provisioned

    And no local redis instance is provisioned

    And no local minio instance is provisioned

    And no local trino instance is provisioned

    When the secondary connects to its primary

    Then control-plane endpoints resolve to the primary

    And cache endpoints resolve to the primary''s redis

    And object-store endpoints resolve to the primary''s minio

    And Trino coordinator endpoint resolves to the primary

    '
- id: REQ-1231
  status: complete
  group: 7. Result Delivery
  category: Protocol-Specific Result Handling
  priority: MUST
  type: behavioral
  description: 'Airport Flight do_get routes through the governed_table_scan_stream() terminal, returning
    (schema, batch_generator). ENGINE-routed queries return the engine''s lazy record-batch stream via
    execute_engine_stream; DIRECT-routed queries return typed Arrow RecordBatches (65_536 rows/batch)
    via execute_native_stream reshaped by _typed_batches_from_rows, wrapped in flight.GeneratorStream.
    The advertised schema (flight_info / list_schemas) is byte-stable: derived from the query''s TYPED
    output columns (DuckDB result schema on ENGINE; native driver result-column types mapped via to_ir→_ir_to_arrow
    on DIRECT), independent of result row count. governed_table_scan_schema() derives that schema without
    opening a row-streaming cursor (ENGINE closes the lazy reader; DIRECT releases the eagerly-opened
    cursor via close()/on_release). Per-batch is_rowid derives from PK metadata, not whole-table materialization.
    This supersedes REQ-1218''s _retype_null_columns pattern.'
  use_case: Streaming governs all result transport uniformly without OOM risk on large table scans. Byte-stable
    schemas separate catalog identity (known pre-execution) from result streaming. Eager cursor release
    on schema-only close frees resources before drain completes. Per-batch rowid avoids full-table buffering
    for UPDATE/DELETE echo.
  code:
  - provisa/api/airport/query.py
  - provisa/api/airport/server.py
  - provisa/executor/result.py
  - provisa/federation/runtime.py
  tests:
  - tests/integration/test_airport_service_e2e.py
  - tests/integration/test_streaming_memory_bounded_e2e.py
  - tests/unit/test_airport_streaming_terminal.py
  since: 2026-07
  e2e: true
  e2e_reason: Airport Flight streaming behavior (schema stability, rowid derivation, memory bounds) is
    only verifiable through end-to-end protocol interaction with the Arrow Flight server and multiple
    backend routes (ENGINE vs. DIRECT).
  scenario: 'Given an airport Flight do_get request for a large table

    When the request arrives at the governed_table_scan_stream terminal

    Then the schema is derived from the query''s TYPED output columns and advertised immediately

    And the schema bytes remain stable regardless of row count or early disconnection

    When the consumer pulls record batches (65_536 rows each) from the generator

    Then is_rowid pseudo-columns derive per-batch from PK metadata without buffering the full table

    And early close() releases the server-side cursor without draining all results

    And memory usage stays bounded even for multi-GB result sets

    '
- id: REQ-1232
  status: complete
  superseded_by: REQ-1276
  group: 13. Multi-Tenancy & Organizations
  category: Authentication
  priority: MUST
  type: constraint
  description: Every credential (API key / token / session) is scoped to exactly one organization. Org
    identity is always determined by the credential's active_org_id in the authentication middleware,
    never by TLS SNI, HTTP header, or first-membership fallback.
  use_case: Ensures that a credential cannot be used to access data from a different organization. Users
    who hold memberships in multiple organizations must obtain a distinct org-scoped credential per organization.
  code:
  - provisa/auth/middleware.py
  tests:
  - tests/integration/test_auth_org_scoping.py
  - tests/unit/test_auth_middleware.py
  since: 2026-07
  scenario: 'Given a user who is a member of both org-acme and org-beta

    When the user authenticates with a credential scoped to org-acme

    Then queries executed with that credential can only access org-acme''s tables and data

    And the credential''s active_org_id determines authorization, not the {org}.provisa.dev subdomain
    or any HTTP header

    When the user needs to query org-beta''s data

    Then they must use a separate credential scoped to org-beta

    And the TLS SNI hostname and HTTP Host header serve as ergonomic addressing only and cannot override
    the credential''s org identity

    '
- id: REQ-1233
  status: accepted
  group: 13. Multi-Tenancy & Organizations
  category: Org Identity & Subdomain Addressing
  priority: MUST
  type: infrastructure
  target: 2026-Q3
  description: 'The subdomain `{org}.provisa.dev` is the org identity. `cloud.provisa.dev` is the org-agnostic
    entry point (login + org picker). Every data/control endpoint is addressed by the org subdomain: `bolt://{org}.provisa.dev`,
    `postgresql://…@{org}.provisa.dev`, the Arrow Flight Location, `grpc+tls://{org}.provisa.dev`, MCP,
    and `https://{org}.provisa.dev/data`.'
  use_case: Subdomain-based org identity is ergonomic for multi-org SaaS and self-integrating with DNS/TLS
    (every subdomain is a distinct DNS name and TLS certificate request). It requires no protocol-specific
    multiplexing headers.
  code:
  - provisa/auth/middleware.py
  - provisa/api/app.py
  tests:
  - tests/integration/test_subdomain_routing.py
  scenario: 'Given a user of acme.provisa.dev

    When they connect a pgwire client to postgresql://user@acme.provisa.dev:5439

    Then the subdomain acme names the org

    And queries execute within acme''s data plane

    Given a user switching orgs at cloud.provisa.dev

    When they select org beta

    Then they are redirected to beta.provisa.dev

    And their session is org-scoped to beta

    '
- id: REQ-1234
  status: complete
  group: 13. Multi-Tenancy & Organizations
  category: Org Identity & Subdomain Addressing
  priority: MUST
  type: behavioral
  target: 2026-Q3
  since: 2026-08
  description: 'The subdomain org selector is delivered in two transports: the HTTP `Host` header for
    web/REST/GraphQL, and TLS SNI on the wire protocols that terminate TLS through a stdlib ssl.SSLContext
    (pgwire, Bolt). One string, one rule: `provisa.security.sni.org_from_host` reads the labels, and the
    HTTP middleware calls the same function, so the two transports cannot drift. An sni_callback installed
    on each listener stashes the indicated hostname on the connection object the handshake produced —
    the wrapped socket for pgwire, the transport ssl_object for Bolt — because that object is the only
    one that outlives the callback and is reachable from the protocol handler. The name is a request and
    not a grant: it reaches resolve_session_org as requested_org, which refuses any org the authenticated
    principal is neither a member of nor holds the cross-org right for, exactly as Host and x-provisa-org
    do. The leftmost label is read as an org only when it could have been created as one (REQ-1309), so
    127.0.0.1 names no org named "127"; the control-plane host `cloud.*` names no org by its hostname
    (REQ-1276) and continues to take an explicit header. gRPC, Arrow Flight and MCP are out of scope:
    they hand their certificates to grpc.ssl_server_credentials, pyarrow and uvicorn respectively, none
    of which exposes a servername callback, and they keep their existing x-provisa-org metadata channel.'
  use_case: SNI extraction on wire protocols applies the same org routing to pgwire and Bolt connections
    without a protocol-specific header mechanism. A driver that resolves acme.provisa.dev through DNS
    already sends the name in the ClientHello; nothing about the client changes.
  code:
  - provisa/security/sni.py
  - provisa/core/org_ids.py
  - provisa/api/app_startup.py
  - provisa/pgwire/server.py
  - provisa/bolt/session.py
  - provisa/auth/middleware.py
  - provisa/api/admin/orgs_router.py
  tests:
  - tests/unit/test_wire_sni.py
  scenario: 'Given a pgwire client connecting to acme.provisa.dev on port 5439

    When the TLS ClientHello arrives

    Then the sni_callback stashes "acme.provisa.dev" on the wrapped socket

    And the handler reads org "acme" off it and passes it to resolve_session_org as a request

    And a principal with no membership in acme and no cross-org right is refused


    Given a Bolt client connecting to beta.provisa.dev:7687

    Then the same rule reads org "beta" off the transport ssl_object


    Given a client that dialed 127.0.0.1 and sent no servername

    Then no org is requested and the org resolves from the principal alone

    '
  relates_to:
  - REQ-1276
  - REQ-1309
  - REQ-1266
- id: REQ-1235
  status: accepted
  group: 13. Multi-Tenancy & Organizations
  category: Org Identity & Subdomain Addressing
  priority: MUST
  type: constraint
  target: 2026-Q3
  description: 'The subdomain never authorizes — it only names the org. The authenticated credential must
    authorize that org: an org-scoped credential''s `active_org_id` must EQUAL the subdomain org (reject
    on mismatch); a person-scoped (OIDC) identity must be a MEMBER of the subdomain org (check user_org_memberships)
    else reject. No default org, no fallback — an unnamed or unauthorized org is a rejection, never a
    guess.'
  use_case: Subdomain naming + credential authorization prevents a credential intended for org-acme from
    accessing org-beta data. A person can hold memberships in multiple orgs but must use org-matched credentials
    or re-authenticate for each org.
  code:
  - provisa/auth/middleware.py:240-280
  tests:
  - tests/integration/test_auth_org_scoping.py
  - tests/unit/test_auth_middleware.py
  related_reqs:
  - REQ-1232
  scenario: 'Given a credential with active_org_id=acme

    When a request arrives for acme.provisa.dev

    Then the subdomain org matches the credential''s org

    And the request is authorized

    When the same credential arrives at beta.provisa.dev

    Then the subdomain org (beta) ≠ credential''s org (acme)

    And the request is rejected

    Given a person-scoped OIDC identity with memberships in [acme, beta]

    When the person lands on acme.provisa.dev

    Then the auth middleware checks user_org_memberships for acme

    And the check passes; acme-scoped session is minted

    When the person later accesses beta.provisa.dev

    Then a fresh membership check is required for beta

    And a new org-scoped session is minted

    '
- id: REQ-1236
  status: accepted
  superseded_by: REQ-1235
  group: 13. Multi-Tenancy & Organizations
  category: Org Identity & Subdomain Addressing
  priority: MUST
  type: constraint
  target: 2026-Q3
  description: 'For wire protocols, the credential is org-scoped (per REQ-1232), so SNI is a cross-check:
    parse org from SNI, reject the connection if it ≠ the credential''s active_org_id. Catches the error
    case of right-credential/wrong-subdomain.'
  use_case: Wire SNI cross-check prevents typos or DNS hijack risks where a client connects to the wrong
    subdomain with a valid but mismatched credential. Catches human error early in TLS handshake.
  code:
  - provisa/pgwire/server.py
  - provisa/bolt/server.py
  - provisa/auth/middleware.py
  tests:
  - tests/integration/test_wire_sni_cross_check.py
  scenario: 'Given a pgwire credential with active_org_id=acme

    When the client connects via SNI beta.provisa.dev

    Then the auth layer extracts SNI org (beta)

    And compares it to the credential''s org (acme)

    And the mismatch is detected

    Then the connection is rejected with a clear error

    '
- id: REQ-1237
  status: accepted
  group: 13. Multi-Tenancy & Organizations
  category: Org Identity & Subdomain Addressing
  priority: MUST
  type: behavioral
  target: 2026-Q3
  description: 'OIDC/social identity is person-scoped; Provisa mints the org-scoped session. Google/OIDC
    providers (e.g., provisa/auth/providers/oauth.py:74-76, keycloak.py:68-70) return `AuthIdentity(user_id=sub,
    email=…)` with NO `active_org_id` — the IdP proves the person, never the org. After IdP auth completes,
    Provisa mints its OWN org-scoped session (sets `active_org_id`) gated by a membership check. The subdomain
    selects the org pre-auth, so no org-picker screen is needed: land on `acme.provisa.dev` → OIDC login
    → membership check on `acme` → acme-scoped session returned.'
  use_case: 'Person-scoped IdP identity + Provisa-minted org-scoped session separates authentication (IdP:
    who am I?) from authorization (Provisa: which orgs can I access?). Enables subdomain-driven org selection
    without extra UI screens.'
  code:
  - provisa/auth/providers/oauth.py:74-80
  - provisa/auth/providers/keycloak.py:68-70
  - provisa/auth/middleware.py:240-280
  tests:
  - tests/integration/test_oidc_to_org_session.py
  scenario: 'Given a user landing on acme.provisa.dev (no active session)

    When they are redirected to Google for OIDC

    Then the IdP returns AuthIdentity(user_id=sub, email=user@example.com)

    And no active_org_id is present in the IdP response

    When Provisa receives the IdP token

    Then it checks user_org_memberships for the user in org acme

    And finds the membership exists

    Then it mints a new org-scoped session with active_org_id=acme

    And sets the User-Agent cookie as org-scoped

    And the user is logged into acme

    When the user later visits beta.provisa.dev

    Then the acme session is no longer valid (cross-subdomain)

    And a fresh OIDC → membership check → new beta session is required

    '
- id: REQ-1238
  status: accepted
  group: 13. Multi-Tenancy & Organizations
  category: Org Identity & Subdomain Addressing
  priority: MUST
  type: constraint
  target: 2026-Q3
  description: The `x-org-id` header path in provisa/auth/middleware.py:252-253 currently passes the header
    without checking `user_org_memberships` — a user could pass any org_id and gain cross-org access.
    This is the interactive org-switcher lane for a person-scoped session with no subdomain (e.g., POST
    to /api/switch-org with x-org-id). The passed org MUST be validated against membership; reject if
    not a member. This corrects a defect that was previously masked by single-org deployments.
  use_case: Header-path membership validation closes a privilege-escalation hole where a person-scoped
    session could be tricked into accessing an org they are not a member of via header manipulation.
  code:
  - provisa/auth/middleware.py:251-253
  tests:
  - tests/integration/test_x_org_id_validation.py
  - tests/unit/test_auth_middleware.py
  scenario: 'Given a person-scoped session (from OIDC, no active_org_id yet)

    When they submit a request with x-org-id: beta

    And they are not a member of org beta

    Then the middleware validates the membership check

    And finds no membership

    And rejects the request with 403 Forbidden

    Given the same session with x-org-id: acme

    And they ARE a member of org acme

    Then the middleware validates and finds membership

    And allows the request

    And sets active_org_id=acme for the duration

    '
- id: REQ-1239
  status: accepted
  group: 13. Multi-Tenancy & Organizations
  category: Deployment & Infrastructure
  priority: MUST
  type: infrastructure
  target: 2026-Q3
  description: Wildcard TLS for `*.provisa.dev` via ACME DNS-01. Obtain a true wildcard `*.provisa.dev`
    cert from Let's Encrypt using ACME DNS-01 with a scoped Cloudflare API token (Zone.DNS:Edit on provisa.dev
    only). One wildcard certificate covers `cloud.provisa.dev` and every `{org}.provisa.dev` across all
    protocols (SNI matches regardless of port). Cert is written to `PROVISA_TLS_CERT` and `PROVISA_TLS_KEY`
    paths that packaging/linux/first-launch.sh:443-451 already adopts. Node-side renewal timer (90-day).
    Replaces the self-signed dev stand-in for public SaaS deploys.
  use_case: '`.dev` is HSTS-preloaded, so a real trusted cert is mandatory for any public deploy. A single
    wildcard cert covers unlimited subdomains without per-subdomain provisioning or Cloudflare TLS termination
    (which would break wire protocols). Automated ACME renewal ensures uptime across certificate lifecycle.'
  code:
  - packaging/linux/first-launch.sh:443-451
  - provisa/api/app.py
  - provisa/pgwire/server.py
  - provisa/bolt/server.py
  tests:
  - tests/integration/test_wildcard_cert_provisioning.py
  - tests/e2e/test_acme_dns01_renewal.py
  scenario: 'Given a Provisa SaaS deployment on provisa.dev

    When the infrastructure runs cert provisioning via ACME

    Then a DNS-01 challenge is issued against Cloudflare (using scoped token)

    And the wildcard cert *.provisa.dev is obtained

    When the cert is written to PROVISA_TLS_CERT/PROVISA_TLS_KEY

    Then all protocol servers (API, pgwire, bolt, Arrow Flight, gRPC, MCP) read and use it

    And cloud.provisa.dev and all {org}.provisa.dev subdomains are valid under the cert

    When the cert is 30 days from expiry

    Then a renewal timer triggers

    And a new cert is obtained and deployed without downtime

    '
- id: REQ-1240
  status: accepted
  group: 13. Multi-Tenancy & Organizations
  category: Deployment & Infrastructure
  priority: MUST
  type: infrastructure
  target: 2026-Q3
  description: 'Cloudflare DNS split: Zone `provisa.dev` stays on Cloudflare. `provisa.dev` + `www.provisa.dev`
    remain Cloudflare-proxied (orange) for the marketing site. `cloud.provisa.dev` and `*.provisa.dev`
    are DNS-only (grey-cloud) records pointing at the GCP L4 LB IPs, so raw TCP reaches the LB and in-container
    TLS terminates. Cloudflare proxy carries only HTTP(S) and would terminate TLS itself, breaking wire
    protocols. No GCP Cloud DNS zone needed; DNS-01 ACME uses the Cloudflare token.'
  use_case: DNS-only records for app subdomains allow wire protocols (pgwire, bolt, Arrow Flight, gRPC,
    MCP) to reach the load balancer with raw TCP, where in-container TLS terminates per-port. Cloudflare
    proxy on marketing site keeps SEO and CDN benefits without interfering with protocol servers.
  code:
  - packaging/linux/first-launch.sh
  - infrastructure/cloudflare-setup
  tests:
  - tests/integration/test_dns_split_routing.py
  scenario: 'Given a DNS lookup for www.provisa.dev

    Then Cloudflare returns its proxied IP

    And the marketing site is served via Cloudflare CDN

    Given a DNS lookup for acme.provisa.dev

    Then Cloudflare returns the GCP L4 LB IP (grey-cloud)

    And raw TCP connections reach the load balancer

    And TLS terminates in-container per protocol

    '
- id: REQ-1241
  status: accepted
  group: 13. Multi-Tenancy & Organizations
  category: Deployment & Infrastructure
  priority: MUST
  type: constraint
  target: 2026-Q3
  description: 'A session is always scoped to exactly one org and therefore one federation engine. `state.federation_engine`
    is a per-process singleton built once at startup (provisa/api/app.py:270), read with no org parameter
    at provisa/api/_query_helpers.py:126, so one process serves one engine. Subdomain-pinned single-org
    sessions (per REQ-1235) enforce this invariant: each org gets its own Provisa instance (or process)
    with its own federation engine binding. Until per-org engine routing is built (REQ-1244), sessions
    cannot switch engines without re-launching the process.'
  use_case: Single-engine-per-session is a hard invariant that simplifies caching, planning, and freshness
    logic. Processes are stateless and can be scaled horizontally per-org or per-engine-instance. Cross-engine
    session switching requires building an org→engine registry and replacing the singleton (future work,
    REQ-1244).
  code:
  - provisa/api/app.py:270
  - provisa/api/_query_helpers.py:126
  tests:
  - tests/unit/test_session_org_invariant.py
  scenario: 'Given a Provisa instance deployed for org acme

    When state.federation_engine is initialized

    Then it is built as a per-process singleton

    And all queries in this process route through that engine

    When a request arrives for org acme

    Then the session''s org matches the engine''s org

    And the query executes against acme''s federation engine

    When a user later attempts to switch to org beta

    Then the current session cannot switch engines (REQ-1242 handles re-auth)

    '
- id: REQ-1242
  status: accepted
  group: 13. Multi-Tenancy & Organizations
  category: Deployment & Infrastructure
  priority: SHOULD
  type: behavioral
  target: 2026-Q3
  description: In-session org switch (e.g., click "switch to acme" in UI) tears down the org-scoped session
    and mints a new one for the target org (membership-checked), REUSING the already-proven IdP identity
    (no password/IdP round-trip required), then redirects to `{org}.provisa.dev`. It is a scoped re-login,
    NOT a live cross-engine swap — it preserves the single-engine-per-session invariant (REQ-1241). Future
    work (REQ-1244) will enable live multi-engine switching without re-auth.
  use_case: Org-switch without IdP round-trip is ergonomic for multi-org users. Redirecting to the target
    subdomain re-establishes DNS and TLS SNI for clean cross-org addressing. Re-minting the session (not
    retaining it) respects the single-engine invariant until multi-engine routing is built.
  code:
  - provisa/api/auth_router.py
  - provisa/auth/middleware.py
  tests:
  - tests/integration/test_org_switch_ergonomic.py
  - provisa-ui/e2e/org-switcher.spec.ts
  scenario: 'Given a user logged into acme.provisa.dev (acme-scoped session)

    When they click "Switch to org: beta" in the UI

    Then a POST to /api/switch-org?target_org=beta is sent

    And the handler checks membership in org beta (using the existing IdP identity)

    And finds membership valid

    Then the acme session is invalidated

    And a new beta-scoped session is minted (reusing the IdP proof)

    And the response redirects to beta.provisa.dev

    When the browser follows the redirect

    Then TLS SNI is beta.provisa.dev

    And cookies are set for the beta subdomain

    And the user is logged into beta without re-entering IdP credentials

    '
- id: REQ-1243
  status: accepted
  group: 13. Multi-Tenancy & Organizations
  category: Deployment & Infrastructure
  priority: MUST
  type: structural
  target: 2026-Q3
  description: 'Two isolation lanes coexist: (a) Pooled/shared Trino cluster — the free/small tier, "trino-level
    isolation," all orgs start here. (b) BYO (bring-your-own) cluster — a customer points Provisa at their
    OWN federation engine instance (e.g., Databricks, Snowflake); self-service, the customer may change
    their own fed-engine instance, billed separately. Both lanes route through the SAME _govern_and_route/_execute_plan
    pipeline; routing is org-specific at deployment time (which instance serves which org), not in-session
    switching (REQ-1244 defers per-org dynamic engine selection).'
  use_case: Dual-lane model serves free/small orgs economically on shared infrastructure while enabling
    enterprise customers to run on their own data warehouses. Unified pipeline means no code branching
    and shared planner/cache/freshness logic.
  code:
  - provisa/api/app.py:270
  - provisa/compiler/_govern_and_route
  - provisa/federation/runtime.py
  tests:
  - tests/integration/test_pooled_lane.py
  - tests/integration/test_byo_lane.py
  scenario: 'Given an org alice in the free tier

    When a query is submitted

    Then the deployed Provisa instance for alice has federation_engine = shared Trino

    And the query routes through the shared engine

    Given an org acme in the BYO tier

    When their Provisa instance is deployed with federation_engine = their Databricks

    And a query is submitted

    Then the query routes through acme''s Databricks

    And the same _govern_and_route pipeline is used

    And both alice and acme share cache and planner logic

    '
- id: REQ-1244
  status: proposed
  group: 13. Multi-Tenancy & Organizations
  category: Deployment & Infrastructure
  priority: SHOULD
  type: constraint
  target: 2026-Q4
  description: 'Per-org engine routing is deferred. In-session multi-engine switching requires replacing
    the per-process singleton (app.py:270 / _query_helpers.py:126) with an org→engine registry that feeds
    the SAME _govern_and_route/_execute_plan pipeline. Until built, heterogeneous engines are served via
    subdomain-pinned single-org/single-engine sessions (REQ-1241/1242). Future work: build org-scoped
    engine registry, keyed by org_id, read at query time to select the federation engine per-session org.'
  use_case: Dynamic per-org engine routing (without re-auth) enables Provisa to serve one org with Trino,
    another with Snowflake, and a third with Databricks from the SAME deployment and process. Preserves
    the single-engine-per-session invariant while allowing org-to-engine mapping to change at deployment
    time.
  code:
  - provisa/api/app.py:270
  - provisa/api/_query_helpers.py:126
  - provisa/federation/runtime.py
  tests:
  - tests/integration/test_per_org_engine_routing.py
  scenario: 'Given a multi-org SaaS deployment with org→engine registry

    When org alice queries (pinned to shared Trino)

    Then the registry maps alice → shared_trino

    And the federation engine is selected at query time

    When org acme queries (pinned to their Databricks)

    Then the registry maps acme → acme_databricks

    And acme''s Databricks engine is used for the query

    When the registry is updated to remap alice to a private Databricks

    Then no code changes or process restart are needed

    And the new engine is used on the next query

    '
- id: REQ-1245
  status: proposed
  group: 13. Multi-Tenancy & Organizations
  category: Deployment & Infrastructure
  priority: MUST
  type: infrastructure
  target: 2026-Q4
  description: 'Control-plane persistence is required for multi-instance fleet deployments. The control_plane
    store is currently in-memory (provisa/control_plane/store.py:16, "V1: no DB persistence") — a blocker
    for running multiple Provisa instances behind a load balancer. Multiple instances cannot share state
    (sources, roles, registered tables, relationships, etc.) and cannot coordinate cache invalidation
    or governance updates. Persistence must use the same org-scoped control-plane backend (PostgreSQL/SQLite/MySQL)
    that holds platform state (orgs, users, memberships), ensuring each org''s control plane is isolated
    and durable.'
  use_case: Persisted control plane enables horizontal scale-out of Provisa instances, load-balanced by
    subdomain org, each instance serving its own org's control-plane state from the shared backend. Required
    for production SaaS deployments with high availability.
  code:
  - provisa/control_plane/store.py:16
  - provisa/core/database.py
  - provisa/core/schema_admin.py
  tests:
  - tests/integration/test_control_plane_persistence.py
  - tests/integration/test_multi_instance_coordination.py
  scenario: 'Given a fleet of Provisa instances behind a load balancer

    When instance-1 adds a source to org acme''s control plane

    Then the source is persisted to the shared backend

    When instance-2 handles the next query for acme

    Then it reads the same source from the persistent backend

    And no state divergence occurs

    When any instance invalidates a cache key for acme

    Then the invalidation is broadcast to all instances serving acme

    (Future: req-distributed cache invalidation)

    '
- id: REQ-1246
  status: proposed
  group: 2. Authentication & Identity
  category: Authentication
  priority: MUST
  type: behavioral
  target: 2026-Q4
  description: First verified Firebase login provisions a user_profile record, decoupled from the local_users/bcrypt-password
    path. No password is created for Firebase identities. Firebase ID token serves as the credential.
    Provisioning occurs in the initial POST /auth/register on a Firebase identity (REQ-121 validates Firebase
    auth; REQ-124 is hard-gated to provider=="basic" and cannot onboard Firebase identities).
  use_case: Enables self-service signup on cloud.provisa.dev with Firebase as the primary identity provider,
    separating password-managed local users from federated identities.
  code:
  - provisa/api/auth_router.py
  - provisa/auth/
  - provisa/core/schema_admin.py
  tests:
  - tests/integration/test_firebase_signup.py
  - tests/unit/test_firebase_user_provisioning.py
  scenario: 'Given a user with a Firebase identity authenticating via /auth/register for the first time

    When they submit a valid Firebase ID token

    Then a user_profile record is created with their Firebase email and sub

    And no local_users/password record is created

    And the next request with that token is recognized as an existing user

    When that user later authenticates

    Then they use their Firebase ID token, not a password

    '
- id: REQ-1247
  status: proposed
  group: 10. UI & Admin Surfaces
  category: Authentication
  priority: MUST
  type: ui
  target: 2026-Q4
  description: 'Self-service signup landing on cloud.provisa.dev presents two options to a not-logged-in
    user: LOG IN or CREATE ACCOUNT. The choice routes to distinct flows: LOG IN → Firebase login (existing
    user or new self-registration via Firebase), CREATE ACCOUNT → signup flow including org membership
    selection (REQ-1248 / REQ-1249 / REQ-1250).'
  use_case: Org-agnostic entry point ergonomically guides new users into account creation while retaining
    fast login for existing users.
  code:
  - provisa-ui/pages/auth/landing.tsx
  - provisa-ui/components/auth/signup-choice.tsx
  tests:
  - provisa-ui/e2e/auth-landing.spec.ts
  scenario: 'Given an unauthenticated user navigates to cloud.provisa.dev

    When the page loads

    Then two buttons are displayed: "LOG IN" and "CREATE ACCOUNT"

    When they click "LOG IN"

    Then the Firebase login flow is initiated

    When they click "CREATE ACCOUNT"

    Then the signup flow is displayed (REQ-1248 / REQ-1249 / REQ-1250)

    '
- id: REQ-1248
  status: proposed
  group: 13. Multi-Tenancy & Organizations
  category: Org Membership
  priority: MUST
  type: behavioral
  target: 2026-Q4
  description: During account creation, a Firebase-authenticated user auto-joins an org if they hold a
    valid org_invites token (via invite URL or code). Provisioning reuses existing invite validation/consumption
    logic, upserting user_org_memberships at the inherited role from the invite record. If no invite is
    held, they proceed to REQ-1249 or REQ-1250 (create new org or request admission to existing org).
  use_case: 'Decouples Firebase signup from org membership: invited users skip org selection, while non-invited
    users choose between creating a new org or requesting membership in an existing one.'
  code:
  - provisa/api/auth_router.py
  - provisa/api/admin/invites_router.py
  tests:
  - tests/integration/test_signup_with_invite.py
  scenario: 'Given a user with a valid org_invites token completes Firebase authentication

    When they reach account creation and the system detects the valid invite

    Then user_org_memberships is upserted with the org_id and inherited role

    And the user is auto-joined to the invited org

    And the invite token is consumed (marked expired/used)

    And the flow proceeds to post-signup org navigation

    '
- id: REQ-1249
  status: proposed
  group: 13. Multi-Tenancy & Organizations
  category: Org Membership
  priority: SHOULD
  type: behavioral
  target: 2026-Q4
  description: 'During account creation, a Firebase user without an org invite may CREATE A NEW ORG (self-service):
    inserts an orgs row with the user as owner/admin, inserts user_org_memberships at owner/admin role,
    provisions the per-org schema org_{id}, and registers a control-plane tenant initialized to the shared/pooled
    Trino cluster (REQ-1243/1244). The createOrg endpoint (currently admin-only /admin/orgs) becomes available
    as part of the signup flow.'
  use_case: 'Enables self-service multi-org SaaS: new users can immediately spin up a workspace (org)
    without waiting for admin provisioning.'
  code:
  - provisa/api/auth_router.py
  - provisa/api/admin/orgs_router.py
  - provisa/core/schema_admin.py
  tests:
  - tests/integration/test_signup_create_org.py
  scenario: 'Given a Firebase user without an org invite during signup

    When they select "Create New Org"

    Then an orgs row is inserted with org_name set to user input

    And a user_org_memberships record is created with the user at owner/admin role

    And the per-org schema org_{id} is provisioned (CREATE SCHEMA org_{id})

    And a control-plane tenant is registered, initially pinned to the shared Trino cluster

    And the user is logged into the new org

    '
- id: REQ-1250
  status: proposed
  group: 13. Multi-Tenancy & Organizations
  category: Org Membership
  priority: SHOULD
  type: structural
  target: 2026-Q4
  description: 'Org membership is pull-based via org_join_requests, the inverse of org_invites. New registry
    table org_join_requests(user_id, org_id, status, requested_at, decided_by, decided_at) is added to
    REGISTRY_TABLES in provisa/core/schema_admin.py. Per-org visibility flag orgs.open_to_requests: if
    False (invite_only, the default), admission requests are silently rejected (no signal that the request
    was received, to avoid org-name enumeration). If True (open_to_requests), requests are logged pending
    admin approval. Requesters must be Firebase-authenticated. Anti-abuse: at most one open request per
    (user, org); rate-limited per requester; stale requests auto-expire (same TTL as org_invites).'
  use_case: Pull-based membership model enables users to request admission to public orgs without requiring
    org admins to discover and invite them. Silently rejecting invite_only requests prevents org enumeration
    attacks.
  code:
  - provisa/core/schema_admin.py
  tests:
  - tests/integration/test_org_join_request_table.py
  - tests/unit/test_org_join_request_visibility.py
  scenario: 'Given an org acme with open_to_requests = True

    When a Firebase user requests membership to acme

    Then an org_join_requests record is created with status=''pending''

    And only one open request per (user, org) is allowed

    Given an org beta with open_to_requests = False

    When a user requests membership to beta

    Then the request is silently rejected (no error to the user)

    '
- id: REQ-1251
  status: proposed
  group: 13. Multi-Tenancy & Organizations
  category: Org Membership
  priority: SHOULD
  type: behavioral
  target: 2026-Q4
  description: Admin approval/denial of org_join_requests. When an admin approves a request, the approval
    path reuses the membership-insert logic (provisa/api/auth_router.py and related) minus the token check,
    granting the lowest-privilege member role (never admin). Deny path closes the request (status='denied').
    Both actions record decided_by (admin user_id) and decided_at.
  use_case: Enables org admins to gate membership while supporting the pull-based admission model.
  code:
  - provisa/api/admin/orgs_router.py
  - provisa/api/auth_router.py
  tests:
  - tests/integration/test_org_join_request_approval.py
  scenario: 'Given a pending org_join_requests record for user jane requesting membership in org acme

    When an acme admin approves the request via the admin surface

    Then user_org_memberships is upserted for jane → acme at member role

    And org_join_requests.status is set to ''approved''

    And decided_by and decided_at are recorded

    When the admin denies the request instead

    Then org_join_requests.status is set to ''denied''

    And jane does not gain membership

    '
- id: REQ-1252
  status: proposed
  group: 10. UI & Admin Surfaces
  category: Org Administration
  priority: SHOULD
  type: ui
  target: 2026-Q4
  description: Admin UI surface (extend OrgsTab.tsx / add admin router endpoint) to list pending org_join_requests
    for the org, approve requests, and deny requests. Display requester email, request date, and admin
    action buttons.
  use_case: Provides org admins with a centralized interface to manage pull-based membership requests.
  code:
  - provisa-ui/admin/orgs/OrgsTab.tsx
  - provisa/api/admin/orgs_router.py
  tests:
  - provisa-ui/e2e/admin-join-requests.spec.ts
  scenario: 'Given an org admin viewing the Orgs admin panel

    When they navigate to the "Pending Requests" tab

    Then pending org_join_requests are listed with requester email, request date

    When they click "Approve" on a request

    Then the approval flow is triggered (REQ-1251)

    When they click "Deny"

    Then the deny flow is triggered (REQ-1251)

    '
- id: REQ-1253
  status: proposed
  group: 11. Platform, Infrastructure & Delivery
  category: Cloud Load Balancing
  priority: MUST
  type: infrastructure
  target: 2026-Q4
  description: 'All protocol surfaces (API port 8000, UI port 3000, Arrow Flight 8815, pgwire 5439, Bolt
    7687, MCP 8009, gRPC 50051) must be fronted by a single shared external passthrough load-balancer
    endpoint per cloud provider, not one LB per protocol. This is required by the subdomain-as-org model
    (REQ-1233): {org}.provisa.dev must resolve to one A record and reach every protocol by preserving
    the destination port to the backend node.'
  use_case: 'Enables the subdomain-as-org identity model where a single DNS name and IP address serve
    all protocols. Cloud-specific realizations: GCP uses a backend-service passthrough NLB forwarding
    rule with all_ports=true on one static IP; AWS uses a single Network Load Balancer with one listener/target-group
    per protocol port sharing the NLB''s endpoint; Azure uses a single Standard Load Balancer with one
    LB rule per protocol port on a single frontend public IP. Backend liveness is gated by the API HTTPS
    /health probe. Refs: REQ-1233, REQ-1239, REQ-1240, REQ-1226.'
- id: REQ-1254
  status: complete
  group: 11. Platform, Infrastructure & Delivery
  category: Cloud Load Balancing
  priority: MUST
  type: infrastructure
  since: 2026-07
  description: Web UI must be published on host port 443 across all cloud Terraform deployments (GCP,
    AWS, Azure). The UI container continues listening on port 3000 with TLS; docker-compose publishes
    ${UI_PORT}:3000 with UI_PORT set to 443 in cloud environments. first-launch.sh persists UI_PORT into
    the systemd EnvironmentFile so provisa start picks it up.
  use_case: The .dev gTLD is HSTS-preloaded in browsers, forcing http to https:443. Publishing the UI
    on port 443 allows cloud.provisa.dev and {org}.provisa.dev URLs to resolve without a port suffix,
    composing with the single shared passthrough LB (REQ-1253) which forwards all ports to the backend
    node preserving destination port.
  code:
  - first-launch.sh
  - terraform/gcp/main.tf
  - terraform/aws/main.tf
  - terraform/azure/main.tf
  - docker-compose.yml
  scenario: 'Given a cloud deployment of Provisa on GCP/AWS/Azure

    When cloud Terraform applies with UI_PORT=443 in the EnvironmentFile

    Then the web UI is reachable at https://cloud.provisa.dev (port 443) or https://{org}.provisa.dev
    (port 443)

    And the UI container continues listening on localhost:3000 with TLS

    And no port suffix is required in the URL due to HSTS enforcement

    '
  related:
  - REQ-1253
  - REQ-1226
- id: REQ-1255
  status: proposed
  group: 13. Multi-Tenancy & Organizations
  category: Org Membership
  priority: SHOULD
  type: behavioral
  target: 2026-Q4
  description: During self-service org creation (REQ-1249), a Firebase user is offered an opt-in checkbox
    or toggle to seed the newly created org with the pre-federated demo configuration, including demo
    sources (GraphQL, OpenAPI petstore) and sample data tables with pre-registered relationships and governance
    policies.
  use_case: Reduces time-to-value for new orgs by providing immediate hands-on federation examples without
    requiring manual source registration or data import. Users can explore Provisa's query composition,
    relationship discovery, and governance features against real federated sources in seconds.
  code:
  - provisa/api/auth_router.py
  - provisa/api/admin/orgs_router.py
  - provisa/core/schema_admin.py
  - provisa/demo/
  relates_to:
  - REQ-1249
  - REQ-960
  scenario: 'Given a Firebase user without an org invite during signup

    When they select "Create New Org" and see a checkbox "Seed with demo configuration"

    Then org creation proceeds with an optional flag to invoke the demo seeding logic

    And if checked, _seed_built_in_sources() and _seed_ops_pg() run scoped to the new org_id

    And the new org is immediately populated with demo GraphQL/OpenAPI sources, sample tables, and governance
    rules

    '
- id: REQ-1256
  status: proposed
  group: 11. Platform, Infrastructure & Delivery
  category: Kubernetes Deployment
  priority: MUST
  type: infrastructure
  target: 2026-Q4
  description: 'Helm chart must ship a served UI surface: a provisa-ui Deployment + Service exposing the
    SPA on port 3000 internally, reverse-proxying the API backend. PROVISA_API_URL is set to the in-cluster
    API service (https://{release}-provisa:8000 when TLS enabled), matching the fixed provisa/ui_server.py
    reverse-proxy hop.'
  use_case: Enables Kubernetes deployments to serve the full Provisa UI stack (frontend SPA + backend
    federation engine) through a single release, maintaining parity with cloud-VM Terraform deployments.
  code:
  - charts/provisa/templates/ui-deployment.yaml
  - charts/provisa/templates/ui-service.yaml
  - charts/provisa/templates/ui-configmap.yaml
  - charts/provisa/values.yaml
  scenario: 'Given a Helm release deployed to a Kubernetes cluster

    When the Provisa chart is installed with UI enabled

    Then a provisa-ui Deployment is created running the SPA on port 3000

    And a Service exposes the UI internally

    And PROVISA_API_URL env var points to https://{release}-provisa:8000 (or http if TLS disabled)

    And the reverse proxy routes /api/* requests to the in-cluster provisa API service

    '
- id: REQ-1257
  status: proposed
  group: 11. Platform, Infrastructure & Delivery
  category: Kubernetes Deployment
  priority: MUST
  type: infrastructure
  target: 2026-Q4
  description: 'Helm chart must support operator TLS on every endpoint (REQ-1226 parity): PROVISA_TLS_CERT
    and PROVISA_TLS_KEY env vars, a TLS cert Secret mounted into provisa and provisa-ui containers, uvicorn
    --ssl-certfile/--ssl-keyfile flags, and /health probes using scheme: HTTPS when TLS enabled.'
  use_case: Ensures Kubernetes deployments meet enterprise TLS requirements for all protocol endpoints,
    matching the Terraform cloud-VM deployment model where the operator can supply their own certificates
    or use cert-manager integration.
  code:
  - charts/provisa/templates/provisa-deployment.yaml
  - charts/provisa/templates/ui-deployment.yaml
  - charts/provisa/templates/tls-secret.yaml
  - charts/provisa/values.yaml
  scenario: 'Given a Helm release with TLS enabled in values.yaml

    When the chart mounts a TLS Secret containing tls.crt and tls.key

    Then PROVISA_TLS_CERT and PROVISA_TLS_KEY point to the mounted cert paths

    And the provisa container starts uvicorn with --ssl-certfile and --ssl-keyfile

    And livenessProbe and readinessProbe use scheme: HTTPS

    And the provisa-ui reverse proxy communicates with the API over HTTPS

    '
- id: REQ-1258
  status: proposed
  group: 11. Platform, Infrastructure & Delivery
  category: Kubernetes Deployment
  priority: MUST
  type: infrastructure
  target: 2026-Q4
  description: 'Helm provisa Service must expose all protocol endpoints served by the API container: pgwire
    (5439), Bolt (7687), MCP (8009), gRPC (50051), in addition to API (8000) and Arrow Flight (8815).'
  use_case: Allows external clients to reach every Provisa protocol (SQL, graph, LLM, RPC) through the
    Kubernetes Service endpoint, matching the multi-protocol API design and enabling parity with cloud-VM
    deployments where all ports are exposed through the passthrough load balancer.
  code:
  - charts/provisa/templates/provisa-service.yaml
  - charts/provisa/values.yaml
  scenario: 'Given a Helm release deployed to Kubernetes

    When the provisa Service is created

    Then the Service exposes named ports for: api (8000), flight (8815), pgwire (5439), bolt (7687), mcp
    (8009), grpc (50051)

    And clients can connect to each protocol through the Service ClusterIP or LoadBalancer endpoint

    '
- id: REQ-1259
  status: proposed
  group: 11. Platform, Infrastructure & Delivery
  category: Kubernetes Deployment
  priority: MUST
  type: infrastructure
  target: 2026-Q4
  description: 'Helm chart cert provisioning must be a values.yaml toggle: supply an existing TLS Secret
    name, or specify cert-manager issuer configuration to auto-provision certificates. Self-signed generation
    via openssl is NOT used — certificate source is Kubernetes-native (Secret or cert-manager).'
  use_case: Provides enterprise-grade certificate management without requiring manual self-signed generation,
    enabling operators to integrate their PKI (cert-manager, external CA) directly into the Helm values
    configuration.
  code:
  - charts/provisa/templates/tls-secret.yaml
  - charts/provisa/templates/certificate.yaml
  - charts/provisa/values.yaml
  scenario: 'Given a Helm values.yaml with tls configuration

    When tls.enabled: true and tls.secretName is provided

    Then the chart uses that existing Secret without generating certificates

    When tls.enabled: true and tls.certManager is configured with an issuer

    Then cert-manager creates a Certificate resource

    And the issued TLS cert is automatically mounted into provisa and provisa-ui containers

    And the user is logged into the fully-seeded org

    '
- id: REQ-1260
  status: complete
  group: 11. Platform, Infrastructure & Delivery
  category: Test Infrastructure
  priority: SHOULD
  type: infrastructure
  since: 2026-07
  description: Linux-only RLIMIT_AS memory-bounded streaming tests (test_streaming_memory_bounded_e2e.py)
    run on macOS via Docker in the "linux-mem" lane of scripts/test-all. The lane auto-enables when host
    is macOS and docker CLI is present, boots a real PostgreSQL in a Linux container, and provides external
    connectivity via PROVISA_MEM_TEST_DSN.
  use_case: Allows memory-bounded test suite to run on macOS development machines without manual Docker
    setup, ensuring streaming correctness under memory constraints is verified across platforms.
  code:
  - scripts/test-all
  - docker/mem-lane.Dockerfile
  - scripts/_mem-lane-entrypoint.sh
  - tests/integration/test_streaming_memory_bounded_e2e.py
  - tests/conftest.py
  - tests/integration/conftest.py
  tests:
  - tests/integration/test_streaming_memory_bounded_e2e.py
  scenario: 'Given a macOS host with Docker CLI present

    When scripts/test-all is run

    Then the linux-mem lane auto-enables

    And a Linux container is booted with PostgreSQL

    And the streaming tests run inside the container pointing to PROVISA_MEM_TEST_DSN

    And test environment disables isolated-stack provisioning (PYTEST_NO_DOCKER=1) and Trino wait (PROVISA_SKIP_TRINO_WAIT=1)

    And on Linux hosts the lane is a no-op (core lane already runs tests natively)

    '
- id: REQ-1261
  status: proposed
  group: 2. Authentication & Identity
  category: Multi-Tenant Onboarding
  priority: MUST
  type: behavioral
  target: 2026-Q4
  description: 'Self-service multi-tenant onboarding with four coupled workstreams: (1) sign-up → create-org
    → become-org-admin workflow where any authenticated user creates a new organization (org_<id> PG schema
    + role_<id>), specifying zero or more allowed email domains that will auto-join via domain matching,
    and becomes its admin, replacing the single-admin bootstrap gate; (2) split super-admin roles into
    platform-scoped (tenant/org/user/billing/provisioning management, zero standing data access) and org-scoped
    (data plane: sources, model, members, secrets within one org_<id>), with cross-plane data access requiring
    audited, time-boxed break-glass elevation; (3) request-to-join by known org identifier (org name or
    GUID), reusing the approval queue pattern from creation_requests; (4) domain-based auto-join where
    a user''s verified email domain is matched against an organization''s allowed-domains list captured
    at org creation, automatically joining the user to the org if a match is found. Deferred to future
    release: admin-initiated clickable invite links (org_invites accept flow).'
  use_case: Enables SaaS multi-tenant usage where each organization manages its own users, data sources,
    and semantic model independently. Platform operators cannot accidentally/intentionally leak org data
    — control-plane and data-plane admin roles are separated by design, preventing the code conflation
    in provisa/api/admin/schema_common.py:134 and schema_query.py:223. Request-to-join by org identifier
    provides a self-service alternative to admin-initiated invites, reducing operational overhead. Replaces
    REQ-1266 single-admin bootstrap (mutually exclusive at runtime).
  scenario: 'Given a new user navigating to the login page

    When the user signs up with email/password or OAuth

    Then they verify their account

    And create a new organization (provisioning org_<id> schema and role_<id>)

    And specify zero or more allowed email domains (e.g., @company.com)

    And become the admin of that org

    When another user with a verified email matching one of the org''s allowed domains signs up

    Then they are automatically routed to and joined in that organization

    And become a member (not admin) with appropriate default role

    When a user with a non-matching email domain signs up

    Then they create their own org or later request to join a known org by name/GUID

    When a platform super-admin operator logs in

    Then they can see the list of all orgs and their users/billing

    But cannot read the data inside any org_<id> without break-glass elevation

    When that platform admin attempts to access an org''s data sources

    Then the request is rejected (org admin role is required)

    When the platform admin opens a support ticket and requests temporary access to debug tenant data

    Then they initiate an audited break-glass elevation with a time limit

    And access is logged and reviewed

    When an existing user is out-of-band told an org''s name or GUID

    Then at signup or in org-join settings they supply the org name/GUID

    And a request-to-join is created and routed to that org''s admin

    When an org admin reviews the request in an approval queue (same pattern as creation_requests)

    Then they approve or deny the request

    When approved, a user_org_memberships row is written with a role

    When a user joins an org (creates or requests or via domain match)

    Then they see only that org in their workspace selector

    And can only access resources scoped to that org_<id>

    '
  related_reqs:
  - REQ-063
  - REQ-434
  - REQ-480
  - REQ-1262
  - REQ-1264
  - REQ-1266
  - REQ-1267
- id: REQ-1262
  status: proposed
  group: 2. Authentication & Identity
  category: Multi-Method Authentication
  priority: MUST
  type: behavioral
  target: 2026-Q4
  description: 'Login page surfaces multiple simultaneous authentication providers (Google, GitHub, email/password),
    advertised by the server as a list. Recommended architecture: Firebase-native multi-method (all yield
    a Firebase ID token validated by one provider) plus optional server-issued JWT provider, with Firebase
    account-linking ensuring one account per email.'
  use_case: Provides user choice for signup/login and reduces vendor lock-in by supporting multiple identity
    providers on a single login surface.
  scenario: 'Given a login page with multiple providers enabled on the server

    When the server advertises available providers

    Then the login page renders N sign-in surfaces (Google, GitHub, email/password)

    When a user signs in via one provider

    Then they receive a Firebase ID token (or server JWT) validated by the provider

    When the same user signs in again via a different provider linked to the same email

    Then they are logged into the existing account (Firebase account-linking)

    And their org membership and roles are preserved

    '
- id: REQ-1263
  status: complete
  since: 2026-08
  group: 2. Authentication & Identity
  category: API Token Management
  priority: MUST
  type: behavioral
  description: Personal Access Tokens (PATs) enable long-lived credentials for NON-BROWSER protocol access
    (pgwire/Postgres, Bolt/Neo4j, Arrow Flight, gRPC, MCP). Tokens are user-generated in the UI, stored
    hashed with owner + org + scopes/role + expiry + last-used. Every protocol resolves tokens to AuthIdentity
    via a single AuthProvider.validate_token method, reusing the central identity provider and enforcing
    per-org scope. A validated token resolves to its OWNER'S account — user id, email and display name
    from the user profile — so audit and usage reporting attribute the act to the person, identically
    to an interactive session; the token's own label is carried as a separate claim recording WHICH of
    that person's credentials was used. Token management is self-service on the user's own profile, where
    the secret is displayed exactly once at issuance and the listing shows only a display prefix and lifecycle
    timestamps.
  use_case: Allows programmatic clients (Python/Node scripts, BI tools, ETL pipelines) to authenticate
    against all Provisa protocols using the same role/scope model as browser auth, eliminating the need
    for separate credential management per client type.
  scenario: 'Given a user logged into the Provisa UI

    When the user creates a Personal Access Token in the settings

    Then the token is generated and displayed once

    And stored hashed in the database with owner, org, role, and expiry

    When a pgwire/Bolt/Flight/gRPC client sends the token in a request

    Then the server calls AuthProvider.validate_token(token)

    And resolves it to the same AuthIdentity as browser auth

    And the identity carries the owner''s account email and display name, not the token''s label

    And audit rows and usage reports attribute the act to the owner, naming the token separately

    And enforces the token''s org scope and role

    When the token expires

    Then subsequent calls are rejected with 401 Unauthorized

    When the user revokes the token

    Then all subsequent calls are rejected immediately

    '
  code:
  - provisa/auth/pat.py
  - provisa/auth/models.py
  - provisa/auth/wiring.py
  - provisa/api/pat_router.py
  - provisa/api/auth_router.py
  - provisa/api/flight/server.py
  - provisa/api/mcp/server.py
  - provisa/grpc/auth.py
  - provisa/pgwire/server.py
  - provisa/bolt/session.py
  - provisa/core/org_membership.py
  - provisa/core/schema_admin.py
  - provisa-client/provisa_client/client.py
  - provisa-ui/src/components/PersonalAccessTokens.tsx
  tests:
  - tests/integration/test_personal_access_tokens.py
  - tests/unit/test_auth_surface_conformance.py
  - tests/unit/test_bolt_auth.py
  - tests/unit/test_flight_auth.py
  - tests/unit/test_grpc_auth.py
  - provisa-ui/src/__tests__/PersonalAccessTokens.test.tsx
  - provisa-client/tests/test_client.py
- id: REQ-1264
  status: accepted
  group: 2. Authentication & Identity
  category: Platform Admin Break-Glass Access
  priority: MUST
  type: behavioral
  target: 2026-Q4
  description: Platform super-admin is a seeded local break-glass account (username + bcrypt password
    via simple auth provider), created at install with an installation-generated random password. Password
    is not static (never admin/admin) and is surfaced via Terraform output or GCP cloud-init console log,
    never persisted in config. First login enforces password rotation. This account is SSO-independent
    and works when Firebase/OAuth is broken — the exclusive recovery/emergency access path for platform
    operators.
  use_case: Ensures platform operators can always access the system for emergency recovery and initial
    setup, independent of external identity provider availability or misconfiguration. Seeded local admin
    provides break-glass access without relying on Firebase/OAuth uptime.
  scenario: 'Given a fresh Provisa deployment

    When the installer generates a platform-admin account

    Then a random password is generated and logged to Terraform output or cloud-init console

    And the password is NOT written to any config file or documentation

    And the account is created with username platform-admin using bcrypt hashing

    When an operator logs in with the platform-admin account for the first time

    Then the system enforces password rotation before granting full access

    When Firebase or all OAuth providers are offline

    Then the operator can still authenticate via the local break-glass account

    And regain platform access to diagnose and restore the identity provider

    '
  related_reqs:
  - REQ-1261
  - REQ-1262
- id: REQ-1265
  status: proposed
  group: 2. Authentication & Identity
  category: Helm Auth Configuration
  priority: MUST
  type: infrastructure
  target: 2026-Q4
  description: Helm chart deployment MUST NOT require Firebase. Operator specifies auth provider at install
    time via values.yaml (OIDC issuer URL, SAML metadata, LDAP server, or local break-glass). Provisa
    configures AuthProvider accordingly, decoupled from cloud Firebase bootstrap path. Enterprise identity
    providers (corporate OIDC/SAML/LDAP) are the primary target, not Google-only sign-in.
  use_case: Helm chart deployments target enterprises whose identity infrastructure is internal (OIDC/SAML/LDAP),
    not Google. Auth provider configuration at install time enables enterprises to integrate their own
    identity systems directly without external dependency or admin UI manipulation.
  code:
  - charts/provisa/values.yaml
  - provisa/auth/provider.py
  scenario: 'Given a Helm deployment targeting an enterprise

    When the operator sets values.yaml with auth.provider: oidc, auth.oidc.issuerUrl, auth.oidc.clientId,
    etc

    Then Provisa initializes an OIDC AuthProvider using those values

    And users authenticate via the enterprise OIDC server

    When the operator sets auth.provider: saml with auth.saml.metadataUrl

    Then Provisa initializes a SAML AuthProvider

    When the operator sets auth.provider: ldap with auth.ldap.server, auth.ldap.baseDn, etc

    Then Provisa initializes an LDAP AuthProvider

    And Firebase is NOT required, consulted, or initialized

    When the operator sets auth.provider: local (break-glass only)

    Then only the platform-admin break-glass account (REQ-1264) is available

    '
  related_reqs:
  - REQ-1259
  - REQ-1261
  - REQ-1262
  - REQ-1263
  - REQ-1264
- id: REQ-1266
  status: proposed
  group: 2. Authentication & Identity
  category: Cloud Appliance Bootstrap
  priority: SHOULD
  type: behavioral
  target: 2026-Q4
  description: Firebase single-admin bootstrap is an optional convenience for cloud-appliance deployments
    (GCP cloud-init / Terraform). First Google-signed-in user becomes sole initial admin. This path is
    NOT required or supported by Helm/enterprise deployments and is NOT a fallback if OIDC/SAML/LDAP is
    misconfigured. Supersedes informal "REQ-1259" label; that requirement concerns TLS certificates, not
    authentication.
  use_case: 'Cloud appliance deployments targeting individual users or small teams on GCP provide fastest
    time-to-value: zero auth config required, sign in with Google, immediate product access. Enterprises
    use Helm with their own OIDC/SAML/LDAP (REQ-1265) instead.'
  scenario: 'Given a GCP cloud-init-deployed Provisa instance with no auth provider configured

    When the first user navigates to the login page

    Then Google Sign-In is the only option

    When they sign in with their Google account

    Then they become the platform admin and org owner

    When a second user attempts to sign in

    Then they receive 403 Forbidden (single-admin model)

    When the operator invites the second user via email

    Then the second user can sign in and is assigned a role by the first admin

    When the operator configures a Helm chart with auth.provider: oidc

    Then the Firebase bootstrap path is NOT used and is superseded by OIDC (REQ-1265)

    '
  related_reqs:
  - REQ-1261
  - REQ-1262
  - REQ-1264
  tests:
  - tests/integration/test_org_creator_role_request.py
  - tests/unit/test_source_catalog_refresh.py
- id: REQ-1267
  status: proposed
  group: 2. Authentication & Identity
  category: Runtime Auth-Enforcement Gate
  priority: MUST
  type: behavioral
  target: 2026-Q4
  description: A single built image/wheel serves unsecured (demo/none), basic, or firebase/IdP deployments
    and can switch enforcement at runtime WITHOUT a process restart or rebuild. The API exposes /setup/status
    with auth_enabled flag and /auth/provider-type (returns "firebase"|"basic"|null) — both reachable
    before authentication. Auth middleware uses lazy provider resolver for deferred IdP boot and runtime
    reconfiguration. SPA login gate is driven by runtime signals, not build-time flags. This requirement
    supersedes the informal "REQ-1259" label used in auth-enforcement-gate code; the actual REQ-1259 is
    Helm TLS certificate provisioning (unrelated).
  use_case: Enables a single production binary to serve multiple deployment scenarios (unsecured demo,
    enterprise IdP, basic auth) and dynamically switch authentication enforcement at runtime, eliminating
    build-per-deployment overhead and supporting auth migration without downtime.
  scenario: 'Given a Provisa deployment with no auth configured (demo mode)

    When /setup/status is called by an unauthenticated SPA

    Then it returns {"auth_enabled": false}

    And /auth/provider-type returns null

    And the SPA renders no login UI, allows immediate access

    When PROVISA_IDP environment is set to firebase

    Then the auth middleware defers IdP initialization

    And on next request, provider resolves lazily

    And /auth/provider-type returns "firebase"

    And /setup/status returns {"auth_enabled": true}

    And the SPA re-renders with Google Sign-In UI

    When auth configuration is updated at runtime via admin panel

    Then RequireAuth redirects unauthenticated users to /login

    And authFetch attaches bearer token to same-origin REST

    And the system enforces the new auth scheme without restart

    When server switches from unsecured to firebase mode

    Then existing sessions remain valid during transition

    And new sessions use the firebase provider

    '
  related_reqs:
  - REQ-120
  - REQ-1265
  - REQ-1266
- id: REQ-1268
  status: complete
  group: 13. Multi-Tenancy & Organizations
  category: Per-Request Multi-Org Data Plane
  priority: MUST
  type: structural
  description: OrgRuntime dataclass encapsulates a single org's slice of AppState; OrgRegistry holds dict[org_id,
    OrgRuntime] + per-org asyncio.Lock for safe double-checked builds with no TTL. current_org ContextVar
    selects the active org; AppState property shims resolve the ContextVar-selected runtime or default
    to the default-org runtime when unset. require_current_org() raises when unset (no silent fallback).
  use_case: 'Enables request-scoped multi-org isolation: each request runs against its designated org''s
    isolated state, catalogs, and connection pools without global state mutation or cross-org leakage.'
  code:
  - provisa/app_state.py
  - provisa/multitenancy/org_runtime.py
  tests:
  - tests/unit/test_org_runtime.py
  since: 2026-07
- id: REQ-1269
  status: complete
  group: 13. Multi-Tenancy & Organizations
  category: Org-Prefixed Catalog Naming
  priority: MUST
  type: constraint
  description: org_prefixed_catalog(org_id, base, *, default_org) returns bare base name for default org,
    else org_{id}__{base}. System/fixed-warehouse catalogs are never prefixed. Prevents cross-org catalog
    name collisions when multiple orgs share the same demo source IDs.
  use_case: Avoids catalog name collisions in shared orchestrator environments (e.g., pooled Trino) when
    multiple orgs instantiate identical demo sources.
  code:
  - provisa/multitenancy/catalog_naming.py
  tests:
  - tests/unit/test_org_isolation.py
  - tests/integration/test_org_auto_join.py
  since: 2026-07
- id: REQ-1270
  status: complete
  group: 13. Multi-Tenancy & Organizations
  category: HTTP Org-Routing Middleware
  priority: MUST
  type: behavioral
  description: OrgRoutingMiddleware routes each HTTP request to the active org's runtime, built on-demand
    from persisted seeded_demo config, gated on multitenancy feature flag. Resolves current_org ContextVar
    before handlers execute.
  use_case: Ensures every request executes within its designated org's isolated AppState, enforcing tenant
    isolation at the HTTP boundary.
  code:
  - provisa/middleware/org_routing.py
  tests:
  - tests/unit/test_org_isolation.py
  scenario: 'Given multitenancy is enabled

    When an HTTP request arrives with org_id in the routing context

    Then OrgRoutingMiddleware resolves the org''s OrgRuntime

    And current_org ContextVar is set before handlers execute

    When a handler accesses AppState properties

    Then they resolve via ContextVar-selected org''s runtime

    When the request completes

    Then current_org ContextVar is cleaned up

    '
  since: 2026-07
- id: REQ-1271
  status: complete
  group: 13. Multi-Tenancy & Organizations
  category: Self-Service Org Creation
  priority: SHOULD
  type: behavioral
  description: Any authenticated (non-anonymous) user may POST /admin/orgs and becomes org_admin; anonymous
    requests rejected 401 under auth. Membership is granted synchronously in the admin plane; org_admin
    role assignment in the tenant plane granted asynchronously after schema provisioning.
  use_case: Enables users to self-provision new orgs without platform admin intervention, supporting multi-org
    SaaS workflows.
  tests:
  - tests/unit/test_org_create_provisioning.py
  code:
  - provisa/api/admin/orgs_router.py
  scenario: 'Given an authenticated user

    When they POST /admin/orgs with {org_id, org_name, include_demo}

    Then they immediately receive provisioning_state: "provisioning"

    And an async task begins provisioning the org schema

    When the async task completes successfully

    Then provisioning_state transitions to "ready"

    When an anonymous user attempts POST /admin/orgs

    Then they receive 401 Unauthorized

    '
  since: 2026-07
- id: REQ-1272
  status: complete
  group: 13. Multi-Tenancy & Organizations
  category: Async Org Provisioning
  priority: MUST
  type: behavioral
  description: POST /admin/orgs returns immediately with provisioning_state="provisioning"; a background
    task executes provision_org + build_org_runtime + grant_org_role, then updates provisioning_state
    to "ready" (or "failed"+provisioning_error). orgs table gains provisioning_state, provisioning_error,
    seeded_demo columns. Errors are persisted, not swallowed.
  use_case: Decouples org creation from slow provisioning operations (schema build, connection pool initialization),
    enabling responsive UI feedback during multi-org onboarding.
  code:
  - provisa/api/admin/orgs_router.py
  - provisa/multitenancy/org_provisioning.py
  tests:
  - tests/integration/test_create_org_onboarding.py
  - tests/unit/test_org_create_provisioning.py
  scenario: 'Given a user creates a new org

    When the POST /admin/orgs response is sent with provisioning_state: "provisioning"

    Then a background task runs provision_org + build_org_runtime

    And the user can poll GET /admin/orgs/{id}/status

    When provisioning completes

    Then provisioning_state updates to "ready" atomically

    When provisioning fails

    Then provisioning_state updates to "failed"

    And provisioning_error is persisted with the error details

    '
  since: 2026-07
- id: REQ-1273
  status: complete
  group: 13. Multi-Tenancy & Organizations
  category: Org Readiness Notification Seam
  priority: MAY
  type: behavioral
  description: notify_org_ready(org_id, user_id) stub called when async provisioning completes; currently
    logs only. Defers email implementation; reserved for future notification channels (email, in-app,
    webhooks).
  use_case: Provides a seam to inject notifications (email, dashboard alerts) once an org is ready for
    use, supporting future multi-channel notification infrastructure.
  code:
  - provisa/multitenancy/org_provisioning.py
  scenario: 'Given an org has completed async provisioning successfully

    When provisioning_state updates to "ready"

    Then notify_org_ready(org_id, creator_user_id) is called

    And the function logs the notification event

    When future notification backends are implemented

    Then this seam can be extended to emit email/in-app/webhook notifications

    '
  since: 2026-07
- id: REQ-1274
  status: complete
  group: 13. Multi-Tenancy & Organizations
  category: Org Admin Authorization Fix
  priority: MUST
  type: constraint
  description: _require_org_admin authorization handler allows platform admins any org, confines org_admin
    to their active_org_id (backed by admin-plane membership row), allows dev/no-auth, rejects all else.
    Closes authz hole in create_invite/list_invites/revoke_invite endpoints.
  use_case: Prevents org admins from inviting users to orgs they do not own, enforcing multi-org isolation
    boundary at the authorization layer.
  code:
  - provisa/api/admin/auth.py
  - provisa/api/admin/invites_router.py
  tests:
  - tests/unit/test_tenant_isolation_contract.py
  since: 2026-07
- id: REQ-1275
  status: complete
  group: 13. Multi-Tenancy & Organizations
  category: UI Onboarding Flow
  priority: SHOULD
  type: ui
  description: Member-less authenticated user (authEnabled && token && orgMemberships.length===0) is gated
    to OnboardOrgPage. Form accepts org_id, org_name, include_demo checkbox, submits to POST /admin/orgs,
    polls provisioning status until ready/failed, binds org to localStorage (provisa_org), calls selectOrg,
    refreshes identity via AuthContext.refresh(), then SPA-navigates to /query. AuthContext now exposes
    refresh() method.
  use_case: Guides newly authenticated users with no org membership through self-service org creation
    and onboarding, automating post-signup navigation and org binding.
  code:
  - provisa-ui/src/pages/OnboardOrgPage.tsx
  - provisa-ui/src/context/AuthContext.tsx
  scenario: 'Given a user authenticated via Firebase with no org memberships

    When they load the app

    Then they are routed to OnboardOrgPage

    When they submit the org creation form

    Then provisioning begins and status polling starts

    When provisioning completes (status: "ready")

    Then the org is bound to localStorage

    And AuthContext.refresh() fetches updated identity with org membership

    And the user is navigated to /query

    '
  since: 2026-07
  e2e: true
  e2e_reason: 'Org creation, provisioning poll, and post-onboarding navigation require full stack: backend
    provisioning, identity refresh, and SPA routing integration.'
- id: REQ-1276
  status: complete
  group: 13. Multi-Tenancy & Organizations
  category: Subdomain-based Org Resolution
  priority: MUST
  type: behavioral
  description: 'HTTP request org is authoritative from Host subdomain (leftmost label of acme.provisa.org
    → org acme), except control-plane host cloud.provisa.* which requires explicit x-org-provisa header.
    Apex/bare hosts (provisa.org, localhost) resolve to no org (None). Subdomain/header replaces prior
    active_org token claim and x-org-id header. Membership validation unchanged: org not in user''s orgs
    is rejected 403 (unless user is platform admin).'
  use_case: Establishes Host subdomain as the authoritative org identity source across HTTP transports,
    enabling transparent multi-org routing without requiring org claims in tokens or API headers. Exception
    for control-plane preserves bootstrap and cross-org admin access patterns.
  code:
  - provisa/auth/middleware.py
  tests:
  - tests/unit/test_auth_middleware_multitenancy.py
  scenario: 'Given a request to subdomain host acme.provisa.org

    When the Host header is parsed

    Then the leftmost label (acme) is resolved as the active org

    Given a request to control-plane host cloud.provisa.dev

    When x-org-provisa header is present with value "acme"

    Then the header value determines the active org

    When x-org-provisa header is absent

    Then the active org is None

    Given a request to apex host provisa.org or localhost

    When the Host header is parsed

    Then the active org is None

    Given an authenticated user not a member of the resolved org

    And the user is not a platform admin

    When an API request is made

    Then the request is rejected with 403 Forbidden

    '
  since: 2026-07
- id: REQ-1277
  status: proposed
  group: 2. Authentication & Identity
  category: Member-less User Onboarding
  priority: SHOULD
  type: behavioral
  description: 'A newly authenticated user belonging to zero orgs is offered three onboarding paths: (1)
    join an existing org by entering its org id, (2) create a new org (existing behavior), or (3) auto-redirect
    to join if an existing org''s id matches the user''s email domain.'
  use_case: Extends REQ-1266 by providing member-less users flexibility beyond org creation alone, reducing
    friction for users wanting to join existing teams or leveraging email domain matching for automatic
    org discovery.
  target: 2026-Q4
  scenario: 'Given a user authenticated with zero org memberships

    When they are presented the onboarding page

    Then they see three options: join by org id, create new org, and auto-detect by email domain

    When they enter an org id

    Then they are added to that org and redirected to the workspace

    When they select create new org

    Then the org creation flow proceeds (REQ-1266 behavior)

    When an org exists with id matching their email domain

    Then they are automatically redirected to join that org

    '
  e2e: true
  e2e_reason: Org joining, discovery logic, and post-onboarding routing require full end-to-end verification
    across authentication, org lookup, and UI navigation.
- id: REQ-1278
  status: complete
  group: 11. Platform, Infrastructure & Delivery
  category: Enterprise Deployment Tenancy Enforcement
  priority: MUST
  type: infrastructure
  description: Enterprise deployment modules (terraform/gcp, terraform/aws, terraform/azure) enforce single-tenancy.
    The multitenancy variable is removed; PROVISA_MULTITENANCY is hardcoded to false. Single administrator
    bootstrap is required (REQ-1266).
  use_case: 'Clarifies deployment architecture: enterprise customers deploy isolated single-tenant instances
    with hardcoded tenancy model, eliminating multitenancy configuration surface and reducing operational
    risk.'
  code:
  - terraform/gcp/main.tf
  - terraform/aws/main.tf
  - terraform/azure/main.tf
  since: 2026-07
- id: REQ-1279
  status: complete
  group: 11. Platform, Infrastructure & Delivery
  category: SaaS Multi-tenant Deployment Module
  priority: MUST
  type: infrastructure
  description: 'New terraform/gcp-saas module enables fully-automated multi-tenant SaaS deployments. Multitenancy
    is forced true (not a variable). Architecture includes Cloud SQL managed control plane (private IP
    via Private Service Access), single Trino coordinator (n2-standard-4, DB offloaded) fronting raw-TCP
    protocol listeners behind a shared passthrough NLB, and autoscaled Spot worker MIG (region instance
    group manager, min=0 scale-to-zero → max). Supporting app change: packaging/linux/first-launch.sh
    persists CONFIG_DB_* and PROVISA_EXTERNAL_CONTROL_DB into systemd EnvironmentFile allowlist so the
    coordinator''s control plane points at the external Cloud SQL.'
  use_case: Enables Provisa SaaS operation with automated, cost-optimized infrastructure supporting multiple
    tenants on shared compute and isolated control planes, following docs/deployment/gcp-saas-infra-plan.md.
  code:
  - terraform/gcp-saas/main.tf
  - packaging/linux/first-launch.sh
  since: 2026-07
- id: REQ-1280
  status: accepted
  group: 11. Platform, Infrastructure & Delivery
  category: Commercial Positioning
  priority: MAY
  type: infrastructure
  description: 'Provisa SaaS bills on two metered SKUs mapped to the existing gcp-saas topology (REQ-1279):
    (1) Serving lane — the "active-hour" SKU at $3.25/active-hr, metering warm coordinator uptime (the
    always-on Trino planner + persistent-protocol listeners: pgwire/bolt/Flight/gRPC/MCP). Any hour the
    endpoint is active bills a full hour. Margin ~87–97%. (2) Analytical lane — the "worker-hour" SKU
    at $2.50/worker-hr, metering Spot worker MIG uptime per query, scale-to-zero between queries. Margin
    ~97% Spot / ~89% on-demand. Egress is a cost-recovery passthrough across both lanes at Hasura parity
    ($0.13/GB vs $0.12 cost); only result bytes leaving GCP are Provisa''s cost (source-cloud egress bills
    the source owner).'
  use_case: A customer buys either or both SKUs. Splitting the SKU captures both markets; a single unit
    misprices one (active-hour framing underprices analytical compute ~8x; worker-hour framing cannot
    express a warm always-on endpoint).
  related_reqs:
  - REQ-073
  - REQ-1075
  - REQ-1279
  since: 2026-07
  competitive_position:
    status: parity
    rationale: Metered active-hour (Hasura v2 $3.00/active-hr advanced source) and worker-hour (Starburst
      Galaxy Pro $3.00/worker-hr) are the established billing units in each respective market; Provisa's
      two-SKU model captures both by pricing $3.25/active-hr and $2.50/worker-hr respectively.
- id: REQ-1281
  status: complete
  group: 11. Platform, Infrastructure & Delivery
  category: SaaS Billing
  priority: SHOULD
  type: behavioral
  description: 'The active hours the REQ-1454 meter records are POSTED to the merchant of record as Lemon
    Squeezy usage records, since the Starter price is usage-based (scheme volume, usage_aggregation sum,
    first tier a $25 fixed_fee) and the invoice therefore carries the units Provisa posts and nothing
    else -- without this every Starter org is billed the bare minimum whatever it runs. Records are posted
    against the SUBSCRIPTION ITEM, not the subscription: its id appears only inside the subscription payload
    (first_subscription_item), so orgs.ls_subscription_item_id is written at every point a paid subscription
    starts -- webhook activation, its reconciliation backstop, trial conversion, and paid-without-trial.
    An hourly sweep (ten past the hour) reports each billable org''s newly closed hours over [orgs.usage_reported_through,
    current hour) with action=increment, and advances the watermark to that bound only after the record
    is accepted, so a failed post is re-sent whole by the next sweep and an accepted one is never counted
    twice. The watermark opens at the PAID start, leaving the trial''s own hours below it and free; a
    later plan change re-points the item id and never reopens the watermark. Only closed hours are reported
    -- the accruing hour would be posted again next sweep -- and an idle span advances the watermark without
    posting. An hour that only served egress bytes (ops=0) is metered but is not an active hour and is
    not billed. This is the single writer of metered units, which is what lets REQ-1469''s bill read its
    metered line off the posted records rather than compute a third independent number.'
  use_case: A Starter customer''s invoice charges the hours they actually used above the $25 minimum,
    computed once by the meter and posted to the merchant of record rather than estimated separately by
    the bill page.
  related_reqs:
  - REQ-1280
  - REQ-1075
  - REQ-1279
  - REQ-1452
  - REQ-1454
  - REQ-1469
  code:
  - .claude/commercial/provisa_commercial/usage.py
  - .claude/commercial/provisa_commercial/lemonsqueezy_client.py
  - .claude/commercial/provisa_commercial/org_db.py
  - .claude/commercial/provisa_commercial/schema.py
  - .claude/commercial/provisa_commercial/models.py
  - .claude/commercial/provisa_commercial/trial.py
  - .claude/commercial/provisa_commercial/router.py
  - .claude/commercial/provisa_commercial/__init__.py
  tests:
  - .claude/commercial/tests/test_usage_reporting.py
  - .claude/commercial/tests/test_trial_lifecycle.py
  - .claude/commercial/tests/test_billing_webhook_idempotency.py
  since: 2026-08
  scenario: 'Given a Starter org whose paid subscription opened at 12:00 and which queried in the 12:00
    and 13:00 hours

    When the reporting sweep runs at 14:10

    Then a usage record of quantity 2 is posted against the org''s subscription item with action=increment
    and orgs.usage_reported_through advances to 14:00, and a second sweep over the same span posts nothing'
  target: 2026-Q4
- id: REQ-1282
  status: complete
  group: 13. Multi-Tenancy & Organizations
  category: Post-provisioning Welcome Screen
  priority: SHOULD
  type: ui
  description: 'After an org reaches provisioning_state=ready, OnboardOrgPage renders a welcome phase
    displaying: (a) org is ready, (b) creator is org administrator, (c) invitation guidance with link
    to /team, (d) role management guidance with link to /security/roles, and navigation buttons: Invite
    teammates, Manage roles, Go to workspace.'
  use_case: Provides clear post-provisioning feedback and guidance for newly created orgs, establishing
    org_admin identity and reducing friction to inviting team members and configuring roles.
  code:
  - provisa-ui/src/pages/OnboardOrgPage.tsx
  - provisa-ui/src/context/AuthContext.tsx
  scenario: 'Given an org creator''s new org reaches provisioning_state=ready

    When OnboardOrgPage loads

    Then the org is selected and identity is refreshed via AuthContext.refresh()

    And org_admin capabilities are resolved

    And a welcome phase is rendered displaying org readiness, creator admin role, and invite/role guidance

    When the user clicks "Invite teammates"

    Then navigation occurs to /team

    When the user clicks "Manage roles"

    Then navigation occurs to /security/roles

    When the user clicks "Go to workspace"

    Then navigation occurs to /query

    '
  since: 2026-07
  e2e: true
  e2e_reason: Welcome phase rendering requires integration of provisioning state polling, org binding,
    identity refresh, and capability resolution before SPA navigation occurs.
- id: REQ-1283
  status: complete
  group: 13. Multi-Tenancy & Organizations
  category: Org-admin Team Management
  priority: SHOULD
  type: ui
  description: Org-admin Team page (/team) gated on user_management capability (held by org_admin template
    role); allows org_admin to create/copy/revoke invite links scoped to active org and choose role for
    each invitee on redemption. Backed by org-admin-scoped endpoints POST/GET/DELETE /admin/invites (REQ-1274).
    NavBar exposes "Team" link gated on user_management.
  use_case: Enables org_admin self-service team member invitation with role assignment, decoupling member
    provisioning from superadmin-only /admin/orgs tab and supporting decentralized org administration.
  code:
  - provisa-ui/src/pages/TeamPage.tsx
  - provisa-ui/src/components/NavBar.tsx
  - provisa-ui/src/App.tsx
  - provisa-ui/src/i18n/locales/en/team.json
  - provisa-ui/src/i18n/locales/en/navBar.json
  related_reqs:
  - REQ-1274
  - REQ-1266
  scenario: 'Given a user is an org_admin in their active org

    When they navigate to /team

    Then the page loads and displays org-admin controls

    When they create an invite link

    Then a new invite is created scoped to the active org

    When they copy an invite link

    Then the link URL is copied to clipboard

    When they assign a role to an invitee on creation

    Then the role is persisted and applied on invite redemption

    When they revoke an invite link

    Then the invite is marked invalid

    When the user does not have user_management capability

    Then access to /team is denied

    When the NavBar renders

    Then a "Team" link appears if user has user_management

    And the link routes to /team

    '
  since: 2026-07
  e2e: true
  e2e_reason: 'Team page functionality requires full stack integration: capability gating, org scoping,
    API round-trips for invite create/list/delete, role assignment, and NavBar link visibility based on
    capability resolution.'
- id: REQ-1284
  status: complete
  group: 2. Authentication & Identity
  category: Organization Access Control
  priority: MUST
  type: behavioral
  description: Organizations can define an optional email-address rule (regex pattern) for membership.
    When a user redeems an invite to join that organization, the server validates the authenticated user's
    email against the rule. If the email does not match, the join is rejected. Organizations with no rule
    accept any email.
  use_case: Organizations need to restrict membership to users with company email addresses (e.g., @acme.com)
    or other domain-based rules to ensure data governance and compliance within trusted user pools.
  code:
  - provisa/api/auth_router.py
  - provisa/core/schema_admin.py
  tests:
  - tests/integration/test_redeem_invite.py
  - tests/unit/test_org_join_policy.py
  - tests/e2e/test_platform_auth_flow.py
  since: 2026-07
  scenario: 'Given an organization with an email rule "@acme\\.com$"

    When a user with email "user@acme.com" redeems an invite

    Then the join succeeds and the user is added to the organization


    Given an organization with an email rule "@acme\\.com$"

    When a user with email "user@rival.com" redeems an invite

    Then the join is rejected due to email mismatch


    Given an organization with no email rule

    When a user with any valid email redeems an invite

    Then the join succeeds regardless of email domain


    Given an organization admin

    When they create an organization

    Then they can set an optional email validation regex

    When they edit the organization

    Then they can update or clear the email rule

    '
  e2e: true
  e2e_reason: 'Email validation during invite redemption requires end-to-end verification through the
    invite API: token validation, user authentication, email extraction, regex matching, and org join
    transaction.'
  competitive_position:
    status: parity
    rationale: Email domain restrictions for organization membership are standard in multi-tenant SaaS
      platforms (Slack, GitHub, Atlassian). Provisa offers the same capability, allowing orgs to enforce
      membership policies at the email domain level.
- id: REQ-1285
  status: proposed
  group: 2. Authentication & Identity
  category: Organization Access Control
  priority: SHOULD
  type: behavioral
  target: 2026-Q4
  description: Organizations may enable an "auto-join" flag with a configured default role (e.g., a low-privilege
    read-only role like "analyst"). When auto-join is enabled, any newly authenticated user whose email
    matches the org's email rule (or any user if no email rule exists) is automatically granted membership
    in that org with the default role — no explicit invite needed. When auto-join is disabled (the default),
    joining requires an explicit invite. Auto-join depends on the org email-rule feature (REQ-1284) to
    scope which accounts may self-join.
  use_case: Auto-join reduces friction for domain-scoped organizations by eliminating the need for explicit
    invites when email-based membership rules already verify eligibility. Users matching the email rule
    gain immediate access with a safe default role, speeding time-to-first-query.
  code:
  - provisa/api/auth_router.py
  - provisa/core/schema_admin.py
  tests:
  - tests/integration/test_redeem_invite.py
  scenario: 'Given an organization with auto_join=true and auto_join_role="analyst"

    When a newly authenticated user with email "user@acme.com" first signs in

    And the org has an email rule "@acme\\.com$"

    Then the user is automatically added to the organization

    And assigned the "analyst" role without needing an invite


    Given an organization with auto_join=true and no email rule

    When a newly authenticated user signs in

    Then the user is automatically added to the organization

    And assigned the configured auto_join_role


    Given an organization with auto_join=false (the default)

    When a newly authenticated user signs in

    Then no automatic membership is granted

    And the user must redeem an invite to join


    Given an organization admin

    When they create or edit an organization

    Then they can enable/disable auto_join

    And specify the default role for auto-joined members

    '
  e2e: true
  e2e_reason: 'Auto-join membership grant requires end-to-end verification through a real authentication
    flow: email extraction from the IdP, email rule validation, automatic membership insertion (platform
    plane), and role assignment (tenant plane) — all observable only through the /auth/me endpoint and
    membership checks after sign-in.'
  related_reqs:
  - REQ-1284
  - REQ-1266
  competitive_position:
    status: parity
    rationale: Auto-join with email domain scoping is standard in multi-tenant SaaS (Slack Teams, GitHub
      org auto-add). Provisa offers comparable convenience while maintaining email-rule-based governance
      control.
- id: REQ-1286
  status: complete
  group: 2. Authentication & Identity
  category: Identity Resolution
  priority: MUST
  type: constraint
  since: 2026-07
  description: The default org ID has a single source of truth from the control plane's resolved org_id
    (the value that names the tenant schema org_<id>). config.default_org_id is None by default and only
    overrides when explicitly set; if neither resolves, the system raises rather than guessing. Unresolved-identity
    states (server error, network failure, auth failure) must be distinguished from resolved identities
    with zero org memberships — the UI must not collapse failed /auth/me into "no account access".
  use_case: Three divergent literals ("root" in config, "default" in control plane, "root" hardcoded in
    registry seed) caused active_org_id to name an org whose org_<id> schema was never created, leading
    to /auth/me 500 errors and false "no account access" reports. A single source of truth prevents org
    ID aliasing. Distinguishing error states from empty memberships ensures the UI routes users forward
    (to OnboardOrgPage for org creation/join) instead of dead-ending with a generic access-denied message.
  code:
  - provisa/auth/wiring.py
  - provisa/core/models.py
  - provisa/core/schema_admin.py
  - provisa/core/control_plane.py
  - provisa/api/startup_seed.py
  - provisa/api/auth_router.py
  - provisa-ui/src/api/admin.ts
  - provisa-ui/src/context/AuthContext.tsx
  - provisa-ui/src/App.tsx
  - provisa-ui/src/i18n/locales/en/onboardGate.json
  tests:
  - tests/unit/test_default_org_id_resolution.py
  - provisa-ui/src/__tests__/OnboardGate.test.tsx
  - provisa-ui/e2e/onboarding-gate.spec.ts
  scenario: 'Given a Provisa instance with no config.default_org_id set

    When the control plane resolves an org_id from deployment context

    Then that org_id is the canonical identifier for the tenant schema org_<id>

    And /auth/me reports state.org_id from the control plane, not a literal fallback


    Given a Provisa instance with both control plane org_id and config.default_org_id set

    When an authenticated user calls /auth/me

    Then config.default_org_id takes precedence (explicit override)


    Given neither control plane nor config.default_org_id resolves

    When the system starts

    Then it raises an error rather than guessing a default


    Given a /auth/me request that fails at the server (5xx error)

    When the UI receives the response

    Then OnboardGate renders a "can''t reach Provisa / retry" screen distinct from the login screen

    And does not report the failure as an access decision


    Given an identity that successfully resolves with zero org memberships

    When the user first signs in

    Then OnboardGate routes to OnboardOrgPage (create/join org)

    And does not render the "access denied" screen

    '
  e2e: true
  e2e_reason: Identity resolution correctness and error-state routing are only verifiable through a full
    authentication flow with real server response codes and UI navigation.
  competitive_position:
    status: ahead
    rationale: Explicit single source of truth for default org ID and precise error-state handling (distinguishing
      server failure from access denial) prevents silent data model mismatches and improves UX during
      onboarding and transient failures. Competitors like Hasura typically collapse all /auth failures
      into generic "unauthorized" without differentiating network/server errors from auth denials.
- id: REQ-1287
  status: complete
  group: 13. Multi-Tenancy & Organizations
  category: Org Invitations
  priority: SHOULD
  type: behavioral
  description: 'Onboarding independently answers three questions: whether the user has a Provisa account,
    whether they have a pending org invitation, and whether they have org membership. Addressed invitations
    store the invitee''s email in the org_invites table, enabling the UI to surface one-click accepts;
    link invites remain shareable without email.'
  use_case: Users with pending org invitations can directly accept them on arrival without providing an
    invite token. The onboarding flow correctly distinguishes between account creation, org invitation
    acceptance, and org membership scenarios.
  code:
  - provisa/core/schema_admin.py
  - provisa/api/auth_router.py
  - provisa-ui/src/pages/OnboardOrgPage.tsx
  tests:
  - tests/integration/test_redeem_invite.py
  - tests/unit/test_onboarding_invites.py
  since: 2026-07
  scenario: 'Given an invited user arrives at the onboarding page without an invite token

    When their identity email matches an open, unexpired org invitation

    Then the UI displays the invitation and offers one-click accept

    And failed my-invites lookup does not block org creation

    '
- id: REQ-1288
  status: complete
  group: 2. Authentication & Identity
  category: Platform Admin Bootstrap
  priority: MUST
  type: behavioral
  since: 2026-07
  description: 'Before any superadmin exists (bootstrap_superadmin enabled and no superadmin_bootstrap
    row), a public GET /auth/bootstrap-status endpoint returns {"unclaimed": bool}. The login page displays
    a first-login notice warning that whoever signs in first becomes platform administrator, then shows
    provider choices. The endpoint bypasses bearer-token validation in _SKIP_PATHS.'
  use_case: Transparent disclosure of the bootstrap-admin privilege grant prevents surprise superadmin
    escalation and ensures the first authenticator understands the security implications. The public endpoint
    enables the UI to probe bootstrap status independently of authentication state.
  code:
  - provisa/api/auth_router.py
  - provisa/auth/middleware.py
  - provisa-ui/src/api/admin.ts
  - provisa-ui/src/pages/LoginPage.tsx
  - provisa-ui/src/i18n/locales/en/loginPage.json
  tests:
  - tests/integration/test_bootstrap_status.py
  - tests/unit/test_auth_middleware.py
  - provisa-ui/src/__tests__/LoginPage.test.tsx
  - tests/unit/test_auth_middleware_multitenancy.py
  - provisa-ui/e2e/onboarding-gate.spec.ts
  scenario: 'Given bootstrap_superadmin is enabled and no superadmin_bootstrap row exists

    When a client calls GET /auth/bootstrap-status

    Then a 200 response is returned with {"unclaimed": true}

    And no bearer token is required


    Given bootstrap_superadmin is enabled and a superadmin_bootstrap row exists

    When a client calls GET /auth/bootstrap-status

    Then a 200 response is returned with {"unclaimed": false}


    Given an unauthenticated user navigates to the login page

    When bootstrap status returns unclaimed=true

    Then a notice displays above provider buttons

    And states that the first sign-in will grant platform admin privileges

    And provider choices render normally


    Given an unauthenticated user navigates to the login page

    When bootstrap status returns unclaimed=false

    Then no first-login notice appears


    Given the UI cannot reach the bootstrap-status endpoint (network/server error)

    When the login page renders

    Then provider choices still appear

    And the failure to fetch bootstrap status does not block sign-in UI rendering

    '
  e2e: true
  e2e_reason: Bootstrap-status disclosure requires verification of unauthenticated endpoint access, login
    page rendering conditional on bootstrap state, and integration of the status probe with the provider-type
    UI flow — only achievable through an end-to-end authentication session.
  related_reqs:
  - REQ-1266
  competitive_position:
    status: ahead
    rationale: Explicit first-login platform-admin disclosure at sign-in time (before any credential is
      created) is unusual in multi-tenant SaaS; most platforms grant admin silently or require email verification.
      Provisa's transparent notice prevents misconfiguration surprises and aligns with compliance-first
      principles for self-hosted deployments.
- id: REQ-1289
  status: complete
  group: 13. Multi-Tenancy & Organizations
  category: Onboarding
  priority: MUST
  type: behavioral
  since: 2026-07
  description: 'Onboarding never dead-ends: a stored credential that the deployment rejects (/auth/me
    returns 401/403) is discarded by OnboardGate, which returns the user to the sign-in page to authenticate
    or create an account. Onboarding independently answers three orthogonal questions — does the user
    have a Provisa account, do they have a pending org invitation, do they have org membership — each
    with its own forward path. Transient server failures (5xx or unreachable) retain a separate retry
    screen (REQ-1286) and are not treated as access decisions.'
  use_case: Users with stale credentials (e.g., post-password-reset, post-org-removal) must be able to
    re-authenticate rather than encountering a terminal "No account access" message. Platforms with an
    unclaimed admin slot cannot tell users "ask an administrator for an invitation," so onboarding must
    independently decompose the account-presence, invitation-presence, and membership-presence decisions
    and provide forward paths for each.
  code:
  - provisa-ui/src/components/OnboardGate.tsx
  tests:
  - provisa-ui/src/__tests__/OnboardGate.test.tsx
  - provisa-ui/e2e/onboarding-gate.spec.ts
  scenario: 'Given an authenticated user with a stored credential

    When /auth/me returns 401 or 403

    Then OnboardGate discards the credential and redirects to sign-in

    And the user can re-authenticate or create a new account


    Given an unauthenticated user navigating to onboarding

    When their identity email matches an open org invitation

    Then the invitation is surfaced and accepted with one click

    And they are not blocked by missing account creation


    Given an unauthenticated user navigating to onboarding

    When they have a Provisa account but no org membership

    Then the UI offers to create or join an organization

    And they progress forward without a "no account access" dead-end


    Given an unauthenticated user navigating to onboarding

    When /auth/me is unreachable or returns 5xx

    Then a transient-failure retry screen appears (REQ-1286)

    And the user is not incorrectly treated as lacking account access

    '
  e2e: true
  e2e_reason: Credential rejection, identity resolution, onboarding UI routing, and server error handling
    are only verifiable through a live-stack round-trip from browser through API with real authentication
    responses and UI navigation.
  related_reqs:
  - REQ-1286
  - REQ-1287
  competitive_position:
    status: ahead
    rationale: Most SaaS onboarding platforms render a terminal "access denied" message for any auth failure
      without decomposing account-presence, invitation-acceptance, and membership-status. Provisa's orthogonal-question
      model with forward paths for each scenario (with or without admin presence) is uncommon in cloud
      offerings.
- id: REQ-1290
  status: complete
  group: 1. Access Governance & Security
  category: Authentication
  priority: MUST
  type: behavioral
  since: 2026-07
  description: Claiming the sole platform-administrator slot is an explicit act via POST /auth/claim-bootstrap,
    never a side effect of authentication. AuthMiddleware only reads the superadmin_bootstrap singleton;
    the endpoint first-writes the id=1 row (first writer wins), returns 404 when bootstrap mode is off
    and 401 without identity, and reads back the holder on losing claims.
  use_case: Prevents stale credentials held by browsers from silently reactivating platform admin on page
    refresh before the user reads the REQ-1288 first-login disclosure. Explicit claim ensures the user
    understands the implications and selects a sign-in provider on the login page after reading the disclosure.
  code:
  - provisa/api/auth_router.py
  - provisa/core/schema_admin.py
  tests:
  - tests/integration/test_redeem_invite.py
  - tests/unit/test_auth_middleware_multitenancy.py
  - tests/e2e/test_platform_auth_flow.py
  - tests/e2e/test_error_code_contract.py
  scenario: 'Given bootstrap mode is on and the admin slot is unclaimed

    When an authenticated user POSTs /auth/claim-bootstrap after reading the first-login disclosure

    Then the user''s identity is written to superadmin_bootstrap.holder_id

    And the endpoint returns 200 with the holder


    Given bootstrap mode is on and another user already claimed the slot

    When a different authenticated user POSTs /auth/claim-bootstrap

    Then the endpoint returns 200 with the existing holder''s identity

    And the second user is not made admin


    Given bootstrap mode is off

    When a user POSTs /auth/claim-bootstrap

    Then the endpoint returns 404


    Given bootstrap mode is on and the slot is unclaimed

    When an unauthenticated user POSTs /auth/claim-bootstrap

    Then the endpoint returns 401


    Given bootstrap mode is on and the slot is unclaimed

    When AuthMiddleware processes an authenticated request

    Then it only reads superadmin_bootstrap (does not write)

    And the slot remains unclaimed even after multiple authenticated requests


    Given bootstrap mode is on and the admin slot is unclaimed (single-tenant deployment)

    Then /auth/login and onboarding do not return 403 "registration closed"

    And the claim endpoint remains reachable to allow bootstrap slot claiming

    '
  e2e: true
  e2e_reason: Authentication state, bootstrap mode state, first-write claim semantics, endpoint routing
    (404/401/200), and AuthMiddleware read-only enforcement are only verifiable through a live end-to-end
    auth flow with real state changes to the superadmin_bootstrap singleton.
  related_reqs:
  - REQ-1288
  competitive_position:
    status: ahead
    rationale: Most multi-tenant platforms grant platform admin implicitly (by email domain, first account,
      or pre-seeded credentials) without requiring explicit acknowledgment. Provisa's explicit claim prevents
      accidental admin elevation and ensures every deployment operator consciously reads the first-login
      disclosure before becoming the sole admin.
- id: REQ-1291
  status: complete
  group: 1. Access Governance & Security
  category: Authentication
  priority: MUST
  type: behavioral
  since: 2026-07
  description: AuthProvider takes an authVersion prop that is bumped by App whenever a token is stored
    or dropped. The provider re-runs the identity bootstrap (fetchMe + roles/domains) when authVersion
    changes, ensuring a just-signed-in user resolves to their real identity instead of staying at userId=null.
  use_case: Without authVersion, a just-signed-in user remained at userId=null because the identity fetched
    at mount was never revisited. OnboardGate then read the fresh token alongside the stale (null) identity,
    read it as a rejected credential, deleted it, and sent the user back to sign-in — an unbreakable loop.
  code:
  - provisa-ui/src/context/AuthContext.tsx
  - provisa-ui/src/App.tsx
  tests:
  - provisa-ui/src/__tests__/OnboardGate.test.tsx
  - provisa-ui/e2e/onboarding-gate.spec.ts
  scenario: 'Given an unauthenticated user at the sign-in page

    When they sign in and a token is stored

    And App bumps authVersion

    Then AuthProvider re-fetches /auth/me

    And userId resolves to the real identity

    And OnboardGate does not delete the fresh token

    '
  e2e: true
  e2e_reason: Token storage side-effects, AuthProvider re-bootstrap, and OnboardGate routing logic are
    only verifiable end-to-end through a full browser render cycle.
- id: REQ-1292
  status: complete
  group: 1. Access Governance & Security
  category: Authentication
  priority: MUST
  type: behavioral
  since: 2026-07
  description: An unclaimed platform-admin slot outranks org onboarding in the routing gate. A token-holding
    user is dropped back to sign-in instead of org creation when the admin slot is still unclaimed.
  use_case: A valid credential from before the admin-plane was wiped would otherwise skip sign-in entirely
    (where the first-login disclosure renders) and land on "create an organization" on a deployment with
    no administrator. This prevents accidental onboarding without understanding the implication of claiming
    the sole admin slot.
  code:
  - provisa-ui/src/components/OnboardGate.tsx
  - provisa/auth/middleware.py
  tests:
  - provisa-ui/src/__tests__/OnboardGate.test.tsx
  - provisa-ui/e2e/onboarding-gate.spec.ts
  scenario: 'Given bootstrap mode is on and the admin slot is unclaimed

    And a user holds a valid credential

    When the user navigates to the app

    Then /auth/bootstrap-status returns unclaimed=true

    And OnboardGate drops the token

    And the user is redirected to sign-in

    And org onboarding is not rendered

    '
  e2e: true
  e2e_reason: Bootstrap status resolution, token deletion, and OnboardGate routing are only verifiable
    through end-to-end browser navigation with real bootstrap state.
- id: REQ-1293
  status: complete
  group: 1. Access Governance & Security
  category: Data Isolation
  priority: MUST
  type: constraint
  since: 2026-07
  description: The tenant plane is isolated by schema (org_<id>), not row-level predicates. Admin resolvers
    must not additionally filter rows by org_id because rows seeded into an org schema carry org_id='root'.
    A failed tables query must render an error, not an empty grid.
  use_case: Schema isolation is the security boundary between tenants. A row-level org_id predicate that
    failed for seeded rows with org_id='root' caused an org_admin of a non-root org to see vanishing domains,
    and a failed query rendering as an empty table read as "org has no data" instead of "query failed".
  code:
  - provisa/api/admin/schema_query.py
  - provisa-ui/src/pages/TablesPage.tsx
  tests:
  - provisa-ui/src/__tests__/TablesPageLoadError.test.tsx
  - tests/e2e/test_platform_auth_flow.py
  scenario: 'Given a tenant-plane query in an org_<id> schema

    When the query executes

    Then it operates only on rows in that schema

    And no row-level org_id filter is applied

    And a failed query renders an error, not an empty result

    '
  e2e: true
  e2e_reason: Schema routing, query failure handling, and UI error rendering are only verifiable end-to-end
    through a request from UI to server with real schema isolation.
- id: REQ-1294
  status: complete
  group: 1. Access Governance & Security
  category: Authentication
  priority: MUST
  type: ui
  since: 2026-07
  description: 'After claiming the platform-admin slot the user is shown a welcome modal stating three
    things and nothing else: they are now this deployment''s platform_admin and the administrator of the
    root org, which already holds the demo assets; backup platform_admins start with an invitation to
    join root, after which the platform_admin assigns them the platform_admin role (REQ-1298); and the
    site is now open for org creation — the next person who signs in creates their own organization. The
    flag is read once at mount and cleared when dismissed.'
  use_case: Claiming the sole admin slot is an irreversible act that happens behind a provider redirect
    — the user clicks "Sign in with Google" and lands in the app with no statement of what they now are.
    This modal is that statement. A single platform_admin account is a deployment-level single point of
    failure, so the moment the role is granted is the moment to say backups are possible. And the operator
    needs to know the deployment's gate has opened — nobody else can claim platform admin, but anyone
    signing in from here creates an org. Telling the platform admin to go invite someone instead conflates
    platform administration with org administration.
  code:
  - provisa-ui/src/components/PlatformAdminWelcomeModal.tsx
  - provisa-ui/src/App.tsx
  tests:
  - provisa-ui/src/__tests__/PlatformAdminWelcomeModal.test.tsx
  scenario: "Given a user has just claimed the platform-admin slot\nAnd the CLAIMED_ADMIN_FLAG is set\
    \ in localStorage\nWhen the app shell renders\nThen PlatformAdminWelcomeModal appears\nAnd it names\
    \ the user as this deployment's platform_admin\nAnd it names the user as the administrator of the\
    \ root org, which already holds the demo assets\nAnd it states that backup platform_admins start with\
    \ an invitation to join root, after which they are\n  assigned platform_admin\nAnd it states that\
    \ the site is now open for org creation on the next sign-in\nAnd it does not tell the platform admin\
    \ to invite people into an org they administer\nAnd clicking dismiss clears the flag\n"
  e2e: true
  e2e_reason: Modal lifecycle (showing exactly once across navigations) and localStorage coordination
    between LoginPage and the app shell are only verifiable end-to-end.
- id: REQ-1295
  status: complete
  group: 1. Access Governance & Security
  category: Authentication
  priority: MUST
  type: constraint
  since: 2026-07
  description: The UI may only place an ASSIGNED role in the X-Provisa-Role header. When auth is enforced
    and the roles query fails or yields no assigned role, AuthContext reports the error and leaves the
    role list empty — it must never fall back to a fabricated full-capability admin role. The DEFAULT_ADMIN_ROLE
    fallback applies only where auth is not enforced (dev mode).
  use_case: The server honors X-Provisa-Role only for roles the user is assigned (provisa/auth/middleware.py),
    so handing an org_admin a client-side admin role makes every subsequent request 403 "Role 'admin'
    is not assigned to this user" — the exact failure a self-service org creator hit when a roles query
    failed and the client fabricated a fallback.
  code:
  - provisa-ui/src/context/AuthContext.tsx
  - provisa/auth/middleware.py
  tests:
  - provisa-ui/src/context/__tests__/AuthContext.roles.test.tsx
  - tests/integration/test_org_creator_role_request.py
  - tests/e2e/test_platform_auth_flow.py
  scenario: 'Given an authenticated user with assigned roles

    When AuthContext resolves roles

    Then only assigned roles are offered to select


    Given auth is enforced

    When the roles query fails

    Then AuthContext reports the error

    And the role list is empty

    And no fallback admin role is fabricated


    Given auth is NOT enforced (dev mode)

    When the roles query fails

    Then AuthContext offers DEFAULT_ADMIN_ROLE

    And the error is not reported

    '
  e2e: true
  e2e_reason: Role filtering, error reporting, and X-Provisa-Role header placement on requests are only
    verifiable end-to-end through the full auth stack with real role assignments.
- id: REQ-1296
  status: proposed
  group: 1. Access Governance & Security
  category: Authentication
  priority: MUST
  type: behavioral
  since: 2026-07
  description: A claimed platform-admin slot lands the administrator in a populated deployment, not an
    empty one. The bootstrap org id is 'root' and its tenant schema is org_root — this is the value the
    control-plane resolver returns (REQ-1286), not a literal layered over it, so there is still one source
    of truth for the default org id. It is provisioned with the demo assets (registered sources, tables,
    the ops and meta domains, relationships and views) before the first sign-in completes. The claiming
    identity is written as a member of 'root' holding platform_admin — so they administer root as well
    as the deployment — and 'root' is their active_org_id, so the first screen after the welcome modal
    renders real data and a real role.
  use_case: The first sign-in on a wiped multitenant deployment claimed the slot and then rendered "No
    roles configured" and "You do not have permission to view this page" with no tables, no ops or meta
    domain, and no demo data. Nothing in REQ-1288/1290/1292/1294 said what a freshly claimed platform
    admin should hold — those cover only the claim ceremony — so the post-claim state was unspecified
    and the deployment shipped an empty one. A platform administrator with nothing to look at cannot tell
    a working deployment from a broken one.
  code:
  - provisa/api/auth_router.py
  - provisa/api/startup_seed.py
  - provisa/core/org_provisioning.py
  - provisa/core/org_membership.py
  tests:
  - tests/integration/test_first_login_bootstrap_admin.py
  scenario: 'Given a wiped deployment with bootstrap mode on and the admin slot unclaimed

    Then an org with id ''root'' exists

    And its tenant schema is org_root

    And the control-plane resolver returns ''root'' as the default org id

    And ''root'' carries the demo assets: registered sources, registered tables, the ops and meta domains


    Given the slot is unclaimed

    When a user POSTs /auth/claim-bootstrap and wins the claim

    Then the claiming identity is a member of ''root''

    And the identity holds platform_admin in ''root''

    And /auth/me returns active_org_id ''root'' with a non-empty org_memberships list


    Given the platform admin has just claimed the slot

    When the app shell loads the query screen

    Then the role selector offers platform_admin

    And the demo tables and the ops and meta domains are listed

    '
  e2e: true
  e2e_reason: The post-claim state spans the control plane (org row, membership), the tenant plane (org
    schema, seeded demo assets, role assignment), and the UI's role and domain rendering — only a live
    first-login walk shows whether the administrator actually lands on data.
  related_reqs:
  - REQ-1290
  - REQ-1294
  - REQ-1297
- id: REQ-1297
  status: proposed
  group: 1. Access Governance & Security
  category: Authorization
  priority: MUST
  type: behavioral
  since: 2026-07
  description: 'Every org schema is seeded with exactly five default roles: platform_admin, org_admin,
    analyst, developer, modeler. These five ids are the whole vocabulary — there are no aliases. The role
    ids ''admin'' and ''superadmin'' are retired: they exist today as both seeded role rows and as the
    platform-bypass keywords in provisa/api/auth_router.py and the UI capability gates, and both uses
    are replaced by platform_admin. Existing assignments naming the retired ids are rewritten to platform_admin
    at seed time; nothing resolves them afterward. platform_admin is the deployment-wide control-plane
    administrator (the role the bootstrap claim grants), responsible for org lifecycle, infra/engine settings,
    and recovery operations. The platform-bypass (has_platform_bypass in provisa/security/rights.py) is
    scoped to control-plane surfaces only and does not bypass any data-plane capability check, even in
    root; platform_admin holds no standing data capabilities anywhere. org_admin administers data-plane
    operations in a single org — members, invites, sources, governance, querying — in every org including
    root. developer builds against the data — query development, view and relationship authoring, full
    results, write. analyst reads — usage and query development, no authoring or governance. modeler is
    the discovery role and the ONLY seeded role holding ignore_relationships, so it may join across relations
    the approved relationship catalog does not yet cover; that is how a model is DETERMINED. Every other
    governance check (column visibility, RLS, masking, domain access) still applies to it, and testing
    the result of a modelling change is done by querying as a role WITHOUT ignore_relationships, which
    ENFORCES the model. analyst deliberately does not hold it — the least-privileged default never breaks
    out of the model. The retired discovery_mode request flag on POST /data/sql, which let ANY caller
    waive the capability check, the domain check and the relationship guard at once from the request body,
    is DELETED — a break-out is a grant on a role, never a field a client sets on itself. All five are
    system roles (org_id NULL, identical capability sets in every org) and are not editable through the
    roles admin surface.'
  use_case: The seeded catalog was ad hoc — 'admin' and 'analyst' existed by accident of the boot seed
    and 'org_admin' was added later for self-service org creation, leaving no role for a person who builds
    views and relationships but must not administer anything. A fixed four-role catalog gives every fresh
    org and every fresh deployment the same starting vocabulary. Separating platform_admin (control-plane,
    no data access) from org_admin (all data-plane administration, including in root) makes the platform/org
    administration split explicit in the role id and capability set, enabling auditable recovery and preventing
    silent data access.
  code:
  - provisa/core/schema.sql
  - provisa/security/rights.py
  - provisa/auth/middleware.py
  tests:
  - tests/integration/test_first_login_bootstrap_admin.py
  - tests/integration/test_invite_role_authz.py
  - tests/integration/test_system_roles_seed.py
  scenario: 'Given a newly provisioned org schema

    Then roles platform_admin, org_admin, analyst, developer and modeler exist

    And each has org_id NULL

    And no role with id ''admin'' or ''superadmin'' exists


    Given an existing deployment carrying assignments to ''admin'' or ''superadmin''

    When the seed runs

    Then those assignments name platform_admin

    And the platform-bypass check recognizes platform_admin only


    Given the five default roles

    Then platform_admin carries the platform-bypass capabilities for control-plane operations only

    And org_admin, developer, analyst and modeler do not

    And modeler is the only seeded role holding ignore_relationships

    And platform_admin holds no standing data-plane capabilities in any org


    Given a user holding developer

    When they author a view or a relationship

    Then it is permitted

    And administering members, invites or masking is denied


    Given a user holding analyst

    When they run a query

    Then it is permitted

    And authoring a view or relationship is denied


    Given a platform_admin querying a tenant org they do not hold org_admin in

    When they attempt to access the org''s data

    Then the query is rejected

    And platform-bypass does not exempt the access


    Given a request to edit or delete a default role through the roles admin surface

    Then it is rejected

    '
  e2e: false
  related_reqs:
  - REQ-1296
  - REQ-1266
  - REQ-1303
  impact_note: 'Corrected 2026-07-29. Prior recording conflated platform_admin''s control-plane operations
    with standing data access in all orgs. Corrected text reflects the settled design: platform_admin
    is control-plane only with zero data capabilities. platform-bypass is control-plane only.'
- id: REQ-1298
  status: proposed
  group: 1. Access Governance & Security
  category: Authorization
  priority: MUST
  type: behavioral
  since: 2026-07
  description: A backup platform_admin is made in two steps, never by a second bootstrap claim. An existing
    platform_admin invites the person into the root org, the person redeems the invite and becomes a member
    of root, and the platform_admin then assigns them the platform_admin role in root. The bootstrap slot
    stays claimed by its original holder and is never reopened; platform_admin is a role held in root,
    and holding it there is what confers deployment-wide administration.
  use_case: The bootstrap claim is first-writer-wins and single-use, so it cannot be the way a second
    administrator is created — yet a deployment whose only platform_admin loses their account is a deployment
    nobody can administer. Routing backups through root membership plus a role assignment reuses the invite
    and role-assignment surfaces that already exist, keeps the claim ceremony irreversible, and makes
    the set of platform administrators readable as the platform_admin assignments in one org rather than
    as a hidden singleton row.
  code:
  - provisa/api/admin/invites_router.py
  - provisa/api/auth_router.py
  - provisa/core/org_membership.py
  tests:
  - tests/integration/test_first_login_bootstrap_admin.py
  - tests/integration/test_invite_role_authz.py
  scenario: 'Given a claimed bootstrap slot and a platform_admin signed in

    When they invite a person into the root org

    And that person redeems the invite

    Then the person is a member of root

    And the person does not hold platform_admin yet


    Given a member of root who does not hold platform_admin

    When a platform_admin assigns them the platform_admin role in root

    Then the person holds deployment-wide administration

    And the superadmin_bootstrap holder is unchanged


    Given the bootstrap slot is already claimed

    When any user POSTs /auth/claim-bootstrap

    Then the existing holder is returned

    And no second claim is granted


    Given a user who is not a platform_admin

    When they attempt to assign platform_admin to anyone

    Then the request is denied

    '
  e2e: true
  e2e_reason: The flow crosses the invite control plane, invite redemption, root-org membership, and a
    tenant-plane role assignment — only a live two-account walk shows the second administrator actually
    gains administration without a second claim.
  related_reqs:
  - REQ-1290
  - REQ-1294
  - REQ-1296
  - REQ-1297
- id: REQ-1299
  status: proposed
  group: 1. Access Governance & Security
  category: Authorization
  priority: MUST
  type: behavioral
  since: 2026-07
  description: The deployment can never be left with zero platform_admins. Revoking the platform_admin
    role from the last holder, and deleting or disabling that holder's account, are both rejected. While
    exactly one platform_admin exists the admin overview and the health endpoint carry a standing warning
    naming the risk and the two-step fix (REQ-1298) — a warning, not a block, because a solo operator
    forced to invent a second account produces ceremony rather than redundancy. Recovery when the sole
    holder is unreachable runs at the infrastructure boundary that already exists - a grant command executed
    on the host, plus the SSO-independent break-glass account of REQ-1264. No long-lived recovery code
    is minted at claim time — a bearer secret conferring permanent deployment-wide administration is a
    larger standing risk than the outage it insures against.
  use_case: A platform_admin who revokes their own role, or an operator who deletes the only administrator's
    account, bricks the deployment - nobody can grant the role back through the product because granting
    it requires holding it. The failure is silent until someone needs administration. Refusing the last
    revocation makes the invariant enforceable rather than advisory, and the standing single-holder warning
    turns the far more likely scenario - the sole administrator becoming unreachable - into something
    the operator was told about while they could still act.
  code:
  - provisa/api/admin/roles_router.py
  - provisa/api/admin/users_router.py
  - provisa/cli
  tests:
  - tests/integration/test_first_login_bootstrap_admin.py
  scenario: 'Given exactly one platform_admin exists

    When any actor revokes platform_admin from that holder

    Then the request is rejected

    And the holder still holds platform_admin


    Given exactly one platform_admin exists

    When any actor deletes or disables that holder''s account

    Then the request is rejected


    Given exactly one platform_admin exists

    When the admin overview renders

    Then a standing single-administrator warning is shown

    And it names the invite-into-root then assign path


    Given two or more platform_admins exist

    When one is revoked

    Then the revocation succeeds

    And the warning is not shown


    Given the sole platform_admin is unreachable

    When an operator with host access runs the platform-admin grant command

    Then the named identity holds platform_admin

    And no recovery code was ever minted or stored

    '
  e2e: false
  related_reqs:
  - REQ-1264
  - REQ-1297
  - REQ-1298
- id: REQ-1300
  status: proposed
  group: 1. Access Governance & Security
  category: Authorization
  priority: MUST
  type: behavioral
  since: 2026-07
  description: An organization can be deleted. Deletion drops the org's tenant schema, its membership
    and invite rows in the control plane, and its entry in the org registry, and evicts its OrgRuntime
    from the registry so no in-flight request resolves it. Only a platform_admin, or the org's own org_admin,
    may delete it. The root org cannot be deleted. Deletion is explicit and irreversible - it is never
    a side effect of removing the last member. Because it is irreversible, the UI gates it behind a stern
    confirmation that names the org being destroyed, states plainly that its data cannot be recovered
    by anyone including the platform_admin, and requires the operator to type the org id to proceed -
    a single click must never be enough to destroy an organization.
  use_case: Self-service org creation without deletion is a one-way ratchet - abandoned orgs accumulate
    schemas, source pools, and runtime entries that nobody can remove through the product, and a person
    who creates an org by mistake has no way to undo it. Making root undeletable keeps the deployment's
    own working org and the platform_admin's membership from being destroyed by an ordinary org operation.
  code:
  - provisa/api/admin/orgs_router.py
  - provisa/core/org_provisioning.py
  - provisa/api/org_runtime.py
  tests:
  - tests/integration/test_org_lifecycle.py
  scenario: 'Given an org with a provisioned schema and members

    When a platform_admin deletes it

    Then the org_<id> schema is dropped

    And its membership and invite rows are removed

    And its OrgRuntime is evicted from the registry

    And a subsequent request naming that org resolves to no org


    Given an org_admin of that org

    When they delete their own org

    Then the deletion succeeds


    Given an org_admin of a different org

    When they delete it

    Then the request is rejected


    Given the root org

    When any actor deletes it

    Then the request is rejected


    Given an org whose last member is removed

    Then the org still exists


    Given an org_admin opening the delete-org control

    Then a confirmation naming the org and stating the data cannot be recovered is shown

    And deletion does not proceed until the org id is typed

    '
  e2e: true
  e2e_reason: Deletion spans the control plane, the tenant schema, and the live OrgRuntime registry -
    only an end-to-end run shows that a deleted org stops resolving for requests already in flight.
  related_reqs:
  - REQ-1271
  - REQ-1296
- id: REQ-1301
  status: proposed
  group: 1. Access Governance & Security
  category: Authorization
  priority: SHOULD
  type: behavioral
  since: 2026-07
  description: The root org is a working org, not a placeholder. The platform_admin registers and governs
    whatever data assets they find useful there, alongside the demo assets it ships with (REQ-1296). Root
    additionally carries a registered view over the org registry - one row per organization with its id,
    name, provisioning state, creation time, and the contact details of each of its org_admins - so the
    deployment's own tenancy is queryable through the same surfaces as any other dataset. The view reads
    the control plane and is read-only; it is scoped to root and is not present in any other org.
  use_case: A platform_admin asked "who runs org acme, and when did it appear" has no answer inside the
    product - the org registry is control-plane state reachable only through admin endpoints, so the operator
    drops to SQL against the admin schema. Exposing it as a registered view in root makes the tenancy
    answerable by the query, graph, and API surfaces the platform already provides, and gives root a reason
    to exist beyond holding the demo.
  code:
  - provisa/api/startup_seed.py
  - provisa/core/org_provisioning.py
  tests:
  - tests/integration/test_first_login_bootstrap_admin.py
  scenario: 'Given a provisioned root org

    Then a registered view over the org registry exists in root

    And each row carries the org id, name, provisioning state and creation time

    And each row carries the contact details of that org''s org_admins


    Given a platform_admin querying root

    When they select from the org registry view

    Then every organization in the deployment is returned


    Given any org other than root

    Then the org registry view is not present


    Given a write against the org registry view

    Then it is rejected

    '
  e2e: false
  related_reqs:
  - REQ-1296
  - REQ-1300
- id: REQ-1302
  status: proposed
  group: 1. Access Governance & Security
  category: Authorization
  priority: MUST
  type: behavioral
  since: 2026-07
  description: No organization can be left with zero org_admins by an ordinary administrative act. Revoking
    org_admin from the last holder in an org is rejected, as is removing that holder's membership. The
    org is not nagged about having a single administrator - a standing warning would be invasive and a
    two-person org forced to invent a second administrator account produces ceremony rather than redundancy.
    Deleting the org is still available to its last org_admin (REQ-1300) - that path is a deliberate,
    confirmed destruction, not an accidental orphaning.
  use_case: An org whose sole administrator is revoked cannot invite anyone, cannot grant roles, and cannot
    delete itself - every act of administration requires the role that nobody now holds. Its members can
    still query, so the failure stays invisible until someone needs to change something. Refusing the
    last revocation removes the accidental route into that state; the unexpected-departure route is handled
    by platform_admin-mediated recovery (REQ-1303) rather than by pestering every small org.
  code:
  - provisa/api/admin/roles_router.py
  - provisa/core/org_membership.py
  tests:
  - tests/integration/test_org_lifecycle.py
  scenario: 'Given an org with exactly one org_admin

    When any actor revokes org_admin from that holder

    Then the request is rejected


    Given an org with exactly one org_admin

    When that holder''s membership is removed

    Then the request is rejected


    Given an org with exactly one org_admin

    When its Team page renders

    Then no single-administrator warning is shown


    Given an org with two or more org_admins

    When one is revoked

    Then the revocation succeeds

    '
  e2e: false
  related_reqs:
  - REQ-1299
  - REQ-1300
  - REQ-1303
- id: REQ-1303
  status: proposed
  group: 1. Access Governance & Security
  category: Authorization
  priority: MUST
  type: behavioral
  since: 2026-07
  description: When an org's administrators are unreachable, recovery runs through the platform_admin
    via an explicit, audited operation — not through a silent platform bypass that leaves no trace and
    not through a secret the org was supposed to keep. A platform_admin may grant org_admin in any org
    at their own discretion; whatever verification satisfies them that the request is legitimate is a
    business process outside the product, not an automated check. The grant is a named operation, distinct
    from ordinary querying, and it writes an entry naming the platform_admin and the grantee into THAT
    org's audit trail so the intervention is visible to the org afterward. The same authority runs the
    other way — a platform_admin may revoke org_admin from any user in any org, subject only to the last-org_admin
    invariant (REQ-1302), and that revocation is audited into the org's trail identically. No per-org
    break-glass key is ever minted — platform_admin holds zero standing data capabilities in any org (REQ-1327),
    so a customer-held token would add a permanent bearer secret. Tokens of that kind belong to vendors
    who are cryptographically unable to recover a customer's data; Provisa is not one.
  use_case: This is the contact-the-vendor path every SaaS platform ends up with — Google Workspace, Atlassian
    and GitHub all resolve an orphaned org through an out-of-band proof to the provider, and for a Provisa
    deployment the provider is the platform_admin. Separation of control-plane (platform_admin) from data-plane
    (org_admin) means recovery capability exists only through explicit operations that land in the org's
    own audit trail. Turning a potential silent backdoor into a recorded, revocable intervention enables
    compliance audits (SOC 2, ISO 27001) and prevents platform_admin from bypassing governance in data
    orgs.
  code:
  - provisa/api/admin/orgs_router.py
  - provisa/api/admin/roles_router.py
  - provisa/core/org_membership.py
  tests:
  - tests/integration/test_org_lifecycle.py
  scenario: 'Given an org whose only org_admin is unreachable

    When a platform_admin grants org_admin to a named member of that org

    Then that member holds org_admin

    And an entry naming the platform_admin and the grantee is written to that org''s audit trail

    And the platform_admin does not gain org_admin or data access in that org


    Given an org with two or more org_admins

    When a platform_admin revokes org_admin from one of them

    Then that user no longer holds org_admin

    And the revocation is written to that org''s audit trail


    Given a user who is not a platform_admin

    When they grant or revoke org_admin in an org they do not administer

    Then the request is rejected


    Given any org

    Then no break-glass key or recovery code exists for it

    '
  e2e: false
  related_reqs:
  - REQ-1274
  - REQ-1299
  - REQ-1302
  - REQ-1327
  impact_note: Corrected 2026-07-29. Prior recording stated "the platform_admin already holds every capability
    in every org," which contradicts the settled design where platform_admin is control-plane only (REQ-1327).
    Corrected text reflects that platform_admin has zero standing data capabilities and recovery is audited
    and revocable.
- id: REQ-1304
  status: proposed
  group: 1. Access Governance & Security
  category: Authorization
  priority: SHOULD
  type: behavioral
  since: 2026-07
  description: Pressing delete on an organization downloads that org's configuration before the deletion
    is confirmed. The download is the live config YAML built for that org (REQ-164 build_live_config_yaml,
    resolved through the org's OrgRuntime) - its registered sources, tables, relationships, views, materializations,
    domains, roles and grants - which is enough to recreate the org's shape later. It is configuration,
    not data - the rows in the tenant schema are gone for good, and the confirmation (REQ-1300) still
    says so. The download is not gated by the live_config_export opt-in - that flag exists because a normalized
    patch cannot stay faithful to a hand-authored config file, and this is a standalone snapshot, not
    a patch. A failure to build the config aborts the deletion rather than proceeding without the snapshot.
  use_case: Deletion is irreversible, so the moment before it is the last moment the org's structure exists
    anywhere. An operator who deletes an org they built over months loses not just the rows but every
    source registration, relationship and view definition that made it useful - and those are the expensive
    part to rebuild. Handing the config back on the way out turns an irreversible act into a recoverable
    one for everything except the data itself, which costs nothing to offer and removes most of the reason
    to hesitate over a deletion that should happen.
  code:
  - provisa/api/admin/orgs_router.py
  - provisa/api/admin/config_export.py
  - provisa-ui/src/pages/TeamPage.tsx
  tests:
  - tests/integration/test_org_lifecycle.py
  scenario: 'Given an org_admin pressing delete on their org

    When the confirmation is shown

    Then that org''s live config YAML has been downloaded

    And it carries the org''s sources, tables, relationships, views, domains and roles


    Given a deployment with live_config_export disabled

    When an org_admin presses delete

    Then the config download still happens


    Given an org whose live config cannot be built

    When an org_admin presses delete

    Then the deletion is aborted with the build failure reported


    Given a platform_admin deleting another org

    When the confirmation is shown

    Then that org''s config is downloaded, not root''s

    '
  e2e: true
  e2e_reason: The download is a browser-side effect of pressing delete on a live org - only an end-to-end
    run shows the file arriving before the confirmation resolves.
  related_reqs:
  - REQ-164
  - REQ-1300
- id: REQ-1305
  status: proposed
  group: 1. Access Governance & Security
  category: Authorization
  priority: MUST
  type: behavioral
  since: 2026-07
  description: Removing a person from an org is an org_admin operation and it removes them from both planes.
    Today DELETE /admin/orgs/{org_id}/members/{user_id} requires the platform bypass (the _require_superadmin
    guard, retired by REQ-1297) so an org_admin cannot offboard anyone from their own org, and it deletes
    only the control-plane user_org_memberships row - the tenant-plane user_role_assignments row written
    by grant_org_role/grant_org_admin survives in org_<id>, so re-adding the person silently restores
    the privileges they had before. Removal must be available to the org's own org_admin (and to a platform_admin
    for any org), and must delete the membership row, every user_role_assignments row for that user in
    that org schema, and any personal access token (REQ-1263) scoped to that org for that user, in one
    operation. Offboarding is the mirror of the invite flow (REQ-1283) and belongs on the same Team page.
  use_case: An org_admin who can invite but cannot remove has half an administrative surface - the person
    who leaves the company stays a member until someone with deployment-wide authority is asked to act.
    The two-plane split makes the partial delete worse than an outright missing feature - the operator
    sees the member disappear from the Team page and reasonably concludes the access is gone, while the
    role grant that actually confers capability is still sitting in the tenant schema. A long-lived protocol
    token scoped to the org is the same failure at a longer horizon, since it authenticates without a
    browser session to invalidate.
  code:
  - provisa/api/admin/orgs_router.py
  - provisa/core/org_membership.py
  - provisa-ui/src/pages/TeamPage.tsx
  tests:
  - tests/integration/test_org_lifecycle.py
  scenario: 'Given an org_admin and a member of their org

    When they remove that member

    Then the user_org_memberships row is deleted

    And every user_role_assignments row for that user in org_<id> is deleted

    And every personal access token that user holds scoped to that org is revoked


    Given a removed member who is later re-invited and redeems

    Then they hold only the role the new invitation named


    Given an org_admin of a different org

    When they remove a member from an org they do not administer

    Then the request is rejected


    Given an ordinary member with no user_management capability

    When they remove another member

    Then the request is rejected

    '
  e2e: false
  related_reqs:
  - REQ-1283
  - REQ-1297
  - REQ-1306
- id: REQ-1306
  status: proposed
  group: 1. Access Governance & Security
  category: Authorization
  priority: MUST
  type: behavioral
  since: 2026-07
  description: A user may leave an org on their own. Leaving performs the same two-plane removal as an
    org_admin offboarding them (REQ-1305) and additionally suppresses auto-join (REQ-1285) for that user
    and org, so a deliberate departure is not undone by the next sign-in matching the org's email rule.
    The last org_admin of an org cannot leave it - the last-admin invariant (REQ-1302) applies to departure
    as much as to revocation, and the paths out are to promote another org_admin first or to delete the
    org (REQ-1300). If the org the user leaves is their active_org_id, the next request resolves another
    membership; a user who leaves their only org returns to onboarding (REQ-1287) rather than to an error.
  use_case: Membership can currently only be granted, never given up - a person invited to the wrong org,
    or one who has finished a contract, has no way to detach themselves and must ask an administrator
    of an org they no longer want to be in. Auto-join makes the omission self-inflicting - without an
    exclusion, every removal is reversed on the removed person's next sign-in, so leaving would not stay
    done.
  code:
  - provisa/api/admin/orgs_router.py
  - provisa/core/org_membership.py
  - provisa-ui/src/components/UserProfileModal.tsx
  tests:
  - tests/integration/test_org_lifecycle.py
  scenario: 'Given a member of an org who is not its last org_admin

    When they leave it

    Then their membership and tenant-plane role assignments in that org are removed

    And a later sign-in matching the org''s email rule does not re-join them


    Given the last org_admin of an org

    When they leave it

    Then the request is rejected

    And the response names promoting another org_admin or deleting the org


    Given a user leaving the org that is their active_org_id

    And who belongs to another org

    Then the next request resolves that other org


    Given a user leaving their only org

    Then the next sign-in lands on onboarding

    '
  e2e: true
  e2e_reason: Leaving changes membership, the active org resolution, and which surface the user lands
    on - only an end-to-end run shows the departure resolving to onboarding rather than to a broken session.
  related_reqs:
  - REQ-1285
  - REQ-1302
  - REQ-1305
- id: REQ-1307
  status: proposed
  group: 1. Access Governance & Security
  category: Authorization
  priority: MUST
  type: behavioral
  since: 2026-07
  description: A user may delete their own Provisa account. Deletion leaves every org they belong to (REQ-1306),
    revokes every personal access token they hold (REQ-1263), and removes their user_profile record (REQ-1246)
    and any local_users credential row. It is refused while the user is the last org_admin of any org
    or the last platform_admin of the deployment (REQ-1299, REQ-1302) - the response names each org that
    blocks it, so the user knows exactly what to hand off. Deletion is irreversible and carries the same
    stern confirmation as org deletion (REQ-1300). It does not delete the orgs the user belonged to, nor
    any data they registered - those belong to the org, not to the person.
  use_case: An account that can be created by signing in but never removed is a standing record of a person
    who may have no further relationship with the deployment, and for a SaaS deployment it is also a deletion
    request that has to be satisfiable. Refusing while the user is somebody's last administrator is the
    same invariant as everywhere else - self-deletion must not be the back door that orphans an org the
    other paths were built to protect.
  code:
  - provisa/api/auth_router.py
  - provisa/core/org_membership.py
  - provisa-ui/src/components/UserProfileModal.tsx
  tests:
  - tests/integration/test_org_lifecycle.py
  scenario: 'Given a user who is not the last administrator of anything

    When they confirm account deletion

    Then they are removed from every org they belonged to

    And every personal access token they hold is revoked

    And their user_profile and any local credential row are deleted

    And their orgs and the data registered in them still exist


    Given a user who is the last org_admin of an org

    When they request account deletion

    Then the request is rejected

    And the response names that org


    Given the last platform_admin

    When they request account deletion

    Then the request is rejected


    Given account deletion

    When the confirmation is shown

    Then it states the account cannot be recovered

    '
  e2e: false
  related_reqs:
  - REQ-1246
  - REQ-1299
  - REQ-1306
- id: REQ-1308
  status: proposed
  group: 1. Access Governance & Security
  category: Authorization
  priority: MUST
  type: behavioral
  since: 2026-07
  description: A user can never change their own role in an org. Changing a member's role is an org_admin
    operation, available only to the org_admin of that org (and to a platform_admin for any org, REQ-1303),
    and it writes the change to that org's audit trail naming who made it. This closes the loop the X-Provisa-Role
    header opened - REQ-1295 already requires the UI to place only an ASSIGNED role in that header, and
    this states the matching server-side rule - role assignment is an administrative act, and a request
    that would grant its own sender a capability they were not assigned is rejected whatever surface it
    arrives on.
  use_case: Role selection is presented to the user as a control they operate - the role selector in the
    NavBar - so the boundary between choosing among roles you hold and acquiring one you do not has to
    be enforced by the server rather than by the shape of the UI. Every non-browser protocol carries the
    same role selection over pgwire, Bolt and Flight, where no UI constrains it at all.
  code:
  - provisa/api/admin/roles_router.py
  - provisa/auth/middleware.py
  tests:
  - tests/integration/test_org_lifecycle.py
  scenario: 'Given a member of an org

    When they assign themselves a role

    Then the request is rejected


    Given an org_admin

    When they change a member''s role in their own org

    Then the change takes effect

    And an entry naming the org_admin, the member and the new role is written to the org''s audit trail


    Given a user selecting a role over any protocol

    When the role is not assigned to them in the active org

    Then the request is rejected

    '
  e2e: false
  related_reqs:
  - REQ-1295
  - REQ-1303
  - REQ-1305
- id: REQ-1309
  status: proposed
  group: 1. Access Governance & Security
  category: Authorization
  priority: MUST
  type: behavioral
  since: 2026-07
  description: An org id is validated on creation and immutable thereafter. It must match a conservative
    identifier pattern (lowercase letters and digits only - no hyphen, no underscore - starting with a
    letter, 2-40 characters), because the value becomes both the PostgreSQL schema name org_<id> and the
    Host subdomain (REQ-1276) - today CreateOrgBody.id is an unconstrained str whose only check is a duplicate
    lookup. Reserved ids are rejected - root (REQ-1296), default, public, admin, information_schema, and
    anything beginning pg_ - since each either collides with a real schema or with the deployment's own
    org. The id can never change afterward - renaming an org changes its display name only, leaving the
    schema, the subdomain and every bookmarked URL intact - and the rename UI states that.
  use_case: The org id is not a label, it is a schema name and a hostname, so an unvalidated value is
    a correctness problem before it is a security one - an id containing a quote, a dot, or mixed case
    produces a schema that DDL cannot address or a subdomain that does not resolve, and the failure appears
    during background provisioning rather than at the point of entry. Reserving the names is what keeps
    a self-service creation from colliding with root or with a PostgreSQL system schema. Immutability
    is the other half - once an org id is in DNS, in connection strings, and in the schema name, changing
    it is not a rename but a migration.
  code:
  - provisa/api/admin/orgs_router.py
  tests:
  - tests/integration/test_org_lifecycle.py
  scenario: 'Given a create-org request whose id is not a valid identifier

    When it is submitted

    Then it is rejected before any row is inserted

    And the response names the required format


    Given a create-org request naming a reserved id

    When it is submitted

    Then it is rejected


    Given an existing org id

    When another user creates an org with the same id

    Then it is rejected as already existing


    Given an existing org

    When its name is changed

    Then its id, schema and subdomain are unchanged


    Given any request that would change an org id

    Then it is rejected

    '
  e2e: false
  related_reqs:
  - REQ-1271
  - REQ-1276
  - REQ-1296
- id: REQ-1310
  status: proposed
  group: 1. Access Governance & Security
  category: Authorization
  priority: MUST
  type: behavioral
  since: 2026-07
  description: An invitation addressed to an email address is delivered to that address. Today create_invite
    stores the invitee email but nothing sends anything - there is no SMTP or mail-provider code in the
    codebase - so the only way to see an invitation is GET /auth/my-invites after signing in, which requires
    the invited person to independently discover the deployment and create an account unprompted. The
    message names the org, who invited them, the role they will hold, the expiry, and a link that carries
    them into the redemption flow. Mail delivery is configured like every other backing service, and tests
    exercise it against a local SMTP server the harness starts - the test asserts on the captured message,
    never on a stub or a log line. A link invitation with no email address is unchanged - the org_admin
    distributes it themselves.
  use_case: Invitation is one of the three onboarding questions (REQ-1287) and it is the only one whose
    answer currently depends on the invitee already knowing to look. An invite that exists in a table
    but reaches nobody is the same as no invite for every user who was not told out of band, and it makes
    the expiry (7 days by default) run against a clock the invitee cannot see. Testing against a real
    local SMTP server rather than a mock is what makes the assertion meaningful - a mocked send proves
    the call was made, not that a message a person could act on was produced.
  code:
  - provisa/api/admin/invites_router.py
  - provisa/core/mail.py
  tests:
  - tests/integration/test_invite_delivery.py
  scenario: 'Given an org_admin creating an invitation addressed to an email address

    When the invitation is created

    Then a message is delivered to that address

    And it names the org, the inviter, the role and the expiry

    And it carries a link that opens the redemption flow


    Given an invitation created with no email address

    Then no message is sent

    And the link invitation is returned as before


    Given the integration suite

    When invitation delivery is exercised

    Then it runs against a local SMTP server started by the harness

    And the assertions read the captured message


    Given mail delivery that fails

    Then the invitation creation reports the failure rather than reporting success

    '
  e2e: false
  related_reqs:
  - REQ-1283
  - REQ-1287
- id: REQ-1311
  status: proposed
  group: 1. Access Governance & Security
  category: Authorization
  priority: SHOULD
  type: behavioral
  since: 2026-07
  description: A single user may create at most 100 organizations. The cap is a backstop against runaway
    or automated creation, not a product tier - every org provisions a schema and optionally seeds demo
    data, and nothing currently bounds how many one account can trigger. Orgs the user has deleted (REQ-1300)
    do not count against it. Reaching the cap returns a clear refusal naming the limit.
  use_case: Self-service creation (REQ-1271) is open to any authenticated user, and each creation runs
    background provisioning that builds a schema and a data-plane runtime. Without a ceiling, one account
    - scripted, or simply confused - can consume the deployment's schema budget and provisioning capacity.
    100 is far above any plausible legitimate use and far below the point where it becomes a resource
    problem, which is what a backstop should be.
  code:
  - provisa/api/admin/orgs_router.py
  tests:
  - tests/integration/test_org_lifecycle.py
  - tests/unit/test_org_id_validation.py
  scenario: 'Given a user who has created 100 orgs

    When they create another

    Then the request is rejected naming the limit


    Given a user at the cap

    When they delete one of their orgs

    Then they may create another


    Given a user who belongs to many orgs they did not create

    Then those do not count against their cap

    '
  e2e: false
  related_reqs:
  - REQ-1271
  - REQ-1300
- id: REQ-1312
  status: proposed
  group: 1. Access Governance & Security
  category: Authorization
  priority: MUST
  type: behavioral
  since: 2026-07
  description: Deletion scrubs personal data while preserving the record that the deletion happened. Deleting
    an org (REQ-1300) drops every tenant-scoped row and cascades the control-plane rows keyed to it; what
    survives is the billing and usage record needed to invoice, which names the org and not a person,
    and the platform audit entry recording the deletion. Deleting an account (REQ-1307) replaces every
    reference to that user id - orgs.created_by, org_invites.created_by and used_by, audit attributions
    - with an opaque tombstone token, and erases the personal attributes on user_profile (name, email,
    avatar). References are tombstoned rather than nulled because org_invites.created_by is NOT NULL and
    because a dangling id is worse than an explicit one - the record stays referentially intact and stops
    identifying anyone. Audit entries are never deleted by either operation; an audit trail that erases
    on request is not one.
  use_case: This is the shape every SaaS platform converges on under GDPR and CCPA. Article 17(3) permits
    retention for legal obligation and for establishing or defending legal claims, which is what keeps
    billing records and audit trails out of the erasure - and platforms that delete audit records on request
    fail their own security obligations doing it. Tombstoning is the published practice at GitHub (the
    ghost user), Slack (deactivated account) and Atlassian (account anonymization) for the same reason
    it is right here - the opaque id keeps foreign references and audit history coherent while the person
    behind it becomes unidentifiable. Nulling would require schema changes the NOT NULL columns forbid
    and would destroy the ability to answer who created a still-live org.
  code:
  - provisa/api/admin/orgs_router.py
  - provisa/api/auth_router.py
  - provisa/core/org_membership.py
  tests:
  - tests/integration/test_org_lifecycle.py
  scenario: 'Given an org with members, invites, and registered data

    When it is deleted

    Then every tenant-scoped row is dropped

    And its control-plane membership and invite rows are gone

    And its billing and usage records are retained

    And an audit entry recording the deletion is retained


    Given a user who created an org and several invitations

    When they delete their account

    Then every reference to their user id is replaced with an opaque tombstone

    And their user_profile personal attributes are erased

    And the orgs and invitations themselves still exist


    Given a tombstoned user id

    When an org''s creator is displayed

    Then it shows a deleted-user placeholder rather than a dangling identifier


    Given any deletion

    Then no audit entry is removed

    '
  e2e: false
  related_reqs:
  - REQ-1300
  - REQ-1307
- id: REQ-1313
  status: proposed
  group: 1. Access Governance & Security
  category: Authorization
  priority: MUST
  type: behavioral
  since: 2026-07
  description: An invitation may only confer a role the inviter is entitled to confer, and the role must
    exist in the target org. Today CreateInviteBody.role_id is an unvalidated str stored verbatim and
    passed straight into grant_org_role at redemption, which upserts whatever string it is given - org_invites.role_id
    is a plain Text column with no foreign key, since it references the per-org roles table. The invitation
    must be rejected at creation when the named role does not exist in that org's schema, and platform_admin
    may be named only in an invitation into root (REQ-1298) - an org_admin cannot confer it in their own
    org. Redemption revalidates rather than trusting the stored value, since a role can be removed between
    creation and redemption.
  use_case: This is an escalation path that REQ-1297 arms. Capability resolution reads the global roles
    map by role id without regard to which org the assignment lives in, so a tenant-plane assignment of
    platform_admin resolves deployment-wide capabilities. Once REQ-1297 seeds all four default roles into
    every org schema, an org_admin can invite an accomplice as platform_admin in their own org and produce
    a platform administrator without ever touching root - which is exactly what REQ-1298 declares to be
    the only path, and which nothing currently enforces. Validating at creation also turns a redemption-time
    failure the invitee cannot act on into a refusal the inviter sees immediately.
  code:
  - provisa/api/admin/invites_router.py
  - provisa/api/auth_router.py
  - provisa/core/org_membership.py
  tests:
  - tests/integration/test_invite_role_authz.py
  scenario: 'Given an org_admin creating an invitation naming a role that does not exist in their org

    When it is submitted

    Then it is rejected and no invitation row is written


    Given an org_admin of an org other than root

    When they create an invitation naming platform_admin

    Then it is rejected


    Given a platform_admin creating an invitation into root naming platform_admin

    Then it is accepted


    Given a valid invitation whose role is removed from the org before redemption

    When it is redeemed

    Then the redemption is rejected rather than assigning a role that does not exist

    '
  e2e: false
  related_reqs:
  - REQ-1297
  - REQ-1298
  - REQ-1308
- id: REQ-1314
  status: proposed
  group: 1. Access Governance & Security
  category: Authorization
  priority: MUST
  type: behavioral
  since: 2026-07
  description: An invitation that names no role confers analyst, the least-privileged of the four default
    roles (REQ-1297). CreateInviteBody.role_id currently defaults to None and redemption passes it unconditionally
    into the user_role_assignments upsert, so a link invitation created without a role produces a null
    assignment rather than a member who can do anything. The default is applied when the invitation is
    created, so the stored row always names a concrete role and the invitee is told which one before redeeming.
  use_case: A link invitation is the shareable path an org_admin distributes themselves, and it is exactly
    the case where naming a role per invitee is inconvenient - so it is the case most likely to be created
    without one. Defaulting to the lowest-privilege role is the only safe direction, and resolving it
    at creation rather than at redemption means the invitation, the delivered message (REQ-1310) and the
    redemption all agree on what is being offered.
  code:
  - provisa/api/admin/invites_router.py
  tests:
  - tests/integration/test_invite_role_authz.py
  scenario: 'Given an invitation created with no role named

    Then the stored invitation names analyst


    Given that invitation

    When it is redeemed

    Then the member holds analyst


    Given an invitation created naming a role

    Then the named role is stored unchanged

    '
  e2e: false
  related_reqs:
  - REQ-1297
  - REQ-1310
  - REQ-1313
- id: REQ-1315
  status: proposed
  group: 1. Access Governance & Security
  category: Authorization
  priority: SHOULD
  type: behavioral
  since: 2026-07
  description: An org whose provisioning failed is recoverable, not abandoned. Background provisioning
    records provisioning_state=failed with the error, and there is currently no way forward - no retry
    endpoint exists, the registry row and whatever partial schema the failure left behind persist, and
    the org counts against the creator's cap (REQ-1311). The creator or a platform_admin may retry provisioning,
    which drops any partial schema and runs the build again from the beginning; a failed org may also
    be deleted (REQ-1300) without the confirmation ceremony a provisioned org requires, since it holds
    no data anyone could lose. A failed org does not count against the creation cap.
  use_case: Provisioning builds a schema, seeds roles and optionally loads demo assets, so it can fail
    on anything from a transient connection loss to a bad demo asset - and the state it leaves is a half-built
    schema the user cannot see and cannot address. Without a retry the only recovery is to create a second
    org under a different id, which leaves the first one occupying its id permanently, since ids are immutable
    and reserved by existence (REQ-1309). Making the retry idempotent from a clean slate is what keeps
    a second failure from compounding the first.
  code:
  - provisa/api/admin/orgs_router.py
  tests:
  - tests/integration/test_org_lifecycle.py
  scenario: 'Given an org in provisioning_state failed

    When its creator retries provisioning

    Then any partial org_<id> schema is dropped

    And provisioning runs again from the beginning

    And the state becomes provisioning then ready


    Given an org in provisioning_state failed

    When it is deleted

    Then no typed confirmation is required


    Given a user at the creation cap holding a failed org

    Then the failed org does not count against the cap


    Given a retry that fails again

    Then the state returns to failed with the new error

    '
  e2e: false
  related_reqs:
  - REQ-1272
  - REQ-1300
  - REQ-1311
- id: REQ-1316
  status: complete
  target: 2026-Q4
  group: 3. Source Registration & Data Modeling
  category: Semantic Interchange (Apache Ossie)
  priority: SHOULD
  type: behavioral
  since: 2026-07
  description: Provisa interoperates with Apache Ossie (incubating; formerly Open Semantic Interchange)
    via a boundary converter, not internal adoption. Export renders the governed model as Ossie YAML -
    Table to dataset (source = federated catalog.schema.table, primary/unique keys from column config
    and UniqueConstraint), Column to field (expression = column reference, dialect ANSI_SQL), Relationship
    to relationship (from/to datasets plus column lists). The CANONICAL export surface is a live endpoint
    that derives the document from the governed model on every read (REQ-1321 - the interchange artifact
    is generated from the operating declaration, so it cannot be stale); *.ossie.yaml file download is
    the secondary form. Note the standard defines only the file format, not a service protocol - the live
    endpoint is Provisa's delivery of the standard's document, not a compliance requirement. Import ingests
    an Ossie semantic model (e.g. produced by the dbt or Snowflake converters) and materializes proposed
    Provisa tables and relationships for onboarding - it ingests definitions, never data. UI surface -
    export is reachable from the Model area (endpoint URL plus download); import is an upload flow that
    lands as a review screen of the proposed tables and relationships, which the modeler accepts or trims
    before anything registers - imported definitions never bypass registration review. Internal vocabulary
    is never renamed to Ossie's - the spec is a pre-0.2.0 draft that self-declares breaking changes, so
    coupling is confined to the adapter. If Provisa later adds a metrics object it adopts the Ossie metric
    shape natively (name, per-dialect aggregate expression, datatype, ai_context) so export of that slice
    is lossless. Governance, RLS, lineage, and graph semantics stay internal; they may ride an optional
    `provisa` custom_extensions slot for round-trip fidelity but interop never depends on other tools
    reading it.
  use_case: Ossie is the vendor-neutral semantic-metadata standard backed by Snowflake, Databricks, dbt
    Labs, Salesforce, and 50+ others, and its dataset/field/relationship/metric structure maps nearly
    one-to-one onto Provisa's Table/Column/Relationship model. Export lets every BI and AI tool converging
    on the standard consume Provisa's federated catalog; import bootstraps onboarding from ecosystems
    that already maintain semantic models, instead of hand-registering tables.
  code:
  - provisa/ossie/convert.py
  - provisa/api/admin/ossie_router.py
  - provisa-ui/src/pages/metrics/OssieInterchangePanel.tsx
  tests:
  - tests/unit/test_ossie_convert.py
  - provisa-ui/src/pages/__tests__/ossie-import.test.tsx
  scenario: 'Given a governed Provisa model with tables, columns, and relationships

    When the live Ossie endpoint is read

    Then the returned document validates against the Ossie JSON schema with datasets, fields, and relationships
    intact


    Given a model change after a prior endpoint read

    When the endpoint is read again

    Then the document reflects the current governed model with no regeneration step


    Given an Ossie semantic model produced by another vendor''s converter

    When it is imported

    Then Provisa proposes matching tables and relationships for registration


    Given a Provisa export re-imported into Provisa

    Then governance metadata carried in the provisa custom_extensions slot survives the round trip

    '
  e2e: false
  related_reqs:
  - REQ-1321
- id: REQ-1317
  status: complete
  target: 2026-Q4
  group: 3. Source Registration & Data Modeling
  category: Semantic Metrics
  priority: SHOULD
  type: behavioral
  since: 2026-07
  description: Provisa has a first-class Metric object - a named, governed aggregate definition with no
    grain of its own. Shape follows Apache Ossie's metric natively (name, aggregate ANSI-SQL expression,
    datatype, description, ai_context) plus Provisa governance fields (role visibility, steward/version,
    same pattern as tables). Dataset binding is implicit through the field references inside the expression,
    with relationship-derived joins when an expansion spans datasets. A metric query (metric plus a requested
    dimension set) is expanded by the compiler into GROUP BY SQL inside the single settled query pipeline
    - metrics are a compile-time expansion, never a second pipeline. Metrics appear in the UI as a third
    Model-nav item beside Views and Commands.
  use_case: A metric is neither a derived column (which has per-row grain) nor a view (which has fixed
    grain) - it is an aggregate definition whose grain is bound at query time, the concept every semantic
    layer since BusinessObjects has converged on. Defining it once, governed, gives every consumer the
    same formula for "net revenue" instead of each dashboard, agent, and view re-implementing it. Adopting
    Ossie's shape from birth makes interchange (REQ-1316) lossless for the metric slice with no translation
    layer.
  code:
  - provisa/core/models.py
  - provisa/compiler/metric_expand.py
  - provisa/pgwire/_pipeline.py
  - provisa/core/repositories/metric.py
  - provisa-ui/src/pages/MetricsPage.tsx
  tests:
  - tests/unit/test_metric_expand.py
  - tests/unit/test_metric_admin.py
  scenario: 'Given a governed metric net_revenue with an aggregate expression over order fields

    When a query requests net_revenue grouped by region and month

    Then the compiler expands it to GROUP BY SQL with joins derived from registered relationships

    And executes it through the standard governed pipeline


    Given a metric whose expression references fields the caller''s role cannot see

    When the caller queries the metric

    Then governance denies it exactly as for a direct query on those fields


    Given the Model nav group

    Then Metrics appears as a sibling of Views and Commands

    '
  e2e: false
  related_reqs:
  - REQ-1316
- id: REQ-1318
  status: complete
  target: 2026-Q4
  group: 3. Source Registration & Data Modeling
  category: Views (Governed Computed Datasets)
  priority: SHOULD
  type: behavioral
  since: 2026-07
  description: Views may be defined from metrics. Table gains view_metrics {metrics, dimensions, filters},
    mutually exclusive with view_sql, validated at parse - one view concept, two definition forms, no
    separate view type. A view_metrics view closes the metric's open grain at definition time; the compiler
    generates its SQL, and the view regenerates whenever a referenced metric changes, so it cannot drift
    from the business definition by construction. Free-hand view_sql may also reference metrics inline
    via metric('name'), expanded at compile time with a lineage edge to the Metric object, giving hand-written
    views the same recompile-on-change property when they reference rather than re-implement. All definition
    forms use the existing materialization, refresh, debounce, and live-delivery machinery unchanged,
    since downstream code keys off "table with a definition". UI surface - the existing Views page gains
    a definition-mode toggle (SQL editor vs metric/dimension picker); views do not split into a new nav
    item.
  use_case: Column lineage already tracks which columns feed a free-hand view, but it cannot see definitional
    duplication - a hand-written SUM that re-implements net revenue keeps computing the old formula after
    the business definition changes, and every lineage edge it has is still valid. Referencing the metric
    instead of copying its formula turns that silent drift into an automatic recompile. Composing a metric
    with dimensions is also simply the fastest way to make a correct governed view.
  code:
  - provisa/core/models.py
  - provisa/core/config_loader.py
  - provisa/api/admin/_metric_views.py
  - provisa-ui/src/pages/tables/ViewDefinitionForm.tsx
  tests:
  - tests/unit/test_metric_admin.py
  - provisa-ui/src/pages/__tests__/views-definition-mode.test.tsx
  scenario: 'Given a view defined as view_metrics with metrics [net_revenue] and dimensions [region, month]

    When the net_revenue metric definition changes

    Then the view''s SQL regenerates from the new definition


    Given a table config setting both view_sql and view_metrics

    When the config is parsed

    Then validation rejects it


    Given a free-hand view_sql containing metric(''net_revenue'')

    When the view is compiled

    Then the reference expands to the governed definition and a lineage edge to the metric is recorded


    Given a view_metrics view with materialize enabled

    Then existing MV refresh and debounce machinery applies unchanged

    '
  e2e: false
  related_reqs:
  - REQ-1317
- id: REQ-1319
  status: complete
  target: 2026-Q4
  group: 3. Source Registration & Data Modeling
  category: Semantic Metrics
  priority: MAY
  type: behavioral
  since: 2026-07
  description: 'Every query surface projects metrics through one compiler expansion plus that protocol''s
    native addressing and metadata idiom - the definition (description, ai_context) travels with the value
    everywhere, with no copies. SQL/pgwire: a reserved metrics schema where each metric is a relation
    whose selected/grouped columns are the dimension choice, description surfaced via pg_description.
    GraphQL: a metrics block inside the _aggregate root field (REQ-653) with the definition in introspection
    docs. Bolt/Cypher: a provisa.metric() procedure, plus Fact/Dimension labels on role-tagged tables
    in db.labels and the label map (metric lineage as graph NODES needs a new meta-graph surface and is
    deferred). Arrow Flight: metric flight descriptors returning Arrow tables. gRPC: metric asks ride
    the semantic-SQL form through the proxy''s governed path - the one expansion, no second IR representation
    (REQ-1321). MCP: list_metrics and query_metric tools carrying ai_context, so agents select governed
    meanings instead of composing aggregation SQL. NL: the schema matcher resolves metric vocabulary directly
    to metric plus dimensions. Streaming: view_metrics + materialize + Kafka sink yields push-on-change
    metrics from existing machinery. Observability: metric evaluations traced and exportable as OTel metrics.'
  use_case: Structural aggregates (sum/avg/count) tell a consumer what is summable; metrics tell it what
    is meaningful. Projecting one governed definition into each protocol's own metadata channel - pg catalog
    comments, GraphQL docstrings, graph properties, MCP tool descriptions - means a DBeaver user, a dashboard,
    and an AI agent all read the same meaning of net revenue from the surface they already use, and hallucinated
    formulas become structurally impossible for agents that query metrics by name.
  code:
  - provisa/compiler/metric_expand.py
  - provisa/compiler/aggregate_gen.py
  - provisa/bolt/session.py
  - provisa/api/mcp/tools.py
  - provisa/api/flight/catalog.py
  - provisa/nl/schema_matcher.py
  - provisa/pgwire/catalog_populate.py
  tests:
  - tests/unit/test_metric_tools.py
  - tests/unit/test_bolt_metric_call.py
  - tests/unit/test_nl_metrics.py
  - tests/unit/test_sql_gen_aggregate.py
  scenario: 'Given a governed metric net_revenue

    When a SQL client browses the catalog on pgwire

    Then metrics.net_revenue appears with its description in pg_description


    Given a GraphQL client introspecting a table with enable_aggregates

    Then the _aggregate metrics block lists net_revenue with its definition as documentation


    Given an MCP agent calling list_metrics

    Then it receives names, descriptions, and ai_context, and query_metric executes the governed expansion


    Given the federated graph over Bolt

    Then a Metric node for net_revenue links via DERIVES_FROM to its source columns

    '
  e2e: false
  related_reqs:
  - REQ-1317
  - REQ-1318
  - REQ-1316
- id: REQ-1320
  status: complete
  target: 2026-Q4
  group: 3. Source Registration & Data Modeling
  category: Semantic Metrics
  priority: SHOULD
  type: behavioral
  since: 2026-07
  description: 'The entity/fact modeling roles (REQ-1164) are retained after lowering and projected into
    every surface, instead of being discarded at registration. The spec stays attached to the artifacts
    it generates - same one-source principle as view_metrics - so downstream consumers see "dimension
    with SCD2 history" and "fact at grain X", not plain tables. Projections: Ossie export emits role-faithful
    fact/dimension datasets and flags time dimensions via dimension.is_time (REQ-1316); each measure declared
    in a fact spec auto-registers a Metric object (REQ-1317) whose valid grouping dimensions derive from
    the entity attributes reachable over the fact''s FK relationships; MCP and NL receive the star shape
    (facts, dimensions, join edges) as grounding context and the NL matcher biases join paths fact-to-dimension;
    the federated graph labels nodes :Fact/:Dimension so Bloom renders the star; GraphQL introspection
    docs and pg_description carry role and SCD mode, and enable_aggregates defaults on for facts. The
    lowering itself stays pure - roles are metadata carried alongside, never a change to what is generated.'
  use_case: REQ-1164 captures star/vault semantics at authoring time and evaporates them before any consumer
    can use them - downstream surfaces see undifferentiated tables. Retaining the role turns guesswork
    into derivation everywhere it matters. "Which groupings are valid for this measure" becomes computable,
    agents get the schema-shape context that makes generated queries correct, BI tools that infer star
    roles read them from the catalog instead of guessing, and the Ossie export preserves the fact/dimension
    distinction the standard itself is built around. This is the entity-taxonomy layer BusinessObjects
    universes carried and Ossie lacks - held internally, projected outward where expressible.
  code:
  - provisa/mv/modeling.py
  - provisa/api/admin/modeling_register.py
  - provisa/cypher/label_map.py
  - provisa/compiler/schema_gen.py
  tests:
  - tests/unit/test_modeling.py
  - tests/unit/test_cypher_label_map.py
  scenario: 'Given an entity spec with history=scd2 and a fact spec with declared measures

    When the specs are lowered and registered

    Then the generated MVs and relationships carry their modeling roles as metadata


    Given a fact spec declaring a measure

    When it is registered

    Then a Metric object auto-registers with grouping dimensions derived from reachable entity attributes


    Given the federated graph over Bolt

    Then entity-derived nodes are labeled :Dimension and fact-derived nodes :Fact


    Given an Ossie export of a model built from entity/fact specs

    Then datasets carry their fact/dimension roles and time dimensions are flagged is_time

    '
  e2e: false
  related_reqs:
  - REQ-1164
  - REQ-1316
  - REQ-1317
  - REQ-1319
- id: REQ-1321
  status: complete
  target: 2026-Q4
  group: 0. Architecture & Design Principles
  category: Data-Model-Driven Estate
  priority: MUST
  type: constraint
  since: 2026-07
  description: 'GOVERNING PRINCIPLE - declarative from source to consumer, and the declaration is the
    entire basis of platform operation. The unbroken declarative chain (source registration -> conformed
    entity/fact REQ-1164/REQ-1320 -> metric REQ-1317 -> view/materialization REQ-1318 -> surface projection
    REQ-1319 -> interchange REQ-1316) is not documentation of the system; it is the system''s only input.
    Every operational behavior - queries compiled, MVs built, lineage recorded, protocol schemas served,
    definitions exported - is DERIVED from the declaration, so the model cannot disagree with operation:
    there is no second artifact to drift against. Metadata-as-description can lie; metadata-as-execution
    cannot. COROLLARY (the testable invariant): any feature that introduces operational behavior not derived
    from the declaration violates the architecture. New capabilities extend the declarable model and derive
    their runtime from it; they never add side-channel configuration, hand-maintained artifacts, or behavior
    the declaration does not determine. Generalizes REQ-964''s data-model-driven estate from materialization
    to the full source-to-consumer span. STATED LIMIT (the two hand-stitched seams): the chain runs boundary
    to boundary - it cannot reach inside systems of origin or presentation tiers, which by definition
    are not incorporable. Provisa cannot attest that Salesforce is configured with proper controls, nor
    that a workbook presents figures correctly. The guarantee is therefore conditional and precisely scoped:
    GIVEN the registration assertion about the origin is true, every derived artifact is provably consistent
    with it; GIVEN the consumer references the definition (e.g. metrics.<name>) rather than copying its
    formula, what is presented is what is defined. The platform''s job at the seams is to make the premises
    explicit, few, and auditable - Ossie import shrinks the origin-side assertion (ingest existing semantic
    models rather than re-type them); metric references shrink presentation-side trust from re-implement-the-definition
    to reference-it, a seam that cannot drift, only dangle - and a dangling reference fails loudly.'
  use_case: Every hop being a spec that GENERATES the next artifact (never merely describes it) yields
    three properties no seam-ful stack has - lineage total and exact by construction rather than reconstructed
    by parsing; impact analysis as graph traversal ending at actual consumers (a GraphQL field, an MCP
    tool, a Kafka topic); regeneration replacing stewardship, since a definition change recompiles everything
    downstream because downstream was generated in the first place. Competitors cannot retrofit this -
    their runtimes predate their catalogs, so their metadata describes from the outside. Stating the principle
    gives future requirements and reviews an invariant to test against.
  code:
  - provisa/pgwire/_pipeline.py
  - provisa/api/admin/ossie_router.py
  tests:
  - tests/unit/test_governed_chokepoint.py
  related_reqs:
  - REQ-964
  - REQ-1164
  - REQ-1316
  - REQ-1317
  - REQ-1318
  - REQ-1319
  - REQ-1320
- id: REQ-1322
  status: complete
  target: 2026-Q4
  group: 3. Source Registration & Data Modeling
  category: Semantic Metrics
  priority: MAY
  type: ui
  since: 2026-07
  description: 'The Explore/SQL page generates SQL from semantic objects, not just table structure. (1)
    Role-aware browser - the SchemaBrowser groups by Metrics / Facts / Dimensions (REQ-1317/REQ-1320 metadata);
    facts and dimensions render distinctly on the JoinCanvas and FK edges between them pre-draw along
    the star. (2) Metric-driven generation - dragging a metric onto the canvas surfaces its valid dimension
    set (entity attributes reachable over the fact''s relationships, derived per REQ-1320) as selectable
    groupings, and the canvas emits semantic SQL against the reserved metrics schema (REQ-1319), e.g.
    SELECT region, month, value FROM metrics.net_revenue GROUP BY region, month. The UI never reimplements
    join or aggregation generation - the server''s compiler expansion is the single generator (REQ-1321);
    the canvas generates the ask, the pipeline generates the plan. (3) Round trip to definition - the
    generated query lands in the editor, editable as ordinary SQL, and the ViewModal gains save-as-metric-view,
    persisting the selection as a view_metrics definition (REQ-1318) instead of frozen SQL text. (4) Semantic
    SQL is the artifact; the physical expansion is a VIEW of it - a read-only preview pane (same gesture
    as EXPLAIN) shows the compiler-derived physical SQL for trust and debugging, and derived output is
    never the editable master (REQ-1321). Expanding the metric into the editor is an explicit detach action
    and a ONE-WAY TRIP: the query becomes free-hand SQL, the metric link is visibly severed, and there
    is no re-ingestion of edited physical SQL back into metric references - de-compiling hand-edited SQL
    is not attempted, ever.'
  use_case: The JoinCanvas already generates SELECT/JOIN SQL from dragged tables and registered relationships,
    but it can only express structure - nothing tells it which tables are facts, which columns are measures,
    or which groupings are valid. With roles and metrics in the model, the guided field-well experience
    a BI tool maintains in its own separate modeling layer falls out of metadata the platform already
    operates on, and the saved artifact stays live against the metric definition rather than drifting
    as frozen SQL.
  code:
  - provisa-ui/src/pages/sql/SchemaBrowser.tsx
  - provisa-ui/src/pages/sql/JoinCanvas.tsx
  - provisa-ui/src/pages/SqlPage.tsx
  tests:
  - provisa-ui/src/pages/__tests__/metric-explore.test.tsx
  - provisa-ui/src/pages/__tests__/metric-detach.test.tsx
  e2e: false
  related_reqs:
  - REQ-1317
  - REQ-1318
  - REQ-1319
  - REQ-1320
  - REQ-1321
- id: REQ-1323
  status: complete
  group: 10. UI & Admin Surfaces
  category: Metrics Management
  priority: SHOULD
  type: ui
  since: 2026-07
  description: 'The Metrics page follows the app-wide detail-then-edit pattern with inline editing: clicking
    a metric row expands a read-only MetricDetailPanel below it. Clicking Edit inside the panel swaps
    the panel for an inline edit form in place (no modal). New Metric opens an inline creation card above
    the table. The delete confirmation is the only modal on the page.'
  use_case: Consistent detail-then-edit UX with inline editing across all admin surfaces (metrics, sources,
    facts) reduces cognitive load, keeps row layout clean, and provides immediate affordance for editing
    without modal context switches.
  code:
  - provisa-ui/src/pages/MetricsPage.tsx
  - provisa-ui/src/pages/metrics/MetricDetailPanel.tsx
  tests:
  - provisa-ui/e2e/metrics-detail-edit.spec.ts
  e2e: true
  e2e_reason: Inline editing, detail panel expansion, form state swapping, and modal absence are only
    verifiable through full UI interaction, not unit tests.
  related_reqs:
  - REQ-1317
  - REQ-1320
- id: REQ-1324
  status: complete
  group: 10. UI & Admin Surfaces
  category: Metrics Management
  priority: SHOULD
  type: ui
  since: 2026-07
  description: 'The metric create/edit form extends fact-as-metric-source (REQ-1320) to the UI through
    a three-picker builder: source fact table (filtered to modelingRole=fact), measure column, and aggregate
    function (SUM/AVG/COUNT/MIN/MAX). When the builder composes the metric, the datatype is automatically
    derived from the aggregate: COUNT → bigint, AVG → numeric, SUM/MIN/MAX → the measure column''s dataType
    (fallback numeric). The Datatype field remains editable as an escape hatch. The free-text expression
    textarea remains the escape hatch for non-fact-sourced metrics. Metric.fromFact is string|null storing
    the source fact table name.'
  use_case: Guided metric authoring through semantic builders with automatic datatype derivation reduces
    expression errors and ensures correct type inference; the editable Datatype field and expression escape
    hatch preserve flexibility for complex derived metrics.
  code:
  - provisa-ui/src/pages/MetricsPage.tsx
  - provisa-ui/src/types/admin.ts
  tests:
  - provisa-ui/e2e/metrics-detail-edit.spec.ts
  e2e: true
  e2e_reason: Builder picker behavior, automatic datatype derivation, expression composition, and UI state
    synchronization with saved metrics are only verifiable through full UI interaction.
  related_reqs:
  - REQ-1317
  - REQ-1320
  - REQ-1323
- id: REQ-1325
  status: complete
  group: 2. Authentication & Identity
  category: Firebase Configuration
  priority: SHOULD
  type: infrastructure
  since: 2026-07
  description: Local development serves a static provisa-ui/public/firebase-config.js stub setting window.__PROVISA_FIREBASE__
    = null (the configured-off state). In cloud deployments, ui_server.py's explicit route shadows the
    static file. This eliminates the unconditional 404 for /firebase-config.js on every page load in vite
    dev.
  use_case: Eliminates a noisy 404 in the development console for every page load when Firebase is not
    configured in local dev, improving developer experience and reducing noise in browser DevTools. Extends
    REQ-1266 Firebase bootstrap support by providing a fallback configuration in local dev.
  code:
  - provisa-ui/public/firebase-config.js
  - provisa/ui_server.py
  e2e: true
  e2e_reason: Static file serving in development and runtime route shadowing in cloud deployments are
    only verifiable through end-to-end deployment and network testing.
  related_reqs:
  - REQ-1266
- id: REQ-1326
  status: complete
  group: 2. Authentication & Identity
  category: Session Management
  priority: MUST
  type: behavioral
  since: 2026-07
  description: Client state scoped to one signed-in session (localStorage keys provisa_token, provisa_org,
    provisa_role, and persisted Apollo snapshot apollo-cache / admin-schema-version) must be cleared when
    a session starts and when it ends. Additionally, a persisted provisa_org is honored only while /auth/me
    still reports membership of that org; otherwise it is discarded and the active org falls to active_org_id,
    else sole membership, else none.
  use_case: After the control plane was wiped, a fresh sign-in that claimed the platform-admin slot showed
    no rights until a logout/login cycle. The server wrote both planes and answered 200 to every request;
    the stale provisa_org from the previous session named a deleted org and rode on every request as X-Org-Provisa.
    Session state isolation prevents stale org membership from deleted orgs causing unauthorized access.
  code:
  - provisa-ui/src/lib/session.ts
  - provisa-ui/src/apolloClient.ts
  - provisa-ui/src/pages/LoginPage.tsx
  - provisa-ui/src/components/NavBar.tsx
  - provisa-ui/src/context/AuthContext.tsx
  tests:
  - provisa-ui/src/__tests__/SessionIsolation.test.tsx
  - provisa-ui/e2e/onboarding-gate.spec.ts
  scenario: 'Given a user with prior session data in localStorage from a deleted org

    When the user signs in to a fresh session

    Then all localStorage state (provisa_token, provisa_org, provisa_role, apollo-cache,

    admin-schema-version) is cleared before any API request

    And the active org is reconciled against /auth/me membership

    And if provisa_org names a deleted org, it is discarded and org falls to active_org_id or sole

    membership


    Given a user with an active session

    When the user logs out

    Then all client state is cleared from localStorage and Apollo cache

    And subsequent navigation finds no persisted session state

    '
  e2e: true
  e2e_reason: Session lifecycle behavior (sign-in clearing stale state, logout cleanup, org membership
    reconciliation) is only verifiable through full sign-in/logout round-trips with server state changes.
- id: REQ-1327
  status: complete
  group: 1. Access Governance & Security
  category: Authorization
  priority: MUST
  type: constraint
  since: 2026-07
  description: 'platform_admin is a purely control-plane role with ZERO data capabilities anywhere — including
    root. All data-plane administration (members, invites, sources, tables, governance, querying) belongs
    to org_admin, in every org including root. Bootstrap grants the claimant BOTH roles in root: org_admin
    (root is their working org) and platform_admin (deployment operation: infra/engine settings, config
    reload, org lifecycle, audited orphaned-org recovery grants). The platform_admin role''s capability
    set drops the admin/superadmin data-wildcard capabilities; the platform_bypass does not bypass any
    data-plane capability check, even in root. Demo/install config column visible_to lists must not name
    platform_admin (a control-plane role cannot appear in data grants); they name org_admin, analyst,
    developer. Single-tenant: one person holding both roles; the roles never merge because org_admin of
    root must not inherit deployment powers when a second org appears. When an org''s administrators are
    unreachable, recovery runs through the named, audited grant_org_admin operation: a platform_admin
    invoking it grants org_admin to a named member of that org, written to that org''s audit trail, and
    revocable through the same operation.'
  use_case: Multi-tenant SaaS governance requires a hard boundary between the provider's control-plane
    operations and customer data access. Unified role inheritance conflates recovery capability with standing
    access and creates a silent backdoor that compliance frameworks (SOC 2, ISO 27001) cannot audit. Separation
    of planes makes platform_admin's actions inside customer orgs explicit, audited, and revocable, turning
    a standing privilege into a break-glass intervention available only through audit trails. Even in
    single-tenant, keeping roles separate allows the platform to scale to multi-tenant without redesigning
    role semantics.
  code:
  - provisa/security/rights.py
  - provisa/api/admin/orgs_router.py
  - provisa/api/admin/roles_router.py
  - provisa/core/org_membership.py
  tests:
  - tests/integration/test_org_lifecycle.py
  - tests/integration/test_invite_role_authz.py
  - tests/unit/test_auth_middleware_multitenancy.py
  - tests/unit/test_client_role.py
  - tests/unit/test_auth_middleware.py
  - tests/e2e/test_platform_auth_flow.py
  scenario: 'Given a platform_admin and a tenant org they do not hold org_admin in


    When the platform_admin attempts to query that tenant org''s data without holding org_admin


    Then the query is rejected


    And platform_bypass does not exempt the access



    Given a platform_admin who needs to recover an orphaned tenant org


    When they invoke grant_org_admin naming a member of that org


    Then that member is assigned org_admin in that org''s schema


    And the operation is recorded in that org''s audit trail


    And the platform_admin does not gain org_admin or any data access in that org


    And they can later revoke org_admin through the same operation



    Given a single-tenant deployment with one person holding both platform_admin and org_admin in root


    When they query root''s data or administer sources


    Then they act as org_admin only (the capability check)


    And platform_bypass is never consulted for data-plane operations

    '
  e2e: true
  e2e_reason: platform_bypass scoping to control-plane only and role separation (platform_admin never
    grants data access) are only verifiable end-to-end with real role resolution across both planes and
    live audit trail writes. Single-tenant role non-merging is only verifiable when the second org appears.
  related_reqs:
  - REQ-1297
  - REQ-1298
  - REQ-1303
  - REQ-1305
  impact_note: 'Formalizes the settled design on 2026-07-29, correcting defective prior recordings in
    REQ-1303 and REQ-1297. The platform/tenant separation (platform_admin = control-plane only, zero data
    access anywhere including root; org_admin = all data-plane administration in every org) was the design
    intent but was contradicted by recorded text ("platform_admin already holds every capability in every
    org"). Amended REQ-1303 and REQ-1297 now state the separation correctly. REQ-1327 documents the full
    refined constraint: capability-set changes (no admin/superadmin data wildcards), bootstrap dual-role
    grant, visible_to lists never naming platform_admin, role non-merging in single-tenant. Invite-escalation
    validation must ensure platform-level invites never resolve to org_admin (REQ-1298 area).'
- id: REQ-471
  status: complete
  since: 2026-07
  group: 5. Query Languages, Compilation & Operations
  category: Compiler & Schema
  priority: MUST
  type: constraint
  description: 'A registered table''s virtual name (alias) is a SQL-plane identifier whose sole authority
    is the SQL naming convention (global_sql_naming_convention, default snake_case). The GraphQL surface
    applies gql naming conventions at schema generation and never mints the SQL name. Defect fixed 2026-07-29:
    register_table previously derived the default alias from the source''s gql_naming_convention with
    apollo_graphql (camelCase) fallback, causing __provisa__ view/entity/fact registrations (no source)
    to get camelCase SQL names (e.g., dim_pet → dimPet). Now derives from global_sql_naming_convention
    and collapses to NULL when the normalized name equals table_name.'
  use_case: Keeping SQL and GraphQL naming authorities separate ensures the SQL plane (federated query
    routing, native dialect emit) operates on a stable snake_case schema, while the GraphQL surface applies
    naming conventions independently at schema-generation time.
  code:
  - provisa/api/admin/schema_mutation_ops.py
  tests:
  - tests/unit/test_naming.py
  - tests/integration/test_schema_mutation_api.py
- id: REQ-1328
  status: complete
  since: 2026-07
  group: 3. Source Registration & Data Modeling
  category: Source Identification
  priority: MUST
  type: structural
  description: 'The sentinel source ID for definition-backed relations (views, entity/fact lowerings,
    metric-composed views) is renamed from __provisa__ to __derived__, centralized as provisa.core.models.DERIVED_SOURCE_ID.
    The name is invariant under materialization (a materialized fact is not "virtual") and clarifies the
    provenance relationship: "derived FROM other relations". The Tables UI renders a "derived" badge instead
    of printing the sentinel. No dual-accept of the old ID (no-backcompat rule).'
  use_case: '"Derived" is the established term (SQL-standard "derived table"; Denodo base/derived views;
    Dremio physical/virtual datasets). Centralizing the sentinel simplifies its use across admin APIs
    and UI surfaces, and the badge provides user-facing clarity about lineage.'
  code:
  - provisa/core/models.py
  - provisa/core/config_loader.py
  - provisa/api/admin/modeling_register.py
  - provisa/api/startup_seed.py
  - provisa/api/admin/schema_mutation_ops.py
  - provisa-ui/src/types/admin.ts
- id: REQ-1329
  status: proposed
  target: 2026-Q4
  group: 3. Source Registration & Data Modeling
  category: Source Identification
  priority: MUST
  type: structural
  description: 'Replace the sentinel source ID entirely: source_id becomes nullable, and registered relations
    gain an explicit definition kind (base | derived). Provenance surfaces from definition lineage, not
    a source slot. The sentinel remains a storage detail until this refactoring is complete, at which
    point registration schemas, admin APIs, and the UI all reference the explicit kind field.'
  use_case: Removing the sentinel simplifies the schema model and makes the provenance relationship explicit
    in the type system, eliminating special-case handling and reducing surface area.
- id: REQ-1330
  status: complete
  since: 2026-07
  group: 11. Platform, Infrastructure & Delivery
  category: Email Delivery
  priority: MUST
  type: behavioral
  code:
  - provisa/core/mail.py
  - provisa/core/models.py
  - provisa/api/admin/invites_router.py
  tests:
  - tests/integration/test_invite_delivery.py
  - tests/unit/test_mail_port.py
  description: 'Transactional email delivery (org invites today; any future outbound transactional mail)
    exists only in SaaS deployment mode and is sent exclusively through an internal EmailSender abstraction
    (port/adapter). Application code never imports or configures a concrete provider; it depends only
    on the port, so the provider can be swapped without touching call sites. The initial adapter is a
    transactional email service (Resend or AWS SES) sending as invites@provisa.dev, authenticated by SPF/DKIM
    records on provisa.dev; inbound MX for provisa.dev remains with Microsoft 365 (receiving is out of
    scope for the application). Prohibited transports: direct SMTP submission to Microsoft 365 (smtp.office365.com)
    and any locally hosted SMTP relay. Self-hosted (non-SaaS) deployments send no email: the feature is
    gated on SaaS mode and invite flows in self-hosted mode surface the invite link for out-of-band delivery
    instead of sending.'
  use_case: Invites must reach users reliably without binding the codebase to one email vendor. A port/adapter
    seam keeps the provider a deployment detail (avoiding the Microsoft 365 SMTP basic-auth retirement
    and its throughput limits), while SaaS-only gating keeps self-hosted installs free of outbound-mail
    infrastructure, credentials, and deliverability liability.
  scenario: 'Given a SaaS-mode deployment with the transactional provider adapter configured


    When an org admin sends a member invite


    Then the invite email is dispatched through the EmailSender port


    And the message is sent as invites@provisa.dev via the configured provider adapter


    And no application module outside the adapter references the concrete provider



    Given a self-hosted (non-SaaS) deployment


    When an org admin creates a member invite


    Then no email is sent


    And the invite link is returned for out-of-band delivery



    Given the provider is swapped (e.g. Resend to SES)


    When the new adapter is configured


    Then invite flows work unchanged with no modification to any caller of the EmailSender port

    '
  related_reqs:
  - REQ-1030
  - REQ-1298
- id: REQ-1331
  status: complete
  since: 2026-07
  group: 11. Platform, Infrastructure & Delivery
  category: SaaS Infrastructure
  priority: MUST
  type: infrastructure
  code:
  - terraform/gcp-saas/main.tf
  - terraform/gcp-saas/front-door/proxy.py
  description: The SaaS shared IP is fronted by an always-free e2-micro "front door" VM running a stdlib-only
    TCP proxy that splices every protocol port to the coordinator. The front door replaces the regional
    passthrough NLB, which cost ~$18/mo in forwarding-rule hours and could not wake a stopped backend.
  use_case: The e2-micro always-free tier eliminates the forwarding-rule cost entirely, and a custom proxy
    enables wake-on-hit (start the coordinator on traffic) and idle-stop (stop after inactivity) that
    a managed NLB cannot provide. The same static IP persists across coordinator stop/start cycles, keeping
    DNS and client configuration stable.
- id: REQ-1332
  status: complete
  since: 2026-07
  group: 11. Platform, Infrastructure & Delivery
  category: SaaS Infrastructure
  priority: MUST
  type: infrastructure
  code:
  - terraform/gcp-saas/front-door/proxy.py
  description: When the coordinator is stopped and a client connects on any protocol port, the front door
    immediately triggers instances.start. HTTPS surfaces (UI 443, API 8000) return a TLS 503 "waking"
    page (HTML for browsers) or JSON for API clients; raw TCP ports (pgwire 5439, bolt 7687, flight 8815,
    mcp 8009, grpc 50051) hold the connection open up to ~110s until the listener is up, then splice the
    client directly to the backend.
  use_case: HTTPS clients need only refresh (browsers) or retry (API clients); no manual intervention.
    Raw TCP clients are transparent to the wake delay, improving UX for database drivers and CLI tools
    that poll for readiness.
  scenario: 'Given the coordinator is stopped and a client connects on the API port (8000)


    When the front door receives the connection


    Then it triggers instances.start


    And returns a TLS 503 response with JSON {"status": "waking"} or an HTML page


    Given the coordinator is stopped and a pgwire client connects (port 5439)


    When the front door receives the connection


    Then it triggers instances.start


    And holds the connection open (up to 110s) without sending data


    And waits for the coordinator to boot and accept on port 5439


    Then splices the client directly to the backend

    '
- id: REQ-1333
  status: complete
  since: 2026-07
  group: 11. Platform, Infrastructure & Delivery
  category: SaaS Infrastructure
  priority: SHOULD
  type: behavioral
  tests:
  - tests/unit/test_front_door_wake_idle.py
  code:
  - terraform/gcp-saas/front-door/proxy.py
  - terraform/gcp-saas/variables.tf
  description: After idle_stop_minutes (default 60) of zero traffic across all protocol ports, the front
    door stops the coordinator via instances.stop and reduces idle cost to ~$0.65/day (disk + Cloud SQL
    f1-micro + static IP). A boot-grace period (default 10 min) after wake prevents the idle reaper from
    immediately stopping a just-booted coordinator.
  use_case: 'Scale-to-zero idle costs are the enabler for a viable free tier and low-commitment trials.
    The grace period prevents flapping: a client that connects briefly during boot won''t trigger an immediate
    stop cycle.'
  scenario: 'Given a SaaS deployment with idle_stop_minutes=60 and boot_grace_minutes=10


    When no traffic arrives on any port for 60 minutes


    Then the front door calls instances.stop


    And the coordinator shuts down


    Given the coordinator is stopped


    When a new client connects


    Then the front door calls instances.start


    And waits 10 minutes (boot grace) before the idle reaper may stop again


    Then if no new traffic arrives, instances.stop is called

    '
- id: REQ-1334
  status: complete
  since: 2026-07
  group: 11. Platform, Infrastructure & Delivery
  category: SaaS Infrastructure
  priority: SHOULD
  type: behavioral
  tests:
  - tests/unit/test_front_door_wake_idle.py
  code:
  - terraform/gcp-saas/front-door/proxy.py
  - terraform/gcp-saas/variables.tf
  - terraform/gcp-saas/main.tf
  description: The front door serves an authenticated wake/verify API on front_door_status_port (default
    9443, HTTPS only). GET /status returns coordinator state (RUNNING | STOPPED | PROVISIONING) and per-port
    reachability (true | false). POST /wake synchronously calls instances.start and waits for the coordinator
    listener to accept. Bearer token (front_door_status_token output) is required on both endpoints. This
    API answers even while the coordinator is stopped, making it ideal for customers and automation that
    cannot retry.
  use_case: Automated systems (CI/CD, integrations) that lack retry logic benefit from explicit pre-wake
    and status polling instead of blindly retrying a connection. The dedicated status port isolates control
    traffic from data ports and provides a probe endpoint for monitoring.
  scenario: 'Given the front door is running and the coordinator is stopped


    When a client sends GET /status with the Bearer token


    Then the response includes {"status": "STOPPED", "ports": {"5439": false, "443": false, ...}}


    Given the coordinator is stopped


    When a client sends POST /wake with the Bearer token


    Then the front door calls instances.start


    And polls the coordinator listener until it accepts


    And returns {"status": "RUNNING"} when ready


    Given an invalid or missing Bearer token


    When a client requests /status or /wake


    Then the front door returns 401 Unauthorized

    '
- id: REQ-1335
  status: complete
  since: 2026-07
  group: 11. Platform, Infrastructure & Delivery
  category: SaaS Infrastructure
  priority: SHOULD
  type: structural
  code:
  - terraform/gcp-saas/main.tf
  - terraform/gcp-saas/variables.tf
  description: 'The coordinator machine type is e2-standard-4 (4 vCPU, 16 GB, ~25% lower per-core throughput
    than n2-standard-4). Boot disk is pd-balanced (not pd-ssd): with node-scheduler.include-coordinator=false
    and the control plane offloaded to Cloud SQL, the coordinator performs no scan spill, so pd-ssd IOPS
    buys nothing. When paying concurrency exists, revisit back to n2-standard-4.'
  use_case: 'E2''s lower per-core cost reduces idle-stop economics and planning-latency impact is negligible
    under the light concurrent load expected at first scale. The coordinator idle-stops via the front
    door anyway, so the choice is reversible: moving to n2 is a one-line variable change when needed.'
- id: REQ-1336
  status: complete
  since: 2026-07
  group: 11. Platform, Infrastructure & Delivery
  category: SaaS Infrastructure
  priority: MUST
  type: structural
  code:
  - terraform/gcp-saas/main.tf
  description: 'The front-door service account is granted a custom IAM role (provisaSaasFrontDoor) with
    three permissions only: compute.instances.start, compute.instances.stop, compute.instances.get. The
    role is scoped to the project, and the front door assumes this identity via the VM''s service account
    binding. No other permissions are granted.'
  use_case: 'Least-privilege isolation: the front door cannot modify the network, delete instances, or
    access storage or databases. Compromised front-door code (e.g. via supply-chain attack on the proxy.py
    script) cannot escalate beyond starting/stopping the coordinator and checking its status.'
- id: REQ-1337
  status: complete
  since: 2026-07
  group: 1. Access Governance & Security
  category: Access Control
  priority: MUST
  type: constraint
  code:
  - provisa/security/rights.py
  - provisa/core/db.py
  - provisa/core/schema.sql
  - provisa-ui/src/components/NavBar.tsx
  - provisa-ui/src/App.tsx
  - provisa/api/admin/_platform_guard.py
  - provisa/api/admin/settings_router.py
  tests:
  - tests/unit/test_rights.py
  - tests/unit/test_governance.py
  - tests/unit/test_auth_middleware.py
  - tests/integration/test_auth_integration.py
  - tests/integration/test_governance_integration.py
  - provisa-ui/e2e/auth-providers.spec.ts
  description: 'Every authorization or visibility gate — server and UI — must test a CAPABILITY (right)
    the acting role carries, never a role id or name. A role id is identity only. Converting identity
    to capabilities happens in exactly one place per surface: capabilities_for_claims in provisa/security/rights.py.
    Two capabilities control deployment-wide settings: platform_settings (read/modify federation engine,
    cache storage, encryption provider, auth provider, config file, query-engine lifecycle) and cross_org
    (act in an org one is not a member of; holding this capability also marks a role CONTROL-PLANE, hence
    off the data plane per REQ-1327 and unresolvable in a tenant org per REQ-1297). Seed: platform_admin
    always holds both. org_admin holds platform_settings only in SINGLE-TENANT deployments and never holds
    cross_org in either mode. apply_tenancy_role_grants in provisa/core/db.py re-asserts these grants
    on every init_schema.'
  use_case: Capability-based gates decouple authorization logic from identity, enabling tenancy mode to
    decide grants without duplicating gate logic. Capability gates are re-evaluable at runtime; role identity
    gates are brittle and error-prone when tenancy changes.
  scenario: 'Given a role with platform_settings capability

    When a user holding that role accesses the admin UI

    Then the Admin, Federation Engine, Cache, and Security tabs are visible

    And server routes to the deployment-wide settings handlers accept the request


    Given a multitenant deployment with org_admin in a tenant org

    When org_admin attempts to access platform settings

    Then the Admin, Federation Engine, Cache, and Security tabs are hidden

    And server routes reject the request with 403


    Given a single-tenant deployment with org_admin

    When org_admin accesses the admin UI

    Then platform settings tabs are visible

    And server routes accept the request


    Given an authorization gate in server or UI

    When the gate evaluates permissions

    Then it calls capabilities_for_claims to resolve rights

    And tests capabilities, never role ids or names

    '
  related_reqs:
  - REQ-1327
  - REQ-1297
  - REQ-1266
- id: REQ-1338
  status: complete
  group: 13. Multi-Tenancy & Organizations
  category: Schema Caching
  priority: MUST
  type: structural
  since: 2026-07
  description: Schema build cache (domains, tables, column_types rows) is per-org state on OrgRuntime,
    with AppState property shim routing reads and writes through the ContextVar-selected org runtime.
    Matches existing shims for tables, relationships, metrics, and roles.
  use_case: Prevents one organization's schema from overwriting another's during concurrent schema rebuilds.
    Ensures /data/domains and other schema endpoints return only the querying org's domains, not the last
    org to rebuild its schema.
  code:
  - provisa/api/org_runtime.py
  - provisa/api/app.py
  - provisa/api/data/sdl.py
  tests:
  - tests/unit/test_org_runtime.py
  scenario: 'Given two organizations with different domains configured

    When organization A rebuilds its schema

    Then organization B''s cached schema remains unchanged

    And /data/domains returns only organization A''s domains when queried as organization A

    And /data/domains returns only organization B''s domains when queried as organization B

    '
- id: REQ-1339
  status: complete
  group: 13. Multi-Tenancy & Organizations
  category: Data Source Management
  priority: MUST
  type: constraint
  since: 2026-07
  code:
  - provisa/core/models.py
  - provisa/api/startup_seed.py
  tests:
  - tests/integration/test_org_demo_seed_visible.py
  - tests/unit/test_org_system_source_seeding.py
  description: Org provisioning must seed every system source (including __derived__) into the new org's
    schema before the config load registers tables. Any config tables:row with source_id pointing to a
    system source has a guaranteed FK target; silent table drops from missing source_id references are
    prevented.
  use_case: System sources like __derived__ are seeded per-org by startup_seed._seed_built_in_sources,
    not declared by config. Without pre-seeding, provisioning loads config tables that reference nonexistent
    source_ids, silently dropping those tables from the load.
  scenario: 'Given a new organization being provisioned

    When provisioning begins

    Then _seed_built_in_sources runs before config.tables are registered

    And __derived__ and all system sources exist in the org''s schema before table registration

    And config tables with source_id: __derived__ load successfully

    '
- id: REQ-1340
  status: complete
  group: 1. Access Governance & Security
  category: Access Control
  priority: MUST
  type: constraint
  since: 2026-07
  code:
  - provisa/core/repositories/table.py
  - provisa/core/schema.sql
  - provisa/core/db.py
  tests:
  - tests/integration/test_governance_integration.py
  - tests/integration/test_org_demo_seed_visible.py
  - tests/unit/test_platform_admin_no_data_rights.py
  description: Control-plane role resolution (the set of roles holding cross_org) must not depend on in-memory
    app state. _ungrant_control_plane resolves cross_org-holding roles from the org's own roles table
    on the caller's connection, never from state.roles. Platform-wide seed guarantees the roles table
    exists and contains the correct grants before any table registration runs.
  use_case: During provisioning, build_org_runtime populates state.roles AFTER config table registration.
    Without querying the roles table directly, control-plane roles are unresolvable during provisioning
    and platform_admin survives on every column grant of the new org, violating REQ-1337.
  scenario: 'Given a new org being provisioned

    When _ungrant_control_plane runs before build_org_runtime completes

    Then it queries the org''s roles table on the connection

    And resolves cross_org-holding roles from the table, not from state.roles

    And correctly strips platform_admin from column grants

    '
  related_reqs:
  - REQ-1337
  - REQ-1327
- id: REQ-1341
  status: complete
  group: 10. UI & Admin Surfaces
  category: Internationalization
  priority: SHOULD
  type: structural
  since: 2026-07
  description: 'UI locale translation is performed in-session by the Claude agent via the i18n-translate
    skill. No external translation service or LLM API call is made. Translation is incremental: a per-locale
    translation memory (TM) sidecar at src/i18n/locales/<lng>/.tm.json records the English source string
    per key at translation time. Only missing or stale keys are retranslated.'
  use_case: In-session translation via Claude agent eliminates external service dependencies and API costs,
    reducing operational complexity. Incremental translation memory keeps translation artifacts locally
    and avoids redundant retranslations, accelerating catalog updates. English catalogs (en.json + en/<ns>.json)
    remain the sole source of truth for keys.
  code:
  - provisa-ui/src/i18n/
  - .claude/skills/i18n-translate/
  scenario: 'Given a Provisa i18n locale catalog (e.g., src/i18n/locales/fr.json)

    When a locale catalog is missing or keys are stale

    Then the Claude i18n-translate agent translates via in-session invocation

    And stores (English key → translated value) in src/i18n/locales/fr/.tm.json

    And subsequent updates only retranslate keys absent from .tm.json

    And validation enforces key-set and {{placeholder}} parity per locale

    '
  related_reqs:
  - REQ-1012
- id: REQ-1342
  status: complete
  group: 10. UI & Admin Surfaces
  category: Internationalization
  priority: SHOULD
  type: behavioral
  since: 2026-07
  description: UI language is selected solely from the browser's navigator.language setting (read at runtime
    on each session start), with English (en) as fallback. Regional locale variants (es-MX, es-419) are
    mapped to their base catalog (es). No per-user language override is persisted to localStorage; the
    provisa_lang key is retired. First non-English locale shipped is universal/neutral Spanish (es) with
    full catalog parity.
  use_case: Browser language detection eliminates per-user preference storage, reducing complexity and
    data plane footprint. Automatic regional variant mapping (e.g., es-MX → es) ensures consistent translation
    coverage without fragmenting catalogs. Catalog parity enforcement guarantees every locale is feature-complete
    on release.
  code:
  - provisa-ui/src/i18n/index.ts
  tests:
  - provisa-ui/e2e/i18n.spec.ts
  - provisa-ui/src/__tests__/i18nLanguageSelection.test.ts
  scenario: 'Given a browser with navigator.language set to es-MX

    When the Provisa UI loads

    Then the language is resolved to es (base catalog)

    And supportedLngs ["en","es"] and nonExplicitSupportedLngs are applied

    And no localStorage lookup for provisa_lang occurs

    And the UI renders in Spanish

    '
  e2e: true
  e2e_reason: Browser language detection and fallback behavior can only be verified through actual browser
    navigation and session startup with different navigator.language settings.
  related_reqs:
  - REQ-1012
  - REQ-1341
- id: REQ-1343
  status: complete
  since: 2026-07
  group: 13. Multi-Tenancy & Organizations
  category: Isolated Federation Engine
  priority: MUST
  type: ui
  description: Onboarding create-org page surfaces an "Isolated engine" checkbox. POST /admin/orgs accepts
    isolated_engine (boolean, default false). The flag is persisted as orgs.isolated_engine in the platform
    admin plane (provisa/core/schema_admin.py) and returned by GET /admin/orgs and GET /admin/orgs/{id}/status.
    Until billing gates it, the checkbox is the only acquisition path for the dedicated-engine lane.
  use_case: Orgs opt into dedicated federation-engine resources at creation time, providing the surface
    for multi-tenancy tier differentiation (shared vs. isolated compute).
  code:
  - provisa-ui/src/pages/OnboardOrgPage.tsx
  - provisa-ui/src/api/admin.ts
  - provisa/api/admin/orgs_router.py
  - provisa/core/schema_admin.py
  tests:
  - tests/integration/test_org_lifecycle.py
  - tests/unit/test_isolated_engine_routing.py
  related_reqs:
  - REQ-1043
  - REQ-1067
  - REQ-1244
- id: REQ-1344
  status: complete
  since: 2026-07
  group: 13. Multi-Tenancy & Organizations
  category: Isolated Federation Engine
  priority: MUST
  type: behavioral
  description: OrgRuntime (provisa/api/org_runtime.py) carries isolated_engine/federation_engine/engine_conn/engine_conn_kwargs.
    AppState.federation_engine/engine_conn/engine_conn_kwargs are routed properties resolving through
    the current_org ContextVar. An org with a dedicated engine resolves its own EngineRuntime; every other
    org falls through to the default-org (shared) engine. build_org_runtime binds the dedicated EngineRuntime
    before source registration so the org's catalogs land on its engine; shutdown closes each org engine
    with its org bound.
  use_case: Per-org federation-engine routing isolates catalog and execution scope per tenant, ensuring
    source registrations and queries execute against the org's dedicated or shared engine without cross-org
    interference.
  scenario: 'Given an isolated org with its own federation engine

    When a source is registered for that org

    Then the catalog lands on the org''s dedicated engine, not the shared pool


    Given a non-isolated org

    When a source is registered

    Then the catalog lands on the default-org (shared) engine


    Given an org with a bound EngineRuntime during shutdown

    When org lifecycle completes

    Then the dedicated engine shuts down with its org bound

    '
  code:
  - provisa/api/org_runtime.py
  - provisa/api/app.py
  tests:
  - tests/unit/test_isolated_engine_routing.py
  - tests/integration/test_org_lifecycle.py
  related_reqs:
  - REQ-1043
  - REQ-1244
  - REQ-1243
- id: REQ-1345
  status: complete
  since: 2026-07
  group: 13. Multi-Tenancy & Organizations
  category: Isolated Federation Engine
  priority: MUST
  type: behavioral
  description: isolated_engine_endpoint(org_id) in provisa/federation/engine.py resolves a dedicated Trino
    coordinator endpoint using PROVISA_ISOLATED_ENGINE_HOST_TEMPLATE (an {org_id}-templated hostname)
    and PROVISA_ISOLATED_ENGINE_PORT. If an isolated org is deployed to an environment without the template
    configured, resolution is a hard error (never silently routing to the shared coordinator).
  use_case: Isolated orgs must route exclusively to their dedicated Trino coordinator, with no silent
    fallback to shared infrastructure; missing configuration is a deployment defect requiring explicit
    remediation.
  scenario: 'Given an isolated org and PROVISA_ISOLATED_ENGINE_HOST_TEMPLATE configured

    When isolated_engine_endpoint is called with the org_id

    Then a Trino coordinator endpoint is resolved using the template and port


    Given an isolated org and PROVISA_ISOLATED_ENGINE_HOST_TEMPLATE not configured

    When isolated_engine_endpoint is called

    Then resolution raises a hard error

    '
  code:
  - provisa/federation/engine.py
  tests:
  - tests/unit/test_isolated_engine_routing.py
  related_reqs:
  - REQ-1043
  - REQ-1244
- id: REQ-1346
  status: complete
  since: 2026-07
  group: 13. Multi-Tenancy & Organizations
  category: Isolated Federation Engine
  priority: MUST
  type: infrastructure
  description: The dedicated federation-engine cluster idles/sleeps between sessions like the shared engine's
    front door. bind_terminal (backend lifecycle hook in provisa/federation/backend.py and trino_lifecycle.bind_terminal)
    stores terminal connection kwargs without connecting. TrinoBackend.execute and execute_sync accept
    a kwargs-only (unconnected) terminal; execute_trino's liveness path connects on the first real query.
    Traffic wakes the cluster; org provisioning never does.
  use_case: Dedicated engines reduce operational cost by sleeping when idle, waking only on actual query
    execution, avoiding provisioning/deprovisioning cycles or always-on compute overhead.
  code:
  - provisa/federation/backend.py
  - provisa/federation/runtime.py
  - provisa/federation/trino_lifecycle.py
  tests:
  - tests/unit/test_isolated_engine_routing.py
  related_reqs:
  - REQ-1043
  - REQ-1244
- id: REQ-1347
  status: complete
  since: 2026-07
  group: 11. Platform, Infrastructure & Delivery
  category: Test Infrastructure
  priority: MUST
  type: infrastructure
  description: tests/conftest.py stages the gitignored Trino plugin jar directories (trino-file/trino-sharepoint/trino-splunk)
    into a git worktree by symlinking from the primary checkout before compose up. The itest Trino coordinator
    crash-loops without these plugins present.
  use_case: Integration tests need access to Trino plugins that are excluded from git; symlinking from
    the primary checkout ensures worktree isolation while preventing duplicate jars and avoiding storage
    overhead.
  code:
  - tests/conftest.py
  tests:
  - tests/conftest.py
  related_reqs:
  - REQ-1043
- id: REQ-1348
  status: complete
  since: 2026-07
  group: 1. Access Governance & Security
  category: Authentication
  priority: MUST
  type: behavioral
  description: 'Interactive sign-in runs only on the control-plane host (`cloud.<base>`). An org subdomain
    (`{org}.<base>`, REQ-1276) must not attempt a local sign-in: identity providers authorize exact hostnames
    only (Firebase Authentication rejects wildcard authorized domains with INVALID_AUTHORIZED_DOMAIN),
    and registering each org''s hostname with the IdP would make org creation write to an external control
    plane, which REQ-1054 zero-provisioning forbids. Instead an org subdomain acquires the bearer from
    the control plane through a same-site hidden-iframe postMessage relay served at /auth-relay.html on
    the control-plane origin, and falls back to a redirect handoff to `${controlPlane}/login?next=<this
    url>` when the control plane has no session. Both the relay''s requester origin and the login page''s
    `next` target must be validated as a single-label host under the same base domain, same scheme and
    same port; replies are addressed to that exact origin, never "*". The borrowed token is re-acquired
    periodically because the rotating Firebase SDK runs on the control-plane origin only.'
  use_case: Org subdomains cannot perform independent authentication without violating IdP hostname validation
    constraints and zero-provisioning requirements; a secure relay mechanism allows token acquisition
    through the control plane while maintaining strict origin validation.
  code:
  - provisa-ui/src/lib/authHost.ts
  - provisa-ui/src/lib/crossSubdomainAuth.ts
  - provisa-ui/src/authRelay.ts
  - provisa-ui/auth-relay.html
  - provisa-ui/src/main.tsx
  - provisa-ui/src/pages/LoginPage.tsx
  - provisa-ui/vite.config.ts
  tests:
  - provisa-ui/src/__tests__/crossSubdomainAuth.test.ts
  - tests/unit/test_ui_server_routing.py
  scenario: 'Given an org subdomain requests authentication without an existing session

    When the subdomain initiates sign-in

    Then it attempts a postMessage relay to the control-plane /auth-relay.html

    And if the relay acquires a token from the control plane session, it returns the token to the exact
    subdomain origin

    And if the control plane has no session, the subdomain falls back to a redirect to ${controlPlane}/login?next=<org
    subdomain url>

    And both relay requester and login redirect target are validated as single-label hosts under the same
    base domain, scheme, and port

    And replies are never sent to wildcard origins

    '
  related_reqs:
  - REQ-1276
  - REQ-1054
- id: REQ-1349
  status: complete
  since: 2026-07
  group: 1. Access Governance & Security
  category: Administration & Roles
  priority: MUST
  type: behavioral
  description: 'Org-scoped admin rights define two new capabilities: `org_settings` (surfaces whose subject
    is the org being acted in—AI model/NL provider overrides, domains, scheduled tasks, pending creation
    requests) and `observability` (READ-ONLY performance and health: admin overview, system health, traces).
    org_admin holds both in both tenancy modes. Neither capability implies platform bypass (admin/superadmin),
    deployment-wide settings (platform_settings), or acting in another org (cross_org). Grants are re-asserted
    idempotently in apply_tenancy_role_grants because schema.sql''s ON CONFLICT (id) DO NOTHING cannot
    update a pre-existing role row (V1 has no migrations).'
  use_case: Org admins need independent control over their org's configuration (AI models, NL providers,
    domains, tasks) and observability without access to platform-level or cross-org settings. Request-time
    resolution of org config (resolve_org_config) allows configuration changes to take effect immediately
    on the next query without restart.
  code:
  - provisa/security/rights.py
  - provisa/core/db.py
  - provisa/core/schema.sql
  - provisa/core/schema_org.py
  - provisa/core/org_settings.py
  - provisa/api/admin_router.py
  tests:
  - tests/unit/test_org_scoped_admin_rights.py
  - tests/integration/test_tenancy_role_grants.py
  - tests/integration/test_org_settings_overrides.py
  - provisa-ui/src/__tests__/adminNavCapabilities.test.ts
  scenario: 'Given an org_admin in an org without platform_settings or cross_org capabilities

    When the org_admin modifies org AI model overrides via PUT /admin/ai-models

    Then the org''s org_settings table is updated with the delta

    And subsequent queries in that org resolve the overridden AI model at request time

    And the org_admin cannot access platform-wide deployment config or another org''s settings

    And traces are scoped to the acting org only (observability capability)

    '
- id: REQ-1350
  status: complete
  group: 10. UI & Admin Surfaces
  category: Internationalization
  priority: SHOULD
  type: behavioral
  since: 2026-07
  description: Server-side error i18n emits stable machine codes and JSON parameters alongside English
    messages. ApiError (HTTPException) subclasses and registered handlers emit {detail, code, params}
    payloads; strawberry MutationResult gains optional code/params fields. The UI (serverMessage()/requestFailed()
    in provisa-ui/src/i18n/serverMessage.ts) renders localized error text from the serverErrors i18next
    catalog with graceful fallback to English detail/message when translations are unavailable. Unmigrated
    HTTPException sites and engine-error passthrough (str(e)) remain valid by design.
  use_case: Enables the UI to display localized error messages without requiring server implementations
    for every supported language. Stable error codes allow client-side catalog management and translation
    maintenance independent of server changes. Graceful fallback ensures compatibility with pre-i18n error
    paths.
  code:
  - provisa/api/errors.py
  - provisa/api/app.py
  - provisa-ui/src/i18n/serverMessage.ts
  tests:
  - tests/unit/test_error_handling.py
  - tests/unit/test_client_access.py
  - tests/e2e/test_error_code_contract.py
  scenario: 'Given a mutation or query that results in an error with code auth.not_authorized

    When the error is caught by the registered ApiError handler

    Then the response includes {detail: "English message", code: "auth.not_authorized", params: {...}}

    And the UI calls serverMessage(response) to render the error

    And the i18next catalog serverErrors.AUTH_DENIED is rendered if available

    And if the translation key is missing, the detail fallback is rendered

    '
  e2e: true
  e2e_reason: Error handling, code/params emission, and UI fallback rendering require real server-to-client
    round-trip with actual i18next catalog lookup and missing-key fallback behavior verification.
  related_reqs:
  - REQ-1341
  - REQ-1342
- id: REQ-1351
  status: complete
  group: 10. UI & Admin Surfaces
  category: Navigation & Admin Menu
  priority: MUST
  type: behavioral
  since: 2026-07
  description: Navigation into a nav group must resolve to a route the caller's rights admit. The remembered
    last-visited subnav item (localStorage provisa_nav_last_item) is a browser preference, not an entitlement;
    when the caller no longer holds the right that item's route requires, it is dropped and the group
    is entered on the first item they do hold. When the caller holds no item in the group at all, the
    group is not offered. This applies to clicking a nav group tab, the profile menu's Settings item,
    and the bare /admin URL, which must land identically.
  use_case: Denial fallback replaces the whole page including the subnav, so entering on a denied route
    leaves no tab to click. provisa_nav_last_item is now session-scoped (cleared by clearSessionState()
    at sign-in per REQ-1326), so one identity's remembered tab cannot cross to the next identity on a
    shared browser; no per-login key namespace is required because only one identity is signed in at a
    time.
  code:
  - provisa-ui/src/lib/capabilities.ts
  - provisa-ui/src/components/NavBar.tsx
  - provisa-ui/src/App.tsx
  - provisa-ui/src/lib/session.ts
  tests:
  - provisa-ui/src/__tests__/navEntryItem.test.ts
  scenario: 'Given a signed-in user with limited capabilities in a nav group

    When the user navigates to a group tab or /admin URL

    Then the system checks if the cached last-visited item (from provisa_nav_last_item)

    And if the user no longer holds the right for that item''s route, it is dropped

    And the group is entered on the first item the user holds rights for

    And if the user holds no items in the group at all, the group is not offered in nav

    '
  related_reqs:
  - REQ-1349
  - REQ-1326
- id: REQ-1352
  status: complete
  group: 1. Access Governance & Security
  category: Administration & Roles
  priority: MUST
  type: constraint
  since: 2026-07
  description: The definitions of the two system admin roles—platform_admin and org_admin—belong to provisa/core/schema.sql
    alone. No config file may carry a role block for either id. provisa/core/repositories/role.py::upsert
    returns early for both ids.
  use_case: Config load (_load_config_in_txn -> role_repo.upsert) runs AFTER provisa/core/db.py::apply_tenancy_role_grants
    and overwrites the capabilities column wholesale. A shipped config block that omitted org_settings
    or observability would silently strip both org-scoped rights from org_admin, making the Admin navigation
    group disappear entirely for org administrators. This constraint ensures role seeding from schema.sql
    cannot be overwritten. Granting TO org_admin (visible_to/writable_by/default_assignments) remains
    permitted — it is redefining the ROLE that is refused.
  code:
  - provisa/core/schema.sql
  - provisa/core/repositories/role.py
  tests:
  - tests/unit/test_platform_admin_no_data_rights.py
  related_reqs:
  - REQ-1349
  - REQ-1297
- id: REQ-1353
  status: complete
  group: 0. Architecture & Design Principles
  category: Requirements Taxonomy
  priority: MUST
  type: constraint
  since: 2026-07
  description: Requirement groups are a closed canonical enumeration defined in docs/arch/groups.yaml.
    Every requirement's `group` field must exactly match a name in that file. New groups must be added
    to groups.yaml (with a scope line) before use. Non-canonical group strings are rejected by provisa/tools/req_schema.py
    (Pydantic field validator) and caught by scripts/validate_requirements.py in CI.
  use_case: Prevents free-text group drift that caused 45 variant strings to accumulate across the requirements
    corpus. A closed canonical enumeration with schema enforcement ensures all requirements are consistently
    categorized and discoverable under a stable set of high-level design areas.
  code:
  - docs/arch/groups.yaml
  - provisa/tools/req_schema.py
  - scripts/validate_requirements.py
  unit_test: false
  unit_test_reason: Enforcement of canonical requirement groups is validated by req_schema.py's Pydantic
    field validator and scripts/validate_requirements.py in CI on every requirements.yaml change; no separate
    unit test is needed since the constraint is self-verifying.
- id: REQ-1354
  status: complete
  group: 3. Source Registration & Data Modeling
  category: Catalog Registration
  priority: MUST
  type: behavioral
  since: 2026-07
  description: Trino source-catalog registration drops and recreates the catalog rather than skipping
    when it already exists, ensuring connection properties and credentials refresh on every registration.
    A catalog that refuses to drop is statically file-loaded and must raise an error, never be silently
    worked around.
  use_case: Trino exposes no way to read a live catalog's properties back, and CREATE CATALOG IF NOT EXISTS
    no-ops against an existing catalog. Without dropping and recreating, each source is pinned to the
    credentials it first booted with, preventing credential rotation or connection property updates.
  code:
  - provisa/core/catalog.py
  - provisa/core/trino_system_catalogs.py
  tests:
  - tests/unit/test_source_catalog_refresh.py
  - tests/unit/test_trino_system_catalogs.py
  scenario: 'Given a Trino catalog is already registered with old credentials

    When source registration runs at config load or admin source registration

    Then the catalog is dropped and recreated

    And new connection properties and credentials take effect

    '
- id: REQ-1355
  status: accepted
  group: 13. Multi-Tenancy & Organizations
  category: Tenancy Architecture
  priority: MUST
  type: constraint
  target: 2026-Q3
  description: 'Org is the canonical tenancy subject in Provisa; "tenant" terminology is retired. The
    legacy tenants/tenant_config tables are not a separate subject but attributes of orgs. Billing fields
    (plan, ls_customer_id, ls_subscription_id, kms_key_arn) and tenant_config migrate to orgs; org resolution
    has one path (OrgRoutingMiddleware only). Naming: tenant_db → org_db, tenant_id → org_id, TenantMiddleware
    → retired.'
  use_case: The two nominal org resolution paths were not merely ambiguous — neither ran. Both TenantMiddleware
    and _OrgRoutingMiddleware were registered inside create_app behind `if state.multitenancy:`, and that
    flag is assigned in _load_and_build, which lifespan invokes only after create_app returns; both entrypoints
    use uvicorn factory mode, so the guard always read the False default. With no request-lifetime binder
    for current_org, AppState._active_runtime fell through to the DEFAULT org's runtime on every authenticated
    HTTP request, so a member of one org read another org's data plane. The same dead guard hid the control-plane
    router (404 on every endpoint) and TenantMiddleware's skip-path set (the Lemon Squeezy webhook 401'd
    before its signature was checked). One canonical subject, one resolution path, and no create_app branch
    on a flag that is unset at factory time.
  code:
  - provisa/core/schema_admin.py
  - provisa/auth/middleware.py
  - provisa/api/app.py
  - provisa/core/repositories/org.py
  related_reqs:
  - REQ-1075
  - REQ-456
  - REQ-462
  - REQ-594
  - REQ-1266
  - REQ-1044
  - REQ-1053
- id: REQ-1356
  status: complete
  since: 2026-07
  group: 10. UI & Admin Surfaces
  category: Internationalization
  priority: SHOULD
  type: structural
  description: 'The Provisa UI ships 13 locale catalogs: en (source of truth), es, fr, de, nl, he, ja,
    zh (Simplified), zh-HK (Traditional/HK with exact-tag matching before base zh fallback), ru, it, pt,
    hi. Each non-English locale mirrors en''s structure (core catalog + 94 per-component namespace files,
    2,825 keys) and maintains a .tm.json translation-memory sidecar for incremental retranslation. Key/placeholder
    parity with en is enforced by the locale-diff validator. Translation uses per-locale domain glossaries
    via the i18n-translate skill. Hebrew is catalog-complete with full RTL support (see REQ-1357).'
  use_case: Multiple-locale support enables Provisa to serve global enterprises in their native languages,
    with translation memory and glossaries enabling incremental, efficient localization.
  code:
  - provisa-ui/src/i18n/
  related_reqs:
  - REQ-1012
- id: REQ-1357
  status: complete
  since: 2026-07
  group: 10. UI & Admin Surfaces
  category: Internationalization
  priority: SHOULD
  type: ui
  description: The Provisa UI supports right-to-left (RTL) layout for locales with RTL scripts. RTL direction
    is determined by i18next locale (base subtags he/ar/fa/ur/yi → rtl, all others ltr). Layout is implemented
    via Mantine DirectionProvider wrapping the app and a DirectionSync component (provisa-ui/src/i18n/direction.tsx)
    that keeps <html dir>, <html lang>, and Mantine's direction context in sync on init and runtime languageChanged
    events.
  use_case: RTL language users require properly-mirrored layouts to read and interact with the UI naturally,
    ensuring accessibility and usability for enterprise users in RTL-script regions (Hebrew, Arabic, Farsi,
    Urdu, Yiddish).
  code:
  - provisa-ui/src/i18n/direction.tsx
  - provisa-ui/src/main.tsx
  related_reqs:
  - REQ-1356
  - REQ-1012
- id: REQ-1358
  status: complete
  since: 2026-07
  group: 10. UI & Admin Surfaces
  category: Internationalization
  priority: SHOULD
  type: ui
  description: The in-app guided product tour (provisa-ui/src/tour/) is now integrated into the app-level
    i18n system. All 25 tour step titles and descriptions are extracted into per-locale i18n namespace
    files (provisa-ui/src/i18n/locales/<lng>/tour.json) keyed as tour.steps.step0..step24 with title and
    description fields. The TourStep interface carries a `key` field instead of inline copy. useTour.tsx
    calls t(`tour.steps.${step.key}.title`) and .description via useTranslation(), and translations are
    complete for all 13 supported locales (en, es, fr, de, nl, he, ja, zh, zh-HK, ru, it, pt, hi), preserving
    embedded HTML/SVG/code/emoji verbatim and leaving product/technology proper nouns untranslated.
  use_case: Enables the product tour to surface in users' native languages alongside the rest of the UI,
    ensuring onboarding is accessible and consistent across all supported locales.
  code:
  - provisa-ui/src/tour/tourSteps.ts
  - provisa-ui/src/tour/useTour.tsx
  - provisa-ui/src/i18n/locales/
  scenario: 'Given a user with navigator.language set to es

    When the user loads the Provisa UI and opens the product tour

    Then tour step titles and descriptions render in Spanish

    And all 25 steps are fully translated

    And proper nouns and embedded code remain untranslated by design

    '
  related_reqs:
  - REQ-1356
  - REQ-1012
  - REQ-1342
- id: REQ-1359
  status: complete
  group: 5. Query Languages, Compilation & Operations
  category: Aggregate & Group-by Queries
  priority: SHOULD
  type: behavioral
  since: 2026-08
  description: gRPC, JSON:API, and OpenAPI query surfaces expose aggregate and group-by capability for
    tables with `enable_aggregates` and `enable_group_by` flags set, routed through the existing compiler
    aggregate machinery (provisa/compiler/aggregates.py) rather than per-protocol implementations. JSON:API
    carries `?aggregate=` and `?groupBy=` query parameters; gRPC and OpenAPI define equivalent request
    shapes.
  use_case: Currently, GraphQL/SQL/Cypher surfaces synthesize `{table}_aggregate` and `{table}_group_by`
    queries via compiler aggregates, while gRPC/JSON:API/OpenAPI only issue raw `SELECT <cols> FROM <table>
    LIMIT n`, leaving three protocols without aggregate capability. Protocol parity ensures all surfaces
    can answer the same analytical questions (e.g., "inquiry count by user") without reimplementing aggregation
    logic per-protocol.
  tests:
  - tests/unit/test_jsonapi_aggregates.py
  - tests/unit/test_rest_aggregates.py
  - tests/unit/test_nl_aggregation_routing.py
  code:
  - provisa/grpc/query_ir.py
  - provisa/nl/executor.py
  - provisa/compiler/aggregates.py
  scenario: 'Given a table with enable_aggregates: true

    When a gRPC/JSON:API/OpenAPI client requests an aggregate (sum, count, avg, etc.) on a grouping column

    Then the request is routed through provisa/compiler/aggregates.py

    And results are returned with proper aggregate naming parity (e.g., inquiry_count)

    And gRPC/JSON:API/OpenAPI surfaces feature the same aggregate capabilities as GraphQL/SQL/Cypher

    '
- id: REQ-1360
  status: complete
  group: 3. Source Registration & Data Modeling
  category: Catalog & Schema Discovery
  priority: SHOULD
  type: structural
  since: 2026-08
  description: When a table has `enable_aggregates` and/or `enable_group_by` flags, the catalog and semantic
    metadata layer synthesize virtual (metadata-only, non-materialized) dimension-role and measure-role
    annotations for discovery in the Tables/Model tab, docs, and schema introspection. Group-by-eligible
    columns are labeled as implicit dimension keys; aggregatable numeric and count columns are labeled
    as implicit measures. This does not replace governed, reusable metrics (config/provisa-install.yaml
    `metrics:` block with `visible_to` governance) — only adds metadata for Kimball-style naming and discoverability
    in UI.
  use_case: Ad-hoc analytics queries need to surface which columns can be grouped and which can be aggregated
    to help users explore data without reading schema documentation. Implicit metadata from enable_* flags
    provides this discovery signal in the UI, complementing explicit metrics governance for formal, reusable,
    access-controlled measures.
  code:
  - provisa/core/catalog.py
  - provisa/compiler/aggregates.py
  scenario: 'Given a fact_table with enable_aggregates: true and enable_group_by: true

    When a user views the table in the Explore UI

    Then numeric columns and count-eligible fields appear tagged as "Measures"

    And non-aggregated ID/foreign-key columns appear tagged as "Dimensions"

    And explicit metrics (visible_to-governed) remain separately discoverable

    And no new physical tables or materialized views are created

    '
- id: REQ-1361
  status: complete
  since: 2026-08
  group: 10. UI & Admin Surfaces
  category: Table Discovery
  priority: SHOULD
  type: behavioral
  description: On the Tables list page (provisa-ui/src/pages/TablesPage.tsx), when a table has no explicit
    modeling_role (not registered via the Entity/Fact DSL), render an "implied" fact/dimension badge derived
    from that table's enableAggregates flag (implied fact) and/or enableGroupBy flag (implied dimension).
    Badges are colored grape/teal with variant="outline" to visually distinguish implied role from explicit
    role (REQ-1320).
  use_case: A user who explicitly enables Enable Aggregates or Enable Group By on a table is asserting
    that the table plays a fact or dimension role. Surfacing this intent as a badge on the Tables list
    provides immediate visibility into modeling decisions without requiring per-column inspection (REQ-1360
    handles column-level tagging).
  code:
  - provisa-ui/src/pages/TablesPage.tsx
  tests:
  - provisa-ui/e2e/tables-enable-aggregates.spec.ts
  - tests/unit/test_nl_aggregation_routing.py
  scenario: 'Given a table with enableAggregates: true and no explicit modeling_role

    When a user views the Tables list page

    Then an "implied fact" badge appears with variant="outline" and testid="tables-implied-fact-<table>"

    And the badge title derives from i18n key "tablesPage.modelingRole.impliedFactTitle"

    Given a table with enableGroupBy: true and no explicit modeling_role

    When a user views the Tables list page

    Then an "implied dimension" badge appears with variant="outline" and testid="tables-implied-dimension-<table>"

    And the badge title derives from i18n key "tablesPage.modelingRole.impliedDimensionTitle"

    '
  related_reqs:
  - REQ-1320
  - REQ-1360
- id: REQ-1362
  status: complete
  since: 2026-08
  group: 10. UI & Admin Surfaces
  category: Product Tour
  priority: SHOULD
  type: behavioral
  description: In demo mode, all application page chunks must be preloaded and compiled before the guided
    product tour begins showing any steps. This prevents the tour popover from displaying over a destination
    page stuck in a loading state.
  use_case: Product tours require destination pages to be ready for display when the tour navigates to
    them. Pre-loading ensures a smooth tour experience without popover overlays appearing over in-flight
    page loads.
  code:
  - provisa-ui/src/pageChunks.ts
  - provisa-ui/src/tour/useTour.tsx
  - provisa-ui/src/tour/tourSteps.ts
  tests:
  - provisa-ui/e2e/tour-page-preload.spec.ts
  - provisa-ui/src/__tests__/tourPreload.test.ts
  scenario: 'Given a user initiates the guided product tour in demo mode

    When startTour() is called

    Then all page chunks are prefetched and compiled before step 0 is shown

    And each tour step''s readySelector validates destination page content is loaded before the highlight
    is displayed

    And the tour popover never appears over a page in a loading state

    '
  e2e: true
  e2e_reason: Tour step display timing relative to destination page rendering is only verifiable through
    full end-to-end UI interaction with the tour system navigating between pages.
- id: REQ-1363
  status: accepted
  since: 2026-08
  group: 3. Source Registration & Data Modeling
  category: Domain Hierarchy
  priority: MUST
  type: structural
  description: Add hierarchical domain structure via parent_domain_id to Domain model, with flattening
    applied at execution time to governance, addressing, and access control layers.
  use_case: Enable organizations to structure domains hierarchically for governance metadata and organization,
    while maintaining a single flat execution model and addressing scheme.
  code:
  - provisa/core/models.py
  - provisa/core/governance.py
  - provisa/federation/duckdb_runtime.py
  scenario: 'Given a domain hierarchy with parent-child relationships

    When a role grants access to a parent domain

    Then the grant expands to all descendant domains at build_governance_context() time

    And addressing (domain_schema.table) remains two-part and flat

    And no per-domain scoped namespaces are created (roles, operations, serverInstances remain global)

    And tables continue to attach to exactly one domain (leaf or non-leaf)

    '
  design_notes: 'Mirrors Role.parent_role_id + flatten_roles() pattern (provisa/core/models.py:714,722).
    Flattening applies to: (1) addressing — domain_schema.table two-part unchanged, PG-schema mapping
    flat; (2) roles + rights — parent domain grants expand to descendants at governance context build
    time, Source.allowed_domains and RLS domain_id rules gain subtree expansion; (3) execution — compiler,
    RLS, row-scoping, all surfaces see flattened domain sets. Rejected alternative: recursive domains
    with per-domain scoped namespaces (from Simple is Hard appendix-B) as fragmenting the single control
    plane.'
- id: REQ-1364
  status: proposed
  target: 2026-Q4
  group: 7. Result Delivery
  category: Config Export
  priority: SHOULD
  type: structural
  description: Config exports carry JSON Pointer refs for internal cross-references plus composed physical
    resourcePath URIs for data elements, enabling external validators and interchange converters (Ossie/DDN)
    to resolve semantic and physical locations without second passes.
  use_case: JSON Pointers make exported references self-resolving for downstream validators and format
    converters; resourcePaths provide precise physical source locations (e.g. postgresql://host:5432/db/schema/table#column)
    for interchange systems.
  code:
  - provisa/api/admin/config_export.py
  design_notes: 'Scope is export projection only — config/export layer (provisa/api/admin/config_export.py
    and interchange converters). NOT used in model definition (YAML stays name-based refs) or execution
    (DB stays int FKs); no pipeline changes. Every exported object (domain, table, column, relationship,
    metric, role, RLS rule, function) is addressable at a stable JSON Pointer (e.g., #/domains/finance/tables/orders/columns/amount).
    Internal cross-references in the exported document (relationship endpoints, metric expression refs,
    RLS targets, function returns) are emitted as JSON Pointers instead of bare name strings. Each table
    and column additionally carries a composed physical resourcePath URI (from Source connection info
    + catalog.schema.table, e.g., postgresql://host:5432/db/schema/table#column).'
- id: REQ-1365
  status: accepted
  since: 2026-08
  group: 8. Client Access & Protocols
  category: URI Dereferencing API
  priority: SHOULD
  type: behavioral
  description: 'Governed dereference API for semantic data-location URIs: REST and MCP tool `resolve(uri,
    as_of?)` that takes a Provisa semantic-address URI and returns the governed value(s) it addresses,
    optionally at a point in time. For relational sources, the canonical address shape is `<table>/pk=<value>/<column>`,
    addressing a single value as table → primary-key predicate → column. Only semantic addresses (provisa://<org>/<domain_schema>/...)
    are dereferenceable; physical URIs are refused.'
  use_case: Exported documents, lineage nodes, audit records, and agent citations carry resolvable links
    to live or historical governed values — dereferenceable provenance. Complements REQ-1364 (export addressing).
  code:
  - provisa/api/
  design_notes: 'Execution routes through the single settled query pipeline (_govern_and_route/_execute_plan),
    never a second pipeline. Dereferencing is automatically role-scoped, RLS-filtered, masked, row-capped,
    and audited. Row selection uses primary-key-based predicates validated against the model''s key metadata
    (not free-form SQL). Point-in-time maps to existing bitemporal read path (X-Provisa-As-Of) when target
    table has bitemporal mode enabled. Physical URIs are explicitly rejected.

    Name durability is best-effort, steward-mediated, and NOT guaranteed. There is no schema history,
    so semantic-model renames (domain/table/column renames or re-keying) can orphan previously exported
    addresses. Physical changes (re-platforming, physical schema renames, source moves) never orphan names
    — the address is semantic and binding is indirection. Orphaning therefore only results from deliberate
    steward action, expected to be rare in stable data environments (more common in modernizing ones).
    Stewards may elect to retain old addresses via existing mechanisms — Table.alias or view_sql virtual
    tables over the renamed definition — but the system does not enforce address permanence.'
  scenario: 'Given a semantic-address URI with role-based visibility and RLS restrictions

    When resolve(uri) is called

    Then the returned value is filtered by the caller''s role, RLS rules, and applicable masking

    And the value is audited like any other query execution

    And physical URIs are refused with an error

    '
  e2e: true
  e2e_reason: API behavior verifiable only through end-to-end governance application — role scoping, RLS
    filtering, masking, row capping, and audit logging must be verified across REST and MCP transports
    with actual governance context.
  related_reqs:
  - REQ-1364
- id: REQ-1366
  status: proposed
  target: 2026-Q4
  group: 6. Execution, Routing, Caching & Performance
  category: Federation Engine Abstraction
  priority: SHOULD
  type: structural
  description: 'Spike: generalize time-travel (as_of) from a hardcoded Iceberg/Delta source-type special
    case to a first-class connector capability, enabling synthesized time-travel over non-versioned sources
    (REST APIs, MongoDB, etc.) via materialization snapshots.'
  use_case: Time-travel as an engine-profile capability rather than a hardcoded source list decouples
    the feature from Trino and enables synthesized time-travel over non-versioned sources — a semantic-layer
    differentiator no single engine natively offers, concentrating IP per REQ-825.
  code:
  - provisa/core/source_registry.py
  - provisa/compiler/sql_gen.py
  - provisa/federation/connector_base.py
  - provisa/federation/store_writer.py
  design_notes: 'Spike investigates three escalating degrees: (1) GATE — add time_travel to Capability;
    since capability is per (engine, source_type) and the compiler is engine-blind, the gate moves from
    compile to the PLAN stage (aligned with REQ-825). (2) CONNECTOR-OWNED RENDERING — as_of becomes a
    neutral IR attribute; stage-4 codegen asks the connector to render it. Mechanisms differ structurally
    per engine: Trino = table-suffix clause (FOR TIMESTAMP/VERSION AS OF); DuckDB Iceberg = scan-function
    argument (iceberg_scan snapshot_from_timestamp), which lands in view DDL, so per-query as_of requires
    an inline table-ref substitution, not a clause append. Rides the REQ-825 REMAINING codegen-to-stage-4
    refactor. (3) SYNTHESIZED TT — for MATERIALIZED sources (REQ-826) the copy destroys native TT; retain
    N snapshots in the materialization store instead of replace-style lands and resolve as_of to a retained
    snapshot in the PLAN prep phase. Capability becomes three-valued: native | synthesized | none. Scope
    degree 3 to the Iceberg-backed store only, where Iceberg snapshot history makes it nearly free. Cross-cutting:
    governance-parity conformance (REQ-827) must cover as_of — RLS/masking on historical rows must diff
    identically per engine; synthesized snapshots are governed at read time (stage 3 governs whatever
    relation is read), never at capture time. Deliverable: a decision on degrees 1+2 landing with the
    REQ-825 pipeline work and a scoped design for degree 3 on the Iceberg store backend. Full analysis:
    docs/arch/spikes/0001-time-travel-as-connector-capability.md.'
  related_reqs:
  - REQ-467
  - REQ-825
  - REQ-826
  - REQ-827
  - REQ-841
  competitive_position:
    status: ahead
    rationale: Synthesized time-travel over non-versioned sources (REST APIs, MongoDB, NoSQL) is not offered
      by Trino, Snowflake, or BigQuery, which expose time-travel only on natively versioned tables. Provisa's
      ability to retain materialization snapshots and surface as_of across all source types creates a
      differentiating capability.
- id: REQ-1367
  status: accepted
  target: 2026-Q4
  group: 0. Architecture & Design Principles
  category: Platform Thesis & Operational Proof
  priority: MUST
  type: constraint
  description: 'Provisa''s thesis: with sufficient rigor, every aspect of a data environment EXCEPT the
    systems of origin can be described declaratively, and then PROVEN compliant to policy and to operate
    as declared. Systems of origin are excluded by design — Provisa proves properties of its projection
    of a source, never of the external system itself. "Proven" means operational proof, not formal verification:
    (1) compliance-by-construction where a single enforcement choke point exists (governance defined in
    the semantic layer, enforced once at the governed-IR stage — REQ-825 stage 3, one pipeline); (2) certification
    where realizations vary (governance-parity conformance diffing per engine — REQ-827); (3) drift-impossibility
    where state is derived (engine catalog is rebuildable projection of the declaration, never hand-maintained
    — REQ-843); (4) self-description where operation is observable (the estate''s operational metadata
    as governed MVs over its own event streams — REQ-964 reflexivity, freshness contracts REQ-961). Every
    subsystem admitted to the platform must fit one of these proof modes; a feature that can only be trusted,
    not proven, is a defect in its design.'
  use_case: 'The thesis is the platform''s admission rule — it explains why capabilities are declared
    profiles (REQ-825/840/841), why governance is IR-injected rather than engine-delegated, why derived
    state must be rebuildable, and why the config surface is a queryable ledger (REQ-966). Recording it
    top-level gives future requirements a fixed test: declarative description + a stated proof mode, or
    redesign.'
  design_notes: 'The SOO (system-of-origin) carve-out is a DEFINITION, not an enumerated exception. An
    origin is any point where new information enters the estate by ASSERTION rather than DERIVATION —
    external systems, arbitrary business-rule transforms, human input, ML inference — regardless of whether
    it runs inside or outside the platform. Every node in the estate is exactly one of two kinds: DERIVED
    (declaratively lowered — SQL/IR, MVs, entity/fact lowering, governance transforms — provable in one
    of the four proof modes) or ORIGIN (semantics attested, not proven; governed at its boundary — schema-validated
    both ends, vintage-labeled, lineage-tracked, policy-enforced on output; the external-processor pure
    contract of REQ-957/REQ-976-era adapters is the origin boundary mechanism). Arbitrary computation
    is therefore not EXCLUDED from the estate but RECLASSIFIED: any code may run, provided it is named
    as an origin and passes through the attested boundary contract. A derived node that embeds an arbitrary
    UDF is an origin misclassified as derived — a design defect. Consequence: origin-ness must be an explicit
    classification on every estate node, and the ratio of origin to derived nodes is a measurable estate-maturity
    metric (the "unprovable residue"). The thesis in closed form: the estate is theorems over declared
    axioms; the method makes the axiom set explicit, minimal, and boundary-governed. Every origin is a
    DECLARED BLACK BOX — a first-class registered entity with its own governance regime: named owner,
    explicit contract (schema in/out, purity, vintage), an attestation discipline in place of proof (boundary
    gates/validation, change control), and its own policy scope. Two operational rules follow: (1) MINIMIZATION
    PRESSURE — any logic expressible declaratively must be migrated from origin to derived (rewriting
    an imperative transform as SQL/lowered spec shrinks the axiom set); an origin is admitted only when
    its semantics genuinely cannot be derived. (2) NO ANONYMOUS ORIGINS — an unnamed origin (a UDF or
    business rule embedded in a derived node) is a design defect; the named-origin registry IS the estate''s
    axiom registry and is itself queryable/auditable. Analogy: Rust''s `unsafe` blocks — named, minimized,
    individually justified, with safety proven by construction around them.'
  related_reqs:
  - REQ-825
  - REQ-827
  - REQ-843
  - REQ-957
  - REQ-961
  - REQ-964
  - REQ-966
  - REQ-976
  - REQ-1366
  competitive_position:
    status: ahead
    rationale: Peers offer declarative modeling (dbt, Cube) or policy translation (Immuta) or reconciliation
      (Kubernetes-style controllers) separately; none unify description, enforcement, and operational
      proof over one declared artifact.
- id: REQ-1369
  status: complete
  since: 2026-08
  group: 11. Platform, Infrastructure & Delivery
  category: Data Catalog Integration
  priority: SHOULD
  type: structural
  description: 'Standardize "metadata export" terminology: module path provisa/api/metadata_export/, router
    provisa/api/admin/metadata_export_router.py, entitlement Feature.METADATA_EXPORT, UI component MetadataExportTab,
    API paths /admin/metadata-export, and documentation docs/metadata-export.md. Replaces prior "metadata
    egress" naming across all interfaces.'
  use_case: Consistent naming across code, config, API, and UI reduces cognitive load and improves discoverability
    for users and maintainers.
  code:
  - provisa/api/metadata_export/
  - provisa/api/admin/metadata_export_router.py
  - provisa-ui/src/tabs/MetadataExportTab.tsx
  - docs/metadata-export.md
  relates_to:
  - REQ-1066
  - REQ-1068
  - REQ-1069
  - REQ-1072
  - REQ-1073
  - REQ-1368
- id: REQ-1370
  status: complete
  since: 2026-08
  group: 11. Platform, Infrastructure & Delivery
  category: Demo & Testing
  priority: MAY
  type: behavioral
  description: 'Demo mode exemption for tier entitlement gating: when PROVISA_DEMO=1 (set via `provisa
    run --demo`), require_feature in provisa/control_plane/entitlements.py returns immediately without
    checking tier entitlements, allowing all features to be used in demo environments. setup_router._is_demo
    relocated to provisa/core/demo.py::is_demo for reuse across the codebase.'
  use_case: Demo mode needs to showcase all features without enterprise license checks, enabling quick
    product evaluation and internal testing without mock entitlement infrastructure.
  scenario: 'Given Provisa running with PROVISA_DEMO=1

    When a metadata export request is made (requires METADATA_EXPORT entitlement normally)

    Then the request succeeds without checking tier entitlements

    '
  code:
  - provisa/core/demo.py
  - provisa/control_plane/entitlements.py
  - provisa/cli/setup_router.py
- id: REQ-1371
  status: complete
  since: 2026-08
  group: 11. Platform, Infrastructure & Delivery
  category: Data Catalog Integration
  priority: SHOULD
  type: behavioral
  description: 'CLI job `provisa metadata export` (provisa/cli.py::_cmd_metadata_export) triggers an on-demand
    metadata publish by POSTing to the running server''s /admin/metadata-export/publish endpoint. Thin
    client of the REQ-1072 publish path, suitable for cron. Flags: --api ($PROVISA_API_URL default http://127.0.0.1:8000;
    host names the org under multitenancy), --token ($PROVISA_API_TOKEN), --timeout (300s). Exit 0 on
    full publish, 1 on partial/failed/unreachable.'
  use_case: Cron-friendly CLI command enables automated, scheduled metadata exports without polling the
    admin UI or manually triggering, and integrates with standard deployment pipelines and observability
    tools.
  scenario: 'Given Provisa running with PROVISA_API_URL=https://provisa.example.com and PROVISA_API_TOKEN
    set

    When `provisa metadata export --api https://provisa.example.com --token TOKEN --timeout 300` is invoked

    Then the command POSTs to /admin/metadata-export/publish, waits up to 300s, and exits 0 on success
    or 1 on failure

    '
  code:
  - provisa/cli.py
  tests:
  - tests/unit/test_cli_metadata_export.py
  relates_to:
  - REQ-1072
  - REQ-1074
  - REQ-1369
- id: REQ-1372
  status: complete
  since: 2026-08
  group: 1. Access Governance & Security
  category: Data Catalog Integration
  priority: SHOULD
  type: structural
  description: Per-table data_product flag (Table.data_product in provisa/core/models.py) filters metadata
    exports. build_snapshot in provisa/api/metadata_export/builder.py publishes only tables marked data_product=true.
    Withholds relationship edges, lineage edges, and governance tags that touch unmarked tables. Sources
    and domains are always published. Name resolution for relationships and lineage still sees every table,
    so bare-name ambiguity rejection remains unchanged.
  use_case: Allows organizations to designate certain tables as publishable data products in the catalog,
    withholding internal/staging tables and their downstream governance metadata from export while maintaining
    name resolution for query execution.
  code:
  - provisa/core/models.py
  - provisa/api/metadata_export/builder.py
  tests:
  - tests/unit/test_metadata_snapshot_builder.py
  relates_to:
  - REQ-1070
  - REQ-1071
  - REQ-1369
- id: REQ-1373
  status: complete
  since: 2026-08
  group: 1. Access Governance & Security
  category: Data Governance
  priority: MUST
  type: structural
  description: 'Unified org-level tag registry: single source of truth for all tags, with multi-scope
    applicability. Each tag declares appliesTo (multi-select over object types: source, table/view, column,
    relationship). No per-type registries — a tag is a single identity with fixed semantics even when
    multi-scoped.'
  use_case: Centralized tag management reduces cognitive load, prevents scope drift, and enables consistent
    governance across all taggable objects (sources, tables, columns, relationships).
  code:
  - provisa/core/schema.sql
  - provisa/core/schema_org.py
  - provisa/core/models.py
  - provisa/core/repositories/tag.py
  tests:
  - tests/unit/test_schema_metadata_parity.py
- id: REQ-1374
  status: complete
  since: 2026-08
  group: 10. UI & Admin Surfaces
  category: Admin Console
  priority: MUST
  type: ui
  description: Admin / Tags editor UI with segmented button bar [source | table | column | relationship]
    filtering the single tag registry into four apparent lists. Multi-scope tags appear in each applicable
    list; editing anywhere edits the one tag; scope is shown on row.
  use_case: Segmented UI reduces cognitive load by presenting only relevant tags per object type, while
    maintaining unified identity and single-edit-location semantics.
  scenario: 'Given a tag "technical" with appliesTo=[column, table]

    When viewing the Table segment

    Then the tag appears in the list with scope indicator

    When editing that tag in the Column segment

    Then the same tag is updated

    '
  code:
  - provisa-ui/src/components/admin/TagsTab.tsx
  tests:
  - provisa-ui/src/__tests__/TagsTab.test.tsx
- id: REQ-1375
  status: complete
  since: 2026-08
  group: 1. Access Governance & Security
  category: Data Governance
  priority: MUST
  type: constraint
  description: 'System tags are predefined and immutable: exactly three system tags with fixed semantics
    and scopes, never deletable or redefinable. `technical` (scope: column ONLY — prunes columns from
    a published table''s export; at table level the Data Product flag itself is the export control, and
    a second inverted switch for the same outcome would be a contradictory state), `pii` (scope: column;
    exported as provisa_pii classification; future masking-policy hook), `deprecated` (scope: source,
    table/view, column, relationship; UI warning; exports to each catalog''s NATIVE deprecation construct
    — DataHub deprecation aspect, Atlan DEPRECATED certificate status, Atlas provisa_deprecated classification).
    User-created tags are annotation-only and never acquire behavior or system semantics. Future system
    tags require behavior-driving justification; vocabulary-only tags belong in user registry. Tag assignments
    carry optional `reason` (why the tag is on the object) and `expires_on` (ISO date when assignment
    stops being intended) as TYPED columns, enabling management reporting queries (e.g. overdue removals:
    expires_on < today). For the `deprecated` system tag, `reason` is REQUIRED at assignment time (mutation
    refuses without it, schema.tag_reason_required) and `expires_on` is the planned removal date. Deprecation
    projects outward through every consumer: GraphQL SDL emits @deprecated(reason: "<reason> (removal:
    <date>)") directive (column deprecation on object field, table deprecation on root query field, relationship
    deprecation on navigational field); DataHub gets reason as deprecation aspect note and date as native
    decommissionTime (epoch millis); Atlan gets both in certificateStatusMessage; Atlas carries reason/expiresOn
    as typed classification attributes. UI: assigning a tag opens inline reason/date form (check-then-apply);
    pill tooltips show tag description + reason + planned removal; deprecated pills with past expires_on
    render visually louder (overdue). Per-tag field policies: every registry tag declares reason_policy
    and expires_policy, each one of hidden|optional|required (default optional). The assign mutation enforces
    them — required refuses absence, hidden refuses presence — and the picker UI shows/demands the fields
    accordingly. System-tag policies are fixed in code: deprecated = reason required + expiry optional;
    technical = reason hidden + expiry hidden; pii = expiry hidden. The former hardcoded "deprecated requires
    reason" rule is now just the deprecated tag''s policy. Management reporting: tags and tag_assignments
    are exposed as meta-domain tables (tags_meta unions the code-defined system tags in so the visible
    registry is complete; tag_assignments_meta), and a tag_expiry meta view lists every assignment with
    an expires_on, deriving status expired|expiring at query time — queryable through every governed surface
    (SQL/pgwire for BI, GraphQL, Cypher) under role + domain access. Portable-bootstrap parity: the non-PG
    (SQLite/MySQL) schema path additively reconciles missing columns from the SQLAlchemy metadata (equivalent
    of schema.sql''s ALTER ADD COLUMN IF NOT EXISTS blocks), so metadata-declared columns reach existing
    database files.'
  use_case: System tags drive governed behavior (metadata filtering, classification, deprecation warnings);
    user tags remain advisory, preventing accidental behavior acquisition and maintaining governance integrity.
    Settled tag set provides clear governance surface across all metadata exporters. Typed reason/expiration
    metadata supports audit trails, lifecycle management, and integration with downstream catalog decommissioning
    workflows. Per-tag policies enable fine-grained governance while reducing hardcoded rules. Management
    reporting through meta views makes tag lifecycle queryable by all consumers (BI, exporters, auditing).
    Cross-database schema compatibility ensures bootstrap consistency across deployments.
  code:
  - provisa/core/schema.sql
  - provisa/api/metadata_export/builder.py
  - provisa/api/admin/schema_mutation.py
  - provisa/api/_meta_views.py
  - provisa/core/db.py
  tests:
  - tests/unit/test_metadata_export_model_tags.py
  - tests/unit/test_sdl_deprecation.py
- id: REQ-1376
  status: accepted
  target: 2026-Q4
  group: 1. Access Governance & Security
  category: Data Governance
  priority: MUST
  type: constraint
  description: Tag assignment is human-only, never automatic. UI may suggest likely-technical columns,
    but suggestion requires explicit steward acceptance before recorded. No silent behavior.
  use_case: Keeps governance decisions explicit and auditable; prevents silent mis-tagging that could
    incorrectly exclude data or mis-classify sensitive information.
- id: REQ-1377
  status: complete
  since: 2026-08
  group: 1. Access Governance & Security
  category: Data Governance
  priority: MUST
  type: structural
  description: 'Taggable objects: sources, tables/views, columns, and relationships. UI tag-picker icon
    on each object row filters to tags whose appliesTo includes that object type; assigned tags render
    as pills using existing Badge pattern (wrapping enabled).'
  use_case: Supports rich governance across all data model abstractions; wrapping enables dense lists
    with many tags while maintaining readability.
  code:
  - provisa/api/admin/schema_mutation.py
  - provisa-ui/src/components/TagControl.tsx
  tests:
  - provisa-ui/src/__tests__/TagControl.test.tsx
  - tests/unit/test_metadata_export_model_tags.py
- id: REQ-1378
  status: complete
  since: 2026-08
  group: 1. Access Governance & Security
  category: Data Governance
  priority: SHOULD
  type: structural
  description: 'Tags export as metadata: Atlas exporter maps tags to classifications prefixed `provisa_`
    (REQ-1069 mechanism); annotation-only user tags map to Atlas labels. DataHub/Atlan use native tag
    equivalents. Asymmetry: Atlas cannot tag relationship edges; relationship tags export as relationship
    attributes on Atlas, native tags on DataHub/Atlan.'
  use_case: Enables downstream metadata systems to consume Provisa governance; documents platform capability
    limits (Atlas relationships) and maps to downstream native semantics for adoption by data stewards.
  code:
  - provisa/api/metadata_export/atlas.py
  - provisa/api/metadata_export/datahub.py
  - provisa/api/metadata_export/atlan.py
  tests:
  - tests/unit/test_metadata_export_model_tags.py
  relates_to:
  - REQ-1069
- id: REQ-1379
  status: complete
  since: 2026-08
  group: 0. Architecture & Design Principles
  category: Data Governance
  priority: MUST
  type: constraint
  description: '"technical" tag replaces earlier column-level columnRole: business|technical enum. Consolidates
    governance model: single tag system instead of parallel enum-and-tag approaches.'
  use_case: Unified tagging model reduces code duplication, simplifies UI, and allows future expansion
    to multi-scoped tags (tables can also be technical) without architectural change.
  tests:
  - tests/unit/test_metadata_export_model_tags.py
- id: REQ-1380
  status: complete
  since: 2026-08
  group: 10. UI & Admin Surfaces
  category: Admin Console
  priority: SHOULD
  type: ui
  description: 'Tables list shows PRODUCT pill (indigo Badge) on rows whose registration has dataProduct=true;
    name-cell pill group wraps (enabling multiple pills per row: PRODUCT, plus any assigned user tags).'
  use_case: Distinguishes publishable data products in dense table lists; wrapping accommodates future
    pill expansions (e.g., lineage confidence, certification badges) without layout breakage.
  code:
  - provisa-ui/src/pages/TablesPage.tsx
  relates_to:
  - REQ-1372
- id: REQ-1381
  status: accepted
  target: 2026-Q4
  group: 1. Access Governance & Security
  category: Data Governance
  priority: MUST
  type: structural
  description: 'User attributes for ABAC are a SEPARATE concept from the object-tag registry (REQ-1373):
    object tags are exported governance metadata on sources/tables/columns/relationships; user attributes
    are authorization inputs feeding the `user.<attr>` namespace in RLS filter expressions. `user` is
    NOT an appliesTo scope of the tag registry, and user attributes are never included in metadata export.'
  use_case: Keeps access-control-bearing data out of a registry whose contract is annotation-and-export;
    a steward's tag edit can never silently change row visibility, and user attributes never ship to external
    catalogs.
  relates_to:
  - REQ-1373
- id: REQ-1382
  status: accepted
  target: 2026-Q4
  group: 1. Access Governance & Security
  category: Data Governance
  priority: MUST
  type: structural
  description: 'User attribute sources: the IdP is the system of record for attributes it provides (OIDC
    claims — groups, department, region); locally-administered user attributes supplement what the IdP
    does not carry. On name collision, the IdP value wins and the local value is marked shadowed — never
    silently merged or overridden.'
  use_case: Lets thin IdPs be supplemented without forking identity truth; explicit precedence with a
    visible shadowed state keeps attribute provenance auditable.
- id: REQ-1383
  status: accepted
  target: 2026-Q4
  group: 1. Access Governance & Security
  category: Data Governance
  priority: MUST
  type: structural
  description: Both IdP-sourced and local attributes resolve into the same `user.*` namespace consumed
    by RLS predicates; predicates are source-agnostic. The Security page shows per-attribute provenance
    (IdP vs local, and shadowed state) so admins can see why an attribute has its value.
  use_case: RLS authors write one predicate vocabulary regardless of attribute origin; admins can diagnose
    unexpected row visibility from the provenance display.
- id: REQ-1384
  status: accepted
  target: 2026-Q4
  group: 1. Access Governance & Security
  category: Data Governance
  priority: MUST
  type: structural
  description: 'Tag-scoped RLS rules (follow-on to REQ-1373..1379 and existing table/domain-scoped RLS
    REQ-040/041/402): an RLS rule may attach to a tag; at compile time it resolves to all tables carrying
    that tag and its predicate is AND-ed with any table- and domain-scoped predicates (most-restrictive-wins,
    no precedence tiers; multiple matching tags each contribute AND-ed predicates). Tagging an object
    may only expand restriction reach, never loosen access. Additionally, since the metadata catalog is
    part of the queryable model, RLS filter expressions may reference tag-assignment metadata directly
    (e.g. EXISTS subquery) as the fully general path.'
  use_case: Write a predicate once, attach it to a tag, and every table a steward tags picks it up now
    and in the future — the tag-based-policy pattern inside Provisa's one SQL pipeline.
  relates_to:
  - REQ-040
  - REQ-041
  - REQ-402
  - REQ-1373
- id: REQ-1385
  status: in-progress
  target: 2026-Q4
  group: 11. Platform, Infrastructure & Delivery
  category: Data Catalog Integration
  priority: MUST
  type: structural
  description: 'Semantic URIs (provisa:// scheme) on metadata-exported assets, addressed by business identity,
    self-describing to the point practical: kind is stated in the URI path at the leaf transition and
    domain nesting is unmarked (homogeneous). Grammar: provisa://<org>/<domain>[/<subdomain>...] is a
    domain (no kind keyword — still in the domain tree, unambiguous at any depth, no trailing-slash convention
    needed); provisa://<org>/<domain-path>/tables/<table> is a table (the tables/ keyword marks the kind
    transition); provisa://<org>/sources/<source-id> is a source, provisa://<org>/tags/<tag-id> a tag,
    provisa://<org>/roles/<role-id> a role, provisa://<org>/commands/<name> a command (a tracked function/webhook,
    by its registered name) — reserved plural kind keywords extend to future registries. Fragments carry
    attribute kinds: #field:<column> for a column; #rel:<alias> for a relationship (a navigational field
    anchored at its source table; registry id when the edge has no alias). Instance (row) addressing:
    ?<key>=<value>[&...] query-string predicate — composite keys compose; a primary-key predicate denotes
    exactly one instance; requires a declared row identity (mvPrimaryKey, unique constraint REQ-1093,
    or introspected pk), no declared key = no instance form. Composition: instance + fragment — ?id=123#field:amount
    is a cell, ?id=123#rel:customer the governed traversal from one instance. Dereferencing any instance/traversal
    URI is a GOVERNED query: RLS and masking apply to the resolver''s role. Reserved words: kind keywords
    (tables, sources, tags, roles, and future registry keywords) are illegal as domain names — refused
    at domain creation (schema.domain_reserved_word). Correspondence: the URI is the compressed form of
    the semantic-model JSON Pointer grammar (domain nesting implied, kind stated at the leaf): provisa://acme/sales/emea/tables/orders
    maps to #/children/sales/children/emea/definitions/orders; conversion is mechanical. Metadata export
    emits concept/attribute/relationship URIs only; the instance grammar serves citations, audit trails,
    data-quality findings, agent provenance. Resolution convention: provisa://<org>/<path> resolves via
    the org''s Provisa instance (https mapping), keeping URIs dereferenceable by convention. The physical
    triple (source.schema.table) exports alongside as a binding attribute, so re-platforming a table to
    a different source preserves catalog identity, history, and lineage continuity; identity events (alias/domain
    rename, moving a table between domains) export the previous URI as an alias for continuity. Uniqueness
    precondition: (full domain path, table) via _assert_domain_table_unique generalized to hierarchical
    domains. Exporters carry the URI per vendor: Atlas as a provisaUri entity attribute alongside qualifiedName;
    DataHub in externalUrl + customProperties; OpenLineage as a provisa_uri dataset facet (the cross-catalog
    stitching join key); OpenMetadata in sourceUrl + relationship extension entries; Collibra as a "Provisa
    URI" attribute; relationship and lineage edges reference endpoints by semantic URI enabling cross-catalog
    lineage stitching.'
  use_case: Enables lineage stitching across external catalogs by using business identity rather than
    physical location; preserves catalog continuity when tables are re-platformed to different sources;
    allows lineage and history to follow the business entity through infrastructure changes; supports
    multi-catalog federation by providing stable URIs as cross-catalog join keys.
  code:
  - provisa/api/metadata_export/refs.py
  - provisa/api/metadata_export/builder.py
  - provisa/api/metadata_export/atlas.py
  - provisa/api/metadata_export/datahub.py
  - provisa/api/metadata_export/openlineage.py
  - provisa/api/metadata_export/openmetadata.py
  - provisa/api/metadata_export/collibra.py
  tests:
  - tests/unit/test_metadata_export_semantic_uri.py
  relates_to:
  - REQ-1069
  - REQ-1070
  - REQ-1372
- id: REQ-1386
  status: in-progress
  scenario: 'Given a Provisa install with audit-log records and pii/deprecated tag assignments

    When an ops-domain-authorized user queries a report view (e.g. usage_ranking, pii_access)

    Then the seeded view returns the report rows through the governed pipeline under role + domain access
    control

    '
  group: 5. Query Languages, Compilation & Operations
  category: Operational Views
  priority: SHOULD
  type: behavioral
  description: 'Management/operations reporting is delivered as defined views in the built-in ops domain,
    extending the REQ-884 registry mechanism (_OPS_LOG_TABLE_VIEWS + _seed_ops_domain): each report is
    a view joining query_audit_log to catalog metadata, registered as a first-class ops-domain table so
    it is queryable through every governed surface (SQL/pgwire, GraphQL, Cypher) under role + domain access
    control. Report views: (1) usage_ranking — query count and distinct consumers per table, including
    never-queried tables (deprecation candidates); (2) deprecated_usage — consumption of deprecated tables/elements
    over time, per consumer, to establish a safe-removal date; (3) pii_access — accesses touching PII-classified
    elements: who, role, masked vs unmasked; (4) policy_denials — access attempts blocked by governance
    rules: who, what, which rule; (5) surface_mix — traffic by protocol via the audit-log source column;
    (6) query_health — latency percentiles and error rate by source and table (duration_ms/status_code);
    (7) stale_metadata — tables/elements missing owner, description, or classification, with coverage
    percentage per domain; (8) join_hotspots — most-frequent table_ids co-occurrence, informing materialization/caching.
    Decision: the org_admin role is the designated steward of the ops domain, seeded at ops-domain creation,
    so it never surfaces as a PENDING stewardship gap (REQ-609). Rollup views: because query_audit_log
    is append-only and grows unbounded, trend-oriented reports (usage_ranking, deprecated_usage, query_health,
    surface_mix) read from scheduled daily rollup tables (per-day, per-table, per-role aggregates) rather
    than scanning the raw log; point-in-time reports (pii_access, policy_denials, stale_metadata, join_hotspots)
    remain live views. Implemented so far: all eight views live (seed-time DDL via _OPS_REPORT_VIEWS +
    ops_table_usage unnest spine, per-dialect only at the seed layer), registration under provisa-admin/ops,
    org_admin steward seeding. Outstanding prerequisites: element_ids/masking-outcome/denying-rule-id
    captured at log-write time (query text is encrypted per REQ-689, so these cannot be derived post-hoc
    — until then the reports are table-level), the daily rollup job, and the deferred resource-usage (metrics
    sampler) and source-freshness (sync timestamps) views.'
  use_case: 'Provides self-service management/operations reporting through the governed pipeline without
    custom code: usage-driven deprecation decisions, PII-access oversight, governance-effectiveness inspection,
    and capacity/caching signals — all consumable from any BI tool over pgwire.'
  code:
  - provisa/api/_meta_views.py
  - provisa/api/startup_seed.py
  - provisa/core/db.py
  - provisa/core/schema.sql
  tests:
  - tests/unit/test_ops_domain.py
  relates_to:
  - REQ-884
  - REQ-609
- id: REQ-1387
  status: complete
  since: 2026-08
  group: 3. Source Registration & Data Modeling
  category: Business Glossary & Ontology
  priority: SHOULD
  type: behavioral
  description: 'Dictionary/ontology of business terms over the semantic layer. Every physical field name
    in the semantic layer normalizes deterministically into a term via rules based on common enterprise
    DB naming conventions (case folding, separator/camelCase tokenization, standard abbreviation expansion,
    and stripping of TRAILING proxy tokens — id/key/code/index/reference and their expansions — because
    a column named for a key or code refers to its concept through a stand-in value, so cust_id and the
    customer concept share one term; concept-FORMING suffixes never strip: temporal (order_dt → "order
    date"), locational (cust_city → "customer city") and actor/activity ("created_by" stays the full phrase)
    tokens are what the value is about, and stripping them would merge distinct meanings — the deterministic
    layer may under-merge, curation consolidates, but it must never over-merge; and TOO-GENERIC full phrases
    — bare attribute words like name, identifier, date, location, message, first name, last name — qualify
    with the enclosing table''s concept (singular head noun), so employees.first_name → "employee first
    name" and orders.id → "order identifier" rather than one meaningless shared term. Native-filter pseudo-columns
    (_nf_-prefixed / native_filter_type) are query machinery and derive no terms). Terms are deduplicated;
    each term holds a list of physical refs (catalog-identity resolved: table_id + column name). The term
    unit is the phrase — the full normalized field name — never decomposed into tokens; users resolve
    homonyms and granularity by revising terms or creating abstract terms. Users can rename a term, move
    a physical ref between terms to consolidate, and attach a definition. Terms carry relationships to
    users designated as experts — people who can answer questions about the term or who authored its definition.
    Users can create abstract terms with no physical refs, connected to rooted terms through a closed,
    enum-typed relationship set (KIND_OF, RELATED_TO, PART_OF, SYNONYM_OF); free-form edge types are not
    permitted. Lifecycle is derived from semantic-layer membership — adding an object creates/links terms;
    deleting or modifying a schema element removes its ref, and removal of a term''s last physical ref
    removes the term — unless an abstract term is connected to the rooted graph through it, in which case
    the term is deprecated (kept) instead so no abstract term is left dangling. The term graph is included
    in metadata export (REQ-1068 family) and exposed via an MCP tool for term search/lookup. v1 scope
    is documentation and discovery only — term membership does not drive policy (masking/RLS by term is
    a possible follow-on). Boundary vs tags (REQ-1373): tags remain flat operator-assigned labels; terms
    are the normalized vocabulary derived from and linked to schema. Implemented so far: schema (glossary_terms/refs/edges/experts
    in schema.sql + schema_org, tenant-scoped), deterministic normalizer, repository with the full lifecycle
    (sync rides table_repo.upsert — the single table_columns write path — and refless-term settling rides
    every deletion path via a departed-table sweep, cascade-independent because the SQLite backend runs
    without PRAGMA foreign_keys), the dangling check as true reachability (delete the term unless an abstract
    term would newly lose all paths to rooted terms, judged against a baseline where the candidate still
    anchors), admin REST router (/admin/glossary, org_settings-gated, every mutation queues a REQ-1072
    publish), MCP search_terms tool, term graph in the export snapshot (data_product/technical filters
    extend to refs; terms with no published refs withheld unless abstract; edges need both endpoints),
    and the admin UI Glossary tab with 13-locale i18n; Glossary is a TOP-LEVEL nav item (route stays /admin/glossary,
    org_settings) and Team moved into the Admin group. Verified: unit, PG integration (real config-load
    lifecycle), BDD (feature + implemented steps), Playwright e2e (curation flows against the core lane)
    and MCP protocol-level tests all pass; docs section added to api-reference.md. ADAPTERS (implemented):
    every vendor publishes the term graph natively into a PROVISA-OWNED glossary container created idempotently
    — Atlas/Atlan (glossary API, definition on longDescription, isA/synonyms/seeAlso mapping, Atlan deprecation
    via certificateStatus), DataHub (glossaryNode urn:li:glossaryNode:provisa.<org>, term URNs immutable
    so renames follow deprecation-succession), OpenMetadata (fqn PUT, renames PATCH-rebind by stored UUID
    — GlossaryTermRepository allows rename; KIND_OF as native parent hierarchy, entityStatus deprecation),
    Collibra (Glossary-type domain via the Import API; REPLACE safety re-derived by CONTAINMENT: only
    Provisa-domain assets and Provisa-term relations are ever mentioned). Updates read-merge so steward
    fields on Provisa''s own terms survive; nothing deletes on snapshot absence; NO adapter writes term-to-asset
    assignments (Provisa-authored assignment publishing is the one explicit follow-on). OWNERSHIP CONSTRAINT
    (settled): the REQ-1389 authorship contract extends to glossary items — a vendor-side glossary item
    is modified or deleted ONLY when Provisa created it, tracked the same way as every published asset:
    the vendor''s own id is captured into catalog_bindings at publish, keyed by the term''s URN (provisa://<org>/terms/<name>),
    and that binding is the ownership test. A glossary item with no Provisa binding originated in the
    external system (steward- or vendor-created) and is NEVER modified, re-parented, or deleted, whatever
    its name; steward term-to-asset assignments remain HUMAN-OWNED per REQ-1389 and are never written.
    ADMISSION RULE (settled): what any consuming surface may offer is ONE predicate, core.glossary.live_term_ids,
    shared by the repository and the exporter. A term is LIVE only when it is (1) IN SERVICE — neither
    retired (a curator withdrew it) nor deprecated (the derivation lost its last column); (2) DEFINED
    — a term derived from a column name is a token, not a meaning, so an undefined term is a PROPOSAL
    awaiting a curator, never vocabulary an agent may ground a question on; and (3) GROUNDED — connected,
    over in-service terms, to a term holding a physical ref. The glossary is an entry point INTO the data,
    so every term chain must terminate at a physical column: an abstract term wired to nothing physical
    names no data and is not offered. Groundedness is a property of the term graph, not of any one row,
    so it is computed server-side and rides the admin payload as a `live` flag rather than being re-derived
    in the UI. Connectivity is STRUCTURAL and ignores definitions — an undefined rooted term still CONDUCTS
    grounding to its neighbours, it is merely not offered itself — but out-of-service terms do not conduct,
    so retiring a term also withdraws whatever reached data only through it. The rule cannot be enforced
    at creation (a term must exist before an edge can attach to it), so it is enforced at CONSUMPTION:
    MCP term search, metadata export, and the admin surface all apply it. Rootedness is the caller''s:
    the repository counts every ref, the exporter counts only refs whose column actually publishes. CURATION-PRESERVING
    SETTLE: a term losing its last physical ref is deprecated rather than deleted whenever it carries
    curator work — a definition, a relationship, a named expert, or a retirement — because a derived term
    is born blank, so any of those can only have come from a person and deleting the row would discard
    it silently. Deprecation (system-set) is auto-cleared by _find_or_create_term when a matching column
    reappears; retirement (curator-set) never auto-clears. Blank refless terms are still hard-deleted.
    COLUMN-REMOVAL WARNING (REQ-1484 surface): the pre-save dependents check reports each bound term with
    the outcome already decided by the settle rule — kept (has another column), kept but taken out of
    service (curated), or deleted with the column (derived and blank) — so an administrator reads the
    consequence rather than inferring it. RELATIONSHIP RETYPING: rel_type is part of an edge''s identity,
    so correcting one is a single curation act served by PATCH /admin/glossary/terms/{term_id}/edges —
    the repository does the delete and the insert together inside one transaction, rather than the UI
    publishing twice and leaving a window in which the relationship does not exist. An incoming edge is
    retyped through the same stored forward values shown under reverse labels, so the curator reads the
    sentence on screen while the server still receives the edge''s own direction. GLOSSARY PURPOSE HELP:
    a hover bubble beside the add control states what the glossary is for — that terms for real columns
    are derived automatically, and a hand-added term is warranted only for a concept no single column
    carries — because the button is self-evident and the purpose is not. The same control (provisa-ui/src/components/HelpBubble.tsx)
    sits beside the add action on Sources, Tables/Views, Relationships, Metrics, Tags and the Glossary,
    each carrying purposeTitle/purposeBody/purposeAdd/purposeAria in all 13 locales — the copy says what
    the object does once it exists and what the curator is deciding by creating one, never what the button
    does. An icon-only control has no visible label at all, so on Tables/Views and Relationships the icon
    is itself the hover target (HelpBubble accepts the control as its target instead of rendering a second
    ''?''): the ERD icon on both pages, and the SQL modeling workbench and Suggest with AI icons on Relationships,
    each explaining what the tool behind the glyph opens and whether it edits the model or only reads
    it. The Tables/Views copy carries a third paragraph (purposeModel) because a view is not a saved query:
    registration also fixes how it is served (live, or materialized on a cadence or a calendar window
    per REQ-962), what a refresh writes (replace / upsert / append), whether it keeps append-only history
    for as-of reads (REQ-1162), and its lineage inputs, while + Model lowers an entity or fact onto those
    same primitives plus the relationships (REQ-1164). Security carries the same control beside + RLS,
    stating that a rule is a SQL predicate bound to a role and a scope that the server ANDs into the query
    on reads and writes alike (REQ-038/040), that a table rule wins over a domain rule for that table
    (REQ-402), and that a rule names a role rather than a person. Security''s Roles surface carries its
    own bubble beside + Role, stating that a role is the unit every other permission is written against
    — capabilities decide which operations it may run and domain access which parts of the model it reaches
    (REQ-003/005/042) — and that it carries its own rate and query-complexity ceilings (REQ-369/1174).
    The card is the single explanation style: the NL page and the MCP explorer render their control explanations
    through the same component rather than as Mantine tooltips, with the control itself as the hover target
    so the row keeps its shape.'
  use_case: Bottom-up bootstrap means the glossary is never empty — every column name seeds the vocabulary,
    avoiding the failure mode of top-down glossary projects. Concept-level discovery ("show every physical
    field that means order date"), expert routing for data questions, grounded vocabulary for NL-to-SQL
    and agent surfaces via the MCP term-lookup tool, and glossary interchange with external catalogs through
    metadata export.
  scenario: 'Given tables with columns cust_id, customerId, and CUSTOMER_KEY registered in the semantic
    layer

    When deterministic normalization runs

    Then the fields resolve to deduplicated terms each listing its physical refs

    And a user can move a physical ref to another term, rename the term, and record a definition and experts

    And a user can create an abstract term linked to a rooted term via KIND_OF

    When the last physical ref of a term is deleted from the semantic layer

    Then the term is removed

    But if an abstract term is connected to the rooted graph through the term

    Then the term is deprecated instead and the abstract term is not left dangling

    '
  integration_test: required
  integration_test_reason: Term derivation, ref lifecycle, and deprecation are only verifiable against
    a real catalog registration/deletion flow.
  e2e: true
  e2e_reason: Glossary curation (rename, ref move, definitions, experts, abstract terms) is an admin UI
    workflow; MCP term lookup is a client-facing surface.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  code:
  - provisa/core/glossary.py
  - provisa/core/repositories/glossary.py
  - provisa/core/repositories/table.py
  - provisa/core/schema.sql
  - provisa/core/schema_org.py
  - provisa/api/admin/glossary_router.py
  - provisa/api/admin/column_dependents.py
  - provisa/api/mcp/tools.py
  - provisa/api/metadata_export/builder.py
  - provisa/api/metadata_export/model.py
  - provisa/api/metadata_export/publishing.py
  - provisa/api/metadata_export/atlas.py
  - provisa/api/metadata_export/atlan.py
  - provisa/api/metadata_export/datahub.py
  - provisa/api/metadata_export/openmetadata.py
  - provisa/api/metadata_export/collibra.py
  - provisa-ui/src/components/admin/GlossaryTab.tsx
  tests:
  - tests/unit/test_glossary_normalize.py
  - tests/unit/test_glossary_repository.py
  - tests/unit/test_glossary_admin_surface.py
  - tests/unit/test_mcp_glossary_protocol.py
  - tests/unit/test_metadata_export_glossary.py
  - tests/unit/test_metadata_export_glossary_atlas.py
  - tests/unit/test_metadata_export_glossary_atlan.py
  - tests/unit/test_metadata_export_glossary_datahub.py
  - tests/unit/test_metadata_export_glossary_openmetadata.py
  - tests/unit/test_metadata_export_glossary_collibra.py
  - tests/unit/test_column_dependents.py
  - tests/integration/test_glossary_lifecycle.py
  - tests/steps/steps_business_glossary_ontology.py
  - provisa-ui/src/__tests__/GlossaryTab.test.tsx
  - provisa-ui/e2e/glossary.spec.ts
  - tests/steps/steps_business_glossary_ontology.py
  stakeholders:
  - data-steward
  - app-developer
  relates_to:
  - REQ-1068
  - REQ-1373
  - REQ-1377
- id: REQ-1388
  status: complete
  since: 2026-08
  group: 11. Platform, Infrastructure & Delivery
  category: Data Catalog Integration
  priority: SHOULD
  type: structural
  description: 'Provisa-native Atlas entity typedefs. The Atlas exporter currently transports every source
    through the out-of-the-box rdbms_* entity types (rdbms_instance/rdbms_db/rdbms_table/ rdbms_column),
    recording the real source type only as an attribute — an API or GraphQL source renders as an RDBMS
    in the catalog. Atlas supports registering custom ENTITY typedefs through the same /api/atlas/v2/types/typedefs
    endpoint the exporter already uses for classification typedefs, so Provisa registers its own kinds
    (e.g. provisa_source with a sourceType attribute, provisa_table, provisa_column, provisa_api_endpoint)
    with the attributes the model actually carries (provisaUri, governance document, modeling role), and
    publishes entities under them. Typedefs are registered idempotently before entities, exactly like
    classification defs. Atlan caveat: its layered type system gives custom types reduced native UI treatment,
    so the Atlan adapter may keep the mapped built-in types while plain Apache Atlas gets the native ones
    — a per-provider mapping decision, not a shared constraint.'
  use_case: Catalog readers see what a source actually is (API, GraphQL, warehouse) instead of every source
    masquerading as an RDBMS; Provisa attributes become first-class typed fields Atlas can search and
    facet on, rather than opaque strings on borrowed types.
  relates_to:
  - REQ-1069
  - REQ-1385
  code:
  - provisa/api/metadata_export/atlas.py
  - provisa/api/metadata_export/atlan.py
  tests:
  - tests/unit/test_metadata_export_atlas_native.py
- id: REQ-1389
  status: in-progress
  target: 2026-Q4
  group: 11. Platform, Infrastructure & Delivery
  category: Data Catalog Integration
  priority: MUST
  type: constraint
  description: 'Catalog upsert preserves human enrichment: an attribute-ownership contract for every exporter.
    Publishing is an UPSERT on the vendor''s stable key (Atlas/Atlan: typeName + qualifiedName via /entity/bulk,
    which merges at attribute level; DataHub: per-aspect URN upsert; OpenMetadata: fqn PUT) — never a
    delete-and-recreate, and entities absent from a later snapshot are never pruned by publish. Every
    attribute is classified as either PROVISA-OWNED (identity, physical binding, technical facts, governance
    projections, provisa_* classifications — written on every publish and drift-corrected by the REQ-1072
    reconcile) or HUMAN-OWNED (Atlas userDescription, human-added classifications, glossary term assignments,
    labels, hand-drawn lineage — NEVER written by the exporter, so steward enrichment in the catalog survives
    every publish and reconcile). Consequence: the Atlas governance document moves OFF userDescription
    — the exact field the Atlas UI edits for human descriptions, clobbered today on every publish — onto
    a Provisa-owned typed attribute (REQ-1388 native typedefs). The reconcile''s drift correction applies
    to Provisa-owned attributes only. OWNERSHIP RULE (settled): authorship decides — if Provisa authored
    a value (typed provisa* attributes, the fields it writes, provisa_-prefixed classifications) Provisa
    overwrites it on every publish; if the value originated in the catalog (userDescription, human-added
    classifications, terms, labels, drawn lineage) the catalog wins and Provisa never writes it. This
    is a single-writer MERGE, not a sync: nothing flows back into Provisa (publication is outbound-only),
    each data element has exactly ONE owning system, and the read step exists only because replace-style
    catalog APIs force carrying the catalog-owned fields through unchanged. The merge is NOT optional:
    the classification read-merge and attribute drift-correction ride the single publish path, so all
    three triggers get them — Publish now, the event drain, and the scheduled reconcile (reconcile_cron,
    default hourly). URN-CANONICAL UPSERT: the Provisa URN (REQ-1385, published as the provisaUri attribute)
    is the canonical identity, not the physical qualifiedName. Before publishing, the exporter indexes
    live entities by URN; an entity Provisa published before whose physical address changed (re-platform,
    physical rename) takes the live guid and is UPDATED in place — same catalog entity, new binding, enrichment
    and lineage intact — never duplicated-and-orphaned. UPDATES MERGE: Atlas bulk createOrUpdate replaces
    the full attribute map (empirically verified — an update wiped a steward-written userDescription),
    so every update starts from the LIVE attributes and overlays only the fields Provisa authors; fields
    Provisa never populates survive verbatim. VENDOR-ID TRACKING (settled: "track their guids and use
    them to sync"): every successful publish CAPTURES the vendor''s own id per published asset — Atlas
    entity guid (from the bulk response''s guidAssignments), OpenMetadata entity UUID (from the table
    PUT response), Collibra asset UUID (exact-name lookup after the import job, only when the binding
    is missing or the name changed), DataHub dataset URN — into the catalog_bindings table, one row per
    (provider, semantic_uri) with the vendor-side name-key as physical_key. The next publish USES the
    stored id to rebind on a physical re-address instead of trusting the vendor''s name-keyed upsert:
    Atlas prefers the STORED guid over the laggy basic-search index (the index only covers URIs with no
    binding); OpenMetadata cannot rename by PATCH (name/databaseSchema are not PATCH-updatable and TableRepository
    never opts into renameAllowed), so the rebind is SUCCESSION — the predecessor is read by its stored
    UUID, its steward enrichment carried onto the successor''s first publish, and its description marked
    superseded with a pointer to the new FQN and the Provisa URN; Collibra PATCH-renames the live asset
    by stored UUID BEFORE the name-keyed import so the job matches the SAME asset; DataHub URNs are immutable
    identity, so the successor publishes as a new URN and the OLD bound URN gets the deprecation aspect
    naming the successor and the Provisa URN. The binding lifecycle (load before publish, persist capture
    and prune departed-asset bindings after) rides the single publish path, so all triggers get it — Publish
    now, the event drain, the scheduled reconcile and the CLI publish.'
  use_case: Stewards enrich published assets inside the catalog (descriptions, terms, extra classifications);
    a publish or scheduled reconcile that overwrote that work would make the catalog write-once and untrustworthy.
    The ownership split lets Provisa keep its facts current while human curation accumulates safely.
  relates_to:
  - REQ-1069
  - REQ-1072
  - REQ-1385
  - REQ-1388
  code:
  - provisa/api/metadata_export/atlas.py
  - provisa/api/metadata_export/atlan.py
  - provisa/api/metadata_export/openmetadata.py
  - provisa/api/metadata_export/collibra.py
  - provisa/api/metadata_export/datahub.py
  - provisa/api/metadata_export/publishing.py
  - provisa/api/metadata_export/provider.py
  - provisa/core/repositories/catalog_binding.py
  - provisa/core/schema.sql
  - provisa/core/schema_org.py
  tests:
  - tests/unit/test_metadata_export_atlas_native.py
  - tests/unit/test_metadata_export_bindings.py
  - tests/unit/test_catalog_binding_repository.py
  - tests/unit/test_metadata_export_openmetadata_merge.py
  - tests/unit/test_metadata_export_glossary_atlas.py
  - tests/unit/test_metadata_export_glossary_atlan.py
  - tests/unit/test_metadata_export_glossary_openmetadata.py
- id: REQ-1390
  status: complete
  since: 2026-08
  group: 10. UI & Admin Surfaces
  category: Admin Management UI
  priority: SHOULD
  type: behavioral
  description: Admin Reports viewer lists ops-domain registered tables as management reports. Selecting
    a report opens the server-paged governed viewer — one page (100 rows + lookahead row) per SELECT *
    with LIMIT/OFFSET through the SQL pipeline; the full dataset is never loaded. "Add report" registers
    a governed Provisa view as a DERIVED source in the ops domain; delete is allowed only for custom DERIVED-source
    reports (system seeded reports are not deletable).
  use_case: Operations team can view and manage registered tables as reports via the admin console without
    requiring SQL knowledge; auto-registration of governed views simplifies report creation.
  scenario: 'Given an ops-domain registered table exists

    When the admin navigates to /admin/reports

    Then the table appears as a management report entry; clicking it opens the server-paged

    governed viewer, "Add report" registers a new DERIVED source, and delete is allowed only for custom
    reports.

    '
  code:
  - provisa-ui/src/components/admin/ReportsTab.tsx
  - provisa-ui/src/components/GovernedTableViewer.tsx
  - provisa-ui/src/pages/AdminPage.tsx
  - provisa-ui/src/App.tsx
  - provisa-ui/src/components/NavBar.tsx
  tests:
  - provisa-ui/src/__tests__/adminNavCapabilities.test.ts
  relates_to:
  - REQ-1386
- id: REQ-1391
  status: complete
  since: 2026-08
  group: 10. UI & Admin Surfaces
  category: Results Grid
  priority: SHOULD
  type: behavioral
  description: 'Shared results grid provides sort, per-column filter, and multi-level group-by toggled
    via a per-column Layers icon; grouping renders collapsible group-header rows (presentation only, never
    aggregates); filter/group/sort choices persist per table in localStorage and restore on next visit.
    Two modes: client mode (SQL workbench — choices apply to the fetched sample in the browser) and server-paged
    mode (governed viewer — choices are pushed into the query as WHERE/ORDER BY and the pager advances
    on a hasMore lookahead, total unknown).'
  use_case: SQL workbench, admin Reports viewer, and Tables-page preview modal share a consistent, reusable
    grid component with interactive exploration without aggregation overhead.
  scenario: 'Given results are displayed in the grid

    When a user toggles Layers on a column and selects group-by

    Then collapsible group-header rows appear; sort/filter/group choices persist in localStorage

    and restore automatically on the next visit to the same table.

    '
  code:
  - provisa-ui/src/pages/sql/useResultsGrid.ts
  - provisa-ui/src/pages/sql/ResultsGrid.tsx
  tests:
  - provisa-ui/src/pages/sql/__tests__/useResultsGrid.test.tsx
- id: REQ-1392
  status: complete
  since: 2026-08
  group: 10. UI & Admin Surfaces
  category: Table Preview & Governance
  priority: SHOULD
  type: behavioral
  description: 'Table preview modal is the server-paged governed viewer: each page is its own SELECT *
    with LIMIT pageSize+1/OFFSET, filters and sorts pushed into the query, group columns leading the ORDER
    BY (contiguous groups across pages) and primary-key columns appended as a stable-paging tiebreaker
    — arbitrarily large tables cost one page per view. Tables backed by APIs with native params require
    path_param values (and accept optional query_param values) as WHERE predicates before Preview or Profile
    may run; profile then samples via the governed SQL pipeline (row-capped) instead of the bare admin
    endpoint.'
  use_case: Users can preview large tables safely within governance limits; native-param tables (APIs
    with path/query parameters) cannot be profiled without parameter binding, preventing catalog errors
    and ensuring governed access.
  scenario: 'Given a table backed by an API with native path parameters exists

    When a user clicks Preview or Profile without providing path_param values

    Then the action is blocked; once path_param values are provided, Profile runs via the governed SQL
    pipeline

    and the preview modal serves one governed page at a time through governance controls.

    '
  code:
  - provisa-ui/src/components/TablePreviewModal.tsx
  - provisa-ui/src/components/GovernedTableViewer.tsx
  - provisa-ui/src/components/nativeParams.ts
  - provisa-ui/src/components/NativeParamsModal.tsx
  - provisa-ui/src/pages/tables/TableReadView.tsx
  - provisa-ui/src/pages/TablesPage.tsx
  tests:
  - provisa-ui/src/components/__tests__/nativeParams.test.ts
- id: REQ-1393
  status: complete
  group: 1. Access Governance & Security
  category: Authentication
  priority: MUST
  type: constraint
  since: 2026-08
  description: Failed authentication attempts are counted and locked out at the credential-validation
    layer (provisa/auth/throttle.py), not per surface. Every surface that validates a credential — HTTP,
    pgwire, Bolt, gRPC, Arrow Flight, MCP, and the POST /auth/login routes — counts into one process-wide
    store, so attempts spread across protocols accumulate against the same subject. The lockout key is
    the principal the protocol names (HTTP Basic user, pgwire startup user, Bolt principal) and, where
    a protocol names none, the SHA-256 digest of the credential. Defaults are 5 failures within a 300-second
    window, then a 900-second lockout; auth.login_throttle overrides them. A lockout raises LockedOut,
    a PermissionError rather than a ValueError, so no surface can report it as an ordinary wrong password
    — HTTP answers 429 with Retry-After, pgwire answers FATAL 28000, Bolt answers Neo.ClientError.Security.AuthenticationRateLimit.
    A successful validation clears the subject's history; a non-credential failure (identity-provider
    outage, database fault) is neither counted nor swallowed.
  use_case: Without a shared counter an attacker guesses a password on whichever surface is unmetered,
    or resets their allowance by switching protocol. Placing the counter where credentials are validated
    makes the brake protocol-independent, and refusing to distinguish a lockout from a bad password would
    leave clients retrying into a wall.
  code:
  - provisa/auth/throttle.py
  - provisa/auth/wiring.py
  - provisa/auth/middleware.py
  - provisa/auth/providers/simple.py
  - provisa/auth/providers/basic.py
  - provisa/pgwire/server.py
  - provisa/bolt/session.py
  - provisa/grpc/auth.py
  - provisa/api/flight/server.py
  - provisa/api/mcp/server.py
  - provisa/core/models.py
  tests:
  - tests/unit/test_login_throttle.py
  scenario: 'Given auth.login_throttle allows 3 attempts

    When alice presents a bad password once over HTTP, once over pgwire and once over Bolt

    Then the third attempt reaches the configured limit

    And the next attempt on any surface is refused as a lockout, not as a wrong password

    And HTTP answers 429 with Retry-After, pgwire answers FATAL 28000, and Bolt answers Neo.ClientError.Security.AuthenticationRateLimit

    And the correct password is refused too until the lockout expires

    '
  related_reqs:
  - REQ-124
  - REQ-693
  - REQ-1263
- id: REQ-1394
  status: complete
  group: 1. Access Governance & Security
  category: Authentication
  priority: SHOULD
  type: behavioral
  since: 2026-08
  description: pgwire negotiates SCRAM-SHA-256 (RFC 5802) when auth.scram is enabled under the basic provider,
    so a local password is proven to the server without ever crossing the wire. The server advertises
    AuthenticationSASL (code 10) listing only SCRAM-SHA-256 — never -PLUS, because channel binding is
    not implemented — and drives the two round trips from provisa/pgwire/server.py. The verifier lives
    in its own scram_credentials table, one row per user, holding PostgreSQL's pg_authid spelling "SCRAM-SHA-256$<iterations>:<salt>$<storedkey>:<serverkey>"
    at 4096 iterations. Verifiers are written at the only four moments a plaintext password exists — admin
    user creation, admin password change, setup bootstrap and wizard, and self-service registration —
    and deleted with the account. A username with no verifier is answered with PostgreSQL's mock authentication,
    a well-formed exchange derived from the name and a per-process seed that no proof satisfies, so the
    handshake is not a name oracle. A verified proof is turned into a session by re-reading the account
    through BasicAuthProvider.identity_for, so a deactivated user is refused after a correct proof exactly
    as after a correct password. Failed proofs and lockouts run through the REQ-1393 throttle, checked
    before any verifier is read. SCRAM is offered only under the basic provider — a bearer token or personal
    access token is an opaque secret in the password field that SCRAM cannot carry — and those deployments
    keep the cleartext request protected by TLS. Existing bcrypt hashes cannot be converted to verifiers,
    so a deployment turning auth.scram on requires every local user to set their password again.
  use_case: A cleartext password over pgwire is safe only for as long as the transport is, and it is replayable
    by anything that terminates TLS. SCRAM leaves the server holding a verifier rather than anything that
    can be presented as a credential, gives the client mutual authentication through the server signature,
    and is what every modern libpq-based driver already negotiates by default.
  code:
  - provisa/auth/scram.py
  - provisa/auth/scram_store.py
  - provisa/auth/providers/basic.py
  - provisa/pgwire/server.py
  - provisa/core/schema_admin.py
  - provisa/api/setup_router.py
  - provisa/api/auth_router.py
  - provisa/api/admin/local_users_router.py
  tests:
  - tests/unit/test_scram.py
  - tests/unit/test_scram_pgwire.py
  scenario: 'Given auth.scram is enabled under the basic provider

    When a libpq client connects to pgwire as alice

    Then the server advertises SASL with SCRAM-SHA-256 alone

    And alice''s correct password completes the exchange without the password reaching the server

    And the client can verify the server signature in the final message

    And a wrong proof is refused as FATAL 28P01 and counts against the REQ-1393 lockout

    And a username with no verifier gets an exchange indistinguishable from one that has

    And a deactivated account is refused even when its proof is correct

    '
  related_reqs:
  - REQ-124
  - REQ-693
  - REQ-1393
  - REQ-1228
- id: REQ-1395
  status: complete
  since: 2026-08
  group: 13. Multi-Tenancy & Organizations
  category: Org AI Model Configuration
  priority: SHOULD
  type: structural
  description: In a multi-tenant SaaS deployment, org admins can select the AI model their org uses via
    an API-driven configuration (provider + model + API key per org), rather than using a shared platform-wide
    model.
  use_case: Organizations with distinct governance or compliance needs can use different LLM providers
    or models, enabling flexible multi-tenant deployments where each org controls its own AI surface.
  target: 2026-Q4
- id: REQ-1402
  status: complete
  since: 2026-08
  group: 8. Client Access & Protocols
  category: REST API
  priority: SHOULD
  type: behavioral
  description: The REST `?includeNodes=` parameter (introduced in REQ-1401) now accepts a field-level
    projection in addition to `true`/`false`—either a JSON array or comma-separated list of dot-notated
    paths to select specific scalar fields and relationship traversals, at arbitrary depth, from the nodes
    row-detail.
  use_case: Clients can request only the specific fields they need from nodes detail rows instead of requesting
    all available fields, reducing payload size and allowing selective field hydration in group-by aggregate
    responses.
  code:
  - provisa/api/rest/generator.py
  - provisa/api/rest/openapi_spec.py
  tests:
  - tests/unit/test_rest_aggregates.py
  - tests/unit/test_openapi_spec.py
  scenario: "Given a group-by response exposes rows with a `nodes` detail sub-selection\nWhen a client\
    \ submits `?includeNodes=[\"user_id\",\"user.email\"]` or `?includeNodes=user_id,user.email`\nThen\
    \ the nodes array includes only the user_id scalar field and the user relationship's email field\n\
    And each path segment is translated via apply_gql_name with no local schema-membership check\nAnd\
    \ an unresolvable path is left for parse_query's own GraphQL validation to reject with a 400,\n  same\
    \ as by_cols/groupBy elsewhere in this module\nAnd dot paths may traverse relationships to any depth\
    \ the schema itself supports, not just one hop\nAnd the selection is internally compiled to a nested\
    \ GraphQL selection set\n"
  related_reqs:
  - REQ-1401
- id: REQ-1403
  status: complete
  since: 2026-08
  group: 5. Query Languages, Compilation & Operations
  category: GraphQL Schema Generation
  priority: MUST
  type: structural
  description: GraphQL relationship field names are generated via rel_field_name() which now respects
    the configured naming convention (snake or apollo_graphql/camelCase), matching the behavior of apply_gql_name()
    for scalar fields. Previously rel_field_name() always produced camelCase regardless of the global
    gql naming convention setting, creating inconsistency between how relationship and scalar fields were
    named.
  use_case: Ensure relationship field names follow the same naming convention as scalar fields—a schema
    configured with gql="snake" now produces snake_case relationship field names (e.g. "home_region" instead
    of "homeRegion").
  code:
  - provisa/compiler/naming.py
  tests:
  - tests/unit/test_rest_aggregates.py
- id: REQ-1404
  status: complete
  since: 2026-08
  group: 5. Query Languages, Compilation & Operations
  category: Cypher Compiler
  priority: MUST
  type: behavioral
  description: ExprContext.resolve_property() must resolve post-WITH property references using the variable's
    resolved SQL alias from _var_table (the WITH-segment CTE name), not the raw Cypher variable name.
  use_case: Cypher queries that carry a node variable through a WITH clause with an aggregate and then
    access that node's properties in RETURN fail with "Column cannot be resolved" errors without correct
    SQL table qualification.
  code:
  - provisa/cypher/expr_context.py
  tests:
  - tests/unit/test_cypher_translator.py
  scenario: 'Given a Cypher query with a node variable carried through a WITH clause with an aggregate
    (e.g. `WITH u, COUNT(i) AS c`)

    When the RETURN clause accesses properties of that node variable (e.g. `u.name`)

    Then the generated SQL correctly qualifies property references using the resolved SQL alias (e.g.
    `_w0.name`)

    '
- id: REQ-1405
  status: complete
  since: 2026-08
  group: 5. Query Languages, Compilation & Operations
  category: Aggregate & Group-by Queries
  priority: MUST
  type: behavioral
  description: _resolve_aggregation_plan (provisa/nl/runner.py) must derive includeNodes dot-paths (dim_paths)
    from SELECT-projected columns of a joined dimension table (e.g. users.name alongside COUNT(inquiries.id)),
    so JSON:API and OpenAPI's synthesized includeNodes= value reflects the actual relationship-detail
    fields the SQL/GraphQL branches resolved, instead of defaulting to includeNodes=true which only returns
    the base aggregate table's own scalar fields. gRPC's {Type}GroupByRequest gains include_nodes (bool)
    and include (repeated string) fields, and {Type}GroupByRow gains a typed repeated {Type} nodes field,
    so the gRPC group-by RPC can return the same joined-dimension detail rows as JSON:API/OpenAPI/GraphQL
    instead of only groupKey/aggregate.
  use_case: NL Explore UI queries like "show aggregate count of inquiries by user with user details" must
    include the joined user detail fields (name, email, etc.) in the SQL, GraphQL, JSON:API, OpenAPI,
    and gRPC panels' generated requests/calls alike.
  code:
  - provisa/nl/runner.py
  - provisa/grpc/proto_gen.py
  - provisa/grpc/query_ir.py
  - provisa/grpc/server.py
  tests:
  - tests/unit/test_nl_aggregation_routing.py
  - tests/unit/test_grpc_aggregates.py
  - tests/unit/test_grpc_server.py
  scenario: 'Given SQL that joins a dimension table and projects its columns alongside an aggregate on
    the base table (e.g. "SELECT users.name, COUNT(inquiries.id) FROM users JOIN inquiries ON users.id
    = inquiries.user_id GROUP BY users.name")

    When _resolve_aggregation_plan parses this SQL

    Then dim_paths contains the joined-table columns as includeNodes dot-paths (e.g. "user.name"), and
    JSON:API/OpenAPI''s generated query text includes them via includeNodes=, and gRPC''s QueryXGroupBy
    call is shown with include_nodes=true, include=[user]

    '
- id: REQ-1406
  status: complete
  since: 2026-08
  group: 5. Query Languages, Compilation & Operations
  category: Natural Language Query
  priority: SHOULD
  type: behavioral
  description: Both NL→SQL generation paths produce a best-effort Cypher form alongside SQL. The GraphQL-backed
    ("strict") path compiles NL to GraphQL first, then reuses the same SQL and Cypher compilation the
    GraphQL Explorer uses, so join reachability and Cypher shape come from approved schema relationships
    rather than prompt discipline. The direct-SQL path calls `best_effort_cypher_for_sql` to run the already-generated
    semantic SQL through the same `semantic_sql_to_cypher` translator once, with no per-query result_limit/params
    list. Cypher is None (not an error) when the SQL has no Cypher-representable pattern; the SQL result
    stands on its own either way.
  use_case: A user asking an NL question in the SQL Explorer or NL/SQL editor panel sees a Cypher form
    of the answer alongside SQL, without needing to separately compose or understand Cypher, matching
    the existing NL→SQL/Cypher/GraphQL pipeline description.
  code:
  - provisa/nl/runner.py
  - provisa/nl/loop.py
  - provisa/nl/prompt.py
  - provisa/nl/job.py
  - provisa/api/data/endpoint_dev.py
  - provisa/api/rest/nl_router.py
  tests:
  - tests/unit/test_nl_runner.py
  - tests/e2e/test_bolt_cypher.py
  scenario: 'Given an NL question that maps to a joinable, Cypher-representable query shape

    When the question is submitted through either the GraphQL-backed or direct-SQL NL path

    Then the response includes a Cypher form derived from the same compiled SQL, alongside the SQL and
    (for the GraphQL-backed path) GraphQL forms

    '
- id: REQ-1407
  status: complete
  since: 2026-08
  group: 11. Platform, Infrastructure & Delivery
  category: Cloud Appliance Bootstrap
  priority: MUST
  type: behavioral
  description: docker-compose.app.yml passes PROVISA_ENGINE through to both the provisa and provisa-ui
    containers. Previously the variable was set in provisa.env but not forwarded into the containers'
    environment, so the federation engine silently fell back to DuckDB instead of the pinned engine (e.g.
    trino) even when provisa.env correctly specified it.
  use_case: A cloud node deployment that pins PROVISA_ENGINE=trino in provisa.env must actually run against
    Trino; a silent fallback to DuckDB serves wrong/incomplete data for orgs that depend on Trino-only
    federation behavior.
  code:
  - docker-compose.app.yml
  tests:
  - tests/unit/test_infra_requirements.py
  scenario: 'Given provisa.env sets PROVISA_ENGINE=trino

    When the provisa and provisa-ui containers start via docker-compose.app.yml

    Then both containers see PROVISA_ENGINE=trino in their environment and the federation engine selects
    Trino, not the DuckDB default

    '
- id: REQ-1408
  status: complete
  since: 2026-08
  group: 5. Query Languages, Compilation & Operations
  category: Natural Language Query
  priority: SHOULD
  type: behavioral
  description: NL group-by results support a dot-path `include` projection (`rel.col`, plus bare base-table
    scalars) instead of only whole-relation names, mirrored consistently across gRPC (`GroupByRequest.include`),
    JSON:API (`?include=`), and REST (`?includeNodes=` dot-paths). In strict NL mode, the gRPC/JSON:API/OpenAPI/REST
    query plan is now derived by rewriting the already schema-validated GraphQL branch's query (root field,
    `by:` argument, `aggregate` and `nodes` sub-selections) rather than re-parsing the compiled SQL, because
    a GraphQL query with a `nodes` sub-selection compiles to SQL with a subquery in its outer FROM that
    the SQL-based resolver cannot map back to a table.
  use_case: An NL group-by question that also asks for specific joined-dimension columns (not every column
    on the related table) gets identically-scoped projections across every generated protocol surface,
    instead of some surfaces over-fetching whole relations.
  code:
  - provisa/grpc/query_ir.py
  - provisa/nl/runner.py
  - provisa/nl/executor.py
  - provisa-ui/src/pages/NlPage.tsx
  tests:
  - tests/unit/test_grpc_aggregates.py
  - tests/unit/test_nl_aggregation_routing.py
  scenario: 'Given an NL group-by question projecting specific columns from a joined dimension table

    When the strict GraphQL branch resolves a nodes sub-selection with dot-path columns

    Then the gRPC, JSON:API, and REST generated queries all request the identical dot-path projection

    '
- id: REQ-1409
  status: complete
  since: 2026-08
  group: 13. Multi-Tenancy & Organizations
  category: Org AI Model Configuration
  priority: SHOULD
  type: behavioral
  description: The per-operation Model field in the org's AI Models admin surface is a picker whose options
    are read live from the selected vendor's own list-models API (`GET /admin/ai-models/vendors/{vendor}/models`,
    resolved with the org's key and falling back to the deployment credential the same calls already use),
    and which still accepts a typed value for a model the listing does not carry. A vendor that publishes
    no list-models API, or has no key set, or rejects the call leaves the field a plain typed input with
    the reason stated beneath it.
  use_case: An org admin picks the model a role uses from the names their vendor actually serves — including
    models released after this build shipped — instead of transcribing a model id by hand and discovering
    a typo only when the next NL query fails.
  code:
  - provisa/llm/vendor_models.py
  - provisa/api/admin/ai_models_router.py
  - provisa-ui/src/api/aiModels.ts
  - provisa-ui/src/components/admin/AiModelsTab.tsx
  tests:
  - tests/unit/test_admin_config_tabs.py
  - provisa-ui/src/__tests__/AiModelsTab.test.tsx
  scenario: 'Given an org has set an API key for its selected vendor

    When the admin opens the AI Models tab and focuses a role''s Model field

    Then the field offers the model names that vendor''s list-models API currently returns

    And a name not in that list can still be typed and saved

    And a vendor with no list-models API or no usable key leaves the field typeable with the reason shown

    '
- id: REQ-1410
  status: complete
  since: 2026-08
  group: 1. Access Governance & Security
  category: Ops Domain Governance
  priority: MUST
  type: behavioral
  description: Every column registered under the `ops` domain carries a read grant for the domain's steward
    (`org_admin`) — both seeds write it, and startup converges any column already stored without it while
    preserving grants held by other roles. `ops` is a lockdown domain, where an empty `visible_to` means
    visible to no role, so an ungranted column keeps its table out of every role's compilation context
    and the report's `"ops"."<table>"` reference reaches the engine unrewritten.
  use_case: An org admin opens Admin -> Reports and every report renders, instead of the telemetry-backed
    ones failing with SCHEMA_NOT_FOUND on schema 'ops' because their columns were seeded ungranted.
  code:
  - provisa/api/startup_seed.py
  - provisa/compiler/schema_gen.py
  tests:
  - tests/unit/test_ops_domain.py
  scenario: 'Given the ops telemetry tables were seeded before the steward grant existed

    When the server starts

    Then every ops column lists org_admin in visible_to

    And grants previously made to other roles are preserved

    And an org_admin SELECT against an ops table is rewritten to its physical catalog reference

    '
- id: REQ-1411
  status: complete
  since: 2026-08
  group: 5. Query Languages, Compilation & Operations
  category: SQL Rewriting
  priority: MUST
  type: behavioral
  description: The DIRECT lowering (`rewrite_semantic_to_physical`) normalizes table references before
    substituting physical names, so a table whose physical name differs from the name clients see (`ops.query_audit_log`
    -> `org_<id>.query_audit_log_ops`) keeps the semantic name as the reference's explicit alias. Substituting
    first renamed the reference before the alias was pinned, aliasing it to the physical name and stranding
    every column qualifier.
  use_case: A report or table read that routes DIRECT to the provisa-admin plane returns rows instead
    of "missing FROM-clause entry for table query_audit_log".
  code:
  - provisa/compiler/sql_rewrite.py
  tests:
  - tests/unit/test_direct_route_alias_rewrite.py
  scenario: 'Given an ops table registered under a physical name that differs from its semantic name

    When a SELECT qualifying its columns by the semantic name is lowered for the DIRECT route

    Then the physical reference carries the semantic name as its alias

    And every column qualifier resolves against a table in the FROM clause

    '
- id: REQ-1412
  status: complete
  since: 2026-08
  group: 13. Multi-Tenancy & Organizations
  category: Federation Engine
  priority: MUST
  type: behavioral
  description: 'An org administrator moves the acting org between three federation-engine lanes from an
    "Org Engine" admin tab: shared (the pooled coordinator, REQ-1243), isolated (a coordinator SaaS operates
    for that org alone, REQ-1043), and external (a coordinator the org itself operates, named by host
    and port). The mode is derived from the orgs row -- external_engine_host set means external, else
    isolated_engine decides -- never stored twice. An external endpoint outranks both the deployment''s
    shared endpoint and the isolated-host template. The surface is gated on org_settings, not platform_settings:
    the engine KIND stays deployment-wide. Isolated is refused with 503 where the deployment has no PROVISA_ISOLATED_ENGINE_HOST_TEMPLATE,
    since routing to the shared coordinator would void the isolation the org was promised. Changing the
    lane rebuilds the org runtime under the registry lock so the change reaches queries immediately. The
    isolated lane is also entitled, not merely available: a dedicated coordinator is compute the platform
    stands up and pays for, so on a commercial deployment it belongs to the Pro plan and a move onto it
    from a lesser plan is refused 402 org_engine.lane_not_entitled before anything is written or provisioned.
    The GET reports isolated_entitled alongside isolated_available so the tab disables the lane the org
    cannot buy rather than failing the save. The shared and external lanes are ungated -- shared is where
    every org starts, and an external engine is the org''s own compute. A deployment with no commercial
    plugin, and an org with no billing row, gate no lane.'
  use_case: An org with its own Trino cluster points Provisa at it; an org needing isolation from other
    tenants requests a dedicated SaaS coordinator; either returns to the shared lane.
  code:
  - .claude/commercial/provisa_commercial/org_engine_router.py
  - provisa/api/app.py
  - provisa/api/org_runtime.py
  - provisa/core/schema_admin.py
  - provisa/federation/trino_lifecycle.py
  - provisa/core/commerce.py
  - .claude/commercial/provisa_commercial/entitlements.py
  - provisa-ui/src/components/admin/OrgEngineTab.tsx
  tests:
  - tests/unit/test_org_engine_lane.py
  - .claude/commercial/tests/test_org_engine_lane.py
  - .claude/commercial/tests/test_lane_entitlement.py
  - provisa-ui/src/__tests__/OrgEngineTab.test.tsx
  - provisa-ui/src/__tests__/adminNavCapabilities.test.ts
  scenario: 'Given an org whose runtime names a coordinator the org operates

    When its terminal connection kwargs are resolved

    Then they target that coordinator rather than the shared or SaaS-dedicated endpoint

    And an org administrator sees the Org Engine tab in either tenancy mode

    '
- id: REQ-1413
  status: proposed
  target: 2026-Q4
  group: 3. Source Registration & Data Modeling
  category: Tag Registry
  priority: SHOULD
  type: behavioral
  description: 'Two semantic-content tags join the code-defined system tag set (REQ-1375, SYSTEM_TAGS
    in provisa/core/models.py) rather than being seeded as rows: `entity` marks a column holding names
    of real-world things, and `natural_language` marks a column holding language a person wrote, whose
    contents are unreachable by relational predicates and require regex, ILIKE, or semantic search to
    extract. Both are column-only, reason_policy and expires_policy hidden — they are states, not countdowns.
    Being intrinsics they are present in every install with no migration and no seed path, and are immutable
    by construction: there is no row to edit or delete, and the existing mutation guards (schema_mutation.py
    SYSTEM_TAG_IDS, config_loader.py) already refuse them. `natural_language` is chosen over `prose`/`unstructured`
    because it is a positive, testable claim about content, which makes the exclusion definitional rather
    than a caveat: serialized formats (JSON, XML, log lines, base64) are parseable, not written, so they
    do not qualify and never enter a text corpus. Entity TYPE is not a tag — the dimension table names
    it (`customers.name` tagged `entity` is a customer entity); neither is the join key, which is the
    table''s PK. Combined with the REQ-1320 modeling role, `[dimension, entity]` on a column reads as
    the canonical name of a modeled entity.'
  use_case: 'Consumers that must distinguish language from codes get it from the catalog instead of guessing:
    NL schema matching prefers entity columns when a question contains a proper noun, catalog search can
    answer "where does ''Acme'' live", vector generation knows which columns are worth embedding, and
    masking review sees that entity columns are usually the join between pii and non-pii data. A downstream
    retrieval or chunking service can discover the same facts without a Provisa-specific client.'
  code:
  - provisa/core/models.py
  - provisa/core/repositories/tag.py
  - provisa/api/admin/schema_mutation.py
  scenario: 'Given an install whose tags table holds only user tags

    When the tag registry is read

    Then entity and natural_language are present as system tags alongside pii, technical and deprecated

    And a request to edit or delete either is refused

    And both are assignable to columns only

    '
  related_reqs:
  - REQ-1320
  - REQ-1373
  - REQ-1375
  - REQ-1377
- id: REQ-1414
  status: proposed
  target: 2026-Q4
  group: 3. Source Registration & Data Modeling
  category: Tag Registry
  priority: SHOULD
  type: behavioral
  description: 'Tag assignments are readable as a queryable relation, not only through the admin API:
    a `column_tags` view yields one row per (object_type, source, schema, table, column, relationship,
    tag_id), NULL at the levels coarser than the assignment''s own grain, so table-level and source-level
    tags are not lost to a column-only projection. The view UNIONs the code-defined system tags in front
    of the tag_assignments table exactly as repository reads do (REQ-1375 intrinsics are never stored,
    so a naive view over the table silently omits pii, technical, deprecated, entity and natural_language).
    The view carries the same visibility filter as registered_tables: a principal who cannot see a table
    cannot enumerate its tags, since column names and pii markings are themselves disclosure. Because
    it is an ordinary relation it reaches every protocol — pgwire, Flight, GraphQL — with no per-surface
    work.'
  use_case: An external consumer (a retrieval/chunking service, a catalog crawler, a governance report)
    discovers which columns are entity names and which are natural language using stock SQL over pgwire,
    with no Provisa-specific driver. A chunking consumer's safety rule becomes `natural_language AND NOT
    pii`, evaluated from the catalog rather than from someone remembering.
  code:
  - provisa/core/repositories/tag.py
  - provisa/pgwire/catalog_populate.py
  - provisa/compiler/schema_gen.py
  scenario: 'Given a column tagged natural_language and a table tagged deprecated

    When the column_tags view is queried

    Then a row names the column with tag_id natural_language

    And a row names the table with a NULL column and tag_id deprecated

    And system tags appear even though no row for them exists in the tags table

    And a role without visibility of that table sees neither row

    '
  related_reqs:
  - REQ-1373
  - REQ-1375
  - REQ-1377
  - REQ-1413
- id: REQ-1415
  status: proposed
  target: 2026-Q4
  group: 3. Source Registration & Data Modeling
  category: Tag Registry
  priority: MAY
  type: behavioral
  description: 'Tag IDs are emitted into the bracketed comment suffix alongside the modeling role, so
    a catalog surface shows `[dimension, entity, natural_language]` where it shows `[fact, scd2]` today
    (append_modeling_tag, provisa/core/modeling_tags.py, REQ-1320) — reaching pg_description, GraphQL
    introspection and the Flight/MCP catalog description through the one shared formatter. The comment
    channel is a HUMAN-FACING MIRROR and is explicitly not the machine contract: the REQ-1414 view is
    authoritative, and nothing derives behavior by parsing the comment. To keep the mirror unambiguous
    the constraint is enforced upstream at tag creation rather than at render time — user tag IDs are
    restricted to a slug charset ([a-z0-9_-]), so a tag can never contain a comma or bracket and no surface
    needs escaping rules that would drift between the three.'
  use_case: Someone reading a column in DBeaver or a GraphQL schema sees its governance and semantic tags
    inline, without a second lookup against the admin API.
  code:
  - provisa/core/modeling_tags.py
  - provisa/pgwire/catalog_populate.py
  - provisa/compiler/schema_gen.py
  - provisa/api/flight/catalog.py
  - provisa/api/admin/schema_mutation.py
  scenario: 'Given a dimension column tagged entity and natural_language

    When its description is read from pg_description, GraphQL introspection or the Flight catalog

    Then each shows the same bracketed suffix listing the modeling role and both tags

    And an attempt to create a user tag whose id contains a comma or bracket is refused

    '
  related_reqs:
  - REQ-1320
  - REQ-1413
  - REQ-1414
- id: REQ-1416
  status: complete
  since: 2026-08
  group: 13. Multi-Tenancy & Organizations
  category: Federation Engine
  priority: MUST
  type: behavioral
  description: 'The SaaS CREATES an isolated org''s dedicated federation-engine coordinator, rather than
    only resolving where one would live. isolated_engine_endpoint (REQ-1043/REQ-1244) formats a hostname
    from PROVISA_ISOLATED_ENGINE_HOST_TEMPLATE; the provisioner creates a single-node Trino coordinator
    container carrying exactly that name as its container name and network alias, on the container network
    named by PROVISA_ISOLATED_ENGINE_NETWORK, from the image named by PROVISA_ISOLATED_ENGINE_IMAGE, bounded
    by PROVISA_ISOLATED_ENGINE_MEMORY, over the Docker Engine API on a bind-mounted socket. One derivation
    names both, so the terminal an org binds and the container that answers cannot drift apart. Its config.properties
    and jvm.config are uploaded into the created container rather than bind-mounted, because a bind mount''s
    source path is on the Docker host, which the app process only reaches through the socket. The coordinator
    runs catalog.management=dynamic and receives no catalog files -- the org''s runtime issues CREATE
    CATALOG on its own terminal -- and its jvm.config deliberately omits the shared cluster''s OpenTelemetry
    javaagent, whose jar exists only on a compose mount. Provisioning is idempotent (an existing container
    is started, a running one left alone) and happens BEFORE the org runtime binds its terminal, both
    at org creation with isolated_engine and when the engine-lane tab moves an org onto the isolated lane;
    leaving the lane removes the container, which holds no data. Creating an org with isolated_engine
    is refused with 503 where no host template is configured, the same gate the engine-lane tab applies.
    The Docker socket is granted through a separate docker-compose.isolated-engine.yml overlay a deployment
    opts into, never by default: a process that can create containers on the host can do anything on the
    host.'
  use_case: An org that requires tenant isolation gets a coordinator of its own that actually exists,
    from the onboarding checkbox or the Org Engine tab, without an operator standing one up by hand.
  code:
  - provisa/federation/isolated_provisioner.py
  - provisa/federation/engine.py
  - .claude/commercial/provisa_commercial/org_engine_router.py
  - provisa/api/admin/orgs_router.py
  - docker-compose.isolated-engine.yml
  tests:
  - tests/unit/test_isolated_engine_provisioner.py
  - tests/unit/test_org_engine_lane.py
  - .claude/commercial/tests/test_org_engine_lane.py
  scenario: "Given a deployment configured with an isolated-engine host template, image, network and socket\n\
    When an org is created with isolated_engine, or moved onto the isolated lane\nThen a coordinator container\
    \ is created whose name and network alias are the hostname that\n  org's terminal dials, on the network\
    \ the app can reach\nAnd it is started with dynamic catalog management and waited on until it reports\
    \ ready\nAnd re-provisioning the same org reuses that container instead of colliding on its name\n\
    And moving the org off the isolated lane removes it\nAnd creating an org with isolated_engine where\
    \ no template is configured is refused with 503\n"
  related_reqs:
  - REQ-1043
  - REQ-1067
  - REQ-1244
  - REQ-1412
- id: REQ-1417
  status: complete
  since: 2026-08
  group: 8. Client Access & Protocols
  category: JSON:API
  priority: MUST
  type: behavioral
  description: 'The JSON:API surface speaks physical column and relationship names end to end, in the
    parameters it accepts and the keys it emits alike. include, fields[type], sort and filter[col] all
    name columns and relationships as the SQL plane spells them (global_sql_naming_convention, REQ-471),
    including the dotted rel.column form of include and includeNodes; attributes, the nodes projection
    under a groupBy result, and the keys inside included come back in that same spelling. The GraphQL
    naming convention (gql_naming_convention, camelCase under apollo_graphql) belongs to the GraphQL surface
    alone, so the handler borrows it only for the GraphQL text it synthesizes: names are translated physical
    -> exposed at that emit boundary and translated back on the way out, the rule ?groupBy= (REQ-1361)
    and gRPC''s include (REQ-1408) already follow. The translation authority is the compiler''s own record
    of the rename (ctx.exposed_to_physical, keyed by the table_id a sideloaded relationship reaches through
    ctx.joins), never a second application of the convention. A name that is not a physical column or
    relationship is a 400 naming the offending parameter, not a silently dropped filter or sort. The generated
    foreign-key column selected to resolve a relationship''s linkage is likewise derived as <rel>_id and
    translated, rather than assuming the exposed spelling.'
  use_case: A caller who knows the tables -- or an NL-generated JSON:API URL -- uses one vocabulary across
    SQL, ?groupBy=, gRPC and JSON:API, instead of having to know which surface renamed a column.
  code:
  - provisa/api/jsonapi/naming.py
  - provisa/api/jsonapi/generator.py
  - provisa/nl/runner.py
  tests:
  - tests/unit/test_jsonapi_aggregates.py
  - tests/unit/test_jsonapi_physical_naming.py
  - tests/unit/test_nl_jsonapi_include_roundtrip.py
  - tests/unit/test_nl_aggregation_routing.py
  scenario: "Given a domain whose GraphQL convention renames breed_name to breedName\nWhen a JSON:API\
    \ request asks for fields[pets]=breed_name, sort=-breed_name,\n  filter[breed_name]=Poodle, or include=pet.breed_name\n\
    Then each is accepted and compiled against the exposed GraphQL name\nAnd the response attributes,\
    \ included entries and groupBy nodes are keyed breed_name\nAnd a request naming breedName instead\
    \ is rejected with a 400 citing that parameter\n"
  related_reqs:
  - REQ-257
  - REQ-471
  - REQ-1361
  - REQ-1408
- id: REQ-1418
  status: complete
  since: 2026-08
  group: 13. Multi-Tenancy & Organizations
  category: Multi-Tenancy
  priority: MUST
  type: behavioral
  description: An organization on the external engine lane runs an engine KIND of its own, not only a
    coordinator of its own. orgs.engine_kind names any _ENGINE_BUILDERS key and orgs.engine_url_enc holds
    that engine's DSN encrypted at rest with the same service as api_sources.auth (REQ-686), so one organization
    queries its own Databricks, Snowflake, BigQuery, ClickHouse, Fabric, Synapse or SQLAlchemy store while
    the deployment stays on Trino. NULL means the deployment's kind -- the state of every shared and isolated
    organization, and of an organization that moved only its coordinator (REQ-1412). Which address an
    organization must supply is derived from the chosen kind's ENGINE_REGISTRY config_fields via engine_addressing
    (federation_engine_url = a DSN, federation_engine_host = a host/port pair), never by inspecting the
    value typed; the bundled trino kind, whose coordinator the deployment manages, is endpoint-addressed
    when an organization operates one. build_org_runtime passes the kind to build_engine and records the
    DSN on the OrgRuntime, where configured_engine_url reads it back through the AppState shim ahead of
    $PROVISA_ENGINE_URL and the persisted federation_engine_url, so every URL-addressed backend resolves
    the acting organization's warehouse with no per-backend change. The native tier's single-org guard
    (REQ-1266) admits an organization that holds its own EngineRuntime, since the collision it prevents
    is a SHARED process-wide runtime. The admin surface never returns a stored DSN -- it reports whether
    one is set, and an omitted external_url on write means unchanged.
  use_case: A tenant with an existing Databricks warehouse points its organization at that warehouse and
    keeps its data in its own account, without the deployment changing engine for every other tenant.
  code:
  - provisa/core/schema_admin.py
  - provisa/api/org_runtime.py
  - provisa/api/app.py
  - provisa/federation/engine.py
  - provisa/federation/native_backend.py
  - .claude/commercial/provisa_commercial/org_engine_router.py
  - provisa-ui/src/components/admin/OrgEngineTab.tsx
  tests:
  - tests/unit/test_org_engine_lane.py
  - .claude/commercial/tests/test_org_engine_lane.py
  - provisa-ui/src/__tests__/OrgEngineTab.test.tsx
  scenario: "Given a deployment running the bundled Trino engine\nWhen an organization administrator moves\
    \ the organization to the external lane,\n  chooses the Databricks kind and supplies its warehouse\
    \ URL\nThen the URL is stored encrypted and the organization's runtime is rebuilt on a Databricks\
    \ engine\nAnd that organization's queries resolve its own DSN while every other organization keeps\
    \ Trino\nAnd re-saving without re-entering the URL keeps the stored one\n"
  related_reqs:
  - REQ-1043
  - REQ-1266
  - REQ-1412
  - REQ-1416
- id: REQ-1421
  status: complete
  since: 2026-08
  group: 13. Multi-Tenancy & Organizations
  category: Multi-Tenancy
  priority: MUST
  type: behavioral
  description: 'The engine kinds offered to an organization name the PRODUCT it runs, not the library
    or the process that hosts it. ENGINE_REGISTRY carries one kind per network-addressable relational
    database (_RDB_KINDS: MySQL, MariaDB, Oracle, SQL Server, Db2, Redshift, Greenplum, CockroachDB, YugabyteDB,
    openGauss, TiDB, SingleStore, Vertica, Exasol, Teradata, SAP HANA, SAP ASE, SAP SQL Anywhere, MonetDB,
    Firebird), each on the same self-only land-everything runtime as the generic sqlalchemy kind and differing
    only in the DSN its dialect takes; the sqlalchemy kind remains as the catch-all for a database with
    no key of its own. File-embedded stores (SQLite, Access) are never offered, since a store on someone
    else''s local disk is not reachable from where Provisa runs. The org-engine list additionally drops
    the kinds that run INSIDE the Provisa process even though they carry a federation_engine_url -- embedded
    ClickHouse (chdb), whose URL is a local data directory -- because an organization cannot operate one;
    its external counterpart is the clickhouse-server kind.'
  use_case: An administrator choosing an engine for the organization picks "MySQL" or "Oracle Database"
    from the list, rather than reading a library name and inferring which databases it reaches.
  code:
  - provisa/federation/engine.py
  - .claude/commercial/provisa_commercial/org_engine_router.py
  tests:
  - tests/unit/test_federation_engine.py
  - .claude/commercial/tests/test_org_engine_lane.py
  scenario: 'Given a deployment offering the org-engine kinds

    When an organization administrator opens the external-engine kind list

    Then each network-addressable relational database appears as its own named kind

    And embedded ClickHouse is absent while ClickHouse (Server / Cloud) is present

    And no kind is named after the connection library

    '
  related_reqs:
  - REQ-905
  - REQ-916
  - REQ-1418
- id: REQ-1422
  status: complete
  since: 2026-08
  group: 11. Platform, Infrastructure & Delivery
  category: OpenTelemetry Instrumentation
  priority: MUST
  type: infrastructure
  description: 'The OTel signal store reclaims its own object-store footprint on a schedule, with no operator
    action. A scheduled job runs both `expire_snapshots` AND `remove_orphan_files` on otel.signals.{logs,metrics,traces}
    at the configured `ops_snapshot_retention_hours`. Expiring snapshots only unlinks them; `remove_orphan_files`
    is what deletes the bytes. Trino floors both retentions at 7 days and rejects a shorter threshold
    outright, so the job sets the matching per-catalog session properties (`otel.expire_snapshots_min_retention`,
    `otel.remove_orphan_files_min_retention`) with the statement so the configured value is the one that
    applies. Raw parquet staging is reclaimed by the same guarantee: the compactor lists every date partition
    present and drains oldest-first, so a file whose date segment has rolled over is still compacted and
    deleted rather than stranded forever.'
  use_case: An unattended node keeps running. Without scheduled reclamation the SaaS coordinator reached
    57 GiB of Iceberg metadata behind 93 MiB of data, plus 70586 stranded parquet files (15 GiB), filled
    its boot disk, and every ops view creation and Iceberg commit failed.
  code:
  - provisa/scheduler/jobs.py
  - provisa/api/app_startup.py
  tests:
  - tests/unit/test_otel_trace_pipeline.py
  scenario: 'Given an OTel signal store with snapshots and orphan files older than the configured retention

    When the scheduled reclamation job runs

    Then expire_snapshots and remove_orphan_files both execute for every signal table

    And the configured retention applies rather than Trino''s seven-day floor

    And raw parquet from an earlier date partition is compacted and deleted rather than stranded

    '
  related_reqs:
  - REQ-302
  - REQ-303
- id: REQ-1423
  status: complete
  since: 2026-08
  group: 11. Platform, Infrastructure & Delivery
  category: OpenTelemetry Instrumentation
  priority: MUST
  type: infrastructure
  description: The OTel/results object store is a single set of coordinates every participant resolves
    from the same environment variables, so a deployment can point it at storage that is not the node's
    own disk. The parquet writer (otlp2parquet), the compactor's boto3 client, and the Iceberg catalog
    Trino dials all derive endpoint, bucket, credentials and region from PROVISA_OTEL_S3_ENDPOINT / _BUCKET
    / _S3_ACCESS_KEY / _S3_SECRET_KEY / _S3_REGION. No overlay hardcodes a value for any of them. The
    SaaS deployment sets these to Cloudflare R2, which has no size bound and shares no space with the
    engine's spill directory; the bundled MinIO remains the default for self-hosted compose.
  use_case: Telemetry and query results competed with Trino's spill space on one boot disk, so a large
    table could fail a query by filling the disk telemetry was also writing to. Hardcoding the bundled
    MinIO on the writer meant repointing a deployment moved only the reader, and the two silently addressed
    different stores.
  code:
  - docker-compose.observability.yml
  - docker-compose.app.yml
  - provisa/core/trino_system_catalogs.py
  tests:
  - tests/unit/test_otel_endpoint_not_invented.py
  scenario: 'Given a deployment configured with an external object store

    When otlp2parquet writes parquet and the compactor reads it

    Then both address the same endpoint, bucket, credentials and region

    And no compose overlay supplies a hardcoded object-store address

    '
  related_reqs:
  - REQ-1332
  - REQ-1422
- id: REQ-1424
  status: complete
  since: 2026-08
  group: 3. Source Registration & Data Modeling
  category: Metadata Management
  priority: SHOULD
  type: behavioral
  description: 'The ops domain exposes a tag_usage report: one row per tag in the registry, giving the
    breadth of its application (total assignments, and distinct sources, tables, columns, relationships
    and commands carrying it), its expiry posture (assignments with an expiry date, and how many have
    passed), and the traffic reaching what it marks (distinct statements, distinct users, last read time).
    It is driven from tags_meta rather than the tags table, so the code-defined system tags — which have
    no rows — appear, and a tag nobody has applied appears with zeroes. Assignment counts and usage counts
    are computed as independent aggregates and joined to the tag, never joined to each other in one pass,
    which would multiply each assignment by the statements that read it. Usage counts distinct statements
    over the distinct tagged tables, so a table carrying one tag at table level and on several columns
    still counts a statement once.'
  use_case: An org admin deciding whether a classification scheme is working needs to see which tags are
    actually applied, which are declared but unused, and which mark data that is genuinely being read
    — before extending the scheme or retiring part of it.
  code:
  - provisa/api/_meta_views.py
  - provisa/api/_catalog_descriptions.py
  tests:
  - tests/unit/test_ops_domain.py
  scenario: 'Given tags applied to tables and columns, and audit-logged statements reading those tables

    When an org admin queries ops.tag_usage

    Then every tag in the registry has a row, including unapplied system tags

    And each row reports its assignment breadth and the distinct statements and users reaching it

    And a tag applied to both a table and its columns does not count one statement more than once

    '
  related_reqs:
  - REQ-1373
  - REQ-1386
- id: REQ-1425
  status: complete
  since: 2026-08
  group: 1. Access Governance & Security
  category: Query Observability
  priority: MUST
  type: behavioral
  description: 'Every terminal of the one query pipeline emits an attributed query span, not only the
    ENGINE terminal. The ops queries report selects spans named provisa.query.*, so a statement executed
    on any other terminal was invisible to it: a single-source statement the router pushed down (DIRECT,
    span named direct.execute) and every meta/ops statement served from the admin tenant_db (no span at
    all) executed, audited and returned rows while the report read zero. Both planners now set span_attrs
    on their DIRECT plans as they already did on their ENGINE plans, _run_plan_terminal forwards plan.span_attrs
    through execute_native into execute_direct, and the admin terminal opens its own span. Naming follows
    the engine convention — provisa.query.direct and provisa.query.postgres when the plan carries attributes,
    the previous unattributed names otherwise — so a statement with no acting principal (seeding, rebuilds,
    where begin_audit returns None) still traces but never enters the report.'
  use_case: An operator watching the queries report needs it to account for the traffic the deployment
    actually served, not only the subset that happened to be federated.
  code:
  - provisa/pgwire/_pipeline.py
  - provisa/federation/runtime.py
  - provisa/executor/direct.py
  tests:
  - tests/unit/test_otel_trace_pipeline.py
  scenario: "Given a governed statement a user issues against a single reachable source\nWhen the router\
    \ pushes it down to the DIRECT terminal\nThen the span it emits is named provisa.query.direct and\
    \ carries provisa.table, provisa.domain\n  and provisa.role\nAnd a meta statement served from the\
    \ admin tenant_db emits provisa.query.postgres the same way\nAnd both appear in the ops queries report\
    \ alongside federated statements\n"
  related_reqs:
  - REQ-074
  - REQ-825
  - REQ-1386
- id: REQ-1426
  status: complete
  since: 2026-08
  group: 3. Source Registration & Data Modeling
  category: Schema Registration
  priority: MUST
  type: behavioral
  description: 'No registered column is ever persisted without a data type. The catalog rendered such
    a column as "unknown" and the SQL layer had no type to compile against. Every writer now resolves
    the type it already holds before the write: the GraphQL remote types its native-filter (_nf_*) columns
    from the argument''s resolved type, the gRPC remote keeps ColumnDef.type on both output and input
    columns, the GovData import maps the JDBC type code it already read, the DDN import maps the ObjectType
    field type it previously discarded, and view registration infers output types with SQLGlot''s annotator
    (which types projected expressions - aggregates, concatenations, CASTs - that name-matching could
    never type, and whose plain column references keep the referenced table''s stored spelling verbatim).
    A data type is design-time metadata: inference runs only while a design is being authored - discovery
    at registration, the SQLGlot annotator on a view, a mapper reading a spec - and its result is written
    into Provisa''s own metadata. Nothing inspects a data source for a type afterwards. The config loader
    therefore assigns no types at all; it carries what the YAML declares, and the shipped seed configs
    declare a type for every column. Onboarding closes the remaining gap: the registration form shows
    the discovered type for each column, lets the steward pick from the canonical IR vocabulary for any
    column the source could not describe, and refuses to submit while a selected column is untyped. Hasura
    v2 metadata names columns but carries no types and the converter runs offline against files, so it
    emits null - which makes the emitted YAML an incomplete design the user must finish before it will
    load. An unassigned type fails loudly: view registration returns schema.column_types_unresolved naming
    the columns, and the table repository - the single write path for table_columns - raises rather than
    persisting the null. There is therefore no unknown data type at run time.'
  use_case: An operator browsing the catalog needs every column to state its type, and the compiler needs
    a type for every column it plans against.
  code:
  - provisa/core/repositories/table.py
  - provisa/api/admin/_table_ops.py
  - provisa/api/admin/graphql_remote_router.py
  - provisa/api/admin/grpc_remote_router.py
  - provisa/core/config_loader.py
  - provisa/govdata/schema_import.py
  - provisa/ddn/mapper.py
  tests:
  - tests/unit/test_registered_columns_always_typed.py
  - tests/unit/test_view_column_types.py
  scenario: "Given a table registered through any registrar - source, view, GraphQL remote, gRPC remote,\n\
    \  OpenAPI, GovData, DDN or a YAML config\nWhen its columns are persisted\nThen every column carries\
    \ a data type\nAnd a column whose type cannot be resolved is refused with a message naming it\nAnd\
    \ no table_columns row is ever written with a null data_type\n"
  related_reqs:
  - REQ-038
  - REQ-039
  - REQ-421
- id: REQ-1427
  status: complete
  since: 2026-08
  group: 13. Multi-Tenancy & Organizations
  category: Multi-Tenancy
  priority: MUST
  type: behavioral
  description: 'An isolated org''s sources are provisioned onto its own coordinator even though that coordinator
    is asleep. The org''s engine terminal is deliberately bound with connection kwargs and no open connection,
    so the dedicated cluster can idle-stop between sessions and wake on real traffic. Source registration
    is that traffic: the Trino backend''s catalog lifecycle - create, drop and analyze - reuses a live
    terminal connection when there is one and otherwise opens one from the stored kwargs for the duration
    of the DDL, closing it afterwards and never adopting it as the terminal. Gating the DDL on an open
    connection alone made registration a silent no-op, so an org that moved to an isolated engine had
    its catalogs issued nowhere and every query against it failed with CATALOG_NOT_FOUND while the same
    query succeeded on the shared engine. The Provisa-owned catalogs - provisa_admin, otel and results
    - and the ops views built on them follow the same contract for the same reason: they were registered
    only on the deployment''s shared engine, so an org that moved to its own coordinator kept them on
    the coordinator it had just left and every admin report against otel.signals failed CATALOG_NOT_FOUND.
    They are registered on whichever terminal the org actually runs on, before the ops views that are
    created inside otel.'
  use_case: An org admin who switches the org to a dedicated engine expects its sources to be queryable
    immediately, and the admin reports to keep working, without a manual catalog rebuild or a restart.
  code:
  - provisa/federation/backend.py
  - provisa/api/app.py
  tests:
  - tests/unit/test_isolated_engine_provisioning.py
  scenario: 'Given an org bound to its own federation engine whose coordinator has no open connection

    When the org runtime is built and its sources are registered

    Then the catalog DDL reaches that coordinator on a connection opened from the stored kwargs

    And the connection is closed afterwards rather than becoming the org''s terminal

    And the org''s first query resolves its catalogs instead of failing CATALOG_NOT_FOUND

    And the ops catalogs and views are seeded on that same coordinator

    '
  related_reqs:
  - REQ-1043
  - REQ-1244
  - REQ-1266
  - REQ-1332
- id: REQ-1428
  status: complete
  since: 2026-08
  group: 11. Platform, Infrastructure & Delivery
  category: OpenTelemetry Instrumentation
  priority: MUST
  type: behavioral
  description: 'No OTel signal''s compaction backlog may starve another''s. The scheduled compaction job
    drains logs, metrics and traces from the object store into Iceberg, each signal with its own per-run
    file budget. Draining them one after another in a fixed order made the budget meaningless: on the
    SaaS node a single run spent minutes on logs and metrics and was killed - by a restart or the node''s
    idle-stop - before traces, last in the order, ever got a turn. Trace rows stopped landing in Iceberg
    entirely, so the ops queries report read an empty table while fifteen thousand trace files accumulated
    unconsumed. The three signals are therefore compacted concurrently, and a failure in one still lets
    the others complete before the error surfaces. Fairness alone does not make the lane sustainable:
    a run''s throughput is set by how fast it reads the object store and how many rows it commits. Each
    chunk''s files are therefore fetched concurrently rather than one round trip at a time, and only telemetry
    an ops view reads enters the lane at all - the collector drops spans from other services and from
    asyncpg auto-instrumentation, the control plane''s own per-statement database chatter, which measured
    1080 of every 1134 spans arriving. Those spans remain visible to live trace inspection; they just
    do not become Iceberg rows. The batch a run commits is likewise sized to the work rather than to a
    limit that no longer exists: an Iceberg commit costs seconds whatever it carries, so the shipped ten-row
    default - the ceiling the Trino client''s parameterised-statement header once imposed, left behind
    when the inserts began inlining their literals - held the drain rate a hundredfold below what the
    job could sustain. The collector''s filter protects only objects written after it shipped; the same
    predicate therefore applies again at compaction, on rows already read. Twenty-one thousand trace objects
    predating the filter were still in the bucket, asyncpg statement spans making up most of their rows,
    and compaction walks oldest-date-first - so every one of those rows would have been committed to Iceberg,
    seconds per commit, before the job reached the provisa.query spans the ops report reads. Dropping
    them in memory costs one filter; the objects are deleted from the bucket either way.'
  use_case: An operator opens admin / reports / queries and sees the queries that just ran, rather than
    an empty table caused by one signal's backlog monopolising every compaction run.
  code:
  - provisa/scheduler/jobs.py
  - provisa/core/models.py
  - observability/otel-collector-config.yaml
  - scripts/deploy-cloud.sh
  tests:
  - tests/unit/test_otel_compaction_fairness.py
  - tests/unit/test_otel_endpoint_not_invented.py
  - tests/unit/test_otel_trace_pipeline.py
  scenario: 'Given trace, log and metric files are all waiting in the object store

    And draining the logs alone exceeds the time the process survives

    When the compaction job runs

    Then traces are compacted in the same run rather than waiting for logs to finish

    And the ops queries report returns the spans of queries that have executed'
  related_reqs:
  - REQ-302
  - REQ-303
  - REQ-1425
- id: REQ-1429
  status: complete
  since: 2026-08
  group: 11. Platform, Infrastructure & Delivery
  category: Query Engine Federation
  priority: MUST
  type: behavioral
  description: 'Rebuilding one org''s runtime may not take a system catalog away from the orgs sharing
    the coordinator. Provisa registers its own catalogs dynamically and refreshes one by dropping it first,
    because Trino exposes no way to read a live catalog''s properties back. That is right at boot, when
    the coordinator serves nobody yet. It is wrong on every later per-org rebuild: `otel` and `results`
    are deployment-scoped - their specs take no org id and are identical for every org - so re-registering
    them on behalf of one org drops a catalog the others are querying, and the drop races the create.
    Changing an org''s engine on the SaaS node did exactly that: the rebuild dropped `otel`, its own CREATE
    lost the race and failed CATALOG_NOT_FOUND, the whole rebuild aborted, and the shared coordinator
    was left with no `otel` at all - the ops reports and the org-engine tab then failed for every org
    on it. A per-org rebuild therefore ENSURES the system catalogs rather than re-registering them: it
    creates only what the coordinator is missing. `provisa_admin` is the exception and is still refreshed,
    because its JDBC URL names the org''s own schema and a stale one points the control plane at another
    org.'
  use_case: An org administrator switches the org's query engine and the other orgs on the shared coordinator
    keep working, rather than losing the telemetry catalog their reports read.
  code:
  - provisa/core/trino_system_catalogs.py
  - provisa/federation/backend.py
  tests:
  - tests/unit/test_trino_system_catalogs.py
  scenario: 'Given a shared coordinator that already has the provisa_admin, otel and results catalogs

    When one org''s runtime is rebuilt

    Then only provisa_admin is re-registered

    And otel and results are left in place for the orgs querying them'
  related_reqs:
  - REQ-1332
  - REQ-1412
  - REQ-1427
- id: REQ-1430
  status: complete
  since: 2026-08
  group: 2. Authentication & Identity
  category: Authentication & Identity
  priority: MUST
  type: behavioral
  description: An identity bootstrap that has not finished is not a denial. CapabilityGate rendered its
    NotAuthorized fallback whenever the unioned capabilities were empty, which includes every render before
    /auth/me and the roles query answer. Against a cold coordinator that is tens of seconds, so a signed-in
    admin opening an admin page was told "You do not have permission to view this page" (with "No roles
    configured" in the header) and then watched the real page appear on its own - indistinguishable from
    a spontaneous reload. The gate, the /admin entry resolver, and the role selector therefore consult
    the auth context's `loading` flag first and say the credentials are being checked; the denial and
    "No roles configured" are stated only once the bootstrap has settled.
  use_case: A signed-in administrator opens an admin page while the identity fetch is still in flight
    and sees that their credentials are being checked, rather than a permission denial that reverses itself.
  code:
  - provisa-ui/src/components/CapabilityGate.tsx
  - provisa-ui/src/components/CredentialCheck.tsx
  - provisa-ui/src/components/RoleSelector.tsx
  - provisa-ui/src/App.tsx
  tests:
  - provisa-ui/src/__tests__/CapabilityGate.test.tsx
  - provisa-ui/src/__tests__/RoleSelector.test.tsx
  scenario: 'Given a signed-in user whose identity bootstrap has not yet returned

    When they open a capability-gated page

    Then the page says the credentials are being checked

    And the permission denial is shown only after the bootstrap settles without the capability'
  related_reqs:
  - REQ-1291
  - REQ-1295
  - REQ-1349
- id: REQ-1431
  status: complete
  since: 2026-08
  group: 5. Query Languages, Compilation & Operations
  category: Operational Views
  priority: SHOULD
  type: behavioral
  description: 'The rule between the report list and the report on the admin Reports tab is a grab handle:
    dragging it sets the list width, clamped between 160px and 560px so neither pane can be dragged away.
    Report names and descriptions are long enough that a fixed 240px list truncated them while the report
    pane had room to spare.'
  use_case: An administrator widens the report list to read full report descriptions, then narrows it
    again to give the report grid more room.
  code:
  - provisa-ui/src/components/admin/ReportsTab.tsx
  tests:
  - provisa-ui/src/components/admin/__tests__/ReportsTab.resize.test.tsx
  scenario: 'Given the admin Reports tab

    When the rule between the list and the report is dragged

    Then the report list resizes with the pointer, clamped to its minimum and maximum width'
  related_reqs:
  - REQ-1386
- id: REQ-1432
  status: complete
  since: 2026-08
  group: 11. Platform, Infrastructure & Delivery
  category: OpenTelemetry Instrumentation
  priority: SHOULD
  type: behavioral
  description: 'Tracing is enabled per subsystem, under observability.subsystem_traces. The switches are
    named for what each subsystem is in this product - HTTP API, outbound HTTP calls, catalog database,
    result cache, document sources, search sources, gRPC services - not for the instrumentation library
    behind it. The catalog database is off by default: every catalog read and metadata write is an asyncpg
    call, and instrumenting them buries the query spans the live trace panel exists to show. Each switch
    gates one instrumentor inside setup_otel, since an instrumentor patches its driver globally and cannot
    be undone later. An unknown subsystem name is rejected rather than written to the config file. The
    admin Observability tab carries the same switches.'
  use_case: An operator opens the live trace panel, sees it filled with driver-level Postgres spans, and
    turns off the subsystems they do not care about from the Observability tab.
  code:
  - provisa/core/models.py
  - provisa/api/otel_setup.py
  - provisa/api/admin/settings_router.py
  - config/provisa.yaml
  - provisa-ui/src/api/admin.ts
  - provisa-ui/src/components/admin/ObservabilityTab.tsx
  tests:
  - tests/unit/test_otel_subsystem_traces.py
  - provisa-ui/src/components/admin/__tests__/ObservabilityTab.subsystems.test.tsx
  scenario: 'Given a deployment with no subsystem_traces stated in its config

    When the API starts

    Then the catalog database emits no spans and every other subsystem does'
  related_reqs:
  - REQ-302
  - REQ-303
  - REQ-545
- id: REQ-1433
  status: complete
  since: 2026-08
  group: 11. Platform, Infrastructure & Delivery
  category: Infrastructure
  priority: SHOULD
  type: behavioral
  description: Restarting the query engine from the admin console speaks the Docker Engine API over the
    bind-mounted socket rather than shelling out to a docker CLI, which the app image does not carry.
    The container name comes from the request, else QUERY_ENGINE_CONTAINER, else discovery against the
    containers docker reports - matching the engine name as a whole name component, so compose's compose-trino-1
    is found where the bare engine name was not. Two matches are refused rather than guessed, and an unmounted
    socket is reported as such.
  use_case: A platform admin presses Restart Engine on a compose deployment and the engine restarts, instead
    of the request failing with "docker not found on PATH".
  code:
  - provisa/api/admin/settings_router.py
  tests:
  - tests/unit/test_query_engine_restart.py
  scenario: 'Given the docker socket is mounted and the engine runs as compose-trino-1

    When a platform admin restarts the query engine

    Then the container compose-trino-1 is restarted through the socket'
  related_reqs:
  - REQ-1337
  - REQ-1416
- id: REQ-1434
  status: complete
  since: 2026-08
  group: 10. UI & Admin Surfaces
  category: UI
  priority: MUST
  type: behavioral
  description: The bearer token is asked for at request time rather than read from the copy mirrored into
    localStorage on the last rotation. window.fetch, Apollo's HttpLink fetch, and the cross-subdomain
    auth relay all resolve currentBearer(), which asks a live Firebase session for its current token -
    the SDK re-mints an expired one - and writes it back to storage; deploys with no Firebase session
    (basic auth, an org subdomain holding a borrowed copy) keep storage as their only source. The Swagger
    UI explorer receives its light/dark theme stamped into the server-rendered HTML because a srcdoc frame
    has no location.search to read it from, and the CodeMirror panels on the graph and SQL explorers resolve
    the computed color scheme instead of rendering a fixed palette. Every save and cancel control shows
    a text label beside its icon rather than being icon-only, the observability subsystem panel offers
    a Cancel that restores the last server-acknowledged values, and each Platform Settings control carries
    inline descriptive help.
  use_case: A user leaves a query tab idle past the token lifetime, returns, and runs a GraphQL query
    - it succeeds instead of answering "Invalid or expired token".
  code:
  - provisa-ui/src/lib/firebase.ts
  - provisa-ui/src/lib/authFetch.ts
  - provisa-ui/src/apolloClient.ts
  - provisa-ui/src/authRelay.ts
  - provisa-ui/src/components/admin/ObservabilityTab.tsx
  - provisa-ui/src/pages/AdminPage.tsx
  - provisa/api/rest/openapi_spec.py
  tests:
  - provisa-ui/src/__tests__/requestTimeToken.test.ts
  - provisa-ui/src/__tests__/crossSubdomainAuth.test.ts
  scenario: 'Given the token mirrored in localStorage has expired

    When the page issues an authenticated request

    Then Firebase mints a current token and that token is sent'
  related_reqs:
  - REQ-1266
  - REQ-1318
  - REQ-1317
- id: REQ-1435
  status: complete
  since: 2026-08
  group: 1. Access Governance & Security
  category: Observability
  priority: MUST
  type: behavioral
  description: OTel instant columns are timestamps end to end, not epoch-nanosecond integers. The otlp2sql
    writer casts an int64 nanosecond field to timestamp by way of timestamp('ns') before narrowing to
    timestamp('us') - a direct cast to microseconds reads the nanos as micros and lands in the year 57000
    - and the compactor refuses to append into a table whose physical schema still carries the integer
    column, naming the drop that lets it recreate the table at the current schema rather than silently
    writing the wrong type.
  use_case: An operator opens the traces report and reads a start time, instead of a 19-digit number.
  code:
  - provisa/observability/otlp2sql.py
  - provisa/observability/ops_schema.py
  - provisa/compiler/type_map.py
  - provisa/scheduler/jobs.py
  tests:
  - tests/unit/test_otlp2sql.py
  - tests/unit/test_otel_trace_pipeline.py
  scenario: 'Given an OTel parquet file whose start_time is int64 nanoseconds

    When the compaction job writes it into otel.signals.traces

    Then the column is TIMESTAMP(6) holding the same instant'
  related_reqs:
  - REQ-1386
- id: REQ-1436
  status: complete
  since: 2026-08
  group: 10. UI & Admin Surfaces
  category: UI
  priority: MUST
  type: behavioral
  description: A filter that matches nothing leaves the grid standing. The governed viewer swaps in bare
    text only for a relation with no projection at all; an empty PAGE keeps the header row, and with it
    the filter inputs, so the filter that emptied the result can still be cleared. The grid renders a
    single full-width empty row under the headers in place of the missing data.
  use_case: An admin types a filter into a report, gets no rows, and clears the filter - instead of being
    stranded on a page with no controls.
  code:
  - provisa-ui/src/components/GovernedTableViewer.tsx
  - provisa-ui/src/pages/sql/ResultsGrid.tsx
  tests:
  - provisa-ui/src/pages/sql/__tests__/ResultsGrid.filter.test.tsx
  scenario: 'Given a report filter that matches no rows

    When the page renders the empty result

    Then the column headers and their filter inputs are still on screen'
  related_reqs:
  - REQ-1386
- id: REQ-1437
  status: complete
  since: 2026-08
  group: 10. UI & Admin Surfaces
  category: UI
  priority: SHOULD
  type: behavioral
  description: A grid cell renders an instant for a reader rather than for a parser - "2026-08-12 12:01:59",
    with no T separator, no fractional seconds and no zone suffix. A value carrying a zone is shown on
    the viewer's own clock, because that is the clock the reader compares it against; a value carrying
    none is already the reader's wall time and is shown unchanged rather than shifted by an offset it
    never had. Anything that is not an instant passes through untouched.
  use_case: An admin scans logged_at down an ops report and reads the times at a glance.
  code:
  - provisa-ui/src/pages/sql/formatCell.ts
  - provisa-ui/src/pages/sql/ResultsGrid.tsx
  tests:
  - provisa-ui/src/pages/sql/__tests__/formatCell.test.ts
  scenario: 'Given a cell holding 2026-08-12T12:01:59.174658Z

    When the grid renders it

    Then it reads 2026-08-12 12:01:59 in the viewer''s own time zone'
  related_reqs:
  - REQ-1435
  - REQ-1386
- id: REQ-1438
  status: complete
  since: 2026-08
  group: 10. UI & Admin Surfaces
  category: UI
  priority: SHOULD
  type: behavioral
  description: The reader chooses how many rows a page holds - 25, 50, 100, 250, 500 or 1000 - from a
    control in the results toolbar, offered whether or not the current result spills past one page. The
    choice is retained per report alongside the other grid choices and restored on the next visit. Resizing
    returns the reader to the first page, since the page numbers now point elsewhere in the relation,
    and the governed viewer treats the size as part of its query key because the LIMIT is pushed into
    SQL - a resize refetches rather than re-slicing.
  use_case: An admin works a report 500 rows at a time and finds it still at 500 on the next visit.
  code:
  - provisa-ui/src/pages/sql/useResultsGrid.ts
  - provisa-ui/src/pages/sql/ResultsGrid.tsx
  - provisa-ui/src/pages/sql/types.ts
  - provisa-ui/src/components/GovernedTableViewer.tsx
  tests:
  - provisa-ui/src/pages/sql/__tests__/useResultsGrid.test.tsx
  scenario: 'Given a report shown 100 rows a page

    When the reader selects 25 and revisits the report later

    Then the report opens at 25 rows a page, on the first page'
  related_reqs:
  - REQ-1386
- id: REQ-1439
  status: complete
  since: 2026-08
  group: 1. Access Governance & Security
  category: Governance
  priority: SHOULD
  type: behavioral
  description: Every ops report that carries a user id also carries that person's name. The directory
    that maps an IdP subject to a name lives in the platform control plane, a separate engine from the
    tenant plane the report views are built in, so the auth middleware writes a tenant-local user_directory
    mirror on the same sign-in that upserts the platform profile. query_audit_log_ops, deprecated_usage,
    pii_access and policy_denials each LEFT JOIN that mirror and project display_name as user_name; a
    principal with no directory row still appears, with an empty name. Aggregate-only reports, which count
    distinct users rather than naming them, are unchanged.
  use_case: An admin reads the policy denials report and sees who was refused, by name.
  code:
  - provisa/api/_meta_views.py
  - provisa/auth/middleware.py
  - provisa/core/schema.sql
  - provisa/core/schema_org.py
  - provisa/api/_catalog_descriptions.py
  tests:
  - tests/unit/test_ops_domain.py
  scenario: 'Given an audit row logged for a user who has signed in

    When the pii_access report is read

    Then the row carries that user''s display name beside the user id'
  related_reqs:
  - REQ-1386
  - REQ-1266
- id: REQ-1440
  status: complete
  since: 2026-08
  group: 10. UI & Admin Surfaces
  category: UI
  priority: SHOULD
  type: behavioral
  description: 'Any grid of results - a report, a table or view preview, a workbench result - exports
    to an Excel workbook alongside the CSV. Each column is written with the type it actually holds rather
    than as text: a column whose every non-null value is a number is written as numbers, a column whose
    every non-null value parses as an instant is written as dates carrying the same wall clock the grid
    displays, formatted yyyy-mm-dd hh:mm:ss or yyyy-mm-dd when the value has no time part. A column that
    disagrees with itself is written as text, because writing part of it as a date would silently reinterpret
    the rest. A null cell is left empty rather than filled with the word null.'
  use_case: An analyst opens an exported report in Excel and sorts by date and sums a measure without
    first re-typing either column.
  code:
  - provisa-ui/src/pages/sql/exportXlsx.ts
  - provisa-ui/src/pages/sql/formatCell.ts
  - provisa-ui/src/pages/sql/ResultsGrid.tsx
  tests:
  - provisa-ui/src/pages/sql/__tests__/exportXlsx.test.ts
  scenario: 'Given a report holding a numeric column and a timestamp column

    When the reader downloads it as XLSX

    Then the numeric cells are numbers and the timestamp cells are formatted dates'
  related_reqs:
  - REQ-1386
  - REQ-1441
- id: REQ-1441
  status: complete
  since: 2026-08
  group: 10. UI & Admin Surfaces
  category: UI
  priority: SHOULD
  type: behavioral
  description: 'An exported workbook carries a second sheet recording how the first was produced. For
    a report or a table/view preview that is the relation and the name it is registered under, its source,
    description, API endpoint and view definition where those exist, the parameter values supplied, and
    the governed statement that actually fetched the rows; for a workbench result it is the statement
    in the editor. To those the grid adds the facts of this particular export: the export time in UTC,
    whether the sheet holds the whole result or one page of a server-paged relation and which page at
    what size, the row count, and the reader''s own column filters, sort and grouping. The sheet closes
    with the type every column was written as, so a reader opening the file later can tell a page from
    a whole relation and knows what shaped it.'
  use_case: A reviewer receives an exported report months later and reads off the exact statement, page
    and filters that produced it.
  code:
  - provisa-ui/src/pages/sql/exportXlsx.ts
  - provisa-ui/src/pages/sql/ResultsGrid.tsx
  - provisa-ui/src/components/GovernedTableViewer.tsx
  - provisa-ui/src/pages/sql/ResultsPanel.tsx
  tests:
  - provisa-ui/src/pages/sql/__tests__/exportXlsx.test.ts
  scenario: 'Given a server-paged report narrowed by a column filter

    When the reader downloads it as XLSX

    Then the provenance sheet names the statement, the page and page size, and the filter applied'
  related_reqs:
  - REQ-1386
  - REQ-1440
- id: REQ-1442
  status: complete
  since: 2026-08
  group: 10. UI & Admin Surfaces
  category: UI
  priority: SHOULD
  type: behavioral
  description: The results toolbar carries a clear-filters control on every grid, disabled while no column
    filter is set. Clearing drops every column filter at once and returns the reader to the first page.
    A reader who narrowed several columns until the grid emptied has one control to undo it rather than
    one per column.
  use_case: An admin filters four columns of a report down to no rows and restores the full result in
    one click.
  code:
  - provisa-ui/src/pages/sql/useResultsGrid.ts
  - provisa-ui/src/pages/sql/ResultsGrid.tsx
  tests:
  - provisa-ui/src/pages/sql/__tests__/ResultsGrid.clearFilters.test.tsx
  scenario: 'Given a grid with two column filters that match no rows

    When the reader clicks clear filters

    Then every filter is dropped, the grid shows the first page, and the control is disabled again'
  related_reqs:
  - REQ-1386
  - REQ-1436
- id: REQ-1443
  status: complete
  since: 2026-08
  target: 2026-Q4
  group: 4. Source Connectors
  category: Data Quality Sources
  priority: MAY
  type: behavioral
  description: 'An external data-quality checker (Soda first; the shape generalizes to GX Core and dbt
    tests) registers as a SOURCE TYPE, not as a gate, a subsystem, or a bundled library. Its scan output
    is DATA — a check result is an observation, not a verdict — so it lands through the ordinary source
    path (make_source_land''s fetch contract, REQ-982 shape resolution) and every downstream capability
    is inherited rather than built: cadence, freshness, events, lineage, governance, RLS, grid, export.
    This is REQ-967''s self-describing estate extended to a third-party checker, and it is deliberately
    NOT the REQ-941/REQ-1165 gate path — because a landed observation carries no REQ-964 determinism obligation,
    Soda''s non-deterministic checks (anomaly score, trailing-window change, freshness-vs-now) are admissible
    where they could never sit on a preflight verdict. Enforcement, if wanted, is a SEPARATE and later
    declaration: a preflight or MV over the landed results emitting warn/error/quarantine. Four registration
    decisions follow. (1) The checker is an EXTERNAL PREREQ in the oracledb posture — capabilities.yaml
    provisioning: pip_driver, installed on operator selection, invoked out-of-process over the REQ-940
    shell/HTTP adapter. Provisa vendors and links NOTHING, so a source-available checker licence (soda-core
    is Elastic 2.0) never reaches the distributed artifact. (2) The RESULTS SCHEMA IS FIXED AND SHIPPED,
    never hand-declared per table — unlike a Kafka topic, whose payload only the operator knows, the scan
    envelope is defined by the checker (scan timestamp, dataset, check, definition, outcome in pass/fail/warn/error,
    value). The per-check-type diagnostics block (a schema check''s missing_column_names, a freshness
    check''s max_column_timestamp) is one jsonb column, and REQ-119 promotions are the existing mechanism
    to surface a chosen field as a typed generated column. The checker''s major version is PINNED at registration,
    since v3 SodaCL scans and v4 contract verification differ in result shape. (3) LINEAGE IS DERIVED
    FROM THE CONTRACT, never hand-declared: the contract YAML already names its target dataset, so the
    registration parses it the way extract_inputs parses SQL (REQ-939) and resolves it to the governed
    table — one definition, no second copy that can drift, and a contract naming an ungoverned dataset
    fails loud at registration. Registration therefore carries a contract body plus timing, not an input
    ref. (4) ONE RESULTS NODE PER CONTRACT, never one shared results table for the estate: a single node
    fed by every governed table has a fan-in edge from everything, so blast radius is wrong, vintage is
    meaningless, and a churning table cannot debounce differently from a nightly one. The estate-wide
    scorecard is an MV unioning the per-contract nodes, which is REQ-967''s shape regardless. Timing introduces
    NO new fields: change_signal + cache_ttl give the poll cadence, mv_debounce_quiet/mv_debounce_max_delay
    collapse an upstream burst into one scan (REQ-963), a calendar grain makes it periodic (REQ-962),
    and expected_events holds the scan until its inputs are fresh through the window (REQ-961). probe_type
    watermark on the scan timestamp makes the landing an append, so scan history accumulates with no history
    machinery. Aiming the checker at Provisa''s own pgwire endpoint checks the FEDERATED view rather than
    one source at a time — which the checker cannot do for itself — and because policy applies to that
    connection the scan identity is declared explicitly, so a filtered row set can never produce a silently
    passing check. The checker MUST push its work DOWN to the engine, and Soda does: every aggregate metric
    in a contract compiles into ONE `WITH ... SELECT <aggregate exprs> FROM cte` per dataset, split only
    when the SQL exceeds the dialect''s max statement length, returning a single row of scalars (contract_verification_impl.AggregationQuery.build_sql/execute);
    failed-row output is bounded by sampler_limit through sql_dialect.apply_sampling, which is SQLGlot-rewritten
    the way Provisa''s own compiler rewrites SQL. Nothing streams the dataset into Python, so scan cost
    is a warehouse aggregate scan, not an extract — which is what makes aiming a checker at the FEDERATED
    view viable at estate scale. A checker that only validates dataframes in process (pandera, Deequ without
    Spark) does NOT satisfy this clause and is out of scope.

    (5) THE CHECKER IS PLUGGABLE, AND THE LICENCE TIER IS PART OF THE CHOICE. The registration declares
    which checker it targets; the source shape, the results schema, and the pgwire target are identical
    across them, and only the contract dialect differs. Two are in scope. Soda Core (soda-postgres, Elastic
    License 2.0) — contract YAML, richest check vocabulary. Great Expectations (great-expectations[postgresql],
    Apache 2.0) — expectation suites, no hosted-service bar. dbt tests and Elementary (Apache 2.0) are
    a third family that fits the same landing shape but presumes a dbt project, so they are noted and
    deferred. Commercial monitors (Monte Carlo, Anomalo, Bigeye) invert the shape — there is no local
    checker at all, only a results REST API to poll — which is the SAME fetch contract with a different
    body and is therefore admissible later without a new node kind.

    (6) PROVISIONING IS OPERATOR-SELECTED AND THE CLOUD PLANE IS EXCLUDED BY LICENCE, NOT BY CAPABILITY.
    Each checker is a pyproject extra ([soda], [gx]) acquired from PyPI on selection, never vendored.
    install.sh prompts for one (none|soda|gx, default none) and writes dq_checker to ~/.provisa/config.yaml;
    scripts/provisa maps that to the PROVISA_EXTRAS docker build arg, whose DEFAULT (firebase,vector)
    carries no checker at all. capabilities.yaml gains a cloud_eligible field: soda is cloud_eligible:
    false because Elastic License 2.0 prohibits providing the software to third parties as a hosted or
    managed service, and running it inside the SaaS plane on behalf of tenants is exactly that. The hosted
    plane MUST therefore either omit the source or reach an operator-supplied Soda endpoint the operator
    runs themselves; great_expectations carries no such bar and is cloud-eligible. Nothing else in the
    codebase enforces this split, so the flag is the enforcement point and the cloud build must read it.

    (7) THE CONTRACT IS AUTHORED IN THE UI, as part of the table definition rather than as a file the
    operator maintains beside it. The table edit surface gains a data-quality panel that builds the contract
    from the columns Provisa already knows: a check picker scoped to each column''s actual type (missing/invalid/duplicate/range
    on a numeric, freshness on the watermark column, schema on the table), a threshold editor, and a warn-vs-fail
    severity per check. The CONTRACT TEXT IS THE DEFINITION and the panel''s rows are a READ of it, so
    an org with existing contracts pastes one in and the builder renders it, and an edit to a row is a
    rewrite of that text rather than a second authored copy that can drift. SERIALIZATION IS SERVER-SIDE,
    so the dialect has exactly one implementation and the browser cannot emit a shape the checker refuses.
    A ROW CARRIES THE CHECK''S ARGUMENTS ALONE: the dialect''s envelope (Soda''s single key naming the
    check type, GX''s type/kwargs pair) is restated by the serializer on the one path that writes contract
    text, because the envelope repeats the type the row already names and an operator cannot change it.
    The keys GX''s own serializer writes beside type/kwargs (id, meta, notes) are not editable and are
    carried verbatim rather than dropped, so a pasted suite round-trips unchanged. A consumer reading
    a published assertion wants the check as the checker runs it, so the export joins the arguments and
    the envelope back together (clause 9). A dry-run action executes the contract against the live table
    and shows the outcomes without landing them, since the failure mode this prevents is a contract that
    lands nothing but passing rows because its dataset name resolved somewhere else.

    (8) THE FEATURE IS NOT DONE UNTIL THE SURROUNDING MATERIAL MOVES WITH IT. In scope, not follow-up:
    docs/sources.md gains the checker source type and the licence split; docs/integrations.md and docs/configuration.md
    cover dq_checker and PROVISA_EXTRAS; the installer wizard (install.sh and the macOS SwiftUI parity
    path) offers the choice; the demo config registers one contract over a demo table so the scorecard
    is populated on a fresh install; provisa-ui/src/tour/tourSteps.ts gains a step for the quality panel
    with copy in every locale under i18n/locales/<lng>/tour.json (REQ-1358); and capabilities.yaml carries
    both rows. A shipped checker source with no demo data and no tour step is an unfinished feature, not
    a shipped one.

    (9) A RESULTS TABLE IS IDENTIFIABLE, AND ITS CHECKS PUBLISH ON THE ASSET THEY OBSERVE. The marker
    is a DERIVED tag — computed from the table''s own registration (a checker source plus a registered
    contract), never assigned and never stored — joining fact and dimension, which are derived the same
    way from modeling_role. Derived tags are code-defined intrinsics like the REQ-1373 system tags: synthesized
    in front of the tags table, refused by upsert, delete and assign, absent from the picker, and exposed
    alongside the assignments as a derived_tags view. A stored assignment would either duplicate the registration
    or contradict it. In the metadata export the results table publishes as an ORDINARY table carrying
    that tag — no special casing — while each check in its contract publishes as an assertion on the asset
    the checker OBSERVES (the governed table, or the column when the check names one), because that is
    where a consumer asks whether a column is checked; the assertion names the results table so the two
    halves stay navigable both ways. An assertion is emitted only when BOTH ends survive the Data Product
    filter, since a dangling reference is what that filter exists to prevent. The published definition
    is the check''s own authored text in the checker''s dialect, and the severity is the dialect''s: Soda''s
    threshold.level (defaulting to fail as Soda itself defaults it), while every Great Expectations expectation
    is fail because GX has no warn level to report. Adapters map it natively where a native concept exists
    — OpenMetadata TestDefinition/TestSuite/TestCase with the checker named as the test platform, DataHub
    assertion entities with a content-derived URN so a republished contract addresses the same entity
    — and where one does not, the checks ride the same Provisa-authored carrier the governance facts already
    ride: the Atlas governance document (inherited by Atlan, whose own DQ routes are unverified) and a
    Collibra attribute rather than rows in a steward-curated rulebook. EVERY assertion carries the LAST
    SCAN''S OUTCOME, because a results table is that scan''s rows: the publish path reads each registered
    contract''s results table at its maximum scan_time through the one governed query pipeline and joins
    the rows back to the contract on (target table, column, check type) — the same address the loader
    handed the scan. Checks that share that identity are logged and published with no outcome rather than
    handed a sibling''s verdict, and a contract no scan has reached yet publishes the explicit state never_run,
    since an unrun check must never read as a clean one. warn is published as a failure, not a pass: the
    threshold WAS breached and only the severity is milder. The outcome reaches each catalog by its own
    native run surface where one exists — an OpenMetadata testCaseResult, a DataHub assertionRunEvent,
    and the OpenLineage spec facet dataQualityAssertions with a real success boolean — and rides the Provisa-authored
    carrier everywhere else. Only states that reached a verdict appear on the native surfaces, which can
    express nothing else; never_run, error and skipped are always present in the Provisa carrier.'
  use_case: An org already running Soda points it at a Provisa-governed table, registers the contract
    as a source, and gets scan history, a quality scorecard, lineage from check back to table, and change-driven
    rather than cron-driven scans — without Provisa shipping a data-quality engine or the operator hand-declaring
    a results schema.
  code:
  - provisa/core/models.py
  - provisa/core/config_loader.py
  - provisa/events/handlers.py
  - provisa/processors/shell.py
  - config/capabilities.yaml
  - pyproject.toml
  - Dockerfile
  - docker-compose.app.yml
  - install.sh
  - scripts/provisa
  - provisa-ui/src/pages/tables
  - provisa-ui/src/tour/tourSteps.ts
  - docs/sources.md
  - provisa/core/derived_tags.py
  - provisa/core/repositories/tag.py
  - provisa/api/admin/schema_mutation.py
  - provisa/api/metadata_export/builder.py
  - provisa/api/metadata_export/model.py
  - provisa/api/metadata_export/openmetadata.py
  - provisa/api/metadata_export/datahub.py
  - provisa/api/metadata_export/collibra.py
  - provisa/api/metadata_export/atlas.py
  - provisa/api/metadata_export/openlineage.py
  - provisa/dq/contract.py
  - provisa/api/metadata_export/dq_outcomes.py
  - provisa/api/metadata_export/publishing.py
  tests:
  - tests/unit/test_metadata_export_data_quality.py
  - tests/unit/test_metadata_export_dq_outcomes.py
  - tests/unit/test_dq_contract.py
  scenario: 'Given a Soda contract whose checks name a governed table

    When the contract is registered as a soda source with a debounce window

    Then lineage resolves from the contract to that table, a burst of upstream changes collapses

    into one scan, and the results append to a shipped fixed-schema table keyed by scan timestamp

    Given a contract naming a dataset Provisa does not govern

    Then registration fails loud rather than landing unattributable results

    Given a landed scan result whose outcome is fail

    Then nothing halts by itself, and enforcement exists only if a preflight or MV over the results

    is separately declared

    Given an operator who selects soda at install

    Then dq_checker: soda is written to config.yaml and the image builds with PROVISA_EXTRAS

    including the soda extra

    Given the hosted cloud plane

    Then the soda option is refused because capabilities.yaml marks it cloud_eligible: false, while

    great_expectations remains offered

    Given a governed table open in the UI

    When a check is added in the data-quality panel and dry-run

    Then the generated contract text is visible and editable, and the outcomes display without any

    result rows landing

    Given a results table registered under a checker source

    Then it carries the derived data_quality tag, which no upsert, delete or assign can change

    Given a metadata export of that table and the table its contract observes

    Then the results table publishes as an ordinary table carrying that tag, while each check

    publishes as an assertion on the observed table or column, naming the results table and the

    check''s own severity

    Given an observed table excluded from the Data Product filter

    Then no assertion publishes for it, because the reference would dangle'
  related_reqs:
  - REQ-967
  - REQ-939
  - REQ-940
  - REQ-941
  - REQ-1165
  - REQ-982
  - REQ-119
  - REQ-963
  - REQ-1358
  - REQ-972
- id: REQ-1444
  status: complete
  since: 2026-08
  group: 10. UI & Admin Surfaces
  category: UI
  priority: SHOULD
  type: behavioral
  description: 'A file export — XLSX or CSV — is the whole relation under the reader''s current filter,
    sort and group choices, never the page on screen. A server-paged grid (REQ-1386) holds one page at
    a time, so the export reads the rest itself: the same governed statement the page came from, re-issued
    one chunk at a time with an advancing OFFSET until a chunk comes back short. Both formats take the
    same rows, and both are named after the relation. The XLSX provenance sheet records the scope as every
    matching row, the exported row count, and the statement the export issued. A chunk that fails aborts
    the export and surfaces the error — a short file is never written, because a file that silently stops
    at the page boundary is indistinguishable from a complete one once it leaves the product. The export
    therefore belongs to the grid component rather than the grid-state hook, which by construction sees
    only the page.'
  use_case: An analyst filters a million-row report to a few thousand rows and exports them to Excel,
    getting every matching row rather than the twenty-five on screen.
  code:
  - provisa-ui/src/pages/sql/ResultsGrid.tsx
  - provisa-ui/src/pages/sql/useResultsGrid.ts
  - provisa-ui/src/components/GovernedTableViewer.tsx
  tests:
  - provisa-ui/src/pages/sql/__tests__/ResultsGrid.exportAll.test.tsx
  - provisa-ui/src/components/__tests__/GovernedTableViewer.exportAll.test.tsx
  scenario: 'Given a server-paged report whose filtered result spans more rows than one export chunk

    When the reader exports it to XLSX or CSV

    Then the statement is re-issued with an advancing OFFSET until it returns a short chunk, and the

    file holds every matching row — the workbook additionally carrying the scope and row count on

    its provenance sheet'
  related_reqs:
  - REQ-1386
  - REQ-1441
  - REQ-1442
- id: REQ-1445
  status: complete
  since: 2026-08
  group: 4. Source Connectors
  category: Federation
  priority: SHOULD
  type: behavioral
  description: Google Sheets is reachable from the Trino engine, not DuckDB alone. Trino's gsheets connector
    addresses sheets through a metadata spreadsheet whose rows map a table name to the range backing it,
    so the catalog carries that sheet's id (the source's database) and the service-account key path (the
    source's mapping), and every table listed in the metadata sheet becomes a table in the catalog. The
    reach is ATTACH_R — Trino queries the sheet live in place and cannot write back — so the source dropdown
    offers the type as LIVE on a Trino-pinned deployment rather than greying it out with "LIVE w/ DuckDB".
  rationale: Engine reach is a pure projection of the connector registry (REQ-947), so a type with no
    Trino connector class is unselectable on a Trino deployment however well Trino itself supports it.
    Provisa's cloud node is pinned to Trino, which made Google Sheets unreachable there despite Trino
    shipping the connector.
  use_case: An operator on a Trino-pinned deployment registers a Google Sheets source and queries its
    ranges alongside warehouse tables, without switching the federation engine.
  code:
  - provisa/federation/trino_connectors.py
  - provisa-ui/src/pages/SourcesPage.tsx
  tests:
  - tests/unit/test_gsheets_trino_connector.py
  scenario: 'Given a deployment whose federation engine is Trino

    When the operator opens the source-type dropdown

    Then Google Sheets is selectable and tagged LIVE, and registering one with a metadata sheet id

    and a service-account key path creates a gsheets catalog carrying both'
  related_reqs:
  - REQ-947
  - REQ-842
  - REQ-899
- id: REQ-1446
  status: complete
  since: 2026-08
  group: 10. UI & Admin Surfaces
  category: Product Tour
  priority: SHOULD
  type: behavioral
  description: 'Readiness for a tour step means both halves: the destination''s code chunk is compiled
    (REQ-1362) and the queries that page gates its render on are already in the Apollo cache. The tour
    therefore runs every query its destination pages read (sources, domains, tables, relationships, all
    relationships, RLS rules, roles) before showing step 0, and a query the visitor''s role cannot read
    is swallowed rather than blocking the start. Because the list hooks read cache-and-network, which
    reports loading on every revalidating mount, a page reports "nothing to paint" only when its data
    is undefined — a cached empty list is an answer, so a warm page does not re-blank on re-entry. Steps
    whose highlight target lives in the persistent shell (navbar, subnav) carry a readySelector pointing
    at page content, so the popover never appears over a page still painting its own loading state.'
  code:
  - provisa-ui/src/hooks/useAdminQueries.ts
  - provisa-ui/src/tour/useTour.tsx
  - provisa-ui/src/tour/tourSteps.ts
  - provisa-ui/src/pages/RelationshipsPage.tsx
  tests:
  - provisa-ui/src/__tests__/tourDataPreload.test.tsx
  scenario: 'Given a visitor who has not yet opened the admin pages

    When the visitor starts the product tour

    Then every query the destination pages read has resolved into the Apollo cache before step 0

    appears, a query the visitor''s role cannot read is swallowed instead of blocking the start, and

    re-entering a page whose cached list is empty does not re-blank it'
  related_reqs:
  - REQ-1362
- id: REQ-1447
  status: proposed
  target: 2026-Q4
  group: 13. Multi-Tenancy & Organizations
  category: Federation Engine
  priority: MUST
  type: behavioral
  description: 'SaaS runs ONE engine architecture on one GKE cluster. Shared (REQ-1243) and isolated (REQ-1043,
    REQ-1412) are the same Trino deployment from the same manifests and the same image, differing only
    in tenancy and in which node pool they schedule onto: the shared instance hosts every Starter org
    on a pool that grows as customers sign up, and an isolated instance hosts one org on a pool tainted
    to that org alone. There is no second engine kind, no second provisioning path, and no isolated-only
    configuration -- provisioning takes tenancy and size and is otherwise one code path. This replaces
    two unrelated systems: the shared engine is today a google_compute_instance coordinator plus a regional
    MIG and autoscaler for workers (terraform/gcp-saas/main.tf:297,401,421) with the app in compose on
    that VM, while isolated engines are containers created through the Docker Engine API on that same
    VM''s socket (PROVISA_ISOLATED_ENGINE_DOCKER_SOCKET), joined to PROVISA_ISOLATED_ENGINE_NETWORK. That
    co-tenancy is why an isolated org can currently degrade everyone: HostConfig bounds Memory but never
    NanoCpus, and the generated config sets node-scheduler.include-coordinator=true so the container EXECUTES
    scans on the shared node where the SaaS coordinator only plans. Under scheduler placement the contention
    is structurally absent rather than policed -- a dedicated node pool is the bound, and requests==limits
    (Guaranteed QoS) makes the CPU cap idiomatic instead of an omission. Unifying also removes the divergence
    the isolated path was forced into: _JVM_CONFIG deliberately forgoes the shared cluster''s jvm.config
    because that one loads the OpenTelemetry javaagent from /etc/trino/otel, a path only the compose mount
    provides and whose absence aborts the JVM before Trino logs anything; one image plus a ConfigMap ends
    that split. Kubernetes also supplies the cluster lifecycle SaaS would otherwise author -- per-node
    config with unique node.id, partial-provision reconciliation, cascading teardown, and a readiness
    gate on readyReplicas rather than today''s _wait_until_ready poll of /v1/info starting==false, which
    returns as soon as the coordinator answers and would release queries onto a partially registered cluster.
    The control plane holds scoped k8s RBAC instead of a mounted Docker socket, a materially narrower
    grant than handing the app process a container runtime. provisioning_available() already draws the
    seam and names Kubernetes as the anticipated out-of-band case: PROVISA_ISOLATED_ENGINE_HOST_TEMPLATE
    resolves WHERE an engine lives independently of whether this process can create one, so routing, status,
    and catalog reissue are unchanged. The one GKE cluster is a single failure domain covering both lanes;
    this is not a regression, since both lanes today share one VM.'
  use_case: An org on the isolated lane gets the same engine the shared lane runs, on hardware it alone
    occupies, and a query that saturates it leaves every other org untouched.
  code:
  - provisa/federation/isolated_provisioner.py
  - .claude/commercial/provisa_commercial/org_engine_router.py
  - terraform/gcp-saas/main.tf
  scenario: 'Given an org moved onto the isolated lane

    When its engine is provisioned

    Then it runs the same Trino manifests and image as the shared engine, scheduled onto a node

    pool tainted to that org alone

    And a query that saturates that pool leaves every other org''s latency unchanged

    '
  related_reqs:
  - REQ-1043
  - REQ-1412
  - REQ-1243
- id: REQ-1448
  status: proposed
  target: 2026-Q4
  group: 13. Multi-Tenancy & Organizations
  category: Federation Engine
  priority: MUST
  type: behavioral
  description: 'An isolated engine idles to zero and wakes on traffic, so the org pays for compute it
    uses rather than 730 node-hours a month. Today neither half exists: provision_isolated_engine is reached
    only from admin routes (org_engine_router.py, orgs_router.py), never from a query path, and no reaper
    stops an idle engine -- the container even carries RestartPolicy unless-stopped. Idling scales the
    org''s node pool to zero, not merely its pods: a node with zero pods still bills in full, whereas
    an empty node pool costs nothing, which beats the stopped-VM floor of a retained boot disk. Wake therefore
    pays a node provision of roughly 90-120s, deliberately accepted over keeping warm capacity per org,
    and well inside the tier''s competitive envelope where Databricks classic SQL warehouses cold-start
    in minutes. The query path holds for it rather than timing out. The shared engine never idles; only
    isolated pools scale to zero. Waking is NOT merely scheduling the pods. An engine holds no data and
    its catalogs are issued from the org''s config every time its runtime is built, booting with catalog.management=dynamic
    -- so a resumed engine answers /v1/info with zero catalogs, and a query released against it fails.
    Wake is therefore coupled steps under the registry lock: scale the pool up, await readyReplicas rather
    than the coordinator merely answering, force the org runtime rebuild that reissues CREATE CATALOG,
    and only then release the waiting query. Scaling IN must drain: terminating a worker mid-query kills
    its tasks, so terminationGracePeriodSeconds must exceed the longest permitted query and rely on Trino''s
    SHUTTING_DOWN drain. Keeping an engine warm 24x7 is the org''s choice, expressed as traffic, and is
    billed as the compute it is.'
  use_case: An org that queries for two hours a day pays for two hours of engine time, and an org that
    wants a permanently warm engine keeps it awake by using it and pays accordingly.
  code:
  - provisa/federation/isolated_provisioner.py
  - provisa/federation/trino_lifecycle.py
  - provisa/api/org_runtime.py
  scenario: 'Given an isolated org whose engine has been idle past its threshold

    When the org issues a query

    Then its node pool is scaled up, the org runtime is rebuilt so its catalogs are reissued, and

    the query executes against an engine that can see its sources

    And a scale-in that overlaps a running query drains rather than killing it

    '
  related_reqs:
  - REQ-1447
  - REQ-1281
  - REQ-1043
- id: REQ-1449
  status: proposed
  target: 2026-Q4
  group: 11. Platform, Infrastructure & Delivery
  category: SaaS Billing
  priority: SHOULD
  type: behavioral
  description: Pro is sold as three fixed sizes -- S (4 vCPU / 32GB), M (8/64), L (16/128) -- expressed
    as the node pool machine type and the pod requests/limits of the org's engine (REQ-1447), each a plan-fixed
    constant rather than a control the org adjusts, since each plan is a fixed setting and orgs do not
    tune their own entitlements. Sizes are carried per plan in the entitlements table rather than derived
    from a machine family, so a later size may change family (c3-highcpu for a CPU-bound profile) without
    a schema change. The ladder is vertical because GCE pricing is linear per vCPU and GB, making scale-up
    cost-neutral against scale-out at equal capacity while avoiding network exchange between pods; it
    stops at L well inside the practical single-JVM ceiling, since jvm.config sets MaxRAMPercentage=70
    with G1 and pause times grow with heap and live-set, so useful single-JVM scale ends near 200-300GB
    heap. Past that the same substrate carries the org further by splitting the engine into more worker
    pods with modest heaps rather than one large one, trading in-JVM reference passing for exchange serialization
    -- worth paying only above that heap threshold, which is why S/M/L do not do it. Workloads exceeding
    what one node pool serves are met by the external lane (REQ-1412) or a self-hosted multi-node deployment
    sized at apply time (terraform/gcp node_count, worker_machine_type). Dedicated placement (REQ-1447)
    also makes the lane's charges directly meterable, which is the metering path REQ-1281 lacked. Engine-hours
    accrue only while the org's node pool is scaled up, read from the platform rather than inferred, and
    egress is attributable because the pool serves one org -- neither is derivable for a container sharing
    a node. The derived query.max-memory and query.max-memory-per-node follow from the size rather than
    the deployment-wide PROVISA_ISOLATED_ENGINE_MEMORY; on a single-pod engine the per-node bound must
    not sit below the cluster bound, as today's 30%/60% split does, since both name the same pool and
    the lower value kills queries at half the stated budget. Because the isolated lane cannot degrade
    other tenants once placement is dedicated, it carries no query concurrency or duration limits -- the
    size is the only limit, and the org pays for what it runs.
  use_case: An org picks one of three Pro sizes and is invoiced engine-hours at that size's rate plus
    measured egress, on hardware it exclusively occupies.
  related_reqs:
  - REQ-1447
  - REQ-1448
  - REQ-1281
  - REQ-1280
- id: REQ-1450
  status: proposed
  target: 2026-Q4
  group: 13. Multi-Tenancy & Organizations
  category: Federation Engine
  priority: MUST
  type: behavioral
  description: 'The shared engine (REQ-1243) is one multi-tenant Trino deployment on the GKE cluster (REQ-1447),
    serving every Starter org from a single cluster whose node pool grows as customers sign up. Tenants
    are separated INSIDE that Trino -- by resource group and by the catalog set the org''s runtime issues
    -- not by pod or node, which is exactly what distinguishes it from the isolated lane running the identical
    manifests for one org. Fairness therefore stays the resource manager''s job: trino/etc/resource-groups.json
    (templated at provisa/api/trino_setup.py) becomes a ConfigMap, its per-tenant `tenant-${USER}` subgroup
    unchanged, and the per-query caps of REQ-1044 remain the Starter-side limit since one org''s query
    CAN degrade its neighbours here. Operators must know that ConfigMap updates reach pods on the kubelet
    sync interval, roughly 60s, rather than on Trino''s file-watch interval, so resource-group changes
    propagate more slowly than they do today. Amended: a ConfigMap key is mounted by subPath so that mounting
    it cannot hide the rest of /etc/trino, and a subPath mount is never refreshed by the kubelet at all
    -- so the pod template carries a digest of the rendered config and a resource-group change takes effect
    by rolling the pod, not by waiting out a sync interval. Amended: a shard is ONE pod that both coordinates
    and executes, not a coordinator plus an HPA-scaled worker set. Trino''s include-coordinator inverts
    on a dedicated node pool -- it was a co-tenancy hazard only because the Docker provisioner ran the
    container on the control plane''s own host, and with it false a single-pod shard has no node willing
    to take a split, so every query queues forever. Capacity is therefore added by shards, which this
    requirement already names as the answer past one coordinator, and the worker HPA is deferred to the
    Pro sizes where a shard serves one org and its size is a purchased entitlement. Scale-in must drain
    per REQ-1448. The shared pool scales to zero when no org is active on the shard, on the same node-pool
    mechanism and wake path the isolated lane uses (REQ-1448); it is not exempt from idle-to-zero, it
    simply reaches that state far less often, because the idle window has to be quiet for EVERY org on
    the shard at once rather than for one. The distinction matters most at the two ends. Before the first
    paying org the shard has no tenant at all, and a pool pinned above zero there is the platform''s single
    largest idle line -- one n2-highmem-8 held 24x7 is roughly $383/mo against a zero-customer floor near
    $19/mo, so pinning it would multiply the floor twentyfold to serve nobody. Once a shard carries real
    traffic the pool effectively never reaches zero on its own, which is the correct outcome to arrive
    at by observation rather than by configuration. The Starter wake cost is the REQ-1448 node-provision
    latency on a cold first query, which is acceptable on the entry tier and is the same latency the isolated
    lane already accepts. Idle policy is therefore a per-shard setting -- minimum node count and idle
    window -- not a property of the lane. This deployment replaces the google_compute_instance coordinator
    plus worker MIG. Capacity is added by growing the worker node pool on the SAME cluster, keeping one
    coordinator; workers in a separate cluster could join over flat VPC-native networking but would put
    a cluster boundary in every exchange for no gain. That path ends at the coordinator, not the workers:
    a single Trino coordinator plans, schedules, and assembles results for every concurrent query, and
    open-source Trino has no multi-coordinator mode, so beyond some concurrency more workers stop helping.
    The answer then is a SECOND shared cluster with its own coordinator and Starter orgs assigned across
    them -- sharding, not scaling -- which reuses the endpoint resolution the lanes already share rather
    than introducing a new mechanism. Shards are named shared_1, shared_2, and so on, with the org row
    naming the shard it belongs to. Starter fairness is two mechanisms, not one. The per-query caps of
    REQ-1044 are the hard bound, and metered compute is the soft one: because Starter pays for the hours
    it consumes, an org has an incentive not to crowd its neighbours even while staying inside every cap
    -- which is the failure a limit alone cannot reach. Metering the shared lane therefore CANNOT read
    infrastructure uptime the way the isolated lane does (REQ-1449), since the nodes host every Starter
    org at once; attribution must come from Trino itself, summing per-query CPU time and peak memory per
    org from the event listener. That is a distinct integration from the node-pool uptime read, and it
    is the half of REQ-1281 the shared lane needs.'
  use_case: Every Starter org queries one shared Trino whose capacity grows with signups, with resource
    groups keeping any one org from starving the others.
  code:
  - trino/etc/resource-groups.json
  - provisa/api/trino_setup.py
  - provisa/federation/k8s_provisioner.py
  - terraform/gcp-saas/gke.tf
  - terraform/gcp-saas/main.tf
  scenario: 'Given several Starter orgs on the shared lane

    When one org submits a query that would consume the cluster

    Then its resource group bounds it and the other orgs'' queries continue to be scheduled

    '
  related_reqs:
  - REQ-1447
  - REQ-1243
  - REQ-1044
- id: REQ-1451
  status: proposed
  target: 2026-Q4
  group: 11. Platform, Infrastructure & Delivery
  category: Deployment Topology
  priority: MUST
  type: infrastructure
  description: The GKE cluster (REQ-1447) hosts Trino and nothing else. The control plane -- the Provisa
    API, UI, and background workers -- stays on its own VM as today, with Cloud SQL holding the admin
    database, and is deliberately NOT moved into Kubernetes. Two reasons make the split correct rather
    than merely expedient. First, OrgRegistry serializes runtime rebuilds on an in-process asyncio.Lock
    (provisa/api/org_runtime.py:144-151), and the per-process federation engine is a settled invariant
    (REQ-1241), so more than one replica would let concurrent rebuilds for the same org race; a single-VM
    control plane keeps that lock meaningful. Second, the engine tier is the part with elastic, per-tenant,
    scale-to-zero lifecycle, and the control plane is not -- hosting it on the same substrate would buy
    orchestration it has no use for. The app reaches the cluster through the Kubernetes API authenticated
    by the VM's attached service account, so no key material is exported and the mounted Docker socket
    (PROVISA_ISOLATED_ENGINE_DOCKER_SOCKET) is retired -- a narrower grant than handing the app process
    a container runtime, and the reason isolated engines can no longer land on the control plane's own
    host. Engine endpoints stay resolved by PROVISA_ISOLATED_ENGINE_HOST_TEMPLATE, now naming in-cluster
    services, so routing is unchanged by the substrate move.
  use_case: Operators run one Kubernetes cluster for query engines and one VM for the control plane, each
    sized and scaled for what it actually does.
  code:
  - provisa/federation/isolated_provisioner.py
  - provisa/api/org_runtime.py
  - terraform/gcp-saas/main.tf
  related_reqs:
  - REQ-1447
  - REQ-1450
  - REQ-1241
- id: REQ-1452
  status: proposed
  target: 2026-Q4
  group: 11. Platform, Infrastructure & Delivery
  category: Pricing & Tiering
  priority: MUST
  type: behavioral
  description: 'The published price list is set by the comparables, not derived from cost. Starter bills
    $1.30 per ACTIVE HOUR with a $25/mo minimum and 25 GB of included egress -- the same rate AND the
    same meter shape as Hasura Cloud v2 Professional, which charges $1.30 per hour in which the project
    served at least one request and carries no base fee. Pro bills per ENGINE HOUR at three sizes: Pro
    S (4 vCPU / 32 GB) $1.50 with a $99/mo minimum and 50 GB included, Pro M (8/64) $2.75 with $199 and
    100 GB, Pro L (16/128) $5.50 with $399 and 200 GB. The Pro anchor is Starburst Galaxy Pro at 6 credits
    x $0.50 = $3.00 per WORKER hour; because Galaxy bills per worker and its worker is about 8 vCPU, the
    like-for-like anchor is scaled to the size''s vCPU (Galaxy-equivalent $1.50 / $3.00 / $6.00), which
    puts Provisa at parity on Pro S and 8% under on Pro M and Pro L. Enterprise (BYO engine) bills $75
    per vCPU-MONTH of the capacity the CUSTOMER operates, with a $999/mo minimum and 100 GB included.
    It is deliberately not a flat platform fee: removing our compute cost does not remove the scale of
    what we govern, since a 500-vCPU bank runs every query through the same compiler, governance, catalog
    and event-listener path as an 8-vCPU team, so a flat fee would price the largest customer the platform
    will ever have identically to the smallest. The comp here is Starburst ENTERPRISE -- self-managed,
    licensed per vCPU of the customer''s own cluster -- not Galaxy, which prices compute Provisa would
    be supplying. The rate is 30% of Pro''s implied capacity price ($251/vCPU-mo at the Pro M rate): the
    discount IS the compute the customer brings, sized so that BYO is not the arbitrage every Pro customer
    takes. Capacity is READ from the customer''s coordinator, never self-declared. Egress beyond a tier''s
    included allowance is $0.48/GB, charged ON TOP OF the tier minimum rather than absorbed by it. The
    minimum floors the COMPUTE line only: a month whose compute falls under the minimum still pays for
    every gigabyte past the allowance, because the allowance is already the concession and letting the
    minimum swallow the overage would give away the one line with a real per-unit cost behind it. The
    allowance exists because Hasura''s $0.13/GB parity rate clears only 8% against GCP''s own $0.12/GB
    egress: matching the comp INSIDE the allowance keeps the published number credible while keeping the
    tier''s blended margin intact, and $0.48 is the four-times markup that satisfies REQ-1453 outside
    it. A minimum is charged on every tier even though neither comp has a base fee, because a signed-up
    org holds a share of the fixed floor whether or not it queries; the minimum, not the rate, is what
    makes an idle tenant carry its own weight.'
  use_case: A prospect comparing Provisa against Hasura or Starburst finds the same meter shape at the
    same or a lower rate, and cannot argue the platform is priced above its category.
  code:
  - pricing_model.py
  related_reqs:
  - REQ-1453
  - REQ-1454
  - REQ-1063
  - REQ-1450
- id: REQ-1453
  status: proposed
  target: 2026-Q4
  group: 11. Platform, Infrastructure & Delivery
  category: Pricing & Tiering
  priority: MUST
  type: constraint
  description: 'Every tier must clear roughly 75% MARGINAL gross margin -- revenue minus the infrastructure
    cost that customer alone causes -- at the price REQ-1452 publishes. Pro clears 78-89% at every size
    and usage point because an isolated engine''s cost is one node pool attributable to one org. Starter
    does not clear it unconditionally: a lone Starter org on a shared shard pays for a whole node and
    clears 58%, while the same org at density 2 / 4 / 8 concurrent orgs clears 78% / 88% / 94%. Shared-cluster
    DENSITY is therefore the Starter margin lever, and it improves without touching the price, which is
    the commercial reason the shared lane (REQ-1450) is the default rather than a discount lane. FULLY-LOADED
    margin -- marginal cost plus that customer''s share of the fixed platform floor -- is explicitly NOT
    required to hold at customer #1. The floor is about $211/mo live against $19/mo at zero customers,
    the difference being the two things a live platform must carry: a warm control plane ($98/mo) and
    the REGIONAL admin database ($95/mo) that is all REQ-1459 makes baseline. There is no standby control
    plane in the floor; REQ-1459 defers it. Loading that floor entirely onto the first tenant would price
    that tenant out of the comparison REQ-1452 has to win. Fully-loaded margin instead crosses 75% at
    a THRESHOLD: 8 orgs on Starter, 25 on Pro S, 15 on Pro M, and 1 on Enterprise. The MIX matters more
    than the count -- a single Enterprise account clears the whole floor at customer #1 -- and a lone
    Starter clears -43% fully loaded, which is an accepted and bounded condition of the first months rather
    than a defect, since it is one org carrying an entire platform. Keeping the shared pool scaled to
    zero when no org is active is what holds that threshold where it is: pinning one n2-highmem-8 above
    zero takes the floor from $211 to $594/mo and multiplies the org count needed at every tier by 2.8.'
  use_case: Pricing decisions are checked against a rule that distinguishes the cost a customer causes
    from the cost the platform carries, so an early thin month is not mistaken for a mispriced tier.
  code:
  - pricing_model.py
  related_reqs:
  - REQ-1452
  - REQ-1450
  - REQ-1449
- id: REQ-1454
  status: complete
  since: 2026-08
  group: 11. Platform, Infrastructure & Delivery
  category: Pricing & Tiering
  priority: MUST
  type: behavioral
  description: 'Starter''s billable unit is the ACTIVE HOUR: any clock hour in which the org submitted
    at least one query is billed once, regardless of how many queries it submitted or how long they ran.
    It is deliberately NOT a vCPU-hour, because on a shared shard the nodes host every Starter org at
    once and no per-org vCPU-hour exists to read; the active hour is the only unit the platform can report
    truthfully AND the customer can predict. The signal is per-query attribution taken at Provisa''s ONE
    audit seam (finalize_audit), after its pending guard, NOT node-pool uptime, which is what the isolated
    lane meters (REQ-1449), and not a Trino event listener: every protocol -- HTTP, pgwire, Bolt, Flight,
    gRPC, MCP -- reaches the engine through that seam, so it is the only point that sees each org''s statements
    once and exactly once, while a coordinator-side listener would see only the shared lane and would
    meter platform work (materialization refreshes, schema rebuilds) the audit seam already excludes.
    A statement with no acting principal is not a user query and meters nothing; a REJECTED statement
    does meter, because the org occupied the hour and charging only successes is gameable. N statements
    inside one clock hour collapse to one billed hour. Pro and Enterprise keep their own units: Pro bills
    engine hours read from its dedicated node pool''s uptime, and Enterprise bills no compute at all.
    An hour with no query is not billable on any tier; the tier minimum, not a phantom hour, is what covers
    an idle month.'
  use_case: A Starter customer's invoice can be reconciled against their own query log, and the platform
    can produce that invoice from a shared cluster it cannot partition.
  code:
  - provisa/audit/pipeline.py
  - provisa/core/commerce.py
  - .claude/commercial/provisa_commercial/usage.py
  - .claude/commercial/provisa_commercial/schema.py
  - pricing_model.py
  tests:
  - tests/unit/test_audit_pipeline.py
  - .claude/commercial/tests/test_tier_caps_trino.py
  - .claude/commercial/tests/test_billing_summary.py
  scenario: 'Given a Starter org on a shared shard

    When it submits forty queries inside one clock hour and none in the next

    Then exactly one active hour is billed for that period

    '
  related_reqs:
  - REQ-1452
  - REQ-1450
  - REQ-1449
  - REQ-1281
- id: REQ-1455
  status: in-progress
  target: 2026-Q4
  group: 11. Platform, Infrastructure & Delivery
  category: Pricing & Tiering
  priority: MUST
  type: behavioral
  description: 'Starter opens with a free evaluation period bounded on BOTH axes: 14 days, 40 active hours,
    and 25 GB of egress, whichever is reached first. Two bounds rather than one because either alone is
    open-ended in the other dimension -- a time box with no usage cap subsidises a load test, and a usage
    cap with no clock leaves a dormant trial holding a floor share indefinitely. The trial runs on the
    shared lane only; it never provisions an isolated engine. Worst-case acquisition cost is therefore
    a known number, $23.96 at density 1 falling to $5.62 at density 8, repaid by roughly four days of
    gross profit at density 4. A payment card is required AT SIGNUP, captured with a zero-dollar authorisation,
    and the trial ENDS BY CONVERTING to paid Starter rather than by suspending -- the Neon shape. The
    customer is warned at 80% of either cap and three days before expiry, and may cancel at any point
    before conversion. This is the opposite of REQ-1064''s enterprise trial, which auto-downgrades on
    expiry; the difference is deliberate, because a self-serve tier whose default outcome is suspension
    trains the customer to treat the platform as disposable. Billing consequence: because the subscription
    is created WITH a trial period at signup rather than at a deferred checkout, subscription_created
    fires before any revenue exists. Entitlement must key off subscription STATUS, never off a landed
    payment, or every trialling org reads as unpaid. All THREE bounds are enforced. The egress one reads
    org_usage_hour.egress_bytes, which is fed from the TRANSPORT write seams (provisa.core.egress) rather
    than from the audit seam the active-hour meter fires at: a streaming result is finalized before it
    is drained, so a byte count taken where the statement completes describes rows the client may never
    receive. What left the socket is observable only where the write happens, so each protocol reports
    its own writes into an in-memory buffer that a scheduled drain folds into the meter. HTTP, pgwire
    and Bolt report exact byte counts; Arrow Flight and gRPC report the payload size they can see before
    handing off to a C writer (Table/RecordBatch.nbytes, Message.ByteSize()), which misses framing --
    approximate is accepted for those two, and stated rather than hidden. A process that dies with a buffer
    loses those bytes, which under-counts and so never over-bills. Precedence when more than one bound
    falls in the same sweep: days, then active hours, then egress. The conversion notice names the active
    hour over egress because that is the unit the customer is quoted a rate for. The clock is Provisa''s,
    not the provider''s: Lemon Squeezy can express 14 days and nothing else, and the active-hour bound
    is a fact only the REQ-1454 meter can see. Conversion is forced on the provider by moving its trial_ends_at
    to now, which is what makes it charge the first period. The provider is moved FIRST -- an org recorded
    as converted whose merchant of record never billed it is a paying customer who is never invoiced.'
  use_case: A prospect can complete real due diligence on live data without a purchase order, and becomes
    a paying customer at the end of it without a second decision.
  code:
  - pricing_model.py
  - provisa_commercial/trial.py
  - provisa_commercial/usage.py
  - provisa_commercial/lemonsqueezy_client.py
  - provisa_commercial/router.py
  - provisa/core/egress.py
  scenario: 'Given a Starter signup with a card captured under a zero-dollar authorisation

    When the org reaches 40 active hours on day nine

    Then the trial ends at that moment and the subscription converts to paid Starter, not to suspended

    '
  related_reqs:
  - REQ-1452
  - REQ-1454
  - REQ-1064
  - REQ-594
  - REQ-1473
- id: REQ-1473
  status: complete
  since: 2026-08
  target: 2026-Q3
  group: 11. Platform, Infrastructure & Delivery
  category: Pricing & Tiering
  priority: MUST
  type: structural
  description: 'Plans, tier ceilings, the active-hour meter and the merchant-of-record integration ship
    as a SEPARATE distribution (provisa_commercial) that only the hosted deployment installs. The open-source
    wheel and the demo build carry neither the pricing model nor the code that charges for it. Provisa
    core never names that package: provisa.core.commerce is the one seam, it resolves the import once
    per process, and every hook through it degrades to a no-op when the import fails -- nothing is metered,
    no ceiling resolves, no /billing routes mount, no trial sweep is scheduled. That is the CORRECT behaviour
    for a self-hosted deployment rather than a degraded one: there is no subscription to enforce, so the
    seam returns "no caps apply" instead of a default set of ceilings, because guessing a tier for a deployment
    with no billing subject would paywall software the customer already owns. An ImportError is therefore
    recorded at debug and never raised, while any OTHER exception from the plugin''s import propagates
    -- a hosted node that silently ran unmetered would be billing nobody. The commercial control-plane
    schema follows the same rule: the billing columns on orgs and the org_usage_hour meter are attached
    to the core registry metadata at plugin import time, so init_registry_schema creates and back-fills
    them through the same portable create_all + add_missing_columns path as every other registry column
    and no migration exists. This is why the seam must load before the registry pass; bring_up_platform
    owns that ordering.'
  use_case: An open-source or demo install runs the whole product unmetered and unbilled, and the hosted
    deployment is the only build that carries how Provisa charges.
  code:
  - provisa/core/commerce.py
  - provisa/core/control_plane.py
  - provisa_commercial/__init__.py
  - provisa_commercial/schema.py
  scenario: 'Given a Provisa install with no commercial plugin

    When an org submits a governed statement

    Then it executes with no tier ceiling and nothing is metered, and the app exposes no /billing routes

    '
  related_reqs:
  - REQ-1044
  - REQ-1452
  - REQ-1454
  - REQ-1455
  - REQ-1355
- id: REQ-1456
  status: proposed
  target: 2026-Q4
  group: 11. Platform, Infrastructure & Delivery
  category: Pricing & Tiering
  priority: MUST
  type: behavioral
  description: 'Launch ships the SHARED lane and Starter alone. Pro and Enterprise are designed, priced
    and published, but presented as forthcoming: the marketing site and the in-product plan picker name
    both tiers with their REQ-1452 rates and state that they are coming, and neither is orderable. Publishing
    the unshipped rates is the point rather than an oversight -- a lone entry tier with no visible ceiling
    reads as a toy, and a prospect who can see the isolated and BYO tiers priced at or under Starburst
    can size their own future spend before committing to Starter. The sequencing follows the cost structure:
    the shared lane is the only lane whose margin improves with each signup (REQ-1453), so it is the correct
    lane to carry the fixed floor first, and it is the lane the isolated tiers are built on top of rather
    than beside. An interest signal on either forthcoming tier routes to the sales inbox on the REQ-1052
    path; it must never provision, because the isolated node pool it implies does not exist at launch.'
  use_case: A launch visitor sees the whole ladder, buys the one rung that exists, and registers interest
    in the rungs that do not.
  code:
  - provisa-ui/src/pages
  - pricing_model.py
  scenario: 'Given the launch pricing page

    When a visitor selects Pro

    Then the tier is shown at its published rate marked as coming soon and the selection routes to sales,
    with no engine provisioned

    '
  related_reqs:
  - REQ-1452
  - REQ-1450
  - REQ-1052
  - REQ-1053
- id: REQ-1457
  status: proposed
  target: 2026-Q4
  group: 11. Platform, Infrastructure & Delivery
  category: Deployment Topology
  priority: MUST
  type: constraint
  description: 'Every engine Provisa runs is the SAME container image. A shared shard''s coordinator and
    workers (REQ-1450), a Pro org''s dedicated coordinator at any of the three sizes (REQ-1452), and the
    engine an Enterprise customer operates themselves (REQ-1412) are one build, and the tiers are distinguished
    by pod count, machine size and configuration -- never by a different artifact. The permitted axes
    of variation are exactly three: the ROLE the process takes (coordinator versus worker, already the
    only difference between trino/etc/config.properties and trino/etc/worker/config.properties), the SIZE
    it is given (heap percentage and the query memory limits derived from it), and the DISCOVERY address
    it registers against. Anything that would require a second build -- a tier-only plugin, a tier-only
    patch, a fork for BYO -- is out of bounds, because the moment two images exist the tiers acquire independent
    release, upgrade and CVE timelines and a customer''s engine can be behind the shared one in ways nobody
    planned. This is what lets REQ-1452 price a linear ladder: n2 bills per vCPU and per GB, so Pro S,
    M and L cost exactly 1x, 2x and 4x, which is only true while the three sizes are one engine scaled
    by machine rather than three products. It is also what lets REQ-1456 publish Pro before shipping it
    -- the engine already exists and runs in production on the shared lane; what does not exist yet is
    a node pool. One divergence exists today and must be closed by the move to GKE rather than preserved.
    The isolated provisioner''s _JVM_CONFIG (provisa/federation/isolated_provisioner.py) deliberately
    omits the OpenTelemetry javaagent that trino/etc/jvm.config loads, because the jar reaches the shared
    cluster on a compose mount at /etc/trino/otel and a missing -javaagent jar aborts the JVM before Trino
    logs anything. That is a mount artifact, not a design difference: on GKE the agent ships INSIDE the
    image and the existing -Dotel.javaagent.enabled flag becomes the per-lane switch, so the two lanes
    stop differing in their JVM at all. Configuration divergence that remains must be deliberate and stated
    -- the shared cluster''s fault-tolerant execution settings (retry-policy=TASK and the killer policy)
    are absent from a provisioned isolated coordinator, and each such gap is either a decision or a defect,
    never an accident of two files drifting. Memory limits in particular must be derived from the pod''s
    OWN size and the pool''s shape: query.max-memory-per-node fixed at a fraction of heap is wrong on
    a single-pod engine, where that one node IS the cluster, and the error grows with the size sold.'
  use_case: An operator upgrades Trino once and every tier moves together, and a customer running the
    BYO engine is running the same binary the platform runs.
  code:
  - provisa/federation/isolated_provisioner.py
  - trino/etc/config.properties
  - trino/etc/worker/config.properties
  - trino/etc/jvm.config
  scenario: 'Given the shared lane, a Pro coordinator and a BYO engine

    When each reports its version and build

    Then all three are the same image, differing only in role, size and discovery address

    '
  related_reqs:
  - REQ-1447
  - REQ-1450
  - REQ-1452
  - REQ-1456
  - REQ-1412
- id: REQ-1458
  status: proposed
  target: 2026-Q4
  group: 11. Platform, Infrastructure & Delivery
  category: Pricing & Tiering
  priority: MUST
  type: behavioral
  description: 'Every tier has a CEILING at which the price list stops and a contract begins. Starter''s
    ceiling is 400 active hours a month, one shared shard, and the per-query caps of REQ-1044. Pro''s
    is Pro L (16 vCPU), ONE engine, one region, with the HA add-on covering the engine only. Enterprise''s
    is 128 vCPU of customer-operated capacity. Reaching a ceiling NEVER interrupts service: the meter
    keeps running at the published rate and a negotiated arrangement replaces it, because cutting off
    the largest customer at the exact moment they commit is the one outcome worth engineering against.
    The two kinds of ceiling convert differently, and the difference is the point. Starter''s converts
    SELF-SERVE, straight to Pro, with no negotiation -- a growing Starter org is the funnel working, not
    an exception to it. Pro''s and Enterprise''s convert to a CONTRACT, because past those points the
    customer is buying an operational commitment -- multiple regions, private networking, a support tier,
    an SLA tighter than the recovery targets REQ-1459 publishes -- which no hourly rate can express and
    which the platform cannot deliver by turning a number up. Without ceilings the published rates are
    a standing offer to the largest customer the platform will ever have, at terms set for the smallest:
    a 512-vCPU Enterprise account would otherwise self-serve $38,400/mo of licence with no SLA conversation,
    no capacity commitment, and no chance to price the support that account actually consumes.'
  use_case: A customer outgrowing a tier gets a conversation rather than an error, and the platform gets
    the chance to price scale before it delivers it.
  code:
  - pricing_model.py
  scenario: 'Given an Enterprise org whose coordinator reports 200 vCPU, above the 128 vCPU self-serve
    ceiling

    When the capacity read lands

    Then service continues and metering continues at the published rate, and a negotiation is opened

    '
  related_reqs:
  - REQ-1452
  - REQ-1044
  - REQ-1459
- id: REQ-1459
  status: proposed
  target: 2026-Q4
  group: 11. Platform, Infrastructure & Delivery
  category: Deployment Topology
  priority: MUST
  type: infrastructure
  description: 'HIGH AVAILABILITY on this platform means a PUBLISHED RECOVERY TIME MEASURED IN MINUTES,
    and it is BASELINE for every tier rather than an upsell. It is baseline because the control plane
    is not a per-tenant component: every surface (UI, GraphQL, SQL, pgwire, Bolt, Flight) routes through
    it, so a control-plane outage is a total outage for every org at once, including the ones paying the
    $25 Starter minimum. The published targets are: control plane about 5 minutes (reschedule the plane,
    then rebuild the org runtimes); admin database 1-2 minutes (regional Cloud SQL); engine about 5 minutes
    without the Pro add-on (a cold pod); engine about 30 seconds with it (repoint to a running standby
    coordinator). THE TWO HALVES OF CONTROL-PLANE HA ARE NOT THE SAME PURCHASE, and only one is baseline
    at launch. A standby VM buys AVAILABILITY -- losing the zone costs minutes rather than a reschedule
    -- and single-zone is the MARKET DEFAULT at this price point: Cloud SQL, RDS Multi-AZ and Neon all
    charge extra for regional, and neither comp (Hasura Cloud, Starburst Galaxy) gives a $25 customer
    a zone guarantee; they publish about 99.9% and so can this platform. The standby is therefore DEFERRED,
    not rejected: it goes in when the first Pro or Enterprise account lands, at $98/mo plus $4 of disk,
    and it stops being idle capacity at that point because MULTIPLE control planes are already a first-class
    idea here and REQ-1451''s one-writer rule is per ORG rather than per fleet -- so the second plane
    takes live org ownership and failover becomes reassignment between two warm planes. The REGIONAL DATABASE
    is the half that is NOT optional, because it buys against DATA LOSS rather than downtime: losing an
    f1-micro''s zone rolls org config, users, compiled models and governance rules back to a backup, which
    is the failure the market does not forgive. Cloud SQL HA cannot use db-f1-micro at all -- shared-core
    tiers have no regional configuration -- so it lands on the smallest dedicated-core tier billed in
    two zones, about $95/mo. Baseline HA therefore costs about $95/mo and takes the live floor to $211/mo
    (REQ-1453); it does not change the zero-customer cost of $19/mo, because the regional database is
    provisioned at the first customer rather than before one exists. THE FAILURE DOMAIN IS THE ZONE, NOT
    THE REGION, and this must be published as plainly as the recovery targets. The control plane, the
    regional Cloud SQL pair and the shared shard all live in ONE region, so a region-wide outage takes
    the control plane and every engine down together and no standby inside that region helps. Regional
    recovery is a RESTORE somewhere else from cross-region backups, measured in HOURS, and that is the
    number to state. Buying past it means a second region -- a standby plane at full price again, a cross-region
    Cloud SQL replica, and continuous replication egress -- which is a NEGOTIATED Enterprise item under
    REQ-1458 and never a line on the shared floor. THE EXPECTATIONS ARE PART OF THE PRODUCT AND MUST BE
    PUBLISHED, not buried. A failover KILLS IN-FLIGHT QUERIES: HA does not mean a query cannot burp. The
    contract is exactly the one a database gives a client whose transaction lost its connection -- the
    query fails cleanly and THE CLIENT RETRIES. Promising more would mean claiming an OSS Trino coordinator
    can hand off a running query, which it cannot. A customer whose real requirement is continuous availability
    runs Enterprise/BYO (REQ-1412) on their own engine, their own redundancy and their own operators;
    that is a truthful answer to a real requirement and a better one than selling a managed SLO the architecture
    would break. STARTER GETS NO ENGINE HA, deliberately. A shared shard is one coordinator, so if it
    restarts every Starter org on it takes a cold start. That is the right trade at a $25 minimum -- the
    tier is SMBs and tinkerers, and a pinned standby shard would cost more than the orgs sitting on it
    pay. PRO HA IS AN ADD-ON priced at 2x the size''s published engine-hour rate (Pro S $3.00/hr, Pro
    M $5.50/hr, Pro L $11.00/hr), because it is a primary plus a PINNED standby and a scale-to-zero standby
    is not a standby. It is cheap to build -- a coordinator holds no data and reissues its catalogs from
    the org''s config on every runtime build, so a standby is a second pod plus a repoint, with no replication
    and no state to move -- and it buys exactly one thing: engine recovery time, seconds instead of a
    cold start. It does not shorten the control-plane target and does not stop a query dying on failover;
    marketing it as zero downtime is the one claim to refuse. ENTERPRISE may take its own CONTROL PLANE
    -- a dedicated VM, a dedicated Cloud SQL instance and its own GKE cluster, none of it shared. What
    that buys is ISOLATION, not a different availability class: the recovery targets are the ones every
    tier already gets. It is the only configuration in which the customer shares NOTHING with another
    org, which is the actual question behind most enterprise security review, and it lifts that org clear
    of contention for control-plane rebuilds. Cost is about $284/mo (a second full platform floor plus
    the $73/mo GKE management fee, since the free-tier zonal cluster is already spent on the shared cluster),
    priced near $1,140/mo on top of the capacity licence; with its own regional database it is about $379/mo,
    priced near $1,520/mo.'
  use_case: A buyer is told in writing what failure looks like -- minutes of recovery, a failed query
    their client retries -- and can choose between the baseline, the Pro engine add-on, a dedicated control
    plane, or BYO, without anyone promising availability the engine cannot deliver.
  code:
  - provisa/api/org_runtime.py
  - provisa/federation/isolated_provisioner.py
  - terraform/gcp-saas/main.tf
  - pricing_model.py
  scenario: 'Given a Pro org without the HA add-on whose engine pod is killed mid-query

    When the query fails and the client retries

    Then a fresh coordinator is running within the published engine recovery target and the retried

    query succeeds against catalogs reissued from the org''s config

    '
  related_reqs:
  - REQ-1451
  - REQ-1452
  - REQ-1458
  - REQ-1412
  - REQ-1450
- id: REQ-1460
  status: proposed
  target: 2026-Q4
  group: 11. Platform, Infrastructure & Delivery
  category: Deployment Topology
  priority: SHOULD
  type: infrastructure
  description: 'A regional standby of the SHARED LANE is ASYMMETRIC: the control plane and its database
    are warm in a second region, and the engine is not. The split is forced by the cost, which is lopsided.
    Warming the plane and its database costs about $230/mo: a standby VM at its full $98/mo plus $4 of
    disk -- net new spend, since REQ-1459 defers the standby and the floor therefore carries none to relocate
    -- a cross-region Cloud SQL replica (about $51/mo plus storage and replication egress) and a second
    GKE cluster management fee ($73/mo, since the free zonal tier is already spent on the primary). Warming
    the SHARED SHARD costs about $383/mo on its own, 1.7x more, because a node pool at min=0 is not a
    standby and pinning one in region B is precisely the fixed cost REQ-1450 was amended to refuse in
    region A. Full warm standby of the lane is therefore about $613/mo, taking the live floor from $211
    to $824/mo (3.9x); the asymmetric form takes it to $442/mo (2.1x). What each buys is stated as the
    recovery target it changes. Today a regional outage is a restore from cross-region backups measured
    in HOURS. With a warm plane and a cold engine it is about 10 minutes -- the plane reschedules and
    rebuilds, the shard scales 0 to 1. With the full warm lane it is about 5 minutes, the plane alone.
    The asymmetric form buys most of the benefit for 38% of the cost, and the roughly 5 minutes it concedes
    are a cold shard start, which is exactly what Starter already accepts every day under REQ-1450''s
    scale-to-zero. Regional standby is NOT folded into the shared floor and is sold as a NEGOTIATED Enterprise
    item under REQ-1458. Recovered at the 4x markup, the asymmetric form needs 37 Starter minimums to
    pay for itself and the full warm lane needs 98; the shared lane cannot absorb the engine half, and
    the customers who require regional continuity are not the ones on the $25 minimum. A customer who
    needs better than the asymmetric target runs BYO (REQ-1412) -- their engine, their regions, their
    redundancy.'
  use_case: An operator can state what a regional outage costs in recovery time at each of three spend
    levels, and sell the standby to the accounts that need it instead of taxing every Starter org for
    it.
  code:
  - pricing_model.py
  - terraform/gcp-saas/main.tf
  scenario: 'Given the asymmetric regional standby is provisioned and region A fails

    When the control plane in region B takes ownership of the orphaned orgs

    Then service resumes within the published regional target once the region-B shard node pool

    scales from zero, and no data is restored from backup

    '
  related_reqs:
  - REQ-1459
  - REQ-1458
  - REQ-1450
  - REQ-1453
  - REQ-1412
- id: REQ-1461
  status: proposed
  target: 2026-Q4
  group: 13. Multi-Tenancy & Organizations
  category: Federation Engine
  priority: MUST
  type: behavioral
  description: 'The cold-shard wake is reconciled with the executor''s retry budget by ORDERING, not by
    widening the budget. A cold start is roughly 90-120s of Autopilot node provision plus Trino start
    (k8s_provisioner._await_ready, bounded by PROVISA_ENGINE_READY_TIMEOUT) while a statement''s retry
    budget is 30s (_retry_budget/PROVISA_RETRY_BUDGET_SECS), so a query that discovered an absent shard
    from inside its retry loop could never wait the wake out -- it would exhaust the budget and fail on
    a shard that was seconds from ready. The wake therefore runs BEFORE the terminal call, at the top
    of _execute_plan and ahead of _run_plan_terminal, so the wait for capacity is not spent from the retry
    budget at all and the budget keeps its intended meaning: transient failures of a shard that is already
    up. Raising PROVISA_RETRY_BUDGET_SECS to cover a cold start is explicitly REJECTED -- it would make
    every genuinely failing query on a warm shard hang for minutes. Because _execute_plan is the one terminal
    seam every surface routes through (REQ-1386), no protocol server carries a wake of its own.'
  use_case: A query that arrives at a shard that is at zero replicas waits out the cold start and then
    executes, instead of failing after 30 seconds against an engine that is still coming up.
  code:
  - provisa/pgwire/_pipeline.py
  - provisa/federation/engine_wake.py
  scenario: 'Given an org whose shard has been scaled to zero

    When the org issues a query

    Then the shard is woken and the org runtime rebuilt before the statement reaches the terminal

    And the statement''s retry budget is not consumed by the wake

    '
  related_reqs:
  - REQ-1448
  - REQ-1450
  - REQ-1386
- id: REQ-1462
  status: complete
  since: 2026-08
  group: 11. Platform, Infrastructure & Delivery
  category: SaaS Infrastructure
  priority: MUST
  type: behavioral
  description: 'The front door''s idle clock is bounded by the coordinator''s own uptime, read from the
    instance''s lastStartTimestamp: a box cannot have been idle for longer than it has been up. The boot
    grace of REQ-1333 keys on _last_start_call, which only the front door''s own wake sets, so a start
    from any other source -- an operator running `gcloud compute instances start` or `reset` for a deploy
    -- left _last_activity at its pre-stop value and the reaper stopped the coordinator on its next tick,
    mid startup script. The uptime bound covers every start regardless of who issued it, and does not
    latch: a box up longer than idle_stop_minutes with no traffic is still stopped.'
  use_case: An operator restarts the coordinator to roll a release and it stays up long enough to install
    and start, instead of being reaped a minute into its startup script.
  tests:
  - tests/unit/test_front_door_wake_idle.py
  code:
  - terraform/gcp-saas/front-door/proxy.py
  scenario: 'Given a coordinator started outside the front door and up for 30 seconds

    And no traffic has crossed the front door since before it stopped

    When the idle reaper ticks

    Then the coordinator is left running

    '
  related_reqs:
  - REQ-1333
  - REQ-1334
- id: REQ-1463
  status: complete
  since: 2026-08
  group: 13. Multi-Tenancy & Organizations
  category: Federation Engine
  priority: MUST
  type: behavioral
  description: The engine idle reaper seeds the boot shard's activity clock when it starts, so a shard
    whose node is up survives a control-plane restart by exactly one idle window rather than forever.
    _last_activity is process state written by ensure_shard_awake; after a restart it is empty and the
    reaper iterates nothing, so a shard that no query touches was never a reap candidate and its GKE node
    billed indefinitely -- the cost the idle-to-zero design of REQ-1448 exists to remove. Seeding at reaper
    start restarts the window at the restart instead of disabling it.
  use_case: A control plane restarted for a release does not leave the shared shard's node billing until
    the next query happens to arrive.
  tests:
  - tests/unit/test_engine_wake.py
  code:
  - provisa/federation/engine_wake.py
  scenario: 'Given a control plane that provisions engines on Kubernetes

    When the idle reaper starts

    Then the boot shard has an activity timestamp

    And an untouched shard is released one idle window later

    '
  related_reqs:
  - REQ-1448
  - REQ-1450
- id: REQ-1464
  status: complete
  since: 2026-08
  group: 13. Multi-Tenancy & Organizations
  category: Federation Engine
  priority: MUST
  type: infrastructure
  description: 'The engine cluster runs in GKE AUTOPILOT mode, and idle-to-zero is a Deployment REPLICA
    count rather than a node-pool size. The Standard topology of REQ-1447/REQ-1448 could not actually
    rest at zero: with the default pool removed, GKE''s own system workloads (kube-dns, metrics-server,
    konnectivity-agent, event-exporter, kube-state-metrics, gmp-operator) are Deployments rather than
    DaemonSets, so the shard pool was the only place left to schedule them; scaling it to zero left them
    Pending and the cluster autoscaler rebuilt a node within seconds, so the shared lane billed an e2-highmem-8
    around the clock whether or not anybody queried it. Autopilot bills POD REQUESTS, makes the system
    workloads Google''s, and holds no nodes when nothing runs, so a shard with zero replicas costs nothing.
    The $0.10/hr cluster fee and its free-tier credit are identical in both modes, so the zero-customer
    floor of REQ-1448 is unchanged. Running COSTS about 6.5% more (a 6 vCPU / 24 GiB pod is $0.3851/hr
    of requests against $0.3616/hr for the e2-highmem-8 that carried it, us-central1 list); break-even
    against keeping a Spot system pool is roughly 460 shard-hours a month, so this is the cheaper shape
    until the shared lane is busy most of the day. Consequences that follow and are part of this requirement
    -- shard isolation is the pod''s Guaranteed QoS (requests == limits) plus the control plane living
    outside the cluster, NOT a nodeSelector or taint, which Autopilot has no pools to honour; the control
    plane''s IAM role drops container.clusters.update (it authorized nodePools:setSize) and its cluster-scoped
    node read, leaving container.clusters.get/getCredentials plus the namespaced RBAC; shard_status derives
    stopped/starting/ready from spec.replicas and status.readyReplicas rather than from a node count;
    scale_shard_to_zero patches the scale subresource to zero and WAITS for the Deployment to observe
    zero replicas, bounded by PROVISA_ENGINE_DRAIN_SECONDS, so a shard is never reported stopped while
    its pod is still draining and still billing; and Cloud DNS uses ADDITIVE VPC scope rather than VPC_SCOPE,
    which Autopilot does not support, valid because a shard''s Service is headless and resolvable under
    the same <service>.<namespace>.svc.<domain> name the control plane already dials.'
  use_case: With no customers querying, the shared engine lane bills nothing at all, instead of an always-on
    node kept alive by the cluster's own system pods.
  tests:
  - tests/unit/test_k8s_engine_provisioner.py
  - tests/unit/test_engine_wake.py
  code:
  - terraform/gcp-saas/gke.tf
  - provisa/federation/k8s_provisioner.py
  - provisa/federation/engine_wake.py
  scenario: 'Given an engine shard that has been idle past its window

    When the reaper stops it

    Then the shard''s Deployment is patched to zero replicas

    And the stop does not return until the cluster observes zero replicas

    And no node pool is resized, because the cluster holds no node pools

    '
  related_reqs:
  - REQ-1447
  - REQ-1448
  - REQ-1450
  - REQ-1451
  - REQ-1453
  - REQ-1463
- id: REQ-1465
  status: complete
  since: 2026-08
  group: 13. Multi-Tenancy & Organizations
  category: Federation Engine
  priority: MUST
  type: infrastructure
  description: 'Both engine-cluster topologies are selectable and exercised, chosen by var.engine_cluster_mode:
    "autopilot" builds the REGIONAL Autopilot cluster of REQ-1464, and "standard" builds a zonal Standard
    cluster carrying a small always-on Spot system pool plus one node pool per shard that autoscales 0..1
    and is tainted provisa.dev/shard=<shard>:NoSchedule. The two shapes are a UTILIZATION crossover rather
    than a dev/prod split -- Autopilot rests at zero but bills about 6.5% more while running, so Standard
    becomes cheaper past roughly 460 shard-hours a month -- and the platform must be able to move between
    them on evidence after the first two or three customers, not rewrite for it. What makes one control-plane
    code path serve both: the shard pod tolerates only its own shard''s taint, so the SAME replica patch
    drives node lifecycle in either mode, and only the pod''s placement stanza differs. Autopilot must
    be regional (the API refuses a zonal Autopilot cluster), which lets the scheduler place a shard pod
    outside the control-plane VM''s zone and bill every result-set byte as cross-zone egress; so PROVISA_ENGINE_CLUSTER_ZONE
    is REQUIRED, never inferred from the location, and every shard pod pins topology.kubernetes.io/zone
    to it in both modes. Switching modes REPLACES the cluster -- enable_autopilot and dns_config are creation-time
    only -- so the cutover destroys every shard and is announced through the maintenance notice of REQ-1466.
    DNS differs by what each mode supports: Autopilot uses CLUSTER_SCOPE plus additive_vpc_scope_dns_domain,
    Standard uses VPC_SCOPE plus cluster_dns_domain.'
  use_case: Once real utilization is known, the operator moves the shared lane to whichever topology is
    cheaper by changing one variable, having already run the platform on both.
  tests:
  - tests/unit/test_k8s_engine_provisioner.py
  code:
  - terraform/gcp-saas/gke.tf
  - terraform/gcp-saas/variables.tf
  - terraform/gcp-saas/main.tf
  - provisa/federation/k8s_provisioner.py
  - packaging/linux/first-launch.sh
  scenario: 'Given the deployment is configured for the standard topology

    When a shard''s engine pod is scheduled

    Then it tolerates only its own shard''s taint and lands on that shard''s node pool

    And it pins the control-plane VM''s zone, so no result set crosses a zone boundary

    And an unrecognised cluster mode is refused rather than guessed at

    '
  related_reqs:
  - REQ-1447
  - REQ-1448
  - REQ-1464
  - REQ-1466
- id: REQ-1466
  status: complete
  since: 2026-08
  group: 13. Multi-Tenancy & Organizations
  category: Platform Administration
  priority: MUST
  type: behavioral
  description: A platform_admin turns a deployment-wide scheduled-maintenance notice on and off, and every
    signed-in user sees it as a banner above the whole application on every route until it is cleared.
    Planned work that replaces the engine cluster -- the topology switch of REQ-1465 is the case that
    forces this -- fails queries for minutes, which is indistinguishable from the product being broken
    unless the deployment says otherwise. The wording is composed by the SERVER, so one deployment says
    one thing on every surface and an administrator with nothing special to say supplies nothing; a supplied
    message overrides it. started_at is stamped by the transition into active rather than supplied, so
    re-arming an open window (longer end time, corrected wording) does not restart its clock and turning
    the notice off clears the stamp. An omitted end time is stated as such rather than implying an imminent
    return. The banner is not dismissible, because a query issued after dismissing it fails exactly the
    same way. Reading the notice needs only authentication -- the people it is for are not administrators
    -- while writing it requires platform_settings, since a false maintenance banner is itself an outage.
    The same control exists as `provisa maintenance on|off|status` so a deploy script can raise the banner
    before the work and clear it after without a browser; the CLI is a thin client of the same endpoints
    and never composes its own wording.
  use_case: Before switching the engine cluster's topology, the operator raises the banner from the deploy
    script; users who query during the window are told it is scheduled work, not a failure.
  tests:
  - tests/unit/test_maintenance_router.py
  - tests/unit/test_cli_maintenance.py
  - provisa-ui/src/__tests__/MaintenanceNotice.test.tsx
  - provisa-ui/src/__tests__/adminNavCapabilities.test.ts
  code:
  - provisa/api/admin/maintenance_router.py
  - provisa/core/schema_admin.py
  - provisa/cli.py
  - provisa-ui/src/components/MaintenanceBanner.tsx
  - provisa-ui/src/components/admin/MaintenanceTab.tsx
  - provisa-ui/src/api/maintenance.ts
  scenario: 'Given a platform admin turns the maintenance notice on without supplying wording

    Then every signed-in user sees the deployment''s standard message on every page

    And extending the window does not restart the elapsed time it reports

    When the admin turns the notice off

    Then the banner disappears and the next window stamps its own start

    '
  related_reqs:
  - REQ-1337
  - REQ-1465
- id: REQ-1467
  status: complete
  since: 2026-08
  group: 1. Access Governance & Security
  category: Data Governance
  priority: MUST
  type: behavioral
  description: 'A tag may take a parameter, assigned as "{tag_id}:{value}" -- `entity:customer` on a column
    says the column holds names of customers, not merely that it holds entity names. The GraphRAG extractor
    needs the entity TYPE to build a vocabulary, and the bare `entity` tag of REQ-1375 cannot supply one,
    so the type travels in the assignment id rather than in a second tag. The id is split on the leftmost
    colon, matching how provisa.auth.role_mapping parses "role_id:domain_id"; unlike that parser, the
    bare form yields None rather than a wildcard, because whether a missing parameter is legal belongs
    to the tag''s param_policy and not to the parser. param_policy is "none" or "required" and deliberately
    has no "optional" value -- a bare `entity` standing beside `entity:customer` would need a reading,
    and the only reading available is a guessed entity type. Assignments store the base id alongside the
    full one and the uniqueness rule is stated over (base_tag_id, object_key), so one column carries exactly
    one entity type: `entity:customer` and `entity:employee` on the same column is a contradiction, and
    uniqueness over the full id would accept both. Every read, count, and removal resolves through the
    base id, so the registry row a parameterized assignment belongs to is always findable and an in-use
    tag is never reported as unused. The permitted values are a CLOSED list held in tag_param_values,
    maintainer-editable through the admin UI even for a code-defined system tag like `entity`, which owns
    no row of its own; an open list would accept `entity:custmoer`, and a misspelt entity type reads downstream
    as an entity that simply is not in the corpus. Starter entity types are seeded once at first boot
    rather than upserted on every boot, since re-upserting would resurrect types a maintainer deleted,
    and a value still carried by an assignment cannot be deleted.'
  use_case: A steward marks CUSTOMER.NAME as `entity:customer` and SALES_REP.NAME as `entity:employee`;
    the GraphRAG loader issues one vocabulary query per entity type instead of one undifferentiated entity
    query, and the extracted graph distinguishes a customer named Mercury from an employee of the same
    name.
  tests:
  - tests/unit/test_tag_params.py
  - provisa-ui/src/__tests__/TagControl.test.tsx
  - provisa-ui/src/__tests__/TagsTab.test.tsx
  code:
  - provisa/core/models.py
  - provisa/core/schema_org.py
  - provisa/core/repositories/tag.py
  - provisa/api/startup_seed.py
  - provisa/api/admin/schema_mutation.py
  - provisa/api/admin/schema_query.py
  - provisa-ui/src/components/TagControl.tsx
  - provisa-ui/src/components/admin/TagsTab.tsx
  scenario: 'Given the `entity` tag requires a parameter and its value list holds customer and employee

    When a steward assigns it to a column without picking a value

    Then the assignment cannot be applied

    When the steward assigns entity:customer and then entity:employee to that same column

    Then the column carries only entity:employee

    And the tag registry reports the `entity` tag as in use rather than unused

    When a maintainer deletes a value that no assignment carries

    Then it leaves the value list and does not return on the next boot

    '
  related_reqs:
  - REQ-1375
  - REQ-1377
  - REQ-1443
- id: REQ-1468
  status: complete
  since: 2026-08
  group: 11. Platform, Infrastructure & Delivery
  category: Docker Compose Organization
  priority: MUST
  type: infrastructure
  description: A packaged demo deploy ships every upstream the demo config names, not only the file-backed
    ones. Three of the demo's sources are served by Provisa's own mock backends -- the OpenAPI petstore
    at petstore-mock:8080, the GraphQL shelter at graphql-demo:4000, and the gRPC enrichment/random command
    server at grpc-demo:50071 -- and a deploy that stages the SQLite files alone leaves those three unresolvable,
    so the demo lands half-federated. The packaged tiers build nothing and never see docker-compose.demo.yml's
    build contexts, so the mock sources are baked into the application image under /app/config/demo/servers
    (NOT /app/demo, which docker-compose.app.yml bind-mounts and would shadow) and the generated demo
    overlay runs them as services off that same image -- their only runtime dependencies, starlette, uvicorn,
    strawberry and grpcio, are already installed there, so no additional image is pulled, built or released.
    The gRPC target is repointed at the service, since the config's native default of localhost is wrong
    inside compose.
  use_case: An operator installs the demo on a fresh SaaS node; the pet, shelter, and enrichment sources
    federate on first boot with no manually started mock servers.
  tests:
  - tests/unit/test_desktop_install.py
  code:
  - Dockerfile
  - packaging/linux/first-launch.sh
  - config/provisa-install.yaml
  scenario: 'Given a packaged install that selected the demo

    Then the demo overlay declares petstore-mock, graphql-demo and grpc-demo off the provisa image

    And the application image carries their sources under /app/config/demo/servers

    And the app container''s gRPC target names the grpc-demo service rather than localhost

    '
  related_reqs:
  - REQ-816
  - REQ-831
- id: REQ-1469
  status: complete
  group: 11. Platform, Infrastructure & Delivery
  category: SaaS Billing
  priority: MUST
  type: behavioral
  description: 'A Starter org signs itself up with a credit card and can answer, without asking anyone,
    what it owes right now and when the card will be charged. Three surfaces, all of them org-scoped and
    all reading the same numbers the invoice will carry. (1) SIGN-UP: an org with no subscription is sent
    to the Lemon Squeezy hosted checkout of REQ-1075 -- the card is entered at the merchant of record
    and never reaches Provisa, which is the whole reason the MoR is there -- and the subscription is linked
    to the org by the webhook, not by the browser''s return trip, so a closed tab still lands the plan.
    (2) CURRENT BILL: the org sees its month-to-date charge broken into the plan''s fixed monthly fee
    and whatever metered usage has accrued against it, dated so it is clear which period is being shown.
    A bill assembled from Provisa''s own meter rather than from the MoR would disagree with the invoice
    the customer receives, so the fixed component comes from the subscription and the metered component
    from the usage records Provisa has already posted -- never from an estimate computed a third way.
    (3) NEXT CHARGE: the renewal date and the amount expected on it are stated explicitly, because the
    complaint a card charge generates is always that it was a surprise. Cancelled and past-due subscriptions
    say so on the same panel rather than showing a next charge that will not happen. Payment method changes,
    invoice history and cancellation are handed to the Lemon Squeezy customer portal instead of rebuilt.
    The entry point is the account (person) menu, not the Admin group: the Admin group holds the operational
    settings of the org, whereas the plan, the running bill and the next charge are the commercial relationship
    behind the account. The route stays /admin/billing and keeps org_settings; the menu item is shown
    only where the deployment mounts the billing routes (billing on /auth/me) and only to the right that
    owns the subscription. Each endpoint names its subject with an org_id the caller supplies, so each
    one checks it: the org_settings RIGHT in the org being acted in (REQ-1337 -- never a role name), or
    cross_org for a control-plane caller. The checkout paths also admit the account that reserved the
    org, because an org awaiting checkout is not bindable (REQ-1476) and its creator would otherwise be
    locked out of the checkout that makes it real.'
  use_case: A Starter customer signs up at $18/mo with a card, opens Billing a fortnight later, sees the
    month-to-date amount and the date the card renews, and needs no support ticket to learn either.
  related_reqs:
  - REQ-1075
  - REQ-1447
  - REQ-1288
  since: 2026-08
  code:
  - provisa-ui/src/components/admin/BillingTab.tsx
  - provisa-ui/src/api/billing.ts
  - provisa-ui/src/components/NavBar.tsx
  - provisa_commercial/router.py
  - provisa_commercial/access.py
  - provisa_commercial/lemonsqueezy_client.py
  - provisa/api/auth_router.py
  scenario: 'Given a Starter org with no subscription and no trial

    When it opens Billing

    Then it is offered the trial and sent to the Lemon Squeezy hosted checkout

    And an org on a subscription sees its month-to-date period, the plan''s fixed fee and the metered
    quantities accrued against it

    And the next charge names the renewal date and the fixed amount

    And a cancelled or past-due subscription says so instead of naming a next charge

    And a deployment without the commercial plugin shows no Billing surface at all

    '
  tests:
  - .claude/commercial/tests/test_billing_summary.py
  - .claude/commercial/tests/test_billing_access.py
  - provisa-ui/src/__tests__/BillingTab.test.tsx
  - provisa-ui/src/__tests__/adminNavCapabilities.test.ts
  - tests/unit/test_auth_me_billing_flag.py
- id: REQ-1471
  status: proposed
  target: 2026-Q4
  group: 13. Multi-Tenancy & Organizations
  category: Federation Engine
  priority: MUST
  type: behavioral
  description: 'A shard that has idled to zero begins its cold start at SIGN-IN, not at the first query.
    The node provision REQ-1448 accepts is roughly 90-120s, and charging it to the first query puts the
    whole of it inside a request the operator is watching. Sign-in is the earliest moment the platform
    knows which shard the session will use, and it is followed by however long the operator spends reading
    a screen and composing a question -- time the wake can run in. The IdP owns login, so there is no
    server-side login POST; the sign-in seam is the first authenticated call of the session, GET /auth/me,
    and the org it reports is the org to warm. The prewarm must not block that call: /auth/me returns
    while the wake runs behind it, so a session over a shard the user never queries costs nothing but
    a scheduled pod. A query arriving mid-warm waits on the wake already in flight rather than starting
    a second one, and a prewarm that fails is reported where the query path can act on it rather than
    failing a sign-in over an engine the user has not yet asked for.'
  use_case: An operator signs in, spends a minute reading the schema browser, runs a query and gets an
    answer without waiting on a node that has been coming up the whole time.
  code:
  - provisa/federation/engine_wake.py
  - provisa/api/auth_router.py
  scenario: 'Given a shard scaled to zero

    When the session''s first authenticated call reaches GET /auth/me

    Then the call returns without waiting and the shard''s cold start has already begun

    And a query issued before the shard is ready waits on that same wake rather than starting another

    '
  related_reqs:
  - REQ-1448
  - REQ-1447
- id: REQ-1472
  status: proposed
  target: 2026-Q4
  group: 2. Authentication & Identity
  category: Authentication
  priority: MUST
  type: behavioral
  description: The break-glass superuser of REQ-125 can sign in from a browser under any auth provider.
    The credential is a username and password, but a deployment fronted by an external IdP -- Firebase
    requires an email address as the username, and issues no token for an account it does not hold --
    has no path by which that account reaches the UI, so the operator's only recourse when the IdP is
    misconfigured is the very console the misconfiguration locks them out of. POST /auth/superuser-login
    is mounted for every provider and exchanges the configured credentials for a session token signed
    with auth.jwt_secret, carrying a type claim that distinguishes it from a user session the basic provider
    signed with the same key. The middleware checks that token ahead of the configured provider and falls
    through to the provider when the bearer is not one of ours, so an IdP token keeps working unchanged.
    Without auth.jwt_secret there is no signing key and the exchange refuses with 503 rather than issuing
    a token under a default key. The login page offers the operator path as a secondary affordance, which
    is also the sign-in an end-to-end suite uses when it runs against a deployment whose provider is an
    external IdP.
  use_case: An operator whose Firebase tenant is misconfigured signs in to the console with the break-glass
    account and repairs it; a Playwright run against the cloud deployment authenticates the same way.
  code:
  - provisa/auth/superuser.py
  - provisa/auth/middleware.py
  - provisa/auth/wiring.py
  - provisa/api/auth_router.py
  - provisa/api/setup_router.py
  - provisa-ui/src/pages/LoginPage.tsx
  - provisa-ui/playwright.cloud.config.ts
  - provisa-ui/e2e/cloud-setup.ts
  - provisa-ui/src/lib/sessionToken.ts
  - provisa-ui/src/components/OnboardGate.tsx
  - provisa/api/rest/openapi_spec.py
  scenario: 'Given a deployment whose auth provider is firebase and whose superuser credentials are configured

    When POST /auth/superuser-login is called with those credentials

    Then a session token is returned and is accepted on every authenticated surface as platform admin

    And a token signed with another key, or a user session signed by the basic provider, is refused

    And the exchange answers 503 when auth.jwt_secret is unset

    '
  related_reqs:
  - REQ-125
  - REQ-124
- id: REQ-1474
  status: complete
  since: 2026-08
  target: 2026-Q3
  group: 11. Platform, Infrastructure & Delivery
  category: Pricing & Tiering
  priority: MUST
  type: behavioral
  description: 'The REQ-1455 free evaluation period is offered once per PERSON, and the record of it outlives
    the org that consumed it. orgs.trial_started_at enforces "once per org" and is exactly as durable
    as the org row, which REQ-1300 lets an org_admin delete: delete the org, create another, sign up again,
    and the same account draws a second free 14 days, repeatable forever, each one holding a share of
    the shared lane and each one ending in a zero-dollar authorisation instead of revenue. Eligibility
    therefore lives in trial_grants, a control-plane table with no foreign key to orgs and nothing cascading
    to it -- org_id is kept as evidence of which org consumed the grant and may name an org that no longer
    exists. A person is recognised by two independent identities, either of which refuses the next trial:
    the authenticated account''s normalized email, and the Lemon Squeezy customer id, which a customer
    who changes address keeps because the merchant of record matches a returning buyer to the customer
    record it holds. Normalization is narrow and only folds spellings the mail host itself treats as one
    mailbox -- case, surrounding space, sub-addressing tags, and Gmail''s dot-insensitivity -- because
    refusing a first trial to someone who has not had one is the more expensive mistake. An address that
    is not an address raises rather than becoming a key that matches nothing. Enforced at the webhook,
    not only at the checkout: POST /billing/trial/start refuses an ineligible account with 409 billing.trial_already_used
    and requires a signed-in identity with an email (401 otherwise, since an anonymous request cannot
    be held to a once-per-person rule), but a minted checkout URL can be reached again and Lemon Squeezy
    will create a second trialling subscription for it. subscription_created is the point every route
    to a subscription passes through, so open_trial decides eligibility there. An ineligible signup is
    NOT refused -- the customer keeps the subscription they just authorised and it starts PAID Starter:
    the provider is charged first (end_trial_at), then the plan and status are written, and trial_started_at
    stays NULL so the REQ-1455 sweep never sees it. Refusing at the webhook would leave a trialling subscription
    live at the merchant of record with no trial recorded in Provisa, which is the one outcome that costs
    money and entitles nothing. The trial belongs to the ORG, so the grant is spent by everyone working
    inside a trialling org, not only by the buyer: joining one -- by redeeming an invite at registration,
    by redeeming one with an existing account, or by REQ-1269 auto-join -- records the joiner''s grant
    against that org. Without it an org on trial could mint an unlimited supply of fresh evaluation periods
    by inviting alternate accounts that each walk away still holding an unspent one. Bound only while
    the org is actually trialling (trial_started_at set, converted_at still NULL): someone invited into
    an org that already converted joined a paying customer and consumed no free evaluation. A principal
    carrying no email address identifies no person and binds nothing; a non-empty value that is not an
    address raises, because that is a broken identity claim rather than an anonymous one. The core reaches
    this through provisa.core.commerce (REQ-1473) and it no-ops entirely on a self-hosted deployment,
    which has no trial to spend. A refusal is recoverable, not a dead end: billing.trial_already_used
    names both readings of the refusal -- bought a trial before, or joined an org that was trialling --
    and gives an address to write to for a reset, because the person who only ever accepted an invite
    spent nothing they knew about and a lost prospect costs more than the trial being refused. The reset
    is the deletion of one trial_grants row.'
  use_case: A customer whose trial ended deletes their org and signs up again; they get the Starter plan
    they were going to be billed for, not another free evaluation period.
  code:
  - provisa_commercial/trial_eligibility.py
  - provisa_commercial/trial.py
  - provisa_commercial/router.py
  - provisa_commercial/schema.py
  - provisa/core/commerce.py
  - provisa/api/auth_router.py
  - provisa/auth/middleware.py
  scenario: 'Given an account that has already been granted the trial on an org it has since deleted

    When it creates a new org and starts a Starter signup checkout

    Then POST /billing/trial/start refuses with billing.trial_already_used

    And a subscription created anyway begins on paid Starter with no trial window

    And the grant is still recorded against that account after the original org is deleted

    And an account invited into an org while its trial was running is refused one of its own

    '
  tests:
  - .claude/commercial/tests/test_trial_eligibility.py
  related_reqs:
  - REQ-1300
  - REQ-1455
  - REQ-1473
  - REQ-1355
- id: REQ-1475
  status: complete
  since: 2026-08
  target: 2026-Q3
  group: 11. Platform, Infrastructure & Delivery
  category: Pricing & Tiering
  priority: MUST
  type: behavioral
  description: 'A Lemon Squeezy store sells more than one product, and it delivers EVERY subscription
    event to EVERY webhook configured on it, each one signed with that webhook''s own secret. A sibling
    product''s subscription therefore reaches POST /billing/webhook with a signature that verifies, and
    the payload carries no marker saying which product it belongs to except the variant id. The variant
    is the test: plan_for_variant_id resolves only the variants this deployment sells, so an event naming
    a variant it does not recognise is not Provisa''s and is answered 200 with {"received": true, "ignored":
    "foreign_variant"} before any org is resolved or any row written. The no-op is a SUCCESS rather than
    a rejection because Lemon Squeezy records a 4xx as a failed delivery, retries it, and disables a webhook
    whose failures persist -- so refusing the other product''s traffic would eventually take Provisa''s
    own events down with it. Answering 200 also keeps the remaining 400s meaning what they should: a Provisa
    subscription with a broken payload, not routine cross-product noise. An event carrying no variant
    id at all is deliberately NOT filtered here, because that is a payload this deployment cannot interpret
    rather than one it can attribute elsewhere, and billing.webhook_missing_variant still refuses it.'
  code:
  - provisa_commercial/router.py
  - provisa_commercial/models.py
  scenario: 'Given a Lemon Squeezy store that sells both Provisa Starter and another product

    When a subscription event for the other product''s variant is delivered with a valid signature

    Then the webhook answers 200 and writes nothing

    And an event for the configured Provisa variant is handled normally

    And an event carrying no variant id is still refused with billing.webhook_missing_variant

    '
  tests:
  - .claude/commercial/tests/test_billing_lemonsqueezy.py
  related_reqs:
  - REQ-1075
  - REQ-1455
  - REQ-1456
- id: REQ-1476
  status: complete
  since: 2026-08
  target: 2026-Q3
  group: 11. Platform, Infrastructure & Delivery
  category: Pricing & Tiering
  priority: MUST
  type: behavioral
  description: 'On a deployment that sells the org, an org is not built until a subscription pays for
    it. POST /admin/orgs registers the row in provisioning_state awaiting_checkout, grants the creator
    membership and spawns nothing: between create and checkout the row is a RESERVATION -- a claimed id
    and nothing else, with no schema, no engine and no data. The subscription_created webhook is the only
    path out of that state, flipping the row to provisioning and starting the build (begin_provisioning,
    which returns False on a redelivery so a repeated webhook builds nothing twice). Because the row is
    not an org anyone can work in, awaiting_checkout is excluded from bindable memberships, from the active-org
    resolution and from auto-join. A reservation holds its id for 30 minutes and is then deleted, releasing
    both the name and the slot it held against the per-user org cap; a creator returning inside that window
    gets their own reservation back rather than a 409, so the UI resumes the checkout they abandoned.
    If the webhook never lands, POST /billing/checkout/reconcile recovers on the return trip: Lemon Squeezy
    carries no custom data on the subscription, so the buyer email is the only key back, and a candidate
    must be a variant this deployment sells, created after the reservation, and bound to no org -- ambiguity
    is refused (409) rather than guessed. The reservation policy lives in the commercial plugin; a self-hosted
    deployment has nothing to charge for and provisions on create as before.'
  code:
  - provisa/api/admin/orgs_router.py
  - provisa/core/commerce.py
  - provisa/core/org_membership.py
  - provisa_commercial/reservations.py
  - provisa_commercial/router.py
  - provisa-ui/src/pages/OnboardOrgPage.tsx
  scenario: 'Given a commercial deployment

    When a signed-in account creates an org

    Then the row is awaiting_checkout and nothing is provisioned

    And the checkout overlay opens on the Starter trial

    And the subscription_created webhook flips the row to provisioning and builds the org

    And a redelivered webhook builds nothing further

    And a reservation abandoned for 30 minutes releases its id

    And the creator returning inside that window resumes the checkout

    And a completed checkout whose webhook never arrived is reconciled on the return trip

    And a self-hosted deployment still provisions immediately on create

    '
  tests:
  - tests/unit/test_org_create_provisioning.py
  - .claude/commercial/tests/test_org_reservations.py
  related_reqs:
  - REQ-1266
  - REQ-1269
  - REQ-1311
  - REQ-1455
  - REQ-1469
- id: REQ-1477
  status: complete
  since: 2026-08
  target: 2026-Q3
  group: 13. Multi-Tenancy & Organizations
  category: Multi-Tenancy & Orgs
  priority: MUST
  type: behavioral
  description: 'Auto-join (REQ-1269) treats a matching email address as proof that the person belongs
    to the org, which only holds for a domain the org controls. A consumer mailbox provider -- gmail.com,
    outlook.com, yahoo.com, icloud.com, proton.me and the rest of the set -- issues addresses to anyone
    who asks, so a rule matching one would hand membership of an org, with its data and its default role,
    to any stranger who signs up at that provider. An org policy that turns auto_join on is therefore
    rejected with orgs.auto_join_public_domain when its email_rule admits a consumer domain, including
    the case of no rule at all (which admits every address). The rule is probed by matching it against
    a sample address at each domain rather than by reading the regex, so it reaches the same verdict here
    as it will at sign-in, and an unanchored or over-broad rule is caught as surely as one naming the
    domain outright. The same check runs again in resolve_auto_join_orgs, so a row written before the
    rule existed or edited straight into the table admits nobody. A public-domain email_rule remains legal
    with auto_join off: it bounds who may redeem an invite, and an invite is still a human decision.'
  code:
  - provisa/api/admin/orgs_router.py
  - provisa/core/org_membership.py
  scenario: 'Given an org admin setting the org join policy

    When they turn auto_join on with an email rule matching gmail.com

    Then the request is refused with orgs.auto_join_public_domain naming the domain

    And turning auto_join on with no email rule at all is refused the same way

    And a rule for a domain the org controls is accepted

    And the same rule with auto_join off is accepted

    And a stored public-domain auto_join rule joins nobody at sign-in

    '
  tests:
  - tests/unit/test_org_join_policy.py
  related_reqs:
  - REQ-1268
  - REQ-1269
  - REQ-1306
- id: REQ-1478
  status: complete
  since: 2026-08
  target: 2026-Q3
  group: 13. Multi-Tenancy & Organizations
  category: Multi-Tenancy & Orgs
  priority: MUST
  type: behavioral
  description: 'A person can end up in an org without doing anything: an email-rule match auto-joins them
    at sign-in (REQ-1269), or an administrator adds them. Either way an org they never asked for appears
    in their switcher with no explanation of where it came from. Each membership therefore records how
    it came about -- joined_via is created, invite, auto_join or admin -- and whether the member has been
    told, in acknowledged_at. Both columns are nullable, so add_missing_columns reconciles existing registries;
    a NULL joined_via predates the column and explains nothing, so it announces nothing. A membership
    born of the user''s own act (creating the org) is written already acknowledged and is never announced.
    Every membership write site routes through membership_values/grant_membership so the provenance is
    recorded once per path, and a re-grant leaves the first way in -- and its acknowledgement -- standing,
    so an admin re-adding a member does not resurrect a notice already dismissed. /auth/me returns joined_via
    and acknowledged with each membership; the UI raises a one-time notice above the app shell naming
    the org and how it was joined, with POST /auth/acknowledge-join to dismiss it. A membership the user
    did not ask for (auto_join, admin) also offers the exit -- the existing leave route, which records
    the auto-join opt-out (REQ-1306) -- while an invitation, which they chose to accept, is explained
    without one.'
  code:
  - provisa/core/schema_admin.py
  - provisa/core/org_membership.py
  - provisa/api/auth_router.py
  - provisa/api/admin/orgs_router.py
  - provisa/auth/middleware.py
  - provisa-ui/src/components/JoinNotice.tsx
  scenario: 'Given a user whose email address matches an org auto-join rule

    When they sign in for the first time

    Then they are shown a notice naming the org and saying their email address joined them to it

    And dismissing it records the acknowledgement so the notice does not return

    And the notice offers to leave the org they did not ask to join

    And a user who accepted an invitation is told so, with no leave option

    And an org the user created raises no notice at all

    '
  tests:
  - tests/unit/test_join_provenance.py
  - tests/unit/test_org_create_provisioning.py
  - provisa-ui/src/__tests__/JoinNotice.test.tsx
  related_reqs:
  - REQ-1266
  - REQ-1269
  - REQ-1306
  - REQ-1477
- id: REQ-1482
  status: complete
  since: 2026-08
  target: 2026-Q4
  group: 11. Platform, Infrastructure & Delivery
  category: SaaS Billing
  priority: SHOULD
  type: behavioral
  description: 'The egress overage the REQ-1454 meter records is billed through a SECOND Lemon Squeezy
    subscription per org, because a variant carries exactly one usage-based price and the plan variant''s
    is already the REQ-1281 active hour -- leaving the $0.48/GB transfer charge with no metered target
    on the plan. Each plan names its own egress variant by environment variable (LEMONSQUEEZY_VARIANT_EGRESS_STARTER
    and its per-size Pro successors); the variant''s graduated tiers hold the plan''s included GB, so
    Provisa posts the org''s TOTAL transfer and the merchant of record applies the allowance, keeping
    the invoice''s egress line its own arithmetic rather than a number Provisa computes twice (REQ-1469).
    Lemon Squeezy has no API to create a subscription, only a checkout, so the transfer subscription is
    ordered by POST /billing/egress/checkout against the org''s plan -- an org on trial is priced as Starter,
    the tier it converts to. Its webhook is recognized BEFORE any plan branch and outside claim_subscription_event:
    it carries a Provisa variant and a real org but is not a plan, so falling through would overwrite
    orgs.ls_subscription_id, and the claim stamp is one column per org that two subscriptions would take
    turns discarding. Its writes are idempotent instead. Binding records ls_egress_subscription_id and
    ls_egress_item_id at every activation including a trial one; egress_reported_through opens only at
    the PAID start, so the trial''s bytes stay free, and trial conversion moves BOTH subscriptions'' trial_ends_at
    to the same instant so the org keeps one billing anniversary. The hourly sweep totals each org''s
    newly closed egress over [egress_reported_through, current hour), posts the whole GB as a usage record
    and persists the sub-GB remainder in egress_carry_bytes -- usage records are integers, and truncating
    would lose most of the traffic of an org that transfers under a GB an hour. A refused post leaves
    the watermark and the carry untouched, so the next sweep re-totals the span and posts it once. GB
    means GiB, matching the trial allowance.'
  use_case: A Starter customer is charged for the data they transfer above their included 25 GB, on the
    same invoice and the same anniversary as their active hours, without Provisa computing a transfer
    charge that could disagree with the one they are billed.
  related_reqs:
  - REQ-1281
  - REQ-1452
  - REQ-1454
  - REQ-1455
  - REQ-1469
  - REQ-1475
  code:
  - .claude/commercial/provisa_commercial/schema.py
  - .claude/commercial/provisa_commercial/models.py
  - .claude/commercial/provisa_commercial/org_db.py
  - .claude/commercial/provisa_commercial/usage.py
  - .claude/commercial/provisa_commercial/router.py
  - .claude/commercial/provisa_commercial/trial.py
  tests:
  - .claude/commercial/tests/test_usage_reporting.py
  - .claude/commercial/tests/test_billing_lemonsqueezy.py
  - .claude/commercial/tests/test_trial_lifecycle.py
  scenario: 'Given a paid Starter org with a transfer subscription whose egress watermark is 12:00, and
    which egressed 2.5 GiB in the 12:00 hour and 0.75 GiB in the 13:00 hour

    When the reporting sweep runs at 14:10

    Then a usage record of quantity 3 is posted against the org''s EGRESS subscription item, its active
    hours are posted separately against the plan item, egress_reported_through advances to 14:00 and egress_carry_bytes
    holds the remaining 0.25 GiB

    And a sweep whose post is refused leaves both the watermark and the carry where they were

    And an org with no transfer subscription reports its hours and nothing else'
- id: REQ-1483
  status: complete
  since: 2026-08
  group: 12. Migration & Compatibility (Hasura)
  category: Hasura Migration
  priority: SHOULD
  type: behavioral
  description: 'The Hasura v2 and DDN converters (REQ-417, REQ-183) exist only as CLIs that read a project
    DIRECTORY and write a config file someone then loads, which puts a migration behind shell access to
    the deployment. Admin -> Import Hasura Config runs the SAME parsers and mappers over an upload, gated
    on org_settings because an org owns its own semantic layer. One staging module turns the single artifact
    an administrator actually has -- a zip of the project directory, the consolidated document export_metadata
    returns (bare or in its {resource_version, metadata} envelope), or a lone .hml -- into the directory-or-document
    shape the existing converters take; a zip is expanded under a bounded size with entries that escape
    the destination refused, and exactly one wrapper directory is stepped through so DDN subgraph names
    come from the project, not from the folder someone happened to zip. The import is two calls because
    the conversion guesses in places: POST /admin/import/hasura/preview converts and returns the generated
    YAML, the converter warnings and per-kind counts while writing NOTHING, and POST /admin/import/hasura/apply
    loads the YAML the administrator approved -- including any edits made in the preview -- through the
    settled org sequence (catalog names first per REQ-1266, then load_config, source pools, PK resolution,
    schema rebuild), never a second loader. replace defaults to off so an import merges; turned on it
    is load_config full-replace semantics.'
  use_case: An administrator migrating off Hasura converts their metadata export in the admin UI, reads
    what the conversion produced and what it could not convert, corrects the connection details in place,
    and applies it to their org -- without a shell on the deployment.
  related_reqs:
  - REQ-183
  - REQ-417
  - REQ-1266
  - REQ-1349
  code:
  - provisa/api/admin/import_router.py
  - provisa/import_shared/upload.py
  - provisa/hasura_v2/parser.py
  - provisa-ui/src/components/admin/ImportTab.tsx
  - provisa-ui/src/api/importer.ts
  tests:
  - tests/unit/test_hasura_import_router.py
  - provisa-ui/src/__tests__/ImportTab.test.tsx
  scenario: 'Given an administrator holding org_settings and a consolidated Hasura v2 metadata export

    When they upload it and choose Convert and preview

    Then the generated configuration, the converter warnings and the source/table/role counts are returned
    and nothing is written to the org

    And applying the previewed YAML loads it into the acting org and rebuilds its schemas

    And an upload that is neither Hasura v2 metadata nor a DDN project is refused with a 400

    And a caller without org_settings is refused with a 403'
- id: REQ-1484
  status: complete
  since: 2026-08
  group: 1. Access Governance & Security
  category: Federation-Wide Provenance Graph
  priority: SHOULD
  type: behavioral
  description: 'A column carries two names and they answer different questions. The SQL and GraphQL surface
    only ever shows the EXPOSED name -- table_columns.alias falling back to apply_sql_name(column_name)
    -- so views, materialized views, metric expressions, RLS predicates, DQ contracts, metric-view grains
    and MV row keys are authored against it and break when it is RENAMED as well as when the column goes.
    Relationships, glossary bindings, tag assignments, the watermark column and column presets store the
    PHYSICAL column_name, which survives the table upsert''s wholesale column replace, so they break only
    on a REMOVAL. Nothing in the table-edit mutation path checked either. columnDependents(tableId, renamed,
    removed) answers what a pending edit would break: the downstream views and MVs come from slicing the
    federation lineage graph (REQ-1160/REQ-1161) at the column''s exposed name, and the artifacts that
    graph does not cover come from a direct registry scan. The query is asked BEFORE the save, because
    a renamed column is still located by the exposed name it currently carries. The warning is ADVISORY
    -- the dialog lists the affected artifacts and the administrator decides -- because the estate''s
    consumers cannot all be reached and a block would strand the edit. Free-SQL-text scans match the column
    as an identifier token rather than resolving scope: over-reporting is the safe direction for a warning.'
  use_case: An administrator renaming a column's SQL alias or dropping a column is told, before the save
    lands, which views, materialized views, metrics, policies and bindings name that column -- and saves
    anyway if that is the intent.
  related_reqs:
  - REQ-1160
  - REQ-1161
  - REQ-939
  - REQ-686
  code:
  - provisa/lineage/dependents.py
  - provisa/api/admin/column_dependents.py
  - provisa/api/admin/schema_query.py
  - provisa-ui/src/pages/tables/columnDiff.ts
  - provisa-ui/src/pages/TablesPage.tsx
  tests:
  - tests/unit/test_column_dependents.py
  - provisa-ui/src/__tests__/columnDiff.test.ts
  scenario: 'Given a registered column whose exposed name a downstream view selects

    When the administrator changes that column''s SQL alias and saves

    Then the dependent view and any materialized view built on it are listed before the save lands

    And confirming saves the edit unchanged

    And a column whose removal only breaks physical-name references reports those on removal, not on a
    rename'
- id: REQ-1485
  status: complete
  since: 2026-08
  group: 11. Platform, Infrastructure & Delivery
  category: Email Delivery
  priority: SHOULD
  type: behavioral
  description: 'Transactional messages carry a branded HTML alternative alongside the plain-text part.
    The message is multipart/alternative and the text part stays canonical: it names the org, the inviter,
    the role, the expiry and the redemption link on its own, so a text-only client loses nothing. The
    branded part is table layout with inline styles drawn from the product palette (ink #1F2933, accent
    #10B981, primary #4f46e5) and contains no images or external stylesheets -- the wordmark is text,
    so a recipient blocking remote content does not see a broken message. User-supplied values (org name,
    inviter, role, link) are HTML-escaped. The branded part travels through the EmailSender port, so both
    the SMTP and Resend adapters send it without a call-site change (REQ-1330).'
  use_case: An invitation that arrives looking like the product it invites the recipient into, while remaining
    readable and complete for clients that render text only or block remote content.
  related_reqs:
  - REQ-1330
  - REQ-1310
  code:
  - provisa/core/mail.py
  tests:
  - tests/unit/test_mail_port.py
  - tests/integration/test_invite_delivery.py
  scenario: 'Given a SaaS-mode deployment with a mail provider configured

    When an org admin invites a member by email

    Then the message is multipart/alternative with a branded HTML part and a complete text part

    And the branded part carries the redemption link, the org, the inviter, the role and the expiry

    And it references no external images or stylesheets'
- id: REQ-1486
  status: complete
  since: 2026-08
  group: 13. Multi-Tenancy & Organizations
  category: Org Branding
  priority: SHOULD
  type: behavioral
  description: 'An org admin puts their organization''s own light branding on the surfaces its people
    meet at the org''s address: a display name, a logo, a primary and an accent color, a welcome line
    on the sign-in page, and a sentence added to every invitation the org sends. The field set is fixed
    and every value is validated at the edit -- colors are six-digit hex, text is capped per surface (display
    name 80, welcome 400, invite 1000), an unknown field is refused rather than dropped, and an emptied
    field clears rather than storing blank. The logo is stored as bytes on the org row (PNG, JPEG, SVG
    or WebP, at most 256 KiB) and served from the platform''s own origin, so the sign-in page depends
    on no tenant-supplied remote host; an SVG is served under a Content-Security-Policy of "default-src
    ''none''; style-src ''unsafe-inline''; sandbox" with X-Content-Type-Options nosniff. Branding is read
    without a bearer, because it dresses the page that renders before a token exists: the addressed org
    follows REQ-1276 -- the Host subdomain, or on the control-plane host the x-org-provisa header (a fetch)
    or an ?org= parameter (an <img>, which sends no headers of the page''s choosing). An address naming
    an org that does not exist answers exactly as an address naming none, so the endpoint cannot be walked
    to enumerate org ids. Writes are gated by the org''s own org_admin (or a platform admin) and replace
    the whole document rather than merging. Branding is additive: an org that set none is presented exactly
    as the product itself.'
  use_case: An organization signing in at its own subdomain sees its own name, mark and colors, and its
    invitations arrive carrying them, without the platform hosting a per-tenant theme.
  related_reqs:
  - REQ-1276
  - REQ-1348
  - REQ-1485
  - REQ-1300
  code:
  - provisa/core/org_branding.py
  - provisa/api/branding_router.py
  - provisa/api/admin/orgs_router.py
  - provisa/api/admin/invites_router.py
  - provisa/core/mail.py
  - provisa/core/schema_admin.py
  - provisa/auth/middleware.py
  - provisa-ui/src/api/branding.ts
  - provisa-ui/src/lib/orgBranding.ts
  - provisa-ui/src/components/OrgBrandingHeader.tsx
  - provisa-ui/src/components/OrgBrandingSettings.tsx
  - provisa-ui/src/pages/LoginPage.tsx
  - provisa-ui/src/pages/TeamPage.tsx
  - provisa-ui/src/main.tsx
  tests:
  - tests/unit/test_org_branding.py
  - tests/unit/test_org_branding_router.py
  - tests/unit/test_mail_port.py
  - tests/integration/test_invite_delivery.py
  scenario: 'Given an org admin who has set a display name, colors, a welcome line and a logo for their
    organization

    When someone opens the sign-in page at that organization''s subdomain

    Then the organization''s name, logo, welcome line and colors are shown without any token

    And an invitation the organization sends carries its display name, its sentence and its primary color

    And an organization that set no branding is presented exactly as the product itself'
- id: REQ-1487
  status: proposed
  target: 2026-Q4
  group: 13. Multi-Tenancy & Organizations
  category: Environments
  priority: SHOULD
  type: behavioral
  description: 'An organization runs its governed model in more than one environment -- a dev environment,
    a test environment, a staging environment, whatever it names -- without becoming more than one organization.
    An environment is a named copy of the org''s governed model; the name is the org''s to choose, and
    one environment named prod exists from the organization''s creation and cannot be deleted or renamed,
    so there is always an environment the address resolves to. Identity, membership, branding, subscription
    tier and the org''s address stay at the org: environments multiply the model, never the tenant. An
    environment is not a hostname label -- REQ-1276 gives the leftmost label to the org and REQ-1348 rejects
    a deeper one as hostile -- so the selected environment travels as an x-provisa-env request header,
    mirroring x-org-provisa, with the selection persisted per user. A request naming an environment the
    org does not have is refused; a request naming none is served by prod, which is the only environment
    every org is guaranteed to have.'
  use_case: A team develops a change against its dev environment and runs its suite against its test environment,
    both inside the one organization its people, invoices and address belong to.
  code:
  - provisa/core/environments.py
  - provisa/auth/middleware.py
  - provisa/core/db.py
  - provisa-ui/src/lib/env.ts
  - provisa-ui/src/components/EnvironmentSelector.tsx
  scenario: 'Given an organization with environments prod and dev

    When a request carries x-provisa-env: dev

    Then it is served from the dev environment''s governed model

    And a request carrying no environment header is served from prod

    And a request naming an environment the org does not have is refused rather than served by prod

    And prod cannot be deleted or renamed

    '
  related_reqs:
  - REQ-1276
  - REQ-1348
  - REQ-1488
  - REQ-1489
- id: REQ-1488
  status: proposed
  target: 2026-Q4
  group: 13. Multi-Tenancy & Organizations
  category: Environments
  priority: SHOULD
  type: behavioral
  description: 'An environment is physically a schema of its own. An org''s governed model already lives
    in one Postgres schema reached by SET search_path TO org_<org_id>; an environment extends that name
    rather than adding a discriminator column to forty tables, so every existing repository query is correct
    in a environment without being rewritten and an environment''s state cannot leak into another environment''s
    reads by a forgotten predicate. The org''s default environment keeps the existing schema name so an
    organization that never creates an environment is unchanged. Environment creation, deletion and renaming
    are org_admin acts recorded in the org''s admin audit log; deleting an environment drops its schema
    and its store, and prod is refused. (Amended 2026-08-21:) An environment is created BY loading a model
    into a name, not by a ceremony of its own. There is no create-then-populate step and no state in which
    an environment exists and holds nothing: loading a build, a config, a set of files or a sha into a
    name that has none creates that environment, provisions its schema and its stores, and writes its
    registry row in one act. The row is what the schema cannot hold -- the plan ceiling and expiry of
    REQ-1523, protected (REQ-1504), per-source boundness (REQ-1491), the environment a credential addresses
    (REQ-1503), drifted (REQ-1524) -- so creation is implicit and the record of it is explicit. The name
    is validated before anything is provisioned, and the environment is unbound when the load finishes,
    whatever it was loaded from.


    (Amended 2026-08-21:) because each environment is a separate runtime, invalidating an org invalidates
    its branches with it: an org is deleted or re-provisioned as a whole, and a branch runtime left behind
    would hold pools and compiled schemas for a schema that no longer exists.

    '
  use_case: An organization adds a dev environment and every governed read in it is scoped by the same
    search_path mechanism that already isolates orgs, with no query rewritten.
  code:
  - provisa/api/org_runtime.py
  - provisa/api/app.py
  - provisa/core/environments.py
  - provisa/core/db.py
  - provisa/core/org_ids.py
  - provisa/core/schema_org.py
  - provisa/api/admin/environments_router.py
  scenario: 'Given an org_admin who creates an environment named dev

    When a request is served in that environment

    Then its governed reads run under that environment''s own schema

    And deleting the environment drops that schema and its store

    And an attempt to delete prod is refused

    '
  tests:
  - tests/unit/test_env_runtime_routing.py
  related_reqs:
  - REQ-1293
  - REQ-1487
  - REQ-1491
  - REQ-1523
  - REQ-1524
- id: REQ-1489
  status: proposed
  target: 2026-Q4
  group: 13. Multi-Tenancy & Organizations
  category: Environments
  priority: MUST
  type: behavioral
  description: 'Copying an environment carries the governed model and nothing else, and what it carries
    is stated as an allow-list rather than an exclusion list, so a table added to the org schema later
    does not travel by default. Every table in the org schema falls in exactly one of four classes. CARRIED
    -- the governed model: domains, naming_rules, registered_tables, table_columns, relationships, metrics,
    roles, rls_rules, tags, tag_param_values, tag_assignments, glossary terms and their refs, edges and
    experts, materialized_views, calendars, kafka_topics, api_endpoints, tracked_functions, tracked_webhooks,
    table_meta_links. IDENTITY ONLY -- sources, api_sources, kafka_sources and kafka_sinks carry their
    id, type and governance fields while their connection values stay behind (REQ-1491). NEVER -- org_secrets,
    user_directory, user_role_assignments, and the org_settings keys that name an external target or a
    per-environment runtime (metadata_export, redirect, cache). NEVER -- the runtime and evidence tables,
    which belong to the environment that produced them: mv_refresh_log, mv_delta_ledger, relationship_candidates,
    creation_requests, api_endpoint_candidates, live_query_state, file_source_mtimes, node_ids, rel_ids,
    query_audit_log, query_sla_log, source_catalog_cache, events, event_status, node_freshness_state,
    preserved_snapshots, admin_audit_log, and catalog_bindings, whose vendor_ref and physical_key address
    another environment''s external catalog. A test enumerates every table declared in schema_org.py and
    fails on any name carrying no classification, so the failure mode for a forgotten table is a red test
    rather than a copied secret. (Amended 2026-08-21:) A source''s mapping is a per-connector JSON bag
    rather than a column set, and it demonstrably holds both credentials and instance identity -- credentials_json,
    certificate_path, certificate_password (REQ-729), storage_config, storage_type, disable_ssl_validation
    -- so classifying sources by column alone would carry a service account into a dev environment. mapping
    is therefore classified PER KEY, not per column, by the connector that owns it: each connector class
    declares the mapping keys that are BINDINGS, and every other key is governance and travels. The declaration
    is default-deny -- a key the connector does not name is a binding and stays behind -- so a connector
    added later cannot leak by omission, and a connector that declares nothing carries no mapping at all.
    The same classification test enumerates the connector registry and fails on a connector whose mapping
    keys carry no declaration.'
  use_case: A platform team adds a table to the org schema and learns from a failing test that they must
    say whether it belongs to an environment copy, instead of discovering months later that it carried
    credentials into a dev environment.
  code:
  - provisa/connectors/base.py
  - provisa/core/env_copy.py
  - provisa/core/schema_org.py
  tests:
  - tests/unit/test_env_copy_classification.py
  scenario: 'Given an environment copy from prod to dev

    When the copy completes

    Then dev holds prod''s governed model

    And dev holds no secret, no user, no role assignment and no runtime history from prod

    And a table in schema_org.py with no classification fails the classification test

    '
  related_reqs:
  - REQ-1487
  - REQ-1490
  - REQ-1491
  - REQ-1492
  - REQ-1524
- id: REQ-1490
  status: proposed
  target: 2026-Q4
  group: 13. Multi-Tenancy & Organizations
  category: Environments
  priority: MUST
  type: behavioral
  description: 'Copying into an environment that already exists is a merge, not a replacement. The carried
    classes of REQ-1489 are merged by identity -- an object present in the source environment is created
    or updated in the target, and the merge reports what it will add, change and leave alone before it
    is applied -- while every excluded class in the target is left exactly as it was. A dev environment
    that has been bound to its own database, granted to its own developers and given its own secrets does
    not lose any of them by taking a newer model from prod, and prod does not acquire dev''s bindings
    or grants by taking a change back. Deletion is explicit: an object the source environment no longer
    has is removed from the target only when the merge is applied with removals, which is stated in the
    report and is a separate confirmation, so a merge cannot silently empty an environment. A merge applies
    whole or not at all.'
  use_case: A team promotes a model change from dev to prod and prod keeps its own production connections,
    its own grants and its own secrets throughout.
  code:
  - provisa/core/env_copy.py
  - provisa/api/admin/environments_router.py
  - provisa-ui/src/components/EnvironmentMergeDialog.tsx
  tests:
  - tests/unit/test_env_merge.py
  scenario: 'Given a dev environment bound to its own sources and granted to its own developers

    When prod''s governed model is merged into it

    Then dev''s model matches prod''s

    And dev''s bindings, grants and secrets are untouched

    And an object prod no longer has is removed only when removals are confirmed

    And a merge that fails part way leaves the environment as it was

    '
  related_reqs:
  - REQ-1489
  - REQ-1491
  - REQ-1492
- id: REQ-1491
  status: proposed
  target: 2026-Q4
  group: 13. Multi-Tenancy & Organizations
  category: Environments
  priority: MUST
  type: behavioral
  description: 'Where an environment points is a per-environment fact that no copy ever supplies. A source
    row travels between environments because registered_tables references it and dropping it would cascade
    the model away, but its host, port, database, username, path and federation_hints do not, and neither
    do an api_source''s base URL and encrypted auth, a kafka_source''s bootstrap servers or a kafka_sink''s
    target. A source that has not been bound in an environment is marked unbound rather than left blank:
    an empty host is not an absent one, and the connection builder reads it as localhost:5432, so a blanked
    copy would connect a dev environment to whatever database is local to the node. The query path checks
    boundness before it builds any connection and refuses a query naming an unbound source, naming the
    source and the environment. Secret references are per-environment for the same reason -- one ${env:...}
    reference resolves identically in every environment of a process, so a carried reference is a carried
    credential; a environment''s bindings resolve through that environment''s own secret keys. (Amended
    2026-08-21:) mapping is stripped by the same rule and for the same reason: the keys the owning connector
    declares as bindings -- an object store''s endpoint and bucket, a certificate path and its password,
    an inline credentials document, a TLS-verification override -- are per-environment facts and stay
    behind with host and port, while the governance keys in the same bag travel. A source whose connector
    declares bindings is unbound in the new environment until every declared binding key has been supplied
    there, so partial binding is unbound, not half-connected. (Amended 2026-08-21, creation:) CREATING
    an environment ALWAYS strips bindings, whatever it was created from. This is absolute and takes no
    argument: a build of another environment, a config an operator uploads, a set of exported files (REQ-1502)
    and a repository sha (REQ-1524) all produce an environment in which every source is unbound. A config
    an operator brings carries connection values and carrying them is ordinarily its purpose, but at creation
    they are dropped and the operator binds afterwards -- one extra act, in exchange for the guarantee
    that NO SINGLE ACTION CAN CREATE A BOUND ENVIRONMENT, so no action can accidentally create one bound
    to production. Making the behaviour depend on where the model came from would put the guarantee behind
    a judgement about provenance, and the case that gets it wrong is the case that matters. Binding is
    therefore always a second, deliberate act against an environment that already exists, and REQ-1499
    is the only way credentials ever arrive by copy. (Amended 2026-08-21, checkout keeps bindings:) Loading
    a different branch or sha into an environment that ALREADY EXISTS is a checkout, and it is not the
    creation of REQ-1488: it replaces the carried classes wholesale -- not a merge by identity, since
    a checkout is what the operator asked for -- and it KEEPS the environment''s bindings, secrets, members
    and grants untouched. That follows from the tree never having held them: there is nothing in the incoming
    tree to overwrite them with. It is also the point of the whole arrangement -- switch to a colleague''s
    branch and keep pointing at your own database -- and it is why creation strips unconditionally while
    checkout strips nothing: creation has no bindings to preserve, and a checkout''s bindings were established
    by the deliberate act this requirement demands. A source that arrives in the incoming tree and has
    no binding in this environment is unbound in the marked sense above, not broken. (Amended 2026-08-21:)
    The second act this requirement demands does not have to be a form: REQ-1529''s binding profiles make
    it a single click on a target an org_admin authored, which keeps the guarantee while removing the
    reason to avoid branching. (Amended 2026-08-21, WHERE the model came from decides whether bindings
    are stripped:) The unconditional strip above is narrowed to what it was actually protecting against
    -- a model arriving from OUTSIDE this organization''s control plane. An uploaded config, an exported
    file set (REQ-1502), a foreign repository sha and any other import produce an environment in which
    every source is unbound, absolutely and with no argument, because the connection values in them were
    authored elsewhere and nobody here vouched for what they point at. A branch taken from the organization''s
    OWN environment inside the same control plane is not that case: the credentials never leave the org,
    are never disclosed to the brancher, and were already reachable by whoever could read that environment.
    Such a branch INHERITS its parent''s bindings, and the developer types nothing. TWO CONDITIONS MAKE
    THAT SAFE, and both are structural. First, an inherited binding is a REFERENCE to the parent environment''s
    binding and never a copy of it: no host, username or secret key is materialized in the branch, so
    a branch discloses nothing, rotating the parent''s credential re-points the branch in the same act,
    and revoking it revokes the branch''s reach. Second, an inherited binding is READ-ONLY -- a write
    through one is refused whatever rights the writer holds, including the environment authority of REQ-1528.
    That is precisely REQ-1492''s compound failure taken apart: the branch inherits where its parent points
    but never the ability to write there, so a branch of prod can read prod-shaped data and cannot damage
    prod. Writing needs a binding the environment established for itself -- a REQ-1529 profile an org_admin
    authored, or a credential an org_admin issued (REQ-1503) -- which is the deliberate second act this
    requirement has always demanded, now demanded only where a write is actually wanted. A branch therefore
    reaches exactly what its creator could already reach, in exactly the mode they could already reach
    it, and no single action still creates an environment that can WRITE to production. (Amended 2026-08-21,
    the mode is the BASE''s:) whether an inherited binding is read-only is not fixed at read-only for
    every branch -- it is the branch policy REQ-1529 puts on the base the branch came from, defaulting
    to read-only. A branch of a base bound to a dev database may write to it if the org_admin marked that
    base branch-writable; a branch of a base bound to production may not, because nobody marked it so.
    The guarantee is unchanged in the case it exists for: no member action produces an environment that
    can write where an org_admin did not say branches may write. (Amended 2026-08-22, THE BRANCH POLICY
    IS WITHDRAWN, REQ-1539:) the read-only-by-default mode and the branch-writable flag that widened it
    are both removed. They answered the wrong question. What a person may do to DATA is what their ROLES
    say, in a branch exactly as in the org, and an environment is a namespace for the MODEL rather than
    a second permission system layered over the one that already answers it. The compound failure this
    requirement names is still taken apart, but at the half that actually caused it: the authority REQ-1528
    confers on whoever creates an environment no longer includes write, full_results or usage, so no member
    action produces write authority over data anywhere. What survives on the write path is REQ-1491 alone
    -- a write through a source unbound here and in everything it inherited from is refused, because there
    is no established target to write to.'
  use_case: A newly created dev environment is inert until an administrator says what it reaches, and
    cannot reach production by inheriting a connection or a credential reference.
  code:
  - provisa/core/env_bindings.py
  - provisa/core/schema_org.py
  - provisa/core/database.py
  - provisa/core/secrets.py
  - provisa-ui/src/components/EnvironmentBindings.tsx
  tests:
  - tests/unit/test_env_bindings.py
  - tests/integration/test_env_deploy.py
  scenario: 'Given an environment copied from prod

    When a query names a source that has not been bound in the new environment

    Then the query is refused naming that source and that environment

    And no connection is attempted with a defaulted host or port

    And the credential reference resolved is the new environment''s, not the source environment''s


    Given a member branches the organization''s own prod environment


    Then the branch inherits prod''s bindings by reference, and the member enters no credentials


    And no host, username or secret key is materialized in the branch


    When they attempt a write through an inherited binding


    Then it is refused whatever rights they hold, because an inherited binding is read-only


    Given a model arriving from outside this control plane -- an upload, an export, a foreign sha


    Then every source in the environment it creates is unbound, with no exception


    '
  related_reqs:
  - REQ-125
  - REQ-1489
  - REQ-1490
  - REQ-1496
  - REQ-1499
  - REQ-1502
  - REQ-1524
  - REQ-1529
  - REQ-1528
- id: REQ-1492
  status: proposed
  target: 2026-Q4
  group: 13. Multi-Tenancy & Organizations
  category: Environments
  priority: MUST
  type: behavioral
  description: 'Write authority is granted into an environment and never carried between environments.
    Provisa executes mutations against sources -- compiled INSERT, UPDATE and DELETE, OpenAPI POST/PUT/PATCH/DELETE,
    gRPC methods that declare side effects, Hasura mutations, ingest DDL -- so a copied environment is
    a writer, not a reader, and inheriting where it points together with permission to write there is
    the compound failure. An environment copy therefore resets table_columns.writable_by to empty and
    carries no user_role_assignments, which restores the default-deny of REQ-867 and REQ-868 in the new
    environment. Role definitions do travel, because rls_rules and writable_by reference roles by id and
    an environment whose RLS points at absent roles states a governance model it does not have. Restoring
    write in an environment is an act recorded in that environment''s admin audit log, and promotion toward
    prod can never confer it: rights in a target environment come only from grants made in that environment.
    (Amended 2026-08-21:) REQ-1491 now takes this compound apart at the binding rather than at the copy:
    a branch of the org''s own environment inherits where its parent points, by reference, and an inherited
    binding refuses writes. Inherited reach without inherited write authority is not this failure.'
  use_case: A developer with write rights in dev acquires none in prod when the model is promoted, and
    an environment cloned from prod cannot mutate anything until someone grants it.
  code:
  - provisa/core/env_copy.py
  - provisa/security/mutation_authz.py
  - provisa/security/rights.py
  - provisa/core/repositories/table.py
  tests:
  - tests/unit/test_env_write_authority.py
  scenario: 'Given a prod environment whose roles hold write on registered columns

    When an environment is copied from it

    Then the new environment''s columns are writable by no role

    And the new environment holds no role assignment

    And the roles the RLS rules reference still exist

    And granting write in the new environment is recorded in its admin audit log

    '
  related_reqs:
  - REQ-1489
  - REQ-1528
  - REQ-867
  - REQ-868
  - REQ-869
  - REQ-1529
- id: REQ-1493
  status: proposed
  target: 2026-Q4
  group: 13. Multi-Tenancy & Organizations
  category: Environments
  priority: SHOULD
  type: behavioral
  description: 'Every environment owns a writable store, because a non-prod environment needs data that
    is neither a read of production nor absent. A regulated organization cannot let developers read production
    rows at all, so a read-only binding to production is not a weaker version of the answer but the wrong
    one; and an environment evolving a data structure needs a shape production does not yet have, which
    no binding to production can hold. The environment store is where both live: a structure the environment''s
    model declares and no source carries is created there, and a dataset is populated by landing an extract
    through the governed read path, so column masking and row-level security are applied by the reading
    environment before any row is written to the receiving environment''s store. The store is the boundary
    an auditor can inspect: the dev store holds only what governance permitted out of prod, and holds
    it whether or not the binding is later changed.'
  use_case: A team develops against a masked extract and against tables that do not exist in production
    yet, without any developer holding a credential that reads production rows unmasked.
  code:
  - provisa/federation/store_writer.py
  - provisa/core/environments.py
  - provisa/ingest/ddl.py
  - provisa/api/admin/environments_router.py
  tests:
  - tests/integration/test_env_store.py
  scenario: 'Given a dev environment and a masked column in prod''s model

    When an extract is landed from prod into the dev environment''s store

    Then the rows written carry the masked value, not the source value

    And rows excluded by prod''s row-level security are not written

    And a table declared only in the dev environment''s model exists in the dev store

    '
  related_reqs:
  - REQ-1491
  - REQ-1494
  - REQ-1495
- id: REQ-1494
  status: proposed
  target: 2026-Q4
  group: 13. Multi-Tenancy & Organizations
  category: Environments
  priority: SHOULD
  type: behavioral
  description: 'Masking gains a deterministic form so a masked extract stays usable. Column masking today
    is redaction -- regex, constant or truncate -- which is correct for a rendered value and destroys
    a key: constant-masking a customer id collapses every row onto one value, and every join through that
    column in the receiving environment is wrong. A keyed deterministic mask maps a value to a stable
    surrogate, so equal inputs mask equal, unequal inputs mask unequal, and joins and cardinality survive
    the extract while the original value does not. The key is per-org and per-environment-pair, held with
    the org''s secrets and never carried by an environment copy, so a surrogate cannot be reversed by
    anyone holding only the extract, and two extracts taken for different environments do not correlate.'
  use_case: A developer joins a masked customer table to a masked orders table in a dev environment and
    gets the same row counts production would give, without either table holding a real identifier.
  code:
  - provisa/security/masking.py
  - provisa/core/schema_org.py
  - provisa/core/env_extract.py
  tests:
  - tests/unit/test_masking_deterministic.py
  scenario: 'Given a column masked deterministically and referenced by a relationship

    When an extract is landed into another environment

    Then equal source values carry equal surrogates and unequal values carry unequal ones

    And a join through that column returns the cardinality the source join would

    And the surrogate cannot be reversed without the key, which the environment copy did not carry

    '
  related_reqs:
  - REQ-1493
  - REQ-1489
- id: REQ-1495
  status: proposed
  target: 2026-Q4
  group: 13. Multi-Tenancy & Organizations
  category: Environments
  priority: MAY
  type: behavioral
  description: An environment populates a table from its own model when no source holds one. A structure
    being designed exists in the environment's model before it exists anywhere else, so there is nothing
    to extract and nothing to mask; the model itself -- column types, nullability, unique constraints,
    relationships and the tags that classify a column -- is enough to generate rows that satisfy it. Generated
    data honours declared keys and referential relationships so a generated set is joinable, and is marked
    as generated in the environment's store so it can never be mistaken for an extract of anything real.
  use_case: A team designs a new fact table and its dimensions in a dev environment and queries it end
    to end before any source has been changed.
  code:
  - provisa/core/env_generate.py
  - provisa/core/schema_org.py
  tests:
  - tests/unit/test_env_generate.py
  scenario: 'Given a table declared only in an environment''s model, with a relationship to another such
    table

    When rows are generated for it

    Then the rows satisfy the declared types, nullability and unique constraints

    And the relationship joins to generated rows on the other side

    And the table is marked in the store as generated

    '
  related_reqs:
  - REQ-1493
  - REQ-1320
- id: REQ-1496
  status: proposed
  target: 2026-Q4
  group: 13. Multi-Tenancy & Organizations
  category: Environments
  priority: SHOULD
  type: behavioral
  description: 'An environment''s governed model is versioned as immutable builds, which is what makes
    promotion and reversal the same operation with a different argument. Every change to an environment''s
    carried classes advances that environment''s build, identified and addressable; a build records what
    the model was, never the excluded classes, so a build is safe to hold and safe to show. Promoting
    is merging a named build of one environment into another under REQ-1490, and reverting is merging
    that environment''s previous build into itself, so an environment can be returned to a state it held
    without anyone reconstructing it by hand. A build''s diff against another build is readable before
    it is applied. This is metadata only: no data moves between environments when a build is applied.
    (Amended 2026-08-21:) A build is physically a COMMIT in the environment''s repository (REQ-1524),
    and the build identifier is that commit. Immutability, addressability, parentage and the readable
    diff between two builds are then properties of the storage rather than machinery Provisa writes and
    must itself prove correct. The previous build an environment reverts to is the commit''s parent, and
    a build safe to hold and safe to show is one whose tree holds only the carried classes of REQ-1489.
    What does NOT follow from the storage is the merge: REQ-1490''s merge is semantic and is computed
    against the tenant control plane, then written through -- git''s textual three-way would resolve two
    edits into a model that parses and does not hold, a relationship whose other side was dropped on the
    branch it merged with. The repository stores the result of a merge; it never computes one. (Amended
    2026-08-21, reverting:) A revert is a merge of the earlier build WITH REMOVALS, never without. REQ-1490
    makes removal a separate confirmation because a merge must not silently empty an environment, and
    that default is right for a promotion -- prod does not lose what dev never had -- but wrong for a
    revert, where an object added after the build being returned to is exactly what the revert exists
    to take away. A revert offered without removals is not a partial revert; it is one that keeps the
    change being reverted. The removals a revert would perform are therefore named first in its report,
    and a revert of a protected environment takes the approval of REQ-1504 like any other merge into one.
    (Amended 2026-08-21, the load is the gate:) The earlier refusal here -- that git must never compute
    a merge, because a textual three-way would produce a model that parses and does not hold -- is narrowed
    rather than kept. Merging happens in the organization''s forge and CI (REQ-1524), so git does compute
    three-way merges of the serialized model, and the protection moves to where it belongs: a TREE IS
    NOT A MODEL UNTIL A LOAD ACCEPTS IT. Loading validates the whole tree -- every reference resolving,
    every carried class coherent with the others -- and applies it whole or not at all, so a merge that
    produced something that parses and does not hold fails at the load, before any schema holds it, and
    fails naming what does not hold. That failure is an ordinary broken build belonging to whoever merged,
    discovered where they are already looking, and it is a better place for the check than a prohibition
    that would have required Provisa to own the merge. (Amended 2026-08-22:) LOADING A TREE IS AN AUTHORED
    CHANGE, not a read. The repository is a projection and never an authority (REQ-1524), so nothing a
    person hand-edits in a working copy reaches the control plane until a LOAD applies it -- but a load
    applies it whole, and the tree carries ``roles``, so a load into a base is capable of rewriting who
    may do what there. Two things bound that and both are the org''s own to set. On the git side, a base''s
    branch is protected by the host''s own rules: prod requiring a pull request approved by named people
    means no tree reaches prod''s branch unreviewed, and hand-editing a working copy produces a proposal
    rather than a change. On the Provisa side, a load into a protected environment is decided the way
    REQ-1504 decides a merge into one -- a request, a report of what it would change, and an approval
    by someone other than the requester. Neither substitutes for the other: git protects the branch, Provisa
    protects the schema, and a load is the one door between them. (Amended 2026-08-22, the load is performed
    WHERE IT LANDS:) There is no push. A load is not something dev does to prod; it is something a person
    does while signed in to the control plane that will hold the result. Whoever loads prod''s branch
    is signed in to the production control plane, holding a production identity, and is answerable to
    production''s own capability checks at the moment of the load -- the same checks any other authored
    change there passes. This is why the branch being readable to everyone costs nothing: a branch is
    a proposal until someone with the standing to accept it, in the place that would hold it, accepts
    it. It also settles the cross-instance question by removing it, since no control plane ever writes
    to another and a build travels between them only as a tree that a person on the far side chooses to
    load. NOTHING LOADS ITSELF: Provisa runs no watcher, webhook, poller or CI step that applies a merged
    branch to the environment it names. A merge into prod''s branch produces a tree and changes no schema,
    and prod holds what it held until someone loads. An automatic load would hand the forge the authority
    the projection rule denies it, and would make the reviewers of a pull request -- who reviewed text
    -- the people who applied a model to production without ever seeing the report of what it would change.
    (Amended 2026-08-22, the CLI:) The load is available as a command -- ``provisa env deploy`` -- so that
    a deployment pipeline can perform it, and this does not reopen what the paragraph above closed. The
    rule is not that a machine may never load; it is that a load is always an INVOCATION CARRYING AN IDENTITY
    against a NAMED control plane, never something the control plane does to itself on noticing a commit.
    The command is a thin client of the same endpoint the UI calls, with the same target, the same capability
    check and the same report; CI holding a credential that passes that check is the organization delegating
    its own standing, which is the org''s decision to make and is revocable as any other credential is.
    What Provisa does not ship is the other thing: a listener inside the deployment that turns somebody
    else''s merge into a change here. (Amended 2026-08-22, the private control plane:) The same rule is
    what makes a DESKTOP control plane (REQ-1126, REQ-1128) a first-class place to work. A developer runs
    their own Provisa, loads the branch they are working on into it, and has the whole model live in a
    control plane that is theirs -- real schemas, real validation, real queries against sources they bound
    themselves -- while nothing they do is visible to anyone else, because a control plane never writes
    to another one. What leaves the desktop is a branch pushed to the org''s repository, which is a proposal;
    what arrives is a branch fetched, which changes nothing until they load it. The blast radius of working
    this way is exactly one machine, and the review path is the same one every other change takes. (Amended
    2026-08-22, what an approver approves:) A load into a protected environment PINS THE COMMIT at the
    moment it is requested. The caller names a ref, that ref is resolved once, and the sha it resolved
    to is what the report describes, what the request stores and what applying later applies -- the ref
    is kept beside it only because a person recognises a branch name. An approval that re-resolved the
    branch would be an approval of a NAME: the branch moves, and the tree that gets applied is one nobody
    read. Staleness is derived the other way round for a load than for a merge, because the tree cannot
    move: what can have changed is the TARGET, so the pinned tree is re-planned against the environment
    as it now stands, and a report that no longer describes what would happen makes the request stale
    rather than approvable. A load and a merge are the same kind of proposal and are held in one table,
    one listing and one decision path: a row names either a source environment or a source ref, never
    both and never neither. Whether the load SEEDS -- REQ-1539''s creation-only classes -- is part of
    what is proposed, because it changes what applying does. (Amended 2026-08-22, a proposal is not a
    deployment:) When ``provisa env deploy`` produces a request rather than a change, it exits NON-ZERO.
    A pipeline that read a pending approval as success would report a release that has not happened, and
    the point of REQ-1504 is that the release has not happened. (Amended 2026-08-22, WHERE THE LOAD SITS IN THE GIT PROCESS:) Stated plainly,
    because the rest of this requirement states it at length: the git process is the ordinary one --
    branch, commit, pull request, review, merge -- and the LOAD is its final step. Merging is the end of
    the REVIEW; the load is the DEPLOY. Everything up to the merge decides what the model should be and
    changes no schema; the load is the act that moves the merged tree into a runtime. Whatever verb an
    org already uses for that step -- deploy, release, activate, provision -- names this one, and there
    is exactly one of it. It is not an addition to git flow but its last stage made explicit, and the
    only reason it is a separate act at all is that a merge writes text and a runtime holds a schema. (Amended 2026-08-22, THE VERB IS DEPLOY:) that step is
    named DEPLOY throughout -- the CLI is ``provisa env deploy``, the endpoint is ``POST
    /admin/orgs/{org}/environments/{env}/deploy``, the audit actions are ``environment.deploy`` and
    ``environment.deploy_requested``, and the code that performs it is ``provisa/core/env_deploy.py``.
    ``load`` described the mechanism (a tree read into a schema) and left every reader to work out what
    the act MEANT; ``deploy`` is the word every engineering organization already uses for the step that
    moves a merged change into a runtime, and the naming should not make people learn a second one.'
  use_case: An administrator promotes a reviewed dev build into prod, sees a regression, and returns prod
    to its previous build without reconstructing anything by hand.
  code:
  - provisa-ui/src/components/EnvironmentBuilds.tsx
  - provisa/api/admin/environments_router.py
  - provisa/cli.py
  - provisa/core/env_approvals.py
  - provisa/core/env_builds.py
  - provisa/core/env_copy.py
  - provisa/core/env_deploy.py
  - provisa/core/env_repo.py
  tests:
  - tests/integration/test_env_deploy.py
  - tests/integration/test_env_deploy_approvals.py
  - tests/unit/test_env_builds.py
  - tests/unit/test_env_deploy_endpoint.py
  scenario: 'Given an environment whose model has changed since its last build

    When the change is committed

    Then a new immutable build identifies that model

    And promoting that build into another environment merges it under the merge rules

    And merging the previous build back returns the environment to its earlier model

    And no data is moved by either operation

    '
  related_reqs:
  - REQ-1489
  - REQ-1490
  - REQ-1502
  - REQ-1504
  - REQ-1524
- id: REQ-1497
  status: complete
  since: 2026-08
  group: 13. Multi-Tenancy & Organizations
  category: Org Onboarding
  priority: SHOULD
  type: behavioral
  description: 'Creating an organization tells its administrator the two things about it that the workspace
    does not show: that the organization has an address of its own -- its id in front of the deployment''s
    base domain, per REQ-1276 -- and that the site at that address can be dressed in the organization''s
    own name, colors and logo, per REQ-1486. The address is shown once over the welcome screen, copyable
    and openable, and the branding editor is one click away. A deployment whose host names no organization
    -- a desktop install, or localhost -- has no such address, and none is shown rather than a guessed
    one. The product''s mark is drawn from one component, so the navigation bar, the landing page and
    the tour offer carry the same mark rather than three copies of it.'
  use_case: An administrator finishes creating an organization already knowing the address to hand to
    their stakeholders and that they can put their own name on it.
  code:
  - provisa-ui/src/components/OrgAddressModal.tsx
  - provisa-ui/src/components/BrandMark.tsx
  - provisa-ui/src/lib/authHost.ts
  - provisa-ui/src/pages/OnboardOrgPage.tsx
  - provisa-ui/src/pages/TeamPage.tsx
  - provisa-ui/src/tour/TourWelcomeModal.tsx
  - provisa-ui/src/i18n/locales/en/orgAddress.json
  tests:
  - provisa-ui/src/__tests__/orgAddressModal.test.tsx
  - provisa-ui/src/__tests__/crossSubdomainAuth.test.ts
  scenario: 'Given an administrator who has just created an organization on a deployment with a base domain

    When the welcome screen appears

    Then the organization''s own address is shown, copyable and openable

    And the branding editor opens from it in one click

    And on a host that names no organization no address is shown

    '
  related_reqs:
  - REQ-1276
  - REQ-1486
  - REQ-1348
- id: REQ-1498
  status: proposed
  target: 2026-Q4
  group: 13. Multi-Tenancy & Organizations
  category: Environments
  priority: MUST
  type: behavioral
  description: 'Every environment operation is available from the provisa command line, because an environment
    change is rarely the only thing happening: it lands beside a schema change in a source, a deployment,
    a data load or a test run, and those are scripted. The environment subcommands cover the whole lifecycle
    -- listing environments, creating and deleting one, binding its sources, showing an environment''s
    current build, diffing two builds, merging one environment or build into another, and reverting an
    environment to a previous build. Each runs the same code the API runs, so the allow-list of REQ-1489,
    the merge and removal rules of REQ-1490 and the binding and write-authority rules of REQ-1491 and
    REQ-1492 hold identically however the operation was invoked; a command line that could copy what the
    API refuses to copy would be the way secrets leave an environment. Every command reports as text for
    a person and as JSON for a script, exits non-zero when it refuses and says which rule refused it,
    and accepts a dry run that prints the merge report -- what would be added, changed, removed and left
    alone -- without applying it, so a pipeline can gate on the report before it applies anything.'
  use_case: A release script alters a table in the source, then merges the dev environment's build into
    prod and fails the pipeline on a non-zero exit, in the same run and without anyone opening the UI.
  code:
  - provisa/cli.py
  - provisa/cli_envs.py
  - provisa/core/env_copy.py
  - provisa/core/env_builds.py
  tests:
  - tests/unit/test_cli_envs.py
  scenario: 'Given a dev environment and a prod environment

    When provisa env merge --from dev --into prod --dry-run is run

    Then it prints what would be added, changed, removed and left alone, and applies nothing

    And the same command without --dry-run applies the merge under the merge rules

    And a merge the rules refuse exits non-zero naming the rule

    And --json emits the same report as machine-readable output

    '
  related_reqs:
  - REQ-1489
  - REQ-1490
  - REQ-1491
  - REQ-1492
  - REQ-1496
- id: REQ-1499
  status: proposed
  target: 2026-Q4
  group: 13. Multi-Tenancy & Organizations
  category: Environments
  priority: MAY
  type: behavioral
  description: 'An organization may replicate an environment whole -- bindings, secrets, users and role
    assignments included -- when what it wants is the control plane as it stands rather than the governed
    model alone. The replicate is the deliberate opposite of REQ-1489 and is fenced accordingly. Its target
    must be a non-prod environment and never prod, which takes merges only; it is an org_admin act carrying
    an explicit acknowledgement that the target will hold production credentials, recorded in the org''s
    admin audit log with who, when and from where; and the target environment is thereafter marked as
    replicated, which means it is governed as production -- every grant in it reaches production data,
    so it is neither a safe place for developers nor a substitute for the masked extract of REQ-1493.
    Only replicate into an environment whose members already hold the production access it confers. (Amended
    2026-08-21:) A replicate targets an environment that ALREADY EXISTS and never creates one, because
    REQ-1491 makes a newly created environment unbound without exception. The order is the fence: the
    environment is created unbound, its members are established, and only then does an org_admin acknowledge
    that it will hold production credentials and replicate into it. Creating and crediting an environment
    in one act is what this forbids.'
  use_case: An administrator reproduces a production incident in a scratch environment that reaches exactly
    what production reaches, and the environment is marked so nobody mistakes it for a development environment.
  code:
  - provisa/core/env_copy.py
  - provisa/cli_envs.py
  - provisa/api/admin/environments_router.py
  tests:
  - tests/unit/test_env_replicate.py
  scenario: 'Given an org_admin replicating prod into a non-prod environment with the acknowledgement
    given

    Then the target holds prod''s bindings, secrets, users and role assignments

    And the target is marked as replicated and governed as production

    And a replicate whose target is prod is refused

    And a replicate without the acknowledgement is refused

    And the act is recorded in the org''s admin audit log

    '
  related_reqs:
  - REQ-1489
  - REQ-1490
  - REQ-1491
  - REQ-1492
  - REQ-1493
- id: REQ-1500
  status: proposed
  target: 2026-Q4
  group: 13. Multi-Tenancy & Organizations
  category: Environments
  priority: MAY
  type: behavioral
  description: 'A non-prod environment can be exported as a configuration a desktop Provisa loads, so
    a developer works against the environment''s model on their own machine. The export carries the governed
    model and the environment''s own bindings and secret references; whether the desktop can resolve those
    references is the developer''s environment to answer, and an unresolved one fails loudly on first
    use rather than defaulting to something local. What the export does not carry is the environment''s
    store: landed extracts, generated sets and materialized views are data, and an environment holding
    large ones is not reasonable to move through a file. The export states what it left behind and how
    large it was, so the developer knows the difference between an environment that exported whole and
    one whose queries will find nothing materialized. Exporting prod is refused -- the desktop is a separate
    instance with no org governance around it, and a production export is a production credential in a
    file.'
  use_case: A developer takes their dev environment onto a laptop, works offline against the model, and
    knows which materialized views did not come with it.
  code:
  - provisa/cli_envs.py
  - provisa/core/env_export.py
  - provisa/core/environments.py
  tests:
  - tests/unit/test_env_export.py
  scenario: 'Given a non-prod environment with a materialized view and a landed extract

    When it is exported for the desktop

    Then the export carries the governed model, the environment''s bindings and its secret references

    And it carries neither the landed rows nor the materialized view

    And it states what it left behind and how large it was

    And exporting prod is refused

    '
  related_reqs:
  - REQ-1498
  - REQ-1493
  - REQ-1487
- id: REQ-1501
  status: proposed
  target: 2026-Q4
  group: 13. Multi-Tenancy & Organizations
  category: Environments
  priority: MUST
  type: behavioral
  description: 'Environments take their shape from two exemplars, and which one governs is decided per
    half of the design rather than argued case by case. Structure follows Hasura: the governed model is
    a metadata artifact that is exported, diffed and applied, versioned as a build, and moved between
    environments by an explicit apply rather than by editing the target -- so an environment''s model
    is reproducible from something a person can read and a pipeline can hold. Per-environment state follows
    LaunchDarkly: what an environment points at, what it may write, who is in it, what it has materialized
    and which keys address it are environment-scoped facts that a promotion never carries, so promoting
    a change into production changes what production means and nothing about what production reaches.
    The two meet at one rule: an object arriving in an environment by merge arrives inert -- present in
    the model, unbound, writable by no role and materializing nothing -- which is the analogue of a flag
    that exists in every environment and is on in one. Absence and inertness are different states and
    are reported differently.'
  use_case: A team reasons about environments with two familiar mental models instead of one novel one,
    and can predict which facts travel with a promotion before they run it.
  code:
  - provisa/core/environments.py
  - provisa/core/env_copy.py
  - provisa/core/env_builds.py
  scenario: 'Given an object that exists in the dev environment and not in prod

    When the dev build is merged into prod

    Then prod holds the object unbound, writable by no role and materializing nothing

    And prod reports it as present and inert rather than as absent or as ready

    And prod''s own bindings, grants, members, materializations and keys are unchanged

    '
  related_reqs:
  - REQ-1489
  - REQ-1490
  - REQ-1491
  - REQ-1492
  - REQ-1496
- id: REQ-1502
  status: proposed
  target: 2026-Q4
  group: 13. Multi-Tenancy & Organizations
  category: Environments
  priority: SHOULD
  type: behavioral
  description: 'An environment''s governed model can be exported to files, held in version control and
    applied back, which is what makes an environment reproducible rather than merely copyable. Export
    writes the carried classes of REQ-1489 as readable files whose ordering is stable, so two exports
    of the same model are the same bytes and a review sees the change and not the serializer. Apply takes
    those files and brings an environment to what they describe under the merge rules of REQ-1490, and
    diff shows what an apply would do without doing it. An apply that would leave an object unusable --
    a relationship whose other side is absent, an RLS rule naming a role that is not there, a metric over
    a column no longer registered -- reports every such object and applies nothing; a model that is inconsistent
    in a file is inconsistent in an environment, and finding out at query time is finding out too late.
    (Amended 2026-08-21:) Export is not an operation an administrator remembers to run: the files exist
    continuously, because REQ-1524 writes them through on every change. Exporting is therefore reading
    the environment''s repository at a build, and holding the model in version control is what the environment
    already does rather than something done to it afterwards. Stable ordering stops being a courtesy to
    reviewers and becomes required -- an unstable serializer would make every commit a diff of itself
    and bury the change nobody could then find. Apply keeps its meaning unchanged: files describe a model,
    the merge rules of REQ-1490 bring an environment to it, and the consistency check refuses the whole
    apply rather than landing part of one.'
  use_case: A team reviews a model change as a diff in a pull request and applies the merged result to
    an environment from the same files.
  code:
  - provisa/cli_envs.py
  - provisa/core/env_copy.py
  - provisa/core/env_files.py
  - provisa/core/env_repo.py
  tests:
  - tests/unit/test_env_files.py
  scenario: 'Given an environment exported to files

    When the same environment is exported again unchanged

    Then the files are byte-identical

    And applying files that describe an RLS rule naming an absent role reports it and applies nothing

    And diff shows what an apply would change without changing it

    '
  related_reqs:
  - REQ-1490
  - REQ-1496
  - REQ-1498
  - REQ-1524
- id: REQ-1503
  status: proposed
  target: 2026-Q4
  group: 13. Multi-Tenancy & Organizations
  category: Environments
  priority: MUST
  type: behavioral
  description: 'A credential addresses exactly one environment. API keys, service tokens and any other
    non-interactive credential are issued into an environment and carry it, so a key cannot be pointed
    at another environment by changing a header: a request whose credential names one environment and
    whose x-provisa-env header names another is refused rather than resolved in favour of either. This
    is what makes an environment safe to hand out -- an application configured with a dev environment''s
    key reaches dev whatever it sends -- and it is why keys are never carried by a copy or a merge. Rotating
    or revoking an environment''s keys affects that environment alone, and deleting an environment revokes
    its keys with it.'
  use_case: A staging application is configured with a staging key and cannot reach production data even
    if its configuration is wrong or its host is compromised.
  code:
  - provisa/auth/middleware.py
  - provisa/core/environments.py
  - provisa/api/admin/environments_router.py
  tests:
  - tests/unit/test_env_credentials.py
  scenario: 'Given an API key issued into the dev environment

    When a request presents it with x-provisa-env: prod

    Then the request is refused rather than served by either environment

    And revoking the dev environment''s keys leaves other environments'' keys working

    And deleting the dev environment revokes its keys

    '
  related_reqs:
  - REQ-1487
  - REQ-1489
  - REQ-1529
- id: REQ-1504
  status: proposed
  target: 2026-Q4
  group: 13. Multi-Tenancy & Organizations
  category: Environments
  priority: SHOULD
  type: behavioral
  description: 'An environment can be protected, and a merge into a protected environment waits for an
    approval by someone other than the person who requested it. prod is protected when the organization
    has more than one member; any environment can be protected by an org_admin. A request to merge into
    a protected environment creates a pending change carrying the merge report of REQ-1490 -- what would
    be added, changed, removed and left alone -- which an approver reads before deciding; approval applies
    it, rejection discards it, and a change whose source environment has moved on since the request is
    stale and must be re-requested rather than applied against a report that no longer describes it. Requests,
    approvals, rejections and applications are recorded in the org''s admin audit log with who and when.
    (Amended 2026-08-21, the request is a STORED object and the report is the review:) A merge request
    is a row with a lifecycle -- requested, approved, rejected, applied, stale -- and not an ephemeral
    confirmation dialog, because the approver is by definition a different person from the requester and
    is therefore not present at the moment of the request. An ephemeral confirmation would force the approval
    to happen inside the requester''s session, which is the one arrangement the requirement forbids. The
    request carries the requester''s own message alongside the REQ-1490 report, and those two together
    ARE the review: the report is the squash. A textual diff of serialized files would tell the approver
    which lines of YAML moved, while the report tells them which domains, metrics, roles and policies
    were added, changed, removed and left alone -- the objects they are being asked to take responsibility
    for. A per-file two-ref diff (REQ-1526) remains available to an approver who wants to read the text,
    and is never the primary artifact. The direction is the developer''s: they propose a merge FROM the
    environment they hold INTO the protected one, which is why nothing in this flow requires the developer
    to hold rights over the target -- proposing is not writing. (Amended 2026-08-22:) A LOAD (REQ-1496)
    into a protected environment is decided here too, and by the same row: the request names a source
    ref and the sha it was pinned to instead of a source environment, and everything else -- the report
    an approver reads, the refusal of a self-approval, the staleness that makes a report nobody can act
    on, the audit entries -- is unchanged. One protected environment has one door, whether the change
    arrives from a sibling environment or from a commit.'
  use_case: A model change reaches production only after a second person has read what it would do.
  code:
  - provisa/core/env_approvals.py
  - provisa/core/env_copy.py
  - provisa/api/admin/environments_router.py
  - provisa-ui/src/components/EnvironmentApprovals.tsx
  tests:
  - tests/integration/test_env_approvals.py
  - tests/unit/test_env_merge_request_endpoints.py
  scenario: 'Given a protected prod environment and a requested merge from dev

    Then the merge does not apply until it is approved

    And the requester cannot approve their own request

    And the approver sees the merge report before deciding

    And a request whose source build has changed is stale and must be re-requested

    And every step is recorded in the org''s admin audit log

    '
  related_reqs:
  - REQ-1490
  - REQ-1498
  - REQ-1525
  - REQ-1528
- id: REQ-1505
  status: proposed
  target: 2026-Q4
  group: 13. Multi-Tenancy & Organizations
  category: Environments
  priority: SHOULD
  type: behavioral
  description: 'Every environment keeps a change history, and any two environments can be compared for
    one object. The history records what changed in that environment''s governed model, who changed it
    and when, and whether the change was made in the environment or arrived by a merge, naming the source
    environment and build when it did -- so the question a production incident asks, what changed here
    and where did it come from, is answered without reconstructing it from builds. Comparison answers
    the other question: for one table, metric or rule, what does dev say and what does prod say, shown
    as a difference rather than as two documents to read side by side.'
  use_case: An administrator investigating a production behaviour sees the change that introduced it,
    which environment it came from and how prod's version of that object differs from dev's.
  code:
  - provisa/core/env_history.py
  - provisa/core/env_builds.py
  - provisa-ui/src/components/EnvironmentHistory.tsx
  - provisa-ui/src/components/EnvironmentCompare.tsx
  tests:
  - tests/unit/test_env_history.py
  scenario: 'Given a change made in dev and merged into prod

    When prod''s history is read

    Then the change is recorded as having arrived from the dev environment and that build

    And a change made directly in prod is recorded as made in prod

    And comparing one object between dev and prod shows the difference between them

    '
  related_reqs:
  - REQ-1496
  - REQ-1490
- id: REQ-1506
  status: proposed
  target: 2026-Q4
  group: 13. Multi-Tenancy & Organizations
  category: Environments
  priority: SHOULD
  type: behavioral
  description: 'A merge can be scoped to part of a model. Promoting one corrected metric should not require
    promoting every other change the source environment has accumulated, so a merge accepts a selection
    of objects and carries them alone. The selection closes over what the chosen objects require: a metric
    brought without the column it reads, or a relationship without the table on its other side, would
    land inconsistent, so the merge report names the objects pulled in by the selection and the merge
    either carries them or refuses. A scoped merge is a merge in every other respect -- the same exclusions,
    the same untouched target state, the same all-or-nothing application, the same approval where the
    target is protected.'
  use_case: An administrator promotes a single corrected metric to production while the rest of the dev
    environment's work stays in dev.
  code:
  - provisa/core/env_copy.py
  - provisa/cli_envs.py
  - provisa-ui/src/components/EnvironmentMergeDialog.tsx
  tests:
  - tests/unit/test_env_merge_scoped.py
  scenario: 'Given a dev environment with several changes and a selection naming one metric

    When the scoped merge is applied

    Then prod holds that metric and none of the other changes

    And the report names anything the metric required that the selection did not

    And a selection that cannot be closed consistently is refused

    And the target''s bindings, grants and members are untouched

    '
  related_reqs:
  - REQ-1490
  - REQ-1502
  - REQ-1504
- id: REQ-1507
  status: proposed
  target: 2026-Q4
  group: 13. Multi-Tenancy & Organizations
  category: Environments
  priority: SHOULD
  type: behavioral
  description: 'Environments are gated by an entitlement feature, not by a product name. The gate is a
    Feature in provisa/control_plane/entitlements.py with a minimum Tier, checked by require_feature the
    same way every other gated capability is; which packaged product carries that tier is a price-list
    decision recorded in REQ-1452 and may change without touching this gate. The minimum tier is premium,
    because the tiers describe different customers rather than different sizes of the same one. The standard
    tier is bought by a reporting team, an analytics group, a data-science team -- people who model and
    query what already exists, for whom one governed model and its history is the whole need. The premium
    tier is bought by a business whose systems depend on this one: a model change there is a change to
    something in production, so it is developed somewhere else, reviewed and promoted, which is exactly
    what an environment is for. An org below the minimum tier has prod and cannot create another environment;
    the environment API and the environment CLI refuse naming the tier the feature requires rather than
    failing obscurely, and the in-product plan picker names environments as what the upgrade buys, using
    whatever the current product name for that tier is. A developer below the gate is not left without
    a local copy: the file export of REQ-1502 carries the model to a desktop install, where they bind
    their own sources, so the model travels and no credential does. When an org that has environments
    moves below the minimum tier, its non-prod environments are frozen rather than deleted -- no queries
    run in them and nothing merges out of them, while their models stay readable and exportable until
    the org deletes them or is entitled again. Billing changes may not destroy a customer''s work.'
  use_case: A reporting team on the standard tier gets one governed model with its history; a business
    on the premium tier develops changes in a dev environment before they reach production.
  code:
  - provisa/control_plane/entitlements.py
  - provisa/core/environments.py
  - provisa/cli_envs.py
  - provisa/api/admin/environments_router.py
  - provisa-ui/src/components/admin/BillingTab.tsx
  tests:
  - tests/unit/test_env_entitlement.py
  scenario: 'Given an org whose tier is below the minimum tier of the environments feature

    When it attempts to create an environment

    Then require_feature refuses naming the feature and the tier it requires

    And the same attempt from the CLI is refused the same way and exits non-zero

    And the org can still export its model to files for a desktop install

    And an org that moved below the gate keeps its non-prod environments frozen and readable rather than
    deleted

    And demo mode is exempt as it is for every other tier gate

    '
  related_reqs:
  - REQ-1487
  - REQ-1053
  - REQ-1066
  - REQ-1073
  - REQ-1370
  - REQ-1452
  - REQ-1502
- id: REQ-1508
  status: proposed
  target: 2026-Q4
  group: 13. Multi-Tenancy & Organizations
  category: Environments
  priority: MUST
  type: behavioral
  description: 'One word, one meaning, and the two words stay apart. ENVIRONMENT names what an organization
    works in -- prod, dev, test, whatever it creates -- and LANE keeps the federation-engine sense it
    already has in REQ-073, REQ-1243, REQ-1412, REQ-1418, REQ-1447 and REQ-1450, where shared, isolated
    and external say WHERE an org''s engine runs. Neither word may be used for the other in the requirements,
    the admin surfaces, the API fields or the CLI, because they are independent facts about an org and
    read as one when the vocabulary blurs: an org on the shared engine lane can hold several environments,
    and an org on an isolated lane may hold only prod. An org''s environments and its engine lane are
    chosen separately and neither constrains the other.'
  use_case: An administrator reading Org Engine and Environments in the same admin area is never in doubt
    which one says where their engine runs and which says what they are working in.
  code:
  - provisa/core/environments.py
  - provisa/core/commerce.py
  - provisa-ui/src/components/admin
  - docs/arch/requirements.yaml
  scenario: 'Given the admin surfaces and the CLI

    When an org''s engine lane and its environments are shown

    Then the engine lane is called a lane and the environment is called an environment, with neither word
    used for the other

    And an org on the shared engine lane may hold several environments

    And an org holding only prod may be on any engine lane

    '
  related_reqs:
  - REQ-1487
  - REQ-1243
  - REQ-1412
  - REQ-1447
- id: REQ-1509
  status: complete
  since: 2026-08
  group: 11. Platform, Infrastructure & Delivery
  category: SaaS Billing
  priority: MUST
  type: behavioral
  description: 'An organization changes its own plan from the Billing page, in either direction, across
    all four orderable plans -- Starter, Pro S, Pro M and Pro L. A plan change moves the EXISTING Lemon
    Squeezy subscription onto the target plan''s variant; it never opens a second checkout, because a
    second checkout mints a second subscription and leaves the org billed twice. Where the org also holds
    the egress subscription of REQ-1482, that subscription is moved to the target plan''s egress variant
    in the same operation: the plan variant carries the active hour and the egress variant carries the
    included GB, so a plan whose egress variant did not move charges the new rate against the old allowance.
    A change that moves the plan but fails to move the egress variant is reported as a failure naming
    both subscriptions and never reported as success -- the move is a variant assignment, so repeating
    it is safe. Both directions take effect at once and are prorated by the merchant of record: the org
    is charged or credited for the remainder of the period at the difference between the two plans, in
    the manner the merchant of record supports: an upgrade invoices the prorated difference immediately,
    and a downgrade issues a credit that lands on the next invoice, because a negative amount cannot be
    collected now. Every plan change is prorated the same way, including Pro L to Pro M and Pro M to Pro
    S, which are downgrades between Pro sizes and not departures from Pro. The plan written to the org
    is the plan Lemon Squeezy returns, applied through the same watermark the subscription webhook uses
    (REQ-1455), so the subscription_updated event that follows is recognized as already applied and the
    merchant of record stays the single source of truth. Metered reporting follows the plan across the
    change. The active hours and egress accrued but not yet reported are posted against the subscription
    items the org was on BEFORE the variant moves, and only then is the org rebound to the items the change
    returns: usage on these plans is billed in arrears, so hours reported after the swap are charged at
    the plan in force when they are reported, and an unflushed span would bill time spent on Starter at
    the Pro rate or post it against an item the subscription no longer holds. The reporting watermark
    is not reopened by a plan change -- it is the boundary between invoiced and uninvoiced time, and moving
    it back re-posts hours already billed. A subscription the merchant of record will not modify over
    the API -- PayPal being the case they name -- is not a failure and not a silent no-op: the change
    is reported as one the customer must complete in the hosted portal, and the organization''s plan is
    left as it was until the provider says otherwise. A plan change made while the organization is still
    on trial converts the trial rather than running alongside it. Downgrading to Starter is refused while
    the organization holds more than Starter''s source limit: Pro admits 100 sources and Starter admits
    10, and an org moved down while over the ceiling would sit above a limit with no way to be brought
    under it except by Provisa deleting the customer''s sources. The refusal names the current source
    count and the limit so the administrator knows how many to remove. Upgrades and changes between Pro
    sizes carry no such precondition, all three Pro sizes admitting the same number of sources. The plan
    determines the engine lane and size (REQ-1412, REQ-1449) rather than being chosen beside them, so
    a completed plan change is followed by the lane and size the new plan entitles; when the billing change
    succeeds and the engine move does not, both outcomes are reported, because the organization is invoiced
    for the new plan either way. The Billing page offers the plans and their machine shapes as the server
    states them, never as figures typed into the page, so what an administrator compares before changing
    plan is what the invoice will charge for.'
  use_case: An organization that has outgrown Starter moves itself to Pro M from the Billing page and
    has the larger engine within the same session, with no sales contact and no second card entry; one
    that over-bought moves back down and sees the credit on its next invoice.
  code:
  - provisa_commercial/router.py
  - provisa_commercial/usage.py
  - provisa_commercial/lemonsqueezy_client.py
  - provisa_commercial/models.py
  - provisa_commercial/org_db.py
  - provisa-ui/src/components/admin/BillingTab.tsx
  tests:
  - .claude/commercial/tests/test_plan_change.py
  - .claude/commercial/tests/test_plan_lane.py
  - provisa-ui/src/__tests__/BillingTab.test.tsx
  scenario: 'Given an organization on the Starter plan with an active Lemon Squeezy subscription

    When an administrator with billing access changes the plan to Pro M

    Then the existing subscription is moved to the Pro M variant rather than a second checkout being opened

    And the egress subscription, where one exists, is moved to the Pro M egress variant

    And the prorated difference is invoiced immediately

    And unreported hours and egress are posted against the previous subscription items before the change

    And the organization is rebound to the subscription items the change returns, with its reporting watermark
    unmoved

    And the organization is recorded on the Pro M plan with the Pro M source limit

    And the subscription webhook that follows is recognized as already applied

    And the organization is moved to the engine lane and size that Pro M entitles

    Given an organization on a Pro plan holding more than 10 sources

    When an administrator changes the plan to Starter

    Then the change is refused, naming the current source count and the Starter limit

    And the organization stays on its Pro plan and its subscription is unchanged

    Given an organization on Pro L holding 4 sources

    When an administrator changes the plan to Pro M

    Then the change is accepted, its credit applied to the next invoice, no source precondition applying
    between Pro sizes

    Given an organization whose subscription is paid through PayPal

    When an administrator changes the plan

    Then the change is reported as one to be completed in the hosted portal and the recorded plan is unchanged

    Given an organization still on trial

    When an administrator changes the plan

    Then the trial is converted rather than continuing beside the new plan

    '
  related_reqs:
  - REQ-1455
  - REQ-1449
  - REQ-1482
  - REQ-1412
  - REQ-1469
  - REQ-1075
- id: REQ-1510
  status: complete
  since: 2026-08
  group: 11. Platform, Infrastructure & Delivery
  category: SaaS Billing
  priority: MUST
  type: behavioral
  description: 'On a hosted deployment an organization''s engine lane and its engine size are DERIVED
    from its plan, not chosen beside it. A Pro plan of any size runs on the isolated lane at that plan''s
    size; trial and Starter run on the shared lane. Nothing else decides it, and no administrator sets
    it: the lane was a stored choice checked against an entitlement (REQ-1412), so an organization that
    bought Pro and never opened the engine tab kept querying the pooled shard it is no longer paying for,
    and one that moved down kept a dedicated engine its plan no longer sells until somebody noticed. Derivation
    removes the gap by construction -- there is one fact, the plan, and the lane is read from it. The
    external lane is not derived and is not part of this: an organization-operated engine is an enterprise
    arrangement, so it stays available to self-hosted and enterprise deployments and is offered to nobody
    on the hosted plans. An isolated organization''s queries dispatch to a shard of its own, distinct
    from the shared shard it came from, and its shared-lane placement is retained rather than released
    so a return to Starter lands back on the shard it already had (REQ-1450). Where the engines are provisioned
    in a cluster (REQ-1447), a dedicated engine is reached the same way a shared one is -- by waking the
    shard and dialling the pod the cluster reports ready -- and not through a templated hostname the control
    plane has to be told about separately; a deployment that can bring up shared shards can bring up isolated
    ones, so entering the lane must not be refused as unavailable while the cluster is standing right
    there. Waking a dedicated engine restores it at the size its plan sells: a wake that reapplied the
    shared shape would return the organization''s engine on hardware smaller than the one it is invoiced
    for, and the size travels with every wake rather than only with the first provision. A size change
    is a configuration revision and therefore a rollout, so the engine comes back on the hardware the
    organization now pays for instead of drifting from it. Every transition is ordered so that no query
    is released against an engine that cannot serve it: the dedicated engine exists and is ready before
    the organization''s runtime is rebuilt onto it, the runtime rebuild -- which reissues the organization''s
    catalogs, a resumed engine holding none -- happens under the organization registry''s lock, and the
    engine being left is removed only after traffic has moved off it. A transition whose billing already
    succeeded but whose engine move failed leaves the organization on the lane it can still serve from
    and reports the failure, rather than stranding it on a lane with no engine.'
  use_case: An organization that upgrades to Pro M is running on a dedicated engine of that size within
    the same session, and one that returns to Starter is back on the shared shard it started from with
    its dedicated engine released -- neither having chosen a lane, and neither able to end up paying for
    one thing while running on another.
  code:
  - provisa/federation/k8s_provisioner.py
  - provisa/federation/engine_wake.py
  - provisa/federation/engine.py
  - provisa_commercial/entitlements.py
  - provisa_commercial/org_engine_router.py
  tests:
  - tests/unit/test_engine_wake.py
  - .claude/commercial/tests/test_plan_lane.py
  scenario: 'Given a hosted deployment whose federation engines are provisioned in a cluster

    When an organization''s plan becomes Pro M

    Then the organization is placed on the isolated lane on a shard of its own at the Pro M size

    And its shared-lane shard placement is retained

    And its dedicated engine is ready before its runtime is rebuilt onto it

    And the runtime is rebuilt under the organization registry lock so its catalogs are reissued

    Given an organization on the isolated lane whose engine has been idled to zero

    When a query arrives for that organization

    Then the shard is woken at the size its plan sells rather than at the shared shape

    Given an organization on Pro M

    When its plan becomes Pro L

    Then the engine is rolled onto the Pro L hardware rather than continuing on the smaller shape

    Given an organization on the isolated lane

    When its plan becomes Starter

    Then it is returned to the shared shard it previously held

    And its dedicated engine is released only after its runtime no longer dispatches to it

    Given an organization whose plan change was billed successfully

    When the engine move for the new lane fails

    Then the organization stays on a lane that can serve queries and the failure is reported

    '
  related_reqs:
  - REQ-1509
  - REQ-1449
  - REQ-1412
  - REQ-1447
  - REQ-1450
  - REQ-1448
  - REQ-1464
- id: REQ-1511
  status: complete
  since: 2026-08
  group: 11. Platform, Infrastructure & Delivery
  category: SaaS Billing
  priority: MUST
  type: behavioral
  description: 'The Billing page is where an organization changes its plan. It shows the four orderable
    plans -- Starter, Pro S, Pro M and Pro L -- with the price and the machine each one buys, marks the
    one the organization is on, and offers the others as a single action. Every figure on the cards is
    the one the server states, never a rate or a machine shape typed into the page: the page already refuses
    to compute money a second way because a total it derived would disagree with the invoice, and hardware
    is the same kind of claim -- a vCPU count written into the browser drifts from the pod the organization
    is actually given. Before the change is made the page says what will happen to the money, in the terms
    the merchant of record actually uses: an upgrade is charged now for the remainder of the period, and
    a downgrade returns a credit that lands on the next invoice rather than as a refund. It also says
    what will happen to the engine, because a plan change moves the organization between the shared and
    dedicated lanes and a dedicated engine takes a minute or two to come up (REQ-1510); the page reports
    that move as it proceeds and reports its failure if it fails, rather than returning to rest as though
    the only thing that changed was a price. A downgrade to Starter that the server refuses because the
    organization holds more sources than Starter admits is shown as what it is -- the number held and
    the number allowed -- and the page does not decide this for itself: the count that matters is the
    server''s at the moment of the change. A plan change offered to an organization still on trial says
    that it ends the trial and begins billing, since that is what it does. Where the merchant of record
    will not take the change over the API and returns a hosted-portal address instead, the page sends
    the administrator there rather than reporting a change that did not happen.'
  use_case: An administrator comparing Pro sizes sees the machine and the price of each, changes to the
    one they want, is told what will be charged now, and watches their new engine come up -- without leaving
    the product, contacting sales, or entering a card a second time.
  code:
  - provisa-ui/src/components/admin/BillingTab.tsx
  - provisa-ui/src/api/billing.ts
  - provisa_commercial/router.py
  tests:
  - provisa-ui/src/__tests__/BillingTab.test.tsx
  scenario: 'Given an organization on the Starter plan

    When its administrator opens the Billing page

    Then the four orderable plans are shown with the price and machine the server states for each

    And the Starter plan is marked as the one in use

    When the administrator chooses Pro M

    Then the page states that the prorated difference is charged now and that a dedicated engine will
    be started

    And on confirmation the plan changes and the engine move is reported as it proceeds

    Given an organization on Pro L holding more sources than Starter admits

    When its administrator chooses Starter

    Then the refusal is shown with the number of sources held and the number Starter allows

    And the organization stays on Pro L

    Given an organization still on trial

    When its administrator chooses a plan

    Then the page states that the trial ends and billing begins

    Given a subscription the merchant of record will not change over the API

    When its administrator chooses a different plan

    Then the administrator is sent to the hosted portal rather than shown a change that did not happen

    '
  related_reqs:
  - REQ-1509
  - REQ-1510
  - REQ-1449
  - REQ-1469
  - REQ-1455
- id: REQ-1512
  status: complete
  since: 2026-08
  group: 11. Platform, Infrastructure & Delivery
  category: SaaS Billing
  priority: MUST
  type: behavioral
  description: 'On a hosted deployment the engine an organization runs on is reported, not chosen. The
    org engine surface shows the lane the organization''s plan puts it on, the size of the engine that
    lane gives it, and the state of that engine -- running, idle, or coming up -- and offers no control
    that changes any of it. Where a change is wanted it names the Billing page, because the plan is the
    thing that decides this (REQ-1510) and a second control that appeared to decide it would be a control
    that either contradicts the plan or silently agrees with it. The lane selector, the engine-kind list
    and the external endpoint form are absent on hosted deployments rather than shown disabled: an organization-operated
    engine is an enterprise arrangement and is not part of any hosted plan, so it is not something a hosted
    administrator is invited to consider and then refused. Those controls remain on self-hosted and enterprise
    deployments, where an organization does operate its own engine and the lane is genuinely its choice.
    The deployment-wide federation-engine surface -- which selects the engine KIND for the whole installation
    and takes effect at the next restart -- is not offered on a hosted deployment either. It is a question
    an installed Provisa has and a hosted one does not: the engine kind is fixed by the cluster the platform
    runs, so the surface could only report a setting nobody may change or accept one that the next deploy
    overwrites. It stays where it belongs, on installed deployments, gated as it already is.'
  use_case: A hosted organization's administrator opening the engine page learns which engine their queries
    run on and whether it is up, and is sent to Billing to change it -- never faced with a lane or an
    engine kind their plan does not sell, and never able to put their organization on a lane it is not
    paying for.
  code:
  - provisa-ui/src/components/admin/OrgEngineTab.tsx
  - provisa-ui/src/components/admin/FederationEngineTab.tsx
  - provisa-ui/src/components/navGroups.ts
  - provisa_commercial/org_engine_router.py
  tests:
  - provisa-ui/src/__tests__/OrgEngineTab.test.tsx
  - provisa-ui/src/__tests__/navEntryItem.test.ts
  - .claude/commercial/tests/test_plan_lane.py
  scenario: 'Given a hosted deployment

    When an organization administrator opens the org engine surface

    Then the lane, the engine size and the engine state are shown

    And no control is offered that changes the lane, the engine kind or an external endpoint

    And the Billing page is named as where the plan that decides them is changed

    Given a hosted deployment

    When an administrator looks for the deployment-wide federation-engine surface

    Then it is not offered

    Given a self-hosted deployment

    When an organization administrator opens the org engine surface

    Then the lane, engine-kind and external endpoint controls are offered as before

    '
  related_reqs:
  - REQ-1510
  - REQ-1511
  - REQ-1412
  - REQ-1418
  - REQ-916
- id: REQ-1513
  status: complete
  since: 2026-08
  group: 11. Platform, Infrastructure & Delivery
  category: SaaS Billing
  priority: MUST
  type: behavioral
  description: 'The source ceiling a plan sells is enforced where sources are created, not only where
    a plan is downgraded. Starter admits ten data sources and Pro admits one hundred, and those are the
    numbers the Billing plan cards print (REQ-1511); registering a source beyond the ceiling is refused
    with the count, the ceiling and the plan named, and with the Billing page named as where the plan
    is changed. The ceiling is read from the same table the plan cards are priced from, so the number
    an administrator chose the plan for is the number the create path enforces. The sources Provisa seeds
    into every organization -- provisa-admin, provisa-otel and the derived sentinel, which back the meta
    and ops domains -- are excluded from the count. They are written by the built-in seed on every boot
    and every organization build, so an organization that has registered nothing at all still holds three
    rows; counting them would charge the customer for Provisa''s own furniture and would put a brand-new
    Starter organization three sources into an allowance it had not begun to spend. The Starter downgrade
    guard (REQ-1509) reads the same counter, so the ceiling that refuses a create and the ceiling that
    refuses a downgrade can never disagree, and a downgrade is never refused over sources the customer
    has nothing to remove of. A source the organization already holds is not tested: create_source is
    an upsert, and editing the connection details of a registered source adds nothing to the count. No
    ceiling applies on a self-hosted deployment. There is no subscription there, so there is no plan to
    read one from, and bounding an operator''s own installation would paywall software the customer already
    owns.'
  use_case: An administrator of a hosted Starter organization registering an eleventh data source is told
    how many of the ten their plan admits are already in use and is sent to Billing, rather than finding
    the eleventh source registered and the plan card's promise meaningless.
  code:
  - provisa/core/models.py
  - provisa/core/repositories/source.py
  - provisa/core/commerce.py
  - provisa/api/admin/schema_mutation.py
  - provisa_commercial/entitlements.py
  - provisa_commercial/router.py
  - provisa-ui/src/i18n/locales/en/serverErrors.json
  tests:
  - tests/unit/test_source_limit.py
  - .claude/commercial/tests/test_plan_change.py
  scenario: 'Given a hosted organization on the Starter plan holding the ten sources it admits

    When an administrator registers another source

    Then the create is refused naming the count, the ceiling and the plan

    And the Billing page is named as where the plan is changed

    Given an organization that has registered nothing

    When its sources are counted against the ceiling

    Then the built-in meta and ops sources are not counted

    Given a source the organization already holds

    When its connection details are edited

    Then no ceiling is tested

    Given a self-hosted deployment

    When any number of sources is registered

    Then no ceiling applies

    '
  related_reqs:
  - REQ-1509
  - REQ-1511
  - REQ-1510
- id: REQ-1514
  status: complete
  since: 2026-08
  group: 10. UI & Admin Surfaces
  category: Application Shell
  priority: MUST
  type: behavioral
  description: When the deployment stops answering, the whole application says so in one place instead
    of leaving each surface to render a transport error in its own vocabulary. A same-origin request that
    never gets a response -- the server restarting after a deploy, a node waking from an idle stop, a
    dropped network -- rejects with a TypeError, and before this the user saw "TypeError. Failed to fetch"
    printed over a GraphiQL result pane, or a list that simply came up empty, with nothing naming the
    cause or telling them to wait. Reachability is DERIVED from the traffic the app already makes, never
    asserted by a caller - the fetch interceptor that carries the bearer (REQ-1267) samples every same-origin
    request, a rejection marks the deployment unreachable and any response marks it reachable again. An
    HTTP error status is a reachable server, and a caller's own abort is not an outage. The notice is
    a modal, not a banner, and is not dismissible - nothing the user does while the server is down can
    succeed, so the interruption is the message. It sits outside the setup, sign-in and onboarding gates,
    because a server that is not answering is what breaks those too. While it is up, an unauthenticated
    /health probe polls until the deployment answers, so the notice clears itself and the recovery does
    not also cost the user a reload. The failure is re-thrown untouched, so no call site loses the error
    it already handled.
  use_case: A deploy restarts the containers while a user has the query page open; instead of a raw "Failed
    to fetch" over the result pane, they are told the server is restarting and the page resumes on its
    own when it comes back.
  tests:
  - provisa-ui/src/__tests__/serverReachability.test.tsx
  code:
  - provisa-ui/src/lib/serverReachability.ts
  - provisa-ui/src/lib/authFetch.ts
  - provisa-ui/src/components/ServerUnavailableModal.tsx
  - provisa-ui/src/App.tsx
  scenario: 'Given the deployment is restarting

    When the page issues a same-origin request that fails at the transport

    Then the application shows one non-dismissible "Server unavailable" notice

    And the underlying error still reaches the caller that made the request

    Given the server is unreachable

    When it starts answering /health again

    Then the notice clears itself without a reload

    Given a request the caller aborted

    When it rejects

    Then the deployment is not treated as unreachable

    Given a request to a foreign origin fails

    When it rejects

    Then the deployment is not treated as unreachable

    '
  related_reqs:
  - REQ-1267
  - REQ-1466
- id: REQ-1516
  status: complete
  since: 2026-08
  group: 10. UI & Admin Surfaces
  category: Application Shell
  priority: MUST
  type: behavioral
  description: A query that is waiting for a stopped engine to start must say so, and the wait must be
    shortened where it can be. Engine shards idle to zero (REQ-1448, REQ-1464), so the first query after
    a quiet period blocks for the two to four minutes Autopilot takes to provision a node and start Trino.
    Before this the only evidence was a request that had not come back, which is indistinguishable from
    a hung server, so every surface rendered an unexplained spinner for minutes. Three parts. The server
    reports engine state WITHOUT starting it - always-on, ready, starting or stopped - for the engine
    the CALLER'S org actually dispatches to - the shared shard on the pooled lane, the org's own coordinator
    on the dedicated one (REQ-1510), always-on for a BYO engine (REQ-1412) or a non-provisioning install.
    The org is taken off the request, never a parameter, so a Pro org is never told the shared shard's
    state. A drain in progress reports starting, because the Deployment still reports ready throughout
    a stop the next query will have to cancel and pay for anyway. Reporting never wakes and never stamps
    activity, or an idle tab polling would hold a pod up indefinitely. The UI DERIVES the notice from
    traffic it already makes - a same-origin request outstanding past a threshold starts the poll, and
    only the server's answer raises anything, so there is no path allowlist to keep current and a wrong
    guess costs one cheap request. The notice is a banner, not a modal, and does not block - the server
    is answering and every surface that does not touch the engine still works. It shows elapsed time,
    because minutes without a number read as a hang. Finally the prewarm widens - sign-in already prewarms
    (REQ-1471), which covers a session that queries soon after it starts but not the tab left open past
    the idle window, so arriving at the query surface starts the engine while the operator is still composing.
  use_case: An operator returns to a tab left open over lunch and runs a query; instead of a spinner with
    no explanation, they are told the engine is starting and how long it has been - and because opening
    the page already started it, the wait is mostly over before they hit Run.
  tests:
  - provisa-ui/src/__tests__/engineWake.test.tsx
  - tests/unit/test_engine_wake.py
  code:
  - provisa/federation/engine_wake.py
  - provisa/api/data/endpoint_dev.py
  - provisa-ui/src/lib/engineWake.ts
  - provisa-ui/src/lib/authFetch.ts
  - provisa-ui/src/components/EngineWakingBanner.tsx
  - provisa-ui/src/pages/QueryPage.tsx
  - provisa-ui/src/App.tsx
  scenario: 'Given the org''s engine shard has idled to zero

    When a query is issued and stays outstanding past the slow-request threshold

    Then the application says the query engine is starting, with elapsed time

    And the query keeps running behind the notice

    Given the request comes back

    When the engine is serving again

    Then the notice clears itself

    Given a slow request the engine is not responsible for

    When the server reports the engine ready

    Then nothing is shown

    Given an org on the dedicated engine lane

    When it asks for engine state

    Then it is told about its OWN coordinator, not the shared shard

    Given an org running its own external engine

    When it asks for engine state

    Then the answer is always-on

    Given a browser polling engine state

    When the shard is stopped

    Then the poll neither starts it nor counts as activity against the idle reaper

    Given an operator opens the query surface

    When the engine is stopped

    Then a prewarm is issued and returns without waiting for the wake

    '
  related_reqs:
  - REQ-1448
  - REQ-1464
  - REQ-1471
  - REQ-1510
  - REQ-1514
- id: REQ-1515
  status: complete
  group: 8. Client Access & Protocols
  category: API & Integration
  priority: MUST
  type: behavioral
  description: A subscription notification must never abort the write it observes. PostgreSQL refuses
    a NOTIFY payload of 8000 bytes or more, and the REQ-258 trigger fires inside the writer's transaction,
    so a row whose JSON exceeds the limit used to fail the INSERT itself. The trigger measures the payload
    and, when it will not fit, truncates the row's JSON to a text field marked truncated, halving the
    character budget until the encoded envelope fits. The subscriber still sees the change and the leading
    columns; a write never fails for want of a notification.
  use_case: A long statement written to query_audit_log no longer fails the audit insert and surfaces
    to the user as an Internal Server Error.
  code:
  - provisa/subscriptions/pg_triggers.py
  tests:
  - tests/unit/test_pg_notify_trigger_sql.py
  - tests/integration/test_pg_notify_payload_limit.py
  since: 2026-08
  scenario: 'Given a registered PostgreSQL table carrying the subscription notify trigger

    When a row is written whose JSON exceeds the NOTIFY payload limit

    Then the write succeeds and the subscriber receives the change announced without its row body

    '
  integration_test: required
  integration_test_reason: The limit is enforced by PostgreSQL at NOTIFY time; only a real server proves
    the write survives it.
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - app-developer
  - ops
  related_reqs:
  - REQ-258
  - REQ-074
- id: REQ-1517
  status: complete
  group: 8. Client Access & Protocols
  category: Observability
  priority: SHOULD
  type: behavioral
  description: A raw-SQL statement must account for itself the way a GraphQL query does. The GraphQL surface
    builds its execution DAG from the compiled field plan, so a SQL statement — which has no fields —
    used to report a single invented row naming the engine and no diagram at all. The governed plan already
    knows what the statement resolved to, so it carries that account — the semantic sources it scanned,
    the reason the router chose DIRECT or ENGINE, and the labels of the optimizations that fired after
    governance (hot-table inlining, API-cache rewrite, dropped UNION branch). The one pipeline terminal
    records the entry and renders the Mermaid DAG from that plan, so the source table and the diagram
    report the route that actually ran. Each optimization appears as its own node feeding the route, which
    is what explains a source the user expected to see scanned and did not. Surfaces that record their
    own entries — the GraphQL field executors, the Cypher router — leave the terminal's recording off,
    so one statement never produces two rows.
  use_case: A developer runs a statement on Explore / SQL with query stats on and sees the same diagram-and-source
    view the GraphQL surface gives, including which sources the hot-table cache served instead of the
    origin.
  code:
  - provisa/executor/plan_stats.py
  - provisa/executor/stats.py
  - provisa/pgwire/_pipeline.py
  - provisa/api/data/endpoint_dev.py
  - provisa-ui/src/components/MermaidDiagram.tsx
  - provisa-ui/src/pages/sql/ResultsPanel.tsx
  tests:
  - tests/unit/test_plan_stats.py
  - tests/unit/test_endpoint_dev_api.py
  since: 2026-08
  scenario: 'Given a SQL statement run with query stats requested

    When the governed plan routes it and the post-governance optimizations fire

    Then the response carries a Mermaid DAG whose scan nodes, optimization nodes and route node report
    the plan that executed

    '
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - app-developer
  related_reqs:
  - REQ-1516
- id: REQ-1518
  status: complete
  group: 13. Multi-Tenancy & Organizations
  category: Multi-tenancy
  priority: MUST
  type: behavioral
  description: The Arrow Flight transport must dial the shard the ACTIVE org queries, not the one this
    control plane booted on. On a deployment that provisions its engines the Zaychik Flight SQL proxy
    is a sidecar in the shard's pod, so its address is that pod's and it dies with it. A single connection
    opened at boot against the boot shard therefore served every Arrow and streaming query from the shared
    shard's proxy — an isolated-engine org's Arrow result came off the shared coordinator, voiding the
    isolation the org is invoiced for (REQ-1510) — and after any shard restart that connection held a
    released pod address. The transport is resolved at the call instead, per shard and per coordinator
    generation (REQ-1448) — the active org's shard is read the same way the wake reads it, a connection
    is opened to that pod's sidecar and cached, and a generation change closes the stale connection before
    redialing. An org running its own coordinator has no sidecar to dial and is refused rather than routed
    to a shard. Deployments that do not provision engines (desktop, self-hosted, tests) keep the single
    proxy beside the control plane.
  use_case: An org on the dedicated engine lane runs an Arrow Flight or COPY-to-Arrow query and the batches
    come off its own coordinator, not the shared one.
  code:
  - provisa/federation/backend.py
  - provisa/federation/engine_wake.py
  - provisa/federation/trino_lifecycle.py
  - provisa/api/app.py
  tests:
  - tests/unit/test_flight_shard_transport.py
  since: 2026-08
  depends_on:
  - REQ-1448
  - REQ-1510
  scenario: 'Given an isolated-engine org whose shard is serving

    When it runs a query over Arrow Flight

    Then the record batches are drained from that shard''s own Zaychik sidecar, and a restart of that
    shard closes the stale connection and redials the new pod

    '
- id: REQ-1519
  status: complete
  group: 8. Client Access & Protocols
  category: Observability
  priority: SHOULD
  type: behavioral
  description: A developer must be able to see how a statement will be executed without running it, and
    see the Provisa rewrites on the same picture as the engine's own plan. Explore / SQL offers Analyze
    beside Sample; it describes the statement through the ONE pipeline — governance, metric expansion,
    semantic lowering, the post-governance optimizations and the route all apply — and the FINAL routed
    SQL is wrapped in the target's own EXPLAIN at the bottom of the pipeline, so the tree is the tree
    for the statement that would have run. The dialects' shapes (PostgreSQL and DuckDB JSON, Trino and
    MySQL, SQLite QUERY PLAN, indented ANALYZE text) are normalized to one operator tree with rows, cost
    and, when the run form is requested, actual timings. A dialect with no EXPLAIN Provisa knows is refused
    rather than guessed at; so is a write, whose ANALYZE form would perform it, a multi-statement batch,
    and any request that also asks for row delivery. The optimizations that fired are returned beside
    the tree and drawn as their own nodes feeding the route, because they are exactly what the engine
    plan cannot say — a table the user wrote that no scan node mentions was inlined, cache-served or dropped.
  use_case: A developer presses Analyze on Explore / SQL and reads the engine's plan for the statement
    Provisa would actually send, with the hot-table inline and cache rewrites called out.
  code:
  - provisa/executor/explain.py
  - provisa/pgwire/_pipeline.py
  - provisa/api/data/endpoint_dev.py
  - provisa-ui/src/components/MermaidDiagram.tsx
  - provisa-ui/src/pages/sql/ResultsPanel.tsx
  tests:
  - tests/unit/test_explain.py
  - tests/unit/test_endpoint_dev_api.py
  since: 2026-08
  scenario: 'Given a read statement and a role whose governance applies to it


    When the developer asks for it to be analyzed


    Then the routed statement is described by the target''s own EXPLAIN and returned as one operator tree
    with the Provisa optimizations that produced it annotated on the route


    '
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - app-developer
  related_reqs:
  - REQ-1517
- id: REQ-1520
  status: complete
  group: 10. UI & Admin Surfaces
  category: Usability
  priority: SHOULD
  type: behavioral
  description: Every hover hint in the console must be painted by the console. Hundreds of controls carry
    a native title attribute, which the browser draws in its own chrome — its own font, colour and placement
    — so the same hint looks foreign on the Tables page and everywhere else it appears. A single mount
    above every route lifts the attribute off the hovered element, so the browser has nothing to draw,
    and repaints the text as a themed bubble positioned against the element, flipping above it near the
    bottom of the viewport. The attribute is restored when the pointer leaves, on Escape, on scroll and
    on unmount, so the element keeps its accessible name; an element whose title is blank is left alone.
  use_case: A user hovers a control on the Tables page and reads the hint in the app's own type and surface
    instead of the browser's tooltip.
  code:
  - provisa-ui/src/components/TitleTooltips.tsx
  - provisa-ui/src/components/TitleTooltips.css
  - provisa-ui/src/App.tsx
  tests:
  - provisa-ui/src/__tests__/TitleTooltips.test.tsx
  since: 2026-08
  scenario: 'Given a control carrying a native title


    When the pointer rests on it


    Then the hint is drawn in the app''s styling and the title is restored when the pointer leaves

    '
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - data-steward
- id: REQ-1521
  status: complete
  group: 8. Client Access & Protocols
  category: Usability
  priority: SHOULD
  type: behavioral
  description: 'The analyzed plan must be readable at full size. The diagram and the operator list sit
    side by side in the results strip, separated by a rule, which is enough to locate a node but too narrow
    for a wide plan. Hovering the pair reveals an expand control that reopens the identical two panes
    in a modal at 90% of the viewport; both readings are rendered from one component so the inline and
    expanded views can never diverge. The rule between the panes is itself the handle: dragging it sets
    the split (clamped between 15% and 85%, and mirrored under RTL), because whether the tree or the diagram
    needs the room depends on the plan.'
  use_case: A developer expands the analyzed plan to read a wide operator tree without scrolling.
  code:
  - provisa-ui/src/pages/sql/ResultsPanel.tsx
  - provisa-ui/src/pages/sql/ResultsPanel.css
  tests:
  - provisa-ui/src/__tests__/SqlAnalyzePanel.test.tsx
  since: 2026-08
  scenario: 'Given an analyzed statement shown in the results strip


    When the developer uses the expand control revealed on hover


    Then the same diagram and operator list are shown in a modal at 90% of the viewport

    '
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - app-developer
  related_reqs:
  - REQ-1519
- id: REQ-1522
  status: complete
  group: 8. Client Access & Protocols
  category: Observability
  priority: SHOULD
  type: behavioral
  description: 'An analyzed plan must carry the row and cost estimates the target engine already computed,
    whichever engine it is. Each names them somewhere else, and only one naming exists on any given node:
    Postgres states Plan Rows and Total Cost; Trino returns its plan as fragments keyed by id, each node
    carrying an estimates list whose first entry is the chosen alternative (outputRowCount, cpuCost);
    DuckDB''s plan states the optimizer''s guess as the Estimated Cardinality string inside extra_info,
    while its profile reports the rows an operator actually produced in operator_cardinality, which wins
    when both exist; MySQL puts the row estimate on the leaf table as rows_examined_per_scan and its costs
    on each block as query_cost and prefix_cost strings. A node the optimizer could not estimate reports
    no estimate rather than a guess, and a NaN estimate never reaches the response, since it is not JSON.
    SQLite has no numbers in EXPLAIN QUERY PLAN at all and reports none.'
  use_case: A developer analyzing a statement on a Trino-, MySQL- or DuckDB-backed instance reads the
    same rows and cost columns they get from Postgres.
  code:
  - provisa/executor/explain.py
  tests:
  - tests/unit/test_explain.py
  - tests/fixtures/explain/trino_481_join_group.json
  - tests/fixtures/explain/mysql_8_join.json
  - tests/fixtures/explain/duckdb_1_5_scan_agg.json
  since: 2026-08
  scenario: 'Given a statement analyzed against an engine that estimates its plan


    When the plan is normalized


    Then each operator carries that engine''s own row and cost estimate, and a node the optimizer could
    not estimate carries none

    '
  bdd: true
  bdd_reason: Behavior expressed as stakeholder-readable Gherkin acceptance criteria.
  stakeholders:
  - app-developer
  related_reqs:
  - REQ-1519
- id: REQ-1523
  status: proposed
  target: 2026-Q4
  group: 13. Multi-Tenancy & Organizations
  category: Environments
  priority: MUST
  type: behavioral
  description: 'An environment name is bounded, and so is the number of them. REQ-1488 makes an environment
    a schema of its own, so a free-form name is a schema name and an unbounded count is unbounded schemas:
    the short-lived per-developer environments an org actually creates -- dev-alice, dev-1482-refund-fix
    -- accumulate until nothing reaps them. A name is therefore lowercase letters, digits and underscores,
    starting with a letter, two to thirty-two characters, and unique within the org. Hyphens are excluded
    for the reason REQ-1309 excludes them from an org id -- the name is interpolated unquoted into schema
    DDL, where a hyphen is a syntax error raised during background provisioning -- and an environment
    loses nothing by it, because REQ-1487 carries the selection in a header rather than a DNS label, so
    dev_alice is the spelling of dev-alice. Thirty-two is the cap the name may reach, not the one it always
    has: the environment''s schema is org_<id>_env_<name> and its caches suffix that, so the real cap
    is whatever PostgreSQL''s 63-byte identifier limit leaves after THIS org''s id, computed against the
    actual id at creation and refused with both lengths named. PostgreSQL truncates a long identifier
    instead of refusing it, so an unchecked name would silently point two environments at one schema.
    The separator is _env_ rather than the __ the compiler spends on catalog names, and because REQ-1309
    forbids an underscore in an org id the first _env_ after the prefix always splits the schema into
    exactly one org and one environment; prod is reserved by REQ-1487 and the platform''s own prefixes
    are refused rather than truncated, because the name reaches a schema identifier. The count of environments
    an org may hold is a limit on its plan, enforced where an environment is created rather than only
    where a plan is downgraded, in the manner REQ-1513 already uses for the source ceiling. An environment
    may carry an expiry chosen at creation, after which it is deleted with its schema and its store; prod
    can carry none. Expiry is opt-in and its absence means permanent -- an environment is never reaped
    for being idle, because a quiet pre-prod is not an abandoned one. An org_admin is told which environments
    expire and when, and may extend or clear an expiry at any time before it fires.'
  use_case: A developer creates dev-1482-refund-fix for a two-week change and gives it a two-week expiry,
    and the org is not left holding forty dev schemas a year later.
  code:
  - provisa/api/admin/environments_router.py
  - provisa/control_plane/entitlements.py
  - provisa/core/environments.py
  - provisa/core/org_ids.py
  tests:
  - tests/unit/test_environments.py
  scenario: 'Given an org at its plan''s environment ceiling

    When an org_admin creates one more environment

    Then the creation is refused naming the ceiling and the plan

    And a name that is not a valid schema identifier is refused rather than rewritten

    And an environment whose expiry has passed is deleted with its schema and store

    And an environment with no expiry is never deleted for being idle

    '
  related_reqs:
  - REQ-1487
  - REQ-1488
  - REQ-1507
  - REQ-1513
- id: REQ-1524
  status: in-progress
  target: 2026-Q4
  group: 13. Multi-Tenancy & Organizations
  category: Environments
  priority: SHOULD
  type: behavioral
  description: 'Every environment has a repository, and the repository is a PROJECTION of the environment,
    never an authority over it. The governed model lives in the tenant control plane and is read from
    there by every query (REQ-1488); each change to a carried class of REQ-1489 additionally serializes
    that class to files and commits them, so an environment''s history, its builds (REQ-1496) and its
    export (REQ-1502) are one artifact instead of three mechanisms. The commit carries the acting user
    as its author and names what changed, which is where REQ-1505''s history comes from without a second
    ledger. Because the repository is derived, a failed commit MUST NOT fail the change it observes --
    a model edit that succeeded is not undone by a disk that would not take a projection of it, which
    is the REQ-1515 rule applied to a different observer -- and the environment is instead marked as having
    drifted. Drift is repairable by construction and the repair ships with the write-through rather than
    after it: rebuilding re-serializes every carried class from the control plane and commits the result,
    which is correct however far behind the tree had fallen. The tree holds the carried classes and nothing
    else; no binding, secret, grant or runtime table is ever written to it, so the REQ-1489 exclusion
    becomes something a person can see in a file listing rather than only something a test asserts. The
    repository is EMBEDDED -- a bare repository per environment, held by the deployment beside the org''s
    own storage -- because REQ-294 makes the distribution airgap-capable and an external forge cannot
    be a dependency of editing a model; the git implementation is therefore pure Python with no native
    library to build. The repository is a normal one on disk, so an organization that wants it on a forge
    adds a remote and pushes with ordinary git; Provisa configures no remote, holds no push credential
    and performs no push, which keeps a credential out of the control plane and keeps Provisa out of a
    business -- remote state, conflicts, rejected pushes -- it has no reason to enter. (Amended 2026-08-21,
    one repository per ORGANIZATION:) The bare repository is per organization and an environment is a
    BRANCH within it, not a repository of its own. The boundary is not a packaging convenience -- a repository
    is exactly the set of refs that can branch from one another and merge back into one another, and that
    set is the org: an environment is always created from another environment of the same org or a sha
    of one, and always merges back into one, while a branch or merge across orgs is never a legal operation.
    Two environments in separate repositories share no object graph, so a pull request between them cannot
    be expressed at all, which would leave REQ-1504''s review-as-a-pull-request unbuildable. A single
    deployment-wide repository with refs namespaced per org is refused for the opposite reason: git objects
    are content-addressed and shared across refs, so one object store holding every tenant would let an
    org fetch another org''s model objects by hash. Per-org is therefore the same isolation boundary as
    the org schema of REQ-1488, drawn where git already draws one, and it is provisioned with the schema,
    the role and the stores at org creation. Creating an environment from a sha is branching that sha
    and loading its tree into the new environment''s schema; the branch is named for the environment.
    (Amended 2026-08-21, three verbs and automatic commits:) Provisa''s git surface is BRANCH, BROWSE
    and LOAD, and nothing else. A branch can be created, the repository''s branches and history can be
    browsed, and any branch or sha can be loaded into an environment. Committing is not a verb: the write-through
    above commits every change as it happens, which is what makes UNDO free rather than a feature -- undoing
    is loading an earlier sha, and it works for any change ever made rather than for the ones somebody
    remembered to mark. The cost is a history of generated messages, and a person who wants a legible
    one squashes it afterwards with ordinary git, which is a better trade than asking everyone to name
    their work at the moment they are least inclined to. Everything else -- pushing, fetching, merging,
    rebasing, promotion, CI -- is done with ordinary git against this repository by whoever wants to do
    it, and Provisa neither performs those operations nor polices them. It is a normal repository, so
    normal tools work on it; a product that reimplemented them would be reimplementing git badly, and
    one that restricted them would be preventing the workflows the organization already has. The consequence
    is accepted deliberately: a branch may move underneath an environment, and that is not an error state
    Provisa tracks. What protects the model is not control over the repository but REQ-1496 -- a tree
    is not a model until a load validates it and applies it whole or not at all. (Amended 2026-08-21,
    BROWSE is a REF browser and the library is pure Python:) The load sources are a ref in the org''s
    repository -- a branch or a sha -- and a set of files the operator brings; there is no third one,
    and in particular there is no server filesystem for a tenant to pick a path out of. The config file
    on disk is the DEPLOYMENT''s boot seed (REQ-164), not a tenant load source, so a browser that showed
    directories would be offering the operator a machine they do not own. BROWSE therefore lists branches,
    their history and the commits within it, and selecting a commit is selecting what LOAD will apply
    -- the tree is shown as the files at that commit, which REQ-1526 makes readable, but it is browsed
    by ref and never by path. The implementation is dulwich: it is pure Python, so it satisfies the no-native-library
    rule above and needs no git binary on the host, which GitPython would (it shells out, so an image
    without git silently loses history) and pygit2 would not satisfy at all (libgit2 is a native build,
    and REQ-294''s airgap distribution cannot carry a compiler). (Amended 2026-08-21, the repository exists
    from the first moment there is a model, and its location is the org''s to choose:) The repository
    is not created lazily by the first change. A deployment launched with --demo has a model before anyone
    edits anything, and that model is committed as the demo repository''s first commit, so the demo opens
    on a history rather than on an empty one and every verb -- BRANCH, BROWSE, LOAD -- works on it before
    a single edit. A deployment launched without the demo has no model, and the act that produces one
    -- creating the org, whose prod environment REQ-1487 gives it at creation -- creates the bare repository
    beside it, in the same act and with the same failure semantics as its schema. There is no state in
    which an environment holds a model and has no repository to project it into. WHERE the repository
    lives is configurable the way every other developer tool makes it configurable: the default is embedded,
    a bare repository the deployment holds (REQ-294''s airgap rule makes that the only default that can
    always work), and an organization may instead point the projection at a local path it already keeps
    or at a remote it already uses. Pointing at a remote makes Provisa push, which is the part REQ-1524
    originally refused, so it is admitted only on the terms REQ-1525 already sets for every other credential:
    the authentication is a REFERENCE resolved at use time (${env:VAR} through provisa/core/secrets.py),
    never a literal in the control plane, and an org that supplies none gets the embedded default rather
    than a broken push. A push that fails is a projection that did not land -- it marks the environment
    drifted and never fails the change it observes, which is the same rule the local commit already obeys.
    (Amended 2026-08-21, the local store is mandatory and the environment IS the working copy:) A configured
    remote is a PUSH MIRROR and never the store. The embedded bare repository is always written first
    and is always what BROWSE and LOAD read; nothing serving a page ever fetches from a remote. That is
    precisely what makes the remote optional -- an org that configures none, or whose remote is unreachable,
    loses a mirror and never loses a verb. There is NO per-developer git storage, no server-side clone
    and no working directory allocated to a user. A developer''s working copy is the ENVIRONMENT, which
    REQ-1488 already makes a schema: creating a branch is creating an environment, and that developer''s
    undo history is that environment''s branch in the org''s one repository, written by the write-through
    above. The quota on this is the plan''s environment ceiling (REQ-1488), already counted and already
    enforced, rather than a second per-user storage quota to allocate, meter and garbage-collect. The
    consequence is that the SaaS deployment and the single-tenant deployment differ in NOTHING here --
    there is no mechanism in one that is absent from the other, no per-seat storage tier and no hosted-only
    path -- which is the reason to prefer this shape over server-side clones even before the operational
    cost is counted. Provisa''s git UI is the three verbs and no more: browse a ref, diff two refs per
    file, merge with a report. Rebase, cherry-pick, force-push and textual conflict resolution are absent
    by construction rather than by omission, because REQ-1490''s merge matches by OBJECT IDENTITY and
    therefore has no textual conflict to resolve; a product that reimplemented those verbs would be reimplementing
    git badly on top of a merge that does not need them. (Amended 2026-08-22, WHERE THE COMMIT IS TRIGGERED
    AND WHEN THE REPOSITORY IS CREATED:) The write-through has ONE trigger: a strawberry SchemaExtension
    registered on the admin GraphQL schema, which after every mutation that completed without errors projects
    the environment''s schema and commits the result. It is one place rather than fifty resolvers because
    a rule that must be remembered at fifty call sites is a rule that will be missed at the fifty-first,
    and a mutation added later is projected without anyone adding a line for it. It projects the WHOLE
    model rather than the rows a mutation appears to touch: a resolver does not report what it wrote,
    and a change set inferred from what it seems to write would be a second, weaker model of the model.
    Projection is deterministic (REQ-1526), so an unchanged model produces a byte- identical tree and
    NO commit is written -- the filter is the tree comparison, which cannot be wrong about what changed.
    It runs AFTER the operation and never before, because a commit records what the model is and not what
    somebody asked it to become; an operation whose result carries errors is skipped for the same reason.
    The organization''s repository is created by the act that produces the organization, in the same provisioning
    path that writes prod''s registry row, and with the same failure semantics as the schema: creating
    it raises, so an org whose repository could not be created never reaches ready. Prod''s FIRST commit
    is written there too, once the org already holds its seeded model (and its demo model when --demo
    asked for one), which is what makes the first user edit read as a diff instead of the whole model
    appearing at once. The environment whose branch receives a commit is the one the request is bound
    to (REQ-1487): an unbound request is the default org''s prod, which is the routing rule read back
    rather than a default invented at the commit point.'
  use_case: An org_admin asks what changed in prod last Tuesday and who changed it, and reads it from
    the environment's history; the same organization reviews a dev-to-prod promotion as a pull request
    in its own GitHub, because the builds are already there.
  code:
  - provisa/core/env_repo.py
  - provisa/core/env_files.py
  - provisa/core/env_project.py
  - provisa/api/admin/model_commit.py
  - provisa/api/admin/schema.py
  - provisa/api/admin/orgs_router.py
  - provisa/api/admin/environments_router.py
  tests:
  - tests/unit/test_env_repo.py
  - tests/unit/test_model_commit.py
  - tests/integration/test_env_repo_write_through.py
  scenario: 'Given an environment whose governed model is changed by an org_admin

    When the change is committed to the tenant control plane

    Then the carried classes are serialized and committed to that environment''s repository

    And the commit names the acting user as its author

    And no binding, secret, grant or runtime table appears anywhere in the tree

    And a commit that fails leaves the model change in place and marks the environment drifted

    And rebuilding a drifted environment re-serializes every carried class and commits the result

    '
  related_reqs:
  - REQ-1487
  - REQ-1488
  - REQ-1489
  - REQ-1490
  - REQ-1496
  - REQ-1502
  - REQ-1504
  - REQ-1505
  - REQ-1525
  - REQ-294
- id: REQ-1525
  group: 13. Multi-Tenancy & Organizations
  category: Environments
  type: behavioral
  target: 2026-Q4
  use_case: A developer writes a webhook URL with the API token pasted into the query string, and is told
    at the moment of writing that the token must be a ${env:...} reference -- rather than discovering
    months later that it is in the org's git history, in every clone of it, and must be rotated.
  scenario: 'Given a carried class field whose value contains a literal credential

    When an author saves the change to the environment''s governed model

    Then the write is refused, naming the field and showing the reference form

    And nothing is written to the control plane and nothing is committed to the environment''s branch

    Given the same field holding a ${env:VAR} reference instead

    When the change is saved

    Then it is accepted and committed, and the diff carries the secret''s name and never its value'
  description: 'The exclusion of bindings and secrets from what an environment carries (REQ-1489) is an
    exclusion of COLUMNS, and it cannot reach inside one. Carried classes hold text their authors compose
    -- metrics.expression, tracked_webhooks.url and its arguments, policy and glossary text -- and a token,
    password or signed URL typed into any of them is carried like the rest of the text, serialized by
    REQ-1524''s write-through, committed, and pushed to whatever remote the org configured. Git is where
    this stops being recoverable: history is immutable by design and a rewrite does not reach clones or
    forks already taken, so the only remedy once a literal is pushed is rotating the credential, and the
    requirement is therefore to keep it from being written rather than to remove it afterwards. A credential
    appearing in a carried field MUST be a reference and never a literal. The reference syntax already
    exists and is already resolved at use time rather than at rest -- ${env:VAR} through provisa/core/secrets.py,
    as sources and Trino catalog files already use -- so what is carried is the NAME of a secret, which
    is meaningless without the environment holding it, and the value stays where REQ-1489 already keeps
    values. This is also why the reference form must remain the ordinary way to write one: a rule that
    is harder to follow than the thing it forbids is a rule that gets worked around. The refusal is at
    the MODEL EDIT and never at the commit. REQ-1524 forbids a failed commit from failing the change it
    observes, so refusing at commit time would accept the secret into the control plane, decline to project
    it, and mark the environment drifted -- leaving the credential in the database, the repository permanently
    behind, and a rebuild that would either write the secret or never converge. Refusing the write leaves
    both stores agreeing and the secret in neither. The write is refused with the field named and the
    reference form shown, so the author''s next act is to write it correctly rather than to find out why
    the editor said no. Detection is by high-confidence provider token patterns and by a value that parses
    as a URI carrying userinfo; entropy alone is NOT a refusal, because a generated identifier and a generated
    key are the same string to a detector and a false refusal against a legitimate expression teaches
    authors to route around the check. What detection misses remains the author''s care, which the requirement
    reduces but does not eliminate, and the environment''s binding path stays the supported way to give
    an environment a credential. (Amended 2026-08-22, WHERE THE REFUSAL LIVES:) The check is a SEAM, not
    a call: it sits in Connection.execute_core beside REQ-828''s meta-RLS guard, so every Core INSERT
    and UPDATE passes through it, and it is applied a second time to the one INSERT that does not route
    through execute_core (the savepoint-isolated insert inside upsert) -- a seam with one hole in it is
    not a seam. It scans ONLY the CARRIED tables of REQ-1489. A credential on an IDENTITY_ONLY table is
    a BINDING, which is where a credential is supposed to be (REQ-1491) and which no copy and no commit
    carries, so scanning those would refuse the supported way to give an environment a credential. Only
    a string value is scanned: coercing a number or a JSON bag to text to search it would report matches
    inside values no author wrote. A value is cleared by the reference appearing WHERE THE CREDENTIAL
    WOULD BE rather than anywhere in the field, so postgres://svc:${env:PGPASSWORD}@host passes while
    the literal form does not.'
  status: in-progress
  priority: MUST
  related_reqs:
  - REQ-1489
  - REQ-1491
  - REQ-1502
  - REQ-1504
  - REQ-1524
  code:
  - provisa/core/env_secrets.py
  - provisa/core/database.py
  - provisa/core/secrets.py
  - provisa/core/schema_org.py
  tests:
  - tests/unit/test_env_secrets.py
- id: REQ-1526
  group: 13. Multi-Tenancy & Organizations
  category: Environments
  type: behavioral
  target: 2026-Q4
  use_case: Two developers onboard tables from different datasources in the same week and their branches
    merge without a conflict, because they added files rather than both editing one; a reviewer reading
    the third's pull request sees six added files named for the tables they onboarded.
  scenario: 'Given an environment whose carried model is written through to its branch (REQ-1524)

    When a table is registered, changed or removed

    Then exactly the file addressing that table changes, and no other file is rewritten

    Given two branches that each added a different table

    When they are merged with ordinary git

    Then the merge is a fast-forward or a trivial two-file union with no conflict

    Given the same entity edited on both branches

    When they are merged

    Then the conflict is confined to that entity''s file and is legible without a model-aware tool'
  description: 'The serialized model is a DIRECTORY OF FILES, one file per entity, and never a single
    document. A single file makes every concurrent change a conflict in the same document: two people
    onboarding unrelated tables collide because both appended to one list, and git''s three-way, which
    reasons about lines and not entities, presents that as a conflict a human then resolves by reading
    YAML indentation. One file per entity turns the common case into an ADD of two different paths, which
    merges with no conflict at all, and confines the genuine case -- the same entity edited twice -- to
    a file small enough to read. Files are addressed by the REQ-1385 URI, with the scheme and org stripped,
    so the path is the address: provisa://acme/sales/tables/Order becomes sales/tables/Order.yaml, sources/pg_main.yaml,
    commands/refund.yaml. Nothing new is invented for this. The addresses are already unique and already
    path-legal: names are normalized and de-conflicted by the naming authority at WRITE time (apply_convention
    plus a conflict check that qualifies by source_id and then by suffix, in provisa/api/admin/schema_query.py),
    _segment() percent-encodes each part, and RESERVED_KIND_KEYWORDS already refuses a domain named for
    a kind, which is the collision that would otherwise break a directory layout. Addressing the tree
    the same way the export addresses assets also means a path in a pull request and an asset in the organization''s
    catalog name one thing. Serialization MUST be deterministic -- fixed key order, lists sorted, one
    style -- or the write-through rewrites files that did not change and git reports conflicts that are
    not real, which would defeat the split. This makes REQ-1502''s stable ordering structural rather than
    a courtesy. The tree holds the carried classes of REQ-1489 and nothing else. Deployment settings --
    server, control_plane, security, the federation engine block, observability, auth, cache, discovery
    -- are not the governed model, belong to the deployment rather than to an environment, and are absent
    from the tree; today''s single config file mixes the two, and splitting it makes that boundary a directory
    listing. Loading accepts EITHER form: a directory, or a single config document, which is what people
    paste, export from elsewhere and keep in existing repositories. Write-through always emits the directory.
    A load is whole or not at all in both cases (REQ-1496).

    (Amended 2026-08-21, WHAT IS AN ENTITY AND WHAT IS PART OF ONE:) a row whose lifetime is its own gets
    a file; a row that CASCADES with another is written INSIDE that one''s file. The rule is read off
    the schema rather than chosen per table, so table_columns, relationships, rls_rules, glossary_term_refs
    and table_meta_links are part of their table''s file and never files of their own -- which is also
    what REQ-1385 already says, since #field: and #rel: are FRAGMENTS of a table URI and a fragment has
    no path. NO SURROGATE KEY AND NO REFERENCE TO ONE reaches a file: registered_tables.id is an autoincrementing
    integer, so the same model copied into a second environment gets different numbers and a naive dump
    diffs against itself; the joining FK is dropped because the nesting now records the join, and a reference
    to another entity is written as that entity''s PATH. Storage columns (tenant_id, org_id, created/updated
    stamps) and derived caches (the clustering run) are excluded for the same reason -- they say where
    a row is stored and when it was written, not what the model says. A BINDING NEVER REACHES THE TREE:
    the columns env_classes classifies as bindings are excluded here too, so a credential cannot reach
    a commit even by being pasted into one (REQ-1491, REQ-1525). Determinism is stated concretely: ONE
    fixed key order (alphabetical -- not a better order, a fixed one), child collections sorted BY ADDRESS
    rather than by the surrogate they arrived in, one YAML style, and model-ordered lists (column_presets,
    unique_constraints) LEFT ALONE, because sorting a list the model orders would change what it says.
    An unset column is absent rather than null, and an empty collection is absent rather than an empty
    list. RESERVED_KIND_KEYWORDS is widened from the four kinds that appear in a URI to every kind the
    tree gives a DIRECTORY to, and create_domain now checks EVERY hierarchical segment rather than the
    id entire: in a URI a kind is only ever preceded by domain segments, but in a directory tree the kind
    and the domain are siblings, so a domain named a/tables plants its own directory exactly where the
    kind directory goes. (Amended 2026-08-22, PROJECTION -- HOW A SCHEMA BECOMES THE TREE:) env_files
    owns the LAYOUT and env_project owns the READ, because the layout question (what is this entity''s
    path, what may appear in a file) is answerable without a database and the read question is not. project()
    reads every carried table once and resolves every surrogate before a file is written: a relationship''s
    target_table_id becomes the target''s PATH, a glossary edge''s to_term_id becomes the term''s path,
    and a tag assignment''s object_key -- which embeds a serial -- becomes the REQ-1385 fragment (#field:,
    #rel:), recomputed on load. A relationship is nested in the file of the table it STARTS at, and so
    is a tag on that relationship; both endpoints are dropped, since the source is the file and the target
    is written as a path. A relationship is sorted and addressed by its id rather than its alias, because
    alias is nullable and a null is no address -- and that id, unlike every other child key, is a name
    the caller chose rather than one the database handed out, so it is written into the file. Each nested
    collection names its OWN table, so that the binding columns excluded from it are its own: excluding
    the parent''s would both miss a child''s binding and delete an innocent child column sharing a name
    with one of the parent''s. Two tables addressing one path is refused rather than silently overwritten,
    as is a tracked function and a webhook sharing a command name. naming_rules is ONE file (naming/rules.yaml)
    because rule order is semantic; a directory of per-rule files would encode that order in filenames.'
  status: proposed
  priority: MUST
  related_reqs:
  - REQ-1385
  - REQ-1489
  - REQ-1496
  - REQ-1502
  - REQ-1524
  code:
  - provisa/api/metadata_export/refs.py
  - provisa/core/models.py
  - provisa/api/admin/_config_io.py
  - provisa/core/env_files.py
  - provisa/core/env_project.py
  - provisa/api/admin/schema_mutation.py
  tests:
  - tests/unit/test_env_files.py
  - tests/integration/test_env_project.py
- id: REQ-1527
  status: in-progress
  target: 2026-Q4
  group: 13. Multi-Tenancy & Organizations
  category: Environments
  priority: SHOULD
  type: behavioral
  description: 'Provisa does not run CI; it integrates with the CI the organization already has, and the
    integration point is the remote of REQ-1524 rather than anything Provisa hosts. Pushing the projection
    to the org''s own remote puts real files in front of their existing pipeline, which is the shape every
    comparable hosted product uses -- the vendor holds the environment, the customer''s git and CI hold
    the pipeline -- and it is the only shape that works for an org whose review policy, secrets and runners
    already live there. Building a CI runner inside Provisa would be building a second, weaker one beside
    the one they trust, and it would be unavailable to the airgapped distribution REQ-294 describes for
    exactly the deployments most likely to require a gate. Provisa''s side of the integration is therefore
    two things and no more. First, an outbound status: a webhook fired when an environment''s projection
    lands or fails to land, carrying the environment, the sha and whether the environment is drifted,
    so the pipeline is triggered by the change rather than by polling. Second, an inbound gate that CI
    can actually assert against -- the dry run of REQ-1490 (plan_copy) against the intended target, which
    answers what merging this ref into that environment WOULD do without doing any of it. That answer
    is what a pipeline check reads: a job fails when the report shows removals nobody asked for, or a
    role or policy changed that the org''s rules protect. The dry run is the same code path the merge
    itself runs, so a check that passes describes the merge that follows rather than an approximation
    of it. The gate is advisory to Provisa and authoritative to the organization: REQ-1504''s approval
    is what actually holds a merge, and a CI check is evidence an approver reads, because a deployment
    whose git host is unreachable must still be able to promote a model. (Amended 2026-08-22, WHERE THE
    TWO HALVES LIVE:) the remote and the status receiver are per ORGANISATION and live on the registry''s
    orgs row (repo_remote, repo_status_webhook) rather than in the org''s settings, because the org''s
    settings live in the schema an environment IS -- storing them there would let one branch mirror somewhere
    the next branch does not, and would make the mirror a thing a copy carries. Both are NULL by default
    and independent: an org may push without reporting, report without pushing, or -- the airgapped deployment
    of REQ-294 and every org that never asked for this -- do neither and still commit, request, approve
    and apply exactly as before. The remote is stored VERBATIM with its secret references intact and resolved
    through REQ-125''s provider at push time only, so the token a push needs never enters the control
    plane, which is the same rule REQ-1525 holds over the model''s own carried fields. The announcement
    runs inside write_through under REQ-1524''s law: an unreachable remote or a receiver returning 500
    is logged and never fails the edit it reports, and the push is attempted before the status is posted
    so a pipeline reading a sha finds a remote that already holds it. An UNCHANGED model announces nothing
    -- write_through returns no sha when the tree matched, and a status stream that fired on every mutation
    touching no carried class would make the pipeline run on nothing. The inbound gate is a GET (/{name}/merge-preview)
    rather than only the existing dry_run flag on POST /merge: the same plan_copy answers both, but a
    runner that got the flag wrong would APPLY the merge it meant to inspect, and a method that cannot
    write is the one to hand a CI job.'
  use_case: An organization's existing GitHub Actions pipeline runs on every push of its Provisa model
    and fails the check when a proposed dev-to-prod merge would delete an RLS rule -- using the same dry
    run the merge itself would perform, and without Provisa running any pipeline of its own.
  scenario: 'Given an organization whose environment repository has a remote configured

    When a carried class changes and the projection is pushed

    Then a status webhook fires naming the environment, the sha and whether the environment is drifted

    Given a CI job asking what merging a ref into a protected environment would do

    When it requests the dry run

    Then it receives the REQ-1490 report -- added, changed, removed, left alone -- and nothing is applied

    And the report describes the same operation the approved merge later performs

    And a deployment with no remote and no CI can still request, approve and apply a merge

    '
  code:
  - provisa/core/env_ci.py
  - provisa/core/env_copy.py
  - provisa/core/env_repo.py
  - provisa/core/schema_admin.py
  - provisa/api/admin/environments_router.py
  tests:
  - tests/unit/test_env_ci.py
  - tests/unit/test_env_merge_request_endpoints.py
  - tests/integration/test_env_ci.py
  related_reqs:
  - REQ-1490
  - REQ-1504
  - REQ-1524
  - REQ-1526
  - REQ-294
  - REQ-125
  - REQ-1525
- id: REQ-1528
  status: proposed
  target: 2026-Q4
  group: 13. Multi-Tenancy & Organizations
  category: Environments
  priority: SHOULD
  type: behavioral
  description: 'Authority over a model is scoped to an ENVIRONMENT, and the ordinary way to acquire it
    is to create one. A member who holds no model-editing rights in an organization can still create an
    environment (REQ-1488 creates it by loading a model into a name), and within the environment they
    created they hold the model-editing rights of an org_admin: they can add and change domains, tables,
    relationships, metrics, roles, policies and glossary -- every carried class of REQ-1489 -- without
    holding those rights anywhere else in the org. This is the reason a read-only developer needs no special
    grant to work: they branch, they own what they branched, and they propose it back through REQ-1504.
    The rights are the CREATOR''s and are anchored on environments.created_by, which already exists; they
    are not inherited by everyone who can see the environment, and they end where the environment ends.
    WHAT THE GRANT IS NOT. It is model-editing rights and nothing adjacent: not user management, not invites,
    not billing, not platform settings, not the ability to protect or unprotect an environment, not the
    ability to grant this same authority to somebody else, and not the ability to approve a merge -- including
    a merge out of the environment they own, which REQ-1504 already forbids the requester from approving.
    An owner is the author of a proposal, never its reviewer. WHY THIS IS SAFE IS AN INTERLOCK AND NOT
    A JUDGEMENT CALL. The compound failure REQ-1492 names is write authority TOGETHER WITH an inherited
    binding: a copied environment that both points at production and may be written to. Here the two halves
    cannot meet, because REQ-1491 strips the bindings from every environment a copy creates and marks
    the rows unbound. The owner therefore has full authority over a model that addresses NOTHING until
    the environment is bound, and binding is not theirs to do -- it needs a credential scoped to that
    environment (REQ-1503), which an org_admin issues. Authority over the model and authority over what
    the model points at are separated by construction, and the environment grant hands out only the first.
    The grant is therefore a consequence of ownership rather than a row somebody administers: it is derived
    from created_by at authorization time, so it cannot drift from the environment it describes, and deleting
    the environment removes it in the same act. (Amended 2026-08-21, this is the ONLY path:) Creating
    an environment is not one way among several to acquire model-editing authority -- it is the only way.
    There is no request-elevation, no temporary grant, no break-glass, and no administrator action that
    hands a member model-editing rights in prod directly; a member either holds them from their role or
    acquires them by creating an environment and owning it. This is what makes the audit trail complete
    rather than merely thorough: every model change that was not made by someone who already held the
    right in the org was made inside an environment that names its creator, was reported by REQ-1490,
    and entered a protected environment only through a REQ-1504 request somebody else approved. There
    is no fourth way in, so there is nothing to reconcile against. It also means privilege escalation
    and change proposal are THE SAME ACT rather than two: the thing a developer does to get authority
    is the thing that scopes it, records it, and guarantees a review before it reaches anywhere protected.
    (Amended 2026-08-21, why the escalation is worthless:) The reason creating an environment can be left
    open to any member is not that the authority is small -- it is org_admin''s model-editing authority
    in full -- but that the authority is USELESS to an attacker. An environment arrives with its bindings
    stripped (REQ-1491) and its secrets not copied at all (REQ-1489 classes org_secrets NEVER_SENSITIVE),
    so to make the model reach any data at all the creator must reconstruct the source URIs and supply
    the credentials themselves. Someone who can already do that gained nothing by creating the environment;
    someone who cannot has escalated into a model that queries nothing. The privilege that matters --
    reaching the organization''s data -- is held by the credential and not by the rights, and the credential
    is the one thing the act does not hand over. That is what makes this path safe to leave open rather
    than merely audited. (Amended 2026-08-21, and this is what makes the rule usable rather than merely
    safe:) an org_admin ordinarily does NOT grant model-editing rights on a base environment (REQ-1529)
    -- not even on dev. Membership in a base buys a developer reading and querying it; it does not buy
    them editing it. The rights to change a model are obtained the one way this requirement admits, by
    branching the base, and they exist only inside the branch the developer owns. Nobody edits dev directly,
    so dev is never in a state nobody proposed; every change to it arrives through a branch and a merge
    request (REQ-1504), which is the same path production changes take and therefore the same audit trail.
    The arrangement costs the developer one click -- the branch needs no credentials, because it inherits
    the base''s -- and it costs the org nothing, because an org_admin who grants no editing rights anywhere
    has not blocked any work; they have routed all of it through review. (Amended 2026-08-21, WHAT THE
    GRANT ACTUALLY IS:) the authority branching confers is the seeded ``developer`` role, not org_admin''s
    rights less a withheld list. That is a smaller grant reached by a shorter argument: developer already
    exists, already means "may build the model and query it", and already excludes the surfaces an org_admin
    keeps -- registering sources and tables, masking, column grants, view governance, access config and
    org settings. The withheld-list construction had to name every right that must not travel, and would
    have silently handed over any right added to org_admin afterwards; naming the role instead means a
    right reaches a branch owner only because somebody decided a developer should hold it. It also answers,
    with no new authorization axis, the worry that a branch owner can change anything: they can change
    what a developer may change. And it bounds the escalation argument above under REQ-1529, where a branch
    does reach its base''s data through the inherited binding -- what its owner may do to that data is
    a developer''s write of the MODEL. (Amended 2026-08-22, THE GRANT CARRIES NO DATA RIGHT, REQ-1539:)
    the authority is the seeded developer role LESS write, full_results and usage. Creating an environment
    is a model-authoring act and confers model-authoring rights; it confers nothing over data, which is
    the roles'' answer and stays the roles'' answer. This is where the escalation REQ-1492 worried about
    is bounded -- at its source, rather than by a policy flag on the base compensating downstream for
    a right that should never have been handed out. (Amended 2026-08-22, AND ROLES ARE NOT AMONG THEM:) the opening list of carried classes a branch owner may edit names ``roles``, and that is withdrawn -- it predates the grant being defined as the seeded ``developer`` role, which holds no ``user_management``, and every role endpoint requires it. A branch owner inherits their environment''s role definitions and may not rewrite them, there or anywhere; changing what a role means in a lane is an org_admin''s act inside that lane (REQ-1539). This is the interlock stated once more at the level of rights: the branch owner has full authority over a model that addresses nothing, and no authority at all over who may do what.'
  use_case: A developer with read-only rights in the org creates a dev environment, rebuilds half the
    governed model inside it, and proposes it to prod for review -- without anyone granting them org_admin,
    and without their environment being able to write to a production database, because it is not bound
    to one.
  scenario: 'Given a member holding no model-editing rights in an organization

    When they create an environment

    Then they hold org_admin''s model-editing rights within that environment and within no other

    And they cannot manage users, invites, billing or platform settings there

    And they cannot protect or unprotect the environment, nor grant the same authority to another member

    And they cannot approve a merge out of the environment they own

    Given the environment they created carries no binding (REQ-1491)

    When they attempt to bind it to a source

    Then the binding requires a credential scoped to that environment, which an org_admin issues

    Given the environment is deleted

    Then the authority derived from owning it is gone with it


    Given a member holding no model-editing rights


    When they seek those rights by any route other than creating an environment


    Then no such route exists: elevation is refused and creating an environment is the only path


    Given a member who creates an environment in order to acquire authority


    When they try to reach the organization''s data from it


    Then they must reconstruct the source URIs and supply the credentials themselves, because neither
    travelled with the environment


    And so the authority they acquired reaches nothing they could not already reach


    Given an org_admin has granted a developer membership in the dev base but no model-editing capability


    When the developer attempts to change the model of dev directly


    Then it is refused


    When the developer branches dev instead


    Then they hold the environment authority inside that branch and may change its model there


    '
  code:
  - provisa/core/env_authority.py
  - provisa/core/schema_admin.py
  - provisa/api/admin/environments_router.py
  tests:
  - tests/unit/test_env_authority.py
  related_reqs:
  - REQ-1487
  - REQ-1488
  - REQ-1491
  - REQ-1492
  - REQ-1503
  - REQ-1504
  - REQ-1524
  - REQ-1529
- id: REQ-1529
  status: proposed
  target: 2026-Q4
  group: 13. Multi-Tenancy & Organizations
  category: Environments
  priority: SHOULD
  type: behavioral
  description: 'An organization''s environments come in TWO KINDS, and the difference is who made them.
    A BASE environment -- dev, staging, prod, whatever the org calls them -- is created by an org_admin,
    bound with its own credentials, and carries its own members and grants: it is the org''s statement
    of where a class of work points and who may do it. A BRANCH is created by a member FROM a base, and
    it inherits that base''s bindings by reference (REQ-1491) rather than asking its creator for credentials.
    That is the whole answer to the friction of branching: nobody retypes a database URI to fix a metric
    definition, because the credentials were authored once, by the person who should author them, on the
    base the branch came from. It also removes the need for a separate profile object -- the base environment
    IS the profile, which is one concept instead of two and one that already has members, grants and an
    audit trail. WHAT A BRANCH MAY DO THROUGH AN INHERITED BINDING is a property the org_admin sets on
    the BASE, not on the branch, and it defaults to READ-ONLY. A base bound to a dev database is ordinarily
    marked branch-writable, so branching dev gives a developer somewhere to write; a base bound to production
    is ordinarily left read-only, so branching prod gives a developer prod-shaped reads and no way to
    damage prod. The default is read-only because the case that gets it wrong is the case that matters:
    an org_admin who does nothing has branches that cannot write anywhere. A branch never widens what
    it inherited -- it cannot mark itself writable, rebind to another target, or branch a base it could
    not read -- so a chain of branches converges on the reach of the base at its root and never exceeds
    it. Branching a branch is branching that base. AND THE GRANTS TRAVEL THE SAME WAY: a member may branch
    only a base they are a member of, and their branch carries no membership or grant of its own (REQ-1489
    classes those NEVER_SENSITIVE) beyond the environment authority its creator holds by owning it (REQ-1528).
    A base is deletable only when no branch still inherits from it, because deleting it would leave those
    branches pointing at a binding that no longer exists. THE SECRETS BELONG TO THE BASE, TOO. An org_admin
    authoring a base environment authors the secrets that base''s bindings need -- the password behind
    its username, the token behind its API source -- and every branch taken from that base reaches them
    through the inherited binding without ever reading one. That is why REQ-1489 can class org_secrets
    NEVER_SENSITIVE and lose nothing: a branch does not need a copy of a secret it resolves by reference,
    and a secret that is never copied is a secret that cannot leak into a branch, an export, or a repository.
    The credential surface of the whole org is therefore as many secret sets as there are bases, not as
    many as there are branches, and rotating one is one edit on one base that every branch of it picks
    up on its next query. (Amended 2026-08-21, ALL GOVERNANCE STATE IS RETAINED:) a branch carries the
    org''s governance with it, whole. Roles, RLS rules, masking and column grants, tags and tag parameters,
    domain assignments, the glossary and its edges, naming rules, metrics, relationships and registered
    tables are all CARRIED classes (REQ-1489), so a branch is governed exactly as its base is rather than
    being an ungoverned scratch copy that behaves like the real thing only until it is merged. Three things
    follow, and they are the reason this matters more than the credential convenience. A developer''s
    query in a branch returns what the same query returns in the base -- same row filters, same masked
    columns, same denied columns -- so a model built in a branch is built against the governance it will
    actually run under, and "it worked in dev" stops being a different statement from "it works in prod".
    A governance change is itself a modelled object, so tightening an RLS rule is proposed, reviewed and
    merged by the same REQ-1504 path as adding a table, with the same before-and-after report -- there
    is no second, unreviewed channel by which access rules change. And nothing has to be re-applied on
    the way back: a merge carries governance in the same act it carries the model, so a branch can never
    land a table that arrives ungoverned because somebody forgot to copy the policy that covered it. What
    is deliberately NOT retained is the identity behind the governance -- user_directory and user_role_assignments
    are NEVER_SENSITIVE -- so a branch holds the org''s rules without holding the org''s people. (Amended
    2026-08-21, A LOGICAL VIEW IS PORTABLE AND ITS HISTORY IS PROVENANCE:) every view Provisa holds is
    a logical object -- a definition in the model rather than a CREATE VIEW executed against some source
    -- and two properties follow that a physical view cannot have. It is portable: a view travels between
    environments as a row like any other carried object, so it reaches a branch and later reaches prod
    by the same reviewed path, with nobody re-running DDL against a database or reconciling which server
    holds which definition. And its evolution is available: each change to a definition is a change to
    a modelled object proposed and merged through REQ-1504, so the sequence of those merges IS the view''s
    history, with the reviewer, the date and the before-and-after report attached to every step. Together
    those answer a question a warehouse usually cannot -- what did this number mean on the day it was
    reported. A figure produced last quarter can be read against the definition in force last quarter
    rather than the one in force now, and the reason it changed is the merge request that changed it.
    Provenance at a point in time is therefore not a second lineage system to build; it is what environments
    and reviewed merges already record.


    (Amended 2026-08-21, HOW THE BINDING IS ENFORCED:) the environment is part of the IDENTITY of a runtime
    and not a variation within one, so an org''s prod and each of its branches hold separate runtimes
    -- separate pools, compiled schemas and catalogs -- keyed by (org, environment), with prod keyed on
    the bare org id so an org that never created an environment is registered exactly where it always
    was. The environment reaching that runtime travels in a second request-scoped variable beside the
    org, because the two are bound by different authorities: the org comes from the authenticated identity,
    the environment from the x-provisa-env header, which is a selection inside an org the caller already
    proved membership of. The name on that header is CHECKED against the org''s registry before anything
    is bound to it -- an unchecked name reaches the schema initializer, which CREATES the schema it is
    given, so a typo would silently provision and serve a nameless empty environment -- and an unknown
    name is refused rather than quietly falling back to prod, because a caller who believed they were
    writing to a branch would otherwise write to production and be told it succeeded. For the same reason
    a request naming an environment whose runtime was never built is refused rather than served by the
    default one. Where a branch''s sources actually point is resolved ONCE per runtime build, at the single
    seam between the sources rows and the runtime that reads them, and each resolved source records WHICH
    environment supplied its binding; a source no environment in the chain bound is left exactly as its
    row has it, unbound, because an empty host is not an absent one. The write check then reads that record
    on the ONE governed pipeline every raw-SQL surface funnels through, and refuses an INSERT, UPDATE,
    DELETE or MERGE whose target is reached through a binding its supplier does not admit writes from
    -- as it refuses one whose target is unbound, or whose target is not a registered table and so cannot
    be attributed to a binding at all.


    (Amended 2026-08-21, THE CHAIN IS WALKED ONCE AND GUARDED:) the lineage walk refuses a cyclic branched_from
    chain by name -- saying which environment closed the ring -- rather than leaving the depth limit to
    catch it, because a ring reported as a chain deeper than the limit describes a tree no organization
    has and sends the reader looking for one. branched_from is written once, at branch time, against an
    environment that already exists, so a repeat is a corrupted registry; the guard is still carried because
    the walk runs on every binding resolution and an unguarded ring would hold an admin-plane connection
    forever. (Amended 2026-08-21, THE ENGINE CATALOG IS NAMESPACED BY ENVIRONMENT:) a source''s physical
    catalog name on the federation engine carries the environment as well as the organization, as org_<id>_env_<env>__<catalog>.
    The coordinator''s catalog namespace is shared across every runtime, and a branch resolves its own
    bindings -- so the same source id in a branch may reach a different host than it does in the base.
    Registered under the base''s name a branch would not shadow the base''s catalog, it would REPLACE
    it, and the base would afterwards be querying the branch''s database. prod keeps the pre-environment
    name, including the default organization''s bare one, so an organization holding only prod is byte-identical;
    a branch is prefixed even in the default organization, because the bare-name exemption belongs to
    prod and not to the organization.


    (Amended 2026-08-22, THE BRANCH POLICY IS WITHDRAWN AND ROLES ANSWER INSTEAD, REQ-1539:) "what a branch
    may DO through an inherited binding" is no longer a property of the base. It was a conflation of two
    different writes -- writing the semantic model and writing the underlying data -- and only the first
    was ever an environment''s business. Data rights are the roles'', and roles are a CARRIED class, so
    each environment holds its own copy: developer can be unrestricted in dev and hold nothing at all
    in prod, which is the same statement the flag was reaching for, made where an org already states who
    may do what. user_role_assignments is CARRIED with them -- otherwise a member would arrive in the
    new environment holding no role and the per-environment role definitions would have nobody to apply
    to. What is still NEVER_SENSITIVE is user_directory: a branch holds the org''s rules and who holds
    which of them, not a copy of the org''s people. Inheriting a binding is now purely about WHERE a source
    points; permission is a question asked and answered elsewhere. (Amended 2026-08-22, WHY THE BINDING KEEPS ITS OWN
    MECHANISM:) The same fall-through could be had by scoping secret references per environment, which
    would delete this inheritance, REQ-1491''s strip and REQ-1489''s allow-list in one stroke -- and it
    is deliberately NOT the design. It would be right only for a shop that already keeps a clean secret
    per environment, and most do not: what they have is one set of credentials somebody authored once.
    Inheritance by reference asks that shop for nothing -- an org_admin binds a base and every branch of
    it already points somewhere -- while per-environment references would ask them to build the
    per-environment secret story FIRST, before anyone could branch at all. The mechanism is kept because
    it is the one that works before a customer has worked anything out.'
  use_case: An org_admin sets up dev, staging and prod once, each with its own credentials and its own
    members; a developer branches dev to fix a metric, is querying real-shaped data immediately without
    seeing a credential, and their branch can write to dev and to nothing else.
  scenario: 'Given an org_admin has created a base environment bound with its own credentials


    When a member of that base creates a branch from it


    Then the branch inherits the base''s bindings by reference and the member enters no credentials


    Given the base is marked branch-writable


    Then writes from its branches through the inherited binding are allowed


    Given the base is left at the default


    Then writes from its branches through the inherited binding are refused, whatever rights the writer
    holds


    When a branch attempts to mark itself writable or rebind to another target


    Then it is refused: a branch never widens what it inherited


    When a member attempts to branch a base they are not a member of


    Then it is refused


    When an org_admin deletes a base that still has branches inheriting from it


    Then it is refused until those branches are gone


    Given the org_admin has authored the secrets a base''s bindings resolve against


    When a branch of that base runs a query through an inherited binding


    Then the secret resolves from the base and the branch holds no copy of it


    When the org_admin rotates that secret on the base


    Then every branch of the base uses the rotated secret with no action of its own

    '
  code:
  - provisa/api/org_runtime.py
  - provisa/api/env_routing.py
  - provisa/api/app.py
  - provisa/compiler/naming.py
  - provisa/api/app_loaders.py
  - provisa/pgwire/_pipeline.py
  - provisa/core/env_bindings.py
  - provisa/core/env_store.py
  - provisa/core/schema_admin.py
  - provisa/api/admin/environments_router.py
  - provisa-ui/src/components/EnvironmentBindings.tsx
  tests:
  - tests/unit/test_env_runtime_routing.py
  - tests/unit/test_env_binding_writes.py
  - tests/integration/test_env_bindings.py
  - tests/integration/test_env_runtime_bindings.py
  - tests/unit/test_env_merge_request_endpoints.py
  - tests/unit/test_env_lineage.py
  related_reqs:
  - REQ-1488
  - REQ-1489
  - REQ-1491
  - REQ-1492
  - REQ-1499
  - REQ-1503
  - REQ-1528
- id: REQ-1530
  status: proposed
  target: 2026-Q4
  group: 13. Multi-Tenancy & Organizations
  category: Environments
  priority: SHOULD
  type: behavioral
  description: 'A DEVELOPER IS ALWAYS LIMITED BY DOMAIN MEMBERSHIP. The role says what kind of act a member
    may perform -- build a view, define a relationship, write a row -- and their domain membership says
    which objects those acts may touch. The two together are the whole rule, and it holds everywhere rather
    than only inside a branch: a developer in the org is limited by their domains, and a developer inside
    a branch they own (REQ-1528) is limited by the same domains, so branching changes what someone may
    do and never what they may do it to. That is the property worth having. It means a branch owner cannot
    reach across into another team''s part of the model, it means a merge request lands in front of the
    people who own the domains it touches rather than in front of the one org_admin who owns all of them,
    and it means neither of those is a special rule about branches -- both fall out of the ordinary role-plus-domain
    check the org already runs. THE SCOPE ALREADY EXISTS. Every governed object is attached to a domain
    (registered_tables.domain_id), and a role already carries domain_access -- ``*`` for every seeded
    role today, which is exactly what makes the current developer unlimited. Limiting a developer is therefore
    narrowing an existing field rather than adding an axis: an org_admin puts a member on the domains
    they work in, and the check that already reads domain_access starts answering differently. WHY THIS
    AND NOT AN INTERSECTION RULE. An earlier form of this made branch authority the intersection of environment
    ownership and domain membership, which is the same outcome reached by a rule that exists only for
    branches and has to be stated, tested and remembered separately. Limiting the role itself is simple
    to understand: one sentence covers prod and every branch of it, and there is no second place for the
    two answers to disagree. (Amended 2026-08-21, MEMBERSHIP IS PER ROLE:) the domain limit is carried
    by the ROLE, in ``roles.domain_access``, and not by the individual grant. The schema also holds a
    domain on each assignment row (``user_role_assignments.domain_id``, written as ``*`` by every programmatic
    grant), and it would have been possible to make a member''s reach the intersection of the two. It
    is not. The role is the scope, for the same reason the role is the capability set: a member''s rights
    are read off the role they hold, in one place, and adding a second scope that has to be intersected
    with the first gives every authorization question two answers that can disagree and a reader no way
    to tell which one governs. So an org_admin scopes a developer by giving them a role whose domain_access
    names their domains -- a sales developer role rather than a developer role granted on sales -- and
    the check that already reads domain_access for queries is the same check that answers whether an edit
    is permitted. The assignment row''s domain stays what it is today, the record of which grant was made,
    and enforcement does not read it. (Amended 2026-08-21, THIS STANDS ON ITS OWN:) per-domain scoping
    of a developer is not a consequence of the environments work and does not depend on it. It is the
    answer to a question the product already had -- an org with more than one domain wants the sales team
    building sales models and not finance ones -- and it would be worth having if branches were never
    built. Environments only made the omission visible, because a branch owner acquires developer rights
    without an org_admin granting them, and an unscoped developer role would have let that reach every
    domain. So the rule is stated once for the org and inherited by every branch, rather than being a
    branch feature: it governs prod for a directly-granted developer exactly as it governs a branch for
    its creator.'
  use_case: An org_admin puts a developer on the sales domain; the developer branches dev, changes sales
    metrics freely, cannot change a finance table in the same model, and the merge goes to whoever owns
    sales.
  scenario: 'Given a developer whose domain membership is the sales domain


    When they edit an object in the sales domain, in the org or in a branch they own


    Then it is allowed


    When they edit an object in the finance domain, in the org or in a branch they own


    Then it is refused, and the object remains readable to them


    Given a role whose domain_access is ``*``


    Then its holder is limited by no domain, which is the state of every seeded role


    Given a merge request touching objects in one domain only


    Then it awaits a decision from that domain''s owners rather than from an org_admin


    '
  code:
  - provisa/core/env_authority.py
  - provisa/core/models.py
  - provisa/core/repositories/role.py
  related_reqs:
  - REQ-609
  - REQ-1489
  - REQ-1504
  - REQ-1528
  - REQ-1529
- id: REQ-1531
  status: proposed
  target: 2026-Q4
  group: 13. Multi-Tenancy & Organizations
  category: Environments
  priority: SHOULD
  type: behavioral
  description: 'EVERY MUTATION THAT NAMES A DOMAIN MUST BE GATED ON IT, AND EVERY MUTATION THAT OUGHT
    TO NAME ONE MUST NAME ONE. REQ-1530 makes a role''s domain_access the limit on what a member may change;
    a limit is only real where it is checked, and today the check runs in two places. ``require_capability(info,
    cap, domain_id=...)`` already exists and already answers correctly -- registering a table and updating
    a table pass it -- so this is about the ops that never call it and the ops that have no domain to
    pass. WHAT IS ALREADY GATED: register_table on the non-view path, and update_table. WHAT NAMES A DOMAIN
    AND IS NOT GATED: delete_table (no capability check at all), upsert_rls_rule and delete_rls_rule (no
    check at all, and domain_id is already in the input -- a domain-level RLS rule IS the governance of
    somebody else''s domain), upsert_metric and delete_metric (gated on table_registration with no domain;
    a metric''s domain is the domain of the table it measures), tracked_functions and tracked_webhooks
    (both carry domain_id), and tag assignment (the domain is the assignment target''s). WHAT HAS NO DOMAIN
    OF ITS OWN AND NEEDS ONE DERIVED: a VIEW. Registering a view carries the domain it is registered into,
    which gates one half; the other half is the SQL, because free-hand view_sql names its tables directly.
    Without a second check a developer on sales writes a view selecting from a finance table and registers
    it into sales, and the finance data arrives inside a domain finance does not own. Both halves are
    required: the domain being registered into must be one of theirs, and every table the view reads must
    be one they may read. The dependency parse that the materialized-view relationship gate already performs
    yields exactly that table set. Creating a FACT or a DIMENSION is the same shape -- both are registrations
    over existing tables, so both are permitted only over tables within the caller''s domains, and the
    dimension links a fact creates are relationships subject to the rule below. RELATIONSHIPS ARE OWNED
    BY DIRECTION. A relationship row is source_table_id -> target_table_id with its alias unique on the
    source, so the edge hangs off the SOURCE table and the source table''s domain owns it. The target
    is referenced rather than changed, and traversing to it is reading -- the target''s own RLS and masking
    still apply at query time -- so creating an edge requires the source''s domain and does not require
    the target''s. What a cross-domain edge does do is make another team''s table reachable from your
    part of the graph, which its owner should see: such an edge lands needs_review, a column relationships
    already carries. WHAT A DEVELOPER MUST NOT DO AT ANY DOMAIN, because these are escalation rather than
    scope: create_role and delete_role (a role carries domain_access, so minting a role with ``*`` is
    minting yourself out of the limit -- neither has any check today), create_domain and delete_domain
    (creating a domain is not an act inside one, and deleting one CASCADEs the registered_tables and rls_rules
    that belong to it -- neither has any check today), and update_source_allowed_domains (widening a source''s
    allowed list reaches into a domain through the back door). Source registration itself needs no new
    rule: a developer does not hold source_registration (REQ-1528). TWO PROPERTIES OF THE GATE ITSELF.
    It lives in the GraphQL resolver layer, so a guard added there is a guard on one surface -- config
    loading, the environments router and any REST admin router writing these tables go around it, and
    the check belongs low enough that they cannot. And it returns early for an anonymous identity and
    when the org is in single-domain mode, both correct, both meaning the whole scheme is inert until
    an org actually uses domains. FINALLY, AN OUT-OF-DOMAIN EDIT SHOULD QUEUE RATHER THAN ERROR: the queue-a-creation-request
    path already exists for the missing-capability case, and REQ-1530 already routes such a request to
    the domain''s owners, so the developer''s work becomes a proposal instead of a refusal.

    (Amended 2026-08-21, WHAT THE BUILD SETTLED:) The domain half of the gate is now a function of its
    own, ``capabilities.require_domain(info, domain_id)``, because an act can be permitted by more than
    one right — registering a view is allowed to a holder of create_view OR query_development — while
    the question of which domains it may touch has a single answer regardless of which right carried the
    caller in; ``require_capability``''s domain argument now calls it, so the three exemptions (dev/no-auth,
    the platform bypass, single-domain mode) are written once. The scope it reads is ``rights.domain_access_for_claims``,
    the companion of capabilities_for_claims, which resolves claims to ``roles.domain_access`` and never
    to the claim''s ``:domain`` suffix — the previous implementation read the suffix, which contradicted
    REQ-1530. ``*`` is interpreted in one place only, ``env_authority.domains_within``. THE CAPABILITY
    EACH UNGATED MUTATION NOW NAMES: create_domain and delete_domain take org_settings (the org''s domain
    vocabulary is the org_admin''s; a member scoped to sales must not answer their own scoping by minting
    a domain, and there is no domain to be a member of yet, so membership does not apply); create_role
    and delete_role take user_management (a role carries both capabilities and domain_access, so minting
    one is how a member would widen their own scope); upsert_rls_rule and delete_rls_rule take masking_config
    plus the domain the rule lands in — named directly for a domain-level rule, looked up from the table
    for a table-level one; delete_table takes table_registration plus the domain that holds the table,
    looked up from the id; update_source_allowed_domains takes source_registration and is NOT gated by
    membership in the domains being listed, because widening the list is granting reach rather than exercising
    it. A fact and a dim need no gate of their own: both lower to register_table, which is gated. THE
    VIEW''S TWO HALVES are both applied at the free-hand path in register_table — the domain registered
    into, and every table the SQL reads, resolved through ``domain_guard.view_read_domains``; CTE names
    are excluded because a CTE is defined in the statement rather than read from the model, and unparseable
    SQL raises rather than returning an empty set. THE RELATIONSHIP asks about the SOURCE table''s domain,
    and when the target lives in another domain the row is written with ``needs_review`` re-asserted AFTER
    the upsert — rel_repo''s upsert clears that flag on conflict because REQ-020 treats a save as an explicit
    re-review, and a cross-domain edge is not the source steward''s to clear. Both the view and the relationship
    queue a creation request on an out-of-domain edit rather than raising, matching what a missing right
    already does; the mutations that delete or administer raise.

    '
  use_case: A developer on the sales domain writes a view over a finance table and registers it into sales;
    the registration is refused because the view reads a table outside their domains, and what they get
    back is a request awaiting the finance owner rather than an error.
  scenario: 'Given a developer whose role names the sales domain


    When they delete a table, edit an RLS rule or edit a metric in the finance domain


    Then it is refused, because the op is gated on the domain it names


    When they register a view whose SQL reads a finance table


    Then it is refused, whichever domain they register the view into


    When they create a relationship whose source table is in sales and whose target is in finance


    Then it is allowed and the edge is marked needs_review for the finance owner


    When they create a relationship whose source table is in finance


    Then it is refused


    When they create a role, create a domain or widen a source''s allowed_domains


    Then it is refused at any domain, because those change the limit rather than act within it


    Given the org is in single-domain mode


    Then no domain gate applies, and the capability check alone governs


    '
  code:
  - provisa/api/admin/capabilities.py
  - provisa/api/admin/schema_mutation.py
  - provisa/api/admin/schema_mutation_ops.py
  - provisa/core/env_authority.py
  related_reqs:
  - REQ-021
  - REQ-041
  - REQ-042
  - REQ-434
  - REQ-609
  - REQ-1140
  - REQ-1317
  - REQ-1528
  - REQ-1530
- id: REQ-1532
  status: proposed
  target: 2026-Q4
  group: 13. Multi-Tenancy & Organizations
  category: Environments
  priority: MUST
  type: behavioral
  description: 'THE BROWSER MUST BE ABLE TO SAY WHICH ENVIRONMENT IT IS READING, AND EVERY REQUEST IT
    MAKES MUST SAY THE SAME ONE. REQ-1487 makes the environment a request-scoped choice carried in ``x-provisa-env``;
    until now nothing in the UI set it, so every screen read prod and the branch was reachable only from
    curl. The selection is per-browser, kept beside the active org in localStorage under ``provisa_env``,
    and a switcher sits next to the org switcher in the NavBar because the two are the same kind of act
    -- which model am I looking at. THE HEADER RIDES ON ONE INTERCEPTOR, NOT ON CALL SITES: ``installAuthFetch``
    already attaches the bearer and the org to every same-origin request, and the environment is attached
    there for the same reason -- around a hundred REST call sites, none of which should know about this.
    GraphQL does not pass through that path, so Apollo''s link attaches it too; a query left without it
    would read prod''s model while the REST surface beside it read the branch. UNLIKE THE ORG, IT IS ATTACHED
    WITH OR WITHOUT A BEARER: a deployment with auth disabled branches its model exactly as an authenticated
    one does, and gating this on a token would pin every such deployment to prod with no way to say otherwise.
    AN UNSELECTED ENVIRONMENT SENDS NO HEADER rather than sending ``prod``: the server serves prod to
    a request naming none, so clearing the selection is deleting the key. SWITCHING RELOADS THE DOCUMENT
    rather than re-rendering, because the Apollo cache holds the previous environment''s domains, tables
    and roles and a re-render would keep serving them. A SELECTION THE ORG NO LONGER HAS IS REPAIRED,
    NOT FALLEN BACK FROM: the switcher drops a stored name absent from the org''s list and reloads --
    the server''s own answer to an unknown environment is a 404 and never prod, and the two must not disagree.

    '
  use_case: A modeler picks `dev` from the switcher beside the org name; the page reloads and every screen
    -- REST and GraphQL alike -- is reading the dev branch of the model until they pick prod again.
  scenario: 'Given an org with more than one environment


    When the modeler picks one from the NavBar switcher


    Then the name is stored and the document reloads


    And every same-origin request carries it in x-provisa-env, GraphQL included


    When they pick prod


    Then the stored name is removed and no environment header is sent


    Given a deployment with authentication disabled


    Then the header still rides, because such a deployment branches its model too


    Given the stored environment has since been deleted


    Then the selection is dropped and the page reloads reading prod


    '
  code:
  - provisa-ui/src/lib/authFetch.ts
  - provisa-ui/src/apolloClient.ts
  - provisa-ui/src/components/EnvSwitcher.tsx
  - provisa-ui/src/components/NavBar.tsx
  related_reqs:
  - REQ-1317
  - REQ-1487
  - REQ-1523
  - REQ-1533
- id: REQ-1533
  status: proposed
  target: 2026-Q4
  group: 13. Multi-Tenancy & Organizations
  category: Environments
  priority: MUST
  type: behavioral
  description: 'THE ORG''S ENVIRONMENTS MUST BE ADMINISTRABLE FROM THE ADMIN UI, AND ITS REPOSITORY MUST
    BE READABLE THERE. Everything REQ-1487..REQ-1531 built was API-only. This adds one admin page at ``/admin/environments``,
    gated on org_settings like every other org-scoped section, with four panels. ENVIRONMENTS: the list
    with its derived facts -- drifted, protected, branch_writable, what it was branched from -- plus create
    (naming what to branch from, and whether it is a BASE per REQ-1529), delete, protect, and the branch-write
    policy. Delete is withheld for prod rather than offered and refused. MERGE: a modal naming the source,
    whether removals are carried, and a PREVIEW that is a separate act from the merge it describes --
    the preview endpoint is a GET so a CI runner cannot apply by accident (REQ-1527). Against a protected
    target the button says Propose, because REQ-1504 turns that merge into a request rather than refusing
    it, and the message field appears with it. REQUESTS: the open proposals with the report each was filed
    with -- that report is what an approver agrees to, so it is shown rather than recomputed -- and approve/reject
    with a note, offered only on a request that is still open and only to somebody who may decide it.
    A state of ``stale`` is the server''s derivation at read time, never a column. REPOSITORY (REQ-1524
    BROWSE): branches, a branch''s commits, the paths at a ref and one file''s text, with an optional
    read-only diff against a second ref. THE BRANCH LIST IS NOT THE ENVIRONMENT LIST -- a ref outlives
    the environment that wrote it, which is the whole reason an earlier state is loadable -- and a commit
    sha is a ref like any other. Paths and text are two calls because a single call carrying every definition
    would download the org''s model to draw a sidebar, and the file list is held together with the ref
    it was read at so that a ref change never asks for the previous ref''s path. The diff withholds the
    editable side and the revert arrows: both sides are history, and an edit there would have nowhere
    to go -- changing the model is the model surface, and undo is loading an earlier sha. INTEGRATION
    (REQ-1527): the remote and the status webhook, in plain text inputs rather than password ones, because
    what belongs in them is a secret REFERENCE resolved at push time and masking one would imply the opposite.
    A cleared field is sent as null -- the org saying it no longer mirrors anywhere.

    '
  use_case: An org_admin opens Environments, branches `dev` from prod, merges it back with a preview first,
    reads the commit the merge wrote in the repository panel, and points the org''s remote at a GitHub
    URL carrying a secret reference rather than a token.
  scenario: 'Given an org_admin on /admin/environments


    When they create an environment naming what it branches from


    Then it appears with what it was branched from and whether it may write through inherited bindings


    When they preview a merge


    Then the report is shown and nothing has been applied


    When the target is protected


    Then the action is Propose, and it becomes a request somebody else decides


    When they open the repository panel


    Then the branches include refs whose environments have been deleted


    And selecting a ref lists its paths, and selecting a path shows that file at that ref


    And a comparison is read-only on both sides


    Given a member without org_settings


    Then the page is not offered in the nav and the administering controls are absent


    '
  code:
  - provisa/api/admin/environments_router.py
  - provisa-ui/src/api/environments.ts
  - provisa-ui/src/components/admin/EnvironmentsTab.tsx
  - provisa-ui/src/components/admin/MergeRequestsPanel.tsx
  - provisa-ui/src/components/admin/RepoBrowser.tsx
  - provisa-ui/src/components/admin/RepoIntegrationPanel.tsx
  related_reqs:
  - REQ-1487
  - REQ-1504
  - REQ-1524
  - REQ-1527
  - REQ-1528
  - REQ-1529
  - REQ-1532
- id: REQ-1534
  group: 13. Multi-Tenancy & Organizations
  category: Environments
  priority: MUST
  type: behavioral
  status: proposed
  target: 2026-Q4
  description: 'AN ENVIRONMENT BEYOND prod MUST BE REFUSED ON A CONTROL PLANE THAT HAS NO SCHEMAS, AND
    NO SCHEMA DDL MAY BE EMITTED THERE. REQ-1488 makes an environment physically a schema, which is what
    keeps one environment''s rows out of another''s read. SQLite -- the store the desktop tier ran before
    REQ-1535 made embedded PostgreSQL the default, and one an operator may still select -- has no schema
    concept at all: init_schema''s portable bootstrap ignores the env argument entirely and creates the
    tables in the backend''s one namespace. Provisioning an environment there therefore reported success
    while writing the new environment''s model on top of prod''s. The creation is refused instead, at
    the endpoint before the name is reserved (409 environments.plane_unsupported) and again in provision_org
    for every other caller (EnvironmentPlaneError), so the org holds prod and nothing else. Separately,
    DROP SCHEMA ... CASCADE is PostgreSQL-only DDL and does not parse on such a backend: both deprovision_org
    and provision_org''s compensating rollback now emit it only where schemas exist. Emitting it there
    was worse than useless -- the rollback raised a syntax error of its own, which replaced the failure
    it was compensating for and was the only thing the caller ever saw.'
  use_case: A maintainer running the SQLite demo tries to branch `dev` from prod and is told plainly that
    environments need a PostgreSQL control plane, instead of receiving a sqlite3 syntax error raised by
    a rollback.
  scenario: 'Given an org on a control plane whose dialect is not postgresql


    When a member creates an environment


    Then the request is refused with environments.plane_unsupported and no name is reserved


    And the org still holds prod and nothing else


    Given an environment provisioning that fails on any plane


    Then the rollback drops schemas only where schemas exist


    And the original failure is what reaches the caller


    '
  code:
  - provisa/core/environments.py
  - provisa/core/org_provisioning.py
  - provisa/api/admin/environments_router.py
  - tests/unit/test_org_isolation.py
  related_reqs:
  - REQ-701
  - REQ-1487
  - REQ-1488
  - REQ-1523
- id: REQ-1535
  group: 11. Platform, Infrastructure & Delivery
  category: Control Plane
  priority: MUST
  type: structural
  status: proposed
  target: 2026-Q4
  description: 'EMBEDDED POSTGRESQL MUST BE THE DEFAULT CONTROL-PLANE STORE IN EVERY SELF-CONTAINED TIER
    (demo and native), REPLACING SQLITE. REQ-1488 makes an environment a schema and REQ-1534 refuses one
    on a plane without schemas, so a SQLite desktop can hold prod and nothing else -- the demo would be
    unable to show the feature it exists to demonstrate, and a native install would differ from the hosted
    tier in what it can express rather than only in scale. The bundled instance is pgserver, a real PostgreSQL
    with a data directory: the profile resolver starts it (persistent and idempotent, so resolving the
    profile is what brings the plane up) and reads the socket directory and port BACK from the started
    server rather than assuming a port, because a guessed port names a socket path that does not exist.
    There is no in-memory form -- an ephemeral profile asking for this store is refused and told to pick
    sqlite, which then holds only prod. Demo and native keep separate data directories so a demo reset
    cannot touch a native install''s plane. A pristine demo start drops the database instead of deleting
    files (V1 has no migrations, so pristine means rebuilt at the current schema), retaining org_settings
    across the wipe by pg_dump and restore.'
  use_case: A maintainer runs the desktop demo and branches `dev` from prod, because the demo plane is
    the same PostgreSQL the hosted tier runs.
  scenario: 'Given the demo or native preset


    When the profile is resolved


    Then the bundled PostgreSQL is started and both plane URLs point at its socket


    Given the same preset with ephemeral true


    Then the profile is refused rather than falling back to an in-memory store'
  code:
  - config/capabilities.yaml
  - provisa/core/desktop_profile.py
  - provisa/core/control_plane_pg.py
  - start-ui-install.sh
  related_reqs:
  - REQ-828
  - REQ-889
  - REQ-1488
  - REQ-1534
- id: REQ-1536
  group: 11. Platform, Infrastructure & Delivery
  category: Control Plane
  priority: MUST
  type: behavioral
  status: proposed
  target: 2026-Q4
  description: 'EVERY CONTROL-PLANE STORE THE CAPABILITY CATALOG OFFERS MUST BE OPENABLE, AND THE ENVIRONMENT
    SUPPORT OF EACH MUST BE ESTABLISHED BY PROBING THE LIVE BACKEND RATHER THAN ASSERTED. config/capabilities.yaml
    offers sqlserver and oracle as control-plane stores, but _ADMIN_ASYNC_DRIVER lists only postgresql,
    sqlite, duckdb, mysql and mariadb -- so selecting either yields "unsupported control-plane store backend"
    at engine construction, an option the wizard offers and the loader refuses. The probe suite settles
    the separate question REQ-1534 raises, per backend and against the real server: whether the STORE
    can hold two same-named tables in two namespaces (postgresql, mysql/mariadb, sqlserver and oracle
    all can; sqlite cannot), and whether PROVISA can enter one of them via Capabilities.enter_org_sql
    (postgresql, mysql/mariadb and oracle can; sqlserver has no dialect branch at all and so reports no
    schemas). The two facts are reported apart because they fail apart: a store with namespaces and no
    scoping statement is a missing dialect entry, not a limit of the backend, and recording it as the
    latter would make a five-line fix look like an unsupported store.'
  use_case: A maintainer choosing a control-plane store is told which ones can hold environments on evidence
    from the backend itself, and any store the catalog offers can actually be opened.
  scenario: 'Given each control-plane store the catalog offers


    When an org provisions prod and then an environment on it


    Then the backend either keeps the two namespaces apart or is refused by REQ-1534


    And no offered store fails at engine construction'
  code:
  - config/capabilities.yaml
  - provisa/core/database.py
  - tests/integration/test_control_plane_env_capability.py
  related_reqs:
  - REQ-828
  - REQ-1488
  - REQ-1534
  - REQ-1535
- id: REQ-1537
  group: 11. Platform, Infrastructure & Delivery
  category: Control Plane
  priority: SHOULD
  type: behavioral
  status: proposed
  target: 2026-Q4
  description: CONFIGURING A REPOSITORY REMOTE MUST PROBE IT, AND OFFER TO CREATE IT WHEN IT IS NOT THERE
    -- NEVER CREATE IT SILENTLY. REQ-1527 pushes each model change to the org's own remote, and a remote
    that does not exist turns every commit into a drift the operator discovers later from a badge. The
    probe runs when the remote is CONFIGURED, which is the moment the operator is present to answer. A
    local path is a repository when it holds HEAD, directly or under .git; a hosted URL is one when its
    refs are reachable. Creation is offered for a missing local path always, and for a missing github.com
    or gitlab.com repository when the stored remote carries a credential -- the credential is read from
    the remote itself and from nowhere else, because REQ-125 and REQ-1525 keep the token out of the control
    plane and leave the ${env:} reference as the only place it lives. Anything else -- an unknown host,
    or a hosted URL with no credential -- is reported as missing and not offered, because creating it
    needs an API this does not have or an authorization nobody gave. Creation is a separate act with its
    own audit entry; a probe never writes.
  use_case: An operator wiring an org to a repository that does not exist yet is told so and offered creation,
    instead of discovering it from a drift badge after the first model change.
  scenario: 'Given an org whose configured remote names no repository


    When the operator configures the repository integration


    Then the probe reports it missing and says whether it can be created


    And nothing is created until the operator asks for it'
  code:
  - provisa/core/env_remote.py
  - provisa/api/admin/environments_router.py
  - provisa-ui/src/api/environments.ts
  - provisa-ui/src/components/admin/RepoIntegrationPanel.tsx
  tests:
  - provisa-ui/src/__tests__/RepoIntegrationPanel.test.tsx
  - tests/unit/test_env_remote.py
  related_reqs:
  - REQ-125
  - REQ-1525
  - REQ-1527
- id: REQ-1538
  group: 13. Multi-Tenancy & Organizations
  category: Environments
  priority: MUST
  type: behavioral
  status: proposed
  target: 2026-Q4
  description: 'THE CHOICE REQ-1529 CALLS BASE-OR-BRANCH MUST BE PRESENTED AS "INHERIT CONNECTIONS", AND
    MUST DEFAULT TO OFF. The governing use case is making a dev environment from prod: the operator wants
    prod''s whole model and specifically does NOT want prod''s connection strings, so the outcome reached
    by touching nothing has to be the one that points nowhere yet. The earlier "Base" checkbox inverted
    that -- left alone it produced an environment resolving production''s host, port, database and username
    by reference -- and named the concept after a position in a tree rather than after what the operator
    is deciding. WHY "CONNECTIONS" AND NOT "CREDENTIALS": sources carries no password column at all; what
    a copy would carry forward is the connection coordinates -- host, port, database, username, dialect,
    path, federation_hints, mapping, cdc, base_url, spec_url, bootstrap_servers, topic -- while the credential
    itself lives in secrets and is never copied by anything (REQ-1491, REQ-1525). Naming the box after
    credentials would name the one thing that was never at stake. The setting is not merely about the
    environment being created: it decides whether that environment owns its connections, and owning them
    is what makes it something later environments can inherit FROM, so the same box also settles how branching
    from it will behave. The wire field is inherit_connections, false by default. A member who cannot
    administer the org sees the box checked and disabled rather than hidden, because inheriting is the
    only kind of environment REQ-1529 lets them create and a hidden box would leave them pressing Create
    into a 403 with nothing on screen to explain it.'
  use_case: An operator creating dev from prod gets prod''s model with no connections and binds each source
    to their own database, without having had to notice a checkbox to avoid pointing at prod.
  scenario: 'Given an operator creating an environment from prod


    When they leave the "Inherit connections" box alone


    Then the new environment carries prod''s model and none of prod''s connections


    And every source in it is unbound until they bind it'
  code:
  - provisa/api/admin/environments_router.py
  - provisa-ui/src/components/admin/EnvironmentsTab.tsx
  - provisa-ui/src/api/environments.ts
  tests:
  - provisa-ui/src/__tests__/EnvironmentsTab.test.tsx
  related_reqs:
  - REQ-1489
  - REQ-1491
  - REQ-1525
  - REQ-1528
  - REQ-1529
- id: REQ-1539
  status: proposed
  target: 2026-Q4
  group: 13. Multi-Tenancy & Organizations
  category: Environments
  priority: SHOULD
  type: behavioral
  description: 'What a person may do to DATA inside an environment is what their ROLES say, and an environment
    never overrides it. An environment is a namespace for the MODEL -- REQ-1488 makes it a schema -- and
    the rights that decide whether a query may write, read full results, or use a source at all are the
    same rights that decide it everywhere else. There is no second permission system layered over the
    one an org already runs. Three things change to make that true rather than merely stated. First, the
    ``branch_writable`` flag is REMOVED: the column, the policy endpoint, the switch in the environments
    table, and the write-time check that read it. It existed to stop a branch of prod from writing to
    prod, but it was set on the wrong object and it answered the wrong question -- it was being read as
    a rule about editing the semantic model, which it never was. Second, the escalation it was compensating
    for is fixed where it arose: the authority REQ-1528 confers on whoever creates an environment is the
    seeded ``developer`` role LESS ``write``, ``full_results`` and ``usage``. Creating an environment
    is a model-authoring act; it hands over model-authoring rights and nothing over data. With that removed
    there is no member action that produces write authority over data anywhere, so no flag is needed to
    bound one. Third, ROLES and their ASSIGNMENTS are both CARRIED classes (REQ-1489), which is what lets
    an org state the intent the flag was reaching for, in the place an org already states who may do what:
    each environment holds its own ``roles`` rows, so ``developer`` can be unrestricted in dev and hold
    zero data rights in prod, while ``user_role_assignments`` travels alongside so a member arrives in
    a new environment holding the same role names those per-environment definitions apply to. Only ``user_directory``
    and ``org_secrets`` stay NEVER_SENSITIVE -- a branch holds the org''s rules and who holds which of
    them, never a copy of the org''s people or their secrets. WHAT REMAINS ON THE WRITE PATH is not a
    permission at all: REQ-1491 still refuses a write into a table nobody registered, and into a source
    unbound in this environment and in every environment it inherited from, because in neither case can
    the binding the write would travel be established -- there is no target, which is a different refusal
    from "you may not". Both are checked on the one governed pipeline every raw-SQL surface funnels through.
    (Amended 2026-08-22: ROLES and ASSIGNMENTS are CARRIED only by a CREATION. They form the SEEDED_AT_CREATION
    class -- a subset of CARRIED that travels when an environment is created and never again. A new environment
    needs them or it opens with nobody able to act, so the create path seeds them from the environment
    it came from; from that moment they are that environment''s own answer to who may do what. No merge
    and no checkout carries them. Without this the review path of REQ-1504 would itself be the escalation
    route: merging an unrestricted branch ``developer`` row into prod would overwrite the restricted one
    that is the whole guarantee, arriving as a model change rather than as a rights change. ``copy_model``
    and ``plan_copy`` therefore take a ``seed`` flag, default false, and only the create endpoint passes
    it.) (Amended 2026-08-22: A LOAD IS HELD TO THE SAME RULE, and this is what keeps a private control
    plane from becoming a way around it. On their own desktop (REQ-1128) a developer is the administrator
    of their own deployment and can grant themselves anything there, which is correct and costs nothing,
    because rights are per control plane and no control plane writes to another (REQ-1496). The one channel
    out is the branch, and the branch carries the projected ``roles`` -- so a load that applied them would
    carry a desktop''s self-granted rights into whatever loaded it. It does not: a load applies CARRIED
    minus SEEDED_AT_CREATION, identically to a merge, and seeds only when the load is what CREATES the
    environment. ``user_role_assignments`` is not projected into the tree at all, so who holds a role
    never leaves the control plane that decided it. Editing a role in a private control plane changes
    that control plane and nothing else, and the branch it produces is a proposal about the MODEL. AND
    UNDERNEATH ALL OF IT the rights question is moot for data, because there is nothing to reach: bindings
    never travel (REQ-1491), a source arriving in a tree arrives unbound, and the connection values it
    would need are secret REFERENCES resolved against the deployment''s own secret store at connect time
    (REQ-125, REQ-1525). A developer who grants themselves every capability on their desktop has granted
    themselves every capability over the sources they bound with their own credentials. Prod''s databases
    are not refused to them; they are not addressable from there at all. (Amended 2026-08-22, THE ROLE
    FILE IN THE TREE, and why it is not encrypted:) The remaining way to try this is the crudest one --
    opening the projected ``roles/*.yaml`` in an editor, writing whatever rights are wanted, committing
    that, and loading it. It does nothing, and it does nothing POSITIONALLY rather than by a check that
    inspects the file: ``roles`` is SEEDED_AT_CREATION, a load applies ``PROJECTED - SEEDED_AT_CREATION``,
    so on every load into an environment that already exists the role files in the tree are not read
    at all -- a hand-written role is exactly as inert as a role the projector produced. The one load
    that does seed is the load that CREATES the environment, and what it creates is unbound (REQ-1491):
    the hand-written rights govern a model that addresses no database. There is no load that both seeds
    and lands somewhere already bound, because an environment is created once and prod exists from the
    org''s first moment. ENCRYPTING THE ROLE DEFINITIONS IS THEREFORE REJECTED. It would buy nothing --
    the files are already ignored on the path that matters -- and it would cost the property this whole
    design is built on: a tree is a PROPOSAL, and a proposal is reviewed. Rights are the part of a change
    a reviewer most needs to read in the diff, and an encrypted blob in a pull request is a change nobody
    can review. Key distribution makes it worse rather than better: whoever loads must decrypt, so the
    key is on the desktop that was the thing being defended against. The guarantee is which CLASS a table
    is in and which ACT is allowed to seed it, and that guarantee holds against an attacker with full
    write access to the tree -- which is the correct place for it, because they have exactly that.) (Amended 2026-08-22, HOW A ROLE DEFINITION IS ESTABLISHED IN A BRANCH, AND WHO MAY CHANGE IT:) A role definition is a ROW IN THE ENVIRONMENT''S OWN SCHEMA. ``roles`` exists only in an org schema, the request''s ``x-provisa-env`` selects which schema the tenant connection is opened against, and the capability check reads the roles it finds THERE. So there is no shared definition to inherit or diverge from: every environment holds a full copy of every role, and ``org_id IS NULL`` on such a row means only that it came from the seed template, never that it is shared across environments. The rows are ESTABLISHED once, by the creating act seeding them from the parent, and after that no merge, no load and no checkout writes them. That is the whole of the inheritance: a branch STARTS as a copy of what it came from, and immediately owns it. AN INHERITOR CANNOT CHANGE THEM -- not in the environment they inherited from and not in their own -- because every role endpoint requires ``user_management``, and REQ-1528 confers on whoever creates an environment the seeded ``developer`` role less the data rights, which never held ``user_management`` to begin with. Nothing about creating a branch is a grant of authority over rights. AN ORG_ADMIN DOES CHANGE THEM by editing the role in the environment they are administering: they hold ``user_management``, the request names the environment, and the write lands on that environment''s copy and nowhere else. This is the mechanism by which a lower lane holds different rights from prod -- ``developer`` unrestricted in dev and stripped of data rights in prod is two edits to two rows, not a policy expressed anywhere. THE EARLIER REFUSAL OF EVERY ``org_id IS NULL`` ROW IS THEREFORE WITHDRAWN: it made exactly the differentiation this requirement promises unreachable through the API, and it protected nothing, because the row it refused was already private to one environment. What replaces it is narrower and is about lock-out rather than about sharing: ``org_admin`` and ``platform_admin`` may not be edited, because they are the roles that carry ``user_management`` itself and an administrator who narrowed their own role would leave the environment with nobody able to undo it. ``analyst``, ``developer`` and ``modeler`` are editable per environment. Deleting a seeded role stays refused in every environment, since assignments name it.)'
  use_case: An org_admin makes developer unrestricted in dev and gives it no data rights in prod; a developer
    branching either one writes freely in dev and cannot touch prod''s data, without anyone setting a
    per-environment policy flag.
  scenario: 'Given an org whose ``developer`` role holds no write right in prod


    And a member who creates an environment from prod


    When they issue an UPDATE against a source bound in that environment


    Then it is refused because their roles confer no write right


    And the same member issuing the same UPDATE in dev, where developer holds write, succeeds'
  code:
  - provisa/core/env_authority.py
  - provisa/core/env_classes.py
  - provisa/core/env_bindings.py
  - provisa/core/env_copy.py
  - provisa/core/schema_admin.py
  - provisa/pgwire/_pipeline.py
  - provisa/api/admin/environments_router.py
  - provisa-ui/src/components/admin/EnvironmentsTab.tsx
  tests:
  - tests/unit/test_env_authority.py
  - tests/unit/test_env_classes.py
  - tests/unit/test_env_binding_writes.py
  - tests/integration/test_env_bindings.py
  - tests/integration/test_env_copy.py
  - tests/integration/test_env_deploy.py
  - tests/unit/test_env_deploy_endpoint.py
  related_reqs:
  - REQ-1488
  - REQ-1489
  - REQ-1491
  - REQ-1492
  - REQ-1528
  - REQ-1529
- id: REQ-1540
  status: proposed
  target: 2026-Q4
  group: 10. UI & Admin Surfaces
  category: Admin UI
  priority: SHOULD
  type: behavioral
  description: 'A single-tenant deployment shows no organization in the navbar. The org switcher is a
    multi-tenancy control: it names the active org so a person who belongs to several knows which one
    they are looking at, and lets them change it. With multi-tenancy off there is exactly one org, so
    the name answers a question nobody can ask and offers a change nobody can make -- it is a permanent
    label consuming navbar space. The component renders nothing when ``multitenancy`` is false, ahead
    of every other condition, including the ``cross_org`` right: seeing every org is still one org here.'
  use_case: A single-tenant install shows the navbar without a standing organization label.
  scenario: 'Given a deployment with multitenancy off


    When any user loads the application


    Then the navbar shows no organization name and no organization switcher'
  code:
  - provisa-ui/src/components/OrgSwitcher.tsx
  tests:
  - provisa-ui/src/__tests__/OrgSwitcher.test.tsx
  related_reqs:
  - REQ-1337
- id: REQ-1541
  status: proposed
  target: 2026-Q4
  group: 13. Multi-Tenancy & Organizations
  category: Environments
  priority: MUST
  type: behavioral
  description: 'The mirror runs BOTH WAYS. REQ-1527 pushes an environment''s projection to the org''s
    own git host; this brings back what that host holds, so the review that happened there can reach a
    runtime here. Without it the loop is open: a pull request merges on GitHub and nothing in Provisa
    can name the merged branch, because the only refs in the org''s repository are the ones its own
    write-through wrote. FETCH is an ACT, never a poll -- ``POST /admin/orgs/{org}/environments/-/repo-integration/fetch``
    and ``provisa env fetch``, both carrying an identity. Provisa dials somebody else''s git host with
    somebody else''s credential, and doing that on a timer would mean an unbounded number of
    authenticated calls nobody asked for, and would make the answer to "what does origin/main point at"
    depend on when a tick last fired rather than on when a person last asked. What arrives lands in a
    SEPARATE namespace, ``refs/remotes/origin/*``: an environment''s branch is written by the
    write-through and by nothing else, so a fetch onto ``refs/heads`` would let a remote reposition an
    environment behind the control plane''s back. A fetched branch is therefore a tree somebody MAY
    deploy and never a model: ``origin/main`` resolves as a deploy ref (REQ-1496), and the deploy pins
    it to a sha exactly as it pins a local branch. The fetch PRUNES -- a tracking ref the fetch did not
    name is deleted, because the branch is gone from the remote and a stale ref would offer a deploy of
    a tree the org has retired. ``GET .../repo-integration/remote-branches`` reads refs and never the
    network, so listing costs nothing and an empty list means "nothing fetched yet" rather than "the
    remote is empty". The credential rule of REQ-125 survives: the remote is stored verbatim with its
    secret reference, resolved only at the moment of the call, and the refs are imported by Provisa
    rather than by configuring a git remote -- which would write the resolved URL, token and all, into
    a config file on disk.'
  use_case: A pull request merges on the org's git host, and the merged branch is deployed into dev.
  scenario: 'Given an organization whose repository remote holds a branch ``main`` that Provisa has
    never fetched


    When an org administrator calls the fetch endpoint


    Then ``origin/main`` names the commit the remote holds, the org''s own environment branches are
    unchanged, and a deploy of ``origin/main`` applies that commit'
  code:
  - provisa/core/env_ci.py
  - provisa/core/env_repo.py
  - provisa/api/admin/environments_router.py
  - provisa/cli.py
  tests:
  - tests/unit/test_env_remote_tracking.py
  related_reqs:
  - REQ-1527
  - REQ-1496
  - REQ-125
  - REQ-1524
- id: REQ-1542
  status: proposed
  target: 2026-Q4
  group: 13. Multi-Tenancy & Organizations
  category: Environments
  priority: SHOULD
  type: behavioral
  description: 'A merge may END the environment it came from. A feature environment exists to carry one
    piece of work, and when that work has landed in its target the environment is not merely idle -- it
    is a second copy of a model that now lives somewhere else, holding schemas, Redis users and a
    branch, and drifting from the target with every later change. ``retire_source`` on the merge says
    so: applying the merge deprovisions the source environment, forgets its registry row and deletes
    its branch. It is OFF by default and it is never inferred -- a merge is a COPY, and a copy that
    also destroyed an environment unless told otherwise would make the ordinary "take my changes into
    dev" call destructive. THE REF GOES, THE HISTORY DOES NOT: deleting a branch removes a name, and
    every commit that name reached stays in the object store, addressable by sha and deployable by
    REQ-1496 -- so retiring an environment can never lose the work it did. The flag rides on the
    approval row for the same reason ``removals`` does: an approver approves a specific operation, and
    "merge this" and "merge this and end the feature environment" are two different ones. It is NOT
    acted on inside the approval module, because deprovisioning is provisioning rather than approval;
    the caller that applies the approved merge performs the retirement, and the row is the record that
    it was approved. ``prod`` is refused: it is the environment every other one is a branch of, it is
    created with the org and it cannot be ended by a merge into something else.'
  use_case: A feature environment's work is merged into dev and the environment is ended in the same act.
  scenario: 'Given an environment ``feature-x`` branched from ``dev``


    When its owner merges it into ``dev`` with ``retire_source``


    Then ``dev`` holds the merged model, ``feature-x`` no longer exists as an environment or a branch,
    and every commit it made is still deployable by sha'
  code:
  - provisa/core/env_retire.py
  - provisa/core/env_approvals.py
  - provisa/core/schema_admin.py
  - provisa/api/admin/environments_router.py
  tests:
  - tests/unit/test_env_merge_request_endpoints.py
  related_reqs:
  - REQ-1524
  - REQ-1496
  - REQ-1488
  - REQ-1487
- id: REQ-1543
  status: proposed
  target: 2026-Q4
  group: 13. Multi-Tenancy & Organizations
  category: Environments
  priority: SHOULD
  type: behavioral
  description: 'Because every model change commits (REQ-1524), an environment can be MOVED ALONG ITS OWN HISTORY
    rather than edited back by hand. Undo deploys the commit before the one the environment is at;
    redo deploys the one it was at before the undo. Both are the ordinary deploy of REQ-1496 -- the
    same transaction, the same validation, the same audit entry -- so there is no second path that
    writes a model and no operation that can leave an environment holding a tree no commit
    describes. REDO NEEDS A STORED CURSOR AND CANNOT BE DERIVED. The write-through keeps committing
    and the branch is append-only, so after an undo the tip is not where the environment was, and a
    commit may have several descendants; "the child commit" is therefore not an answer. The
    environment row carries ``deployed_sha`` -- where the environment actually is, which is not the
    branch tip once an undo has happened -- and ``redo_sha``, the position an undo departed from,
    cleared by any deploy or write-through commit that is not itself an undo, because a new edit
    makes the abandoned future unreachable in intent even though it remains addressable by sha. UNDO
    IS NOT DESTRUCTIVE: it adds a position, never removes a commit, and the tree it left is
    deployable by sha forever. AMENDED 2026-08-22 -- EVERY ENVIRONMENT STARTS ITS HISTORY AT
    CREATION, NOT AT ITS FIRST EDIT. An environment whose branch holds no commit makes the first
    edit anybody makes the first commit of the line, and REQ-1553 then correctly withholds undo from
    a change that was just made, because the first commit has no parent to step back to. So the
    baseline commit is written by the act that creates the environment: prod''s by the org''s
    creation (already the case for an org made through the admin API, and now also for the org a
    process boots from its config file), and a branch''s by the create call, once the model has been
    copied into it. Both messages say what the environment was created holding rather than
    describing a change, because no change happened -- that is the point. AN ENVIRONMENT''S LINE IS
    NEVER STARTED BY AN EDIT, HOWEVER THE ENVIRONMENT CAME TO EXIST. A branch created from another
    environment is seeded from where that one is standing before anything is committed into it, so
    the new line continues the source''s rather than being a root commit, so ordinary git can move
    between the two, and the position is recorded against that seeded sha because the just-copied
    model writes no commit of its own. The guarantee does not belong to the create call alone: the
    boot gives a baseline to every environment of the org whose branch has no ref -- the org
    assembled from a config file, an environment restored beside a volume that outlived the process
    that made it -- and leaves an environment already standing somewhere exactly as it is. THE
    CLIENT MUST RE-READ BEFORE IT ANSWERS. A model edit moves ``deployed_sha`` server-side and
    announces nothing to the environment switcher, so a menu rendered from what was true at mount
    would keep saying undo is unavailable after the very change that made it available; the switcher
    re-reads the environment rows when it is opened, which is the moment the answer is wanted. AND
    THE ENVIRONMENT''S LINE BEGINS WHERE THE ENVIRONMENT DOES, NOT WHERE THE REPOSITORY DOES.
    Because a branch is seeded at its source''s tip, a parent exists one step below the
    environment''s first own commit -- and that parent is the source''s, a tree this environment
    never held. The sha the branch was seeded at is recorded as the floor of the environment''s
    history, and both the offer and the move stop there: a branch created and then changed once
    gives exactly one undo, not two. CHOOSING AN ENVIRONMENT DOES NOT RELOAD THE PAGE, AND NEITHER
    DOES STEPPING ALONG ITS HISTORY. What the reload was for is that no view keeps rendering the
    model just left, and resetting the cache -- the one in memory and the snapshot persisted beside
    it -- is that: the active queries re-run against the environment now selected, and the
    confirmation is a notification. A reload threw the whole application away and put the loading
    splash back on screen in the middle of a two-click gesture.'
  use_case: An editor undoes a column rename and then redoes it, without editing the model by hand.
  scenario: 'Given an environment whose branch holds three commits and which is deployed at the third


    When its editor undoes twice and then redoes once


    Then the environment holds the model of the second commit, ``deployed_sha`` names it, and all three
    commits remain in the repository'
  code:
  - provisa/core/env_repo.py
  - provisa/core/env_deploy.py
  - provisa/api/admin/environments_router.py
  - provisa/api/app.py
  - provisa-ui/src/components/EnvSwitcher.tsx
  - provisa-ui/src/apolloClient.ts
  - provisa/core/schema_admin.py
  - provisa/core/env_store.py
  tests:
  - tests/unit/test_env_undo_redo.py
  - tests/unit/test_env_create_baseline.py
  - tests/unit/test_bootstrap_prod_baseline.py
  - provisa-ui/src/__tests__/EnvSwitcher.test.tsx
  related_reqs:
  - REQ-1524
  - REQ-1496
  - REQ-1544
- id: REQ-1544
  status: proposed
  target: 2026-Q4
  group: 13. Multi-Tenancy & Organizations
  category: Environments
  priority: SHOULD
  type: behavioral
  description: 'A deploy refreshes ONLY WHAT IT CHANGED. Each environment already has its own runtime,
    keyed ``{org}_env_{name}``, so serving a different environment has never restarted anything shared
    -- the cost a person perceives on a switch is the FIRST build of that environment''s runtime, and
    the second switch to it is a cache hit. What was missing is the other half: a deploy changed the
    model rows and left the cached runtime compiling from the old ones, so an environment could serve a
    model it no longer held. The delta decides the refresh. ``DeployDelta`` already names every added,
    changed and removed path, and every path maps to the table that owns it; if no path in the delta
    belongs to ``sources``, then nothing about CONNECTIVITY changed and the compiled schema and catalog
    map are rebuilt while the source pools are kept. A delta that does touch ``sources`` rebuilds the
    environment''s runtime, because a pool pointed at the old connection is not a stale cache but a
    connection to the wrong database. This is what makes undo and redo (REQ-1543) cheap by
    construction rather than by a special case: moving along the history of a model whose sources
    nobody edited costs a schema recompile. The rule is derived from the delta and never from the kind
    of call -- an undo, a merge and a pipeline deploy that change the same paths refresh identically.'
  use_case: An editor undoes a column rename and the environment serves the earlier model without a runtime rebuild.
  scenario: 'Given an environment whose runtime is built and cached


    When a deploy applies a delta that touches no source


    Then the environment serves the deployed model immediately and its source pools were not rebuilt'
  code:
  - provisa/core/env_deploy.py
  - provisa/api/admin/environments_router.py
  - provisa/api/org_runtime.py
  tests:
  - tests/unit/test_env_deploy_refresh.py
  related_reqs:
  - REQ-1543
  - REQ-1496
  - REQ-1488
- id: REQ-1545
  status: proposed
  target: 2026-Q4
  group: 13. Multi-Tenancy & Organizations
  category: Environments
  priority: SHOULD
  type: behavioral
  description: 'A MERGE LANDS AS ONE COMMIT on the target''s branch -- the source''s line squashed FROM
    ITS CURRENT SHA BACK to where the two branches parted. The merge of REQ-1490 copies a model by
    identity rather than replaying a history, so what the target gains is a single new state, and the
    only projection that matches it is a single commit. Until now the merge wrote model rows and
    committed nothing, which left the target holding a model its branch did not describe: the very
    drift the write-through of REQ-1524 exists to prevent. The squash carries the provenance a reader
    needs and nothing it cannot honour -- the message names the source environment and the sha it was
    at, which is the one end of the range the target took; the other end is the merge base and is
    derivable from the two branches. THE SOURCE IS NOT REWRITTEN. Its commits stay in the object store,
    stay reachable by sha and stay deployable, so a squash adds a state to the target rather than
    removing one from anywhere. The same single commit lands whether the merge was direct or approved
    (REQ-1504), because what an approver approved was the state and not a history, and it records the
    target''s position (REQ-1543) so an undo steps back from the merge rather than through it.'
  use_case: A feature environment is merged into staging and staging's branch gains one commit naming the source and its sha.
  scenario: 'Given a source environment at a sha with several commits since the branch point


    When it is merged into a target environment


    Then the target''s branch gains exactly one commit whose message names the source and its sha, and the source''s branch is unchanged'
  code:
  - provisa/api/admin/environments_router.py
  - provisa/core/env_repo.py
  tests:
  - tests/unit/test_env_undo_redo.py
  related_reqs:
  - REQ-1490
  - REQ-1524
  - REQ-1543
  - REQ-1504
- id: REQ-1546
  status: proposed
  target: 2026-Q4
  group: 13. Multi-Tenancy & Organizations
  category: Environments
  priority: SHOULD
  type: behavioral
  description: 'A BRANCH SAYS WHETHER IT HOLDS WORK THE REMOTE DOES NOT, and an operator can send it
    or end it. The mirror after every commit is best effort by design -- REQ-1527 has an unreachable
    remote not fail the edit that reached it -- so a branch CAN hold commits the remote has never seen,
    and until now the only record of that was a line in a log. Three things follow. First, a sync state
    computed from refs alone: local tip, remote-tracking tip, how far ahead, how far behind, and
    ``diverged`` when both lines hold commits the other does not. It never dials the git host, so
    rendering the badge is not an act against another organization''s host with that organization''s
    credential, and ``behind`` is therefore as of the last fetch. Second, an explicit push per
    environment, which is the REPAIR for a mirror that did not land and the reason "is my work safe" is
    a state somebody can act on. A push that returns writes the tracking ref from what was sent. Third,
    deleting the remote branch is a SEPARATE ASK from deleting the local one and never implied by it:
    the remote is where the work survives a lost volume, so an org that retires an environment keeps
    the remote copy unless it says otherwise, and the remote deletion happens only after the local
    retirement succeeded. The push inside the commit path runs off the event loop, because a slow git
    host must not hold the request that made the edit.'
  use_case: An operator sees that a branch is three commits ahead of the remote and pushes it before shutting the environment down.
  scenario: 'Given an environment whose commits did not reach the remote


    When the sync state is read


    Then it reports the branch ahead, and a push makes it report in sync'
  code:
  - provisa/core/env_repo.py
  - provisa/core/env_ci.py
  - provisa/api/admin/environments_router.py
  tests:
  - tests/unit/test_env_sync.py
  related_reqs:
  - REQ-1527
  - REQ-1541
  - REQ-1542
- id: REQ-1547
  status: proposed
  target: 2026-Q4
  group: 13. Multi-Tenancy & Organizations
  category: Environments
  priority: SHOULD
  type: behavioral
  description: 'UPDATING LOCAL FROM THE REMOTE IS AN APPLY, not a ref write. A fetch brings the
    remote''s branches in as tracking refs and moves nothing (REQ-1541), because a branch under
    ``refs/heads`` is written by the write-through and by nothing else -- a fetch that landed there
    would let another system reposition an environment behind the control plane''s back, with the
    environment''s model still holding what it held before. So a pull FETCHES and then APPLIES the
    fetched sha through the one path of REQ-1496: same transaction, same validation, same audit, the
    same delta-scoped refresh (REQ-1544), and the branch moves because the write-through commits the
    model the apply produced. Nothing is applied when the environment is already at the remote''s
    commit. A DIVERGENCE IS REFUSED rather than resolved: both lines then hold commits the other does
    not, and choosing whose work survives is a decision a person makes with both histories in front of
    them, never one an endpoint makes quietly.'
  use_case: A branch reviewed and merged on the org's git host is brought back and becomes the environment's model.
  scenario: 'Given an environment whose remote branch holds commits it does not


    When it is pulled


    Then the fetched tree is applied through the ordinary path and the environment reports in sync'
  code:
  - provisa/api/admin/environments_router.py
  - provisa/core/env_repo.py
  tests:
  - tests/unit/test_env_sync.py
  related_reqs:
  - REQ-1541
  - REQ-1496
  - REQ-1544
  - REQ-1546
- id: REQ-1548
  status: proposed
  target: 2026-Q4
  group: 13. Multi-Tenancy & Organizations
  category: Environments
  priority: SHOULD
  type: behavioral
  description: 'THERE IS ONE VERB AND IT IS "APPLY A SHA". A control plane instance holds one model at
    a time per environment; everything a person can do to that model is choosing which commit it
    equals. Deploy, undo, redo, pull and the landing half of a merge are therefore not five operations
    but one operation with five ways of CHOOSING the sha -- named by ref, the parent of where we are,
    a step back toward where we left, what the remote holds, what another environment holds. Selecting
    an environment is the same act with the sha chosen for you: the head of the branch backing it.
    This is a rule about the INTERFACE as much as the code. "Deploy" is not a verb a person should have
    to learn in order to edit a model, because continuous commit (REQ-1524) means their edits are
    already committed; what they choose between is versions. So the UI offers versions -- this commit,
    the remote''s, another environment''s -- and never a separate deploy action, and the API keeps one
    apply path because a second way to write a model is a second way for an environment to hold a tree
    no commit describes.'
  use_case: A person switches environments, steps back through two versions and takes the remote's, without ever meeting a concept called deploy.
  scenario: 'Given a person editing a model


    When they choose a different version by any route


    Then the same apply path runs and the environment ends at the sha they chose'
  code:
  - provisa/api/admin/environments_router.py
  - provisa/core/env_deploy.py
  tests:
  - tests/unit/test_env_undo_redo.py
  related_reqs:
  - REQ-1496
  - REQ-1524
  - REQ-1543
  - REQ-1547
- id: REQ-1549
  status: proposed
  target: 2026-Q4
  group: 13. Multi-Tenancy & Organizations
  category: Environments
  priority: SHOULD
  type: behavioral
  description: 'A MERGE GOES BACK WHERE THE BRANCH CAME FROM, squashed from where the source now IS.
    The environment a branch was created from is recorded (``branched_from``), so the target of its
    merge is known without anybody choosing it, and that environment is the default the interface
    offers; naming a different one is possible and is an explicit act. What lands is one commit
    (REQ-1545), and the state it holds is the source''s CURRENT POSITION rather than its branch tip --
    which matters exactly when the source has stepped back (REQ-1543), because an environment that
    undid its last two commits means to merge what it is showing, not what it abandoned. This needs no
    special case: the merge copies the model the source HOLDS, and after an undo the model it holds is
    the tree at the undo point. Two options ride the merge and both are off by default. ``retire_source``
    ends the source once its work has landed -- its schemas, its row, its branch. ``retire_remote``
    also deletes the branch on the org''s git host, and is separate because the remote copy is what
    survives a lost volume (REQ-1546): ending an environment here never implies ending it there. Both
    ride the approval row when the target is protected, because an approver approves a specific
    operation and "merge this", "merge this and end the branch" and "merge this and end it everywhere"
    are three of them.'
  use_case: A finished feature environment merges back into the environment it branched from and is removed locally and on the git host in the same act.
  scenario: 'Given a feature environment branched from staging and sitting at an undo point


    When it is merged back with the source and its remote branch retired


    Then staging gains one commit holding what the source was showing, and neither the local nor the remote branch remains'
  code:
  - provisa/api/admin/environments_router.py
  - provisa/core/env_approvals.py
  - provisa/core/schema_admin.py
  tests:
  - tests/unit/test_env_merge_request_endpoints.py
  related_reqs:
  - REQ-1542
  - REQ-1545
  - REQ-1546
  - REQ-1543
- id: REQ-1550
  status: proposed
  target: 2026-Q4
  group: 13. Multi-Tenancy & Organizations
  category: Environments
  priority: SHOULD
  type: behavioral
  description: 'A MERGE IS CONFIRMED IN A DIALOG THAT REQUIRES A COMMENT. The comment is not
    bookkeeping: the merge lands as one squashed commit (REQ-1545), so the range of work it stands
    for is unreadable from the history afterwards, and this sentence is the only account of what the
    range was. The generated provenance -- source environment and the sha it was at -- cannot say it,
    because it records where the work came from and not what the work does; the commit subject is
    therefore the operator''s sentence with the provenance in parentheses after it. An empty comment
    is refused (``environments.message_required``) rather than defaulted, on both the direct merge and
    the proposal, and when the target is protected the comment travels on the request row so an
    approver reads what the requester wrote. Beneath the comment the dialog offers deleting the source
    branch, and only when THAT is checked does deleting the remote branch appear as an option under
    it: the remote copy is what survives a lost volume (REQ-1546), so an interface never offers
    deleting it while leaving the environment standing, and the API refuses that combination
    (``environments.remote_without_local``).'
  use_case: A merge dialog makes the operator say what they are merging, and nests the two destructive options so the more final one cannot be chosen alone.
  scenario: 'Given a merge with a blank comment


    When it is submitted


    Then it is refused with environments.message_required, and no rows and no commit are written'
  code:
  - provisa/api/admin/environments_router.py
  tests:
  - tests/unit/test_env_merge_request_endpoints.py
  related_reqs:
  - REQ-1545
  - REQ-1549
  - REQ-1504
- id: REQ-1551
  status: proposed
  target: 2026-Q4
  group: 13. Multi-Tenancy & Organizations
  category: Environments
  priority: SHOULD
  type: behavioral
  description: 'WHEN THE TARGET BRANCH IS GOVERNED BY PULL REQUESTS, REVIEW IS ASKED FOR ON THE HOST.
    An approval row inside Provisa (REQ-1504) decides nothing in that case: the host will refuse the
    push a local merge produces, and the people who would approve it are already reviewing there. So
    the environment offers "request review", and underneath it is a pull request -- the branch is
    pushed first in the same call (a host cannot review refs it never received), then the request is
    opened head=this environment, base=the branch it came from (REQ-1549) unless another is named,
    titled by the pair and bodied by the required comment (REQ-1550). What Provisa keeps is the link,
    which is what anyone asking "where is this being reviewed" needs. GitHub and GitLab are spoken;
    any other host, and a bare-path mirror, is told plainly that Provisa cannot open the request there
    rather than having one guessed at (REQ-1537). Re-asking for a review of a branch already under
    review returns the OPEN request for the same pair instead of an error, because the person asking
    has lost the link, not created a second review.'
  use_case: A feature environment whose target branch is protected on GitHub asks for review from Provisa and gets back the pull request URL.
  scenario: 'Given an org whose remote is a GitHub repository and an environment branched from staging


    When review is requested with a comment


    Then the branch is pushed, a pull request from that environment into staging is opened, and its URL is returned and audited'
  code:
  - provisa/core/env_remote.py
  - provisa/api/admin/environments_router.py
  tests:
  - tests/unit/test_env_review_request.py
  related_reqs:
  - REQ-1504
  - REQ-1546
  - REQ-1549
  - REQ-1537
- id: REQ-1552
  status: proposed
  target: 2026-Q4
  group: 13. Multi-Tenancy & Organizations
  category: Environments
  priority: SHOULD
  type: behavioral
  description: 'THE STATE OF THE BRANCH BEING WORKED IN IS SHOWN WHERE THE WORK HAPPENS, NOT ONLY ON
    THE ADMIN PAGE. Somebody editing the model is not on the administration page, so the environment
    switcher that is on every screen carries what the admin page carries: how far the environment
    stands from the remote (REQ-1546), and the way back through its own history (REQ-1543). Two
    consequences follow. The sync read is a MEMBER read, not an org_admin one -- a member who owns an
    environment may push it, and the person who made the change cannot be the one person who cannot
    see that a push is owed. And WHETHER THERE IS A REMOTE AT ALL travels with the counts, because an
    org that has never configured one otherwise reads exactly like an org in sync; it is said plainly,
    once, rather than drawn as an empty badge. WHERE each thing is said follows from what it is
    about: how far a branch stands from the remote is about the branch, so it rides the switcher on
    every screen, and stepping that branch''s history is offered there too, beside the name of the
    environment the change was made in and nowhere else. Whether the org mirrors anywhere at all is
    about the ORG, so it is said once, above the tabs of the environments page, and the prose behind
    it folds away -- a working surface is not a place to re-read an explanation. The remote URL
    itself does not travel with any of it: it carries secret references and stays behind the
    org_admin read (REQ-1525).'
  use_case: A member changes the model in a branch environment and sees, without leaving the screen, that the change is theirs alone and has reached no remote.
  scenario: 'Given a member editing the model in an environment whose org has no git remote


    When the environment switcher is read


    Then it says the model is mirrored nowhere, and offers the undo for that environment beside its name'
  code:
  - provisa/api/admin/environments_router.py
  - provisa-ui/src/components/EnvSwitcher.tsx
  - provisa-ui/src/components/admin/EnvironmentsTab.tsx
  tests:
  - provisa-ui/src/__tests__/EnvSwitcher.test.tsx
  - provisa-ui/src/__tests__/EnvironmentsTab.test.tsx
  - tests/unit/test_env_sync.py
  related_reqs:
  - REQ-1543
  - REQ-1546
  - REQ-1525
  - REQ-1528
- id: REQ-1553
  status: proposed
  target: 2026-Q4
  group: 13. Multi-Tenancy & Organizations
  category: Environments
  priority: SHOULD
  type: behavioral
  description: 'NEITHER END OF THE HISTORY IS OFFERED INTO A REFUSAL. Undo and redo are refused at the
    ends of the line -- at the first commit there is nothing behind, and where the environment has not
    stepped back from anything there is nothing ahead (REQ-1543) -- and a control that is offered
    whatever the cursor holds tells the person only by being pressed. So the environment answers with
    both directions: the cursor it stands at is in the control plane and the line it stands on is in
    git, so neither is derivable by a client, and the read that lists environments carries whether
    each way is open. The refusals stay: the answer is a claim about the moment it was read, and the
    server, not the disabled button, is what makes an impossible step impossible.'
  use_case: An environment sitting at the first commit of its history offers no undo, instead of offering one that fails.
  scenario: 'Given an environment deployed once, standing at the first commit of its branch


    When its environments are listed


    Then the environment reports that it cannot step back, and the switcher withholds the undo'
  code:
  - provisa/api/admin/environments_router.py
  - provisa-ui/src/components/EnvSwitcher.tsx
  tests:
  - tests/unit/test_env_undo_redo.py
  - provisa-ui/src/__tests__/EnvSwitcher.test.tsx
  related_reqs:
  - REQ-1543
  - REQ-1545
  - REQ-1552
- id: REQ-1556
  status: proposed
  target: 2026-Q4
  group: 13. Multi-Tenancy & Organizations
  category: Environments
  priority: MUST
  type: behavioral
  description: 'A PULL SAYS WHAT IT OVERWROTE, AND A REFUSED PULL SAYS WHAT COLLIDED. Taking
    what the remote holds is the same act as a merge with the same silent case in it, so it
    carries the same report (REQ-1555), measured from the commit the local branch and the fetched
    sha last shared. TWO DOORS. When the two lines have DIVERGED the pull is already refused, and
    the refusal names only that fact -- which leaves the person who must now decide with no
    account of WHICH objects both sides touched; the refusal therefore carries the conflicts, so
    the answer to "so what collided?" is in the response rather than in a comparison somebody has
    to run by hand. When the pull FAST-FORWARDS it is not refused and nothing about it looks
    dangerous, yet the apply diffs the incoming tree against the environment''s CURRENT
    PROJECTION rather than against the commit that branch stands at -- so an edit sitting in the
    schema which never reached a commit is carried away and reported as an ordinary change. That
    is the DRIFTED environment (REQ-1524, a write-through commit that did not land), and it is
    the case with no divergence recorded anywhere to warn about it. Both are computed at the PATH
    and inside the transaction that applies, and both are REPORTED, NEVER RESOLVED: the pull that
    applies still applies, and Provisa offers no way to choose per object. The report reaches the
    response, the audit record, and the environments screen.'
  use_case: A person pulling the remote's version of an environment can see which objects it overwrote that had been changed locally, and a person refused a pull for divergence can see which objects the two lines both touched.
  scenario: 'Given an environment holds a change that never reached a commit and its remote branch has moved ahead


    When the environment is pulled


    Then the pull applies and names the objects whose local change it carried away'
  code:
  - provisa/core/env_conflicts.py
  - provisa/core/env_deploy.py
  - provisa/api/admin/environments_router.py
  - provisa-ui/src/components/admin/EnvironmentsTab.tsx
  depends_on:
  - REQ-1524
  - REQ-1547
  - REQ-1555
- id: REQ-1555
  status: proposed
  target: 2026-Q4
  group: 13. Multi-Tenancy & Organizations
  category: Environments
  priority: MUST
  type: behavioral
  description: 'A MERGE THAT OVERWRITES SOMEBODY ELSE''S WORK SAYS SO. Provisa merges by
    identity: every object the source has, the target gets. That is right for the object the
    source changed and silently wrong for the object somebody changed IN THE TARGET -- the merge
    carries it away and reports it as an ordinary change, so the one case worth stopping to look
    at is the one case nothing distinguishes. Two models differing is not information about who
    differed. The third input is the commit both lines last held (the merge base, which exists
    because a branch is seeded at its source''s tip -- REQ-1543): an object that differs from
    that base on BOTH sides was edited twice, independently, and one of those edits is about to
    disappear. Every such object is named in the merge report -- the preview, the response, the
    stored approval and the audit record -- by path, with what each side did to it (changed,
    removed or added), because a path is the unit the model is versioned in and a count is not
    something a person can act on. An object both sides changed into the same thing is
    agreement, not a conflict. REPORTED, NEVER RESOLVED: the merge still applies and the source
    still wins, which is what a merge into a target is; Provisa offers no conflict resolution --
    no markers, no per-object choosing, no three-way editor -- because the value is the signal,
    not a merge tool. A conflict is a SMELL: it says two people were working on the same object
    without knowing it, which is a fact about how the org is working rather than a defect in
    this merge. Asked of a MERGE only; a checkout or a creation (REPLACE) is the operator
    stating which model the environment runs, so the target''s divergence is the point of the
    act. When the two lines share no ancestor at all the report says the question could not be
    asked rather than reporting a clean merge, because an empty list under no base means nothing
    was compared. NO MERGE LIBRARY IS USED, and none is needed. dulwich (already a dependency)
    ships three_way_merge, and node-diff3 does the same on the client; both compare TEXT LINE BY
    LINE, so two edits to different lines of one object''s YAML merge cleanly and report nothing --
    which is exactly the overwrite being detected, because Provisa''s applier does not merge text
    at all: it takes the source''s rows whole. The comparison therefore runs at the PATH, matching
    the granularity of what actually gets overwritten. A line-level merger would also produce a
    merged tree nobody asked for, which is the resolution UI this requirement rules out.'
  use_case: A person approving a merge can see which objects the branch is about to overwrite that somebody else changed in the target, and the org learns that two people were editing the same object.
  scenario: 'Given a branch and the environment it was created from have each changed the same table since they parted


    When the merge into that environment is previewed or applied


    Then the report names that table as a conflict, saying what each side did to it, and the merge still carries the branch''s version'
  code:
  - provisa/core/env_conflicts.py
  - provisa/core/env_copy.py
  - provisa/core/env_repo.py
  - provisa-ui/src/components/admin/EnvironmentsTab.tsx
  depends_on:
  - REQ-1490
  - REQ-1504
  - REQ-1526
  - REQ-1543
- id: REQ-1554
  status: proposed
  target: 2026-Q4
  group: 13. Multi-Tenancy & Organizations
  category: Environments
  priority: MUST
  type: behavioral
  description: 'AN ENVIRONMENT IS STATE; A BRANCH IS THAT STATE''S HISTORY. The two are paired one to
    one and are still not the same object, and every surface says so in those terms. An environment is
    the control plane''s state filtered by environment name -- what is live, and where bindings, secrets
    and grants sit; a branch is the line of committed versions of exactly one environment''s model. A
    branch is never shared between environments and is never reassigned to another; work crosses from
    one environment to another by MERGE into the target''s own branch (REQ-1490), which is why the
    excluded classes survive it. Deploying is the one control plane ingesting one sha, and choosing the
    environment is the whole of that act: no branch is named, no remote ref is chosen and no second
    dialog is opened, because there is exactly one branch the choice could have meant. THE PAIRING IS
    PROVISA''S RULE, NOT THE REPOSITORY''S. The repository on disk is ordinary git (REQ-1502), so a
    branch with no environment, or a branch moved under one, can exist and is not corruption: Provisa''s
    own surfaces refuse to create one and report what they find rather than inventing a pairing for it
    or reading it as damage. What protects the model from either remains REQ-1496 -- a tree is not a
    model until a load validates it and applies it whole. (Amended 2026-08-22, THE DEPLOYMENT CONTROL
    PROCESS IS NOT PROVISA''S:) There are many control planes and one remote, so a person editing the
    model in their own control plane is editing a working copy and can wreck it without reaching
    anybody. What may reach a branch is decided by the org''s git host -- pull request requirements,
    reviews, checks -- which Provisa neither performs nor polices, and Provisa adds no second
    deployment-control mechanism of its own beyond the approval REQ-1504 already holds. The seam is
    an INVOCATION: `provisa env deploy --org --env --ref --api --token` tells one named control plane
    to ingest one sha, and it is the pipeline that watched the git host''s rules pass which runs it.
    The exit code is that pipeline''s whole contract: a deploy that became a proposal against a
    protected environment exits non-zero, because a release that has not happened must not be
    reported as one.'
  use_case: A reader of any environments surface can say what is live, what its history is, and what a deploy did, without the three collapsing into one word.
  scenario: 'Given an org whose repository holds a branch created with ordinary git and named after no environment


    When the org''s environments are read


    Then the branch is reported as one the org holds and is not presented as an environment, and no environment is invented for it'
  code:
  - provisa/core/env_repo.py
  - provisa/core/env_store.py
  - provisa/api/admin/environments_router.py
  - provisa/cli.py
  - provisa-ui/src/components/EnvSwitcher.tsx
  tests:
  - tests/unit/test_env_sync.py
  - tests/unit/test_cli_env_deploy.py
  related_reqs:
  - REQ-1487
  - REQ-1488
  - REQ-1490
  - REQ-1496
  - REQ-1502
  - REQ-1548
- id: REQ-1557
  status: proposed
  target: 2026-Q4
  group: 1. Access Governance & Security
  category: Security
  priority: MUST
  type: structural
  description: 'A SECRETS SERVICE, WITH PROVISA''S OWN STORE AS THE ONE THAT IS ALWAYS THERE.
    ${provider:reference} (REQ-125, REQ-557) already names a provider, and a registry already
    exists to hold more than one -- but only ``env`` was ever registered, and ``register_provider``
    has no callers, so in practice a secret can only be an environment variable of the server
    process. THAT MAKES A SECRET SOMETHING ONLY THE OPERATOR OF THE HOST CAN SET: a hosted org
    cannot put its own git token, warehouse password or API key anywhere, because it does not own
    the process environment. The provider set therefore becomes a registry in the shape the
    encryption providers already use (provisa/encryption/registry.py): a named spec with a label,
    a description, the config fields it needs and an availability probe, so a deployment wired to
    HashiCorp Vault, AWS Secrets Manager, GCP Secret Manager or Azure Key Vault reads its secrets
    from there. WHEN NOTHING CENTRAL IS CONNECTED PROVISA IS ITS OWN SECRETS SERVICE: a built-in
    per-org store in the control plane, each value encrypted through the configured encryption
    service (REQ-684, REQ-685) so the plaintext never rests in a column, and never reachable
    across orgs. It is the default, not the degraded case. AN UNKNOWN PROVIDER, AN UNSET NAME AND
    AN UNCONFIGURED BACKEND ALL RAISE -- a secret that cannot be resolved is never an empty string
    and never falls through to another provider. WHAT AUTHORIZES A READ. The built-in store needs
    no key of its own: the row IS envelope ciphertext, so the authority to read it is the
    encryption master key (REQ-684) the process already holds, and a control-plane database copied
    without that key yields nothing. An EXTERNAL provider does need a credential -- a Vault token,
    an AWS role -- and that credential is PROCESS CONFIGURATION resolved through ${env:...} alone,
    never an org secret: a store whose own credential lives inside the store cannot be opened, so
    the chain of trust terminates in the host environment by design. Every read and write is
    scoped to ONE org: a name is looked up only in the namespace of the org the request is bound
    to, so two orgs holding GIT_TOKEN hold two unrelated secrets and neither can name the other''s.'
  use_case: An org running on hosted Provisa can store its own git token or warehouse password without owning the server's environment, and an enterprise with Vault or AWS Secrets Manager reads the same references out of the service it already runs.
  scenario: 'Given a deployment with no external secrets backend configured


    When an org stores a secret and a remote referencing it is pushed


    Then the value is resolved out of Provisa''s own encrypted store and the plaintext appears in no column'
  code:
  - provisa/core/secrets.py
  - provisa/core/secrets_registry.py
  - provisa/core/secrets_store.py
  - provisa/core/schema_admin.py
  depends_on:
  - REQ-125
  - REQ-557
  - REQ-684
  - REQ-685
- id: REQ-1558
  status: proposed
  target: 2026-Q4
  group: 1. Access Governance & Security
  category: Security
  priority: MUST
  type: behavioral
  description: 'ADMIN / SECRETS: NAMES GO IN, VALUES NEVER COME BACK OUT. An org admin creates a
    secret by giving it a name and a value, and from then on the screen, the API and the audit
    record speak only in names -- the list says what exists, who last set it and when, and the
    reference to paste (${secret:NAME}); no endpoint returns a stored value, to anyone, ever.
    THERE IS NO "SHOW" BUTTON, because a value that can be read back through the API is a
    credential the browser has already leaked; a person who has lost a secret replaces it rather
    than reading it. A secret may be REPLACED (same name, new value) and DELETED, and deleting one
    that a stored reference still names is allowed and reported -- the reference is text, its
    resolution is checked when it is used, and Provisa does not hold a secret hostage to a config
    that mentions it. Secrets are the ORG''S, managed by org_admin: a platform admin operates the
    control plane and has no read of any org''s secret values (REQ-1361). The page also names
    which secrets provider the deployment is using, so an org can tell Provisa''s own store from a
    central service it is expected to file the secret in instead. WHERE IT LIVES: Admin / Security
    / Secrets, a sub-tab beside posture, encryption, authentication and local users, reached at
    /admin/secrets. Sharing the section does not mean sharing its right: the other four sub-tabs
    describe the DEPLOYMENT and answer to platform_settings, Secrets is the ORG''S and answers to
    org_settings, and each person is shown only the sub-tabs their capability carries -- a
    platform admin sees the four and no secrets, an org admin sees Secrets and none of the four.
    A deep link to a sub-tab the person may not open lands on one they may. THE PAGE ALSO LISTS
    EVERY SECRETS PROVIDER THE BUILD KNOWS AND LETS THE DEPLOYMENT CHANGE WHICH ONE IS IN USE.
    Each registered backend is shown with its label and description, the one this deployment is
    wired to is marked as selected, and a backend whose client library is not installed is shown
    GREYED OUT, unselectable, and annotated "(requires <library> import)" rather than hidden.
    Hiding it would leave an operator guessing whether Provisa supports their secrets manager at
    all; showing it unavailable answers both questions at once -- it is supported, and the one
    thing missing is a pip install. Selecting a backend writes secrets.provider and its config
    block to provisa.yaml and rebinds the running process, and the backend''s own credential is
    typed as a ${env:...} reference, never a literal, because a credential that opens the secrets
    store cannot come out of it (REQ-1557). Selection is fail-closed: an unavailable or
    unregistered backend is refused with the reason, never silently replaced by another.
    The service panel is COLLAPSIBLE and starts COLLAPSED -- choosing the backend is a once-a-
    deployment act while reading the names is the daily one, so the daily thing is what the page
    opens on -- and its expanded/collapsed state is remembered per browser and restored on the
    next visit. WHO MAY DO WHICH: the secrets SERVICE is the deployment''s and answers to platform_settings;
    the secret NAMES are the org''s and answer to org_settings. Both live on this one sub-tab and
    each person sees only their half -- a platform admin chooses the provider and never sees an
    org''s names, an org admin sees the names and reads which service holds them without being
    able to change it. SECRETS OWNS NO NAV ENTRY OF ITS OWN: it is reached through Admin /
    Security, whose rail entry already stands for the section, and /admin/secrets stays a working
    deep link. A second entry for one sub-tab of a section the rail already lists is a duplicate
    that tells a reader the two are different places.'
  use_case: An org admin can add the git token an environment remote references, rotate it when it expires, and see at a glance which secrets exist without any screen or response ever carrying a secret value.
  scenario: 'Given an org admin has stored a secret named GIT_TOKEN


    When the secrets list is read


    Then it names GIT_TOKEN with who set it and when, and carries no value for it'
  code:
  - provisa/api/admin/secrets_router.py
  - provisa/core/secrets_store.py
  - provisa-ui/src/components/admin/SecretsTab.tsx
  - provisa-ui/src/api/secrets.ts
  - provisa-ui/src/components/admin/SecurityManager.tsx
  depends_on:
  - REQ-1361
  - REQ-1557
- id: REQ-1559
  status: proposed
  target: 2026-Q4
  group: 10. UI & Admin Surfaces
  category: UI
  priority: SHOULD
  type: behavioral
  description: 'ADMIN NAVIGATES DOWN A LEFT-HAND RAIL, NOT ACROSS A TOP BAR. Every other section
    of the product carries a handful of surfaces and reads comfortably as a horizontal sub-bar;
    Admin carries two dozen, which that bar can only crowd, truncate or wrap. So on an admin route
    the section''s entries render as a VERTICAL RAIL down the left edge of the page, the page
    content taking the remaining width, and the horizontal sub-bar is not drawn for that section
    at all -- one navigation, in one place, never both. The rail is the same entry list, gated on
    the same rights and filtered by the same commercial/installed rules as the bar it replaces, so
    a person sees exactly the surfaces they may reach and no link leads to a permission error. The
    rail scrolls on its own when it is taller than the viewport, so the page content is never
    pushed off-screen by the length of the menu, and the entry for the surface being viewed is
    marked current. Being visually different is the point as much as the fit: Admin is where the
    deployment is changed, and it should not look like the page a query is written on.'
  use_case: An administrator sees all two dozen admin surfaces at once down the left edge instead of a crowded top bar, and knows from the layout alone that they are in the part of the product that changes the deployment.
  scenario: 'Given an administrator opens an admin surface


    When the page renders


    Then the admin entries stand in a vertical rail on the left and the horizontal sub-bar is absent'
  code:
  - provisa-ui/src/components/AdminRail.tsx
  - provisa-ui/src/components/NavBar.tsx
  - provisa-ui/src/components/navGroups.ts
  - provisa-ui/src/App.tsx
  - provisa-ui/src/App.css
  depends_on:
  - REQ-1351
- id: REQ-1560
  status: proposed
  target: 2026-Q4
  group: 1. Access Governance & Security
  category: Security
  priority: MUST
  type: behavioral
  description: 'A SECRET BELONGS TO SOMEONE, AND WHO IT BELONGS TO IS PART OF ITS ADDRESS. A
    developer needs credentials of their own -- their git token, their warehouse login -- and
    holding them in the one shared org store means any other developer can write
    ${secret:GIT_TOKEN} into a config and push as them. Provisa refuses that by making the OWNER
    part of the reference rather than a permission checked around it. THERE ARE TWO SECRET
    SCOPES, and the reference grammar names which one it means: ${secret:NAME} is an ORG secret,
    one value that every member of the org resolves to the same thing; ${user:NAME} is a PERSONAL
    secret, resolved against WHOEVER IS ACTING, so the same reference in the same config yields
    each person their own credential and yields nothing at all for someone who has not stored one.
    Impersonation is impossible not because a check forbids it but because there is no way to
    write down "someone else''s secret" -- the personal namespace has no addressable owner.
    ONE SECRETS SERVICE, TWO VAULTS. The scopes are not two backends: the deployment is wired to
    exactly one secrets service (REQ-1557) and both vaults live inside it, the org vault addressed
    by the org and each personal vault by its owner -- in Provisa''s own store the owner is a
    column of the key, in a central service it is a distinct path under the same mount. Choosing
    the service stays one deployment-wide decision; nobody configures a second one to hold their
    own credentials.
    THERE IS NO PRECEDENCE AND NO SHADOWING between the two: ${secret:X} never resolves to a
    personal secret and ${user:X} never resolves to an org one, so the same name may exist in both
    scopes and neither silently answers for the other. An unset personal reference is a fail-closed
    error naming the scope, never a fall-through to the org value.
    WHO MAY WRITE WHICH. A personal secret is the person''s own: any member of the acting org may
    create, replace and delete their own, and NOBODY -- not another member, not the org admin, not
    the platform admin, not cross_org -- may list, replace or delete another person''s, because
    the API takes the owner from the authenticated identity and never from the request. An ORG
    secret is the org''s: creating, replacing and deleting one is org_settings, held by org_admin,
    and it is the org_admin''s act of storing it in the org scope that makes a credential shared.
    A platform admin holds neither over any org -- cross_org confers org LIFECYCLE, not a read of
    the names an org keeps, and the platform-bypass wildcard does not satisfy either gate
    (REQ-1361, REQ-1558).
    TWO SURFACES, NAMED FOR WHOSE THEY ARE. Admin carries "Org Secrets" (org_settings) and "Your
    Secrets" (any member), each listing only its own scope, each showing the reference to paste in
    the grammar of that scope, and each obeying names-go-in-values-never-come-back-out exactly as
    REQ-1558 requires. Separate surfaces rather than one filtered list, because whose a secret is
    decides who can break what by rotating it, and a person should never have to read a column to
    find that out. This supersedes REQ-1558''s single Secrets sub-tab under Security: the org
    scope keeps /admin/secrets, the personal scope is /admin/my-secrets, and both take a nav entry
    of their own -- Security answers to platform_settings, so a multitenant org admin could not
    reach a secrets tab nested under it.
    Every write is audited by name, scope and actor; the audit record of a personal secret names
    its owner as the actor and no one else can read it.'
  use_case: A developer stores their own git token as ${user:GIT_TOKEN} and uses it in a source config, and no other developer in the org can resolve, replace or read it.
  scenario: 'Given two developers each store a personal secret named GIT_TOKEN


    When each resolves ${user:GIT_TOKEN} in the same org


    Then each gets their own value, and neither can list or delete the other''s'
  code:
  - provisa/core/secrets_store.py
  - provisa/core/schema_admin.py
  - provisa/core/secrets.py
  - provisa/api/admin/secrets_router.py
  - provisa-ui/src/components/admin/SecretsTab.tsx
  - provisa-ui/src/components/navGroups.ts
  depends_on:
  - REQ-1557
  - REQ-1558
- id: REQ-1561
  status: proposed
  target: 2026-Q4
  group: 13. Multi-Tenancy & Organizations
  category: Environments
  priority: MUST
  type: behavioral
  description: 'A NODE WHOSE PROJECTION DOES NOT HOLD THE COMMIT STILL LISTS ITS ENVIRONMENTS. The
    repository is a projection and never an authority (REQ-1524), so the control plane naming a
    deployed_sha this node''s object store never received is a state the design admits -- it is
    what DRIFTED means. Reading history from such a store is therefore a BROWSE refusal like any
    other unresolvable ref: parent_of raises RepositoryError rather than letting a dulwich KeyError
    escape, undo and redo answer 404 through the existing browse contract, and the environments
    list reports the history as closed at both ends instead of failing the whole read. The
    environment, its name and its deployed_sha are unaffected: what a projection without the object
    has lost is the ability to walk back from it, and that is the only answer that changes.'
  use_case: Two processes share one control plane but keep separate repository stores; listing environments on the process that did not write the commit must not 500.
  scenario: 'Given an environment whose deployed_sha names a commit this node''s repository does not hold


    When a member lists the org''s environments


    Then the environment is listed with can_undo and can_redo both false, and no request fails'
  code:
  - provisa/core/env_repo.py
  - provisa/api/admin/environments_router.py
  depends_on:
  - REQ-1524
  - REQ-1543
  - REQ-1553
- id: REQ-1562
  status: proposed
  target: 2026-Q4
  group: 11. Platform, Infrastructure & Delivery
  category: Deployment
  priority: MUST
  type: behavioral
  description: 'A HOSTED DEPLOY ASSERTS ITS COMMERCE CONFIGURATION BEFORE IT SHIPS ANYTHING. The
    plan catalog is the first thing the signup page asks for (REQ-1514), and a Lemon Squeezy
    variant id has no default -- variant_id_for_plan raises rather than inventing one (REQ-1455) --
    so a node missing those ids answers GET /billing/catalog with a 500 and nobody can create an
    organization. The deploy therefore preflights the variant, store and signing settings from the
    RUNNING container, exactly as it preflights the engine pin, and refuses to deploy rather than
    leaving a node whose sign-up is broken.'
  use_case: A regenerated provisa.env drops the Lemon Squeezy variant ids; the next deploy stops instead of shipping a node where "Get started" shows Internal server error.
  scenario: 'Given the running API container has no LEMONSQUEEZY_VARIANT_STARTER


    When the operator runs the cloud deploy


    Then it names the missing settings and exits non-zero before pushing any code'
  code:
  - scripts/deploy-cloud.sh
  depends_on:
  - REQ-1455
  - REQ-1514
- id: REQ-1563
  status: proposed
  target: 2026-Q4
  group: 11. Platform, Infrastructure & Delivery
  category: Deployment
  priority: MUST
  type: behavioral
  description: 'A HOSTED DEPLOY ALSO ASSERTS THAT THE COMMERCIAL SEAM ACTUALLY LOADED. The settings
    REQ-1562 preflights are only half of what signup needs; the other half is the plugin that reads
    them. provisa.core.commerce imports provisa_commercial inside a try and every hook degrades to a
    no-op when that import fails (REQ-1473) -- correct for a self-hosted install, wrong for a hosted
    node, where a silent seam mounts no /billing routes at all and the signup page''s GET
    /billing/catalog has no route to answer it. The seam is silent by design, so the deploy is what
    speaks: after the restart it reads commerce.enabled() from the running container and fails the
    deploy when it is False. It is asserted AFTER the restart because the plugin push is what lands
    the tree and the import resolves once per process, and only when the checkout carries the plugin
    -- a checkout without it is a deliberate no-billing deploy, the same rule the plugin push holds.'
  use_case: A stack recreate replaces the containers and drops the hot-pushed plugin; the next deploy stops rather than leaving a hosted node whose "Get started" cannot reach a plan catalog.
  scenario: 'Given the running API container has no provisa_commercial tree


    When the operator runs the cloud deploy from a checkout that carries the plugin


    Then the post-restart verify reports the seam is off and exits non-zero'
  code:
  - scripts/deploy-cloud.sh
  - provisa/core/commerce.py
  depends_on:
  - REQ-1473
  - REQ-1514
  - REQ-1562
- id: REQ-1564
  status: proposed
  target: 2026-Q4
  group: 11. Platform, Infrastructure & Delivery
  category: Deployment
  priority: MUST
  type: behavioral
  description: 'THE BILLING KEY IS ASSERTED FOR THE STORE THE DEPLOYMENT ACTUALLY TRANSACTS AGAINST,
    AND A BLANK KEY IS A MISSING KEY. LEMONSQUEEZY_MODE selects which credential signs every Lemon
    Squeezy call (REQ-1455): test mode reads TEST_LEMONSQUEEZY_API_KEY and leaves LEMONSQUEEZY_API_KEY
    inert. Two consequences follow. First, the reader raises a named error when the selected key is
    empty rather than signing with it -- docker compose forwards every declared variable, so an unset
    key reaches the process as an empty string that os.environ hands back happily, and the request
    then carries "Authorization: Bearer " for the transport to reject; the operator must see the
    setting they never wrote, not a protocol error from inside httpx. Second, the deploy preflight
    asserts the mode-selected key rather than a fixed list, because a node holding the live key with
    the test key blank satisfies a fixed list and still cannot price a single plan.'
  use_case: A hosted node runs in test mode with only the live key configured; the deploy names the
    missing sandbox key instead of shipping a signup page whose plan catalog 500s.
  scenario: 'Given LEMONSQUEEZY_MODE is test and TEST_LEMONSQUEEZY_API_KEY is empty


    When the operator runs the cloud deploy


    Then the commerce preflight names TEST_LEMONSQUEEZY_API_KEY and exits non-zero'
  code:
  - scripts/deploy-cloud.sh
  depends_on:
  - REQ-1455
  - REQ-1514
  - REQ-1562
- id: REQ-1565
  status: proposed
  target: 2026-Q4
  group: 11. Platform, Infrastructure & Delivery
  category: Pricing & Tiering
  priority: MUST
  type: behavioral
  description: 'THE TRIAL RESET THE REFUSAL PROMISES IS AN ENDPOINT, NOT A HAND-WRITTEN UPDATE.
    REQ-1474 refuses a second free trial and tells the person refused to write in and have it reset
    -- copy that commits the platform to an action it had no way to perform, leaving support to
    edit trial_grants by hand on a production control plane. A named operation replaces that:
    DELETE /billing/trial/grants names the person by email address and releases their grant. It is
    gated by cross_org (REQ-1337), the right that lets the control plane act in an org it is not a
    member of, because the person being reset belongs to somebody else''s org. The whole grant is
    released, not the one key that was named: a grant is recorded under BOTH identities REQ-1474
    tracks -- the normalized address and the Lemon Squeezy customer id -- so deleting only the
    address leaves the merchant-side key to refuse the same person the moment they return to
    checkout. Releasing a grant is logged with the operator who released it and the org it was
    spent on. It reports how many keys it released, and releasing a grant that is not held is a
    404 rather than a silent success, so support can tell "reset" from "there was nothing there".'
  use_case: A prospect refused a trial they never knowingly used writes to the address the refusal
    names, and support clears them in one call instead of opening a psql session on the control plane.
  scenario: 'Given a trial grant recorded for ada@example.com under both its email and customer-id keys


    When a platform operator calls DELETE /billing/trial/grants for that address


    Then both keys are released, the release is logged, and Ada''s next Starter checkout is offered the trial'
  code:
  - provisa_commercial/router.py
  - provisa_commercial/trial_eligibility.py
  depends_on:
  - REQ-1337
  - REQ-1455
  - REQ-1474
- id: REQ-1566
  status: proposed
  target: 2026-Q4
  group: 11. Platform, Infrastructure & Delivery
  category: Pricing & Tiering
  priority: MUST
  type: behavioral
  description: 'STARTER IS ORDERABLE WITHOUT A TRIAL. Signup routes Starter through the trial
    checkout because Starter is the plan the trial converts to, so the REQ-1474 refusal -- one free
    evaluation per person -- ended the road: someone who had already spent their grant could not
    order Starter at all, which is the plan they were trying to PAY for. The refusal copy already
    told them otherwise, offering exactly this. So the trial is a property of the checkout, not a
    condition of it: when the buyer is still owed an evaluation the Starter checkout carries the
    variant''s free-trial period, and when they are not it is opened with the trial skipped and
    bills from the first invoice. The plan, its limits, and its variant are identical either way --
    only the trial period differs -- and the response says which was opened so the client can name
    it before the buyer leaves for the checkout. Asking for a TRIAL explicitly, at /trial/start, is
    still refused with the REQ-1474 message; it is the paid road that must never be closed. Two
    conditions remain hard refusals on both roads, because neither is about the trial: an org that
    already started one holds a subscription, and a request carrying no signed-in address identifies
    no person to be held to "once". The signup page is told the same fact BEFORE the buyer chooses:
    the plan catalog reports whether the signed-in account is still owed an evaluation, and when it
    is not the page stops advertising a free trial on Starter -- offering fourteen free days to
    someone the checkout will bill on day one is the version of this the buyer discovers with their
    card already entered.'
  use_case: A prospect whose grant was spent inside an org they were invited to picks Starter at
    signup and reaches a checkout that bills immediately, instead of an error they cannot get past.
  scenario: 'Given the signed-in account already holds a spent trial grant


    When they order the Starter plan from signup


    Then a Starter checkout opens with the trial skipped and the response reports trial false'
  code:
  - provisa_commercial/router.py
  - provisa_commercial/lemonsqueezy_client.py
  - provisa-ui/src/pages/OnboardOrgPage.tsx
  depends_on:
  - REQ-1455
  - REQ-1474
  - REQ-1514
  - REQ-1565
- id: REQ-1567
  status: complete
  since: 2026-08
  target: 2026-Q3
  group: 13. Multi-Tenancy & Organizations
  category: Multi-Tenancy & Orgs
  priority: MUST
  type: behavioral
  description: 'AN AUTO-JOIN RULE IS AN EXCLUSIVE CLAIM ON A SET OF ADDRESSES, AND ITS BREADTH IS
    ACKNOWLEDGED BY THE ORG THAT WRITES IT. REQ-1477 refuses a rule admitting a consumer domain,
    which is the extreme case; between that and a safe rule sits the ordinary one that is merely too
    loose. A rule is a regex, so an unanchored fragment matches any address CONTAINING it --
    "acme\.com" admits notacme.com.au and acme.com.attacker.net, sweeping in people the org never
    meant to admit and granting them its default role and its data. The reverse is equally real and
    must not be forbidden: a company genuinely receives mail at subdivision domains that carry the
    parent inside them (eu.acme.com, acme.com.au for the same firm), so a rule that reaches beyond
    one exact domain is legitimate and cannot simply be banned. So breadth is QUANTIFIED and
    CONSENTED TO rather than guessed at: (1) rules whose match set is unbounded in the ways nothing
    legitimate needs -- unanchored at the domain end, matching the empty local part, or admitting an
    arbitrary suffix after the domain -- are refused outright, because there is no org whose
    membership those describe; (2) any remaining rule that reaches past a single exact domain is
    shown back to the author with what it would admit, warning that a loose filter admits people
    from outside the organization and asking them to review it; and (3) the policy is only saved
    once the author explicitly accepts that risk, so admitting a stranger is a decision somebody
    made rather than a regex nobody read. TWO ORGS MAY NOT HOLD THE SAME AUTO-JOIN CRITERIA. An
    identical rule on two orgs makes the destination of a new sign-up arbitrary, so the second org
    to claim it is refused and told which org holds it -- the collision is settled between the two
    orgs, not resolved silently at the moment a person signs in.'
  use_case: An admin who types @acme\.com without anchoring it is shown that it would also admit
    notacme.com.au and has to accept that before the rule can admit anybody.
  scenario: 'Given an org admin turning auto-join on with an unanchored domain fragment


    When they save the policy without acknowledging its breadth


    Then the policy is refused, naming what the rule would admit beyond the domain they meant


    And a second org claiming a rule another org already holds is refused, naming that org'
  code:
  - provisa/api/admin/orgs_router.py
  - provisa/core/auto_join_rules.py
  - provisa/core/org_membership.py
  - provisa-ui/src/pages/OnboardOrgPage.tsx
  tests:
  - tests/unit/test_auto_join_rules.py
  - tests/unit/test_org_join_policy.py
  - provisa-ui/src/__tests__/OnboardOrgAutoJoinRisk.test.tsx
  depends_on:
  - REQ-1268
  - REQ-1269
  - REQ-1477
- id: REQ-1568
  status: complete
  since: 2026-08
  target: 2026-Q3
  group: 13. Multi-Tenancy & Organizations
  category: Multi-Tenancy & Orgs
  priority: MUST
  type: behavioral
  description: 'AN ADDRESS MATCHING MORE THAN ONE AUTO-JOIN ORG IS A CHOICE PUT TO THE PERSON, NOT A
    SWEEP. resolve_auto_join_orgs joins every matching org at once, so someone whose address is
    claimed by two rules silently becomes a member of both -- including one they have never heard
    of, whose rule happens to contain their domain (REQ-1567). Even with exclusive criteria this
    remains reachable, because a subdivision address can legitimately satisfy a parent org rule and
    a division org rule at the same time. So a single match auto-joins as it does today, and MORE
    THAN ONE match stops and asks: the person is told their address matches several organizations,
    shown which, and picks the one to join -- or declines them all and creates an organization of
    their own, which is the right answer for someone who merely shares a domain suffix with a
    stranger. Declining an org records the REQ-1306 opt-out for it, so the same question is not
    asked again at every sign-in. Nothing is joined until they answer: membership granted while the
    question is open is the outcome this exists to prevent.'
  use_case: A person at eu.acme.com whose address matches both the parent org and their division is
    asked which to join instead of being made a member of both.
  scenario: 'Given a sign-in whose address matches two auto-join orgs


    When the auto-join resolver runs


    Then neither membership is granted and the person is asked which organization to join


    And declining both leaves them able to create their own org'
  code:
  - provisa/api/auto_join.py
  - provisa/api/auth_router.py
  - provisa/auth/middleware.py
  - provisa/core/org_membership.py
  - provisa-ui/src/api/admin.ts
  - provisa-ui/src/pages/OnboardOrgPage.tsx
  tests:
  - tests/integration/test_org_auto_join.py
  - provisa-ui/src/__tests__/OnboardOrgAutoJoinChoice.test.tsx
  depends_on:
  - REQ-1269
  - REQ-1306
  - REQ-1567
- id: REQ-1569
  status: complete
  since: 2026-08
  target: 2026-Q3
  group: 13. Multi-Tenancy & Organizations
  category: Multi-Tenancy & Orgs
  priority: MUST
  type: behavioral
  description: 'AN INVITATION IS A MESSAGE AN ORG_ADMIN CAN ACTUALLY SEND, AND THE JOIN RULE IS
    READABLE AND EDITABLE AFTER THE ORG EXISTS. Provisa composes a branded invitation and delivers it
    (REQ-1310, REQ-1330, REQ-1486), but the team page offered no address field, so every invitation
    was a link the org_admin had to carry by hand and the branded message was never sent -- branding
    that promises delivery and a product that cannot deliver. The invite form takes an email address
    (empty still means a shareable link), the creation response says what became of the message, and
    a failed or unsupported send is reported next to the link the org_admin must now pass on
    themselves rather than logged where nobody sees it. The invitation list names the address each
    invitation went to. Likewise the auto-join rule, written once at onboarding, decides forever who
    walks in unasked: it is read back and edited on the same page, through the same breadth
    acknowledgement the onboarding form uses (REQ-1567), so a company that changes domains or
    discovers its rule admits strangers can correct it.'
  use_case: An org_admin types a colleague's address, the branded invitation is sent, and the same
    page shows the auto-join rule the org is running so it can be narrowed.
  scenario: 'Given an org_admin on the team page

    When they address an invitation to a colleague

    Then the invitation is delivered and any failure is reported with the link to pass on by hand

    And the org auto-join rule is shown and can be edited'
  code:
  - provisa/api/admin/invites_router.py
  - provisa/api/admin/orgs_router.py
  - provisa-ui/src/api/admin.ts
  - provisa-ui/src/components/OrgJoinSettings.tsx
  - provisa-ui/src/pages/TeamPage.tsx
  tests:
  - tests/e2e/test_platform_auth_flow.py
  - tests/integration/test_invite_delivery.py
  - provisa-ui/src/__tests__/TeamPageInviteEmail.test.tsx
  depends_on:
  - REQ-1268
  - REQ-1269
  - REQ-1310
  - REQ-1567
- id: REQ-1570
  status: complete
  since: 2026-08
  target: 2026-Q3
  group: 10. UI & Admin Surfaces
  category: UI & Admin Surfaces
  priority: SHOULD
  type: behavioral
  description: 'THE GUIDED TOUR IS NOT OFFERED OVER A SIGN-IN OR REGISTRATION FORM. An invitation
    link opened while a session is already live renders the sign-in and registration form inside the
    authenticated shell, and the once-per-session tour welcome modal appeared on top of it -- the
    person came to accept an invitation and was shown a tour of a product they have not joined. The
    offer is withheld on the identity routes and, because it is never claimed there, it survives to
    the first page that has a product to show.'
  use_case: Someone following an invitation link lands on the registration form, not on a tour
    invitation covering it.
  scenario: 'Given a browser with a live session

    When an invitation link opens the registration page

    Then the tour welcome modal is not shown

    And the tour is still offered on the first product page they reach'
  code:
  - provisa-ui/src/App.tsx
  - provisa-ui/src/tour/TourAutoStart.tsx
  tests:
  - provisa-ui/src/__tests__/tourWelcome.test.tsx
  depends_on:
  - REQ-1287
- id: REQ-1571
  status: complete
  since: 2026-08
  target: 2026-Q3
  group: 10. UI & Admin Surfaces
  category: UI & Admin Surfaces
  priority: MUST
  type: behavioral
  description: 'SIGN-IN AND REGISTRATION RENDER WITHOUT THE APP SHELL, CENTERED. /login and /register
    are identity routes: they establish who someone is, and nothing on them belongs to a session
    that has not been established yet. Opened with a live session -- the common case for an
    invitation link -- they used to render inside the authenticated shell, putting the full
    navigation, the org, environment and role pickers and the admin rail above a left-aligned form.
    Both paths now render the standalone branded landing card, centered on the page, whether or not
    a token is present.'
  use_case: Someone following an invitation link sees a centered sign-in card, not the product
    navigation of an org they have not joined.
  scenario: 'Given a browser with a live session

    When an invitation link opens the registration page

    Then no navigation bar, org or environment picker, or admin rail is rendered

    And the sign-in card is centered on the page'
  code:
  - provisa-ui/src/App.tsx
  tests:
  - provisa-ui/src/__tests__/IdentityRoutes.test.tsx
  depends_on:
  - REQ-1266
  - REQ-1287
- id: REQ-1572
  status: complete
  since: 2026-08
  target: 2026-Q3
  group: 13. Multi-Tenancy & Organizations
  category: Multi-Tenancy & Organizations
  priority: MUST
  type: behavioral
  description: 'AN INVITATION IS ITSELF THE ADMISSION DECISION, AND THE ORG EMAIL RULE DOES NOT GATE
    REDEEMING ONE. The email rule (REQ-1268) decides who may join an org on their own initiative --
    it is the guard on auto-join and on self-service. An invitation is different in kind: an org
    admin named a person, it is single-use, and it expires. Applying the rule to redemption refused
    exactly the people an admin deliberately reached outside their own domain -- a contractor, an
    auditor, someone whose IdP account carries a different address than their work one -- with an
    error the invitee could do nothing about, and it did so only under bearer identity providers:
    /register on the basic provider never applied the rule to an invite. Redemption now admits any
    valid, unused, unexpired invitation regardless of the rule, under both providers.'
  use_case: An org whose rule admits only its own domain invites an outside contractor, and the
    contractor's invitation link works.
  scenario: 'Given an org whose email rule admits only @acme.com

    And a valid invitation issued by an admin of that org

    When someone whose address is bob@rival.com redeems it

    Then they are joined to the org with the invited role

    And the rule still governs who may auto-join without an invitation'
  code:
  - provisa/api/auth_router.py
  tests:
  - tests/e2e/test_platform_auth_flow.py
  depends_on:
  - REQ-516
  - REQ-1268
- id: REQ-1573
  status: complete
  target: 2026-Q3
  group: 13. Multi-Tenancy & Organizations
  category: Environments
  priority: MUST
  type: behavioral
  description: 'ENVIRONMENTS ARE GOVERNED BY TWO RIGHTS, AND NEITHER IS AN ANALYST''S BY DEFAULT.
    ``environment_management`` is the right to CREATE and DELETE environments; ``environment_switch``
    is the right to BE SERVED BY one other than prod -- to send x-provisa-env and have it honoured.
    They are separate because they answer different questions: the first is who may spend the org''s
    plan ceiling and drop a schema, the second is who may work anywhere but production. The seed
    grants BOTH to org_admin and to developer, and NEITHER to analyst or modeler: an analyst reads
    production, and an environment they cannot create is not one they should be able to switch into.
    WHICH environment a holder may delete is unchanged (REQ-1488 keeps deletion an org_admin act);
    the right decides who may make one at all and who may reach the environments surface.
    This AMENDS REQ-1528, which left creation open to any member on the argument that the authority
    is useless without bindings. That argument holds against an attacker and not against an
    accident: every environment is a schema and a place in the plan ceiling, and a member who has no
    reason to build has no reason to make one. Creation stays REQ-1528''s only path to model-editing
    authority; it is now a path a role has to carry the right to walk. ENFORCEMENT IS AT THE
    SELECTION, not only in the UI: an unheld ``environment_switch`` makes x-provisa-env naming a
    non-prod environment a refusal for every surface at once -- HTTP, GraphQL, SQL, the wire
    protocols -- because the environment is bound in the org-routing middleware, before a route is
    reached. prod needs no right, since it is what a request naming nothing is served by. The
    platform bypass and dev/no-auth mode are exempt exactly as they are for every other capability
    gate, and an environment''s OWNER still holds their REQ-1528 authority inside it -- ownership
    decides what may be done to a model, these two rights decide who may make an environment and be
    served by one.'
  use_case: An org_admin grants a contractor the analyst role; the contractor can query production
    and neither sees the environment switcher nor can create or delete an environment.
  scenario: 'Given a member holding only the analyst role

    When they send x-provisa-env naming a non-prod environment

    Then the request is refused for want of environment_switch

    And POST /admin/orgs/{org}/environments is refused for want of environment_management

    And a member holding developer may do both'
  since: 2026-08
  code:
  - provisa/core/db.py
  - provisa/core/schema.sql
  - provisa/security/rights.py
  - provisa/api/app.py
  - provisa/api/env_routing.py
  - provisa/api/admin/capabilities.py
  - provisa/api/admin/environments_router.py
  - provisa-ui/src/components/EnvSwitcher.tsx
  - provisa-ui/src/components/navGroups.ts
  - provisa-ui/src/App.tsx
  tests:
  - tests/unit/test_env_runtime_routing.py
  - tests/unit/test_environment_rights_seed.py
  - tests/unit/test_env_member_rights.py
  - provisa-ui/src/__tests__/EnvSwitcher.test.tsx
  - provisa-ui/src/__tests__/AdminRail.test.tsx
  depends_on:
  - REQ-1297
  - REQ-1487
  - REQ-1488
  - REQ-1528
