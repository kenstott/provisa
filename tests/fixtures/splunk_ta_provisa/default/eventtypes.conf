# The Authentication model's root constraint requires tag=authentication. A tag attaches to an
# eventtype, so the eventtype is what selects the fixture's events out of the index.
[provisa_authentication]
search = index=provisa_cim sourcetype=provisa:auth
