# The fixture posts JSON event bodies over HEC already using CIM field names, so search-time JSON
# extraction is the entire field-mapping step. A real source whose field names differ would add
# FIELDALIAS-* stanzas here — that is the only difference.
[provisa:auth]
KV_MODE = json
SHOULD_LINEMERGE = false
