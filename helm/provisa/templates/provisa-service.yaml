apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-provisa
spec:
  selector:
    app: provisa
  ports:
    - port: {{ .Values.provisa.port }}
      targetPort: {{ .Values.provisa.port }}
      protocol: TCP
  type: ClusterIP
